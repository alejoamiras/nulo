name: Quality

on:
  workflow_dispatch:
  pull_request:
    branches: [main, dev]
    # `ready_for_review` so a draft marked ready WITHOUT a new push still re-runs
    # CI — otherwise the required `… / Status` checks never report on the PR and
    # hang "Expected" forever → mergeStateStatus=BLOCKED.
    #
    # NO `labeled`/`unlabeled`: nothing in this workflow reads labels, so a label
    # event can only produce a duplicate run of work already done for this SHA.
    # If one lands mid-run the concurrency group cancels the in-flight run, whose
    # `quality-status` job still concludes FAILURE (`always()` + the
    # cancelled-is-failure branch in the aggregate step) and leaves an earlier,
    # losing check-run behind; if it lands after, it re-runs the whole workflow
    # for nothing. The label-gated suites keep both types because their gates
    # genuinely read labels.
    #
    # Gap this leaves: a PR RETARGETED to dev/main fires `edited`, which is not
    # listed here, so `quality-status` can sit at "Expected" forever. `edited`
    # cannot be filtered at the trigger level, and on a title edit the aggregator
    # would run over skipped needs and post a green check that tested nothing —
    # so recover with `gh workflow run pr-quick.yml --ref <branch>` instead.
    types: [opened, reopened, synchronize, ready_for_review]

concurrency:
  group: pr-quick-${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      core-foundation: ${{ steps.override.outputs.full || steps.filter.outputs.core-foundation }}
      aztec-runtime: ${{ steps.override.outputs.full || steps.filter.outputs.aztec-runtime }}
      wallet-bridge: ${{ steps.override.outputs.full || steps.filter.outputs.wallet-bridge }}
      extension: ${{ steps.override.outputs.full || steps.filter.outputs.extension }}
      playground: ${{ steps.override.outputs.full || steps.filter.outputs.playground }}
      landing: ${{ steps.override.outputs.full || steps.filter.outputs.landing }}
      faucet: ${{ steps.override.outputs.full || steps.filter.outputs.faucet }}
      workflows: ${{ steps.override.outputs.full || steps.filter.outputs.workflows }}
      root-config: ${{ steps.override.outputs.full || steps.filter.outputs.root-config }}
      firefox-touching: ${{ steps.override.outputs.full || steps.filter.outputs.firefox-touching }}
      needs-extension-build: ${{ steps.compute.outputs.needs-extension-build }}
      needs-firefox-build: ${{ steps.compute.outputs.needs-firefox-build }}
      needs-faucet-build: ${{ steps.compute.outputs.needs-faucet-build }}
    steps:
      - uses: actions/checkout@v7
        with:
          # Full history: paths-filter needs the merge-base with the comparison
          # branch. On a shallow clone it falls back to a `fetch --deepen` loop,
          # which hits git's "shallow file has changed since we read it" race
          # whenever the merge-base sits beyond the initial depth (structural on
          # long-running branches ~100+ commits from the base).
          fetch-depth: 0
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          ref: ${{ github.event.pull_request.head.sha || github.ref }}
          filters: |
            # Graph-derived (see CI.md "CI gating"): dependency libraries by consumed
            # src + manifest; built targets by whole package. NO `!` negations (the dorny
            # `some`-quantifier footgun). Pinned by scripts/ci-cd/behavior-gating.test.ts.
            core-foundation:
              - 'packages/wallet-core/src/**'
              - 'packages/wallet-core/package.json'
              - 'packages/wallet-crypto/src/**'
              - 'packages/wallet-crypto/package.json'
              - 'packages/extension-messaging/src/**'
              - 'packages/extension-messaging/package.json'
              - 'packages/wallet-sdk-schema-patch/src/**'
              - 'packages/wallet-sdk-schema-patch/package.json'
            aztec-runtime:
              - 'packages/aztec-runtime/src/**'
              - 'packages/aztec-runtime/package.json'
            wallet-bridge:
              - 'packages/wallet-bridge/src/**'
              - 'packages/wallet-bridge/package.json'
            extension:
              # Built target — whole package; design + bridge-core dep libs — src + manifest.
              - 'apps/extension/**'
              - 'packages/design/src/**'
              - 'packages/design/package.json'
              - 'packages/bridge-core/src/**'
              - 'packages/bridge-core/package.json'
            playground:
              - 'apps/playground/**'
            landing:
              # Dead filter — no job consumes it; kept positive-only for the no-`!` invariant.
              - 'apps/landing/**'
            faucet:
              # Built target — faucet (whole package) + its transitive dep libs
              # (bridge-core → wallet-crypto → wallet-core, + design) + the faucet build
              # workflow. Closes the prior under-gate (was faucet + design only).
              - 'apps/faucet/**'
              - 'packages/bridge-core/src/**'
              - 'packages/bridge-core/package.json'
              - 'packages/wallet-crypto/src/**'
              - 'packages/wallet-crypto/package.json'
              - 'packages/wallet-core/src/**'
              - 'packages/wallet-core/package.json'
              - 'packages/wallet-sdk-schema-patch/src/**'
              - 'packages/wallet-sdk-schema-patch/package.json'
              - 'packages/design/src/**'
              - 'packages/design/package.json'
              - '.github/workflows/_build-faucet.yml'
            workflows:
              - '.github/workflows/**'
              - '.github/actions/**'
            root-config:
              - 'package.json'
              - 'bun.lock'
              - 'bunfig.toml'
              - 'biome.json'
              - 'tsconfig.json'
              - '.commitlintrc.json'
              - '.githooks/**'
              - 'patches/**'
            # Files that can change Firefox build divergence (not runtime behaviour).
            # Auditor (codex) corrected this to include shared build inputs in addition
            # to the firefox-only configs; see implementations-plan/ci-cd/build-caching-plan.md §1.1.
            firefox-touching:
              - 'apps/extension/manifest/manifest.firefox.config.ts'
              - 'apps/extension/vite.firefox.config.mts'
              - 'apps/extension/manifest/manifest.config.ts'
              - 'apps/extension/vite.config.ts'
              - 'apps/extension/package.json'
              - 'bun.lock'
              - 'bunfig.toml'
              - '.github/workflows/_build-extension.yml'
              - '.github/actions/setup-bun/**'
      - name: Override for manual dispatch
        if: github.event_name == 'workflow_dispatch'
        id: override
        run: echo "full=true" >> "$GITHUB_OUTPUT"
      - name: Compute downstream flags
        id: compute
        env:
          CORE: ${{ steps.override.outputs.full || steps.filter.outputs.core-foundation }}
          RUNTIME: ${{ steps.override.outputs.full || steps.filter.outputs.aztec-runtime }}
          BRIDGE: ${{ steps.override.outputs.full || steps.filter.outputs.wallet-bridge }}
          EXTENSION: ${{ steps.override.outputs.full || steps.filter.outputs.extension }}
          FAUCET: ${{ steps.override.outputs.full || steps.filter.outputs.faucet }}
          WORKFLOWS: ${{ steps.override.outputs.full || steps.filter.outputs.workflows }}
          ROOT: ${{ steps.override.outputs.full || steps.filter.outputs.root-config }}
          FIREFOX_TOUCHING: ${{ steps.override.outputs.full || steps.filter.outputs.firefox-touching }}
          BASE_REF: ${{ github.base_ref }}
          EVENT: ${{ github.event_name }}
        run: |
          if [ "$CORE" = "true" ] || [ "$RUNTIME" = "true" ] || [ "$BRIDGE" = "true" ] \
             || [ "$EXTENSION" = "true" ] || [ "$WORKFLOWS" = "true" ] || [ "$ROOT" = "true" ]; then
            echo "needs-extension-build=true" >> "$GITHUB_OUTPUT"
          else
            echo "needs-extension-build=false" >> "$GITHUB_OUTPUT"
          fi
          # Firefox build: only when something actually affects build divergence,
          # or on main-targeted PRs (full-sanity gate), or on workflow_dispatch.
          if [ "$EVENT" = "workflow_dispatch" ] \
             || [ "$BASE_REF" = "main" ] \
             || [ "$FIREFOX_TOUCHING" = "true" ]; then
            echo "needs-firefox-build=true" >> "$GITHUB_OUTPUT"
          else
            echo "needs-firefox-build=false" >> "$GITHUB_OUTPUT"
          fi
          # Faucet build runs on faucet-touching PRs + any workflow / root
          # config change that could affect its bundle.
          if [ "$FAUCET" = "true" ] || [ "$WORKFLOWS" = "true" ] || [ "$ROOT" = "true" ]; then
            echo "needs-faucet-build=true" >> "$GITHUB_OUTPUT"
          else
            echo "needs-faucet-build=false" >> "$GITHUB_OUTPUT"
          fi

  commitlint:
    name: Commitlint
    needs: changes
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-1.3.14-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-1.3.14-
            ${{ runner.os }}-bun-
      - run: bun install --frozen-lockfile
      - name: Lint PR commits
        env:
          BASE_REF: ${{ github.base_ref }}
          HEAD_REF: ${{ github.head_ref }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if [ "$BASE_REF" = "main" ]; then
            # Skip on promote / Release PRs to main. The two kinds of main PR:
            #  - the promote PR, whose merge subject is an intentionally
            #    NON-conventional, >100-char "release: promote dev -> main (...)"
            #    release-note line (mandated by CLAUDE.md) — it cannot pass
            #    commitlint by design (no 'release' type; over the header cap);
            #  - the bot Release PR, whose "chore(main): release X.Y.Z" is
            #    reliably conventional anyway.
            # And the dev squash subjects in base..head are already-merged +
            # immutable — re-linting them spuriously failed required Quality on
            # long historical subjects. So commitlint adds no value on main.
            echo "commitlint: skipped on PRs to main (promote/Release subjects are exempt by design)."
          elif [ "${HEAD_REF#sync/main-to-dev}" != "$HEAD_REF" ]; then
            # Skip on the main->dev sync PR (head branch `sync/main-to-dev*`). It
            # is MERGE-committed (not squashed) so main's release commit stays in
            # dev's ancestry — the prerelease version-anchor needs it. That makes
            # base..head carry main's IMMUTABLE, already-merged release/promote
            # history (the same >100-char "release: promote ..." subjects the
            # main skip above exempts) → re-linting them spuriously reds required
            # Quality. The one NEW commit the sync adds (the manifest re-baseline)
            # is bot-generated and reliably conventional. Same rationale, dev side.
            echo "commitlint: skipped on the main->dev sync PR ($HEAD_REF) — carries main's immutable history; the one new commit (manifest re-baseline) is bot-conventional."
          else
            bunx commitlint --from "$BASE_SHA" --to "$HEAD_SHA" --verbose
          fi

  lint-and-typecheck:
    name: Lint + Typecheck
    needs: changes
    uses: ./.github/workflows/_lint-and-typecheck.yml
    with:
      ref: ${{ github.event.pull_request.head.sha || github.ref }}

  unit-tests:
    name: Unit tests
    needs: changes
    uses: ./.github/workflows/_unit-tests.yml
    with:
      ref: ${{ github.event.pull_request.head.sha || github.ref }}

  build-chrome:
    name: Build Chrome
    needs: changes
    if: needs.changes.outputs.needs-extension-build == 'true'
    uses: ./.github/workflows/_build-extension.yml
    with:
      ref: ${{ github.event.pull_request.head.sha || github.ref }}
      target: chrome
      upload_artifacts: true

  build-firefox:
    name: Build Firefox
    needs: changes
    if: needs.changes.outputs.needs-extension-build == 'true' && needs.changes.outputs.needs-firefox-build == 'true'
    uses: ./.github/workflows/_build-extension.yml
    with:
      ref: ${{ github.event.pull_request.head.sha || github.ref }}
      target: firefox
      upload_artifacts: true

  build-faucet:
    name: Build Faucet (${{ matrix.target }})
    needs: changes
    if: needs.changes.outputs.needs-faucet-build == 'true'
    strategy:
      fail-fast: false
      matrix:
        target: [testnet, mainnet]
    uses: ./.github/workflows/_build-faucet.yml
    with:
      ref: ${{ github.event.pull_request.head.sha || github.ref }}
      target: ${{ matrix.target }}
      upload_artifacts: true

  # Smoke e2e moved to its own pr-smoke-e2e.yml workflow:
  # auto-runs on PRs to main, on changes within the `smoke-surface`
  # filter, or via the `e2e:smoke` label. Mirrors pr-network-e2e.yml.

  status:
    name: quality-status
    if: always()
    needs: [changes, commitlint, lint-and-typecheck, unit-tests, build-chrome, build-firefox, build-faucet]
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Aggregate results
        run: |
          for r in \
            "${{ needs.changes.result }}" \
            "${{ needs.commitlint.result }}" \
            "${{ needs.lint-and-typecheck.result }}" \
            "${{ needs.unit-tests.result }}" \
            "${{ needs.build-chrome.result }}" \
            "${{ needs.build-firefox.result }}" \
            "${{ needs.build-faucet.result }}" \
          ; do
            if [ "$r" = "failure" ] || [ "$r" = "cancelled" ]; then
              echo "::error::A required job failed or was cancelled: $r"
              exit 1
            fi
          done
          echo "All required gates passed (or were skipped because their filter did not trip)."
