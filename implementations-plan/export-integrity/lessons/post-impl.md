# Post-implementation lessons — review + codex loop

- **`/code-review max --fix` executed as a fresh-eyes Opus review agent** (the project skill's interactive tour needs a present human; the autonomous run keeps the substance — independent Anthropic-family review, fixes applied by the session, committed separately). First spawn on Fable died on a subagent entitlement error mid-verification ("organization has disabled Claude subscription access" — transient, main session unaffected); the blueprint's documented Opus fallback finished the job. Its dying thread ("length-vs-bytes gate semantics") was independently confirmed and fixed before the retry reported: export gates measured UTF-16 `.length` against a byte cap.
- Review verdict: 0 must-fix, 2 should-fix (encrypt/download cross-latch; too-large picks left the previous selection's CTA live), 4 nits (3 adopted, logger-port nit skipped with reason — pre-existing base-class-wide pattern). It also positively verified the checksum fixed-point, all fences, and pickFile promise hygiene.
- **Codex post-impl: cond-approve → 1 fix round → approve.** Its best catch was second-order: MY review fix (clearing the selection on a too-large pick) opened a stale-decrypt publication race — `{...null}` would resurrect a selection husk and `clearError()` wipe the fresh error. Lesson: every fix that changes shared-state lifecycle needs the same fence discipline as the code it fixes; the re-review of fix diffs is where this class gets caught.
- Fence taxonomy sharpened across the rounds: PUBLICATION fences (don't write stale state — plan-level) vs WORK-ABANDONMENT fences (don't keep deriving secrets for a dead run — post-impl round). Both now hold on every await in the export/encrypt/decrypt paths.
- Consult ledger for the batch, complete: plan R1 reject → R2 cond-approve → final fresh cond-approve (= gate) → cap ratification approve → post-impl cond-approve → re-review approve. Six codex calls, two fable-role reviews (plan + implementation), zero unresolved disagreements.
