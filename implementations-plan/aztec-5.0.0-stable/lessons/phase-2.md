# Phase 2 ✓ — signing-key-root + PXE seam + FPC canonical + compat epoch (2026-07-14)

## Gate result: ✅ all green
`typecheck:all` exit 0 · `test:all` exit 0 (extension 3115 + aztec-runtime 55 + bridge-core 128 + wallet-crypto 33 + the rest) · `lint` exit 0 · all 5 builds exit 0 (chrome, firefox, faucet, playground, landing).

## What shipped

- **The ONE derivation helper** (`packages/wallet-crypto/src/account-derivation.ts`, exported): `deriveSigningKeyFromSeed(seed) = sha512ToGrumpkinScalar([seed, DomainSeparator.IVSK_M])` (D7 — upstream's removed construction verbatim; `IVSK_M = 2747825907`) + `deriveNuloAccountKeys(seed)` → `{signingKey, secretKey}` via upstream `deriveSecretKeyFromSigningKey`. wallet-crypto gained `@aztec/accounts` + `@aztec/constants` deps; aztec-runtime + faucet gained `@nulo/wallet-crypto` (layer-legal).
- **NuloAccount inverted** (`nulo-account.ts`): stores the derived `secretKey` (never the seed, never the signing key), keeps the `SchnorrAccountContract` (kills the second `deriveSigningKey` call at :189), instance derivation now includes 5.0.0's NEW **`immutablesHash`** parameter, `CompleteAddress.fromSecretKeyAndInstance(secretKey, …)`. `ensureRegistered` asserts the PXE-returned `CompleteAddress.address` equals the derived address (final-pass F8).
- **PXE seam**: `updateContract` REMOVED end-to-end (descriptors 22→21, ipxe 18→17 — descriptor-pin tests consciously updated); `registerAccount` derives the full `AccountPrivacyKeys` struct offscreen-side (4 privacy secrets + 2 public keys — the seam wire still carries only `secretKey`); `registerContract` splits class+instance upstream calls, keeps OUR `{instance, artifact?}` shape, and asserts the upstream-returned derived address equals the supplied instance's; `getContractInstance` routes through the new `effective-class.ts` — node-sourced instances REJECT upgrades explicitly (regression pair: immutable passes / upgraded throws), PXE preimages hydrate `current := original` under the documented no-upgrades assumption.
- **Vector regime**: V7a rewritten onto `deriveSigningKeyFromSeed` (fixture value UNCHANGED — construction carried byte-identically, exactly as designed); the full chain (signingKey→secretKey→`AccountPrivacyKeys`→address→CompleteAddress) pinned in `packages/aztec-runtime/src/account/derivation-vectors.test.ts` against the committed regime-B JSON — **NuloAccount reproduces upstream's `getSchnorrAccountContractAddress` oracle addresses exactly**. That test is jsdom-EXCLUDED in the extension runner (bb.js poseidon2 `std::bad_cast` — the V4/V10 limitation) and runs in aztec-runtime's own node-env suite via `test:all`.
- **9 script/fixture sites** migrated to the two-step through the helper (faucet `deploy.ts` restructured `DeployerKeys`; 6 bridge-core scripts; the e2e fixture — **`as any` on `createSchnorrAccount` removed**, arg order now compiler-checked). `deploy-sandbox.ts` untouched (uses upstream `getInitialTestAccountsData`, already 5.0.0-shaped).
- **PrivateFPC canonical re-root**: pin `0x0d4b2c28…` (rc-era operator salt 0) → **`0x257aa870…efc86e9` at canonical salt `0x…01`** across bridge-core (`PRIVATE_FPC_ADDRESS` + new `PRIVATE_FPC_SALT`), the extension's discovery (`fpc/service.ts` — was salt 0, the false-address bug codex 3 caught), the e2e fixture, and the deploy script. New committed descriptor `private-fpc-canonical.json` (address+salt+deployer+aztecVersion+**artifactSha256** `6c0cd8bc…`) machine-asserted by the tripwire test (derivation from the installed artifact at the canonical salt == the canonical address — passed empirically, cross-check #1) . **`check-fpc-version.ts` hardened**: exact full-version (no prerelease-stripping — the old major-only compare was false-green across rc↔stable), artifact digest, descriptor coherence, live `node_getContract` class check with RPC-error≠absence. **Run live: GREEN** (node 5.0.0, digest match, address clean-absent on rollup 1821665230 — deploy owed in Phase 5).
- **Backup compat-epoch 2→3** (codex 9): epoch 2 (rc-era secret-root addresses) is now the designed hard reject; fixtures updated (unit + e2e helpers); the reject test covers 2 AND 4.

## Gotchas

- The extension's vitest re-runs all package tests under jsdom — a node-env-only test needs an explicit exclude there even when its own package suite is node-env (the `test:all` filter runs both).
- The `hydratePreimage` spread broke a `toBe` identity assertion in the cascade test — updated to assert the hydrated shape; identity now only holds for node/known-bundle branches.
- `useFullBackupImport.test.ts` builds checksummed backups with a default epoch — 34 tests fail en masse when the epoch bumps; the fixture default is the single knob.
- biome 2.5.2 (in-range refresh) reformatted a multi-line throw — run `biome format --write` on touched files before committing.

LESSONS_FILE=implementations-plan/aztec-5.0.0-stable/lessons/phase-2.md
