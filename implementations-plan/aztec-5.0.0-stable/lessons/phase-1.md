# Phase 1 ✓ — reference vectors + mechanical bump + break inventory (2026-07-14)

## Gate result: ✅ all green

- **D13 reference vectors committed** (`reference/`):
  - **Regime A** (rc.2-installed upstream `deriveSigningKey` = the D7 construction's reference): 2 seed→signingKey vectors; seed `0x…42` reproduces V7a's fixture `0x14a31cb4…3cbc` exactly (chain closed); separator `DomainSeparator.IVSK_M = 2747825907`; the script itself asserts `deriveSigningKey ≡ sha512ToGrumpkinScalar([seed, IVSK_M])`.
  - **Regime B** (`reference/regime-b/` — self-contained project pinned to the PUBLISHED 5.0.0; tarball digests committed): full chain per seed — signingKey (5.0.0 primitives REPRODUCE the rc.2 values: construction unchanged across versions, asserted in-script) → `deriveSecretKeyFromSigningKey` → `deriveKeys` (all six master secrets) → the complete `AccountPrivacyKeys` wire value (4 privacy secrets + message-signing/fallback PUBLIC keys) → address via UPSTREAM's own `getSchnorrAccountContractAddress` oracle → CompleteAddress via upstream's exact instance sequence (in-script asserts: instance.address == oracle address == completeAddress.address).
  - **5.0.0 API discovery recorded**: instance derivation now takes an **`immutablesHash`** (`accountContract.getImmutablesHash()`) — the Phase-2 inversion must include it; `AccountPrivacyKeys` lives in `@aztec/key-store` (type re-exported through `@aztec/pxe`); `registerAccount(keys, partialAddress): Promise<CompleteAddress>` (return value enables the F8 equality assert).
- **Pins**: 66 `@aztec/*` + 10 `@alejoamiras/*` → `5.0.0` across the 8 package.json; `@aztec/viem@2.38.2` untouched (verified post-sweep). Patches re-keyed (`@5.0.0` files + `patchedDependencies`) and **verified applied** (both installed packages carry the patch's exports map).
- **Lockfile**: `rm bun.lock && bun install` (hoisted linker held); `rg -c '5\.0\.0-rc\.2' bun.lock` = **0**; `bun install --frozen-lockfile` = no changes (CI equivalence).
- **Machine exception diff** (`scripts/lockfile-exception-diff.ts`; full JSON: `phase-1-lock-diff.json`): 34 Aztec-scope moves (the bump) · **152 changed + 4 added + 23 removed non-Aztec entries — dispositioned below**.
- **Provenance (executable)**: all three `@alejoamiras` lines carry npm registry signatures (keyid `SHA256:DhQ8wR5…`) + SLSA v1 provenance attestations with the EXPECTED subjects — accelerator ← `github.com/alejoamiras/aztec-accelerator@main` commit `507efd9`; standards + fee-payment ← `github.com/alejoamiras/ecosystem-tooling@main` commit `45c0c57`. Tarball shasums: accelerator `0d11b062…`, standards `5ff239df…`, fee-payment `bbac579d…`.
- **Nargo tag→commit SHAs at bump time** (for the Phase-4 pin/assert): `AztecProtocol/aztec-packages v5.0.0` = `c5db195d25e03a01e53e1710bf0dedc741bd0578`; `alejoamiras/ecosystem-tooling v5.0.0` = `45c0c578b7cb5a30001f42f0a3fb0a65d9a0db56` — **which equals the npm attestation commit**: the Noir source tag and the published npm artifacts are the same source revision.
- **bunfig**: comment re-dated (5.0.0 set ages out ~2026-07-21; removal follow-up owed). Exclude-list reconciliation vs the NEW lock: **zero changes needed** — every `@aztec` name in the lock (30) is covered; only `@aztec/viem` is uncovered, deliberately (independent line, normal 7-day gate).

## Lock-diff disposition (the 179 non-Aztec entries)

All 152 changed entries are in-range refreshes of ESTABLISHED families, and — the load-bearing property — **the 7-day min-age gate applied to every one of them during this install** (they are not excluded names), so nothing here is younger than 2026-07-07. Families: `@aws-sdk/*` (backup S3 tooling; incl. the `@aws-crypto/*` → internal-checksums restructure that explains most of the 23 removals), `@biomejs/*` 2.5.1→2.5.2 (lint gate watched in Phase 2), `@commitlint/*` 21.1→21.2 (drags `conventional-changelog-*` majors within ITS ranges + the 4 added helper packages), `@oxc-resolver`/`rolldown`/`vite` 8.1.0→8.1.3/`vitest` 4.1.9→4.1.10 (build/test infra — Phase 2 gates exercise them), `puppeteer` 25.2.1→25.3.0 (e2e — smoke gate exercises), faucet `viem` 2.53.1→2.54.6 + `ox`/`ws` (L1 client — Phase 5 verify-l1/smokes exercise), `bn.js` 4.12.3→4.12.4 patches, misc singles (`fs-extra` 10→11 and `convert-source-map` 1.9→2.0 are parent-range-driven). `react-dom`/`scheduler` REMOVED — dropped out of the storybook dependency graph; nothing imports them directly (build gate proves). No unknown/typosquat-shaped names appeared.

## The REAL break found beyond the changelog: the standards `dist/` removal

`@alejoamiras/aztec-standards@5.0.0` removed its `dist/` mirror (ecosystem-tooling stable-revisit decision, PR #2) — deep imports move `dist/src/…` → `artifacts/src/…` (identical suffix; exports map now `"./artifacts/*"`). Swept mechanically across 22 files (faucet/playground/bridge-core/e2e fixtures). `@alejoamiras/aztec-fee-payment` KEEPS its `dist/` — untouched (its imports resolve fine; verified 0 refs needed changing).

**Nested-worktree resolution gotcha (cost ~20 phantom errors):** before the sweep, the broken `dist/` imports did NOT fail as "module not found" — Node/TS resolution walked UP past the worktree root into the PARENT clone's `node_modules` (still rc.2, still has `dist/`) and produced cross-clone type-identity mismatch errors (`…/nulo-2/node_modules/...ContractArtifact` vs `…/worktrees/aztec-5.0.0-stable/node_modules/...ContractArtifact`). In a `.claude/worktrees/*` worktree, ANY import that vanishes from a package can silently resolve against the parent clone's older copy — recognize the mixed-clone paths in errors as the signature.

## The true Phase-2 checklist (21 errors, 13 unique, zero noise)

1. **`deriveSigningKey` removed** (TS2305) ×9: `packages/aztec-runtime/src/account/nulo-account.ts` (production inversion point), `apps/extension/src/wallet/crypto/key-vectors.test.ts` (V7a — replaced by the D13 pair), `apps/faucet/scripts/deploy.ts`, `packages/bridge-core/scripts/{deploy-bridge-testnet,deploy-private-fpc-testnet,deposit-testnet,fuel-testnet,smoke-existing-testnet,smoke-swap-existing-testnet}.ts`. (+ `apps/extension/tests/e2e/fixtures/aztec.ts` — not in `typecheck:all`'s graph but on the inventory; `packages/bridge-core/scripts/deploy-sandbox.ts` imports `createSchnorrAccount` only — re-check its call shape in Phase 2.)
2. **PXE seam** (`packages/aztec-runtime/src/pxe/service.ts`, 5 distinct): `registerContract` takes `ContractInstancePreimage` (no `{instance, artifact}` wrapper) and returns `AztecAddress` (≠ `void`); `getContractInstance` now returns **`ContractInstancePreimageWithAddress`** (the preimage type — so the `currentContractClassId` readers DO lose the field at the seam boundary; per-site disposition lands when the seam types flip in Phase 2); `updateContract` gone from `PXE`; `registerAccount` takes `AccountPrivacyKeys` (not `Fr`).
3. **`createSchnorrAccount` arity** (`apps/faucet/scripts/deploy.ts` TS2554: expected 3-4, got 2) — the signing key is now REQUIRED (upstream kept the `(secret, salt, signingKey)` order; the migration is the semantic two-step through the D7 helper).

Non-typecheck items carried to Phase 2 from the plan: the FPC canonical salt sweep (`fpc/service.ts:100-104` + bridge-core pins), the hardened `check-fpc-version.ts`, the compat-epoch bump, the registration-hygiene + returned-address pins, the effective-class helper.

LESSONS_FILE=implementations-plan/aztec-5.0.0-stable/lessons/phase-1.md
