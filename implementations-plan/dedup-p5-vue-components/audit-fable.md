# Fable audit — dedup-p5-vue-components (Agent Plan leg, Fable 5.1, 2026-09-07)

**Verdict on Plan A: conditional-approve** (conditions 1–6 below). **A vs B: hybrid.** Take B's skips for N1 and M5's toggle; keep A's N2, N4, N7 and everything both share.

## Concerns, by severity

1. **N1 — the "nine sites, one call each" claim is false for three sites, and the lifecycle story is wrong for six.** `EditContactPopup.vue:41-59` refreshes the edit target inside `onContactUpdated`; `EditFpcPopup.vue:138-152` refreshes `fpcToEdit` on update and closes+toasts on delete; `BalanceView.vue:165-177` captures `wasDisplayed` before the filter and resets `displayOption`. `identity` is filler (all nine key on `.id`). Three sites build the client per show (`NewFpcPopup.vue:104-107`, `EditFpcPopup.vue:162-165`, `SelectFpcPopup.vue:47-50`), so the composable would run inside `onShow`, outside any effect scope; the contact popups disconnect in `onHide` (`NewContactPopup.vue:143-148`); `SelectBalanceTypePopup.vue:116-120`'s unmount hook does not include `tokenBalanceService` (disconnected at :149); `SelectFpcPopup` never disconnects or removes handlers. Add-with-existing-id pushes a duplicate at every site while `useEntityCrud.ts:111-116` dedupes — copying that logic silently changes reconnect re-emit behaviour. Net after narrowing: six sites, ≈ −30 lines plus a ≥10-case suite. **B is right to skip N1.**
2. **M5 toggle — a 4-of-7 adoption.** Byte-equivalent toggles also sit at `NewProfileCredentials.vue:36-40`, `auth.vue:267-271`, `change-password.vue:128,169`; scope rules forbid pulling them in. Keep `import-shared.module.css` (`ImportSecretForm.vue:125-146` ≡ `ImportFullBackupForm.vue:157-171` ≡ `ImportMethodPicker.vue:36-50`; `.section` ≡ `.section_last`), skip the toggle.
3. **M1 inference wrong, and CSS unaddressed.** `getMethodLabel` is imported from `@/utils/tx-enrichment` (`CapabilityDetailPanel.vue:13`; `tx-enrichment.ts:53`), so an L3 child imports it directly (`biome.json:374-389` bans only `@/utils/core`, stores and clients/services under `components/**`). Drop `scope-format.ts`: `formatScope`/`fnLabel` are used only inside the three lists, so they move into `ScopePatternList.vue`; `sanitizeWireString` (line 14) then leaves the panel. `$style.detail_list/.bullet/.mono` are also used by the accounts/contracts/classes/privateEvents sections (lines 46-132, 263-290) — the list needs its own rules or a partial. L3 minimum is ≥10 cases; the plan offered 3.
4. **N4 event wiring.** Both URL inputs clear the error on input (`NewEndpointPopup.vue:110`, `EditEndpointPopup.vue:122`); `EndpointFormFields` must emit that or the parent watches `urlTerm`. `modules/settings/networks/` does not exist yet. Edit has no unit test; `tests/e2e/endpoints.test.ts` hits both submit testids.
5. **N7 — reject path has zero tests.** `IncomingTrustPopup.test.ts:242-297` exercises allow only. The extraction is worth it precisely because reject then shares the tested code; the "+1 latch-sharing case" is mandatory. The label string must be built by the caller at call time (evaluated synchronously before the latch, so B-28 at line 274 holds).
6. **N2 inference wrong (benign).** All 14 rows are inside Transitions (12 elements, two hold two rows: `EditProfilePopup.vue:151-158`, `NewNetworkPopup.vue:189-196`). Same-type branches patch in place today and after; identical.
7. **Smaller items.** K8: verify's block (`verify/index.vue:194-216`) matches `DappIdentityBlock.vue:37-59` exactly; delete the `sanitizeWireString` import (`verify/index.vue:10`) or lint fails; `.dapp_*` has no other use. K3: both ops are `exec: ExecutionPayload` (`operation.ts:156-176`); no unit test renders either branch. L2: the merge is equivalent (both conditions and both fallback `v-else-if`s, 792/838 vs 858/898); `RecentActivityView.test.ts:219` mounts shallow with no `token`, so parity is e2e-only. N3: the move regenerates `components.d.ts`; update the explicit imports at `NewContactPopup.vue:7`, `EditContactPopup.vue:7`. I5: `src/design` is not an auto-import dir; add `build-storybook` to the gate.

## Skips
All correct: N5, M6, N3-sender (`NewSenderPopup.vue:124-150`), K3(b), trimAddress (`string.ts:11-14` returns one string with `..`; the popup renders three nodes with a tertiary `•••`).

## Widened / dropped
Widened: `scope-format.ts`, the `identity` option, a `PasswordVisibilityToggle` that leaves three copies behind. Dropped: nothing.

## Looks fine
L5 (`TxFeeRow.vue:119-137` ≡ `TxDebugPanel.vue:100-118`); N8 table (only invisible text-node whitespace changes); N4 classifier (`?.chainId` is safe under Edit's early return at line 59); `FieldWarning` in `components/ui/` with a 5-case test; `EventHandler.remove` (`event-handler.ts:40`) is connection-independent; no Tier-4 id touched; no new biome-ignore needed.
