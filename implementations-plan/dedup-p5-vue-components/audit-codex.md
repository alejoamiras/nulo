**Plan A: conditional-approve only after conditions 1–7 below are adopted. Confidence: high on source findings; moderate on abstraction tradeoffs.**

**A vs B: hybrid—B wins N1, N4, N7 and M5’s toggle; A wins N2, with corrected transition semantics.**

1. **N1’s four options cannot preserve the nine sites.** [EditContactPopup.vue:41](apps/extension/src/popup/components/popups/EditContactPopup.vue:41) refreshes the edited draft and returns **without updating the collection**. `EditFpcPopup.vue:138` updates `fpcToEdit` even when absent from the collection; deletion at `:145` closes/toasts and returns without filtering. `BalanceView.vue:165` captures the selected token **before deletion**, then resets `displayOption`. All disappear under A. Also, SelectBalanceType’s first-match `splice` is not universally equivalent to filtering every match. **Skip N1; do not add callback options to rescue it.**

   Cleanup recon is false: New/EditContact, ImportContacts and New/EditFpc disconnect on hide; SelectFpc creates clients on show and has no disconnect/unmount hook. SelectBalanceType disconnects balances on hide (`:149`), but prices/config on unmount (`:116`). Only BalanceView (`:245`) and send (`:507`) have the relevant multi-service unmount sequences. A single setup subscription also misses FPC clients recreated on each show.

2. **I5’s proposed factory breaks ordinary CSF indexing.** `Colors.stories.ts:11` and its three peers currently export object-literal metadata. Installed Storybook’s `dist/_node-chunks/chunk-SH6RWRE2.js:262` accepts object expressions and throws “default export must be an object” for an ordinary factory-call initializer. Keep literal metadata/title and share only static fields, or skip I5. Add Storybook indexing/build validation; Chrome build does not cover this.

3. **N7 is insufficiently pinned; B wins.** `IncomingTrustPopup.test.ts:242,259,274` covers double-Allow, a changed contract preventing close, and captured symbol. It does **not** cover generation ownership, Reject outcomes, or cross-action re-entry. If retained, require old-request settlement after reopen/new submission, cross-action latch, explicit-true versus false/undefined, rejection, and unchanged/changed identity cases. Preserve action invocation inside `try`, receiver binding, symbol/key capture before awaiting, generation-conditional unlock, and close after `finally` (`IncomingTrustPopup.vue:104,132,156`). The small deletion does not justify this verification burden. Preserve invariant comments, not obsolete review narration.

4. **N2 is worthwhile, but the transition inventory is wrong.** All fourteen rows are inside twelve Transitions. `NewSenderPopup.vue:114` wraps its warning; `EditProfilePopup.vue:151` wraps **both** conditional branches. `NewNetworkPopup.vue:188` also has a two-branch chain. Keep each `v-if`/`v-else-if` branch separately, its precedence, and a single Flex root; collapsing branches into changing slot text changes transition behavior. Minimum parity: exact row/slot/attrs, hidden→shown→hidden, and warning-A→warning-B with Transition unstubbed. Five arbitrary “shape” assertions are filler.

5. **L2’s boolean merge works, but use `props.token`.** There is no script variable `token` (`RecentActivityView.vue:40`); the plan’s literal computed would fail. Preserve fallback suppression by orphan/journal cards (`:839,895`), empty-token state (`:911`), and token/non-token branch remount behavior—use a key if merging the roots. Test token/account fallback, journal+orphan coexistence, empty states, and token-presence switching.

6. **Resolve M1’s API and registration now.** `getMethodLabel` is already imported (`CapabilityDetailPanel.vue:13`), defined in `utils/tx-enrichment.ts:53`; it has no panel closure. Import it directly and preserve **both arguments**, especially the FeeJuice contract guard. Keep `formatScope`/`fnLabel` inside the extracted component unless another consumer needs them. Minimum parity: wildcard/empty/pattern scopes, address child, raw sanitized function, known/unknown labels, third-party `claim`, all three parent branches. Register real extracted children.

   C1/L3/L4 locations pass current import bans; local L2 additions need reconciliation with CLAUDE.md’s package-ownership convention. EndpointFormFields requires explicit imports: Vite scans `src/components`, not popup modules (`vite.config.ts:156`). Rebuild and commit generated declarations; keep local names out of the design resolver.

7. **Close the scope/test ledger.** M5’s toggle has no implementation phase or tests despite `plan.md:68`; explicitly skip it. Four small buttons do not need this abstraction. Skip N4 too: it adds model/event plumbing; if retained, pin autofocus/sanitize/maxLength/testids, URL-input error clearing, and both copies. Its classifier must preserve Edit’s non-optional chain access (`EditEndpointPopup.vue:70`). No deferred IDs are introduced.

**looks fine**

K8 preserves hostname warning, sanitized name and logo branches. N3’s default-primary/red-icon adaptation matches; sender skip is correct, although its Tooltip **does** have `wide`. N5, M6, K3(b), and N8-trimAddress skips are justified. L5/M5 CSS partials fit P4; verify emitted CSS. N8 preserves section order and wrappers. No files changed; no tests run.