REJECT

1. HIGH — `implementations-plan/profile-name-parity/plan.md:13-14,251-259`; `packages/extension/src/onboarding/pages/import.vue:246-267,381-390`; `packages/extension/src/composables/useFullBackupImport.ts:200-235,833-840`: the plan treats onboarding import as the parity reference, but full-backup import there already ignores the typed name and restores the backup-embedded name. Opus is right, and the problem is broader than popup F2. Suggested fix: either hide/disable the name field for full-backup, or thread a `profileNameOverride` through `useFullBackupImport` and `ProfileService.restore` and fix onboarding + popup together.

2. HIGH — `implementations-plan/profile-name-parity/plan.md:220-228,269-273,314`; `packages/extension/tests/e2e/fixtures/extension.ts:138-167,515-560`; `registration.test.ts:23-45`; `passkey-paths.test.ts:46-58,191-194`; `passkey-backup.test.ts:40-45,151-157`; `import-paths.test.ts:61-157,528-583`: Opus’s “tests will hang” finding is correct, and §4.5 understates the blast radius. Shared register/import fixtures depend on auto-naming, so this is not just a couple of test edits. Suggested fix: make the fixture sweep explicit in P4 and remove “existing tests should still pass unchanged.”

3. MED — `packages/extension/tests/e2e/passkey-paths.test.ts:141-160`; `packages/extension/tests/e2e/passkey-backup.test.ts:383-390`; `packages/extension/tests/e2e/fixtures/helpers.ts:879-940`: there is a second test fallout the plan misses and Opus missed too: two passkey reset tests hardcode `"Profile 1"` for destructive confirm. Once tests start supplying explicit names, these fail independently of the submit-time issue. Suggested fix: switch them to the existing `getActiveProfileName` / `resetProfile` helper.

4. MED — `implementations-plan/profile-name-parity/plan.md:24,209-218`; `packages/extension/src/popup/windows/discover/index.vue:162`; `packages/extension/src/popup/pages/settings/security/index.vue:184`; `packages/extension/src/popup/pages/settings/advanced/account-state/senders/index.vue:61`; `packages/extension/src/popup/pages/settings/fpcs/index.vue:83`; `packages/extension/src/components/Header.vue:249`; `packages/extension/src/components/ui/BrutalistTitle.stories.ts:20`: Opus’s paths were sloppy, but the substance is right: §4.4 is incomplete. Most of these are app-level “wallet” references and probably should stay, but the plan must whitelist them instead of asserting there are no other surfaces.

5. MED — `implementations-plan/profile-name-parity/plan.md:359-364`; `packages/extension/src/popup/components/popups/EditProfilePopup.vue:102-110`; `packages/extension/src/wallet/services/profile/service.ts:833-840,871-877,933-939`: the §8 defaults are too weak. `Q1` silent duplicate-allow is not really “status quo” because full-backup restore already auto-suffixes duplicates; `Q5` ignores a real parity gap because edit still caps names at 25 while create/import target 32; `Q6` overstates input sanitization because backup restore trusts unsanitized backup names. Suggested fix: pick an explicit duplicate policy, either align EditProfile to 32 or constrain new flows to 25, and note restore-time sanitization as the real hardening gap.

6. LOW — `implementations-plan/profile-name-parity/plan.md:304-310`; `packages/extension/tests/e2e/fixtures/extension.ts:157-164,808-809`; `packages/extension/src/popup/components/modules/settings/new-profile/NewProfileCredentials.vue:22-55`: the proposed e2e additions still lean on text/placeholder behavior, which conflicts with the `data-testid`-only rule. Suggested fix: if P4 touches popup-create helpers, add password field testids and avoid raw inline-error text assertions.

Things that look fine: the cited popup/onboarding line catalog in §4.1 and §4.3 is mostly accurate; Q2/Q3’s “keep wallet” default is defensible for app-level copy; R1 is a reasonable extraction once full-backup semantics are decided; the 10 composable cases are reasonable, but they do not replace the missing full-backup/import coverage.