APPROVE-WITH-FIXES

1. MED — `packages/extension/src/popup/components/popups/EditProfilePopup.vue:69-90,147-154`; `packages/extension/src/wallet/services/profile/service.ts:392-403`: the new duplicate-block in edit is raceable. `otherProfileNames` loads asynchronously on popup open, but submit is enabled as soon as the user types, and `changeProfileName()` has no server-side uniqueness check. A fast click or automation can rename to a duplicate before the list lands. Suggested fix: gate submit on a `isLoadingOtherNames` flag, or re-fetch/revalidate inside `handleUpdateProfile()` before calling the service.

2. MED — `packages/extension/tests/e2e/scripts/check-derivation-parity.ts:117-150`: this non-test script still drives `/popup/import` without typing `import-name-input`, so it now depends on dead auto-name behavior. Suggested fix: fill `import-name-input` before the private-key import submit, same as the updated fixtures/tests.

3. MED — `packages/extension/src/onboarding/pages/create.vue:176-186`; `packages/extension/src/onboarding/pages/import.vue:320-330`: this drifts from plan v2/Q6. Popup create/import got `sanitize`; onboarding create/import did not. That means bidi/zero-width junk is still accepted through typed onboarding names even though popup blocks it. Suggested fix: add `sanitize` to both onboarding name inputs for parity with the approved plan and popup behavior.

4. LOW — `packages/extension/src/composables/useFullBackupImport.ts:131-137,165-167`; `packages/extension/src/popup/pages/import.vue:321-323`; `packages/extension/src/onboarding/pages/import.vue:270-272`; `packages/extension/src/components/ui/Input.vue:117-132`: backup-name prefills still trust parsed backup Unicode too much. `parsedBackupName` is assigned directly from backup JSON, and `Input` sanitizes only on user input events, not on external `v-model` updates. So a malicious backup can still prefill/render bidi or zero-width characters unless the user edits the field. Suggested fix: sanitize `parsedBackupName` before publishing it, or sanitize in the prefill watcher before assigning `profileName`.

5. LOW — `packages/extension/src/popup/components/popups/EditProfilePopup.vue:88-90`: `bun run audit:vue` is still red on this branch. Typecheck passed, extension tests passed (`151 files / 1724 tests`), but Biome fails on formatting in this touched file. Suggested fix: run formatter on the branch and re-run `audit:vue`; the remaining dev-only lint debt can stay separate, but this branch should not add a failing gate.

Things that look fine: F3’s clone-not-mutate override is correct; the profile object is flat, so the shallow spread is enough. `parsedBackupName` reset belongs in `resetBackupState()` and the guarded prefill watch does preserve user typing during delayed decrypt/parse. The latch-first create/import handlers do close the double-click race you were worried about, and the `getActiveProfileName(page)` swap is valid because the reset page exposes `data-profile-name`.