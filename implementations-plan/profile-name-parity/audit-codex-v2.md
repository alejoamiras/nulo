APPROVE-WITH-FIXES

2. MED — `implementations-plan/profile-name-parity/plan-v2.md:181-188`; `packages/extension/tests/e2e/passkey-paths.test.ts:174-194`: §4.5 still misses one real F2 breakage. The passkey re-import path navigates to `#/popup/import` and clicks `import-option-passkey` without ever typing a name, so it will fail under the new required-input UX. Suggested fix: add this file/range to the F2 fallout table and explicitly type `import-name-input` before `import-option-passkey`.

4. LOW — `implementations-plan/profile-name-parity/plan-v2.md:159`; `packages/extension/src/popup/pages/settings/fpcs/index.vue:83`: the whitelist is materially better, but one entry is still factually wrong. There is no `popup/pages/settings/advanced/account-state/fpcs/index.vue`; the real file is `popup/pages/settings/fpcs/index.vue`. Suggested fix: correct the path so §4.4 remains a trustworthy catalog.

7. MED — `implementations-plan/profile-name-parity/plan-v2.md:124-125,235-246`: making `useProfileNameField.validate()` async introduces a new submission race. Today the create/import handlers gate on `isCreating`/`isImporting` before validation; with `await validate()`, two rapid clicks can both pass the pre-check and resume into duplicate submits. Suggested fix: either keep the composable sync and do async uniqueness lookup in the parent, or set a pre-validation latch / re-check `isCreating` immediately after `await validate()`.

8. LOW — `implementations-plan/profile-name-parity/plan-v2.md:97-99,392`: F3’s sample watch and the risk section disagree. The code sample overwrites unconditionally; the risk section says the watch guards on `profileName.value === ""`. Suggested fix: make the guard explicit in §4.1/F3, otherwise encrypted-backup decrypt can clobber user typing.

Things that look fine: F3 is safe without a new `restore(..., nameOverride)` parameter as long as implementation follows the shown clone pattern (`{ ...profile, name: override }`) rather than mutating `data.profile` in place. I don’t see another non-test caller that programmatically drives popup create/import without typing a name; the remaining callers are route pushes for human UI. The keep-vs-rename classification is mostly defensible: `discover`, `security`, `senders`, `Header`, `done.vue`, and `Wallet version` read as app-level “wallet” usage, not account-record naming. Deferring §7.2 homoglyph defense and §7.3 backup-name sanitization is reasonable for this PR; both are real but pre-existing hardening gaps, not blockers for the parity change itself.