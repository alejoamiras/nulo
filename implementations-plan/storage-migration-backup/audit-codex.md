**Critical**
None found.

**High**
- **H1: The parity test is not sound enough to carry the dropped network-e2e claim.** The plan says C-scoped has a “provable parity test” and uses that in I-D (`implementations-plan/storage-migration-backup/plan.md:17`, `:80`, `:117`). That proves the same `Migrator` behaves the same over identical stores; it does not prove a backup projection is equivalent to live storage. Many backup slices are active-profile subsets: `account.backup()` filters by active profile (`apps/extension/src/wallet/services/account/service.ts:221`), `token.backup()` does too (`apps/extension/src/wallet/services/token/service.ts:529`), and transactions are collected through active profile networks/accounts (`apps/extension/src/wallet/services/transaction/service.ts:286`). A migration that scans a whole root can behave differently on profile-subset backup data than on full live storage. Require per-migration tests with multi-profile live state plus an extracted active-profile backup, or explicitly restrict backup-migratable migrations to local per-row transforms.

- **H2: FPC rehydration fabricates state migrations would not see live.** The plan says to rehydrate `isProtocol` so migrations see “true pre-shape” (`plan.md:38`), but live storage explicitly omits it (`apps/extension/src/wallet/services/fpc/service.ts:38`, `:63`). `getFpcs(undefined)` returns `isProtocol: false` only as decoration for callers (`:122`), backup strips it (`:443`), and restore strips it again (`:475`). Scratch rows for the migrator should match stored rows, not decorated API rows. Otherwise a malicious backup or future migration can branch on a field that never existed in `chrome.storage.local`.

- **H3: Excluding `profile` contradicts “same migrations” for a real persisted root.** The plan excludes `profile` as “never a row-migration target” (`plan.md:41`, also assumed in `:100`), but profiles are stored under `nulo:core:profiles` (`apps/extension/src/wallet/services/profile/repository.ts:22`). Backup exports only `ProfileInfo` (`apps/extension/src/wallet/services/profile/service.ts:878`, `:882`), while restore re-derives encrypted stored rows from `master-key` (`:886`). The plan needs an explicit policy: either a safe profile projection descriptor for metadata-only migrations, or a rule that migrations touching `nulo:core:profiles` are not backup-migratable and must block import with a deliberate release decision.

- **H4: `backupSliceMigrations` for `account-state` is a placeholder, not a design.** The carve-out is correct because account-state reads/restores PXE data (`apps/extension/src/wallet/services/account-state/service.ts:145`, `:192`), but the plan only names a separate lane (`plan.md:16`, `:79`, `:102`, `:123`). It does not define versioning, ordering versus storage migrations and ID remap, shape validation, idempotency, or failure semantics. A4 silently resolves “assumed stable” when it should remain a surfaced ask unless the lane is fully specified.

**Medium**
- **M1: Config fold/unfold can fabricate defaults.** Config backup returns current in-memory props (`apps/extension/src/wallet/services/config/service.ts:41`), and props enumerate the current `Config` object (`apps/extension/src/wallet/config/store.ts:13`). If fold creates a current `Config` and fills missing keys, an old backup sees new defaults before migration. Fold must preserve absence distinctly, or migrations will not see the real old shape.

- **M2: `auth-registry-enabled` omission is a data-loss edge, not just an absent root.** The status root exists (`apps/extension/src/wallet/services/auth-registry/service.ts:62`), defaults to `true` when absent (`:242`), and backup only exports authwits (`:401`). Any migration touching `nulo:core:auth-registry-enabled` should reject or require the backup format to start exporting it; treating absence as empty can erase disabled states.

- **M3: The footprint guardrail overclaims.** `guardCommit` catches writes outside declared writes (`packages/wallet-core/src/migration/migrator.ts:258`), but the proposed registry coverage test (`plan.md:68`, `:99`) only catches uncovered refs. It will not catch migrations that declare covered roots but rely on full-store population, profile-global scans, fabricated defaults, or lossy projection semantics.

- **M4: Phase 3 contradicts the format break.** The plan says to drop `schema-version` and reject old `schema-version:2`-only blobs (`plan.md:50`, `:60`), but also says baseline behavior is identical while migration is not wired (`:83`). Current import rejects on `schema-version !== 2` before checksum (`apps/extension/src/composables/useFullBackupImport.ts:216`), and current tests/builders stamp it (`apps/extension/src/composables/useFullBackupImport.test.ts:137`, `apps/extension/tests/e2e/helpers/import-drivers.ts:239`). This phase is a clean break, not behavior-preserving.

- **M5: Validation gates do not fully prove their phase claims.** Phase 3 claims unit+component but only runs composable/helper tests (`plan.md:84`); the app has a component test command (`apps/extension/package.json:25`). Phase 5’s fixture e2e must prove it did not skip: existing migration e2e skips unless `NULO_E2E_MIGRATION_FIXTURE=1` (`apps/extension/tests/e2e/migration.test.ts:37`, `:109`), and the build fixture is gated separately (`apps/extension/src/e2e/migration-fixture.ts:5`).

- **M6: Pre-finalize rollback hardening is adjacent scope creep.** It is a real bug: the outer catch does not delete the new profile (`apps/extension/src/composables/useFullBackupImport.ts:425`), while only specific branches do (`:309`, `:342`). But migration failures happen before restore and mutate no live state (`plan.md:70`). Keep it as a separate accepted fix or split it out.

**Low**
- **L1: Facts section has misstated or weak citations.** `account/service.ts:58` is not the backup path; actual backup is `:221`. “Verbatim profile-filtered row values” is false for token-balance because it calls `repo.getAll()` after reading profile but does not filter (`apps/extension/src/wallet/services/token-balance/service.ts:259`). The config store path is actually `apps/extension/src/wallet/config/store.ts:10`, not the cited service path.

- **L2: Replay wording is too strong.** The plan says replay is bounded by duplicate-address guard (`plan.md:101`), but that guard only catches existing account address collisions (`apps/extension/src/wallet/services/account/service.ts:232`). Fresh-profile replay of stale contacts/config/authwits/tokens remains accepted user-directed restore, not prevented replay.

- **L3: Pre-checksum parsed metadata must remain UI-only.** `readBackupFile` parses plain JSON and reads `data.profile.type` before checksum verification (`apps/extension/src/utils/full-backup-helpers.ts:39`). That is okay only if no security decision depends on it before `useFullBackupImport` verifies checksum and validates top-level fields.

conditional approve (with conditions: fix H1-H4 and M1-M5 in the plan before implementation; either split M6 out or explicitly accept it as scope; update the Facts section so every file:line claim matches the code)