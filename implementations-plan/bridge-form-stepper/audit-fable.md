# Fable audit transcript — bridge-form-stepper

## Round 1 — dual audit (Plan subagent, model fable, max effort)

Verdict: **conditional approve** — all conditions folded (S7–S12, P1/P2 rewrites). Outline: dedicated stepper (reject card-reuse — the card is status-shaped with resume copy + actions that are wrong mid-takeover). All plan citations verified against the live code. Verbatim findings (paths repo-relative):

- **HIGH-1 — "Frees the form" is false under current gating.** `BridgeForm` gates amount/flip/submit on `busy = depositFlow.busy || withdrawFlow.busy` (`BridgeForm.vue:32,134,168,209`); `deposit()` awaits the sync gate (up to 300×6s) before busy clears (`useDeposit.ts:267`). Manual test 3 would fail. → Folded: P2 re-keys ALL form gating to `formStage` (S7).
- **HIGH-2 — flowStep must be per-record, not per-flow-instance.** With HIGH-1 fixed, a second submit interleaves shared `busy`/`error`/`flowStep` refs (second finally clears the first's busy; errors overwrite; the stepper renders the backgrounded bridge's legs). → Folded: S3 — flow legs write into the per-record journal runtime via `setRecordStep`; error routing per record.
- **HIGH-3 — RETRY is a silent no-op for flow-leg failures.** A deposit failed pre-leafIndex makes `runDepositClaim` bail silently (`useBridgeJournal.ts:447-450`) and the sweep skips it; AND public deposits create their record BEFORE any prompt (`useDeposit.ts:196`) with no clean-reject discard (unlike `useWithdraw.ts:241`) — the draft's "no record lingers" was wrong for them. → Folded: per-phase RETRY routing (S9) + deposit clean-reject parity in the D1 matrix (S8).
- **MEDIUM-1 — invariant over-strong; reload-during-receipt answered.** After completion + the 8s provenance auto-hide, NEW BRIDGE leaves the record in zero surfaces — by design; reload during the receipt shows a ✓ CARD (runtime `hidden` is wiped on reload, nothing re-hides on init). → Folded: invariant scoped to non-completed records; both completed behaviors pinned (S12).
- **MEDIUM-2 — receipt data source.** Live read by id can blank on a cross-tab discard; `lastCompleted` holds ONE txHash — insufficient for both links. → Folded: receipt snapshots direction/amount/both hashes at the stepper→receipt transition (S11).
- **MEDIUM-3 — phase flicker / precedence undefined.** `withRecordLock`'s finally clears `step` on every lock exit including BETWEEN chunked rounds (`useBridgeJournal.ts:336-341`, re-entry `:501-504`) — a live-step stepper regresses CONFIRM→blank→CONFIRM. → Folded: monotonic latch — phase states from record facts, runtime selects the active phase; between-rounds pin (S10).
- **MEDIUM-4 — testing inference wrong.** No flow tests exist; `BridgeForm.test.ts:37-42` mocks the flows wholesale; the flows call wallet singletons + viem directly (`useDeposit.ts:200-244`) — "extend the existing flow fakes" has no referent. → Folded: the mapper carries the proof weight (pure matrices); flows get a `vi.mock` harness (P1 testing strategy).
- **MINOR** — (a) chip row must define the Aztec panel's setting-up/capability-approval/error states (→ wider status chip, folded into D3); (b) manual test 4 overpromised (mid-DEPOSIT reload is an engine-undriveable card — reworded); (c) "Asks: none open" understated the concurrency model (resolved by S3/S7).

Verified facts: flow returns (`useDeposit.ts:280`, `useWithdraw.ts:255`), console-only legs, runtime step/stepDetail feeding cards, `visibleRecords`, panel sizes 94/138, disconnect testids, VerificationModal ownership, strict-hash helpers, `App.vue` v-show mounts. Anti-blind-signing copy sound.
