HOLDS — security disposition stands; one navigation concern kept open (closed in r9).

- r7 Critical REJECTED with a written rebuttal, accepted point-by-point: (a) the DoS asymmetry is real — corrupting `guard`/`secret` destroys sealed material (inherent to storage-write power), while a 1-byte `envelopeMac` flip destroys nothing, so hard-fail would mint a NEW cheap lockout primitive over intact data; (b) F-1's severity was the SILENT swap — the detected path is loud (visible degraded warning), bearer-less, and refuses password-change laundering; (c) the residual's marginal exposure (a flagged quarantined session) weighs against arming the strictly larger no-password storage-writer class. Codex: "The accepted residual stands." Its sharpening of (c) is recorded: a writer can transplant rows BLINDLY without knowing the shared password — but found "no automatic secret disclosure or fund movement from the quarantined session".
- r7 High verified FIXED: lock cleanup now unconditional (cached-profiles fallback on lookup rejection, sequence fence preserved).
- r7 Medium fix ruled INCOMPLETE: a hash gate alone is not an atomic election (the real router moves the hash only after async route resolution), and `includes("/popup/auth")` false-matched `/popup/import?from=/popup/auth` + `/popup/profile/new?from=/popup/auth` (both real, pushed by SelectProfilePopup) — the late push could yank a user out of those flows.
- r7 Low verified FIXED in-diff.
