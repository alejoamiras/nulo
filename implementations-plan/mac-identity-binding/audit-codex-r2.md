BREAKS — embedded-id bypass is closed, but passkey fingerprint binding still misses one session-open path.

- **High — passkey `finalizeRestore` skips the new check.** After `restore()` writes the passkey row and stashes `{secret, dek}`, an A1 writer can blind `walletFingerprint` before finalization. [finalizeRestore](/home/homelab/Projects/nulo/.claude/worktrees/mac-identity-binding/apps/extension/src/wallet/services/profile/service.ts:2513) opens directly with the pending DEK without recomputing the fingerprint, unsealing the current slot, or emitting degradation. Result: clean session plus silently blinded duplicate guard until the next credential unlock. Recompute from `pending.secret`; mismatch should discard the DEK and emit degradation. Ideally also revalidate the stored credential ID/DEK ciphertext against the row created by `restore()`.

- **Medium — the identity guard accepts missing IDs.** [requireIdMatch](/home/homelab/Projects/nulo/.claude/worktrees/mac-identity-binding/packages/wallet-core/src/storage/entity_storage.ts:118) rejects only `embedded !== undefined && embedded !== suffix`; its test explicitly accepts no-ID rows. Profiles have no runtime codec, so deleting `id` lets the row pass and its MAC verify against the requested ID, after which `openSessionVerified` uses `profile.id === undefined` for integrity, deletion, and session scoping. This does not recover clean B adoption, but violates the guard’s security invariant. Opt-in mode should require `typeof id === "string" && id === suffix`.

- **Medium — auth-registry has key/embedded-ID aliasing.** `revokeAuthwits(requestedId)` fetches by suffix but checks only `account`; reconciliation and deletion later use the embedded numeric `row.id`. A same-account row copied under another key can revoke the wrong hash and mutate/delete the original key. No cross-account escape exists because the ownership check holds. The current string-only option cannot be enabled directly; add a canonical identity extractor (`String(row.id)`) or service-level equality checks/key-aware enumeration.

Verified: B’s verbatim row under A is now hidden; MAC sites use requested/session IDs; normal passkey unlock/export checks are correctly ordered; password DEK paths remain covered. The owner-adjudicated derived-only residual now behaves as documented.