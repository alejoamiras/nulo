BREAKS

- **Critical — MAC failure still adopts B’s master.** The committed whole-envelope test copies B into A with `id=A`; verification fails, but [unlockProfile](/home/homelab/Projects/nulo/.claude/worktrees/mac-identity-binding/apps/extension/src/wallet/services/profile/service.ts:510) merely drops the DEK and opens a derived-only session using B’s authenticated `secret+entropy`. With zero existing derived accounts, A still becomes wallet B; only an “imported accounts unavailable” warning appears. The test never asserts the resulting master/address, so it is a false positive.

- **Critical — embedded-row-ID bypass verifies cleanly.** [EntityStorage.get](/home/homelab/Projects/nulo/.claude/worktrees/mac-identity-binding/packages/wallet-core/src/storage/entity_storage.ts:100) does not require the JSON `id` to equal the storage-key suffix. Put B’s entire row under key A while retaining `id=B` and changing only un-MACed `name` to “A”. Unlock fetches it via A but verifies against `current.id === B`, so B’s MAC passes and the DEK is trusted. `getProfiles()` also discards the authoritative suffix, while the selector displays only `name`. Bind/verify using the requested storage suffix and reject `row.id !== suffix`; add a test that does **not** overwrite B’s embedded id.

- **High — F-2 remains open for passkey profiles.** Passkey rows have `walletFingerprint` but no MAC, and the credential wrap key authenticates only `dekSealed`. Blinding the fingerprint therefore survives a completely clean passkey unlock and suppresses duplicate detection. Compare it with `computeWalletFingerprint(recovery.secret)` on every ceremony-based open, or authenticate it with credential-bound material.

- **High — backup export launders the detected swap.** `exportBackupMaterial` intentionally ignores the failed MAC, exports B’s coherent master/entropy/DEK, and restore re-seals it under a fresh id with a valid v3 tag. `exportImportedKeysDek` likewise uses the DEK before MAC validation.

Verified fine: the v3 HKDF/HMAC construction itself is sound; ordinary password unlock, password-change, bearer restore, and finalize sites perform their MAC checks; create’s added lock scope introduces no meaningful TOCTOU; password restore computes after final id allocation; passkey backup restore correctly matches the credential, unwraps the source DEK, and mints a fresh destination DEK. No functional v2 references or fixed v2 E2E/migration fixtures remain; only stale v2 wording in `Profile` JSDoc.