# swap-fuel — phase 1 lessons (router hardening + live deploy script)

## 2026-06-12

- **L1 hardening landed first** (pre-deploy, per ledger L1): `bridgeWithFuel` now requires `fuelReceived >= p.minFuelOutput` (the SIGNED floor binds in the router, not the owner-replaceable target) AND strict token-delta equality across the swap call (`tokenBalBefore - after == fuelAmount`) — closes the residue-theft path from the codex double-audit CRITICAL (hostile target prefunding FJ without pulling the slice). Strict equality is sound because the approval caps the pull. Pins: `test_insufficientFuelReverts` + `test_prefundedTargetNotConsumingSliceReverts` (new `MaliciousPrefundSwap` mock). 28 forge units + 81 bridge-core green; WitnessHash byte-pins untouched (no witness change).
- **Codex consult — pool topology/economics** (xhigh, session 019ebc4f-a37f-7962-be52-35505108ab31): the gate's cheap-FJ requirement (200-500 FJ per bridge, ~0.5 ETH budget, no constant re-hydration) is UNAFFORDABLE against the existing third-party ETH/FJ pool at ~10,000 FJ/ETH (20 max-fuel purchases = 1.0 WETH of depth). Options weighed: (A) plain seed → ~7-10 fills per re-seed, fails the requirement; (B) dump free-minted FJ into the shared pool → extracts third-party ETH from shared infra, not durable (anyone can counter-dump), wrong failure domain; (C) OUR OWN ETH/FJ pool at a different fee tier — deterministic price, no external dependency, FJ side free. **Verdict: C** — ETH/FJ fee=500/spacing=10 at 200,000 FJ/ETH (tick ~122k, far from bounds), AZLO/WETH 100 AZLO/WETH also at fee 500/spacing 10, seeds ~0.22-0.25 WETH + ~0.08-0.12 ETH ≈ 0.35 total. Adopted verbatim (my draft had AZLO/WETH at fee 3000 — switched to 500; route PoolKeys carry fee/spacing so nothing else assumes 3000).
- **DeployFuelLive.s.sol** (new; DeployBridge.s.sol untouched as fork fixture): env-driven token (default live AZLO), idempotent split (`FUEL_SWAP_ADDRESS`/`ROUTER_ADDRESS` reuse ⇒ seed-only re-runs), pool-init front-run guard (aborts unless uninitialized or within ±10% sqrt tolerance of target; post-seed assertion), liquidity defaults derived + documented in-file (AZLO/WETH L=3.2e18 ≈ 0.22 WETH + 22 AZLO; ETH/FJ L=70e18 ≈ 0.11 ETH + 21.4k free-minted FJ). Compiles clean.
- Liquidity-math gotcha worth keeping: my first defaults were 15× off — derive token amounts from L via `amount1 = L·(sqrtP − sqrtPlower)`, `amount0 = L·(1/sqrtP − 1/sqrtPupper)` (plain sqrt-price terms) BEFORE writing constants; the fork test validates empirically either way.
- 1Password SSH agent intermittently locked → pushes queue locally; retry each firing.

NEXT: `DeployFuelLive.fork.t.sol` (fork-proof the production topology end-to-end: seed with production constants, two-hop bridgeWithFuel incl. private variant, live-AZLO Permit2 allowance override assert, quoter probe) — then P1 gate.

## 2026-06-12 — fork rehearsal (P1 gate)

- **DeployFuelLive.fork.t.sol** rehearses the EXACT live topology: LIVE AZLO (permissionless mint + the Permit2 allowance override asserted on deployed bytecode), LIVE canonical Token/FeeJuice portals (real deposits on fork, both isPrivate variants), our two pools seeded with the production constants read FROM the script instance (no constant drift).
- **The front-run scenario is LIVE REALITY, not theory**: the ETH/FJ fee-500 key is already squatted on Sepolia at tick 227931 (~8e9 FJ/ETH) — our first fork run seeded single-sided into it (the band demanded 89k FJ = the full-range-token1 signature). AND the shared fee-3000 pool has drifted from Holonym's ~10,000 FJ/ETH to ~191 FJ/ETH (FJ 50x more expensive) — the recon-era price is stale, which retroactively strengthens the own-pool decision. Our tier moved to **fee 987/spacing 10** (eccentric value, no convention collisions; probe shows 987/1313/10000 free).
- **Fuel fills must be PRODUCT-SIZED**: a 2-AZLO slice (~4000 FJ) craters these depths (12.9% + 25% price moves per hop, -27% second fill); a 0.25-AZLO slice (~500 FJ — the 200-500 FJ design band) sees ~2-3% total slippage. Consequence for P6: the form needs a MAX fuel cap (sibling of MIN_FUEL_FJ), enforced at validation — oversize slices are a self-rug.
- **Honest depth economics at (L_azloWeth=3.2e18, L_ethFj=70e18)**: ~4.5% output decay per consecutive design-band fill, every one of 5 consecutive fills clears a 380-FJ floor with no re-seed. P2 tuning option recorded: narrowing the FJ band (e.g. [115000,130000]) roughly doubles in-range liquidity per ETH (impact → ~2%/fill) at the cost of earlier band-exit under one-way flow.
- Gate: `forge test` 38/38 (all unit + fork suites), WitnessHash byte-pins unchanged, bridge-core 81 green.
