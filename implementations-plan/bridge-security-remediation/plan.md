# Bridge Security Remediation — Implementation Plan

**Repo:** nulo-4 · **Base:** dev · **Tier:** deep · **Source audit:** `audit/security/2026-06-14-bridge-redteam/`
**Routes:** fixed (see `brief.md`). Three independent drafts → consolidated → contradiction-check (codex+opus) → double audit (codex+fresh-opus, both REJECTED a keystone flaw) → iterated with user decisions. This is the post-double-audit plan.

## Summary

Eight findings, two PRs. **PR A** (no redeploy): F-005 (JS) + F-007 (docs). **PR B** (contracts + a live testnet cutover): F-001 + F-002 + F-004 + F-006. F-008 is a documented no-op. **F-003 (contract CI) is NOT done** — user decision; contract tests run locally only (see below).

**Keystone correction (from the double audit):** F-001 deploys the forked portal via **deploy-then-init of the real-`@aztec`-interface body compiled in the l1-contracts root** — NOT a CREATE2 factory. The audit proved (by compile attempt) the full body only compiles in the l1-contracts root (solc 0.8.30, blob remap present); a factory living with the `bridge-evm` artifacts could only embed the minimal-interface **shim**, deploying stub withdraw/outbox bytecode to production. So: real-interface body for deploy/verify; the shim is **test-only** (the in-project `PortalReinit` unit test, which never touches the withdraw path); the cross-chain init order is handled by deploy-then-init + an init-once guard + read-back-abort + tight sequencing.

Three repo facts reshape the brief: the AZLO **Token minter is immutable** (F-002 → full L2-stack redeploy: proxy + token + bridge); **F-004 is bytecode-breaking** (L1 fuel router + swap redeploy); committed-script salts == live config salts (`0x5b11/12/13`=`23313/14/15`) — bump for a new generation, not because anything is stale. Cutover re-points `l1.portal`, `l1.fuel.{router,swapTarget}`, `l2.{proxy,token,bridge}`.

**User decisions (this gate's irreversible calls, now fixed inputs):** (1) **Abandon** old-token balances — fresh start, no migration/notice (testnet). (2) **No contract CI (F-003 deferred)** — the contract test suites run LOCALLY only (the PR-B per-phase gates), not in any CI gate; F-003 (HIGH) stays an open documented finding (accepted: a future Aztec bump / contract regression won't be caught by CI). (3) **Throwaway testnet canary** generation runs a real-network deposit→claim BEFORE the committed cutover. (4) **Defer multisig** — router/bridge stay EOA-owned; multisig is a tracked follow-up.

## Audit verdicts (deep-tier trail; transcripts in audit-codex.md / audit-opus.md)
- **Contradiction-check** (codex + fresh opus): both flagged the compile-split direction, the salt hex/decimal fact, the CREATE2 rationale, and the B6 ordering → all folded.
- **Double audit** (codex + fresh-opus): **both `reject`** — the CREATE2-factory keystone would ship stub bytecode to production (proven by compile attempt). Re-architected to deploy-then-init real-interface + guard + read-back-abort; B3 wiring, `smoke-existing-testnet.ts`, `placePortalSource`, and per-generation salts fixed; the 4 irreversible Asks taken to the user.
- **Final fresh-context codex** (new session `019ecb96`): **`conditional approve`** — conditions (1) same-directory sibling-temp + `fsync` + atomic `rename` for the JSON cutover, (2) a durable write-ahead attempt journal for resume safety. **Both folded** (see the Candidate-config + Resume-safety ledger rows, B4/B6, and Security).

## Decision Ledger (post double-audit)

| Decision | Resolved | Provenance |
|---|---|---|
| F-001 deploy/init | **Deploy-then-init the real-interface forked portal (l1-contracts-root compile) + init-once guard + read-back-abort + tight tx sequencing.** No CREATE2 factory. | codex proposed CREATE2; **double audit (both) rejected it** — the factory in `bridge-evm` would deploy the stub shim (artifact-parity hole, no gate proved real-interface bytecode). Reverts to opus's original deploy-then-init, vindicated. |
| F-001 fork compile | Real-`@aztec`-interface `NuloTokenPortal` staged into `L1_ARTIFACTS_ROOT/test/portals/` (real interfaces, blob remap present, solc 0.8.30) for deploy+verify; minimal-interface shim in `bridge-evm` ONLY for the `PortalReinit` unit test (never deployed, never calls withdraw). | codex+opus contradiction-check + double audit |
| F-002 proxy↔bridge | One-time lockable `set_bridge` (single-shot `PublicImmutable`, `owner==msg_sender`-guarded); remove `set_minter` + owner-mint. Token redeploy mandatory (minter immutable). | all three |
| Retry safety | **Per-attempt FULL-generation salts** (bump proxy+token+bridge+portal together each live attempt) + a resume-from-recorded-manifest rule. A failed attempt that wired the one-shot L2 stack burns the whole generation, not just the portal. | codex double-audit |
| Candidate config | Written **post-deploy** in B6 as a **same-directory sibling temp file under `packages/faucet/public/`** (randomized name, 0600) — NOT `mktemp -d` (a cross-device temp breaks the atomic rename at activation) — then `fsync` + atomic `rename(2)` over `testnet-bridge.json`. Pre-swap validation = on-chain read-back + script-level smokes (`--config`); faucet build/test runs **post-swap** with rollback-by-restore (the faucet statically imports the JSON; a pre-swap app-build against the candidate isn't real without a config-loader refactor — out of scope). | codex+opus double audit; final-pass condition 1 |
| Resume safety | A **durable write-ahead attempt journal** (a per-attempt file appended after EACH contract receipt lands) is the authoritative resume artifact — the post-deploy candidate manifest is too late (a crash after some non-deterministic L1 deploys would leave no record). Resume reads the journal; if not cleanly resumable, bump the full generation and redeploy. | final-pass condition 2 |
| Testnet rehearsal | **Canary generation** (B-canary): deploy a throwaway generation on real testnet, e2e deposit→claim, THEN the real cutover generation. | user decision 3 |
| F-003 (contract CI) | **NOT done** — no new CI gate; contract suites run locally only (PR-B per-phase gates). F-003 (HIGH) stays open/documented. | user decision (post-gate-draft) |
| Old balances / multisig | Abandon old token (no notice); router/bridge ownership stays EOA (multisig deferred). | user decisions 1 + 4 |
| F-004 framing | Closes a consent-to-target gap (hostile target routing within the signed floor), NOT a live drain — the router already binds `routeHash`+`minFuelOutput`+floor (`SwapBridgeRouter.sol:196,204`). | opus contradiction-check |

---

## PR A — independent fixes (no redeploy; lands first)

### Phase A1 — F-005: `runSwapBridge` fail-closed for private fuel (JS) ✓
`flows.ts:runSwapBridge`, before `signTypedData`: when `isPrivate`, throw on missing `fuelSecret` or `fuelRecipient !== PRIVATE_FPC_ADDRESS`. Public path unchanged.
**Gate:** `bun run --cwd packages/bridge-core typecheck && bun run --cwd packages/bridge-core test` — new cases: private+missing-secret + private+wrong-recipient throw before signing (red pre-fix). Layers: typecheck+unit.

### Phase A2 — F-007: verify recovery-hook handling + document (docs-only) ✓
Confirm `onSecret/onSecrets` never log/persist plaintext; add TSDoc + README contract (seal-at-rest; bearer; recipient-commitment backlog).
**Gate:** `bun run --cwd packages/bridge-core test && bun run lint`. Layers: unit+lint.

### F-003 — NOT DONE (user decision)
Contract tests are **not** wired into CI. The contract suites (content-hash keystone, router invariants, the witness pin) run **locally only**, via the PR-B per-phase validation gates (B1/B2/B5). F-003 (HIGH — a future Aztec bump or contract regression could silently strand deposits) stays an **open, documented finding**; mitigation is the local gates + dev discipline. (The `WitnessHash.t.sol` `console2.log`→`assertEq` flip moves into B2, where the 12-field witness pin is updated anyway.)

---

## PR B — contracts + live testnet cutover

Order: B1 portal+guard → B2 L1 fixes+PoC split → B3 proxy redesign (+sandbox/deposit wiring) → B4 deploy-script rework (deploy-then-init, candidate manifest, smoke-existing) → B5 pre-live fork+sandbox gate → **B-canary** (throwaway testnet e2e) → B6 real cutover → B7 re-harden.

### Phase B1 — F-001: forked init-guarded portal (deploy-then-init, real interfaces) ✓
`upstream/NuloTokenPortal.sol` (git-tracked, full canonical body) = canonical + `error AlreadyInitialized();` + `if (address(registry) != address(0)) revert AlreadyInitialized();` as line 1 of `initialize`; canonical `initialize` ABI preserved. **Deploy/verify:** stage into `L1_ARTIFACTS_ROOT/test/portals/` (where `verify-l1.ts:63` already copies), compile at the root's solc 0.8.30 with real interfaces; the DEPLOYED bytecode is this compile. **Test shim** (clearly non-deployable — name it `NuloTokenPortalShim` under `bridge-evm/test/`, NEVER loaded by the deploy path, which uses only the l1-root-built artifact): a minimal-interface copy for the `PortalReinit` unit test only — reproduce the double-cast (`IRegistry.getCanonicalRollup()→IHaveVersion{getVersion}`, then `IRollup{getOutbox→IOutbox,getInbox→IInbox,getVersion}`); the shim never calls withdraw/outbox so its stubs can't reach prod. No factory; init happens in B4's deploy-then-init sequence.
**Gate:** `forge test --root packages/bridge-evm` compiles the shim; `PortalReinit.t.sol` (shim, local deploy): init once → second `initialize` reverts `AlreadyInitialized`. **`ContentHash.t.sol` green.** Separately: the staged real-interface body compiles in the l1-contracts root (`cd node_modules/@aztec/l1-artifacts/l1-contracts && forge build`). Layers: contract unit + content-hash pin + root compile.

### Phase B2 — F-004 + F-006: witness binds swapTarget; minOutput floor; PoC split ✓
`SwapBridgeRouter.sol`: `address swapTarget` as the 12th `BridgeWitness` field (TYPEHASH :52, TYPE_STRING :55, struct :113, `_hashBridgeWitness` :325, both call sites). Threat framing: consent-to-target gap, not a live drain (router already binds routeHash+minFuelOutput+floor at :196,:204). `UniswapFuelSwap.sol:swap`: `require(minOutput > 0)`. JS mirror in `l1.ts` (12-field type/types/iface/hash) + `flows.ts` passes `swapTarget`; flip `WitnessHash.t.sol` `console2.log`→`assertEq` and update its 12-field pin (+ `l1.test.ts`). **PoC split:** `PortalReinit.t.sol` (always-on unit, shim) + `PortalReinit.fork.t.sol` (Sepolia-fork; asserts against the NEW portal — runs in B-canary/B6, NOT B5, since the new portal isn't on-chain at B5).
**Gate:** `forge test --root packages/bridge-evm` — sig bound to swapTarget A reverts after `setSwapTarget(B)`; 12-field WitnessHash assertEq; router/route suites green. `bun run --cwd packages/bridge-core test` — JS witness hash==Solidity typehash. Layers: contract unit + JS cross-pin.

### Phase B3 — F-002: immutable single-minter proxy + lockable set_bridge (+ wiring) ✓
`token_minter_proxy/main.nr`: remove `set_minter` + owner-mint; keep a bootstrap deployer for one-time `set_token` + `set_bridge` (single-shot `PublicImmutable`, **`owner==msg_sender` guarded** so the one-shot can't be front-run); `mint_*`/`burn_*` assert `msg_sender==bridge`. **Also update the wiring in `deploy-sandbox.ts:166-167` AND `deposit-testnet.ts:170-171` from `set_minter`→`set_bridge`** — else B3's own gate (which runs `deploy-sandbox.ts --smoke`) deadlocks. Noir compile is local here (user decision 2).
**Gate:** rc.2 `nargo test --program-dir packages/bridge-aztec/token_minter_proxy` + `bash packages/bridge-aztec/scripts/compile.sh` (proxy/bridge compile, `set_minter` gone) + `bun packages/bridge-core/scripts/deploy-sandbox.ts --smoke` (new wiring: mint-on-claim + burn-on-exit). Layers: Noir compile/unit + sandbox e2e.

### Phase B4 — deploy-script rework: deploy-then-init, candidate manifest, smoke-existing ✓
`deploy-bridge-testnet.ts`: deploy the forked portal (uninitialized) from the l1-contracts-root artifact; deploy new proxy/token/bridge + fuel router/swap with **per-attempt full-generation salts**; wire `set_token`+`set_bridge`(lock); **then `portal.initialize(registry, usdc, bridge)` once** (guard makes re-init revert); read-back asserts (`portal.registry()/underlying()/l2Bridge()`+code hash; `router.swapTarget()`; `proxy.get_token()`; `bridge.get_config_public()`) → abort on mismatch (nothing written = auto-rollback). Append each contract address to a **durable write-ahead attempt journal** as its receipt lands (the authoritative resume artifact — the post-deploy candidate manifest alone is too late). Write the candidate manifest as a **same-directory sibling temp under `packages/faucet/public/`** (randomized name, 0600 — never `mktemp -d`, which risks a non-atomic cross-device rename) with `l1.portal`, `l1.fuel.{router,swapTarget}`, `l2.{proxy,token,bridge}`, `l1.portalSource:"forked-v1"`. Add `--config` to `verify-l1.ts` (+ `placePortalSource` forked-keccak self-pin + verify target `NuloTokenPortal` from the l1-contracts root at 0.8.30) and `fuel-testnet.ts`. **Create `smoke-existing-testnet.ts`** (register contracts from a candidate manifest, NO deploy — model it on `fuel-testnet.ts`; `deposit-testnet.ts` always deploys fresh so it's not adaptable by a flag) covering a plain deposit→claim. A resume-from-recorded-manifest mode (reuse exact salts/args if an attempt partially landed).
**Gate:** a unit/dry check that the candidate-manifest writer + `--config` readers round-trip; `bun run --cwd packages/faucet test`. Layers: script unit + faucet unit.

### Phase B5 — pre-live LOGIC gate (L1 local + Sepolia fork; L2 via testnet, not sandbox) ✓
**REVISED (no sandbox):** the Aztec sandbox cannot exercise the L1↔L2 round-trip (told by the user 2026-06-15; see `lessons/phase-B5.md` + memory `aztec-no-sandbox-l1-l2-bridge`). So the original `deploy-sandbox.ts --smoke` (L2-stack deposit/claim/withdraw) is INFEASIBLE and that coverage relocates to **B-canary** (the throwaway testnet generation is the only faithful L1↔L2 gate). B5 stands as the L1-only EVM logic gate. No candidate manifest here (L1 addresses aren't deterministic pre-deploy — that's B6).
**Gate (revised, GREEN):** local EVM logic `forge test --no-match-path '*[Ff]ork*'` → 29 tests across ContentHash (content-hash pins), PortalReinit (F-001 init-once guard), WitnessHash (F-004 12-field hash pin), RouteValidation, SwapBridgeRouter, MintableERC20; Sepolia-fork `SEPOLIA_RPC_URL=… forge test` → SwapBridgeRouterPermit2Fork 5/5 (incl. `test_witnessTamperReverts` = swapTarget binding + Permit2 nonce-replay/expiry on REAL Permit2) + DeployBridge.fork 1/1; the l1-contracts-root `forge build` of the staged portal (done in B4 — fork builds; pinned reviewed-bytes artifact committed). The L2 keystone `nargo test` is blocked by an aztec-nr rc.2 toolchain error (library `mock_note.nr`, not our code) — content-hash is pinned by the green `ContentHash.t.sol` + re-proven end-to-end at B-canary. The L1↔L2 deposit/claim/withdraw smoke runs at **B-canary** (testnet).

### Phase B-canary — first real testnet deploy (REVISED: not throwaway; held for promote) ✓
**REVISED (user 2026-06-15):** no separate disposable canary. Because there is no L1↔L2 sandbox, ONE real testnet deploy is the L1↔L2 logic gate AND the promotion candidate: deploy via the B4 script path → `smoke-existing-testnet.ts --config <candidate>` (deposit→claim) → if GREEN, HOLD the candidate for B6 promotion; if it fails, triage + fix + redeploy (the first clean deploy wins). The deploy writes `testnet-bridge.candidate.json` only — the app is NOT re-pointed here (that's B6, gated on explicit go). Still does NOT exercise the app-pointer swap/rollback nor close the deploy→init race; accepted for testnet.
**Gate:** candidate deposit→claim round-trip on testnet; on-chain read-backs pass (the deploy aborts otherwise); candidate portal re-init reverts on-chain. Layers: live-network e2e. Held for promote — do NOT proceed to B6 without explicit go.

### Phase B6 — real cutover (gated on B5 + B-canary; irreversible) ✓
**DONE as a PROMOTE, not a fresh redeploy** (user decision: "one deploy, hold for promote — first clean deploy wins"). The B-canary deploy WAS the cutover generation; B6 promoted that validated candidate → live `testnet-bridge.json` (snapshot to `/tmp` + `git` for rollback → `cp` candidate → live → faucet `test` 335 + `build` green → grep confirms no stale old-stack refs). The live deposit→claim is covered by B-canary's smokes on the identical candidate. Etherscan-verify of the new fuel ran during the hold. Committed (720959e cutover + 1063c54 docs), pushed, **PR #92 → dev** opened. The original fresh-redeploy text below is superseded.

Run from a **clean, lockfile-resolved checkout** (the staged-into-`node_modules` deploy/verify root is mutable local state — pin it). Deployer key via env only, never printed. Sequence: snapshot committed config (in-memory) → deploy the real cutover generation (fresh full-generation salts) deploy-then-init per B4 (append the write-ahead journal per receipt) → **read-back + abort on mismatch** → write the candidate manifest as a same-directory sibling temp under `packages/faucet/public/` → script-level smokes against the candidate (`smoke-existing-testnet.ts --config`, `fuel-testnet.ts --config`) + on-chain re-init-reverts assert → `fsync` + **terminal atomic `rename(2)`** of the sibling over `packages/faucet/public/testnet-bridge.json` → **post-swap** `bun run --cwd packages/faucet test && bun run --cwd packages/faucet build` + one live deposit→claim; **rollback = restore the snapshot** if post-swap is red. Etherscan `verify-l1 --config` runs **non-blocking** (post-swap or best-effort — an Etherscan outage must not consume a live attempt; the on-chain read-back is the real gate). Wait a Sepolia confirmation budget before the rename (reorg guard). Old portal/token/bridge abandoned (orphaned old-token balances accepted, user decision 1); grep confirms nothing references old addresses post-swap.
**Gate:** on-chain read-backs == intended; portal re-init reverts on-chain; post-swap faucet test+build green (else rollback); live deposit→claim round-trips. Layers: live-network e2e.

### Phase B7 — F-008 no-op + post-impl re-harden
F-008: lessons note (accepted-latent). Then `/harden security` over the new surface (NuloTokenPortal, redesigned proxy, 12-field router, migration scripts).
**Gate:** harden report shows F-001/F-002/F-004/F-006 closed, no new high/critical; always-on `PortalReinit` unit + 12-field witness pins green in CI. Layers: audit.

---

## Security & Adversarial Considerations
- **Deploy→init window (F-001):** no CREATE2/atomic factory (it would ship stub bytecode — see ledger). The window between portal-deploy and `initialize` is closed by: the init-once guard (a front-run init locks the attacker's state, so the deployer MUST init in the immediately-following tx from the same EOA, ideally same block) + the read-back-abort (a hijacked init → script aborts, treats the portal as burned, redeploys a fresh generation). On testnet with tight sequencing the residual window is small; documented, not zero.
- **Per-attempt generation burn:** one-shot L2 wiring (`set_bridge`/`set_token`) means a failed attempt after wiring burns the whole generation. Mitigation: per-attempt full-generation salts + a **durable write-ahead attempt journal** (appended per receipt) as the authoritative resume artifact; if not cleanly resumable, bump the generation and redeploy. The canary (B-canary) rehearses this on a throwaway generation first.
- **Content-hash drift → strand:** deploy/verify uses the REAL `@aztec` `Hash`/`DataStructures`/interfaces (l1-contracts-root compile) — no stub surface on the production withdraw/outbox path. The shim is test-only. `ContentHash.t.sol` + keystone (CI-gated) + the canary deposit→claim are the backstop.
- **Supply chain:** the staged-into-`node_modules` deploy/verify root is mutable local state — run B6 from a clean lockfile-resolved checkout; the keccak pin authenticates only the vendored portal file, not the toolchain.
- **Candidate manifest TOCTOU + atomic swap:** the candidate is a same-directory sibling temp under `packages/faucet/public/` (randomized name, 0600) — NOT `mktemp -d` (a cross-device rename isn't atomic and can fail at activation); `fsync` + terminal atomic `rename(2)` is the swap.
- **Least-privilege key:** testnet key, env-only, never logged. The cutover is inherently single-hot-key (deployer = portal-init + proxy bootstrap); multisig deferred (user decision 4) — tracked residual. Etherscan verification is non-blocking so an off-chain outage can't consume a live attempt.
- **CI residual (user decision 2):** the required gate runs content-hash + L1 tests + keystone `nargo test`, NOT `aztec compile` of proxy/bridge — a post-merge Noir compile-break to those is unguarded; mitigated by the B3 local compile + dev discipline.
- **Abandoned old stack:** old portal/token/bridge stay exploitable but unreferenced + low-value testnet; old-token balances orphaned with no notice (accepted).

## Assumptions
**Facts (verified):** AZLO Token minter is immutable — the Wonderland Token dist artifact has no `set_minter`/`set_admin`/`upgrade` and sets the minter only in `constructor_with_minter` (NOTE: the artifact's function list does not even expose `mint_to_public` directly — it routes via `public_dispatch`; the load-bearing fact is "no minter-rotation path," which holds). Portal full body compiles only in the l1-contracts root (solc 0.8.30; blob remap `@aztec-blob-lib/=src/mock/libraries` at that root's `foundry.toml:43` + `fs_permissions`); the `bridge-evm` project lacks the remap → standalone full-body compile fails there (proven). `verify-l1.ts:52,63` stages portal source into that root; `placePortalSource` keccak-checks the CANONICAL artifact today → must change for the fork. `getCanonicalRollup()→IHaveVersion` then cast to `IRollup`. CI orchestrator `pr-quick.yml` (`Status` skips→pass `:209-229`); `setup-aztec/action.yml` has only `skip_cli`, detects `4.2.0` not rc.2; **there is no `bun run lint:actions` script**. Committed-script salts == live salts (`0x5b11/12/13`=`23313/14/15`). `deposit-testnet.ts` always deploys a fresh (different: Nulo-USDC/6dp, random salts) stack → not flag-adaptable; `fuel-testnet.ts` IS a register-existing smoke (but lacks `--config`). The faucet **statically imports** `testnet-bridge.json` (`bridge-deployments.ts:8`) → a pre-swap app build against a candidate isn't real without a config-loader refactor. Router already binds `routeHash`+`minFuelOutput`+floor (`SwapBridgeRouter.sol:196,204`).
**Inferences:** the cross-chain init order works as deploy-then-init (portal deployed uninitialized → bridge deployed against the known portal address → portal initialized once with the bridge address); the test shim is safe because it's never deployed AND never calls withdraw; router/swap redeploy needs no V4 pool re-seed (pools key on currencies/fee/spacing/hook, not the caller).
**Asks — RESOLVED by user decisions:** (1) abandon old-token balances (fresh start, no notice). (2) CI light — Noir proxy/bridge compile local-only. (3) throwaway testnet canary before the real cutover. (4) defer multisig (router/bridge stay EOA). Re-pointing `l2.token` + `l1.fuel.*` is a forced consequence of the F-002/F-004 routes (accepted).

## Seeds (draft — finalized post-approval)
See `eli5.html` for the `/goal` + `/loop` blocks (one marked Recommended).
