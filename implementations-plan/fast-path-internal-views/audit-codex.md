needs-rework

**BLOCKER findings**
- The core split is semantically unsound. Your plan filters out `PUBLIC && isStatic` calls from anywhere in the batch ([plan.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan.md:60)), but upstream only optimizes a leading prefix because any earlier non-static call can affect later calls’ observed state ([wallet-sdk utils.ts](/Users/alejoamiras/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/utils.ts:33)). `balance-projector` currently enqueues private before public for each token ([balance-projector.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/token-balance/balance-projector.ts:110)), which is exactly the unsafe pattern. Transparent helper-level filtering changes execution semantics.
- Your fast-arm return-shape fact is wrong. `simulateViaNode` does return `TxSimulationResult[]`, but `publicOutput.publicReturnValues` is `NestedProcessReturnValues[]`, not `Fr[][]` ([public_simulation_output.ts](/Users/alejoamiras/.bun/install/cache/@aztec/stdlib@4.2.0-2b6ecea65106ddd2@@@1/src/tx/public_simulation_output.ts:68), [simulated_tx.ts](/Users/alejoamiras/.bun/install/cache/@aztec/stdlib@4.2.0-2b6ecea65106ddd2@@@1/src/tx/simulated_tx.ts:154)). The plan’s proposed unpack (`flatMap(...)[slot]`) in [plan.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan.md:146) would not match the helper’s `Fr[][]` output contract without extracting `.values ?? []`.

**HIGH findings**
- The fallback design is internally inconsistent once both arms have started. The plan says “run both in `Promise.all`” and also “re-add fastTxCalls to slowTxCalls and continue” on generic fast-arm failure ([plan.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan.md:97), [plan.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan.md:142)). If `simulateViaNode` rejects after `slowArm` has already simulated only the slow subset, you need an explicit second full rerun; mutating arrays is not enough. State safety is fine because `simulateViaNode` is node-only and does not touch PXE ([wallet-sdk utils.ts](/Users/alejoamiras/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/utils.ts:70)), but the orchestration is wrong.
- The concurrency story is overstated. `executeUtility` and `simulateTx` take PXE write locks ([pxe/service.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/aztec-runtime/src/pxe/service.ts:339), [pxe/service.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/aztec-runtime/src/pxe/service.ts:282)); `getSyncedBlockHeader` is a PXE read ([pxe/service.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/aztec-runtime/src/pxe/service.ts:372)). On this guard, a queued writer blocks later reads ([rw-guard.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/wallet-core/src/utils/rw-guard.ts:60), [rw-guard.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/wallet-core/src/utils/rw-guard.ts:98)). So eager utility launch can delay the anchor fetch and erase the intended overlap.
- One verified fact is plainly wrong: in the pinned 4.2.0 dependency graph, `MAX_ENQUEUED_CALLS_PER_CALL = 32`, not 16 ([bun.lock](/Users/alejoamiras/Projects/nulo/nulo-4/bun.lock:355), [constants.gen.ts](/Users/alejoamiras/.bun/install/cache/@aztec/constants@4.2.0-205b1fc419e5581b@@@1/src/constants.gen.ts:50)). `BATCH_SIZE = 12` is still under the limit ([balance-projector.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/token-balance/balance-projector.ts:34)), but the plan’s verification is stale.
- Silent fallback on `node.getNodeInfo()` is the wrong policy. The normal helper path also calls `node.getNodeInfo()` and `completeFeeOptions()` inside `buildTxExecutionRequest` ([nulo-account.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/aztec-runtime/src/account/nulo-account.ts:99)). Existing dApp fast-path code explicitly treats `getNodeInfo` as shared-fate and lets it propagate ([fast-path.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/fast-path.ts:160), [fast-path.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/fast-path.ts:169)). Catching it here just turns the same outage into a slower retry.

**MEDIUM findings**
- The “same block” claim is too strong. The fast arm is explicitly anchored, but `simulateTx` is not; it uses PXE’s live synced state when it eventually runs ([fast-path.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/fast-path.ts:179), [pxe/service.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/aztec-runtime/src/pxe/service.ts:288)). This is a best-effort race, not a correctness invariant.
- The trust boundary is broader than the plan says. Routing depends on ABI metadata from artifacts ([batched-view-simulation.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/helpers/batched-view-simulation.ts:205)), and contract registration can accept dApp-supplied artifacts if the class id matches ([service.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/service.ts:1474)). I would not frame this as only trusting the Noir compiler.
- `hideMsgSender` semantics are currently preserved by the helper for tx-typed public calls ([batched-view-simulation.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/helpers/batched-view-simulation.ts:235), [batched-view-simulation.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/helpers/batched-view-simulation.ts:278)), but `simulateViaNode` ignores that flag when building `PublicCallRequest` ([wallet-sdk utils.ts](/Users/alejoamiras/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/utils.ts:93)). Current callers may not rely on it; the helper contract does.

**NIT findings**
- `IPXE.getSyncedBlockHeader()` does exist on the Nulo PXE surface ([ipxe.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/aztec-runtime/src/pxe/ipxe.ts:49)), `getContractName: async () => undefined` is fine because it is only used for debug-log display ([wallet-sdk utils.ts](/Users/alejoamiras/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/utils.ts:153)), and your order-preservation claim is directionally right only because current 4.2.0 batches and `flatMap`s in order ([wallet-sdk utils.ts](/Users/alejoamiras/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/utils.ts:183), [wallet-sdk utils.ts](/Users/alejoamiras/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/utils.ts:220)).

**ADOPT**
- Restrict optimization to a leading public-static prefix, or move the optimization out of the generic helper and into caller-specific code that can prove independence.
- Fix the data model around fast-arm results: treat them as `NestedProcessReturnValues[]`, not `Fr[][]`, and extract `.values`.
- Split fallback into two cases: pre-dispatch prep failure can fold into the slow arm; post-dispatch generic failure requires an explicit full rerun, likely via `allSettled`.
- If you keep any PXE-anchor read, fetch it before launching eager utility writes, and document the slow-arm anchor as best-effort only.

**REJECT**
- Reject the “transparent helper-level mixed filter split” as currently proposed.
- Reject the “same block” wording; it is not true in the current architecture.
- Reject parity tests that compare full `TxSimulationResult` objects or gas/stats. For this helper, compare encoded/decoded values only.