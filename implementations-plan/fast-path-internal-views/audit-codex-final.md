ship-with-changes

**High**
- B1 is not fully fixed in the written plan. The design text says the projector should produce “all public first, then all private” ([plan-v2.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan-v2.md:36)), but the file-by-file section says to “just swap the two `if` blocks” ([plan-v2.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan-v2.md:183)). Those are not equivalent. Swapping per token turns today’s `[priv1, pub1, priv2, pub2]` into `[pub1, priv1, pub2, priv2]`, so the leading prefix still stops at the first private call in [balance-projector.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/token-balance/balance-projector.ts:110). If you want the full public arm optimized, this must be a two-pass enqueue over the chunk, not a local swap.
- Reordering in `projectChunk` is otherwise safe. The projector maps results back by `calls[i] -> (tbIndex, isPrivate, viewFn)` and writes into `perBalance` by token id ([balance-projector.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/token-balance/balance-projector.ts:147)). Nothing there depends on public/private interleaving order.

**Medium**
- B2 looks actually fixed. Mirroring the slow-arm `.values ?? []` unpack is the right contract, and it matches the current helper’s public path ([batched-view-simulation.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/helpers/batched-view-simulation.ts:163)). I don’t see a fresh blocker there.
- The `Promise.allSettled` rerun path composes with utilities if implemented exactly as described: launch `utility[]` once, rerun only the tx payload, then await the original utility queue once at the end ([plan-v2.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan-v2.md:167), [plan-v2.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan-v2.md:174)). I would make that invariant explicit in the plan: “full rerun does not recreate or re-await utility promises.”
- One residual edge remains: if the fast arm rejects with `SimulationError` and you propagate immediately ([plan-v2.md](/Users/alejoamiras/Projects/nulo/nulo-4/implementations-plan/fast-path-internal-views/plan-v2.md:203)), launched utilities are never awaited. That is also true in the current helper whenever the tx path throws before the utility loop ([batched-view-simulation.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/helpers/batched-view-simulation.ts:136)). Not new, but still worth noting.
- The `hideMsgSender` guard is semantically fine. Breaking the prefix at the first hidden public-static call is conservative and correct. Minor wording fix: at partition time you really have `FunctionCall.hideMsgSender`, not raw `hideSender`/`hideMsgSender` fields ([batched-view-simulation.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/helpers/batched-view-simulation.ts:230), [batched-view-simulation.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/execution/helpers/batched-view-simulation.ts:273)).

**Tests**
- #13 and #14 are good and pin real contracts, not paper invariants.
- #15 is fine if it is an invocation-order test with deferred promises and spy timelines. It will be brittle if implemented with timers or settlement ordering assumptions.
- Add one projector test that proves the new chunk order across multiple tokens, not just “public before private per token.” Current tests mostly assert lengths/result mapping, not global call ordering ([balance-projector.test.ts](/Users/alejoamiras/Projects/nulo/nulo-4/packages/extension/src/wallet/services/token-balance/balance-projector.test.ts:156)).

**Net**
- Most of my earlier findings are genuinely addressed.
- The only material unresolved issue is that v2 still contains two different projector reorder specs. If you correct that to a true public-first-then-private chunk build, I’d be comfortable with the plan.