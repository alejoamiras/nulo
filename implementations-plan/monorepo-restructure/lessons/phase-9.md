# Phase 9 — PR #186 open + dev-merge (#160) resolved
Full local gate green (typecheck/lint/test:all 2637 ext + 422 faucet + 154 wb, ci-gating, 5 builds, smoke pending CI). Opened PR #186 to dev with e2e:smoke+e2e:network labels (force both on the path diff). Dev had advanced: merged #160 (composition-test quality arc) — conflict was 1 CLAUDE.md content + 14 files #160 added inside the renamed packages/extension (git followed the rename into apps/extension); resolved by accepting at apps/ + repathing the COMPOSITION-TESTS.md comment refs. G9 back to ZERO. PR went DIRTY->BLOCKED (CI firing). Remaining: CI green (quality+smoke+network confirmed-fired), release rehearsal cut, /code-review max --fix, codex post-impl audit. ENV: 1Password SSH down all session (push/fetch via gh HTTPS; commits unsigned — re-signable before squash-merge); Foundry libs absent (forge gates env-deferred).

## Release rehearsal ✓ (codex post-impl + the user ask)
The rehearsal repo is a simplified orchestration mirror (no app paths). Ported the restructured `extra-files` (`apps/extension/package.json`) + a stub into its release-please-config, pushed a feat: to main. release-please opened `chore(main): release 0.6.0` (rehearsal PR #11) that bumps BOTH root package.json AND the relocated `apps/extension/package.json` 0.5.0->0.6.0 -> the relocated extra-files path is writable; release orchestration survives the repath. (The other release-path change, git-cliff `--include-path apps/extension/**`, was verified locally: non-empty notes.)
