# Phase 6 ✓ — release.yml + build reusables + setup-actions + root scripts
release.yml git-cliff --include-path -> apps/extension/**; release-please extra-files (both configs) -> apps/extension/package.json; _build-extension/_build-faucet/_smoke-e2e/_network-e2e --cwd + dist + cd -> apps/; setup-aztec/setup-puppeteer read apps/extension/package.json; root package.json 15 --cwd + e2e:agent(agent.sh) + typecheck(--project) -> apps/; check:imports 'biome check packages/' -> 'biome check apps/ packages/' (F4).
B1 (BLOCKING): _lint-and-typecheck.yml setForceLocal grep scope packages/ -> 'apps/ packages/'. Gates: git-cliff smoke non-empty (R2), B1 plant-probe — a setForceLocal in apps/extension/src/ IS caught (verified), faucet/playground/landing builds 0; chrome/firefox builds: stale workspace symlinks from the in-place move (apps/extension/node_modules/@nulo/* -> ../../../<lib>, now apps/<lib>) -> bun install re-linked -> rebuild (CI fresh-checkout unaffected).
