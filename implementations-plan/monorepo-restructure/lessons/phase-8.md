# Phase 7-8 ✓ — e2e harness paths + LIVE docs + archive freeze + G9 ZERO
Single hyphen-safe boundary sweep (rg -l0 | xargs -0 sed) across apps/ packages/ scripts/ contracts/ + root docs + per-package READMEs: packages/{extension,faucet,playground,landing} -> apps/, packages/bridge-{evm,aztec} -> contracts/bridge/{evm,aztec}; the [^-A-Za-z0-9_] boundary protects extension-messaging (a staying lib). Covers Phase 7 (docker-ci-like.sh:88 functional read; agent.sh/check-derivation-parity package-internal comments) + Phase 8 docs (CLAUDE/ARCHITECTURE/CI/README/AUDIT + per-pkg READMEs). 2 compiled Noir artifacts' embedded source-path metadata corrected (loadContractArtifact uses ABI/bytecode, not path).
ARCHIVES FROZEN: implementations-plan/, audit/, architecture/ (incl. plan/my-notes/research — all point-in-time), wallets-architecture-research/ — excluded from G9, not rewritten.
G9 = ZERO live moved-path refs. Gates: typecheck:all 0, lint 0, test:all 0 (clean-install), test:ci-gating 0. NOTE: a partial 'rm apps/*/node_modules' churn had transiently broken the wallet-bridge schema-patch singleton test; a full clean 'rm -rf node_modules && bun install --frozen-lockfile' (CI-equivalent) -> WB 154 pass. CI fresh-checkout is the canonical state.
