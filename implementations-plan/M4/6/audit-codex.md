# M4.6 — codex (xhigh) audit

Date: 2026-04-26

- **BLOCKING** `[plan.md:156]((project root)/implementations-plan/M4/6/plan.md:156)` under-scopes `ProfileService`. The plan only wires zeroization into the session-open paths, but live code still materializes plaintext or passhash in other hot paths: `confirmProfileOperation` unseals and discards `secret` `[service.ts:380]((project root)/packages/extension/src/wallet/services/profile/service.ts:380)`, `exportPlain` unseals then base64-encodes `[service.ts:511]((project root)/packages/extension/src/wallet/services/profile/service.ts:511)`, `exportMnemonic` unseals then derives words `[service.ts:534]((project root)/packages/extension/src/wallet/services/profile/service.ts:534)`, password `restore()` creates `plainSecret` and silently drops `seal()`’s returned `passhash` `[service.ts:640]((project root)/packages/extension/src/wallet/services/profile/service.ts:640)`, and passkey `restore()` derives `recovery.secret` but never uses it `[service.ts:678]((project root)/packages/extension/src/wallet/services/profile/service.ts:678)`. Change Step 5 to include these sites explicitly. Also tighten the import story: saying `importPasswordProfile` zeroes parameters `[plan.md:173]((project root)/implementations-plan/M4/6/plan.md:173)` is not enough because `importEncrypted`/`importPlain`/`importMnemonic` can throw before reaching it `[service.ts:432]((project root)/packages/extension/src/wallet/services/profile/service.ts:432)` `[service.ts:453]((project root)/packages/extension/src/wallet/services/profile/service.ts:453)` `[service.ts:463]((project root)/packages/extension/src/wallet/services/profile/service.ts:463)`. Wrap those public methods, not just the helper.
- **SHOULD-FIX** The “caller owns parameter buffers” contract is not yet a committed code change; it only appears as a checklist item `[plan.md:188]((project root)/implementations-plan/M4/6/plan.md:188)`. Current callee docs for `sealWithPasshash` / `unsealWithPasshash` `[password-secret-box.ts:89]((project root)/packages/wallet-crypto/src/password-secret-box.ts:89)` and `SessionManager.open` `[session-manager.ts:148]((project root)/packages/extension/src/wallet/services/profile/session-manager.ts:148)` do not tell maintainers they must zero after the call. Promote this from “check during review” to explicit JSDoc edits in the plan.
- **SHOULD-FIX** The `Fr` mitigation is close, but the test described at `[plan.md:216]((project root)/implementations-plan/M4/6/plan.md:216)` only mentions `Fr.fromBuffer(buf)`. The new passkey path uses `Fr.fromBufferReduce(Buffer.from(new Uint8Array(masterBits)))` `[passkey-credential.ts:45]((project root)/packages/wallet-crypto/src/passkey-credential.ts:45)`. Amend the plan so the self-test covers the exact post-change patterns (`fromBuffer(Buffer.from(...))` and `fromBufferReduce(Buffer.from(...))`), not just a nearby variant.
- **BLOCKING** `[plan.md:156]((project root)/implementations-plan/M4/6/plan.md:156)` under-scopes `ProfileService`. The plan only wires zeroization into the session-open paths, but live code still materializes plaintext or passhash in other hot paths: `confirmProfileOperation` unseals and discards `secret` `[service.ts:380]((project root)/packages/extension/src/wallet/services/profile/service.ts:380)`, `exportPlain` unseals then base64-encodes `[service.ts:511]((project root)/packages/extension/src/wallet/services/profile/service.ts:511)`, `exportMnemonic` unseals then derives words `[service.ts:534]((project root)/packages/extension/src/wallet/services/profile/service.ts:534)`, password `restore()` creates `plainSecret` and silently drops `seal()`’s returned `passhash` `[service.ts:640]((project root)/packages/extension/src/wallet/services/profile/service.ts:640)`, and passkey `restore()` derives `recovery.secret` but never uses it `[service.ts:678]((project root)/packages/extension/src/wallet/services/profile/service.ts:678)`. Change Step 5 to include these sites explicitly. Also tighten the import story: saying `importPasswordProfile` zeroes parameters `[plan.md:173]((project root)/implementations-plan/M4/6/plan.md:173)` is not enough because `importEncrypted`/`importPlain`/`importMnemonic` can throw before reaching it `[service.ts:432]((project root)/packages/extension/src/wallet/services/profile/service.ts:432)` `[service.ts:453]((project root)/packages/extension/src/wallet/services/profile/service.ts:453)` `[service.ts:463]((project root)/packages/extension/src/wallet/services/profile/service.ts:463)`. Wrap those public methods, not just the helper.
- **SHOULD-FIX** The “caller owns parameter buffers” contract is not yet a committed code change; it only appears as a checklist item `[plan.md:188]((project root)/implementations-plan/M4/6/plan.md:188)`. Current callee docs for `sealWithPasshash` / `unsealWithPasshash` `[password-secret-box.ts:89]((project root)/packages/wallet-crypto/src/password-secret-box.ts:89)` and `SessionManager.open` `[session-manager.ts:148]((project root)/packages/extension/src/wallet/services/profile/session-manager.ts:148)` do not tell maintainers they must zero after the call. Promote this from “check during review” to explicit JSDoc edits in the plan.
- **SHOULD-FIX** The `Fr` mitigation is close, but the test described at `[plan.md:216]((project root)/implementations-plan/M4/6/plan.md:216)` only mentions `Fr.fromBuffer(buf)`. The new passkey path uses `Fr.fromBufferReduce(Buffer.from(new Uint8Array(masterBits)))` `[passkey-credential.ts:45]((project root)/packages/wallet-crypto/src/passkey-credential.ts:45)`. Amend the plan so the self-test covers the exact post-change patterns (`fromBuffer(Buffer.from(...))` and `fromBufferReduce(Buffer.from(...))`), not just a nearby variant.
