# Post-implementation lessons — review + codex loop

- **The fresh-eyes Opus review (code-review max form) found 0 must-fix / 3 should-fix / 5 nits**, all adopted; its strongest catches were second-order interactions: my own short-circuit design would have let the UI walk users into terminal (live Retry button during the authorized run), the degraded branch leaked the attempt budget, and the engine's outer catch charged the episode for free failures. Its verified-clean list (checksum-of-invariants: fences, promise hygiene, decode edges, the gate suite's world model) is as valuable as the findings.
- **Codex post-impl round 1 REJECTED the review-pass fixes themselves** — the `spentAttempt:false` label was unsound (a throw escaping AFTER a landed bump read as free → ambient terminalization returned through the new door), and the marker/bump/clear ordering still had a double-count window. Pattern now twice-confirmed in this pipeline: every fix that touches shared-state lifecycle needs the same adversarial pass as the code it fixes; the re-review of fix diffs is where this class dies.
- **Priorities can legitimately invert as the design strengthens**: the original swallowing `markJournalCounted` optimized never-undercount; once `attemptRecorded` existed, non-swallowing became strictly better on BOTH axes (never-double preserved, the undercount recovered by the next resume). Recorded because the inversion — not either policy — is the lesson.
- **Codex consult ledger for the batch, complete:** plan R1 cond-approve (6 conditions) ∥ fable R1 cond-approve (5) → final fresh pass R1 REJECT (3 blockers) → R2 cond-approve → gate. Post-impl: fresh R1 REJECT (2 blocking, 1 major, 1 small; a first session died mid-run on a transient CLI failure — logged per contract, retried successfully) → R2 cond-approve (1 condition) → R3 **approve**. One standing conservative disagreement (audit/ red-proof retirement), logged in the ledger.
- Owner note mid-run: Cloudflare Pages builds on bun 1.4 and reds PR check rows — NOT in the required set; disregarded for merges per the owner's instruction.
