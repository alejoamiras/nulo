# Implementation lessons — data-safety (batch 5)

- All three fixes were implemented straight off rev 3 with revert-probes (N-06 ×2 red, N-24 ×3 red, N-20 ×4 red) before the review — the probe discipline is now reflexive.
- **Independent max review (Fable agent, own re-probes of every pin): REQUEST-CHANGES — one HIGH finding, a genuine regression MY fix introduced.** The occupied-skip cursor (itself a codex-R1 fold) created an unbounded synchronous loop at the MAX_SAFE_INTEGER boundary: past 2^53 the float `id++` is a no-op, so a hostile DECODABLE row at the boundary (bare `z.number()` id + key-identity accepts 2^53-1) wedged `restore` forever under the service-wide lock — a hostile-input DoS inside the change's own threat model, on the exact key value the sibling N-20 fix hardens against. The 1-row case silently wrote a key-identity-hidden row, contradicting the change's own inline comment. Fixed in `8119b612`: the safe-integer guard lives INSIDE the loop condition (a post-loop-only check still spins when both boundary keys are occupied) + per-row `restoreError`; boundary pin seeds both edge keys. Two stale comments corrected (allocator header; balance-repository's frozen-invariants block pinned the OLD expression).
- Review also verified: the awaited sweep fully closes the import race (sole real-service construction site + ensureInitialized gating); the sweep is fail-open in every hostile direction; the allocator is bit-identical to the old one on every legitimate store (51/51 pre-existing tests); no id-based ordering exists for gap-fill to disturb; every rev-3 specced test present; N-06's non-string-valued pin discriminates its targeted partial regression (full-revert timing pass is expected and covered by the control).
- Meta-lesson (the sharpest of the pipeline so far): **a reviewer-mandated fix is still new code with its own new failure modes** — the occupied-skip loop existed only because round-1 demanded raw-occupancy, and neither codex round nor my implementation pass caught the float-boundary interaction. The independent reviewer running its OWN adversarial probes (not just reading) is what caught it. Keep the review's probe mandate permanent.
