# A11 — codex (xhigh) audit

Date: 2026-04-26
Plan: implementations-plan/A11/plan.md (v0)

- `BLOCKING` [plan.md]((project root)/implementations-plan/A11/plan.md:26) is wrong about component auto-import. Vite only scans `src/components` [vite.config.ts]((project root)/packages/extension/vite.config.ts:137). Any new SFC under `popup/components/modules/...` in A11.2/A11.3/A11.5 must be explicitly imported by the parent, or the destination path must change. Make that a hard rule in the plan, not a risk note.
- `BLOCKING` `useExecutionPayload` is too wide unless you pin lifecycle ownership. The parent currently owns the long-lived clients and hooks [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:362), while `init()` locally creates and disconnects `AccountServiceClient` / `NetworkServiceClient` [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:135). Keep `connect()` / `disconnect()` in the parent. Extract either a one-shot `loadExecutionPayload(requestId, deps)` or a pure `enrichExecutionOperations(payload, profileId, deps)` helper.
- `BLOCKING` A11.5 is underspecified and partly stale. `CapabilityDetailPanel.vue` already does per-capability rendering [CapabilityDetailPanel.vue]((project root)/packages/extension/src/popup/components/modules/capabilities/CapabilityDetailPanel.vue:24), so “component per kind” / `CapabilityScopeTree.vue` is not anchored in current code. Rewrite A11.5 as concrete extracts from [capabilities/index.vue]((project root)/packages/extension/src/popup/windows/capabilities/index.vue:355): `IdentityStrip` [358], `DappIdentityBlock` [370], `AccountSelectionList` [397], and `CapabilityRequestList` for the new/existing cards [461], [512], plus an optional `useCapabilityRequestState()` around the label map and init flow [82], [136].
- `BLOCKING` The cleanup-order note is inaccurate. The real file disconnects `executionService` before clearing `estimateTimers` [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:397). Since this is a no-logic-change arc, the plan must state the actual order being preserved. `useFeeEstimation` should expose `dispose()` and let the parent call it from its existing `onUnmounted`, not own its own lifecycle.
- `NIT` Accept manual smoke for this arc. `connect-dapp.test.ts` is still skipped [connect-dapp.test.ts]((project root)/packages/extension/tests/e2e/connect-dapp.test.ts:5), so a tiny “popup loads” test would not meaningfully verify real request payload rendering.
- `NIT` The ≤400-line target is a good audit guardrail because both files still mix orchestration, view markup, and component-local CSS at 954-1064 lines [plan.md]((project root)/implementations-plan/A11/plan.md:13). Just do not add dummy indirection purely to hit `399`.
- `SHOULD-FIX` CSS isolation is the right call; passing parent CSS-module classes down is not. The fallback suggestion in [plan.md]((project root)/implementations-plan/A11/plan.md:157) is misleading because `<Component :class="$style.foo" />` only reaches the child root and will not preserve nested rules like `.prop :last-child` [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:1012). Move the local `identity_*`, `dapp_*`, `op_*`, and `prop` blocks wholesale with each extracted SFC.
- `SHOULD-FIX` Put ref semantics into the plan signatures. `dapp` and `operations` are refs [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:60), and derived values are computed from them [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:303), [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:322). Same pattern exists in capabilities [capabilities/index.vue]((project root)/packages/extension/src/popup/windows/capabilities/index.vue:58), [capabilities/index.vue]((project root)/packages/extension/src/popup/windows/capabilities/index.vue:109). Spell out `Ref<...>` in A11.1/A11.5.
- `SHOULD-FIX` Skip the extra `OpCard.vue` dispatcher unless you expect reuse. The only call site is the existing three-way branch [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:463). A wrapper plus three children just relocates the switch and makes `UIOperation` typing harder. Import `OpCardSendTransaction`, `OpCardAztecSendTx`, and `OpCardSimple` directly in the parent.
- `SHOULD-FIX` Swap A11.2 before A11.1. The identity strip and dApp block are self-contained template/style islands [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:410), [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:855). A11.1 touches request parsing, auth gating, payload enrichment, and service usage [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:111), [execute/index.vue]((project root)/packages/extension/src/popup/windows/execute/index.vue:368). After the `humanize.ts` pilot, the next safest PR is the visual extraction.
