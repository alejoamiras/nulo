# bun-native-tooling — Arc D of the Bun 1.4 adoption (`/blueprint light`)

Status: **APPROVED v5 — 2026-08-25**, per the owner's standing decision protocol: explicit fresh-context codex `approve` (session `01a037aa-…`, round 3) after codex rounds 1–3 and fresh-context rounds 1–2 were all folded with zero disputes (see [lessons/gate-convergence.md](lessons/gate-convergence.md)). Implementing. `eli5_mode: artifact` — ELI5 Artifact: https://claude.ai/code/artifact/7d4672c0-c282-42a4-afb1-e48b6270f065 (source: `implementations-plan/bun-native-tooling/eli5.html`; redeploy the same path to update). Owner absent; every clarifying question is resolved by the standing decision protocol (codex to convergence, explicit fresh-context approve). Reserved lines (merging, required checks, `@aztec/*`, publish/deploy, feature removal, irreversible data ops) are not touched.

## Goal

Replace the eighteen hand-rolled `node:child_process` calls in `packages/bridge-core/scripts/` with ONE argv-only process primitive whose contract is explicit (no shell string anywhere; throw-with-exit-code-and-stderr by default with `check: false` opt-out; secret-safe failure diagnostics; one binary-resolution rule), keep the domain helpers (`runForge`, `buildForkInL1Root`) on top of it, delete the two duplicated `forgeBin()`s and the raw `cast()`/`execSync` variants, un-pin the hardcoded `cast` path, close the target-program injection holes on the git and forge/cast inputs — with the primitive proven by real-subprocess tests on both engines and the scripts' observable behaviour proven preserved by a static per-site audit plus the key-free `verify-l1 --dry-run` run before and after. The e2e supervisor is out of scope (Arc C left the e2e configs on Node — the dossier's precondition is unmet). `--no-orphans` is NOT enabled anywhere; the arc records the controlled probe that would clear it.

**Done** = `run.ts` + `run.test.ts`; all six child-process files use it; `rg -n 'node:child_process|execSync|execFileSync|spawnSync' packages/bridge-core/scripts/*.ts` matches only `run.ts`; `bun run --cwd packages/bridge-core typecheck` + `test` green, and `run.test.ts` green under BOTH explicit engine invocations (`bun run --cwd packages/bridge-core vitest run scripts/run.test.ts` — Node by the bin's shebang — and `bun run --cwd packages/bridge-core --bun vitest run scripts/run.test.ts` — Bun), whatever the package `test` script is; `bun run test:all` + `bun run lint` green; `verify-l1 --dry-run` prints exactly its Phase 0 baseline for BOTH manifests (testnet: four ✓ lines; mainnet: the circle-proxy skip line + three ✓) before and after; every script's exit codes, soft-fail paths, accepted inputs and messages preserved except the three documented targeted fixes (no feature or input shape is removed); PR to `dev` open with the three required checks green at HEAD; codex loop converged.

## Why this tier (Phase 0.5 rubric)

Novelty LOW (a process primitive; the release scripts already shell natively), blast radius LOW–MED (operator-run deploy/verify/canary scripts — never in CI — but they move real funds on Sepolia/mainnet when an operator runs them, so a behaviour change is expensive to discover), irreversibility LOW (revert one PR), migration cost LOW, external coupling MED (forge/cast from the Aztec/Foundry toolchains), security MED (three shell strings interpolate unvalidated input; the deployer key is in the process env AND in `cast` argv). One MED-HIGH-ish dimension, single package → `light` (dossier tier confirmed).

## Clarifying questions — rulings (codex round 1, `01a03791-…`)

| # | Question | Ruling |
|---|---|---|
| Q1 | Bun-native (`Bun.spawnSync`/`Bun.$`) or Node-API hardening? | **Node-API hardening — approved.** Argv arrays already close shell injection; `Bun.$` adds argument-injection caveats (Bun's own docs), `Bun.spawnSync` has no throw-on-failure/`encoding`/`.status`, `bun-types` is not a dependency (lockfile churn colliding with Arc B's regen or an untyped dynamic import), and a Bun-only suite would red `test:all` on `dev` until Arc C merges. The dossier's Bun-native premise is declined on evidence; the arc keeps its dossier slot name and states the decline in the PR. |
| Q2 | Primitive contract | `run(bin, args, opts): RunResult` over `spawnSync` — argv only (never `shell`), `encoding: "utf8"`; options are PER CALL with Node's defaults otherwise (`cwd`, `env`, `stdio`, `maxBuffer` — 64 MiB only where the forge sites already set it; no global stdin/maxBuffer change; no `timeoutMs`). Throws `RunError` on spawn failure (ENOENT names the binary), signal death or non-zero exit; `{ check: false }` returns `{ exitCode, signal, stdout, stderr, code?: string }` for the callers that interpret failure themselves (`code` is the spawn error's code string; the raw spawn error is never returned). **Secret-safe — the precise guarantee: the primitive never formats or retains argv.** `RunError.message` is `"<bin> failed (exit N | signal S | spawn error CODE): <stderr tail, 4000 chars>"`; the object carries `bin`, `argc`, `exitCode`, `signal`, `stdout`, `stderr`, `code` (the spawn error's `code` string only — NOT the raw spawn error as `cause`, whose enumerable `spawnargs` would leak argv through `util.inspect`/`JSON.stringify`); `PRIVATE_KEY` travels in `cast` argv at `live-intent.ts:211,339`. `spawnSync` can also THROW synchronously before producing a result (an argv value containing a NUL byte raises `ERR_INVALID_ARG_VALUE` whose message echoes the value, on both engines): the primitive catches every synchronous throw and converts it to a `RunError` with the fixed reason `invalid argument` — the original error, its message and its stack are dropped. Three boundaries are OUTSIDE the guarantee and documented: argv is visible to other local processes (`ps`) while the child runs (`PRIVATE_KEY` at `live-intent.ts:211,339`, `ETHERSCAN_API_KEY` at `verify-l1.ts:133`); child-OWNED output (`stdout`/`stderr`) stays verbatim — a child that prints its own argv prints it; the inherited environment is the operator's. Three sentinel tests (a spawned non-zero child, an ENOENT spawn failure, and a NUL-bearing argument — each with a sentinel secret in argv, on both engines) assert the secret is absent from `message`, `stack`, `util.inspect(err)` and `JSON.stringify(err)`. |
| Q3 | Preserve the conductors' soft-fail and `runForge`'s non-zero interpretation? | **Yes, both, verbatim.** `deploy-bridge-{testnet:540,mainnet:509}` keep their ⚠-and-continue on a non-zero `verify-l1.ts` (`check: false`); `runForge()` (`verify-l1.ts:74-97`) keeps interpreting non-zero output (`/already verified/i`, dry-run JSON) via `check: false`; `runForge` and `buildForkInL1Root` stay as DOMAIN helpers — only their process primitive and the duplicated binary lookup collapse into `run.ts`. |
| Q4 | `--no-orphans` | **Off.** Bun's current docs (`run.noOrphans`): parent-death watch via `prctl(PR_SET_PDEATHSIG)` + a `/proc` DESCENDANT walk on Linux (kqueue + libproc on macOS, Job Object on Windows) — a descendant tree, not a process group, so a sibling agent's processes are out of reach by construction, but nothing here spawns long-lived children, so there is no consumer. The controlled probe that would clear it for the e2e supervisor (a later arc): an isolated fixture spawns `bun --no-orphans` as a parent that spawns a detached grandchild; the TEST separately spawns a non-descendant sentinel; the test kills ONLY the recorded parent PID, asserts the parent and its descendant are dead and the sentinel alive, verifies each PID's identity (start time) before any cleanup, and never touches a real sibling agent. |
| Q5 | Importability | Tests import only `run.ts`; no conductor gains exports or guard changes. (`live-intent.ts` is already guarded at `:605-608`; `portal-artifact.ts` is import-safe; the rest stay top-level.) |
| Q6 | Test strategy | Real subprocesses on both engines, no mocks, no runtime-conditional skips: success/stdout, non-zero → `RunError` (exitCode + stderr tail), `check: false` result shape, ENOENT → `RunError` naming the binary, signal death, `maxBuffer` overflow → `RunError`, the secret-sentinel assertion, `resolveBin` env override / candidate order / not-found, `git()`; the scripted child is `process.execPath` (the engine running the test) so both engines run every case with no chooser logic. No timeout coverage (no timeout feature). |
| Q7 | Base branch | `dev`; no `gh stack` on Arc C. |
| Q8 | Behaviour changes | Exactly three targeted fixes, everything else a behaviour-identical refactor: (1) `cast` resolution `CAST_BIN` → `~/.aztec/current/internal-bin/cast` → `~/.aztec/versions/5.0.0/internal-bin/cast` → PATH (`current` is the operator's chosen toolchain; the legacy pin stays as the fallback; `CAST_BIN=cast` selects PATH explicitly; `homedir()` replaces the unexpanded `"~"`); (2) argv-only git with the injection-closing forms below; (3) secret-safe failure diagnostics (today `execFileSync` throws with the full argv in the error — the deployer key). Input validation adds no behaviour change for valid input: `verify-l1.ts` strict-parses `forked-v1` manifests through `parseCandidateManifest` (the schema's stated contract) and keeps the legacy canonical-TokenPortal path (`--config` accepts arbitrary manifests; `verify-l1.ts:106-129` supports legacy shapes explicitly) with NARROW local validation of only the values that reach forge — the addresses forge receives (`l1.usdc`, `l1.portal`, `fuel.core.{router,permit2,feeJuicePortal,swapTarget}`, `fuel.swap.{poolManager,feeJuice,weth}` — the constructor-arg inputs; NOT `quoter`/`pools`/`slippageBps`/`minFuelFj`) via the schema's `evmAddress` primitive (exported), the `sourceContract` enum, and `l1ChainId` as a positive integer ONLY when present — an absent `l1ChainId` keeps today's Sepolia fallback (`verify-l1.ts:108`). No second general-purpose schema. |

## Recon → design (see [recon.md](recon.md))

- 18 call sites, 6 files, 4 file-scoped helpers (2 duplicated), 0 tests, 0 CI invocations; the scripts are the `aztec-update` operator runbook.
- Two patterns: A — argv arrays (cast/forge/bun sub-scripts; already shell-free), B — `execSync` shell strings for git (three of them interpolate unvalidated input).
- No shared primitive exists anywhere in the repo; the release scripts' Bun `$` convention keeps the shell wrapper untested behind a DI seam — the opposite of what a deploy-script primitive needs (the primitive IS the risky part; it gets the tests). Reuse: `candidateManifestSchema`/`parseCandidateManifest` (`src/candidate-schema.ts`) for the forge inputs; `requireAddress()` (`live-intent.ts:97-102`) for the cast inputs; Arc C's `cli.ts` `git()` error-message shape.
- Toolchain on the planning host: forge on PATH (`~/.foundry/bin`), `~/.aztec/current/internal-bin/cast` and the legacy `versions/5.0.0` cast both present → `verify-l1 --dry-run` is runnable key-free (baseline recorded in `lessons/phase-0.md`).

## Architecture & Implementation

1. **`packages/bridge-core/scripts/run.ts`** (new, ~90 lines, imports nothing local — no cycle by construction):
   - `export class RunError extends Error { readonly bin: string; readonly argc: number; readonly exitCode: number | null; readonly signal: NodeJS.Signals | null; readonly stdout: string; readonly stderr: string; readonly code?: string }` — message as in Q2; no argv and no raw spawn error anywhere on the object (`code` is the spawn error's `code` string, e.g. `ENOENT`).
   - `export function run(bin, args: readonly string[], opts?: { cwd?; env?; stdio?; maxBuffer?; check?: boolean }): RunResult` — `spawnSync(bin, [...args], { ...opts-without-check, encoding: "utf8" })` inside a `try` whose `catch` yields `{ code: "EINVAL-ARG" }`-shaped failure with the fixed reason `invalid argument` (nothing from the thrown error is retained); `shell` is not an option; `res.error` → `RunError` (code only), `res.signal` → `RunError`, `res.status !== 0` → `RunError`, unless `check === false` (then the caller gets `{ exitCode: res.status, signal: res.signal, stdout, stderr, code?: res.error?.code }` — the raw error object is never returned either).
   - `export function resolveBin(name: string, opts: { envVar: string; candidates: readonly string[]; prefer: "path" | "candidates" }): string` — `process.env[envVar]` wins verbatim; then the `prefer`-controlled order: `prefer: "path"` = PATH probe (`run(name, ["--version"], { check: false, stdio: "ignore" })` exit 0 → the bare `name`) THEN the first candidate that `existsSync`; `prefer: "candidates"` = the first existing candidate THEN the PATH probe; nothing resolves → throw `"<name> not found — set <envVar>, install it on PATH, or provide one of: <candidates>"`. Forge: `resolveBin("forge", { envVar: "FORGE_BIN", candidates: [~/.aztec/current/bin/forge], prefer: "path" })` = today's order (env → PATH → `~/.aztec/current/bin/forge`). Cast: `resolveBin("cast", { envVar: "CAST_BIN", candidates: [~/.aztec/current/internal-bin/cast, ~/.aztec/versions/5.0.0/internal-bin/cast], prefer: "candidates" })` = Q8's order. Both existing orders are expressible without branching in the callers. **Resolution is lazy**: `forgeBin()`/`castBin()` resolve on first use and memoize — never at module evaluation — so `fuel-testnet.ts`, `fpc-dust-canary-mainnet.ts` and `smoke-existing-mainnet.ts`, which import only `live-intent.ts`'s signer constants, never require `cast` to exist (today's `CAST` constant is a path string, also inert until used).
   - `export function git(args: readonly string[], cwd: string): string` — `run("git", args, { cwd }).stdout.trim()`; eight sites.
2. **Call-site migration, one commit per file** (order: `portal-artifact.ts` → `verify-l1.ts` → `live-intent.ts` → `restore-swap.ts` → `deploy-bridge-testnet.ts` → `deploy-bridge-mainnet.ts`), each preserving exit codes, the soft-fail branches (`check: false` + the existing ⚠ log), `stdio: "inherit"` where output streams to the operator, the `FOUNDRY_PROFILE`-stripped env and `maxBuffer` 64 MiB on the forge sites, and message texts. Injection-closing forms (git 2.53 verified): `restore-swap.ts:97` → `const oid = git(["rev-parse", "--verify", "--end-of-options", `${fromRef}^{commit}`], repoRoot)` then `git(["show", "--end-of-options", `${oid}:apps/faucet/public/testnet-bridge.json`], repoRoot)`; `live-intent.ts:316` → `intent.source.commit` checked against `/^[0-9a-f]{40}$/` then `["diff", "--name-only", "--end-of-options", oid, "HEAD", "--"]`; `live-intent.ts:290` → `["--literal-pathspecs", "status", "--porcelain", "--", intentPath]`; the static `rev-parse HEAD` / full-tree `status` / fixed `show HEAD:path` need no separator. Forge inputs: `verify-l1.ts` parses a `forked-v1` `CONFIG_PATH` through `parseCandidateManifest`; a legacy manifest keeps its existing branch with narrow validation of exactly the values forge receives (`evmAddress.parse` on `l1.usdc`, `l1.portal`, `fuel.core.{router,permit2,feeJuicePortal,swapTarget}`, `fuel.swap.{poolManager,feeJuice,weth}`; the `sourceContract` enum; `l1ChainId` a positive integer only when present, absent → the existing Sepolia fallback) — Q8. Cast inputs: `intent.signer` goes through `requireAddress()` before `live-intent.ts:406,416`; `PRIVATE_KEY` is shape-checked with `/^(?:0x)?[0-9a-fA-F]{64}$/` (cast accepts both forms today) before `:211,:339`; the node-derived addresses already are. Deletions: both `forgeBin()`s, `cast()`, the raw `spawnSync`/`execSync`/`execFileSync` imports.
3. **`run.test.ts`** (colocated, vitest, real subprocesses) — the Q6 list.
4. **Docs**: `packages/bridge-core/README.md` (scripts table gains `verify:l1` + the rule: "scripts spawn ONLY through `run.ts` — argv arrays, never a shell string; `CAST_BIN`/`FORGE_BIN` overrides; the primitive never formats or retains argv in a failure"), `.env.example` (`CAST_BIN`), adoption-map Arc D status + the corrected premise/claims (`Bun.$` in the release scripts, `kill()` return type, `noOrphans` semantics) + the probe, `implementations-plan/index.md`. No CLAUDE.md change (package-local rule).

## Phases

### Phase 0 — Primitive + tests + dry-run baseline ✓ (gate passed 2026-08-25 — [lessons/phase-0.md](lessons/phase-0.md))

`run.ts` + `run.test.ts`; nothing else changes. Record the pre-migration `verify-l1 --dry-run` output for BOTH manifests in `lessons/phase-0.md` (testnet: the default config; mainnet: `--config apps/faucet/public/mainnet-bridge.json` — chain 1, the `circle-proxy` token skip, its own validation path). Validation gate — `bun run --cwd packages/bridge-core typecheck && bun run --cwd packages/bridge-core test && bun run --cwd packages/bridge-core vitest run scripts/run.test.ts && bun run --cwd packages/bridge-core --bun vitest run scripts/run.test.ts && bun run lint`. The two explicit invocations pin the engines regardless of what the package `test` script is: `bun run … vitest run` executes the vitest bin by its shebang (Node 24.18, verified `vitest/4.1.10 linux-x64 node-v24.18.0`), `bun run … --bun vitest run` executes it on Bun — so the gate stays honest before AND after Arc C merges. Pass: all exit 0. Layers: typecheck · unit (both engines, explicitly) · lint.

### Phase 1 — Call-site migration (six commits) ✓ (gate passed 2026-08-25 — [lessons/phase-1.md](lessons/phase-1.md): 18-row audit, `rg` → `run.ts` only, both dry-runs identical to the baselines)

Validation gate — after EACH file: `bun run --cwd packages/bridge-core typecheck && bun run --cwd packages/bridge-core test && bun run lint`; after the last: the `grep` in Done returns only `run.ts`; the static behaviour audit per site in `lessons/phase-1.md` (18 rows: old API → new call; `cwd`/`env`/`stdio`/`maxBuffer` semantics; `.trim()` on captured stdout where the old code trimmed; exit-code path; soft-fail preserved?; input validation added?; resolution lazy?); `verify-l1 --dry-run` for BOTH manifests prints exactly the Phase 0 baseline lines (testnet: four ✓; mainnet: the circle-proxy skip line + three ✓ — key-free, forge on PATH; if the toolchain is unusable on the running host, record the exact blocker instead). Live conductors (`live-intent`, `deploy-bridge-*`, `restore-swap`) cannot be smoke-run without keys and a network — the next `aztec-update` run is their operational proof (owner Ask A2). Layers: typecheck · unit · lint · static audit · key-free dry-run.

### Phase 2 — Docs + dossier corrections ✓ (gate passed 2026-08-25 — [lessons/phase-2.md](lessons/phase-2.md))

README, `.env.example`, adoption-map, `implementations-plan/index.md`. Validation gate — `bun run lint && bun run test:all`; `git diff --name-only <last Phase 1 commit>..HEAD` is `*.md` + `.env.example` only. Pass: exit 0, diff conforms.

## Security & Adversarial Considerations

- **Target-program (argument) injection**: closed per site — git via `--end-of-options`/`--`/`--literal-pathspecs` on the sites that take variable input; forge via the strict manifest schema (`evmAddress` regex, enums, int chain id); cast via `requireAddress` on every address argument and a shape check on the key. The primitive never interprets a string as a command.
- **Secrets**: `PRIVATE_KEY` reaches `cast` as argv (pre-existing; visible in `ps` on the operator host) — unchanged here, owner Ask A3 (`cast wallet` keystores are the alternative). New guarantee: the primitive never formats or retains argv (message, fields, stack, `cause`, synchronous throws). Outside it, documented: argv is visible in `ps` while a child runs (`PRIVATE_KEY`, `ETHERSCAN_API_KEY`); child-owned output stays verbatim; the inherited environment is the operator's.
- **Supply chain**: no new dependency; no workflow change; no `bun.lock` change.
- **Blast radius**: operator scripts moving real value — behaviour preservation is the gate; one commit per file for bisectable revert.
- **Multi-agent host**: nothing spawns long-lived children or kills by name; `--no-orphans` stays off.

## Assumptions

**Facts (verified)**: F1–F9 in `recon.md` (F3's provenance is the bun-types **1.3.14** cache, not 1.4 declarations; F9 is the Bun docs' `run.noOrphans` semantics); the 18-site table; `import.meta.main` is `true` for the entrypoint under both `bun -e` and Node 24.18 ESM — not used by this arc; bridge-core `test` on `dev` is `vitest run` (Node; Arc C's `bun --bun` flip lives on the unmerged #459), `typecheck` is `tsc --noEmit -p tsconfig.scripts.json` (`types: ["node"]`; excludes `deploy-sandbox.ts` + `*.local.ts`); git 2.53.0 accepts `--end-of-options` and `--literal-pathspecs`; both live manifests are `forked-v1` with `l1ChainId`; `verify-l1 --dry-run` baselines: testnet — TestUsdc (1 source), NuloTokenPortal (89), SwapBridgeRouter (4), UniswapFuelSwap (1), exit 0; mainnet (`--config apps/faucet/public/mainnet-bridge.json`) — the circle-proxy skip line, NuloTokenPortal (89), SwapBridgeRouter (4), UniswapFuelSwap (1), exit 0; the explicit engine invocations verified on `deployer-keys.test.ts` (Node: `vitest/4.1.10 linux-x64 node-v24.18.0`; Bun: `--bun`).

**Inferences (to challenge)**: I1 no operator depends on the `5.0.0` cast when `~/.aztec/current` exists (the new order prefers `current`; the read-only subcommands used — `code`, `call`, `balance`, `wallet address` — are stable across Foundry versions); I2 `spawnSync` under Bun honours `maxBuffer`/`encoding` like Node for these sizes (pinned by the test on both engines).

**Design guarantees (not inferences)**: `run.ts` imports nothing local → no cycle.

**Decided for this arc — owner follow-ups, not blockers**: A1 Node-API hardening instead of Bun-native (Q1; reverse by directing a Bun-native primitive stacked on Arc C); A2 the live conductors are proven by the next `aztec-update` run, not here (an operator dry-run is the alternative); A3 `PRIVATE_KEY` (and `ETHERSCAN_API_KEY`) stay CLI arguments to `cast`/`forge` — pre-existing, visible in `ps` (a keystore is the follow-up); A4 the deploy conductors keep their soft-fail on Etherscan verification (F7; hard-fail is a follow-up). FYI: the legacy canonical-TokenPortal path in `verify-l1.ts` is unreachable for every manifest on disk (both are `forked-v1`) and is kept, validated — deleting it is yours to call.

## Decision ledger

| # | Finding (source) | Disposition |
|---|---|---|
| D-1 | Bun-native migration buys no security and adds type/branch coupling (plan I4; codex r1 ruling Q1) | **Adopted**: Node-API hardening; dossier premise declined on evidence |
| D-2 | `RunError` with raw argv would persist `PRIVATE_KEY` (codex r1 High) | **Adopted**: no argv on the error object; sentinel-secret test |
| D-3 | Argv closes shell injection, not hostile flags: git separators, forge/cast input validation (codex r1 High) | **Adopted**: the per-site forms above; `parseCandidateManifest` in verify-l1; `requireAddress` + key shape check in live-intent |
| D-4 | `runForge` interprets non-zero output; conductors soft-fail (codex r1 High) | **Adopted**: `check: false` at those sites; domain helpers kept |
| D-5 | Proposed cast order not behaviour-preserving (codex r1 High) | **Adopted**: `CAST_BIN` → current → legacy → PATH |
| D-6 | Per-call options only; drop `timeoutMs` (codex r1 Med) | **Adopted** |
| D-7 | `--no-orphans` probe must be controlled; docs now specify semantics (codex r1 Med) | **Adopted**: controlled fixture design; still off; recon corrected |
| D-8 | Honest dual-engine gate; run the key-free dry-run (codex r1 Med) | **Adopted**: Phase 0/1 gates; baseline recorded |
| D-9 | Q5 premise, I3 bucket, F3 provenance (codex r1 Low) | **Adopted** |
| D-10 | Legacy TokenPortal branch becomes unreachable under strict validation (plan v2 proposed deleting it) | **Rejected by codex r2** — `--config` accepts arbitrary manifests and the branch is explicit legacy support; deletion is feature removal (owner-reserved). **Adopted instead**: strict-parse `forked-v1`; narrow validation of the forge-bound fields on the legacy path; no second schema |
| D-11 | `RunError.cause` = the raw spawn error leaks argv via its enumerable `spawnargs` (codex r2 High) | **Adopted**: `code` string only; ENOENT sentinel test added |
| D-12 | "never echoes argv" overclaims while `stderr` is retained (codex r2 Med) | **Adopted**: guarantee reworded — never formats/retains argv; child output verbatim |
| D-13 | Accept bare 64-hex keys too (codex r2 Med) | **Adopted**: `/^(?:0x)?[0-9a-fA-F]{64}$/` |
| D-14 | `resolveBin(prefer)` justified; keep forge centralized (codex r2 Low) | **Adopted** |
| D-15 | `spawnSync` throws synchronously on a NUL-bearing argv with the value in the message (fresh r1 Med) | **Adopted**: catch-and-convert with a fixed reason; NUL sentinel test on both engines |
| D-16 | Only the testnet manifest was dry-run (fresh r1 Med) | **Adopted**: both manifests baselined (mainnet: chain 1, circle-proxy skip) and re-run |
| D-17 | Binary resolution must stay lazy; the audit records stdout `.trim()` and option semantics (fresh r1 Med) | **Adopted** |
| D-18 | The dual-engine gate must survive Arc C merging (fresh r1 Med) | **Adopted**: explicit `bun run … vitest run` (Node by shebang) + `bun run … --bun vitest run` invocations |
| D-19 | Document the three boundaries (argv in `ps`, child output, inherited env); qualify `test-soak/cli.ts` provenance; A1–A4 are decisions (fresh r1 Low) | **Adopted** |

## Post-implementation

1. `/code-review max --fix` on `git diff <base>..HEAD -- . ':!implementations-plan'` → separate `fix(review)` commit.
2. Codex audit (`/codex xhigh`, fresh): net diff + this plan + ledger + the adversarial ask + the no-over-engineering and comment-quality rules verbatim (as in Arc C).
3. Verify-then-fix loop, resumed session, until no new material findings; >3 rounds → surface.
4. Delivery.

## Delivery

One branch (`worktree-bun-native-tooling`) off `dev`, one PR via `gh pr create`, title ≤ 93 chars: `refactor(bridge-core): argv-only process primitive for the deploy scripts`. Body: the primitive's contract, the 18-site digest, the three targeted fixes + the validation added, the Bun-native decline with its evidence, the dry-run before/after, the `--no-orphans` probe, the owner asks. `gh pr checks --watch`; assert `quality-status`, `smoke-e2e-status`, `network-e2e-status` SUCCESS at HEAD (the e2e ones emit pass-on-skip: the diff touches no e2e surface). Merge is the owner's.

Rollback: revert the PR (six per-file commits are individually revertible).

## Seeds (FINAL — approval attached no conditions; identical to the ELI5)

```
/goal All phases (0–2) marked ✓ in implementations-plan/bun-native-tooling/plan.md, each ✓ backed by its validation gate as written there passing in the transcript (typecheck + bridge-core tests + run.test.ts under both explicit engine invocations + lint for Phase 0; the per-file gate, the rg proving no direct child_process call outside run.ts, the 18-row static audit and the identical verify-l1 --dry-run output for both manifests for Phase 1; lint + test:all + a docs-only diff for Phase 2); for each phase `LESSONS_FILE=implementations-plan/bun-native-tooling/lessons/phase-N.md` printed; `/code-review max --fix` complete with findings applied and committed separately; the codex fix loop converged (a resumed codex pass reporting no new material findings, quoted); ONE PR to dev exists, created only after convergence, with quality-status, smoke-e2e-status and network-e2e-status all SUCCESS at its HEAD; `bun run test:all` and `bun run lint` exit 0 in the transcript. Reserved for the owner: merging, deleting the legacy TokenPortal path, the key-in-argv question, the soft-fail policy, anything touching @aztec/* or required checks.
```

```
/loop 15m Drive implementations-plan/bun-native-tooling forward. Never idle waiting for my input. Each firing: (1) read plan.md + lessons/ (authoritative), `git status`, `git log --oneline -5`; if a PR exists, `gh pr view --json statusCheckRollup`. (2) Waiting on CI is fine — confirm it is progressing; use the wait to prepare the next step. (3) No task in hand? take the next pending step from plan.md; after each edit run `bun run lint` + `bun run --cwd packages/bridge-core typecheck` + `bun run --cwd packages/bridge-core test`; commit → push. (4) A decision you'd bring to me? call `/codex xhigh` with full context, iterate to a defensible decision, act, log the consult in lessons/phase-N.md; never merge, never delete the legacy path, never touch @aztec/* or required checks. (5) Same step failed 5×? stop, reassess with codex. (6) Phase green = its validation gate in plan.md passes as written: paste the result, mark ✓, write the lessons entry, print `LESSONS_FILE=…`, advance. (7) All phases ✓? run the Post-implementation section (code-review → codex loop → delivery), write the wrap-up (every codex-debated decision with ELI5 context), surface and stop.
```
