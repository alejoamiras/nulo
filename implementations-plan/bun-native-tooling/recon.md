# Recon — bun-native-tooling (Arc D of the Bun 1.4 adoption)

Four read-only sweeps (2026-08-25, base `origin/dev`): (1) every child-process call site in `packages/bridge-core/scripts`, (2) reusable spawn helpers repo-wide, (3) how those scripts are tested and invoked, (4) the Bun 1.4 process-API facts the dossier relies on, checked against the installed runtime and Bun's own bundled docs/types. The dossier's Arc D entry is [`../bun-1.4-adoption/adoption-map.md`](../bun-1.4-adoption/adoption-map.md) § Arc D.

## Reuse map

| Capability needed | What exists | Verdict |
|---|---|---|
| Run a binary with an argv array, capture output, fail loudly | 18 inline `node:child_process` calls in 6 files; four file-scoped helpers, two of them duplicated (`forgeBin()` ×2: `verify-l1.ts:47-55` exits, `portal-artifact.ts:47-53` throws; `cast()` `live-intent.ts:89-91`; `runForge()` `verify-l1.ts:74-97`; `buildForkInL1Root()` `portal-artifact.ts:84-107`). No shared runner anywhere in `scripts/` or `src/`. Nearest shapes elsewhere: `scripts/ci-cd/test-soak/cli.ts:241-245` `git(args, cwd)` (throws `<cmd> <args> failed: <stderr>`) — on Arc C's branch (PR #459), NOT in this `dev`-based worktree; the release scripts' Bun `$` convention (below) | **build new, minimal**: one `packages/bridge-core/scripts/run.ts` (argv-only, never a shell string; throws with exit code + stderr tail, `check: false` opt-out; never formats or retains argv) — only the process primitive and the duplicated binary lookup collapse into it; `runForge()` and `buildForkInL1Root()` stay as domain helpers on top (they interpret non-zero output / parse artifacts) |
| Bun-native shelling precedent | `scripts/release/open-sync-pr-run.ts:112-198`, `scripts/release/auto-unstick-run.ts:122-184`: `const { $ } = await import("bun")` inside `if (import.meta.main)`; `.nothrow().quiet()` for probes, bare (throwing) tags for must-succeed mutations; pure logic tested through an injected `SyncIO`/`UnstickIO` fake, the `$` wrapper deliberately untested. The dossier's "no Bun.$ in the release scripts" (adoption-map.md:17) was a grep artefact — the literal `Bun.$` never appears; `$` is destructured from the dynamic import | **not reused** — Arc D stays on `node:child_process` (codex r1 ruling Q1: argv arrays already close shell injection; Bun-native adds type/branch coupling for no security gain). The precedent matters as evidence only: it shows `Bun.$` IS used in the repo, contrary to the dossier |
| Bun type declarations | `bun-types`/`@types/bun` is NOT a dependency of this repo (0 hits in every `package.json`, every `tsconfig` `types` array — bridge-core: `["node"]`); only a machine-global 1.3.14 cache copy exists | **avoid** — no dependency change in this arc (a lockfile edit would collide with Arc B's regenerated lockfile); typed access through `await import("bun")` narrows to what the call site needs |
| Shell-string git plumbing | `restore-swap.ts:97` (`git show ${fromRef}:…` — `fromRef` straight from argv, unvalidated), `live-intent.ts:290` (`JSON.stringify` as pseudo-quoting — `$()`/backticks/`$VAR` still expand inside POSIX double quotes), `live-intent.ts:316` (`intent.source.commit` from a type-cast, unvalidated JSON) + five static `execSync("git …")` strings | **replace** with argv forms that also close ARGUMENT injection: `rev-parse --verify --end-of-options "<ref>^{commit}"` → OID → `show --end-of-options <oid>:path`; `diff --name-only --end-of-options <oid> HEAD --` after a 40-hex check; `git --literal-pathspecs status --porcelain -- <path>` (plan.md § Architecture 2) |
| Process-group / long-lived child control | e2e supervisor (`apps/extension/tests/e2e/{global-setup,lockfile,reap}.ts`: `spawn(...,{detached:true})`, `killProcessGroup`, `killOrphanByPid`) and `test-soak/cli.ts` (`waitForExit`, group kill + sweep) | **not needed** — every bridge-core call is a one-shot synchronous run; the e2e supervisor stays on Node (Arc C did not move the e2e configs — dossier precondition unmet) |
| Tests for script code | 4 of 25 scripts tested (`deploy-canonical-private-fpc`, `deploy-manifest`, `deployer-keys`, `script-bootstrap`): pure modules with named exports, `vi.mock` of one-level-removed factories, REAL `node:fs` in an OS temp dir, injected `env`. Nothing mocks `child_process`; nothing runs a real subprocess; Arc C's `test-soak/cli.test.ts` (PR #459 branch, not here) runs real subprocess fixtures on both engines | **adapt**: the runner gets its own colocated test that runs REAL subprocesses (`bun -e`, `git --version`, a non-zero exit, ENOENT) — no mocks of `child_process` |
| Script entry-point guard | hand-rolled `isMain` (`fileURLToPath(import.meta.url) === resolve(process.argv[1])`) in `live-intent.ts:607-608`, `check-fpc-version.ts:276-277`; `import.meta.main` appears nowhere in the dir; 17 scripts have zero exports and run at module top level | **adopt** `import.meta.main` (Bun-native, also Node ≥ 24) ONLY where a file must become importable for its test; leave pure top-level conductors alone |
| Binary resolution | `forge`: `FORGE_BIN` → PATH probe → `~/.aztec/current/bin/forge`; `cast`: HARDCODED `~/.aztec/versions/5.0.0/internal-bin/cast` (`live-intent.ts:68`; the `"~"` fallback when `HOME` is unset is never expanded); CI installs forge/cast only via `.github/actions/setup-aztec` (network-e2e), never for these scripts | **fix in passing**: one `resolveBin(name, envOverride, fallbacks)` in the runner module; `cast` gains `CAST_BIN` + the same `~/.aztec/current/internal-bin` fallback (behaviour-preserving default order) |
| CI coverage of the scripts | none — no workflow/action/package.json invokes any of the 25 deploy/verify/canary/live-intent scripts; they are an operator runbook (`.claude/skills/aztec-update/SKILL.md:58,96-108`); only `tsc -p tsconfig.scripts.json` and the vitest suite (`test:all`) see them; `verify:deployments` is a faucet script gating the faucet build offline | validation must be local: typecheck + the runner's real-subprocess tests + `bun run test:all` + lint; the scripts' live behaviour is proven by the next `aztec-update` run, not by this arc |
| `--no-orphans` | locally only the CLI help line exists ("Exit when the parent process dies, and on exit kill every descendant."; the bundled 1.3.14 docs predate it). Bun's current online docs (`bunfig` → `run.noOrphans`, = `--no-orphans` / `BUN_FEATURE_FLAG_NO_ORPHANS=1`) specify it: parent-death watch via `prctl(PR_SET_PDEATHSIG)` + a `/proc` DESCENDANT walk on Linux, kqueue `EVFILT_PROC` + libproc walk on macOS, a kill-on-close Job Object on Windows — a descendant tree, not a process group. The only consumer that would benefit (the e2e supervisor) stays on Node | **do not enable** in this arc; the controlled probe that would clear it is in plan.md Q4 |

## Verified facts (F) and corrected dossier claims

- F1 `bun --version` 1.4.0 (`~/.bun/bin/bun`, `~/.bun-versions/1.4.0`); no 1.3.x binary remains on the machine, so "new in 1.4" claims (detached forwarding, live `process.ppid`, `child.kill()` false-on-dead) cannot be A/B-verified here.
- F2 `Bun.$` is literal-by-default (Bun's bundled `shell.mdx`: no system shell; interpolated values are single literal strings; `$.escape`, `{ raw }` opt out). Its "Argument injection" section warns that a literal argument can still be a hostile FLAG to the target binary — the class the dossier flags for cast/forge. `Bun.spawnSync([bin, ...args])` has no parser at all.
- F3 Types (bun-types 1.3.14 `bun.d.ts:6696-7306`, `shell.d.ts`): `SpawnOptions` has cwd, detached, env, stdio/stdin/stdout/stderr (`"pipe"|"inherit"|"ignore"|…`), onExit, signal, timeout, killSignal (default SIGTERM), maxBuffer — and **no `encoding`** (stdout/stderr are `Buffer`/`ReadableStream`). `SyncSubprocess` has `exitCode`, `success`, `signalCode?`, `exitedDueToTimeout?`, `exitedDueToMaxBuffer?` — **no `.status`**. `Subprocess.kill(): void`. `$.ShellError extends Error { stdout, stderr, exitCode }`; non-zero exits throw unless `.nothrow()`.
- F4 Corrected dossier claims: `child.kill()` "false-on-dead" conflates Node's `ChildProcess.kill(): boolean` with Bun's `Subprocess.kill(): void`; "no Bun.$ in the release scripts" is false (see reuse map).
- F5 Semantic diffs a naïve swap would hit: `execFileSync`/`execSync` THROW on non-zero exit but `Bun.spawnSync` does not (needs `if (!res.success) throw`); five `spawnSync(...).status` sites (`verify-l1.ts:50,90`, `portal-artifact.ts:49,94`, `deploy-bridge-{mainnet:510,testnet:541}`); `encoding: "utf8"` strings become Buffers (`verify-l1.ts:78,81` rely on implicit coercion); `maxBuffer` overflow surfaces as `exitedDueToMaxBuffer` + `killSignal`, not Node's `ERR_CHILD_PROCESS_STDIO_MAXBUFFER`; `stdio` vocabulary is compatible; no bridge-core code reads fields off a caught exec error.
- F6 Bun's `node:child_process` is 🟡 partial (nodejs-compat.mdx: missing `proc.gid/uid`, `Stream`, IPC socket handles) — and these scripts ALREADY run on it under `bun` since Arc A; none of the gaps is exercised.
- F7 Deploy conductors soft-fail their own verification (`deploy-bridge-{testnet:540,mainnet:509}` log ⚠ and continue on a non-zero `verify-l1.ts` exit; gated on `ETHERSCAN_API_KEY`), while `rebuildAndVerifyPortal()` at `:139`/`:153` throws. Pre-existing behaviour; preserved verbatim (an operator-facing semantics change is out of scope — pinned in the plan's Asks).
- F8 Arc A's phase-5 lesson: `Bun.spawnSync` blocks the event loop (deadlocked a `Bun.serve` registry) — irrelevant to these CLI scripts, which have no concurrent listener.
- F9 `run.noOrphans` semantics per Bun's current online docs (see the reuse map row); F3's type facts come from the bun-types **1.3.14** cache, not 1.4 declarations. Planning-host toolchain: forge on PATH (`~/.foundry/bin/forge`), `~/.aztec/current/internal-bin/cast` (no `current/bin/forge`) and `~/.aztec/versions/5.0.0/internal-bin/cast` both present; git 2.53.0 (`--end-of-options`, `--literal-pathspecs` verified). Both live manifests (`apps/faucet/public/{testnet,mainnet}-bridge.json`) carry `portalSource: "forked-v1"` and `l1ChainId` → `verify-l1.ts`'s legacy TokenPortal branch (`placePortalSource()`) is unreachable for every manifest on disk. `verify-l1 --dry-run` (key-free) baseline on unmodified `dev`: TestUsdc 1 source, NuloTokenPortal 89, SwapBridgeRouter 4, UniswapFuelSwap 1, exit 0.

## Search trail (absence claims)

`Bun.$`/`Bun.spawn`/`Bun.spawnSync` literal repo-wide → 0 (the release scripts use a destructured `$`); `execFile` outside bridge-core → 0; execa/cross-spawn/zx/node-pty → 0; a shared run/sh/exec helper in `scripts/`+`src/` → 0 (only the Promise dispatcher `live-intent.ts:619`); `import.meta.main`/`require.main` in bridge-core scripts → 0; `vi.mock` of child_process repo-wide → 0; `*.spec.ts` repo-wide → 0; any workflow/action/package.json/CLAUDE.md/README referencing the six child-process scripts → 0 (aztec-update skill only; `deploy-bridge-mainnet.ts` is undocumented even there); `bun-types` anywhere in the repo → 0; "orphan" in Bun's bundled docs → 0; a 1.3.x bun binary → 0; `setsid`/`pgid`/`no-orphans`/`~/.agents` in code → 0.
