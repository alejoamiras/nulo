Verdict: **hold + fix**

1. `HIGH` [packages/extension/src/popup/windows/capabilities/index.vue:364](../../packages/extension/src/popup/windows/capabilities/index.vue:364) still has the same class of readiness race. `Approve` is gated only on `!requestId`, but `approve()` depends on async state populated in `init()` at [capabilities/index.vue:94](../../packages/extension/src/popup/windows/capabilities/index.vue:94) and reads `payload.value!` / `capabilities.value` at [capabilities/index.vue:160](../../packages/extension/src/popup/windows/capabilities/index.vue:160). `useDappInteractionPayload.load()` sets `requestId` before payload resolution at [useDappInteractionPayload.ts:82](../../packages/extension/src/composables/useDappInteractionPayload.ts:82), so a fast click can still approve an empty grant set. This is the one merge blocker.

2. `MEDIUM` [packages/extension/src/popup/components/popups/ChangeAuthwitsRegistryPopup.vue:66](../../packages/extension/src/popup/components/popups/ChangeAuthwitsRegistryPopup.vue:66) and [RevokeAuthwitsPopup.vue:87](../../packages/extension/src/popup/components/popups/RevokeAuthwitsPopup.vue:87) still have the script-side `.value` bug from audit-8: `if (!isAllowedToExecute) return` instead of `.value`, plus Enter binds straight to the handler at [ChangeAuthwitsRegistryPopup.vue:106](../../packages/extension/src/popup/components/popups/ChangeAuthwitsRegistryPopup.vue:106) and [RevokeAuthwitsPopup.vue:159](../../packages/extension/src/popup/components/popups/RevokeAuthwitsPopup.vue:159). Not introduced by the dev merge, but still live.

3. `LOW` The execute conflict resolution is safe. `initComplete` flips only after `loadInteractionPayload()` succeeded and `_operations` have been committed at [execute/index.vue:260](../../packages/extension/src/popup/windows/execute/index.vue:260). Since `loadInteractionPayload()` throws if `getRequestId()` is missing, `initComplete=true` implies `requestId` exists. Dev’s gate at [execute/index.vue:515](../../packages/extension/src/popup/windows/execute/index.vue:515) is stricter than the old `!requestId` guard and preserves the register-token metadata gate.

4. `LOW` I did not find another remaining test bypass of popup primary actions after the `register-token` fix. The interaction-popup helpers all use `clickByTestId()` in [fixtures/popups.ts](../../packages/extension/tests/e2e/fixtures/popups.ts:126), [popups.ts:152](../../packages/extension/tests/e2e/fixtures/popups.ts:152), [popups.ts:234](../../packages/extension/tests/e2e/fixtures/popups.ts:234), and [popups.ts:263](../../packages/extension/tests/e2e/fixtures/popups.ts:263). The raw `.click()` sites still present are row/menu/toggle interactions, not async-gated popup submit buttons.

5. `LOW` The `commit-tree` rewrite did not break ancestry. Merge commit [3ca772a](../../.git/COMMIT_EDITMSG) has both parents: `d60a1b4` and `8f124f5` (`origin/dev`). The squash commit `d60a1b4` is a normal single-parent commit.

6. `LOW` Branch hygiene is not clean: `implementations-plan/network-followups/audit-codex-rootcause-8.md` exists in the worktree but is **untracked** and absent from `git ls-files`. `audit-codex-rootcause.md` and `investigation-journey.md` are tracked; there is no duplicate tracked copy of `rootcause-8`.

7. `LOW` Token-detail auto-refresh wiring looks correct. The page reads cached balance, then fire-and-forget refreshes on mount / token / account change at [tokens/[id].vue:60](../../packages/extension/src/popup/pages/tokens/[id].vue:60) and [tokens/[id].vue:116](../../packages/extension/src/popup/pages/tokens/[id].vue:116). `ServiceClient.request()` auto-connects on first request, so the event subscription can receive updates without an explicit `connect()`. The only issue I see is cosmetic: `isRefreshingBalance` is dead state.

Recommended changes:
- Add a real readiness gate to `capabilities/index.vue` before merge. Smallest fix: introduce `initComplete`, set it only after `capabilities` / `availableAccounts` are materialized, and gate both template + `approve()`.
- Fix the two authwits popup handlers to use `.value`; also block Enter when the computed is false.
- Add and commit `implementations-plan/network-followups/audit-codex-rootcause-8.md`.
