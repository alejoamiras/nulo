Verdict: **revert the wallet-sdk patch and try a different approach entirely: keep an extension popup open through `connectPlayground` so the SW stays hot during discover→verify.**

1. There is no extra dApp message after `KEY_EXCHANGE_REQUEST` that gates verify. The shipped extension handler sends `KEY_EXCHANGE_RESPONSE` and immediately calls `onSessionEstablished` at [background_connection_handler.js:94](../../node_modules/@aztec/wallet-sdk/dest/extension/handlers/background_connection_handler.js:94). Nulo’s `onSessionEstablished` then only does session lookup/write and `chrome.windows.create(...)` at [background.ts:152](../../packages/extension/src/wallet/services/wallet-sdk/background.ts:152). No later wallet-sdk message is required to open verify.

2. The dApp side also has no “verification-hash request” step. `establishSecureChannel()` resolves as soon as it receives `KEY_EXCHANGE_RESPONSE` and derives the local keys at [extension_provider.ts:75](../../node_modules/@aztec/wallet-sdk/src/extension/provider/extension_provider.ts:75). The playground then does `wallet = await pending.confirm()` at [wallet.ts:89](../../packages/playground/src/lib/wallet.ts:89); `confirm()` is local object construction, not a round-trip to the wallet.

3. Iframe’s `15_000` does not justify bumping extension. The iframe provider is a different transport with explicit `READY`, `DISCOVERY`, and `KEY_EXCHANGE` waits at [iframe_provider.ts:27](../../node_modules/@aztec/wallet-sdk/src/iframe/provider/iframe_provider.ts:27). The extension provider explicitly documents the short timeout as a MessagePort-side MITM defense at [extension_provider.ts:53](../../node_modules/@aztec/wallet-sdk/src/extension/provider/extension_provider.ts:53). There is no evidence that extending it is a supported fix, and your iteration #9 regression argues against it.

4. “Pre-warm by opening + closing a popup” is already happening. Both fixtures open the popup, wait for `#/popup/general`, switch network, then close the page immediately before `connectPlayground` at [extension.ts:320](../../packages/extension/tests/e2e/fixtures/extension.ts:320) and [extension.ts:348](../../packages/extension/tests/e2e/fixtures/extension.ts:348). So that idea has already been tried in substance.

Concrete next step: revert the `KEY_EXCHANGE_TIMEOUT_MS` patch, and in [extension.ts:320-324](../../packages/extension/tests/e2e/fixtures/extension.ts:320) and [extension.ts:348-352](../../packages/extension/tests/e2e/fixtures/extension.ts:348) move `setupPage.close()` to after `connectPlayground()` completes. That is the smallest real change that tests the cold-SW hypothesis instead of re-tuning wallet-sdk protocol timers.
