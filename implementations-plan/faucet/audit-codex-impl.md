**R1**
- [BLOCKER] [B1] Not fixed. `useTokenBalance` does call `wallet.executeUtility(...)` at `packages/faucet/src/composables/useTokenBalance.ts:117`, but it still passes the wrong data on both axes: `contract.methods.balance_of_*().request()` returns an `ExecutionPayload`, not a `FunctionCall` (`~/.bun/install/cache/@aztec/aztec.js@4.2.0@@@1/dest/contract/contract_function_interaction.d.ts:31`), and the wallet type only accepts `scopes` plus optional `authWitnesses` (`~/.bun/install/cache/@aztec/aztec.js@4.2.0@@@1/dest/wallet/wallet.d.ts:194-199`), not the `from/capsules/extraHashedArgs` block in `useTokenBalance.ts:42-48`.
- [LGTM] [B2] Fixed. The connect flow registers only Dripper/USDC/ETH in `packages/faucet/src/composables/useWalletConnection.ts:184-188`; the capability manifest scopes only those three in `packages/faucet/src/lib/capabilities.ts:67-90`; and `packages/faucet/src/contracts/sponsored-fpc.ts:16-19` explicitly keeps SponsoredFPC out of `registerContract`.
- [SHOULD] [B3] Partially fixed, not exactly as audited. There is a commented `feePayer` boundary in `packages/faucet/src/composables/useFaucetDrip.ts:69-76`, but it is not a single-boundary pattern because the send path also widens `wallet` to `any` at `useFaucetDrip.ts:77-78`. Also, the installed `ExecutionPayload` type already includes `feePayer` at `~/.bun/install/cache/@aztec/stdlib@4.2.0@@@1/dest/tx/execution_payload.d.ts:19-37`, so the comment claiming otherwise is stale.
- [BLOCKER] [B4] Only half fixed. The root `audit:faucet` script exists at `package.json:31`, but `.github/workflows/_build-faucet.yml` does not exist, `pr-quick.yml` has no faucet change filter in ` .github/workflows/pr-quick.yml:23-33`, and the required build jobs still only cover the extension in `pr-quick.yml:161-200`.
- [SHOULD] [B5] Partially materialized. The root aliases `dev:faucet`, `build:faucet`, and `test:faucet` use explicit `bun run --cwd packages/faucet ...` at `package.json:11`, `:15`, `:17`, but the audited “per-step explicit `--cwd`” story never lands in CI because the faucet workflow is missing, and `audit:faucet` itself is still alias-chained at `package.json:31`.

**R2**
- [LGTM] `Contract.at(address, artifact, wallet)` is the correct generic signature. The pinned SDK declares exactly that at `~/.bun/install/cache/@aztec/aztec.js@4.2.0@@@1/dest/contract/contract.d.ts:14-30`, so the generic calls in `useTokenBalance.ts:41` and `useFaucetDrip.ts:53` are ordered correctly.
- [BLOCKER] `wallet.executeUtility` does not accept the option keys the faucet passes. The public wallet type is `scopes: AztecAddress[]` plus optional `authWitnesses` (`wallet.d.ts:194-199`), and `wallet-sdk` forwards only those two keys to PXE at `~/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/base_wallet.ts:483-485`. `from`, `capsules`, and `extraHashedArgs` are not part of that API here.
- [LGTM] `Fr.fromBufferReduce(Buffer.from(secret, "utf8"))` then `poseidon2Hash([fr])` is valid and matches upstream exactly. Faucet deployer does it at `packages/faucet/scripts/deploy.ts:272-278`; upstream does the same at `/Users/alejoamiras/Projects/Ecosystem/aztec-standards/scripts/deploy.ts:396-400`.
- [LGTM] The fixed drip amounts are within `u64`. `1_000_000_000n` and `1_000_000_000_000_000_000n` are defined at `packages/faucet/src/constants/tokens.ts:18-21`; the Dripper takes `amount: u64` at `/Users/alejoamiras/Projects/Ecosystem/aztec-standards/src/dripper/src/main.nr:18` and `:29`; both values are safely below `18_446_744_073_709_551_615`.
- [BLOCKER] The reconstruction logic is right, but the committed data is wrong. `deployments.ts` rebuilds instances the same way upstream `computeContractAddresses()` does (`packages/faucet/src/contracts/deployments.ts:68-95` vs upstream `/Users/alejoamiras/Projects/Ecosystem/aztec-standards/scripts/deploy.ts:343-369`), but `packages/faucet/src/contracts/deployments.json:4-31` still contains placeholder `0x1/0x2/0x3` addresses. I locally evaluated `rebuild*Instance()` and got `0x172684be...7070`, `0x1db8cfc9...1f40`, and `0x14f92cda...3964` instead. That means the constants and the registerable instances disagree.
- [BLOCKER] There is a second balance-reader bug beyond the options shape. `useTokenBalance` treats `wallet.executeUtility(...)` as though it returns a raw bigint and feeds the whole object into `toBigInt()` at `packages/faucet/src/composables/useTokenBalance.ts:117-129`, but the real return type is `UtilityExecutionResult` with a `result: Fr[]` payload (`~/.bun/install/cache/@aztec/stdlib@4.2.0@@@1/dest/tx/profiling.d.ts:476-490`). On real SDK data, this code collapses to `0n`.
- [SHOULD] `useFaucetDrip` reads the wrong success shape from `sendTx`. Default mined sends return `{ receipt }`, not top-level `{ txHash }` (`~/.bun/install/cache/@aztec/aztec.js@4.2.0@@@1/src/contract/interaction_options.ts:222-235`, `~/.bun/install/cache/@aztec/wallet-sdk@4.2.0@@@1/src/base-wallet/base_wallet.ts:444-453`). `packages/faucet/src/composables/useFaucetDrip.ts:93-100` never looks at `receipt.txHash`, so the explorer link / stored hash will come back empty on the real path.

**R3**
- [LGTM] Capability approval is a distinct state, not collapsed into verify. The state exists in `packages/faucet/src/composables/useWalletConnection.ts:17`, is entered at `:101`, and has dedicated UI in `packages/faucet/src/components/WalletPanel.vue:76-86`.
- [LGTM] The account-not-deployed banner is implemented and wired as promised. The preflight probe is in `useWalletConnection.ts:170-175` / `:191-200`, and the banner is rendered in `packages/faucet/src/App.vue:38`.
- [LGTM] The global single in-flight gate exists. `packages/faucet/src/composables/useFaucetDrip.ts:26-27` makes it module-global, `:46-50` enforces it, and `packages/faucet/src/components/TokenCard.vue:31-34` disables all four buttons off the shared state.
- [BLOCKER] Phases B/F are not actually complete because the app is still wired to placeholder deployment data. The file exists, the deploy/rebuild helpers exist, but the committed `deployments.json` is not the real output of the deploy script, and that breaks the connect-time registration path.
- [SHOULD] Phase C is incomplete on disconnect handling. The plan called for `provider.onDisconnect()` coverage; there is no `onDisconnect()` subscription anywhere in `packages/faucet/src/composables/useWalletConnection.ts`, and the only disconnect coverage is manual `disconnect()` in `useWalletConnection.test.ts:206-217`.
- [SHOULD] Phase E UI is only partial. `packages/faucet/src/lib/testids.ts:38` declares `fa-drip-status`, but nothing renders it, and `packages/faucet/src/components/TokenCard.vue:74-93` has no success/error status row matching the spec.
- [SHOULD] Phase G is weaker than promised. There are no `tests/e2e/helpers/mockWalletProvider.ts` or `pageHelpers.ts` files under `packages/faucet/tests/e2e`, and `faucet-smoke.test.ts` module-mocks `WalletManager` directly at `:45-47` instead of exercising the postMessage discovery boundary the plan described.

**R4**
- [BLOCKER] The single most likely first-contact failure on alpha-testnet is the stale deployment artifact, not the fee path. The app requests capability scope for `0x1/0x2/0x3` in `packages/faucet/src/lib/capabilities.ts:67-90`, then tries to register reconstructed instances derived from the salts in `packages/faucet/src/contracts/deployments.ts:68-95`; scope enforcement rejects off-scope registrations at `packages/wallet-bridge/src/scope-enforcement.ts:53-63`. With the current committed JSON, connect can fail before any drip happens.
- [SHOULD] The balance tests mask the real SDK contract-read path. `packages/faucet/src/composables/useTokenBalance.test.ts:4-5` fakes `.request()` as `{ call: ... }` instead of a real `ExecutionPayload`, and `:26-30` fakes `executeUtility` as returning raw bigint instead of `UtilityExecutionResult`.
- [SHOULD] The drip tests and smoke test mask the real send shape. `packages/faucet/src/composables/useFaucetDrip.test.ts:45-48` and `packages/faucet/tests/e2e/faucet-smoke.test.ts:115` return top-level `{ txHash }`, so they never catch the real `{ receipt }` default return.
- [SHOULD] The deployment tests do not verify the only invariant that really matters here: reconstructed instance address equals committed JSON address. `packages/faucet/src/contracts/deployments.test.ts:5-35` checks internal consistency only.

**R5**
- [NIT] Over-scoped: `focus-trap` is in `packages/faucet/package.json:32` but is unused anywhere under `packages/faucet`.
- [NIT] Over-scoped: the toast system is a queue of four in `packages/faucet/src/composables/useToast.ts:19-47`, even though the spec framed this as one transient toast at a time.
- [SHOULD] Under-scoped: there is no invariant test or CI step proving `deployments.json` was regenerated from `scripts/deploy.ts`. That is the missing guard that would have caught the current placeholder-address regression.
- [SHOULD] Under-scoped: the app still lacks two “real-world failure” hooks the spec expected to matter most in v1: external wallet disconnect handling and a smoke that exercises the wallet-sdk discovery/message boundary instead of pure module mocks.

**R6**
- [SHOULD] CLAUDE comment compliance is not clean. Several comments reference plan/audit history instead of live invariants: `packages/faucet/scripts/deploy-config.ts:49`, `packages/faucet/src/lib/chain-info.ts:7-13`, `packages/faucet/src/lib/capabilities.ts:6-10`, `packages/faucet/src/lib/errors.ts:3`, `packages/faucet/src/composables/useTokenBalance.ts:24`, `packages/faucet/src/composables/useFaucetDrip.ts:21`.
- [LGTM] I did not find milestone tags like `M4.10` in the reviewed implementation files, and decorative emoji usage is confined to the verification surface / test fixtures.
- [LGTM] Vue SFC ordering is generally respected in the reviewed components: script before template before style, with setup logic in sensible execution order.
- [SHOULD] Colocation is good, but the L3 coverage minima are not met for every composite. `packages/faucet/src/components/composite/AddressDisplay.test.ts` only has 5 cases (`it()` at lines 21, 26, 33, 41, 46), and `packages/faucet/src/components/composite/DripButton.test.ts` only has 5 (`it()` at lines 6, 13, 21, 26, 33), below CLAUDE’s ≥10 target for composites.

**R7**
- [SHOULD] One question before merge: which exact alpha-testnet deployment artifact is this branch shipping against, and why does `packages/faucet/src/contracts/deployments.json` still contain placeholder `0x1/0x2/0x3` addresses instead of the addresses the current deploy/rebuild code derives?