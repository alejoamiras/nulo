Verdict: Reject

## Critical (must fix before merge)
- The URL-defense only exists on the new detail page. The shared terminal-card builder still forwards raw `op.subtitle` in [journal-state.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/utils/journal-state.ts:221), `RecentActivityView` still forwards raw `op.subtitle` and raw `executingTask.origin.name` for awaiting cards in [RecentActivityView.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/components/modules/general/RecentActivityView.vue:157), and both card components render those props verbatim in [TransactionAwaitingCard.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/components/composite/activity/TransactionAwaitingCard.vue:72) and [TransactionTerminalCard.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/components/composite/activity/TransactionTerminalCard.vue:66). `TransactionsList` consumes the same raw builder output in [TransactionsList.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/components/modules/activity/TransactionsList.vue:86). If the threat model is “dApp-controlled label that looks like a link,” the main feed still fails it. Fix by applying one shared formatter at every dApp-label surface, not only `journal/[id].vue`.

## High
- The regex in [journal-state.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/utils/journal-state.ts:135) is too narrow for the stated threat model. It only catches `scheme://`, so `mailto:`, `tel:`, `javascript:`, `data:`, and malformed-but-still-link-looking `http:evil` all pass through. The current test pin in [journal-state.test.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/utils/journal-state.test.ts:316) locks that hole in. If the goal is “URI-looking text should not read like a link,” match `scheme:` broadly, not only `scheme://`.
- The page exposes raw `error.kind` to normal users in [journal/[id].vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/journal/[id].vue:185). Vue escaping makes it non-XSS, but the comment “always safe to display” is still wrong. `JobError.kind` is only `string` in [wallet-core/src/jobs/types.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/wallet-core/src/jobs/types.ts:73), and the persisted schema accepts any non-empty string in [operation-journal/spec.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/operation-journal/spec.ts:187). Today that means internal classifiers like `sw_restart_post_prove`, `popup_bound`, `dapp_execute` leak straight into user UI. Either humanize via a whitelist or gate the raw kind behind dev/debug with the other raw error fields.

## Medium
- `loadOp()` accepts any terminal activity-kind record in [journal/[id].vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/journal/[id].vue:114), including `succeeded`. But the template only renders when `display` exists in [journal/[id].vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/journal/[id].vue:160), and `journalTerminalDisplay()` returns `null` for succeeded records in [journal-state.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/utils/journal-state.ts:97). Result: a stale/manual `/popup/journal/:id` can land on a blank page instead of `RECORD NOT FOUND`. Reject `journalTerminalDisplay(record) === null` during load.
- `tx/[id].vue` itself is internally consistent now: both explorer surfaces are correctly gated on mined status. There is no other missed `explorerUrl` use on that page. The inconsistency is elsewhere: `TransactionCard.vue` still renders an explorer link whenever `hashSlice && explorerUrl` in [TransactionCard.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/components/modules/activity/TransactionCard.vue:172), regardless of pending/mined state. If the rationale is “pending explorer links 404,” the list card needs the same gate.

## Low + Nits
- `sanitizeJournalSubtitle` is the wrong scope/name for what this is turning into. The problem is broader than “journal subtitle”: it is “untrusted dApp-controlled label rendered in wallet chrome.” Keeping it in `journal-state.ts` likely contributed to the missed awaiting/terminal surfaces.
- `onOperationDeleted` in [journal/[id].vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/journal/[id].vue:129) is not where I’d spend merge-blocking energy. I do not see a serious post-unmount race here because the client disconnects on unmount. If you want cleaner lifetime semantics, use `subscribeJob()` or explicitly remove the listener.

## What looks fine
- Gating `error.message` and `error.normalizedRaw` behind `developerMode || debugMode` matches the tx detail page and is the right default.
- The pending-tx detail-page polish itself is coherent: when pending, both link surfaces fall back to copy-hash, and the dropped “Waiting for inclusion” banner does not create a local UX hole.
- No dedicated `journal/[id].vue` component test is acceptable under the stated minimums. The pure state mapping and sanitizer have unit coverage, and the cancel e2e covers the route. If you add one, target delete-redirect, notFound, and dev-gated raw fields rather than re-testing the 3 visual states.

Couldn’t save this to `RESPONSE_FILE`: the variable is unset in this session, and the filesystem is read-only here.