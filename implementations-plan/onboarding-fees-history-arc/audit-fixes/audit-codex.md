Couldn't save to `RESPONSE_FILE`: it is unset in this session, and the filesystem is read-only.

Verdict: Approve-with-changes

## Critical (must fix before approval)
- None.

## High
- Toggle-off→on replay belongs popup-side, not service-side — `implementations-plan/onboarding-fees-history-arc/audit-fixes/plan.md:205`, `packages/extension/src/popup/components/popups/PopupManager.vue:70-77`, `packages/extension/src/stores/app.store.ts:53-73` — `IncomingTransferService` does not own the UI-selected account/network. If you implement the proposed `ConfigService.onUpdate` replay inside the service, you either guess wrong or fan out prompts too broadly. Keep the P4 service guards in `scanContract`/`replayPendingPrompts`, but fire false→true replay from `PopupManager` using `appStore.profile/network/account`.
- `humanizeErrorKind` whitelist is incomplete — `implementations-plan/onboarding-fees-history-arc/audit-fixes/plan.md:80`, `packages/extension/src/wallet/services/operation-journal/reaper.ts:187-193`, `packages/extension/src/popup/pages/journal/[id].vue:121-126` — the plan omits `stuck_queued`, which the shipped reaper already emits. Include `stuck_queued` in the map/tests. Do not pull in token-import-only kinds like `metadata_fetch` / `network_unreachable` / `contract_invalid`; `journal/[id]` rejects `token_import`.
- Production test coverage is still missing the exact regressions that got audited — `implementations-plan/onboarding-fees-history-arc/audit-fixes/plan.md:97`, `implementations-plan/onboarding-fees-history-arc/audit-fixes/plan.md:159-168`, `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:187-214`, `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:637-668`, `packages/extension/src/popup/components/popups/PopupManager.vue:70-77`, `packages/extension/src/popup/components/popups/PopupManager.vue:96-107` — service/repository tests will not catch another forgotten `connect()/getIncomingTransfers()` on the home widget, and the plan does not explicitly pin false→true replay. Add a `RecentActivityView` mount test for bootstrap and a popup-level test for toggle OFF→ON replay.

## Medium
- P3 as written will double-fetch on first mount — `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:213-214`, `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:637-648`, `packages/wallet-core/src/utils/event-handler.ts:22-27`, `packages/extension-messaging/src/background/client.ts:48-60` — `connect()` immediately fires `onConnected`, whose async handler already calls `loadIncomingTransfers()`, and the plan then calls `loadIncomingTransfers()` again. Pick one bootstrap path or guard the first reconnect replay.
- Do not sanitize wallet-controlled terminal `subtitle` copy — `implementations-plan/onboarding-fees-history-arc/audit-fixes/plan.md:65`, `packages/extension/src/utils/journal-state.ts:120-121`, `packages/extension/src/utils/journal-state.ts:145-160` — widening `sanitizeJournalSubtitle` to `scheme:` is fine for untrusted dApp-origin labels, but applying it to `display.subtitle` is blind scope creep. Limit sanitization to `originLabel` / `executingOriginLabel`.
- If P7 stays popup-side, dedupe by trust scope, not bare contract — `implementations-plan/onboarding-fees-history-arc/audit-fixes/plan.md:148`, `packages/extension/src/wallet/services/incoming-transfer/spec.ts:69-76` — trust is keyed by `(profileId, networkId, contract)`. Contract-only dedupe can collapse distinct prompts across networks that reuse the same address.

## Low + Nits
- Do not add a token-contract explorer link in this PR — `packages/extension/src/popup/components/popups/IncomingTrustPopup.vue:17-22`, `packages/extension/src/wallet/config/config.ts:21-30` — the popup payload does not carry explorer/network policy today. Full address + copy is enough; explorer wiring is scope creep.
- For P6, make the expand/copy affordances real buttons with labels/state — `packages/extension/src/components/Popup/Popup.vue:22-34`, `packages/extension/src/popup/components/popups/IncomingTrustPopup.vue:100-127` — the modal already traps focus, but it does not assign initial focus. Use keyboard-reachable controls and `aria-expanded` on the reveal toggle.
- Phase ordering is acceptable, but the rationale text overstates it — `implementations-plan/onboarding-fees-history-arc/audit-fixes/plan.md:227-239` — P3/P4 are not “front-loaded”; they are commits 3/4. That is still fine because P1/P2 are low-risk helper work.

## What looks fine
- Broad `scheme:` matching is the right bias for P1, if you keep it scoped to dApp-controlled origin fields — `packages/extension/src/wallet/services/wallet-sdk/background.ts:416-420`, `packages/extension/src/wallet/services/wallet-sdk/queued-journal.ts:132-145`, `packages/extension/src/wallet/services/execution/service.ts:1191-1203`. These values are not limited to hostnames; a known-scheme allowlist is the wrong trade.
- Apply sanitization at the shared builder/helper layer, not inside `TransactionAwaitingCard` / `TransactionTerminalCard` — `packages/extension/src/utils/journal-state.ts:212-236`, `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:157-160`, `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:343-346`, `packages/extension/src/components/composite/activity/TransactionAwaitingCard.vue:20-54`, `packages/extension/src/components/composite/activity/TransactionTerminalCard.vue:32-57`.
- `hydrateSchedulers()` on `onAccountAdded` is the safer choice for this PR — `packages/extension/src/wallet/services/incoming-transfer/service.ts:241-264`, `packages/extension/src/wallet/services/incoming-transfer/service.ts:282-300`. Account creation is low-frequency; full rebuild avoids additive drift. Keep delete targeted.
- Popup-side queueing is the right location for P7 on this branch — `packages/extension/src/popup/components/popups/PopupManager.vue:47-77`, `packages/extension/src/popup/components/popups/PopupManager.vue:96-107`, `packages/extension/src/wallet/services/incoming-transfer/service.ts:445-475`. Background ack/retry is a protocol change; reconnect replay already gives you recovery.