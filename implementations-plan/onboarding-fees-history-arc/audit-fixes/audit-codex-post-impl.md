Reject.

**High**
- `packages/extension/src/wallet/services/incoming-transfer/service.ts:416`, `:440`, `:497`, `:366`: P8 still does not close the self-note PXE race. If a note is discovered before the outgoing hash is visible in `TransactionService` or the in-flight journal set, `scanContract()` can transition the contract to `pending` and emit the trust popup. The later late-delete only removes the record; it does not roll back the `pending` trust row or retract an already-open prompt. That leaves the first-receive gate vulnerable to a false positive on the user’s own outgoing/change note. Fix by reconciling when a hash later becomes known: delete the record, revert `pending -> unknown` when that contract/account has no remaining hidden records, and ideally reconcile on journal `submitting.txHash`, not only on `onTransactionAdded`.

**Medium**
- `packages/extension/src/popup/components/popups/PopupManager.vue:123`, `:147`, `:156`: `lastVisibility = true` is not race-safe. After `configService.connect()` the client can receive `onUpdate` before the seed `getValue()` resolves. If persisted state is `false` and the first event in that window is `false -> true`, `wasOff` is computed from the optimistic default and the replay is suppressed. The later seed makes the tracker look correct, but the missed `replayPendingPrompts()` is already lost. Fix by ignoring updates until initialization completes, or by seeding before registering/connecting the update listener.
- `packages/extension/src/wallet/services/incoming-transfer/service.ts:366`, `packages/wallet-core/src/utils/event-handler.ts:22`: duplicate `onTransactionAdded` events for the same hash can race and double-emit `onIncomingTransferDeleted`. `EventHandler.invoke()` does not await async callbacks, so two legal same-hash emits can both observe the same `listByTxHash()` result before either delete finishes. Storage deletion is idempotent; event emission is not. Fix with a per-hash in-flight guard or a serialized delete-by-hash path.

**Low**
- `packages/extension/src/popup/components/popups/IncomingTrustPopup.vue:137`, `:154`: the disclosure button always advertises `aria-controls="incoming-trust-contract-full"`, but the controlled element only exists under `v-if`. In the collapsed state the control points at a missing node. Keep the region mounted and hide it with `v-show`/`hidden`, or only set `aria-controls` when the region exists.
- `packages/extension/src/wallet/services/incoming-transfer/service.scenarios.test.ts:6`, `:544`, `packages/extension/src/popup/components/popups/PopupManager.test.ts:9`, `:179`, `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:643`, `packages/extension/src/wallet/services/incoming-transfer/service.ts:164`, `packages/extension/src/popup/components/popups/PopupManager.vue:124`: the backfill overclaims coverage. The suite still does not pin `onAccountAdded -> hydrateSchedulers`, `RecentActivityView`’s explicit `incomingTransferService.connect()`, or the OFF->ON config replay path. Those are not benign defers; they are the exact glue paths that already broke once. Add focused tests for those three. The missing tests for `TransactionIncomingCard.vue`, `fees.vue`, and `journal/[id].vue` are acceptable follow-ups.

**Nit**
- `packages/extension/tests/e2e/onboarding-tab.test.ts:157`: this is a route-behavior pin, not a split-handler pin. It will still pass after consolidating both skip buttons onto one shared handler as long as both land on `/accelerator`. If splitness matters, pin it lower in the stack; otherwise rename the test/comment.

**What’s solid**
- `packages/extension/src/popup/components/popups/PopupManager.vue:60`: the triple-key queue dedup itself is sound. I do not see a partial-cache or async-open hole in the current synchronous queue path.
- `packages/extension/src/utils/journal-state.ts:135`, `packages/extension/src/popup/pages/journal/[id].vue:182`, `:188`: the sanitize widening and `humanizeErrorKind` wiring are applied at the right user-controlled surfaces. `"Error"` is a reasonable fallback; I would not add `console.warn` in that pure UI helper.
- `packages/extension/src/wallet/services/incoming-transfer/service.ts:173`: `onAccountDeleted` cleanup is correct for the actual account model. If the same address ever represented two chainIds, that would already be a deeper `AccountService` invariant failure.
- `packages/extension/src/popup/components/popups/IncomingTrustPopup.vue:103`, `packages/extension/src/popup/components/popups/IncomingTrustPopup.test.ts:101`: focus/reset/copy behavior is implemented and tested correctly. The remaining issue there is the controlled node lifetime, not the interaction model.

Attempted write to `implementations-plan/onboarding-fees-history-arc/audit-fixes/audit-codex-post-impl.md` failed because this session is read-only.