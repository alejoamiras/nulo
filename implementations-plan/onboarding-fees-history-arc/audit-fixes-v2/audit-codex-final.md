Reject.

**Critical**
- `plan.md:Load-bearing invariant` / `plan.md:P7` still misses the real load-bearing race: `reconcileTrust` gates on an initial `pending` read, then later writes `unknown` unconditionally. A concurrent user action can be clobbered. `setTrustAllow` / `setTrustReject` write immediately in [incoming-transfer/service.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/incoming-transfer/service.ts:226), while the planned reconcile writes later at [plan.md](/Users/alejoamiras/Projects/nulo/nulo-2/implementations-plan/onboarding-fees-history-arc/audit-fixes-v2/plan.md:327). Example: popup click `Allow` races journal/tx/hydrate reconcile; reconcile reads `pending`, user writes `trusted`, reconcile finishes and overwrites to `unknown`. Fix: add a per-`(profileId, networkId, contract)` lock or repo-level compare-and-set for `pending -> unknown`. Re-reading once before write is not enough without serialization.
- `plan.md:P8` does not close the orphan race it claims to close. Moving the outgoing/inflight snapshot inside the note loop only narrows the window; it does not eliminate the gap between “final snapshot says safe” and `upsertRecord`. `onTransactionAdded` / journal `submitting.txHash` can still land after the re-snapshot and before the write, leaving the same orphan hidden record. The current “more invasive” alternative is actually the needed one: post-upsert validation/delete or an atomic `upsert-if-ungrounded` helper.
- `plan.md:P8`’s perf claim is wrong. `collectOutgoingTxHashes` is not an in-memory set lookup; it calls [TransactionService.getTransactions](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/transaction/service.ts:84), which reloads storage. `collectInflightTxHashes` calls [OperationJournalService.getOperations](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/operation-journal/service.ts:331), which also reloads storage. So “cheap / cached / low cost” in [plan.md:P8](/Users/alejoamiras/Projects/nulo/nulo-2/implementations-plan/onboarding-fees-history-arc/audit-fixes-v2/plan.md:449) is false. Fix the analysis, then redesign the fix.

**High**
- `plan.md:P9`’s repository-bypass is functionally wrong, not just ugly. Writing `IncomingTransferRepository.setTrust(..., "trusted")` directly skips the `setTrustAllow` side effects in [incoming-transfer/service.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/incoming-transfer/service.ts:232): reveal hidden rows and emit `onIncomingTransferAdded`. Since token deletion only stops schedulers and does not clear incoming state [incoming-transfer/service.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/incoming-transfer/service.ts:346), a delete/re-add flow can leave old hidden records stranded forever under a now-`trusted` contract. Fix: do not let `TokenService` poke the repo directly. Extract a lower-level awaited helper that performs the full “trust allow” semantics without a service-graph cycle.
- `plan.md:P7`’s journal-hook guard is too weak. `if (op.progress?.stage !== "submitting") return` does no-op on other stages, yes, but `onOperationUpdated` is not a stage-transition event. It also fires from [setOperationMeta](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/operation-journal/service.ts:307). Today that is mainly `token_import`, but the plan leaves this as an open question even though source already answers it. Fix: make the hook idempotent by `op.id`/`txHash` and route both journal + `onTransactionAdded` through one guarded delete helper.
- `plan.md:Profile Isolation` / `plan.md:P14` hand-waves away a real supported scenario. Same mnemonic / restored master key can absolutely yield the same Aztec address in two profiles because account derivation uses the profile secret, chain, type, and index, not `profileId` [account/service.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/account/service.ts:186); importing/restoring that secret is first-class [profile/service.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/profile/service.ts:598). Calling this “pathological” is indefensible. Worse, account rows are keyed by `address` [account/service.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/account/service.ts:23), so same-address cross-profile is a broader collision risk, not a thought experiment.
- `plan.md:P12` is too narrow even if you stay inside `send.vue`. The page snapshots `tokens`, `tokenBalances`, and `contacts` on mount [send.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/send.vue:350). The proposed watcher only refetches `tokens`. That can still leave balances/contacts stale after profile/network/account change. Fix: rehydrate the whole identity-scoped page state, not one field.
- `plan.md:P12` also should not stop at `send.vue`. Grep hits multiple one-shot token consumers: [activity.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/activity.vue:147), [tx/[id].vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/tx/[id].vue:137), [journal/[id].vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/pages/journal/[id].vue:140), [RecentActivityView.vue](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/components/modules/general/RecentActivityView.vue:126), plus popup flows like `NewTokenPopup` and `SelectTokenPopup`. “Audit during implementation” in lessons is not enough. P12 needs an explicit consumer inventory and disposition table as exit criteria.
- `plan.md:P4b`’s “if P4a confirms something else, stop and surface to user” is too weak for an approval-gate plan. You already know at least two concrete branches from source: replay-before-triple-ready, and popup remount resetting local queue state while replay never rearms. Encode those branches now; don’t ship a plan that turns back into investigation mid-flight.
- `plan.md:Rollback units` is over-grouped. `{P5,P7,P8}` is not a true atomic unit: P5’s per-hash guard/account filter is independently valuable and safe if P7/P8 revert. `{P6,P4a,P4b}` is worse: P4a is a failing repro and should survive any rollback. Fix the rollback notes to reflect dependency direction, not “revert as a unit” convenience.

**Medium**
- `plan.md:Open questions` contains questions the repo already answers. `Tx` does not carry `profileId` [transaction/spec.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/transaction/spec.ts:91). `onOperationUpdated` can fire without a stage transition because `setOperationMeta` emits it [operation-journal/service.ts](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/wallet/services/operation-journal/service.ts:321). That drift is a smell: this plan is still asking the codebase questions it could have closed.
- `plan.md:P8` test design is too loose. To validate this race, the test must force the event after the final dedupe snapshot and before the write. A generic “before upsert lands” description is not precise enough. Use a deferred promise barrier around the final snapshot/upsert boundary.
- `plan.md:P7`’s scanContract invocation is still under-specified. The section says reconcile should run from `scanContract`, but the pseudocode in P8 only re-snapshots and `continue`s. If notes are skipped as outgoing/inflight and no delete event fires, stale `pending` can linger until another site happens to sweep it. Spell out the exact end-of-scan reconcile point.

**Low / Nit**
- The document says “15 phases” but enumerates P1–P14. That’s not substantive, but it matches the broader problem: some of this is still one revision away from being truly locked.

**What’s solid**
- Moving the failing C2 repro ahead of the popup fixes is correct. That was wrong before; it’s right now.
- Rejecting reverse-write-order in favor of a consumer-layer first pass was the correct pivot. The popup shell really does make “profile last” worse.
- The per-account direction for R' is the right axis. The problem is missing serialization/profile isolation, not the idea of scoping by account.
- Popup auto-close plus queue splice belongs with backend `pending -> unknown`. Splitting those would leave stale popup state.
- The policy boundary for C1 is right: popup-origin add should be frictionless, dApp-origin add should not. The implementation path in P9 is the part that’s wrong.