Reject.

**Critical**
- `plan.md:P12 — E1 profile-switch atomicity`: reversing `appStore.profile` to “last write” breaks the current popup shell. `bootstrapActiveProfile()` mutates `appStore.network` inside `initNetworks()` (`packages/extension/src/composables/useProfileBootstrap.ts:23-46`), and `popup/app.vue` immediately watches `appStore.network` and reads `appStore.profile.id` to fetch accounts (`packages/extension/src/popup/app.vue:97-127`). With `profile` written last, that watcher can hydrate the wrong profile’s accounts or run with `undefined`. Fix: do not land “profile last” against the current popup shell; either diagnose E1 at the `send.vue` layer first or refactor the popup watcher/bootstrapping contract together. Also, P12 only removes `auth.vue:101`; it leaves duplicate unlock-side effects at `packages/extension/src/popup/pages/auth.vue:103-108`.

**High**
- `plan.md:P9a` after `plan.md:P5` violates the brief’s repro-first requirement for C2. P5 changes `PopupManager` before the failing popup-close/reopen repro is captured, so it can erase or narrow the original bug. Fix: move the failing C2 e2e ahead of P5.
- `plan.md:P9b — C2 popup-reopen fix`: H2 is not a credible primary branch. `ServiceClient.connect()` invokes `onConnected` on the first successful connect (`packages/extension-messaging/src/background/client.ts:48-61`), so “onConnected only fires on reconnect” is contradicted by the client. H3 is the live branch: replay fires before `appStore.profile/network/account` are ready (`packages/extension/src/popup/components/popups/PopupManager.vue:105-112,141-160`). Fix: make H3 the default hypothesis and spell out concrete H4/H5 branches, otherwise P9b is a replan, not a fallback.
- `plan.md:Load-bearing invariant` / `plan.md:P6`: the four-site reconcile still misses a real PXE ordering hole. `scanContract()` snapshots outgoing/inflight once (`packages/extension/src/wallet/services/incoming-transfer/service.ts:416-424`) and only later persists the record (`:458-459`). If `submitting.txHash` or `onTransactionAdded` fires after the snapshot but before the upsert, the planned reconcile only demotes `pending -> unknown`; it does not delete the newly-inserted grounded hidden record. That orphan can later surface on a future `setTrustAllow`. Fix: when scan-side reconcile sees a grounded hidden record, delete it immediately before evaluating trust, or refresh outgoing/inflight before `upsertRecord()`.
- `plan.md:P8 — onUserAddedToken event + auto-trust`: the dispatcher chain is safe from dApp smuggling, but the chosen implementation still races. `TokenService.addToken()` emits `onTokenAdded` (`packages/extension/src/wallet/services/token/service.ts:183`), `EventHandler.invoke()` does not await async subscribers (`packages/wallet-core/src/utils/event-handler.ts:22-27`), and `IncomingTransferService.onTokenAdded` starts a scheduler with an immediate poll (`packages/extension/src/wallet/services/incoming-transfer/service.ts:325-343,309-323`). An `onUserAddedToken` emitted after `onTokenAdded` can lose to that first scan and still open the trust popup. Fix: guarantee the trust write happens before scheduler start, not merely “after addToken succeeded.”
- `plan.md:P12` is also where consolidation picked the wrong branch from the parallel drafts. The lower-risk suspect is still `packages/extension/src/popup/pages/send.vue:350-379`: it snapshots tokens/balances once on mount and never re-fetches on profile/network/account identity changes (`:302-348`). Fix: reproduce E1 first and prove the broken layer before touching global bootstrap ordering.

**Medium**
- `plan.md:Load-bearing invariant` is not fully profile-isolated. The invariant is keyed by `(profileId, networkId, contract)`, but outgoing grounding uses `TransactionService.getTransactions(account)` which is only account-scoped (`packages/extension/src/wallet/services/transaction/service.ts:84-85`). If the same address exists in two profiles, one profile’s outgoing txs can ground another profile’s hidden records. Fix: either document this as residual risk and scope sweep/reconcile to the active profile only, or make outgoing lookup profile-aware before calling the invariant exhaustive.
- `plan.md:P6 -> P7` is not independently safe. P6 introduces `pending -> unknown` in backend state, but without P7 the open popup and queued duplicates remain stale. Fix: ship the first `pending -> unknown` transition together with popup auto-close/queue splice, even if the journal hook itself stays separate.
- `plan.md:P13` closes the old codex Low #2 gaps, but it does not fully pin the new glue added by P5-P9. Missing pins: “manual token add does not prompt on first scan,” “replay re-fires when the active triple becomes ready after connect,” and a popup-shell integration test for the P12 cascade. Fix: make those phase-local tests, not generic backfill.

**Low**
- `plan.md:P11` is fine on the narrow sanitize boundary as long as it keeps `sanitizeJournalSubtitle(op.subtitle)` exactly where it is now (`packages/extension/src/popup/pages/journal/[id].vue:95-98,180-183`; `packages/extension/src/utils/journal-state.ts:135-147`). The plan should not imply broader sanitization than that. Fix: add one pin that the new category/context helper is invariant under `op.subtitle` changes.

**Nit**
- `plan.md:P9b` references “H1–H5” but only operationalizes H2/H3, and `plan.md:Asks` says “replan” otherwise. That is not a real fallback. Fix: enumerate the remaining concrete branches or drop the H4/H5 framing.

**What’s solid**
- Keeping `trusted` and `blocked` out of auto-demotion is correct.
- The per-record account check inside reconcile is the right answer for multi-account same-contract.
- The C1 dApp-bypass claim is currently solid: `register_token` stays `OriginType.DAPP` through dispatcher -> execute popup -> `approveInteraction()` -> `executionService.executeRegisterToken()` -> `tokenService.addToken({ origin: "dapp" })` (`packages/wallet-bridge/src/dispatcher.ts:451-488`, `packages/extension/src/popup/windows/execute/index.vue:358-361`, `packages/extension/src/wallet/services/execution/service.ts:1089-1091`).
- P7’s popup auto-close plus queue splice is necessary; without both, the close watcher just reopens the same contract.
- P1/P2/P3 are correctly scoped and low risk.
- P10’s categorical label/context helper is the right shape if it continues to read only wallet-controlled journal fields.