# Phase 3 lessons — profile entropy model

- **Scope pull-forward (documented deviation):** `importPlain`/`importEncrypted`/`exportEncrypted` and their UI branches were cut HERE, not P4 — under required-entropy they are *fundamentally* incompatible (a bare 32-byte master cannot produce words that re-derive it through one-way PBKDF2), and leaving them would have forced a legacy entropy-less profile shape through every P3 invariant. `export/key.vue` died with them (it called `exportEncrypted`). P4 is now pure copy/e2e surgery. Same arc, same PR — phase-boundary placement only.
- **PasswordSecretBox v2** (guard+master+entropy triple, purpose-AAD each): the slot-swap AAD test pins the actual R1 attack (master ciphertext moved into the entropy slot decrypting into lying recovery words). `reseal` handles all three atomically — the audit-H2 pin asserts entropy is never left decryptable under a retired password.
- **Pairing checks live at every entropy-DECRYPTION site** (password unlock, exportMnemonic-before-reveal, restore) + the entropy-MAC on the bearer path (mismatch → silentClose → forced password unlock). The doctored-backup restore check THROWS pre-persistence rather than returning a per-row restoreError — fail-loud before anything seals; the test pins zero profiles created.
- **Restore-secret test fixtures needed real derivation pairs**: the old fixed `fill(11)` master keys can't pass the pairing check; a memoized `restorePairFor(fill)` helper (entropy→words→PBKDF2→master) replaced all 11 literal sites. PBKDF2's 2048 iterations are cheap enough that this cost nothing measurable.
- **Formatting gotcha**: five files failed biome FORMAT (error-level) after heavy editing — `biome check --diagnostic-level=error` isolates real errors from the warning noise; `--write` on the five files fixed it.
- **Epoch 3→4**: registry constant + rejection tests updated; buildSyntheticBackup-style unit fixtures updated here; the *e2e* fixture literals (`import-drivers.ts`, `passkey-backup.test.ts`) remain P4 items with the rest of the e2e surgery.
- **Regime record redefined in place** (owner-ratified A2): kdf label v2 + `NULO_KDF_SPEC` canonical string + `kdfDigest` (sha256, recomputed by the test — closing the "bare label has no tripwire" gap) + ack + the module's own rules text gained the one-time pre-launch carve-out.
- Gate: `bun run lint` 0 · `bun run typecheck:all` 0 · `bun run test:all` all packages green (extension 4380 / wallet-crypto 55 / aztec-runtime 183).
