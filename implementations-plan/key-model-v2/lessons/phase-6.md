# Phase 6 lessons — network e2e reconciliation

- **The prover-ON frozen-account canary is GREEN** (`bun run e2e:agent tests/e2e/network/frozen-account-canary.test.ts …`, 91.8s): derive under NULO-ACCOUNT-KDF v2 → **ctor-deploy with a REAL SNARK proof** → authwit consume → SW-restart re-derive. This is the CLAUDE.md-mandated gate for any KDF change, and it validates the ENTIRE v2 chain end-to-end on a live sandbox: recovery words → PBKDF2 master → `deriveAccountSeed` (l1ChainId + Nulo separator) → signing key → frozen address → a proven transaction. The address KAT can't see execution breakage (frozen bytecode vs newer simulator/prover); the canary is the only gate that can — and it's green.
- **profile-reimport-matrix GREEN (3/3)**: delete → full-backup re-import (same-id tombstone path) + delete → fresh-recovery-phrase re-import, both fully functional (PXE reads + writes) after the KDF v2 + recovery-phrase-import changes.
- **Runner discipline:** `bun run e2e:agent` (parallel-safe: owns its own sandbox/ports/data-dir per worktree, per the owner) FATAL-bails if the default whole-suite run includes `@requires-proverless`-marked files without `NULO_E2E_PROVERLESS=1` — but the canary needs prover ON. Resolution: pass the specific (non-proverless) files as args (`agent.sh` forwards `"$@"` to vitest), which runs a prover-ON build against just the KDF-relevant tests. The full ~70-file network suite runs on the PR via CI's required `network-e2e-status` gate (unchanged dApp/execution tests exercise the same account contract the canary just proved).
- **M8 imported-account live-tx leg**: not run as a dedicated e2e (downgraded to unit coverage per M8's allowance — the signing branch is proven in `import-export.test.ts`, and live proving on the identical `NuloAccount` shape is exactly what the canary above exercises). The `signingKeyHex` affordance on `setupPreFundedAccount` is in place if a dedicated leg is wanted later.
- Gate: `bun run e2e:agent <canary> <reimport-matrix>` → 5 passed, exit 0 (prover-ON build, live sandbox).
