approve-with-fixes

1. `Medium` — [multi-account-from.test.ts:32](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/tests/e2e/network/multi-account-from.test.ts:32>), [wallet.ts:135](</Users/alejoamiras/Projects/nulo/nulo-2/packages/playground/src/lib/wallet.ts:135>), [transactions.ts:47](</Users/alejoamiras/Projects/nulo/nulo-2/packages/playground/src/sections/transactions.ts:47>). The characterization is currently vacuous: `requestCapabilities()` sets `selectedAccount = granted[0]`, and `pg-btn-sendTx-default` always uses `selectedAccount` as `opts.from`, so the test never actually sends from a non-first account. That also means minting both accounts is dead work; single pre-mint is sufficient under the current wiring. Fix: add a real sender override before the click (prefer a playground control or page hook that sets `selectedAccount` to `accountAddresses[1]`), then assert the execute popup still shows `accountAddresses[0]`. If you keep the current path, rename the test because it does not verify “ignores `opts.from`”.

2. `Medium` — [tx-sendTx-noFrom.test.ts:31](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/tests/e2e/network/tx-sendTx-noFrom.test.ts:31>), [tx-sendTx-feePayer.test.ts:18](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/tests/e2e/network/tx-sendTx-feePayer.test.ts:18>), [tx-sendTx-sponsoredFpc.test.ts:20](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/tests/e2e/network/tx-sendTx-sponsoredFpc.test.ts:20>). The three opportunistic tests did not actually switch to `dappConnectedExtensionWith*Cap`; they still pay the full `requestCapabilities` popup inside the test budget. That leaves cold-shard latency in place and misses the stated amortization benefit. Fix: change all three to `dappConnectedExtensionWithTransactionCap`, use the fixture’s `accountAddress`, and delete the in-test bundle-select / capabilities-popup round-trip.

3. `Medium` — [popups.ts:369](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/tests/e2e/fixtures/popups.ts:369>). `waitForSendTxActiveStage()` encodes “anything except pending/queued”, not “the active non-terminal stages”. Today that is mostly saved by [RecentActivityView.vue:207](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/popup/components/modules/general/RecentActivityView.vue:207>) filtering `terminalAt !== null`, but the helper contract is looser than the intent and would silently start passing failure-path DOM if the UI ever kept awaiting cards mounted through terminal transitions. Fix: replace the selector with an explicit allowlist for `simulating`, `proving`, `submitting`.

4. `Low` — [TransactionCardLayout.vue:50](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/components/composite/activity/TransactionCardLayout.vue:50>), [TransactionAwaitingCard.vue:18](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/components/composite/activity/TransactionAwaitingCard.vue:18>). `stage` is typed as plain `String`, even though e2e now depends on exact journal literals. A parent can pass any string and still produce a `data-stage` that the tests may match. Fix: import `type PropType` from `vue` and `type JobStage` from `@nulo/wallet-core/jobs`, then tighten both props to `String as PropType<JobStage | null | undefined>`.

5. `Low` — [TransactionAwaitingCard.test.ts:64](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/components/composite/activity/TransactionAwaitingCard.test.ts:64>). Phase A coverage is incomplete here: `TransactionCardLayout.test.ts` covers all 8 stages plus omission, but `TransactionAwaitingCard.test.ts` only checks `"proving"` and still references the removed `waitForSendTxProvingStage` contract. Fix: mirror the table-driven 8-stage matrix here and add one case asserting `data-stage` is omitted when `stage` is unset.

6. `Low` — [README.md:117](</Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/tests/e2e/README.md:117>), [plan.md:20](</Users/alejoamiras/Projects/nulo/nulo-2/implementations-plan/journal-stage-restructure/plan.md:20>), [eli5.html:114](</Users/alejoamiras/Projects/nulo/nulo-2/implementations-plan/journal-stage-restructure/eli5.html:114>). The archive/docs still describe a proving-only helper, but the landed implementation is `waitForSendTxActiveStage()`. Fix: add a resolution note at the top of the plan/ELI5 or rewrite those passages to say “active stage (`simulating|proving|submitting`)”; for the README, I would remove the historical paragraph entirely rather than keep a misleading summary.

No active workflow/script references to `NULO_E2E_SKIP_DEFERRED_SLOW` remain outside historical docs. The `noFrom` carve-out is defensible as-is.