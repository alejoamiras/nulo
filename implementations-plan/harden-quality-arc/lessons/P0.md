# P0 — Branch + scaffold

**Status:** ✓ done.

## What happened
- `git fetch --prune` on `dev`; confirmed **remote `dev-quality` absent** via `ls-remote --heads … dev-quality` (no output). No stale local `dev-quality` either (the batch-2 one was never persisted locally / already promoted into `dev`).
- `dev` had advanced since the blueprint was drafted: `ea2d5a4` → **`8e919f6`** via `refactor(repo): restructure to apps/ + packages/ + contracts/ layout (#186)` (+ `db0d745` faucet L1 fix). Cut `dev-quality` **fresh off `dev` @ `8e919f6`**.
- Scheduled the autonomous driver as cron job `d54f7670` (every 20m, session-only).

## Re-index (the restructure was cosmetic — owner-confirmed "only a move of folders")
The #186 restructure moved the 4 **apps** out of `packages/` into `apps/`; the 7 **libs** stayed in `packages/`:
- `packages/{extension,faucet,landing,playground}/…` → `apps/{…}/…`
- `packages/{wallet-core,wallet-crypto,extension-messaging,aztec-runtime,wallet-bridge,design,bridge-core}/…` → **unchanged**.
- `key-vectors.test.ts` now lives at `apps/extension/src/wallet/crypto/key-vectors.test.ts` (it's the extension's crypto oracle, not wallet-crypto's).
- The 3 `nulo-schema-patch.ts` copies → `apps/{extension,faucet,playground}/…`.

Re-indexed the findings + plan so the loop's `file:line` grep still resolves: prefix-swapped the 4 app paths across `audit/quality/2026-06-29-ext-ultra/{report.md,report.html,findings/*}` + `implementations-plan/harden-quality-arc/{plan.md,eli5.html,audit-*.md}` (the trailing slash protected `packages/extension-messaging/` + the libs). Bumped the plan's dev base SHA `ea2d5a4` → `8e919f6`. Verified: `consolidated.md` now has `apps/extension/` (17) / 0 stale; libs (`extension-messaging`, `wallet-bridge`) intact; 22 Q-headers intact.

**Tooling note:** macOS has **GNU sed** (not BSD) — `sed -i ''` fails (`-i` takes no empty-suffix arg → treats the script as a filename). Used a Python `str.replace` pass instead. Prefer Python for bulk multi-file rewrites here.

## Cruft note (NOT arc scope)
The restructure left untracked working-tree leftovers in the now-empty old dirs: `packages/extension` (~1038 files: `tsconfig.tsbuildinfo` + build output), `packages/faucet` (~11k: `.env.local`, node_modules), `packages/landing` (`src/generated/release.json`). All **untracked** (absent on a fresh clone, so CI never sees them). Left in place unless they break a local gate.

## Gate
- Remote dev-quality verified-absent → created + pushed.
- `bun run lint` clean.
