# Q-22 — cross-package documentation drift sweep

**Status:** ✓ DONE. Squashed to `dev-quality` @ `498c07e` (PR #196). Gate: full network `28473431132` + smoke `28473429583` + quality `28473428086` green. typecheck:all 12/12 + lint clean. docs/comments only — no codex post-impl.

## What shipped (all source-verified before edit)
- `wallet-crypto/README.md`: PBKDF2 `250k`→`600k` iterations (matches `encryption-key.ts` `PBKDF2_ITERATIONS = 600_000`; the README was the drift, the code was already OWASP-2023-correct).
- Aztec `4.2.0`→`5.0.0-rc.1` (pin verified) across: `aztec-runtime/README.md`, `wallet-bridge/README.md`, and the upstream-citation comments in `batched-view-simulation.ts` (×2) + `aztec-runtime/src/pxe/service.ts`. `MAX_ENQUEUED_CALLS_PER_CALL = 32` re-verified still correct → kept `=32`.
- `password-secret-box.test.ts` GUARD-canary comment `spec.ts`→`password-secret-box.ts` (where `ENCRYPTION_GUARD` actually lives).
- `extension-messaging/README.md`: added the omitted `src/core/` file-map rows (`base-service`/`base-client`, `decode`/`error-response`, `rpc-methods`/`initialization`).

## Key lessons
- **Honesty on un-vendored upstream line numbers.** The `@aztec/pxe` SOURCE (`src/pxe.ts`, `pxe.js`) is not in `node_modules` (only `dist`), so `pxe.ts:328-336` / `pxe.js:627` couldn't be re-checked against `5.0.0-rc.1`. Decision: bump the VERSION label to the verified pin, KEEP the line citations as the author's carried-forward pointers, and DOCUMENT the limitation rather than fabricate verification. The verified-stable `=32` constant (unchanged across the bump) is corroborating evidence the upstream wasn't drastically rewritten.
- **`wallet-core types:[]` drift was already MOOT** — grep found no such claim in the current README; the cited instance is absent (stays dropped, as the plan noted).
- **commitlint `footer-max-line-length = 100` applies to footer lines too** — the AFK-signing tag `[AFK: …]` must be ≤100 chars per line (a one-liner over 100 fails the commit; wrap or shorten).
- **Docs/comments-only ⇒ build output byte-identical** (comments stripped) → the full-network e2e is a same-runtime confirmation, not a behavior test. Ran it anyway per the owner's full-network-per-PR ruling.

## Note — AFK signing
PR #196 merged GitHub-signed (squash). The branch commit `e52460a` + this bookkeeping were authored during the 1Password SSH outage → `-c commit.gpgsign=false` (signed-first, bypass-fallback). Backfill-on-return candidates (`git log --grep "backfill"`).
