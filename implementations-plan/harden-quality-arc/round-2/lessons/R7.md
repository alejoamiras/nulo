# Lessons — R7 (final sweep + confidence + hand-off) · finale

Round-2 code-complete at dev-quality `2f3d9d4` (docs-on-top `2f6c120`). STOPPED for owner QA + merge of #220.

## The confidence pass EARNED its cost — twice
Two independent hostile legs (codex xhigh + a fresh-context Fable subagent), and each paid off:
- **codex opened with BLOCK** on two "cross-profile" HIGHs. The disciplined response was NOT to accept
  it and start fixing (that would have been scope-creep into owner-gated fail-open→closed changes) NOR
  to dismiss it — but to VERIFY the concrete reachability claims against the authoritative
  `findings/Q-13/plan.md` + the actual dispatcher. Verification showed the flagged methods aren't in
  `METHOD_REGISTRY` (not dApp-dispatchable) and the one dApp→token path passes server-derived
  `ctx.profileId`. Resuming codex WITH those three facts, it revised to SHIP-WITH-NOTES itself:
  "my BLOCK over-scoped extension-Port-RPC defense-in-depth into dApp reachability." Lesson: a BLOCK
  from a local-analysis tool is a hypothesis, not a verdict — the reachability context it lacks is
  exactly what the authoritative finding plan already established. Push back with facts; log the exchange.
- **Fable caught a real arc side-effect codex missed**: R1.4a hardened the balance PROJECTOR to skip a
  foreign/missing token, but its sibling DISPLAY path `getTokenBalances` still threw — a one-token
  white-screen the arc's own Q-01 codec-hiding newly enabled. This is the R1.4b lesson verbatim ("when
  you close a fail-open getter, grep its internal callers for the ones without a catch") — and it took
  a FRESH pair of eyes to spot the sibling. Fixed R6.5/#245, failing-first, fail-safe. Two legs of
  DIFFERENT model families with DIFFERENT framings caught DIFFERENT things; neither alone was sufficient.

## Adjudicating "BLOCK" at the finale without either rushing or rubber-stamping
The tension: a BLOCK can't be waved away (you'd ship a hole), but accepting it blindly at R7 could mean
(a) fixing owner-gated hard-limit items autonomously or (b) infinite scope creep. The resolution is the
loop's own rule — "act on the STRONGER argument, log it" — where "stronger" is decided by grounding in
the authoritative finding plan + code verification, not by which agent spoke last. Outcome: one finding
was a real in-scope fail-safe fix (did it), the rest are pre-existing owner-gated defense-in-depth
(surfaced in the WRAP-UP as the round-3 candidate). Both audits are committed as `audit-{codex,fable}.md`
so the owner sees the full reasoning, not just a verdict.

## Ops: the sweep is on CODE HEAD, docs layer on top
The R7 sweep ran on `2f3d9d4` (pure code state); WRAP-UP/eli5 are docs-only commits on top → swept code
== promoted code (round-1's established pattern, restated in the plan). And `gh run watch` foreground
will hit the 10-min tool cap on a ~25-min network run — poll the conclusion after, don't trust the
watch's exit alone.

## What STOP means here
The arc never merges `dev-quality → dev`. R7's terminal act is the hand-off: green sweep + two
SHIP-WITH-NOTES passes + refreshed WRAP-UP/eli5 + four owner action items (the #220 commitlint reword,
the 1Password/unsigned-commit note, the R6 oracle-diff review, and the round-3 Q-13-residual candidate).
The owner QAs and merges #220. Done.
