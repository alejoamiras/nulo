# Lessons — R0 (UPDATE.md upgrade-checklist doc)

Merged `efc10a7` (PR #227). Light/docs. Quality run `28535527725` green; docs-only so smoke/network were N/A (NOT dispatched — `workflow_dispatch` bypasses the paths-filter, so dispatching them would run the full 25-min suite pointlessly for a markdown change).

## What shipped
`UPDATE.md` at repo root: the manual `@aztec/*` + Noir bump checklist. Seeded couplings (verified this session, current line `5.0.0-rc.1`):
- Bun patches on Noir wasm (`patches/@aztec%2Fnoir-{acvm_js,noirc_abi}@<ver>.patch`) — filenames pin the version; re-verify they still apply on bump.
- Noir WASM resolution — `apps/extension/vite.shared.ts:63` (nodejs-entry alias, fixes `__wbindgen_malloc undefined`) + `vite.config.ts:79`/`:286` (dedupe/exclude).
- Class-id invariants — `packages/aztec-runtime/src/pxe/artifact-class-id.ts` + the deferred fixture.
- `WalletSchema` patch — `packages/wallet-sdk-schema-patch/src/{apply,register}.ts`.
- Native proving — SHA-pinned `accelerator-server` in `_network-e2e.yml`.
Wired a pointer from `CLAUDE.md` § Dependency policy.

## The convention established (load-bearing for R4)
Any code that types against an `@aztec` shape MUST append to `UPDATE.md` § "Types coupled to `@aztec` shape" (`file:line` + what breaks on upstream drift). **R4/P18b is the first phase that must honor this** — its strong-typed PXE descriptor is exactly the coupling the owner wanted logged.

## Note
`gh pr checks 227` listed only the Cloudflare Pages entries, not the Quality check-runs — but the Quality *run* (`28535527725`) was `success` and `mergeStateStatus=CLEAN`, which is the authoritative merge signal. Don't over-read a sparse `pr checks` list; confirm via the run conclusion + mergeState.
