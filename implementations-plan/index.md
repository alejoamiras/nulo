# Implementations plan index

Format: `- [plan-name](plan-name/plan.md) — status — one-line hook`

- [faucet-bridge](faucet-bridge/plan.md) — completed (mega-deep) — L1↔L2 Portal bridge shipped into the faucet (public PR #76, private PR #78); swap/fuel remains a future arc
- [bridge-ux-trust](bridge-ux-trust/plan.md) — implemented (deep), awaiting manual testnet validation — swap-style bridge form + in-flight journal + seal-trust cache; PV5 HIGHs folded in
- [bridge-ux-feedback](bridge-ux-feedback/plan.md) — implemented (mid), awaiting manual testnet validation — live card narration, self-resolving dones, dual balances, direction chips, explorer links
- [bridge-form-stepper](bridge-form-stepper/plan.md) — shipped in PR #80 — takeover stepper, CAS foreground, dopamine UX
- [bridge-seal-backup](bridge-seal-backup/plan.md) — shipped in PR #81 — per-bridge sealed recovery file + restore
- [token-identity](token-identity/plan.md) — implemented + deployed (mid), awaiting manual testnet validation — NULO/OLUN/AZLO live, isTokenRegistered + contracts field-diff re-consent
- [incoming-trust-state-machine-refactor](incoming-trust-state-machine-refactor/plan.md) — completed (mega-deep, PR #75) — global service Lock serializing the incoming-transfer trust FSM; race class closed
- [authwit-lifecycle-and-execution-followups](authwit-lifecycle-and-execution-followups/plan.md) — completed (mid; 3 plan audits + 4-way post-impl fan-out), PR #85 awaiting QA+merge — grantPublicAuthwit RPC + lifecycle e2e (CI-gated) + exec follow-ups; audit caught+fixed a dead scope gate
- [execution-decomposition](execution-decomposition/plan.md) — completed (PR #83, awaiting manual QA + merge) — facade 2,302 → 746 lines, 3 executors + lane, zero behavior change
- [swap-fuel](swap-fuel/plan.md) — foundation merged (deep), private gas + B-presets in follow-up — bridge-and-fuel: atomic AZLO deposit + V4 two-hop slice→FeeJuice (live fee-987 pools), fjwc self-paying claim + inclusion-grade recovery; fuel withheld on private bridges until private FeeJuice ships
- [e2e-proverless-stub](e2e-proverless-stub/plan.md) — all phases ✓, PR #86 (deep; 3 plans + audit round + post-impl codex audit) — proverEnabled:false for bulk network e2e + SW-side barrier-gated stub for sequencing/cancel + prover-ON canaries; double-opt-in + DCE + negative-grep prod guard
- [method-metadata-registry](method-metadata-registry/plan.md) — **COMPLETE — merged via PR #91 (2026-06-15)** (deep; 3 plans + double audit + final codex pass + post-impl audit) — Q1 quality arc: folded six scattered wallet-method-metadata tables in @nulo/wallet-bridge into one MethodDescriptor registry; derive the tables; build-time exhaustiveness + runtime dispatch-entry guard kill the F1 silent-authz-hole class. (#91 also mooted Q21.)
- [profile-flow-dedup-q2](profile-flow-dedup-q2/plan.md) — implemented (deep), PR open vs dev, awaiting review + manual QA — post-impl codex audit: conditional approve (0 high/critical; 2 low test-coverage gaps addressed). Q2 quality arc: extract useProfileImportFlow/useProfileCreateFlow from the duplicated popup/onboarding profile import+create flows + relocate PasskeyCeremonyDialog/runPasskeyCeremony to neutral paths; behavior-preserving except 2 ratified quirk fixes (onboarding double-bootstrap, popup-create Enter guard)
- [private-fuel](private-fuel/plan.md) — in progress (deep), approved; P0–P3 code ✓ (private fuel deposit→claim end-to-end + no-fuel L7 faucet-only; faucet 330/330, bridge-core 107/107). Remaining: sandbox-gated P2 network-e2e + P4 live dust-canary + P5 harden — private Fee Juice via Wonderland PrivateFPC; gas-follows-token, B-presets, fee-method matrix
- [fuel-direct-bridge](fuel-direct-bridge/plan.md) — implemented (deep), all phases ✓ incl. Phase 5, **PR #104** — Fuel tab: L1 fee-asset → L2 Fee Juice direct bridge (no swap), public + private; reuses the bridge journal/engine via an additive `assetKind`; private = carrierless embedded-FPC claim. Post-impl codex audit remediated (HIGH recovery-stranding + UNDERLYING hard-block + fee-juice labelling). Local-gates-only (bridge-core 128 · faucet 366 · smoke 14 · typecheck:all · lint · build); live correctness deferred (I2 — gated on the `PRIVATE_FPC` re-canary)
- [fuel-l1-mint](fuel-l1-mint/plan.md) — implemented (light), both phases ✓, on **PR #104** — "Mint $AZTEC" card on the Fuel tab via the testnet `FeeAssetHandler.mint` (fixed 1000) so a zero-balance user can mint then fuel; pinned handler + fail-closed `FEE_ASSET()` cross-check + dedicated mint state + connected/Sepolia gate; also fixed the FuelForm default (12→20, above the 16 floor). codex plan conditional-approve folded; faucet 393 · smoke 14 · typecheck · lint · build green
- [proverless-network-stabilization](proverless-network-stabilization/plan.md) — approved (deep; 3 plans + double audit + final codex pass), ready to implement — get network e2e reliably green on every PR (zero flakiness) then make it required; root-cause + fix the proverless timing-races (#86) + #85's additions; journal-truth assertions, real-runner soak gate, broadened CI filter, retry:1 live
- [bridge-security-remediation](bridge-security-remediation/plan.md) — PR A merged (F-005/F-007); PR B cut over to testnet, awaiting review (deep, codex-guided deploy rework) — init-once portal (F-001) + single-minter proxy (F-002) + swapTarget-bound/non-zero-minOutput fuel (F-004/F-006); testnet-validated (both bridge paths + on-chain re-init revert); F-003 deferred, F-008 skipped
- [design-system-externalization](design-system-externalization/plan.md) — round-1 all 5 phases ✓ + user visual sign-off received (2026-06-18, both apps "no deltas") across 5 stacked PRs (#102 tokens · #103 base/theme/font takeover · #106 L1 core · #107 pure L2 · #109 p5 docs/cleanup); codex post-impl audit ✓ (no blocking; 2 Mediums folded in) + a faucet light-bg regression caught at the visual gate and fixed (5 dropped globals restored faucet-locally); `/code-review max --fix` + audit:vue + smoke green; network e2e via CI (deep; 3 plans + double audit + targeted takeover re-validation) — AMBITIOUS + SUPERVISED: single-source token contract + base/theme/font takeover (relocate-don't-drop) + externalized 9 primitives via an unplugin-vue-components resolver (zero template churn); look-same gated by machine checks + human visual; Button/SubPageHeader/ToastManager/Spinner/Banner/LoadingState/Tooltip/Popover/Input + storybook-rolldown fix deferred to round 2
- [design-system-externalization-round-2](design-system-externalization-round-2/plan.md) — **all 7 phases ✓ + user visual sign-off (2026-06-20, extension "no deltas" chrome+firefox; faucet correct)**; 2 stacked PRs (#123 P1–P6 holdouts · #124 P7 faucet cutover, revertible); post-impl `/code-review max` (fixed the Button wrapper link-branch attr-fallthrough) + codex `xhigh` audit (ship-with-fixes, no high/critical) + audit:vue/lint green (deep; 3 plans + contradiction-check + double audit + final codex conditional-approve) — finished the 9 L2 holdouts + composables into @nulo/design: router-free Button/SubPageHeader bases + extension wrappers (resolver discipline: add-on-delete only), Spinner superset reconcile, toast/outside composables behind `.js` re-export shims, Tooltip/Popover teleportTo, Input + internal sanitizeString, storybook rolldown fix + faucet base.css parity guard; the faucet AppButton→Button consolidation was ~invisible (already uppercase font-headline; the one visible change is the 4s spinner); toast-region unification + AppButton retirement deferred to round 3
- [design-system-externalization-round-3](design-system-externalization-round-3/plan.md) — **✓ COMPLETE — all 4 phases ✓; PR #127 → dev (network e2e green + user visual no-deltas sign-off 2026-06-21, on a v5/aztec-5.0 rebuild)** (`/blueprint light`; codex pre-audit conditional-approve + post-impl ship-with-fixes [1 MED fixed]; `/code-review max` clean) — round-3 close-out: **toast KEPT SEPARATE** (not unified — faucet queue vs extension single-transient; different host contexts; faucet already shares the package `Toast.vue` card), drop the `dark` color name (split: `tertiary` dots / `secondary` metadata), retire `AppButton` (migrate `DripButton` → `Button`, disable-on-loading made mandatory), delete the 9 round-1 local SFC shadows so the resolver finally takes effect (Checkbox/Toggle are reconciliations not pure ports — per-component tests + network e2e + human visual no-deltas sign-off)
- [quality-dedup-quick-wins](quality-dedup-quick-wins/plan.md) — **COMPLETE — all 5 arcs merged to dev** (mid; dual audit re-scoped 7→5 against current dev; R3=normalize) — Q16 dead-code (#105) · Q22 serializer (#108) · Q20 CAIP (#111) · Q14 restore-loop+R3 (#112) · Q7 config-sprawl+e2e:all-drift-fix (#113); one PR per arc, auto-merge --admin on verified-green. Network-gated arcs (Q22,Q7) initially red on a base-staleness bug — branches were synced to dev@#114, BEFORE #115's network-e2e de-flake; re-syncing onto #115 turned the suite green (the failing tests — authwit-lifecycle/incoming-transfers — were exactly what #115 fixed). Follow-ups: Q21 dropped (mooted by #91); **Q19 → own plan** (90 getActiveProfile sites, ~37 deliberate non-throwers, authz-sensitive)
- [network-e2e-required](network-e2e-required/plan.md) — approved (deep; 3-plan consolidation + 3 audit cycles; codex conditional-approve, condition folded), ready to implement via the /loop seed — Network e2e green on every PR + flip required, fixing roots: de-retry the masked suite (~8 retry:1/2 + 1 test.skip), measure-then-fix F1 authwits page, post-send authwit trust point (pending+reconcile, cap pre-send), boot-failure sentinel (exit 86), widen paths-filter; supersedes the proverless-e2e-{fix,diagnosis} follow-ups
- [aztec-5.0-upgrade](aztec-5.0-upgrade/plan.md) — **all phases ✓ — full network-e2e GREEN incl. 6B native-proving (CI run 27839983521); PR #122 open vs dev**. P1–P5 + P6/P6B + P7 done; test:all green (extension 2466 · faucet 336 · bridge-core 112 · aztec-runtime 32), lint 0, build, Noir recompile, verify:deployments. Nine 5.0 CI-adaptation fixes (lessons/phase-6.md): anvil-rename · forge-PATH · fee-ceiling · isL1ToL2→checkpoint · accelerator origin-gating · native-proving timeout · **senderForTags (shield/private-tx keystone)** · Fee-Juice claimability force-block · PrivateFPC construction-time deploy. Post-impl: /code-review + codex audit in progress. Deferred/operational: bridge --smoke + on-chain redeploy + keystone + min-age-excludes removal (~06-22). Was: approved (mega-deep; 5 research artifacts + independent Sonnet plan [fable unavailable] + 2 codex rounds: round-1 reject → fixed → round-2 conditional-approve, 3 conditions folded), ready to implement via the /loop seed — Aztec 4.2.0 → 5.0.0-rc.1 protocol HARD FORK across all TS packages + 3 Noir bridge crates; PublicKeys hash + immutables_hash address change, Schnorr→Poseidon2, PXE pre-v6 wipe, fee/gas + tx-receipt + deploy API churn; 8 phases, native-proving network-e2e on the canary shards = definition of done; accelerator-server downloads 5.0 bb on demand (no binary release needed); document-the-reset (no migration UX)
- [fuel-portal-v5-fix](fuel-portal-v5-fix/plan.md) — **✅ COMPLETE (light; all 3 phases ✓, codex post-impl `no high/critical`)** — child of aztec-5.0-upgrade testnet bring-up. The fueled-bridge Fee-Juice claim hung on V5 because our SwapBridgeRouter pointed FEE_JUICE_PORTAL at the dead V4 portal 0xd3361019 (V5's is 0x7c4176bf) → the FJ L1→L2 message never landed in the V5 inbox (token side worked via a separate fresh portal). Fixed: re-pinned the V5 portal in 5 Solidity/env sites + guarded PoolSetupHelper (router-only redeploy) + redeployed router 0xa2003149 (reuse swap 0x459ea79d + seeded pools) + promoted candidate manifest live. Validated end-to-end: candidate smoke self-paying claim PASSED in 3.2m; FJ message now folds into the V5 inbox (idx 3451904, checkpoint 3372 defined) vs pre-fix ~117M garbage. Codex 3× (plan 019ee564 + gate 019ee5b2 + post-impl 019ee648); wallet unchanged (claim pins leaf index, not portal). Follow-ups: optional old-router neutralization + reuse-matrix env guard + pre-existing DeployFuelLive.fork.t.sol PoolAlreadyInitialized drift. NOTE: commits local-only — push blocked on SSH key.
- [private-fuel-fee-fix](private-fuel-fee-fix/plan.md) — DRAFT (light; codex audit pending), awaiting approval — child of fuel-portal-v5-fix. After the portal fix, the PRIVATE fueled claim reverts `amount >= max_gas_cost` in Wonderland's PrivateFPC. 3 codex agents (019ee66b-01a4/-05ea/-09e5) confirmed NOT a Wonderland/5.0 bug — the FPC matches 5.0 getFeeLimit. Two causes: (1) fee-ceiling DRIFT — applyEmbeddedFpcGasCap pins maxFeesPerGas=minFees but finalizeGasLimits refetches+overwrites; (2) minFuelFj calibrated vs actual fee, not the getFeeLimit ceiling the FPC enforces. Fix: pin the ceiling (keep tight; leeway goes in minFuelFj not per-claim) + extend fuel-testnet with the private path + recalibrate empirically (≥3 runs) + fee-methods e2e gate.
- [vitest-vite8-dedupe](vitest-vite8-dedupe/plan.md) — implemented (mid), PR #169 (e2e CI in progress) — one vite@8.x in the tree by **DELETING the unused vite-plugin-vue-devtools chain** (codex caught it's dead — not a version bump) + deduping vitest via root `overrides:{vite:^8.0.0}` + extension `test`→`vitest run`; behavior-neutral (test:all counts identical to vite 7). Audits: codex reject→delete-don't-bump · Sonnet conditional · final codex conditional · post-impl codex conditional (index fix)
- [no-fuel-claim-fee-source](no-fuel-claim-fee-source/plan.md) — **✅ COMPLETE (light; all 3 phases ✓)** — child of private-fuel-fee-fix. The private fuel claim credits FJ as a PRIVATE balance at the Wonderland PrivateFPC; a subsequent NO-FUEL bridge claim couldn't spend it (the no-fuel gate read only public FJ → "no gas"). Fix (shipped behavior, after a user-directed simplification): the no-fuel claim now **unblocks** when there is gas in EITHER balance and lets the **wallet's own fee picker** select the method (Public OR Private Fee Juice, or Sponsored) — the faucet does NOT pre-select, exactly as the public path always worked. `decideNoFuelClaimGate` (`allow`/`none`/`unverifiable`, **fail-closed** on unread); manifest gains `PrivateFPC.balance_of` (utilities) + `pay_fee` (sim+tx — the wallet calls `pay_fee` when the user picks Private Fee Juice). The original private-first deterministic supply + the **codex-C** estimate/cache machinery (conservative pre-flight gate + exact gasLimits cached from the journal's first post-sync simulate) was implemented + **PROVEN live on V5** (a tx self-paid from existing private FJ via pay_fee, FPC 295.14→292.84 FJ, spent 2.29) then REMOVED per the user's "don't pre-select, let the wallet pick" preference; `privateFeeJuicePayment` survives in bridge-core for the fuel-testnet proof. Codex plan-audit reject→conditional-approve + estimate-fork verdict C; post-impl codex stalled 2× (service-side) → substituted by a documented self-audit (`no high/critical`). Gates: faucet 347 · bridge-core 116 · lint 0. Commits LOCAL-ONLY (unsigned — 1Password agent was locked; push + signature backfill pending). Follow-up: dApp-supplied-FPCFeePaymentMethod settlement e2e (needs the cluster-A+B FPC-balance fixture); re-run post-impl codex when the service recovers.
- [private-fuel-fee-fix](private-fuel-fee-fix/plan.md) — **✅ COMPLETE (light; all 3 phases ✓, codex post-impl `no high/critical`)** — child of fuel-portal-v5-fix. After the portal fix, the PRIVATE fueled claim reverted `amount >= max_gas_cost` in Wonderland's PrivateFPC. 3 codex agents (019ee66b) proved NOT a Wonderland/5.0 bug (FPC matches 5.0 getFeeLimit). Root cause: the wallet's embedded-FPC maxFeesPerGas drifted (post-sim refetch×multiplier inflated the FPC ceiling). Fix: (P1) finalizeGasLimits pins the pre-sim cap for embedded payments (no refetch); (P2) fuel-testnet gained a true private-FPC variant (register-only, salt 0 — no deploy per 019ee697) + reprice-per-retry + 1.5× pad — which CAUGHT the inclusion-reject codex predicted (static cap fell below risen base fee) and fixed it; (P3) calibrated minFuelFj from the real getFeeLimit (~2.5 FJ ceiling → 4× = 10.17 FJ, grounded vs the V4-era 11 FJ guess). PROVEN live: 3 private-FPC self-paying claims SETTLED on V5. Codex plan 019ee67b (reject→conditional-approve, headroom-in-committed-cap) + post-impl 019ee6e0 (no high/critical; 3 LOW hardened: component-wise predicted fees, narrowed fallback, 4× margin). Settlement gate reframed (live proof > sandbox e2e; bridge-on-sandbox e2e = follow-up). Validated the user's "multiply for leeway" instinct. NOTE: commits local-only — push blocked on SSH key.
- [q3-transport-unification](q3-transport-unification/plan.md) — **COMPLETE — all 6 phases ✓, post-impl audit resolved; merged dev (aztec 5.0); PR #121 awaiting final-network + review** (deep; 3 plans + double audit [both reject R1] + final codex pass [reject v2 → approve v3] + post-impl codex audit [changes-required → 5 findings fixed]) — Q3 quality arc: de-forked the two parallel RPC transport stacks in @nulo/extension-messaging (Port background vs sendMessage offscreen) into one shared `BaseServiceClient` correlator + `BaseService` core via template-method hooks; offscreen errors flipped string → typed WalletError with stable leak-free `toWalletResponseError` Rpc* cases; callable-any-method closed by an exhaustiveness-checked `rpcMethods` registry across 21 services + PxeService; + onDisconnect→reconnect unit coverage. Per-phase network green (jobs confirmed run, not skipped); exactly-once unit-proven, lifecycle real-Chrome-e2e. Codex: D10 guard design (option A) + post-impl audit (2 HIGH/2 MED/1 LOW all fixed). See `lessons/wrap-up.md`
- [passkey-display-name](passkey-display-name/plan.md) — **✅ implemented (light), PR #138 → dev** — brand the iCloud Keychain / password-manager label of NEW passkeys from opaque `profile-<id>` to `nulo-{name}-{id}`: pure sanitizing `passkey-label.ts` threaded through both create paths (incl. the PATH B `acquireRecovery→createForNewProfile→createKey` chain codex caught) into the one WebAuthn `buildCreateOptions`; cosmetic-only (no key/address change), extension-only (faucet has no passkeys); codex post-impl `no high/critical`; manual iCloud smoke confirmed
- [frontend-ux-fixes](frontend-ux-fixes/plan.md) — **✅ COMPLETE — all 6 phases ✓ + user keyboard/visual sign-off (2026-06-22); PR #140 → dev** (mid; dual audit reject×2 + final fresh-context codex reject — all folded; post-impl codex `ship`) — six reported extension UX fixes: P1 Settings "Profile" row copy + contact double-border; P2 retire `vault` → reusable flat-grey initials `AccountAvatar`; P3 Send recipient card (name + masked `0x……tail` + **optional one-tap reveal** — informed risk-acceptance over a forced gate); P4 `AddressInput` pin-on-blur (long `0x…` can't be drag-scrolled at rest; value + e2e selector preserved); P5a tab-order ROOT fix (no positive `tabindex`; `<div>` Toggle/DropdownItem → `0` + keyboard ops; `DropdownRoot` arrow-nav decoupled to `[data-dropdown-item]`; roving method tablist; CLAUDE.md "Keyboard & focus order" convention) / P5b sweep + human sign-off. Audits caught: false "verified facts" (hashToEmoji-as-indicator, wrong onboarding file), a `<div>`-tabindex focus regression the first revision introduced, and the wrong `test:components` gate command. Plus a user-directed Send-layout polish (de-stacked paddings, smaller token picker, recipient card style D, worded red "Invalid address" hint)
- [quality-arc-completion](quality-arc-completion/plan.md) — **✅ COMPLETE on branch `dev-quality` (off dev 65961f1); ready to promote → dev** (meta-orchestration) — finished the `/harden quality` arc (run 2026-06-11-ultra-50b45d): 8 contained findings (Q12 e2e-fixtures · Q15 purge-cascade · Q17 ContractResolver · Q6 activity-feed · Q8 popup-forms · Q13 PXE-subset · Q9 service-readiness · Q18 execution-tuples), each re-verified→blueprinted→implemented inline→units+smoke+FULL network-e2e (jobs-run)→squash-merged. Q8 carried a codex-found display-reset regression (fixed `c41a2928`). **4 post-integration audits across 2 model families (3 codex + 1 Claude) converged on that single bug**; + a focused token-balance purge-cascade test (#139). Capstone full-network sweep GREEN. DEFERRED (supervised later, not touched): Q4/Q5/Q10/Q11/Q19(authz)/Q23.
- [design-system-faucet-adoption](design-system-faucet-adoption/plan.md) — **IMPLEMENTED — Phases 1–4 ✓ (8 commits on `feat/faucet-design-adoption`); Phase 5 human visual gate HELD for user; not yet PR'd** (deep; 3 plans + contradiction-check + double audit + final fresh codex [R1 `reject`→restructured, fable `conditional`, final `019eeef4` `conditional`] + post-impl codex `019eef2f` `no high/critical` [2 med/low folded] + self-audit; **PR #147 → dev open, awaiting human visual sign-off**) — ~17 `<Flex>` adoptions across 9 components + views; gates green (403 unit · 14 smokes · build · lint · audit:faucet); pills (`Tag`/`Badge`) tested → documented non-fits; brutalist controls/footers/grids/off-scale-gap view roots kept local; Phase-5 visual gate caught + fixed a missing-`gap--28` regression — whole-faucet `@nulo/design` adoption via a **primitives-only** `unplugin-vue-components` resolver (`Flex`/`Text`/`Icon` + `Tag`/`Badge` if a pill fits) wired into vite + BOTH vitest configs (faucet tests render prims for real — no stubs; the extension's vitest doesn't even use this plugin, so Phase 1 carries a real bare-tag proof). The audits killed the original zero-value import-cutover (keep `Button`/`Card`/etc. explicit), caught a machine-invisible `.links`-orphan regression, fixed an `--frozen-lockfile` sequencing bug + `biome.json` faucet-types exclude, and turned an asserted-low reuse ceiling into an evidence-based inventory (`Tag` was extracted from the faucet → primary pill win; `Text` font via utility classes; class-preserving swaps allowed). Seam-grouped (wallet/verification → journal/stepper/receipt → form/card), consume-only, visual-equivalent light-theme bar, mandatory human visual gate (round-1 missing-rule lesson)
- [release-dev-to-main](release-dev-to-main/plan.md) — **✅ APPROVED (deep) 2026-06-22 — executing (agent-driven)** — coordinated dev→main stable release of all 3 surfaces (extension GitHub Release + faucet auto-deploy + landing) via existing automation as-is. Decisions: **v0.23.0** (`Release-As`, NOT the 1.0.0 release-please defaults to), faucet contracts NOT redeployed (drips dead until follow-up — A5), accept the cross-fork window (A6), re-run on e2e flake (A7), agent drives every step. 3 independent runbooks converged; 2 conditional-approves + a final fresh-codex **reject** (5 findings: landing-wiring, unwired A6/A7, A1↔ledger contradiction, loose gates, DAG error) — all folded. v4 manual unstick accepted; post-step = prerelease-manifest re-baseline (stale 0.20.2 → 0.23.0)
- [release-dx-hardening](release-dx-hardening/plan.md) — **✅ COMPLETED 2026-06-22 → PR #149 (into dev)** (deep, staged rollout; closing gauntlet: /code-review + codex post-impl audit [4 real fixes — see [audit-codex-postimpl.md](release-dx-hardening/audit-codex-postimpl.md)] + /harden token least-privilege). Phase 4 (throwaway test repo) skipped per the fallback → the staged real-release rollout is the rehearsal. **Post-merge action: flip `AUTO_UNSTICK_ENABLED=on` after the 1st flag-off release, then add `verify-live` to the status aggregator.** — made dev→main near one-click after the painful v0.23.0 cut. 8 phases (harness → cheap-config → faucet chain-identity → test-repo → deploys+live-smoke → auto main→dev sync → in-run auto-unstick → docs). Scope: `bump-minor-pre-major:true` (no more accidental 1.0.0), commitlint title-lint on promotes, **drop the faucet `VITE_CHAIN_*` env path** (`.env.example:32` shipped the stale `4127419662` that broke prod), fail-closed post-deploy verify against an emitted `build.json`, auto-sync (open-PR-let-GitHub-conflict), and the v4 auto-unstick **in-run + behind `vars.AUTO_UNSTICK_ENABLED`** (codex's race-avoiding design + Plan-sub's kill-switch). Validated via unit-tested `scripts/release/*.ts` + `dry_run` + a throwaway test repo; closing gate = next real release runs push-button. NOT marketplace. Both independent audits conditional-approve (conditions folded); `/harden security` queued post-impl
- [required-check-mismatch](required-check-mismatch/plan.md) — **✅ COMPLETE 2026-06-24 — dev+main re-pointed to bare names; #171 merged with plain `gh pr merge` (no --admin), #172 deliberate-red correctly blocked, main #173 + flake re-run** (mid; codex + opus-Plan dual audit [both conditional-approve] + final fresh-context codex pass [**reject** → all 5 findings folded]) — **root cause of "every PR needs `--admin`"**: branch protection required `Quality / Status` ×3 but GitHub Actions produces bare `Status` ×4 (only `uses:` jobs get a `{workflow}/{job}` prefix; the `Quality /` form was a hand-typed phantom) → required gates hang `Expected` forever → orange → forced `--admin` repo-wide. Verified live on #169/#160/#137 (PROOF 1–5; no merge queue; main `strict:true`/dev `strict:false`). Fix: rename the 3 required aggregator jobs to unique bare names → re-point `required_status_checks.checks` (NOT deprecated `contexts`; app_id-pinned, read-modify-write preserving `strict`) union-first, **dev fully then promote→main then re-point main** (pre-rename main PRs would re-hang); positive AND negative acceptance on dev. Retracts the raw-merge-API "clean path" claim (admin-bypass); fixes the `--admin` docs to the two-gates truth (name-mismatch verified cause; `required_signatures` separate, author-only-squash scoped); Smoke stays required (stale "advisory" doc corrected)
- [dependency-refresh](dependency-refresh/plan.md) — **IMPLEMENTED — PR #178 → dev** (light; codex conditional-approve, 3 conditions folded) — one consolidated in-range `bun update` across all 11 pkgs + root (186 bun.lock entries, 12 package.json `^`-floor bumps, no cross-major, `@types/node` ≤24; full-diff min-age 186/186 ≥7d); the planned 2-stage libs/tooling split proved mechanically impossible in bun (per-child update refreshes each child broadly) → collapsed to one refresh + push-through. zod-v5 premise moot (no v5; on 4.4.3). Fallout fixed inline: biome 2.4.15→2.5.0 (`noSvgWithoutTitle` `.svg`-exclude, `noRedundantRoles`, `useVueMultiWordComponentNames` off, `preset` migrate) + `@types/webextension-polyfill` 0.12.5 Alarm cast. typecheck:all+lint+test:all + 5 builds + smoke e2e green; network e2e in CI
