# Q9 — service readiness (mid → codex-narrowed; gate deferred)

**Branch:** `q9/transport-readiness-gate` off dev-quality `33f3f21`. **Finding:** `verified.md` Q9. Audit framing: "architectural **drift**, NOT proof of a pervasive current null-deref path."

## Re-verification
Valid. `BaseService` (`extension-messaging/src/core/base-service.ts:70`) has `ensureInitialized()`; methods hand-copy `await this.ensureInitialized()` (profile 24, network 17, …). `ServiceCollection.start()` runs topological phases from declared `dependencies`; only 2 services declared them (contact, incoming-transfer).

## Codex consult (xhigh, sess bguESCDM)
Recommended: (1) central RPC readiness gate in `handleRequest` (inside try, after `beforeInvoke`), (2) expand `dependencies` on clear init-time callers (TokenBalanceService the clearest), (3) KEEP per-method preambles — bulk removal unsafe (no reliable RPC-only signal: token-balance + incoming-transfer call peers in-process). Double-await harmless.

## DECISION — ship the safe lever (deps), DEFER the gate
- **Shipped: deps-expansion (COMPLETE).** A grep of every service `init()` body for awaited peer-service calls found **TokenBalanceService is the ONLY init-time peer-awaiter still missing a declaration** — its `init()` awaits `profileService.getActiveProfile()` + `tokenService.getTokensRaw()` (service.ts:92-94), today relying on those callees' `ensureInitialized` poll. (execution's lone hit is a deferred `getChainId` CLOSURE, not an init-time await; contact + incoming-transfer already declare.) Added `dependencies = [ProfileService.name, TokenService.name]`. Topology tests pass — no cycle, ordering correct. Behaviour-preserving (callee preambles remain as backup; only startup ordering tightens).
- **Deferred: the transport gate.** It is the substantive "centralize transport-side gating" lever, BUT: (a) it CHANGES base-class behaviour — codex concern 1: RPCs that touch only local state can currently run before init; gated, they inherit the 30s poll/timeout, and a service with a hung `init()` would block ALL its RPCs (today local-state RPCs still answer); (b) it **breaks 14 base-service dispatch tests** (background+offscreen) which `new TestService()` then dispatch WITHOUT `start()` — empirically verified (14 failed). For a finding the audit explicitly calls "drift, NOT a live null-deref," a base-class readiness-contract change affecting every service + 14-test churn is disproportionate risk under the preserve-verbatim/no-flake mandate. Codex is advisory; the user's behaviour-preservation instruction takes precedence. Deferred to a focused follow-up: gate (correctly placed) + start() the dispatch tests + cold-start/backup-restore regression.

## Validation
- `bun run lint` exit 0 · `bun run typecheck` exit 0.
- wallet-core/base (topology) + token-balance tests: 38 passed (no cycle introduced; phase ordering correct).
- Network + smoke CI exercise cold-start (the only observable effect of the deps reorder).

## Outcome
Deps lever fully applied (the one missing init-awaiter declared); preambles kept; gate deferred with rationale. Behaviour-preserving.
