# Q13 — pin the PXE in-process subset (light/mid, type-only)

**Branch:** `q13/pxe-subset-assertions` off dev-quality `b866bb1`. **PR:** #132. **Finding:** `verified.md` Q13.

## Re-verification (step 1)
Valid. PXE in-process surface hand-synced across 3 declarations, no drift guard:
- `spec.ts` `Methods` = **21** methods (each with a leading `network` param).
- `ipxe.ts` `IPXE` = **18** (drops `network` + promisifies; omits `getNoteSchemas`, `getBlockTimestamp`, `clearChainState` — those are SW-only).
- `proxy.ts` `PXEProxy implements IPXE` = the 18 currying delegations.
- `client.ts` = per-method zod (can't be type-generated → stays hand-written).
Counts match the audit (21/18). aztec 5.0 didn't change the split.

## Implementation (type-only, zero runtime change)
`pxe/subset.ts`: canonical `PXE_SUBSET_METHODS` (the 18 in-process names, `as const`) + compile-time assertions via `Expect<T extends true>` + a robust `Equal<A,B>`:
1. `PxeSubsetMethod extends keyof Methods` — every subset key is a real Method.
2. `Equal<keyof IPXE, PxeSubsetMethod>` — IPXE's method set === the subset.
3. `InstanceType<typeof PXEProxy> extends IPXE` — proxy stays an IPXE impl (belt-and-suspenders over its `implements IPXE`).
Per **constraint #14**: pins the KEY boundary only (no mapped-type derivation — that must strip `network` + promisify, and `client.ts` zod can't be generated). The const is tree-shaken (no bundle impact); the assertions are compile-time. `subset.test.ts` pins the count (18) + the deliberate SW-only exclusions.

## Validation
- `bun run lint` exit 0 · `bun run typecheck:all` exit 0.
- **Where the guard runs:** local `bun run typecheck` is extension-only (vue-tsc on the extension tsconfig) and does NOT cover aztec-runtime. The assertions are enforced by `typecheck:all` → `bun run --filter '@nulo/*' typecheck` → aztec-runtime's `tsc --noEmit`, which CI's Quality (`_lint-and-typecheck.yml` runs `typecheck:all`) executes. So the guard IS live in CI.
- **Drift-guard PROVEN:** temporarily dropping `getContracts` from the list → `subset.ts: error TS2344: Type 'false' does not satisfy the constraint 'true'` (assertion #2 fired); reverted → clean. A type assertion that doesn't actually catch drift is worthless, so this verification is the real test.
- subset.test.ts: 2 passed.
- Network e2e: dispatched (type-only → vacuous, but ran per the every-arc mandate; also confirms the build compiles with subset.ts).

## Minor learning
Avoid backticks in `git commit -m "...double-quoted..."` — the shell runs them as command substitution (got `command not found: implements`/`network`). Use a body-file or single quotes.
