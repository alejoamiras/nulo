# Q12 — e2e fixture dedup (light)

**Branch:** `q12/e2e-fixture-dedup` off `dev-quality`. **Finding:** `audit/quality/2026-06-11-ultra-50b45d/findings/verified.md` Q12.

## Re-verification (step 1)

All four duplication classes confirmed STILL present on `dev-quality` (file had grown to 1322 lines post-Q1/Q3):
- Inline `phase` step-tagger redefined verbatim in **4** fixtures (`dappConnectedExtensionPerTest`, `…WithAccountsCap`, `…WithTransactionCap`, `…WithFirstTwoAccountsCap`).
- launch→registerProfile→openPopup→waitForHash(general)→switchToLocalNetwork→close→connectPlayground ladder repeated across those 4.
- Cap-grant choreography (select bundle → snapshotResultSeq → waitForPopup(60s) → click → wait `cap-account-item`(60s) → read ids → approveCapabilities → waitForPgResult) repeated **3** times.
- `TEST_PASSWORD` literal re-declared across many files.

**Key finding — the deferral condition triggered.** The code's own comment (`extension.ts:288-301`) said the cap-grant helper was "intentionally duplicated rather than abstracted … Refactor once we have three or more such fixtures (CLAUDE.md 'same code in 3 places' threshold)." There are now exactly **three** cap-grant fixtures, so the comment self-authorizes the extraction. No conflict; no codex consult needed (the only open question was this comment, resolved by reading it).

## Implementation (behaviour-preserving)

Three module-private helpers added to `fixtures/extension.ts` before the fixtures block:
- `makePhase(prefix)` → returns the generic `PhaseTagger`. Identical `[<prefix>:<step>] <msg>` format. (The standalone `connectPlayground`'s own `step` wrapper uses a *different* format — `connectPlayground:<name> — <msg>` — and was left untouched.)
- `setupConnectedPlayground(prefix, beforeClose?)` → the shared register→network→connect spine. `beforeClose(setupPage, phase)` hook lets `…WithFirstTwoAccountsCap` insert its `createSecondAccount` + persistence-assert between the network switch and the popup close, preserving exact step order.
- `grantCapBundle(ctx, page, bundle, pick)` → the cap choreography; `pick(accountIds, capPopup)` owns the per-fixture selection + its failure messages. `pickFirstAccount` (module const) covers the two single-account fixtures; the first-two fixture passes an inline `pick` carrying the verbatim storage-dump discrimination error.

**Verbatim preservation:** every phase name, every timeout (60s popup/row, 30s pg-result, 30s waitForHash), every error string (`"capabilities popup returned no accounts"`, the `exposed only […]` discriminator with `AT-CAP-TIME`/`POST-CREATE`), and every step ordering are byte-identical to the originals. No `data-testid` touched. `noUncheckedIndexedAccess` is off → `const [accountAddress] = await grantCapBundle(...)` types as `string` cleanly.

## TEST_PASSWORD consolidation (scope note)

Audit said "re-declared in 4+ test files". Found a single canonical home was missing: `helpers/import-drivers.ts` already *exported* it, but `helpers.ts`, both `sw-*.test.ts`, and `check-derivation-parity.ts` re-declared the same literal, and `registerProfile` + `feeJuiceImportedExtension` inlined it. Created a **zero-import** `fixtures/constants.ts` as the single source (zero-import → no new cycle with the existing `extension.ts ↔ helpers.ts` function-cycle). `import-drivers.ts` now re-exports from it (its two consumers unaffected).

Went one step past the literal `TEST_PASSWORD` redeclarations to also source the same-valued password in `security.test.ts` (`ORIGINAL_PASSWORD`), `security-backup.test.ts` (`PASSWORD`), `registration.test.ts` (`testPassword`), and `auth-flows.test.ts` (inline) from `TEST_PASSWORD` — **preserving each semantic name** via `const X = TEST_PASSWORD`. Rationale: `registerProfile` now sources `TEST_PASSWORD`, so any test that unlocks a fixture-registered profile with a hardcoded copy was coupled-by-value, not by reference — a latent drift hazard the audit flags. `NEW_PASSWORD` / `WrongPassword!` (distinct values) left untouched. `onboarding-tab.test.ts`'s deliberately-different `OnboardingTest_!23` left untouched.

After: `"TestPassword123!"` appears in exactly ONE place (`constants.ts`).

## Validation

- `bun run lint` → EXIT 0 (51 pre-existing repo-wide warnings unchanged; `readPopupAddress` dead-code warning in `check-derivation-parity.ts` is **pre-existing**, untouched by this arc, out of scope).
- `bun run typecheck` (vue-tsc, whole extension incl. e2e) → EXIT 0.
- Smoke (`bun run test:e2e`) → **68 passed / 6 skipped / 1 failed**. The single failure (`passkey-backup › Escape during modal resets agreement gate`, 10s `waitForFunction` timeout, all 3 retry attempts) is **PRE-EXISTING**: reproduced identically on base `dev-quality` (standalone `passkey-backup` run there fails the same test the same way). Shares zero code with Q12 (uses `setupPasskeyVirtualAuth` not `registeredExtension`, its own `ENCRYPT_PASSWORD`, failing wait routes through untouched `patchPagePolling`). Passkey-ceremony Escape-gate UI bug pre-dating the arc — NOT a Q12 regression, NOT one of the 8 findings → tracked separately, not fixed here (scope).
- Network → dispatched on the q12 branch: run `27911369853` (Quality `27911370756`). See CI-trigger note below.

## CI-trigger learning (MARATHON-WIDE — applies to every arc)

`pr-quick.yml`, `pr-smoke-e2e.yml`, `pr-network-e2e.yml` all filter `pull_request: branches: [main, dev]`. **A PR whose base is `dev-quality` triggers NONE of them** (PR #128 showed only Cloudflare checks). All three expose `workflow_dispatch`, and dispatch bypasses the `changes` paths-filter (every network job runs). **Arc gate mechanism: `gh workflow run <wf>.yml --ref <arc-branch>`, then read the run conclusion** — do NOT wait for PR checks that never appear. `dev-quality` has no branch protection → the arc merges on MY validation (dispatched CI green + jobs-confirmed-run); no `--admin` needed.

## Outcome

Net −39 lines across 11 files, all test-infra (zero production runtime). Merge SHA: <FILL>.
