# Q17 — finish ContractResolver extraction (mid → SHRUNK)

**Branch:** `q17/ensure-registered` off dev-quality `051546c`. **Merge:** squash **`54d0b39`** (PR #130). **Finding:** `verified.md` Q17.

## Re-verification (step 1) — SHRUNK
The audit's run (2026-06-11) predates work that already landed most of Q17:
- `findFunctionByName` (`contract-resolver.ts:43`) + `findFunctionBySelector` (`:50`) are **already extracted** AND **migrated** to the call sites (`tx-request-builder.ts:275/306`). The audit's "name-lookup ×4 / selector-scan ×3" families are done.
- `ensureContractsRegistered` (batch, takes Maps) already exists (`:134`).
- `transfer-public.ts` `getCandidates` is a domain ABI-shape matcher, NOT a generic lookup → out of scope.

**Remaining piece:** the hand-rolled single-contract `getContracts()→registerContract` prologue still at **4 sites**: token (clearChainState path ×1 at :312, import path ×1 at :397) + fpc (:259, :359).

## Implementation
Added `ensureRegistered(pxe, contract, instance, artifact)` — a single-contract free function (twin of the batch method), placed beside `findFunctionByName`/`findFunctionBySelector` in `contract-resolver.ts`. Replaced all 4 prologues with it. Per **constraint #6**, each caller keeps its own artifact resolution + frozen error strings (`"contract instance not found"`/`"contract artifact not found"` in token; the FPC-specific messages in fpc) — the helper dedups ONLY the register-iff-absent guard. New `contract-resolver.test.ts` cases pin register-when-absent + skip-when-present.

## Validation
- `bun run lint` exit 0 · `bun run typecheck` exit 0
- Targeted units (contract-resolver + token + fpc): 51 passed (incl. 2 new `ensureRegistered` cases). Full unit suite + build validated via **Quality CI 27913405058 = success** (avoiding local-machine exhaustion per Q15 lesson).
- Network CI **27913404523** = all 8 Aztec-agent jobs green (none skipped). Smoke CI **27913405682** = success.

## Outcome
1 helper + 4 site migrations + 2 tests. Merge **`54d0b39`**. Behaviour-preserving; frozen error strings untouched. Net −? lines (token −17, fpc −14, +helper).
