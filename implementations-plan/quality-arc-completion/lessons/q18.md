# Q18 — execution tuples → named result objects (mid → SHRUNK)

**Branch:** `q18/named-result-objects` off dev-quality `5b5f34e`. **Finding:** `verified.md` Q18 (step 1 only — internal tuples; the public `TransferRequest` param-object is step 2, explicitly out of scope).

## Re-verification — heavily SHRUNK
Most of Q18's cited internal tuples were ALREADY converted to named objects by the execution decomposition (#83, `a03586a`) + the aztec 5.0 churn:
- `StandardTxRequestResult` / `FeeEstimateResult` no longer exist.
- `fee-strategy.ts` returns a named `FeeEstimate extends BuiltStandardTx` (comment: "Field names — not positions — are the contract; same-typed slots made the old tuple a silent-transposition hazard" — i.e. Q18's exact concern, already addressed).
- The fee strategies (fpc/embedded/fee-juice/…) all return named objects; `built[0..7]` positional indexing is gone.
- The `operation-planner` `[op, token, fn, args]` site is already `return { op, token, fn, args }`.

**Only remnant:** `OperationPlanner.processAztecJsPayload` returned `[Action[], AccountFeePaymentMethodOptions, FeeOptions]` — a 3-tuple of three different types. (The contract-resolver `[string, object]` pairs are NOT Q18-cited and are different-typed/low-risk → left.)

## Implementation
Introduced `export interface ProcessedAztecJsPayload { actions; feePaymentMethod; feeOptions }`; `processAztecJsPayload` now returns it. Migrated the 4 consumers — `dapp-send-executor` ×2 (`{ actions, feeOptions: fee }`, skipping feePaymentMethod), `view-executor` ×2 (`{ actions, feePaymentMethod, feeOptions: fee }`) — and 5 test sites (3 operation-planner destructures + 3 mock returns across view-executor/dapp-send-executor, array→object). Intra-extension (both ends ship together); behaviour-preserving (same values, named instead of positional).

**Constraint #3 (FpcStrategy byte-parity):** untouched — fpc-strategy already returns a named `{ ...built, feePaymentMethod }` (done by #83); this arc only touched operation-planner + its consumers, never normalizing the fee-strategy family.

## Validation
- `bun run lint` exit 0 · `bun run typecheck` exit 0.
- Full execution test suite: **281 passed** / 1 skipped / 7 todo (24 files) — operation-planner, dapp-send-executor, view-executor + all execution tests green.
- Network e2e (sendTx / simulate / profile flows) exercises processAztecJsPayload end-to-end.

## Outcome
1 tuple → named object + 4 consumers + 5 test sites. The bulk of Q18 was pre-done by #83; this finishes the last internal tuple. Behaviour-preserving; FpcStrategy parity intact.
