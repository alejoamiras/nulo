# Phase 8 — wizard UI, smoke, retire legacy (2026-09-02)

Branch `any-erc20-bridge/wizard` (Arc 4).

## What shipped

- **Step components** (`components/send/`): `StepStrip` (roving tablist), `TokenTile` (committed sprite via `<use>` when `SPRITE_KEYS` has the `chain:address` key, else a striped monogram whose hue hashes the key — never the symbol), `TokenList` (`listbox`, one Tab stop), `PasteAddress` (20-byte hex gate, lowercase), `TokenStep`, `ChoiceCards` (TOKEN / TOKEN + gas / Gas; gas cards carry the no-route reason; exits reduce to TOKEN), `GasBreakdown` (two lines, "change" reveals the tx-target control), `AmountStep`, `ReviewDetails` (collapsed), `ReviewStep` (five lines, first-time note without addresses, burn-before-finish line on exits). `assets/token-sprite.svg`: five original disc-and-glyph marks for mainnet USDC/USDT/WETH/WBTC/cbBTC; `token-sprite.test.ts` refuses `<script>`, `on*`, `<foreignObject>` and any id off `chainId:0x…`. `SpriteSheet.vue` adopts PARSED `<symbol>` nodes (DOMParser + `importNode`) so no raw-HTML sink exists.
- **Shell**: `App.vue` → `IS_MAINNET ? MainnetPlaceholderView : <AppShell>` with `AppShell` a `defineAsyncComponent` — the only way the wallet-session singleton's module is never evaluated on mainnet (verified: a lazy chunk the mainnet entry references only through `__vite__mapDeps`). `AppShell.vue` tabs Faucet / Send. `SendView.vue` → `IS_PLACEHOLDER ? placeholder card : <SendWizard>` (the placeholder must not instantiate the wizard composables, hence the child). `SendWizard.vue` owns every composable, the `wizard | stepper | receipt` machine, the journal-foreground reconciliation, step gating, the grant prompt (deposits at Sign & send inside `useSend`, exits at selection with a stale outcome discarded), `portalVerified` (`portalOf` vs `predictPortal`), the estimate strings.
- **Two flags, not one**: `IS_MAINNET` (build target) drives the placeholder-only shell, the skipped node probe and the remote-free CSP; `IS_PLACEHOLDER` (`manifest.bridge === null`) only blanks the SEND tab — on testnet the FAUCET tab keeps working while the generation is not yet promoted. Testnet CSP gains `https://tokens.uniswap.org`.
- Send records in the existing surfaces: stepper `register` phase, receipt token + gas + "review said / you got" + add-token CTA, journal card shows a persisted `blocked` reason with DISCARD as the only action.

## Gotchas

- `?raw` SVG imports work under the faucet's jsdom vitest (unlike `.css`), so the sprite keys derive at module init from the same file the sheet renders.
- `parseAmount` in `lib/format.ts` truncates and returns `0n` on junk — useless for a field; `parseAmountStrict` returns `null` and `toDecimalString` renders MAX.
- The first integration run had `SendWizard.test.ts` asserting object identity on a route object and a stale-selection expectation that leaked across tests — fixed on the shell side, not by loosening the components.
- `bun run --cwd apps/faucet test:e2e` went red the moment `useWalletConnection` imported the v2 reader (module-init strict parse of a v1 file); it turns green with the manifest flip in the retire step — the reason the retire step is one commit, not a series.

## Send smoke (`tests/e2e/send-smoke.test.ts`, jsdom, 12 scenarios)

Mounts `SendView` with only the two wallet panels stubbed; the real wizard composables and the real journal engine run over jsdom `localStorage`; every selector is a `data-testid`. Scenarios: list + paste (invalid paste error); the grant prompt at Sign & send — granted → `runSend`, declined → nothing signed; a stale-selection completion discarded; no-route disables the gas cards; the first-time note + `register+claim`; portal-exists-unregistered; the 2-tx private auto-continue (`register,claim`, `sendStepperRegister`); gas-only journals no token block; an exit prompts at SELECTION and a paused hub/L1 refuses before any authwit while unpaused burns through the hub with the burn line on review; the placeholder network renders `sendUnavailable` and instantiates nothing. bb.js poseidon throws `BBApiException: std::bad_cast` under jsdom, so `computeSecretHash` and the two claim-secret derivations are replaced by a mutually consistent keccak stand-in — consistency is the only property the send depends on — and `Contract.at` + four `deposit-flow` exports are faked for the same reason. **The smoke found a real gap**: a paused exit surfaced through the generic red error line; `useHubExit` now exposes `paused: "l1" | "l2" | null` (a state, cleared on the next attempt; the provisional record is discarded) and `ReviewStep` renders `sendPausedNotice` — "your balance is untouched" — instead of an error.

## Retire legacy (one step, `bfb62506`)

Both public manifests are v2 (testnet a placeholder until its generation is promoted — the SEND tab shows the placeholder card, the FAUCET tab keeps working; mainnet a placeholder by design). Deleted: the `token_bridge` + `token_minter_proxy` crates, `NuloTokenPortal.sol` + build + pins, `PortalReinit`/`FormalPortal`/`PortalRoundtripFuzz` (its shared fakes moved to `test/mocks/AztecFakes.sol`, 11 importers), `DeployBridgeMainnet.s.sol` (`IV4Quoter` moved to `src/interfaces/`), the v1 reader and its 40 importers' v1 halves, `BridgeView`/`FuelView`/`BridgeForm`/`FuelForm`/`MintTestUsdc`/`MintFuelAsset`/`BridgeAddToken`, `useDeposit`/`useFuel`/`useWithdraw`/`useL1Usdc`/`useL1FeeAsset`, both legacy smokes, `mainnet-bridge.candidate.json` + the publicly served `mainnet-bridge.journal.jsonl`, `deployments.candidate.json`. Moved: `getRetainedSealKey` → `useSend`, the journal card's gas-recovery actions → `composables/fuel-recovery.ts`; `deriveSendDepositStage` takes a `Pick` so one phase table serves both record shapes (the duplicated copy table and its baseline directive went with it). `FactoryFork`/`SwapBridgeRouterPermit2Fork` read `AZTEC_REGISTRY` from env (they used the retired portal only to reach `registry()`). The hub artifact was rebuilt because its embedded `file_map` carried `claim_secret`'s old comment — the grep gate is on the artifact too; class id unchanged. The domain-separator string `nulo_dom_sep__token_bridge_private_claim_secret` stays (keystone identifier; `token_bridge_` does not match `token_bridge\b`). `faucet-smoke` needed a `mountApp()` that warms the async `AppShell` import — every case had gone red the moment `App.vue` went async, not just the tab case. Baseline: 54 → 49 cognitive, 9 → 5 lines, 0 inserted.

## Gate

fast ✓ (`lint` 0, `typecheck:all` 0) · faucet ✓ 81 / 970 · audit ✓ (`audit:faucet` incl. `verify:deployments` + build) · verify ✓ (sandbox candidate: hub + USDC/USDT/PXO `[OK]`) · forge ✓ 19 suites / 140 · halmos ✓ 8 / 2 / 2 · txe ✓ 65 · sole ✓ (3 hub sites, 14 regressions) · core ✓ 47 / 341 · `compile.sh --check` ✓ (`0x08e8…0617` unchanged) · both builds ✓ (mainnet `_headers`: `connect-src 'self' data: blob:`; no `AppShell` reference in the mainnet entry) · e2e ✓ 2 / 19 · legacy grep ✓ empty · `v-html` ✓ none · `logoURI` ✓ never requested · testid coverage ✓ (`components/send/testid-coverage.test.ts`, 15) · parity/drift/contrast ✓ (`app.css.parity`, `theme-vars`, `network-targets` tests) · baseline ✓ shrink only.

## Arc-4 quality loop

### Reviewer pass (fresh-context Opus, medium) — 18 findings, no blockers, all acted on (`7c788a99`)

The ones that mattered: **an exit on a `portal-only` token** (L1 registration exists, hub never bound it) passed `exitTokenBlock`, had no L2 ceiling and, on the private path, no simulate — a burn authwit against a token the hub cannot burn, a reverted tx, a stuck provisional record. Now an exit requires the HUB binding (`ExitTokenNotRegisteredError`, before any authwit), the wizard gates the plan on it and the Amount step renders the reason, and the private preflight builds its off-chain witness first and simulates with it (simulating `exit_to_l1_private` without the witness fails on the missing witness, not on the exit — every private exit would have broken). **The review truncated amounts to two decimals** (0.005 USDC rendered as "0.00" on the screen the user signs, while the receipt's "review said" used full precision) — `toDecimalString` everywhere an amount is about to be signed. **`portalVerified` was a boolean** that rendered "will be created" for both an unreadable chain and a non-zero portal that DISAGREED with the CREATE2 prediction — four states now; a mismatch warns and blocks confirm. The public send ran `ensurePermit2Approval` with an empty record id (an unexplained `approve` in the wallet, a junk `runtime[""]`) — the provisional record now opens before the approval on both paths and re-keys onto the claim hash; `setRecordStep("")` throws. `validateTokenBlock` — the gate for every resumed claim, exit consume and backup import — had no direct test (8 cases now). The requested-token set grew without bound while the wallet truncates its grant at 256 (an unbreakable re-prompt loop past ~251 browsed tokens): LRU of 32 browsed + pinned journal tokens, `forgetHubToken`. A panel's own `retryCapabilities()` owning the flow read as "declined" (the wallet declined nothing): `retryCapabilities(): Promise<boolean>`, outcome `busy`. `readSendReceiptLeaves`'s `{ intent } as SendParams` left `amount` undefined so a receipt-only gas recovery could journal `received: "0"` — `MissingBridgeAmountError`. A11y: focus followed nothing on a step change (keyboard users dropped to `<body>` three times per send), the privacy switch had no accessible name, invalid inputs carried no `aria-invalid`/`aria-describedby`, the list's arrows moved focus but not selection; the sprite adopter now allowlists elements and strips `on*`/`href`. Duplicate amount validation (the step owns it; the wizard gates the transition on the step's verdict — and the transition itself is gated, not just the disabled button). Dead testids, a `headersPlugin` comment saying the opposite of the code, `AZTEC_REGISTRY` undocumented on the forge side.

Reviewer verdict: "The security spine of this arc holds up under adversarial reading … the grant seam is per-address and correctly ordered … `registerHubContracts` derives instances only from attested or manifest words, the journal's `blocked`/`validateTokenBlock`/`deploymentMatches` triad re-validates on every run so a localStorage rewrite buys nothing but a re-block, the private `claimSalt` handling matches the Noir derivation contract exactly, both pause switches are read before any authwit … there is no HTML sink anywhere in the wizard."

Integration gotcha: the smoke's session stub resolved `undefined` from `retryCapabilities`, which the new contract reads as "the session was busy" — five token-carrying scenarios went red until the stub answered `true` (the real session's meaning).

### Codex pass (xhigh, adversarial) — session `01a064e8-6687-7560-9039-1209f2e4a5bf`

Round 1 verdict: "Do not ship yet: two ordinary user paths are blockers — private fueled deposits strand after L1, and public exits fail before authorization." 14 findings, 13 acted on:

- **Blocker — private `token+gas` never wrote its fuel block.** A private token leg knows its record id before the signature, so `persistPreTx`'s "rewrite when the id changed" branch never ran and the fuel material (`bridgeSecretSalt`, secret, hash) never reached the record — the private fee ladder stopped forever after L1 funds moved. Private gas-only stored the salt but sealed no envelope, so `resolvePrivateSecret` refused it and export was unavailable. Both paths now persist the complete fuel block before the approval and seal every private fueled envelope; the unit tests run the real seal/unseal.
- **Blocker — the public exit simulated before its authwit existed.** `preflightHubExit` ran `exit_to_l1_public` (which burns via `burn_public`) before `set_authorized`, so the Token refused it as unauthorized and every public exit was refused — hidden by tests and the smoke stubbing the preflight. The sandbox had the order right (authwit → simulate → send); the app now matches it, and the tests fake the hub's simulate so it only passes after the authwit tx.
- Should-fix: `performExit` re-checks the exact grant before any authwit; both lanes read the live `eth_chainId` before the signature/burn and again before L1 finalization (an attached RPC on another chain could supply false portal/pause state); a relayer finalizing first (`callerOnL1` is zero) is completion, not a retry loop (Outbox consumption is read back); the sponsored-plus-standalone ladder's standalone Fee Juice claim is launched and journaled; pins are derived from the live journal (discard/clear unpin); the local journal is deep-validated at load with invalid entries quarantined, not crashing boot nor reaching `requestHubToken`; the wallet's capability answer is intersected with the request per scope (an extra contract is ignored, a missing scope is not a grant, `"*"` is empty); a poisoned list cannot label an address — live metadata wins, a conflict warns, list rows and the review show the checksummed address, display strings are control-filtered and capped; a quote clears synchronously and is keyed by its token/probe, and the reviewed plan is a frozen snapshot (any change returns to the Amount step, saying why); the wizard adopts exactly the record id the flow resolves to, never "the first new record"; a backup file over 1 MiB is refused before it is read.
- Rejected (repo policy): "restore MAX's native tab stop" — in-field secondary controls are `tabindex="-1"` by the owner-accepted keyboard rule.

Codex "looks fine": claims cannot be redirected by altering recipient/token/portal fields (chain binding + recipient-derived private secrets fail closed); hostile ERC-20 event spoofing is stopped by emitter filtering; selection epochs and in-app account-switch gating hold; no HTML sink; remote `logoURI` unused; CSP appropriately narrow.

Round 2 (resumed) verdict: "substantially improved, but one blocker remains; three hardening gaps also survive." Four findings, all acted on (`66a0c457`): **a PUBLIC gas-only deposit stored its secret only in the fuel block and the claim resolver read the top-level one — refused after the irreversible L1 deposit** (the smoke had checked only the record's shape; it now asserts the claim was attempted with that secret) — the resolver is now the one source of truth over the fuel block; a private token+gas claim unsealed the envelope's salt but the hub branch discarded it and trusted the mutable journal copy (a tampered salt = a wrong-secret loop despite a correct seal) — the envelope travels through `claimSend`, the sealed salt is authoritative and corrects the record; forged journal token blocks were pinned into the grant and registered from attacker words before `validateTokenBlock` ran — blocks are attested against the factory first, a failing one is `blocked`, an unattestable one waits for the L1 client (a detached-scope watch, since a component-scoped one dies at unmount); the backup validator omitted several persisted optional fields — every one is typed now, with writer→validator round trips for every send and exit path (and the split let a complexity suppression go: baseline −1).

Round 3 (resumed) verdict: "Not ship-ready: one new blocker remains in boot attestation." — a regression the round-2 attestation fix introduced: a wallet on another chain made `registrationOf` answer empty and genuine records were marked `blocked` for good, and the address-only watch never re-ran on a network switch. Fixed in `f008f318` (chain asserted on both sides of the read — a mismatch is unavailable, never a contradiction; the watch keys on `[address, chainId]`; the wrong-chain case pins nothing and blocks nothing, and switching back pins). Items 1, 2 and 4 confirmed fixed.

Round 4 (resumed, scoped to that one fix — the hard stop is three rounds, and this pass verifies a regression of my own making rather than opening new scope), quoted: "Verified: `f008f318` closes the wrong-chain terminal-blocking path and safely re-attests after network switches. No new material findings." Loop converged.

## Arc-4 gate (final tree)

fast ✓ (`lint` 0, `typecheck:all` 0) · faucet ✓ 1060 · e2e ✓ 19 · core ✓ 379 · both builds ✓ · baseline ✓ shrink only (−1 more in round 2). The contract-side gates were unchanged by the loop (no contract file touched after the retire step's run).

