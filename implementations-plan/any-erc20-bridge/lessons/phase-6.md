# Phase 6 — conductor + sandbox smoke (2026-09-02)

Branch `any-erc20-bridge/core` (Arc 3), rebased on `/l2` after the hub gained its `exits_paused()` view.

## What shipped

- **Hub `exits_paused()` view** (on `/l2`, `e6993208`): the guardian switch was observable only by simulating an exit and string-matching "exits paused". +1 TXE test (65 in the manifest), artifact rebuilt, class id `0x05cd…` → `0x08e8238e48de3370cb952f7689d8697c56d6978e793b7ffc6587ecc768610617`. `hubExitsPaused` in `hub-l2.ts`; the smoke's pause preflight reads it.
- **`scripts/generation.ts`** — the generation deploy as a network-agnostic module: publish the Token + hub classes → predict the factory from the deployer nonce (the implementation is the factory's own nonce-1 child) → derive the hub at `salt = Fr(factory)` → PortalFactory (refuses if the nonce moved: the hub is already bound) → SwapBridgeRouter → hub (`universalDeploy`, CHECKPOINTED) → readbacks (`L2_HUB`, `FACTORY`, `FEE_ASSET`, `token_for(0)`). Every step is journalled through `openDeployJournal` and skipped on resume — resume reuses the recorded identities, never fresh ones. `preCreateToken`: `createPortal` (idempotent) → teach the wallet the derived Token → `register_token` after the register leaf is consumable → `ManifestToken`.
- **`scripts/sandbox/local-network.ts`** — an OWNED local network: four ports bind-tested in the static window below the ephemeral floor, `aztec-anvil` + `aztec start --local-network` from the package's own `@aztec/aztec.js` pin, `detached` process groups, real-disk data dir under `~/.cache/nulo-bridge-sandbox/<runId>`, rows claimed in `~/.agents/ports.md` under an `O_EXCL` lock. `stop()` signals only the groups it spawned; `SANDBOX_L1_RPC` + `SANDBOX_NODE_URL` attach instead of booting.
- **`scripts/deploy-sandbox.ts`** rewritten: Permit2 + Multicall3 `anvil_setCode`'d from Sepolia, `MockSwapTarget` + four `MintableERC20`s (USDC, USDT, PXO portal-only, NORT routeless), generation + pre-creation, v2 manifest written atomically (journal keyed by the rollup address, so a fresh chain gets a fresh journal). `--smoke`: 13 flows, all through the production modules (`runSend`, `claimViaHub`, `exitViaHub`, `consumeWithdrawal`, `discoverFuelRoute`); every paid claim's `transactionFee` feeds `calibrateFuelBudgets` and the run prints `fjPerTx`/`fjRegister`.
- **`scripts/calibration.ts`** — worst paid plain claim (+20 % margin) = `fjPerTx`; the worst registering shape's excess over it = `fjRegister`; sponsored samples are excluded rather than allowed to drag the maximum to zero.
- **`scripts/deploy-generation.ts`** — the testnet conductor (`deploy` / `pre-create --token` / `calibrate --samples`), pinned-signer check, `UniswapFuelSwap` first, then the generation, then `SEED_TOKENS` pre-created + `SeedTokenPool.s.sol` per token, candidate written to `apps/faucet/public/testnet-bridge.candidate.json` with the Sepolia V4 pins and the prior live manifest's budgets carried.
- `PoolSetupHelper` extracted to `script/PoolSetupHelper.sol` (five importers re-pointed). Faucet `verify-deployments.ts`'s opt-in bridge check re-derives the hub + every token from the v2 manifest. `live-intent.ts` binds the deployed hub to the manifest's `[token_class_id, l1_factory, guardian]` through the instance's initialization hash + class — no wallet needed. Legacy fixture `fixtures/sandbox-manifest.json` + a shape test.

## Sandbox smoke (5.2.0 local network) — the run

```
✅ (a) public deposit → claim_public — claim, +100000000 USDC
✅ (b) private deposit → claim_private — claim, +50000000 USDC privately
✅ (b) relayed private claim + wrong-recipient rejection
✅ (c) token+gas, self-paying claim — claim paid for itself with the 4e19 FJ-wei the same send bridged
✅ (d) gas-only with the fee asset — bridge() into the FeeJuicePortal, +2e19 FJ-wei claimed as fee juice
✅ (e) public exit → L1 withdraw — released 10000000 USDC-units on L1
✅ (e) private exit → L1 withdraw — released 5000000 USDC-units on L1
✅ (f1) relayer registers before the depositor claims
✅ (f2) two concurrent first-time deposits — register+claim + claim (the loser observed the race)
✅ (f3) portal-only token registers on its first claim — register+claim
✅ (f4) routeless token refused before signing — discoverFuelRoute → no-route
✅ (g) rejected registration × sponsored / fee-juice-with-claim / private FPC — tampered register rejected, corrected register+claim landed
✅ (h) guardian pause blocks exits, not claims
```
~6.6 min with the prover off.

## Gotchas (exact strings)

- **The node silently stops sequencing after ~7 blocks.** `stdio: "pipe"` with no consumer fills the 64 KiB pipe and the child blocks on write. Drain BOTH streams.
- **`SEQ_MIN_TX_PER_BLOCK=0` does not mint empty blocks** on 5.2.0 — the chain builds a block per tx, so `getL1ToL2MessageCheckpoint` anchors never advance on their own. `forceBlock` (a public AuthRegistry revoke of a random never-granted witness — needs no contract of ours) per poll, and a heartbeat across `consumeWithdrawal`'s proven wait.
- **`No contract instance found for address 0x…`** — `register_token` enqueues `Token::at(derived).constructor_with_minter`, so the wallet must hold the derived instance BEFORE the tx can be built (the oracle resolves the callee locally). `registerHubToken` first, always.
- **Permit2 `SignatureExpired` (`0xcd21db4f`)** — anvil's timestamp races ahead of wall clock; deadlines come from `getBlock().timestamp`.
- **Permit2 `InvalidSigner` (`0x815e1d64`) on gas-only** — a real bug in `send-flow.ts`: the direct-gas `bridge()` lane signed a witness carrying `fuelSecretHash` (the router zeroes it) and zeroed `aztecRecipient` + `tokenSecretHash` (the router takes the gas recipient and secret as the TOKEN ones). Unsignable — and had it landed, it would have minted Fee Juice to L2 address zero behind an unopenable secret. `buildWitness` now takes the entrypoint; `send-flow.test.ts` pins the shape against the router's field layout.
- **PrivateFPC `Amount too low to cover gas cost`** — the FPC asserts against the COMMITTED `maxFeesPerGas`; the blunt 1e13 setup ceiling prices a budget no slice covers. `predictedWorstMinFees(node).mul(2)` for that lane.
- `nonce too low` from concurrent viem deploys on one L1 account → sequential. `[aztec] Error: Address already in use` prints on every boot and is harmless.
- Long runs launched as the agent's background shell (the 6.5-min smoke, a codex resume) were killed mid-flight twice each by the tool harness, not by anything in the run; `setsid nohup … &` from a tiny launcher script plus a Monitor that tails the log's `✅`/`exit=` lines survives it (the run's own SIGTERM handler reaped the network cleanly on each kill — no orphans).
- `TxReceipt` exposes `transactionFee` but no `gasUsed`, so the budgets derive from fees, not gas units (the plan's "gas-unit budgets" wording is superseded — fees are what the manifest's `fjPerTx` is spent as).

## Arc-3 quality loop

### Reviewer pass (fresh-context Opus, medium) — 17 findings, all acted on (`23247c6a`)

The blocker: **`readLeaves` parsed the router's `Bridge`/`BridgeWithFuel` event over the whole receipt** — viem's `parseEventLogs` matches on topic0 only, and the token being bridged runs arbitrary code inside the Permit2 pull BEFORE the router emits, so a hostile ERC-20 could emit a same-signature event with an attacker-chosen leaf index that the journal, the backup and every claim retry would then carry (a vector the any-ERC-20 design newly opens — in v1 the token was ours). Fix: only logs the router emitted count, and exactly one must match (`routerEvent`); pinned by a forged-log test. Should-fixes: `parseManifestV2Strict` had no production caller, so a candidate whose `l2Token` was not the hub's derivation promoted cleanly (now `assertFaucetCandidateShape` is async over both halves); `assertZeroSeed` compared the factory but not the hub, and `verifyGenerationBindings` never read `factory.L2_HUB` (a self-consistent hub built with another class id would promote and never learn a token — both closed); the fuel ladder's `schema >= 2` widening misclassified a schema-3 token-only private send as "private-incomplete" (now `boughtFuel` reads the record's intent); `isRegisterRace` and the sync-retry predicate matched the same ambiguous string (`No non-nullified L1 to L2 message`) with opposite handling — a not-yet-consumable register leaf fell through to a plain claim that failed on the uninitialized binding and escaped the retry loop (now the fallback re-reads `token_for` and only takes the plain path when the hub knows the token, which must also be the journal's `l2Token`); `deployFactory` threw "generation is dead" for a factory that landed ahead of its journal entry (now adopted after `IMPLEMENTATION` + `L2_HUB` agree); `hubExitsPaused` defaulted to "open" on an unrecognized shape (fails closed); `InexactPull` was undecodable; `metadataReturndataBytes` accepted any ABI offset where the sanitizer accepts only `0x20`. Nits: `seedPool` journalled a tx hash it could never read under inherited stdio; a half-set `SANDBOX_*` attach booted a second network silently; a lost registry lock leaked rows without a word; `script-send.ts` and the `bridgeWithFuel` arm of `runSend` had no tests (now 6 + 2 cases: token+gas witness↔calldata mapping, the fuel-only zero-fields shape); one "legacy" in a comment; `logoURI` is https-only.

Reviewer verdict: "witness matches `_hashBridgeWitness` field-for-field across all four intent × privacy × entrypoint combinations … `assertRouteGrammar` is a faithful restatement of `_validateRoute` … both hand-written ABIs match the Solidity and the hub method signatures match `main.nr`; the portal derivation, the register content hash and the word sanitizer are byte-exact mirrors of the factory."

### Codex pass (xhigh, adversarial) — session `01a063f8-c982-7133-bb61-00c100f14850`

Round 1 verdict: "not ship-ready; three promotion/deployment blockers plus several recoverability and hostile-input defects." 13 findings, all acted on:

- **Blocker — `walletChainId`**: the testnet conductor wrote the bare rollup version; the app matches a network by `(l1ChainId ^ rollupVersion) >>> 0`, so a real generation would have deployed cleanly and been unusable by every wallet. Fix: `src/wallet-chain-id.ts` `walletChainIdOf` (pinned against both live networks), used by both conductors and by `live-intent`, whose `verifyCandidate` now refuses a candidate whose chain ids differ from the intent's identity. (The sandbox wrote `31337` for both — also wrong, also fixed.)
- **Blocker — promotion verified a subset**: `verifyGenerationBindings` read owner/`IMPLEMENTATION`/`swapTarget` only; a lookalike router answering `swapTarget` correctly could promote. Fix: `verify-l1.ts` exports `verifyL1Manifest` (every constant, every token, immutables-masked code hashes; `main` guarded by `import.meta.main`) and promotion runs it after its own readbacks.
- **Blocker — split identity**: intent identity was probed at `intent.primaryRpc` while the hub readback used the process-global `NODE_URL`. Fix: the pinned RPC flows through `reportHubBindings`.
- Should-fix: private first claim re-reads `token_for` after its own registration and refuses a block whose `l2Token` is not the binding (named error, no wasted claim); factory nonce read at `pending` and pinned on the deploy tx; the manifest derives the HUB from its record (`deriveManifestHub`, `constructorArtifact === "constructor"`, `salt === Fr(factory)` at schema level) before deriving tokens from it; journals open with an `identity` stamp (`l1ChainId`, `rollupVersion`, deployer, registry, feeJuicePortal) and refuse a different network; `local-network.ts` `stop()` tracks exit events and never signals a group whose child already exited; metadata reads carry the factory's `METADATA_GAS` (100k) cap; **gas-share sized the input by `×(1+s)` so the signed floor landed at `target·(1−s²)`** — now two exact ceilings (`⌈target·BPS/(BPS−s)⌉`, then `⌈quote·in/out⌉`; a single folded division still lost one unit at s = 1 bps, rate 3/7 — pinned by a property test over five slippages × three rates); a hub that landed ahead of its journal entry is adopted after class/init-hash/salt agree; `--dry-run` no longer deploys the L2 account; `runSend` refuses a zero recipient, a non-positive amount, a non-positive `minFuelOutput` and a ragged route before deriving secrets or signing.
- Accepted as designed (told codex): backup validation cannot derive `l2Token` (needs the hub + class id, async); the hub-side binding check — now also pre-claim on the private path — is the enforcement point.

Codex "looks fine": witness field order + direct-gas zeroing match Solidity; router-event emitter filtering + factory read-back sound; Noir argument/secret ordering matches; registration-race fallback preserves the token binding; signed claim floors clamp correctly; the token-list path enforces byte/redirect/schema limits; no raw-secret logging in the send or relay paths.

Round 2 (resumed) verdict: "8 fully fixed, 5 only partially fixed; one promotion blocker remains." Six residuals, all acted on (`5a0de6c0`): the code-hash check trusted a pre-existing `out/` and SKIPPED when forge/artifacts were unavailable — fatal for a promotion gate — so `verifyL1Manifest` gained `{ strict }` (clean `forge build --force`, every former skip a failure; promotion passes it) and reads `TokenPortalImpl`'s `FACTORY`/`INBOX`/`OUTBOX`/`ROLLUP_VERSION`/`L2_HUB` back; the router's owner was printed, never compared (now must be the guardian); `assertIdentityUnmoved` omitted `l1ChainId`; a KEPT sandbox redeployed its mock swap target and the candidate recorded the fresh one while the resumed router kept the old (now the record's `permit2`/`feeJuicePortal`/`swapTarget` are the router's own immutables); the decoded metadata multicall was still unbounded (three gas-capped raw calls, 256-byte decode cap, `decimals` mirrors `_readDecimals`); a non-zero but off-curve `fuelRecipient` passed (now `AztecAddress.isValid()` on both recipients). Codex confirmed the two-ceiling gas-share proof, the pending-nonce pin, hub adoption, the journal stamp, and that `verifyL1Manifest`'s module-level failure counter is safe on live-intent's sequential path.

Round 3 (resumed, final) verdict, quoted: "ship-ready from this TypeScript review; implementation matches the stated fixes and 65 focused tests pass. No new material findings." — all six residuals confirmed. Loop converged in three rounds (the hard stop).

## Gate

fast ✓ (`bun run lint` exit 0, `typecheck:all` exit 0) · core ✓ (bridge-core 48 files / 358 tests; faucet 64 / 712) · sandbox ✓ (13 flows × 4 full runs on the 5.2.0 line, the last two after the review + codex fixes; real `register_*` publication on every first-time flow is the registry-selector canary) · verify ✓ (`BRIDGE_MANIFEST=<sandbox candidate>` → hub + three tokens `[OK]`) · crash-resume at every conductor step ✓ (`generation.test.ts`: journal crash after each of the five steps → identical record, only un-landed L1 deploys re-sent; adopt-landed-factory and adopt-landed-hub; moved nonce refused before broadcast) · measured budgets ✓ (`fjPerTx=17785608960000`, `fjRegister=11924383920000` on the local network).
