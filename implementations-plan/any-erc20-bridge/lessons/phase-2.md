# Phase 2 — Router (2026-09-02)

Branch `worktree-any-erc20-bridge` (Arc 1, PR `any-erc20-bridge/l1`).

## What shipped

- `SwapBridgeRouter.sol`: constructor gains `factory`; immutables `FACTORY`, `FEE_ASSET` (= `feeJuicePortal.UNDERLYING()`); errors `ForeignPortal`, `FuelOnlyLeg`, `RouteRequired`, `AmountExceedsL2Max`. D23 per-entrypoint portal rule in `_requireFactoryPortal` (predict → compare → `createPortal`, all **before** the Permit2 pull); `bridgeWithFuel` accepts `fuel == total` (fuel-only: `tokenPortal/aztecRecipient/tokenSecretHash` must be zero, no token leg, zero token key/index in the event) and an empty `path` iff `bridgeToken == FEE_ASSET` (identity: `fuelReceived = fuelAmount`, the swap target is never called); u128 cap on both entrypoints (D13). The body was split into `_swapFuel` / `_depositFuel` / `_depositTokens` — the hashed witness and the 12-field type string are untouched (`WitnessHash.t.sol` pins unchanged, zero vector edits).
- `script/DeployGeneration.s.sol`: the abstract `GenerationDeployer` (factory → swap → router; fork-fixture only after the codex round — `_resolveFactory` requires `PORTAL_FACTORY`). The three legacy deploy scripts inherit it so they compile against the 4-arg router until P8 retires them.
- Test fixtures consolidated: `test/mocks/RouterMocks.sol` (one `MockPermit2`, `MockSwap`, `MaliciousPrefundSwap`, `ConfigurableSwap`, `MockFeeJuicePortal`, `MockTokenPortal`, `FakePortalFactory` — previously four divergent copies across the unit/blackhat/fuzz/fork files) and `test/mocks/RouterFixture.sol` (router over the REAL factory + capturing Inbox, with `portalBalance` / `lastMintWas*` observers).
- Tests: `SwapBridgeRouter.t.sol` (27: D23 on both entrypoints, direct gas, private fee-asset → clone, fuel-only + leg smuggling, identity partial/full/floor, caps, FoT, pause propagation, 2 metered gas tests), `SwapBridgeRouterFuzz.t.sol` (7: + fuel-only, identity split, foreign-portal-before-pull on both entrypoints), `SwapBridgeRouterInvariant.t.sol` (5 over the real factory: handler adds `fuelOnly`, `identity`, `createPortal`, `guardianPause`; I5 = no deposit while paused, clone stable, `unexpectedRevert` flag so the campaign's `fail_on_revert=false` cannot hide an honest action reverting), `FormalRouter.t.sol` (8 halmos + 5 canaries: `+fuelOnly_conservesUserFunds`, `+identity_conservesUserFunds`, `+bridge_rejectsForeignPortal` ∀ portal on both entrypoints, `+bridgeWithFuel_partialRejectsFeeJuicePortal` ∀ split × privacy), `BlackhatAudit.t.sol` (12: F-A…F-H kept, + F-I look-alike portals, F-J partial AZTEC+gas into the FeeJuicePortal, F-K fuel-only smuggling, F-L bridge token re-entering from `transferFrom`), `SwapBridgeRouterPermit2Fork.t.sol` (15 on live Sepolia: real Permit2 + real V4 + real registry/Inbox + real FeeJuicePortal; clone created inline; fuel-only real swap; identity partial + fuel-only; direct gas; foreign portal refused **before** Permit2 so the nonce survives).
- `.gas-snapshot` (metered `bridge()` first-time vs known) + CI step `forge snapshot --match-test test_gas_ --check --tolerance 2`; `_bridge-contracts.yml` halmos table → `8/1/2/2`; `router-abi.ts` + pin test gain the readbacks (`FACTORY`, `FEE_ASSET`, `feeJuicePortal`, `swapTarget`) and the four errors; `contracts/bridge/evm/README.md` rewritten (A-1 → the factory-bound portal threat model, test matrix, pinned install).

## Measurements

- **I5 corrected.** First-time `createPortal` on live Sepolia (real Inbox): **322,900 gas** (`test_gas_createPortal_live`, asserted `< 450k`). The plan's 150–200k estimate missed the 4-slot `Registration` record (~88k) and the real Inbox leaf (~80k). Metered router calls under the capturing mock: `bridge()` first-time 651,407 vs known 235,200 (the mock Inbox's seven cold SSTOREs inflate the delta; the live number above is the honest one). A once-per-token cost; if it ever matters, the words + key could move to event-only storage (−~66k) — deliberately NOT done now (the record is what `registrationOf` recovery reads).

## Deviations / notes

- **`DeployFuelLiveForkTest` (legacy) fails against live Sepolia** for reasons unrelated to the router: `PoolAlreadyInitialized` on its "attacker pre-inits our key" scenario and `insufficient output` on its production topology (the live AZLO/WETH pool has drifted from the script's price assumptions). Both belong to the legacy fuel deploy path retired in P8; not touched beyond the compile-fix (an honest factory model binding AZLO to the live legacy portal).
- The old `testFuzz_hostilePortal` ("strand into router is a documented surface") is gone; its replacement `testFuzz_foreignPortalRejectedBeforePull` asserts the opposite, which is the point of D23.
- `MainnetFuel.fork.t.sol` compiles against a `FakePortalFactory` (its token leg was already a mock; the real factory would need a mainnet registry it does not have). It is retired in P8 anyway.
- halmos now takes ~15 s for the router suite (154 paths on the split proof); fine.

## Arc-1 quality loop

- **`/code-review medium --fix`** — the repo's `code-review` skill is an interactive human tour (one stop per message), unusable under `/goal` autonomy, so the step ran as a fresh-context reviewer subagent (Opus, medium) over the arc diff with security/test-quality/simplification/comment lenses. 12 findings, all acted on in `e2ee7cca`: the CI gas-snapshot step selected the fork suites (setUp skips → `--check` fails on the missing entry); the factory invariant's pause detectors signalled with a revert the campaign's `fail_on_revert=false` swallowed (now ghost flags + `invariant_pauseHolds`); `_readDecimals` did a whole `returndatacopy` (now a one-word buffer); `register-hash.ts` duplicated the four hash primitives from `content-hash.ts`; two `FeeOnTransferERC20`s; four decision-id references in comments; `BindingFactory` inserted inside another contract's docstring; the "mutation canary" headers overclaimed; a stale `router-abi.ts` comment; the plan's "exact-in on the Permit2 pull" was unimplemented (now `InexactPull` in `_pullTokensWithWitness`); `FakePortalFactory.predictPortal` returned zero for unbound tokens; `predictPortal` returned a checksummed address into a lowercase-address codebase.
- **`/security-review`** — not a skill in this repo; covered by the reviewer's security lens + the codex pass below (both explicitly adversarial).
- **Codex xhigh (`gpt-5.6-sol`, session `01a062f1-30a1-7351-8854-f54bf65dcbe5`) — FIX-THEN-SHIP, 10 findings**, all acted on in `084ef0b7`: (1) HIGH — `setSwapTarget` could run from inside the Permit2 pull (a hostile bridge token made router owner rotates the target after the witness bound it) → `nonReentrant` + blackhat F-M; (2) `DeployGeneration.run()` + factory auto-deploy contradicted D26/D32 (the conductor predicts the factory address for the hub salt) → fixture-only, `PORTAL_FACTORY` required; (3) D33's by-name halmos gate was missing → all 13 `check_*` names asserted (ANSI-stripped); the canary debate resolved with real MUTANTS for the gating proofs (`RouterWithoutPortalRule`, `PortalImplWithoutPause`; `_requireFactoryPortal`/`_requireDeposit`/`_requireWithdraw` are `internal virtual` for that purpose only) and witnesses for accounting/authority/prediction proofs; (4) two handler actions bypassed `unexpectedRevert`; (5) a 64-byte string head with `len = 31` sanitized into 31 underscores → fails closed to empty (`HeadlessStringERC20`); (6) the TS sanitizer's string overload diverged from on-chain bytes on invalid UTF-8 → bytes-only, `ff41 → _A` pinned; (7) the ABI-pin vitests skipped in the unit job (no forge artifacts) and never ran in the forge job → run there with `test -f` guards; (8) plan said decimals `returndatasize ≥ 32`, implementation requires `== 32` → plan fixed; (9) README overclaimed ("only an empty word", "pause bounded to delay") → cosmetic poisoning + unbounded pause stated; (10) router step-narration comments deleted; the legacy scripts' pre-existing plan/phase comments left (files retired in P8).
- **D39 nuance recorded**: forge cannot catch a forge-std assertion failure across an external self-call as a plain revert (the cheatcode records the failure), so "the property must fail under a deliberate break" is implemented as mutants of the guard under test, not as assertion-failure canaries. Proofs whose guard cannot be mutated without touching production storage/auth (authority, prediction, accounting) keep reachability witnesses; each `Formal*` header says which.
- **Convergence** — codex resumed on `084ef0b7` (same session): "FIX-THEN-SHIP" on three residuals, none material to the contracts — the ABI-pin `test -f` guard omitted `TokenPortalImpl.json`; the router-abi "same shape" pin compared only outputs (an `error ForeignPortal(address)` would have passed); the README's test inventory was stale (140 / F-L → 146 / F-M). All three fixed in the follow-up commit. Its reverification, quoted: "the shared reentrancy guard closes mid-bridge target rotation; `_readWord` handles 32/64/96-byte edges safely; virtual hooks are internal and production deploys the concrete implementation; mutants exercise the intended deleted guards; invariant catches now signal failures; TS initcode matches pinned OZ source; register encoding/sanitization matches Solidity. Local gates: Forge 146/146; targeted TS 17/17." and "I agree with leaving the identified pre-existing legacy-script comments for their scheduled deletion." No new contract-level finding → the arc-1 loop converged at round 2 (hard stop is 3).

## Gate

- fast ✓ (`typecheck:all` clean; `lint` 0 errors — the 29 warnings / 13 infos are dev's baseline)
- forge ✓ 146 passed / 0 failed (after the quality loop; 140 at the P2 commit) (`--no-match-contract Fork`)
- halmos ✓ `FormalRouterTest 8 · FormalPortalTest 1 · FormalFactoryTest 2 · FormalCloneTest 2`, 4 summaries (solc `unsafe-typecast` lints on `bytes4(reason)` selector reads and the guarded `uint8(d)` are compiler notes, not proof output)
- fork ✓ `SwapBridgeRouterPermit2ForkTest 15/15`, `FactoryForkTest 5/5`, `DeployBridgeForkTest 1/1`, `BlackhatV4ForkTest 6/6`; legacy `DeployFuelLiveForkTest 1/4` (drift, above)
- core ✓ 295 passed / 1 skipped
- `WitnessHash.t.sol` vectors unchanged ✓ · `.gas-snapshot` committed ✓

## Tooling notes

- `vm.expectRevert` / `vm.prank` bind to the NEXT external call — including a `view` inside an argument builder (`_simpleParams` → `factory.predictPortal`, `_signSimple` → `router.hWitness`). Build params/signatures into locals first. Bit this five times across P1/P2; the fixture helpers now all take pre-built params.
- `forge snapshot --match-test … --check --tolerance 2` is the cheapest honest gas gate; `vm.pauseGasMetering()` around fixture work makes the number the router call alone.
- halmos handles `try/catch` + `bytes4(reason)` selector assertions fine (paths ≈ 2× the branch count).
