# Phase 4 — TXE suite + tripwires (2026-09-02)

Branch `any-erc20-bridge/l2` (Arc 2).

## What shipped

- `token_bridge_hub/src/test/`: `register.nr` (14: the P0 four + foreign sender, swapped portal, leaf replay, second factory leaf for one ERC-20 → `token_of` init-nullifier, relayer-first register + depositor claim, register-and-claim on a registered token, two tokens independent, discovery zero, decimals 0/255), `claims.nr` (10, two tokens: portal-A message cannot mint token B, unregistered token, zero amount, wrong secret/recipient/amount, replay, relayer credits the recipient), `claims_private.nr` (10: F-007 redirect, cross-token, unregistered, zero amount/recipient, wrong salt, replay, relayer), `exits.nr` (13: public/private burn with authwit-to-hub, a token-A authwit replayed against token B on both domains, no authwit, unregistered, zero recipient/amount, over-exit dies on the burn), `guards.nr` (3: `_register` only-self incl. the guardian, `_assert_exits_open` only-self), `pause.nr` (7: non-guardian + admin rejected, public + private exits refused while paused, claims + registration unaffected, unpause restores with the balance intact), `keystone.nr` (2). After the arc-2 quality loop: **64 named tests** (+ `bind_cannot_consume_a_real_factory_leaf`, `register_token_reaches_the_publish`, `register_and_claim_public_reaches_the_publish`, `register_with_swapped_words_rejected`, `register_with_wrong_name_word_rejected`, `second_factory_leaf_with_same_words_different_portal_rejected`; `correct_registration_succeeds_after_a_wrong_attempt_elsewhere` renamed to `bind_token_alone_registers` — it never made a wrong attempt), all in `txe-manifest.txt`; the `?` conditional feature was deleted (dead since Verdict B).
- Harness (`utils.nr`): two-token fixtures, `send_register_message_from` (arbitrary L1 sender), `register`, `fund_public`, `fund_private`.
- `check-sole-consumer.sh`: `check_file <file> <expected>`; the hub expects 3 sites and the third must be `_bind` consuming with `[REGISTER_SECRET]`; the default run checks BOTH crates (so CI's existing `sole-consumer` job covers the hub with no workflow change); self-test gains two hub regressions (a fourth site; a caller-secret register). 7 regressions rejected.
- `txe-ts-map.md`: property → TXE test → L1/TS counterpart.

## Exact strings (TXE 5.0.1 — discovered by running, then pinned)

| Situation | String |
|---|---|
| consume of a message that does not exist — private | `No L1 to L2 message found` |
| consume of an already-nullified message — private | `No non-nullified L1 to L2 message found` |
| consume of a message that does not exist — public | `Tried to consume nonexistent L1-to-L2 message` |
| consume of an already-nullified message — public | `L1-to-L2 message is already nullified` |
| `PublicImmutable::initialize` twice (first-registration-wins) | `Attempted to emit duplicate nullifier` |
| read of an uninitialized `PublicImmutable`, both domains | `Trying to read from uninitialized PublicImmutable` |
| missing/mismatched public authwit | `unauthorized` |
| missing/mismatched private authwit | `Unknown auth witness for message hash` |
| `#[only_self]` | `can only be called by the same contract` |
| Token over-burn public / private | `attempt to subtract with overflow` / `Balance too low` |
| `publish_contract_instance_for_public_execution` under TXE (class nullifier never mined) | `Nullifier read request at index N is reading an unknown nullifier` — pinned as `is reading an unknown nullifier` |

## Arc-2 quality loop (reviewer pass, medium)

14 findings, all acted on. The material one: **the publish-free `bind_*` harness entrypoints ship in the deployed ABI and could consume a REAL factory leaf without publishing** — in the deployed hub a caller front-running the first depositor with `bind_token` would (if the enqueued constructor's revert semantics ever changed) bind a Token nothing can construct and burn the token's one-shot leaf. Fix: `_bind` picks its consumption secret by the publish flag — `REGISTER_SECRET` (0) for `register_*`, `BIND_HARNESS_SECRET` (0xB1AD, in `register_hash_lib`) for `bind_*` — so a factory leaf (committed to `compute_secret_hash([0])`) is unconsumable through the harness path; the harness injects harness-secret messages. This is the recorded deviation from D17 ("hub code unchanged"): the harness shape lives in the contract as two entrypoints that are inert on a real network, guarded by `check-sole-consumer.sh` (the `_bind` consume must pick between exactly those two constants and be keyed on `l1_factory`). Also: `word_to_str`'s round-trip assert was unreachable (`to_bytes` is a 31-limb decomposition — the oversized-word keystone pins the real guard) → deleted; the withdraw message now follows the burn on both exits (holds by construction, not by revert semantics); `run-txe-tests.sh` anchors manifest names on the module separator and fails without a manifest; `check-sole-consumer.sh` counts occurrences across every non-test source and pins the register consume's sender (9 self-test regressions); the hub-keystone CI step proves it ran tests; `hub-parity` passes the resolved toolchain as `AZTEC_HOME`; `compile.sh` decides library-vs-contract from `Nargo.toml`; `hub-token.ts` takes an optional `tokenClassId` to refuse a manifest/installed-class mismatch; shared fixtures (`register_a/b`, `NONCE`, `L1_RECIPIENT`) replaced ~25 duplicated lines; the pause "no burn persists" claims were reworded to what the TXE can observe. The hub artifact was rebuilt with the pinned `aztec compile`; class id `0x1d3c…` → `0x05cdfde821ff38289b9227048ab990d207e8308dff2fe6095aec0fefc8615b3b` (re-pinned in `noir-artifact-classids.test.ts`; `hub-token` vectors unchanged — the Token class is untouched).

- **Codex xhigh (`gpt-5.6-sol`, session `01a06356-e25b-7732-b02a-1a5e317e6f54`) — FIX-THEN-SHIP, 10 findings, none contract-level** (its "checked and sound": sender keying, harness-secret isolation, first-registration-wins, cross-token isolation, typed-arg derivation + constrained publish, **the register consume nullifier is revertible under 5.0.1 — a failed public phase does not strand the leaf**, ctor → `_register` → claim ordering, per-Token authwit consumer, burn-before-message, pause ordering). Acted on in `133a4b0c`: `tokenClassId` required (a mismatch throws, pinned); `run-txe-tests.sh` — flags never disable the manifest, the hub manifest must name ≥ 40 tests; `check-sole-consumer.sh` — comments stripped before scanning, `_bind` must read `l1_factory` from storage (11 self-test regressions); `compile.sh` restores artifacts on any exit; the finding id and narration-only comments removed; D21's round-trip assert recorded in the plan as an identity under 5.0.1 (deleted).
- **Convergence** — the contract converged at round 1 (codex: no contract-level finding; every security inference in the prompt confirmed). Rounds 2 and 3 were a tripwire-script tail, each reverifying the previous fixes: round 2 (`133a4b0c`) — "Reverified: required class-ID comparison and negative test; metadata/map/D21/comment fixes; 64 current manifest names are unique; sole-consumer self-test passes 11/11. No new forbidden references appeared" with three residuals on the scripts (block-comment stripping, value-taking flags disabling the manifest, restore leaving a renamed artifact behind) fixed in `2dc54483`; round 3 — "Reverified: 13/13 current self-tests pass; TXE `--` splitting, unique manifest floor, and compile artifact cleanup are structurally sound" with ONE residual (a `/*` inside a string literal defeating the two-pass stripper) fixed here with a single-pass, string-first alternation and regression 14. Three rounds is the loop's hard stop; the last fix was applied without a fourth resume. Nothing in the hub contract or the TXE suite changed after round 1.

## Notes

- **Never type-check the hub crate with `nargo compile`**: it overwrites the committed TRANSPILED artifact with a plain one and the TXE runner then fails every test with "public bytecode has not been transpiled". Use `nargo-5.sh <crate> check`, or restore the artifact from git (`git checkout -- …/target/*.json`) afterwards. (Bit once here.)
- "No burn persists" on a paused exit is atomicity: a `should_fail_with` test cannot observe state after the panic, so the property is shown by `exits_resume_after_unpause` (balance intact after a pause/unpause cycle) rather than after the refused call.
- `add_public_authwit_from_call` works with `create_light_account`; the private authwit needs `create_contract_account` (a SchnorrAccount that can answer `is_valid`).
- A `global` initializer may call `EthAddress::from_field` without the `FromField` trait in scope; function bodies cannot.

## Gate

- fast ✓ (`lint` 0 errors) · forge ✓ / halmos ✓ (unchanged since `bdc69423`) · keystone ✓ 10/10 + hub 2/2 · txe ✓ 64/64 named (exit 0; one re-run after the oracle server dropped mid-run with `client error (Connect)` — environmental, 30 GB box at 8 GB used) · sole ✓ (self-test 9 regressions + both crates) · core ✓ 298 · `compile.sh --check` ✓ (hub `0x05cd…`, tree clean). Every `should_fail_with` is an exact string.
