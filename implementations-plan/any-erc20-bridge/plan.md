# any-erc20-bridge — plan

| | |
|---|---|
| Tier | `mega-deep` (novelty HIGH, blast radius HIGH, irreversibility HIGH, migration MEDIUM, external coupling HIGH, security HIGH) |
| Status | **approved 2026-09-02** (owner set the `/goal` seed after the Q&A round; no conditions) — implementing from P0. Seeds below are final. |
| Worktree | `.claude/worktrees/any-erc20-bridge` on `worktree-any-erc20-bridge`, base `origin/dev` @ `8d6cca3d` |
| `eli5_mode` | artifact (URL recorded in § Seeds once published) |
| Budget | recon done (2 agents); codex `xhigh`; `/code-review medium` on the four code arcs, `low` on the docs arc, none on the throwaway spike branch; Round-2 split audit per the tier |
| Inputs | `brief.md` (task + owner-locked decisions), `recon.md` (reuse map + feasibility), `research/*.md` (module maps), `audit-*.md` (transcripts + dispositions) |

## Summary

One L1 `PortalFactory` mints a **storage-less portal clone per ERC-20** (OZ `Clones` with immutable args) and attests the token's identity + metadata to **one ownerless L2 `TokenBridgeHub`** through an L1→L2 `register` message. The hub **deploys the per-token L2 `Token` itself** from that attestation (Wonderland `vault_deployer` pattern) and serves every claim and exit. The router derives the legal portal from `bridgeToken` per entrypoint (closing A-1) and gains a fuel-only path (swap-in-place gas). bridge-core grows a generation manifest, deterministic derivation with cross-toolchain keystones, per-token journals, route discovery with a first-class `no-route`, and a runtime token list. The tools app becomes a 3-step wizard (Token → Amount → Review) with a per-token wallet grant. Done = testnet live via the new conductor with pre-created fake blue chips + documentation updated everywhere; mainnet is a later owner-run arc behind `/harden security`, and the mainnet build shows a placeholder until then.

Eleven phases (P0–P10) in six work arcs (Arc 0 spike + Arcs 1–5), five stacked PRs (Arcs 1–5).

## Decision ledger

| # | Decision | Source | Rejected alternative(s) | Status |
|---|---|---|---|---|
| D1 | The **factory** sends the `register` message (`msg.sender == factory` is the L1 actor — `Inbox.sol:107`); the content carries the portal address `Clones` actually returned. The hub never recomputes CREATE2. | main + fable | AVM CREATE2 in the hub (codex): keccak in Noir, 3-toolchain address keystone, `bind_l1` must carry `implementation`; a Noir/Solidity mismatch strands every deposit | **owner-accepted 2026-09-02** |
| D2 | Genesis without `bind_l1`: predict `factory = CREATE(deployerEOA, nonce)` → hub address from `constructor(token_class_id, l1_factory)` → deploy the factory with the nonce asserted at send → publish the Token class → deploy the hub. No deployer role on L2 (ordering per D32). **The conductor is idempotent**: an already-published Token class or an already-published/initialized hub instance with identical args is success after readback (universal deploy lets anyone front-run the byte-identical instance — harmless). | fable; idempotence: codex SEC-9 + fable S6 | `bind_l1` (main, codex) | **owner-accepted 2026-09-02** |
| D3 | Guardian pause bits **unbounded** and **global** (deposits + withdraws), `Ownable2Step`, `renounceOwnership` disabled. | owner (locked) | expiring pause + cooldown; timelocked rescue; per-portal pause bits (fable S2 — NOT offered: it would reopen this decision) | **owner-confirmed 2026-09-02** — global blast radius is an accepted consequence, § Security |
| D4 | Mainnet `tools.nulo.sh`: **SEND tab fully disabled**, no legacy-exit affordance; legacy contracts leave manifest + UI; the mainnet build renders one static placeholder (A4). | owner | disabled + legacy exit; pin CF Pages | **owner-confirmed 2026-09-02** — owner accepts stranding any legacy L2-USDC holder |
| D5 | Fee-on-transfer **rejected everywhere** (exact-in in portal and router); rebasing unsupported (documented caution). | owner | delta accounting | **owner-confirmed 2026-09-02** |
| D6 | `createPortal` idempotent; the **router** calls it inline (before any Permit2 pull) when a token leg targets a token without a portal. The brief's `createPortalAndDeposit…` factory helpers are dropped (the router is the only UI path; direct EOA users do `createPortal` + `portal.deposit*`). | codex + main | factory helpers (codex, fable) | adopted — A5a ✓ |
| D7 | The factory deploys `TokenPortalImpl` in its constructor (`FACTORY` immutable, no cycle). | codex | separate impl deploy | adopted |
| D8 | Name/symbol sanitized on L1 to **31 printable-ASCII bytes** (`0x20–0x7E`, else `_`), carried as `bytes32` words (`0x00 ‖ b0..b30` == `FieldCompressedString.value`) in event, manifest, journal. Event = `PortalCreated(address indexed token, address indexed portal, bytes32 nameWord, bytes32 symbolWord, uint8 decimals, bytes32 registerKey, uint256 registerIndex)`. | fable + codex | raw truncation — aztec.js's UTF-16 `charCodeAt` encoding would make a non-ASCII `name()` unconsumable and strand the batched deposit | adopted — A5b ✓ |
| D9 | Salt = `bytes32(uint256(uint160(erc20)))`; args = `abi.encodePacked(erc20)`; L2 token salt = `erc20.to_field()`. | main | `keccak256(abi.encodePacked(erc20))` (codex, fable) — equivalent | adopted — A5c ✓; keystone-pinned |
| D10 | `REGISTER_SECRET = 0`; `REGISTER_SECRET_HASH = compute_secret_hash([0])` pinned in Solidity + TS from the Noir keystone. | fable | secret `1` | adopted |
| D11 | **Public first claim = ONE L2 tx via the hub's `register_and_claim_public`** (private entry publishes the Token, enqueues `_register`, the Token ctor and `claim_public` in order). **Private first claim = TWO txs — owner-confirmed 2026-09-02 (option a)**: the canonical aztec-standards `Token` reads `minter` historically in `mint_to_private` (`main.nr:410-411`) and its partial-note entry `initialize_transfer_commitment` (`:242`) carries the standard init-check, so nothing private can run before the constructor is mined. Rejected: forking the Token with `#[noinitcheck]` (a Nulo-compiled class for every bridged token); a register relayer (owner declined the infra). The app auto-continues REGISTER → CLAIM (two wallet prompts, once per token, first private depositor only). No dependence on a wallet-side mixed `BatchCall` — which has never run in this app (today's is `BatchCall(wallet, [])`) and cannot be tested in TXE (one `call_private` = one tx). | codex (function) + fable C1/F2 (why) | wallet `BatchCall([register_token, claim_public])` (main/fable draft) | adopted (replaces the earlier "I3 verified" claim, which was wrong) |
| D12 | Spikes + keystones are **Arc 0** on a throwaway branch `any-erc20-bridge/spike`; keystone tests cherry-picked into Arcs 1–2. | codex + fable | L1 first | adopted |
| D13 | `amount ≤ type(uint128).max` in portal (content-hash-neutral) and router; decimals accepted as any `u8`, all UI/gas math in base units, tests at 0/18/19/38/255. | fable + main; codex SEC-12 | none | adopted |
| D14 | Gas share: seed from one dust quote (linear rate × `txTarget` × `fjPerTx` [+ `fjRegister`] × (1 + slippage)), then **re-quote the actual slice and bump it in ≤ 3 bounded steps** until the real quote clears the D31 floor — else show "amount too small" before signing (a linear seed alone under-quotes on nonlinear pools and would revert at the floor). Floor `minFuelFj`; cap 50 % of `amount`; `txTarget ∈ [1, 30]`, default 20 (D42). `fjPerTx`/`fjRegister` are manifest `swap` fields calibrated on testnet. | main + fable; codex (search), final pass C3/D2 | single linear extrapolation | adopted (amended after D31) |
| D15 | Route candidates **this plan**: identity (AZTEC), `WETH → native/FJ` (1 hop), `T/WETH{tiers} → native/FJ`; probed via Multicall3 dust quotes, best output wins. Connector shapes (`T/C → C/WETH → …`) and `T/FJ` direct pools are **deferred to the mainnet arc** — nothing on testnet exercises them. | fable + codex; trimmed per fable O1 | five-shape matrix now | adopted |
| D16 | Noir line stays v5.0.1. | recon | bump | adopted |
| D17 | TXE fallback if in-contract publish can't be exercised: the harness deploys the token via the raw `txe_oracles::deploy(…, deployer = hub)` + `call_public(from = hub, ctor)`; hub code unchanged. Under this verdict TXE proves neither canonical publication nor revert semantics — **the sandbox smoke (P6) is the blocking gate for real `register_*`**, not corroboration. | fable + codex; codex INF-1 | SDK-side deploy as a design fallback | adopted |
| D18 | Journal schema 3 = **discriminated union on `intent`**: `token` / `token+gas` REQUIRE the `token` block, `gas` forbids it; backups fail closed on a missing block. Recovery-key domain keeps `{chainId, portal, bridge (= hub), secretHashHex}` — `portal` is per token. But `portal` does **not** determine the L2 token (D30: words + decimals do), so **every resume and import validates the whole token block against `registrations[erc20]`** (words, decimals, `registerKey`/`registerIndex`, derived `l2Token`) before any grant or execution; a mismatch is a hard stop, never a silent fix-up. | main + fable; codex SEC-5; final pass C2/D1 | wider crypto domain (codex) — the validation, not the key, is what must widen | adopted (amended) |
| D19 | **Legacy retirement is sequenced**: Arcs 1–3 ADD alongside legacy; all legacy artifacts/crates/scripts/consumers are deleted in ONE step (P8) after the last consumer is rewritten, so `typecheck:all` is green at every arc. | fable | delete in Arc 2 | adopted |
| D20 | TXE floor ≥ **40** named tests via a committed per-crate manifest (`token_bridge_hub/txe-manifest.txt`); names that exist only under spike Verdict A are marked conditional. | main; codex/fable | bare count ≥ 33 | adopted |
| D21 | `register_token` / `register_and_claim_public` take **compressed words** (`name_word: Field, symbol_word: Field`) — the single datum shared by event, message, manifest and journal. In-circuit the hub reconstructs `str<31>` via `FieldCompressedString::from_field(w).to_bytes().as_str_unchecked()` (a stdlib builtin, `noir_stdlib/src/array/mod.nr:303`; `to_bytes` is a 31-limb decomposition that fails closed on a wider word — under 5.0.1 that decomposition and `from_string` are exact inverses, so the round-trip assert originally specified here was an identity and was deleted in the arc-2 loop), then derives the Token address from the **typed, re-serialized** ctor args (`Token::interface()` + `hash_args`), never from `context.get_args_hash()`. P0 keeps a compile + keystone-parity check. | codex IMP-2 + fable R2-X1 (words); fable S7 (typed derivation) | `str<31>` args (66-field ABI, TS synthesizes the string) | adopted — dispute closed |
| D22 | **Per-token wallet grant**: grants are exact-address, no wildcards (`capabilities.ts:33`); a request adding a new address/function **re-prompts and the approval replaces the stored grant**, scope checkers union downstream (`wallet-bridge/src/dispatcher.ts:304-327,384-415`; `dispatcher.test.ts:1488`). The app requests `manifest ∪ journal-token-set ∪ {selected Token}` — one wallet prompt per new token, **raised at "Sign & send" for deposits (before the Ethereum signature, so a declined prompt cancels with nothing on-chain) and at token selection for exits (Aztec balances are needed at Step 1)** — under a **selection epoch** (serialized requests; a completion for a stale selection is discarded; the returned scopes are verified to contain the current Token before it is registered); journal lane resume is gated on the grant; **never** wildcard `burn_*` (wildcard authwits route to silent execution — `method-scope-checkers.ts:273-278`). `register_*` cannot simulate before the instance is in the PXE (`publish_contract_instance.nr:14`). | fable S1/R2-C5 + codex IMP-1/M4/V1; timing: owner (A6) | wildcard Token scopes | adopted — A6 ✓ |
| D23 | **Router portal rule per entrypoint** (fixes a design bug in the first consolidation): `bridge()` → `tokenPortal == portalOf(bridgeToken)` after inline create, **or** the canonical `feeJuicePortal` iff `bridgeToken == FEE_ASSET && !isPrivate` (direct gas); `bridgeWithFuel(fuel < total)` → `tokenPortal == portalOf(bridgeToken)` ONLY (a partial "AZTEC + gas" remainder must never enter the FeeJuice portal — it would mint gas, and `depositToAztecPrivate` doesn't exist there); `bridgeWithFuel(fuel == total)` → `tokenPortal == 0`. `_checkPortal` + `createPortal` run **before** the Permit2 pull. Private gas is, as today, a public FeeJuice deposit whose recipient is the private FPC (`isPrivate = false` on that leg). | codex SEC-2/SEC-3; fable R2-C6 | "feeJuicePortal allowed whenever bridgeToken == FEE_ASSET" | adopted |
| D24 | TXE server deps are a committed mini-project `contracts/bridge/aztec/txe-server/{package.json,bun.lock}` installed with `--frozen-lockfile` (today `run-txe-tests.sh:68-70` does a lockless `bun add @aztec/txe@5.0.1`). | codex SEC-11 | runtime `bun add` | adopted |
| D25 | Sprite is **hand-assembled and committed**, keyed by `chainId:address` (a list/pasted "USDC" never wears the USDC logo by symbol), svgo-stripped (`<script>`, `on*`, `foreignObject`). | fable S3/O2 | generator script; symbol keys | adopted |
| D26 | One TS conductor (`deploy-generation.ts` with a `pre-create` subcommand); `DeployGeneration.s.sol` is a fork-test fixture only; the public `derive_token` view is dropped (TXE reads `token_for` after a fixed-vector register). | codex SCOPE-1/2 | three operator surfaces | adopted |
| D27 | Exit direction lives as a **direction segment above Step 1** (`Ethereum → Aztec` / `Aztec → Ethereum`): it swaps Step 1's balance column to L2, reduces Step 2 to `TOKEN` (recipient = connected L1 address, no gas outcomes), and Step 3 to send / arrives / network fee / takes. | main (fable O3) | separate exit tab | adopted — **A8** (UX) |
| D28 | **The factory keeps one on-chain registration record per token**: `registrations[token] = { address portal; uint8 decimals; uint64 registerIndex; bytes32 nameWord; bytes32 symbolWord; bytes32 registerKey }` (packed into 4 slots, ~90k gas once per token; `registerKey` is what checkpoint polling consumes — `journal.ts:70-73`). The *clones* stay storage-less. The record is the authoritative source of the words + the register leaf for every client (one `eth_call` through the wallet-tunnelled RPC — no `eth_getLogs`), and `verify-deployments` reads it back per manifest token. | fable R2-S1 | words only in the event (needs throttled `eth_getLogs`); "zero storage" read as factory-wide | adopted |
| D29 | **"First-time" is a three-way L1/L2 state** `(portalOf, token_for)`, decided at claim time, not at form time: `token_for(erc20) ≠ 0` → `claim_public`/`claim_private`; else `register_and_claim_public` (public) or `register_token` (private) — and if that tx is dropped because another actor registered first (the publish nullifier is a duplicate), the app retries as claim-only. The conductor's `pre-create` checks `token_for` before registering. Tests: relayer-first, concurrent first-time deposits, portal-exists-but-unregistered. | codex M2 + fable R2-S1/R2-C1 | decide once on `portalOf == 0` | adopted |
| D30 | **Words at creation are authoritative**: the wizard's pre-tx derivation from live metadata is a preview only; after the L1 receipt the app re-reads `registrations[token]`, re-derives `l2Token`, re-grants/registers if it changed, and rewrites the journal `token` block (`registerIndex`/`registerKey` included) — the recovery-key domain is unaffected because `portal` is metadata-independent. A renamed/upgraded token (MATIC→POL) therefore never produces a dangling `l2Token`. | codex M1 + fable R2-S2/R2-C4 | trust the pre-tx UI read | adopted |
| D31 | `minFuelOutput = max(quote × (1 − s), minFuelFj)` is what gets signed — the swap reverts on L1 rather than stranding Fee Juice below the claim floor (today `quote ≥ minFuelFj` is checked but `quote × (1 − s)` is signed, `deposit-flow.ts:738,751`). First-time token+gas deposits add a calibrated manifest `fjRegister` on top of `txTarget × fjPerTx` because the registration tx (publish + ctor + register + mint) costs far more than a private transfer. | fable R2-S3/R2-C2 | — | adopted |
| D32 | Genesis order + determinism: publish the Token and hub **classes** first (L2-only, no address dependency), then the L1 factory, then the hub instance with **`salt = Fr(factory)`** so a lost journal can never orphan a factory (the hub is recomputable from the factory alone). | fable R2-O1/R2-O3 | random salt journaled | adopted; D2 amended |
| D33 | **CI compiles the hub** with the pinned 5.0.1 toolchain and asserts class-id + normalized-bytecode parity with the committed artifact (today CI never compiles Noir contracts, so source and artifact can silently disagree). P0 probes whether `aztec-nargo compile` transpiles public bytecode; if not, the workflow installs the transpiler beside it. Halmos gate asserts committed `check_*` **names**, not just counts. | codex M3/M5 | class-id pins on committed artifacts only | adopted |
| D34 | **The tools app is a standard dApp**: nothing in `apps/extension` or the wallet packages (`wallet-bridge`, `aztec-runtime`, …) is touched or tested by this plan; the wizard uses only the public `@aztec/wallet-sdk` surface (`requestCapabilities`, `registerContract`, `sendTx`, `executeUtility`). If the wallet rejects a legitimate dApp flow, that is a wallet bug filed and fixed on the wallet's side. The wallet ↔ hub seam is exercised by the owner's P10 checklist (register-and-claim public, 2-tx private, exit with authwit). | owner (A9, 2026-09-02) | an extension network e2e (fable R2-S4); wallet-package unit tests | adopted — A9 ✗ |
| D35 | Under TXE Verdict B the Noir side of the token-address keystone is a **`contract_library_method` pinned by a plain `#[test]`** (`aztec-nargo test`, no TXE, no publish) against the `hub-token.test.ts` literal. | fable R2-C3 | verdict-conditional TXE assert only | adopted |
| D36 | **The register message is consumed in the PRIVATE phase, before publishing.** `register_token` reads `l1_factory` (constant-cost private read), calls `PrivateContext::consume_l1_to_l2_message(register_hash, [REGISTER_SECRET], l1_factory, leaf)`, and only then publishes the Token instance and enqueues the ctor + `_register` (which now only initializes the two maps). A wrong-metadata or already-consumed attempt fails in private with **zero side effects** — rollback of a persisted publish nullifier is no longer load-bearing (the final pass showed the earlier "inert" claim was wrong: the registry emits the token address as a nullifier, `contract_instance_registry_contract/src/main.nr:154-156`, so a stranded publish would make that token permanently unregisterable). Consume sites become three (`register_token`, `claim_public`, `claim_private`); `check-sole-consumer.sh` counts and names them. | final pass B-I4 | public consume + proven rollback per fee mode | adopted |
| D37 | Exact **portal-debit** on `withdraw` (`bal(before) − bal(after) == amount`, content-hash-neutral) so a sender-side tax can never drain the reserve; **no recipient-credit check** — it would brick every withdrawal of a token that later enables a transfer tax. A later-enabled tax is paid by the withdrawer and documented as such. | final pass A1 (partially) | exact recipient credit (final pass) | adopted |
| D38 | **Live cross-binding readback** (`verify-l1.ts` + `live-intent.ts`, blocking at P6/P10): every immutable on every live contract — router → `factory`/`permit2`/`feeJuicePortal`/`swapTarget`; factory → `IMPLEMENTATION`/`INBOX`/`ROLLUP_VERSION`/`L2_HUB`/owner; implementation → `FACTORY`/`INBOX`/`OUTBOX`/`L2_HUB`; swap target → `poolManager`/`feeJuice`/`weth`/owner; hub → `l1_factory`/`token_class_id` — plus runtime code hashes against the built artifacts. A router built against a different factory would silently reopen the arbitrary-portal class. | final pass A2 | owners + two pointers only | adopted |
| D39 | **CI compiles the hub with the pinned `aztec compile` + `bb` path** (a mini-project `contracts/bridge/aztec/toolchain/{package.json,bun.lock}` pinning `@aztec/aztec@5.0.1` + the bb binary), never plain `aztec-nargo compile` (`compile.sh:7-10`: non-transpiled output). `compile.sh --check` compiles and diffs class id + normalized bytecode against the committed artifact; it is a **local gate on P3/P4/P8** and a CI job. Halmos proofs each carry a forge-level mutation canary (the property must fail under a deliberate break) and run warning-free — a named proof that silently prunes to vacuity is caught. | final pass B-I7/B-I6/C4 | spike question; names-only assertion | adopted; D33 amended |
| D40 | **L2 exit-only pause.** The hub takes `guardian: AztecAddress` as a constructor arg (the hub address commits to it; no transfer, no repoint) and keeps `exits_paused: PublicMutable<bool>`; `set_exits_paused(bool)` is guardian-only; `exit_to_l1_public` asserts inline, `exit_to_l1_private` via the enqueued `_assert_exits_open` self-call, so a paused private exit reverts atomically and no burn persists. **Claims are never pausable** — a deposit already made must always be claimable. Pausing L1 withdraws + L2 exits together closes the burn-then-frozen window. Testnet guardian = the deployer's Aztec account; mainnet = a dedicated account (`/harden security`). | owner (Q2, 2026-09-02); fable/codex mainnet note | ownerless hub | adopted — D3/D-hub amended |
| D41 | Testnet fuel routes: seed TOKEN/WETH pools for **fake USDC and fake USDT**; real Sepolia WETH is 1-hop; cbBTC/WBTC show `no-route`. | owner (A1 + one more route) | USDC only | adopted — A1 ✓ |
| D42 | Gas-share default `txTarget = 20`. | owner (A2) | 15 | adopted — A2 ✓ |

Round-2 dispute closure: D21 → words (both reviewers, with the in-circuit round-trip); the **witness stays 12 fields** (both reviewers — intent is a derived, on-chain-enforced predicate; a 13th field would rewrite the `WitnessHash` vectors and show the signer nothing the zeroed fields don't); the L2 *exit-only* pause under a constructor-immutable guardian was raised for mainnet and **the owner adopted it now (D40)**; `/harden security` stays a **blocking** mainnet decision on recovery/migration.

## Architecture & Implementation

### L1 — `contracts/bridge/evm/src/`

**`TokenPortalImpl`** (`is ReentrancyGuardTransient`; compiled in-project via the `@aztec/` remap). Clone runtime = 65 bytes; the implementation is an ordinary contract.

```solidity
IPortalFactory public immutable FACTORY;   // msg.sender at construction (D7)
IInbox  public immutable INBOX;  IOutbox public immutable OUTBOX;  uint256 public immutable ROLLUP_VERSION;
bytes32 public immutable L2_HUB;
constructor(IRegistry registry, bytes32 l2Hub);            // rollup → inbox/outbox/version as NuloTokenPortal.sol:74-81
function underlying() public view returns (IERC20);        // address(bytes20(Clones.fetchCloneArgs(address(this))))
function depositToAztecPublic(bytes32 to, uint256 amount, bytes32 secretHash) external nonReentrant returns (bytes32 key, uint256 index);
function depositToAztecPrivate(uint256 amount, bytes32 secretHash) external nonReentrant returns (bytes32 key, uint256 index);
function withdraw(address recipient, uint256 amount, bool withCaller, Epoch epoch, uint256 numCheckpointsInEpoch, uint256 leafIndex, bytes32[] calldata path) external nonReentrant;
```
Bodies are **content-hash-identical** to `upstream/NuloTokenPortal.sol:93-186` (not byte-identical — A5d): `l2Bridge → L2_HUB`, `underlying → underlying()`; the three `abi.encodeWithSignature` preimages (`:104`, `:136`, `:176-180`) untouched — `ContentHash.t.sol` and `PortalRoundtripFuzz.t.sol` pass with zero vector edits. Inserted, all content-hash-neutral: `if (FACTORY.depositsPaused()) revert Paused()` / `withdrawsPaused()`; `require(amount <= type(uint128).max)` (D13); exact-in on deposit `require(bal(after) − bal(before) == amount)` (D5) and exact portal-debit on withdraw (D37). Zero storage; no `initialize`.

**`PortalFactory`** (`is Ownable2Step`; owner == guardian; `renounceOwnership` reverts)

```solidity
event PortalCreated(address indexed token, address indexed portal, bytes32 nameWord, bytes32 symbolWord, uint8 decimals, bytes32 registerKey, uint256 registerIndex);
event PauseChanged(bool deposits, bool withdraws);
TokenPortalImpl public immutable IMPLEMENTATION;  IInbox public immutable INBOX;  uint256 public immutable ROLLUP_VERSION;  bytes32 public immutable L2_HUB;
bytes32 public constant REGISTER_SECRET_HASH = <compute_secret_hash([0]), keystone-pinned>;   // D10
struct Registration { address portal; uint8 decimals; uint64 registerIndex; bytes32 nameWord; bytes32 symbolWord; bytes32 registerKey; }   // D28, 4 slots
mapping(address token => Registration) public registrations;  mapping(address portal => address token) public tokenOf;
function portalOf(address token) public view returns (address);  // registrations[token].portal
bool public depositsPaused;  bool public withdrawsPaused;
constructor(IRegistry registry, bytes32 l2Hub, address guardian);   // deploys IMPLEMENTATION (D7)
function salt(address token) public pure returns (bytes32);          // bytes32(uint256(uint160(token)))  (D9)
function predictPortal(address token) public view returns (address); // Clones.predictDeterministicAddressWithImmutableArgs(IMPLEMENTATION, abi.encodePacked(token), salt(token))
function createPortal(address token) public returns (address portal); // idempotent (D6)
function setPaused(bool deposits, bool withdraws) external onlyOwner;
```
`createPortal` first call: `require(token.code.length > 0)`; metadata via gas-capped `staticcall`s with bounded returndata copies — `decimals()` **required** (`returndatasize == 32` — a `string` return's offset word must not read as 32 decimals — value ≤ 255, else `revert NoDecimals()`), `name()`/`symbol()` accept `string` or `bytes32` (MKR-style), default empty; sanitize to 31 printable-ASCII bytes (D8); clone; `INBOX.sendL2Message(L2Actor(L2_HUB, ROLLUP_VERSION), registerHash, REGISTER_SECRET_HASH)`; emit. `registerHash = Hash.sha256ToField(abi.encodeWithSignature("register(address,address,bytes32,bytes32,uint8)", token, portal, nameWord, symbolWord, decimals))`; write `registrations[token]` (incl. the returned `key` + `index`) + `tokenOf[portal]`; emit. Metadata is sampled once at first creation — a front-runner cannot change portal or token identity, only freeze the first-observed sanitized words.

**`SwapBridgeRouter`** — constructor gains `IPortalFactory factory`; witness stays 12 fields (`SwapBridgeRouter.sol:113-126` ↔ `l1.ts:25-38` ↔ `WitnessHash.t.sol`).

- Intent is derived first: `fuelOnly = (fuelAmount == totalAmount)`. **Before the Permit2 pull** (D23): `bridge()` → `tokenPortal == factory.portalOf(bridgeToken)` (calling `factory.createPortal(bridgeToken)` when empty) **or** `bridgeToken == FEE_ASSET && tokenPortal == feeJuicePortal && !isPrivate`; `bridgeWithFuel` partial → `tokenPortal == portalOf(bridgeToken)` only; `bridgeWithFuel` full → `tokenPortal == 0 && aztecRecipient == 0 && tokenSecretHash == 0`.
- `bridgeWithFuel`: `0 < fuelAmount <= totalAmount`; at equality the token leg is skipped (event emits zero token key/index); **empty `path` allowed iff `bridgeToken == FEE_ASSET`** (identity swap: `fuelReceived = fuelAmount`) — "AZTEC + gas" is one tx with the remainder in AZTEC's factory portal. Exact-in on the Permit2 pull (D5); `totalAmount ≤ uint128.max` (D13).
- Router owner (unchanged from today): `setSwapTarget` + `sweep` — in the threat model (§ Security), readbacks in `live-intent.ts`.
- Halmos: `check_bridge_rejectsForeignPortal`, `check_bridgeWithFuel_partialRejectsFeeJuicePortal`, `check_bridgeWithFuel_fuelOnly_conservesUserFunds`; existing proofs extended to the boundary. Invariant handler gains `createPortal` + `setPaused`; `testFuzz_hostilePortal` flips to "reverts before any pull".

### L2 — `contracts/bridge/aztec/token_bridge_hub/` (replaces `token_bridge` + `token_minter_proxy`)

```rust
#[storage] struct Storage<C> {
  l1_factory:     PublicImmutable<EthAddress, C>,                       // constructor (D2)
  token_class_id: PublicImmutable<ContractClassId, C>,
  guardian:       PublicImmutable<AztecAddress, C>,                      // constructor (D40) — pause-only role
  exits_paused:   PublicMutable<bool, C>,
  portal_of:      Map<AztecAddress /*l2 token*/, PublicImmutable<EthAddress, C>, C>,
  token_of:       Map<EthAddress /*erc20*/,      PublicImmutable<AztecAddress, C>, C>,   // discovery + first-registration-wins (defense in depth against a second factory-authored leaf)
}
global REGISTER_SECRET: Field = 0;

#[external("public")] #[initializer] fn constructor(token_class_id: ContractClassId, l1_factory: EthAddress, guardian: AztecAddress)     // universal deploy
#[external("public")] fn set_exits_paused(paused: bool)   // guardian-only (D40)
#[external("public")] #[only_self] fn _assert_exits_open()
#[external("private")] fn register_token(erc20: EthAddress, portal: EthAddress, name_word: Field, symbol_word: Field, decimals: u8, register_leaf_index: Field)   // D21
#[external("private")] fn register_and_claim_public(erc20, portal, name_word, symbol_word, decimals, register_leaf_index, to: AztecAddress, amount: u128, secret: Field, claim_leaf_index: Field)   // D11
#[external("public")] #[only_self] fn _register(erc20: EthAddress, portal: EthAddress, token: AztecAddress)   // maps only (D36)
#[external("public")]  fn claim_public(token: AztecAddress, to: AztecAddress, amount: u128, secret: Field, message_leaf_index: Field)
#[external("private")] fn claim_private(token: AztecAddress, recipient: AztecAddress, amount: u128, claim_salt: Field, message_leaf_index: Field)
#[external("public")]  fn exit_to_l1_public(token: AztecAddress, recipient: EthAddress, amount: u128, caller_on_l1: EthAddress, authwit_nonce: Field)
#[external("private")] fn exit_to_l1_private(token: AztecAddress, recipient: EthAddress, amount: u128, caller_on_l1: EthAddress, authwit_nonce: Field)
#[external("public")] #[view] fn portal_for(token) -> EthAddress;  fn token_for(erc20) -> AztecAddress   // discovery views return ZERO when unregistered (is_initialized/read_unsafe); claims/exits keep the hard-reverting read
```
Surface: constructor, 7 mutating externals (incl. `set_exits_paused`), 2 views, 2 `#[only_self]`.
- `register_token` (private): **consume first** — `consume_l1_to_l2_message(register_content_hash(erc20, portal, name_word, symbol_word, decimals), [REGISTER_SECRET], l1_factory.read(), register_leaf_index)` (D36); then `name = FieldCompressedString::from_field(name_word).to_bytes().as_str_unchecked()` and `assert(FieldCompressedString::from_string(name).value == name_word)` (same for symbol; D21); `token = derive_token(erc20, name, symbol, decimals)` — a `contract_library_method` (D35) building `ContractInstance{ salt: erc20.to_field(), deployer: self.address, original_contract_class_id: token_class_id, initialization_hash: compute_initialization_hash(Token::interface().constructor_with_minter(name, symbol, decimals, self.address, ZERO).selector, hash_args(args)), immutables_hash: 0, public_keys: PublicKeys::default() }.to_address()` from the typed args; `publish_contract_instance_for_public_execution(&mut context, token)` (v5.0.1, private-only, asserts `deployer == this`); enqueue `_register(erc20, portal, token)`; enqueue `Token::at(token).constructor_with_minter(name, symbol, decimals, self.address, ZERO)` (initializer guard passes: `deployer == msg_sender == hub`). `register_and_claim_public` = the same + enqueue `self.claim_public(token, to, amount, secret, claim_leaf_index)` last — the kernel orders enqueued public calls by side-effect counter (`private-kernel-lib/src/accumulated_data/sort_by_counter.nr`), so `_register` → ctor → `claim_public` (→ `mint_to_public`, which sees the pending init nullifier in the AVM).
- `_register` (public, `#[only_self]`): `portal_of.at(token).initialize(portal)`; `token_of.at(erc20).initialize(token)` — nothing else. Wrong metadata or an already-consumed leaf fails **in private** with no side effects (D36). A second factory-authored leaf for the same erc20 with different words derives a different token and fails on `token_of.initialize` (first registration wins).
- Claims/exits = `token_bridge/src/main.nr:94-158` with `config.portal → portal_of.at(token).read()` (private read = constant cost; public and private reads both hard-revert if unregistered — `public_immutable.nr:184,329`) and `TokenMinterProxy → Token::at(token)`; `claim_private`'s consume block (`:124-132`, F-007 `derive_claim_secret`) verbatim. Exits assert `!exits_paused` first (inline in public; enqueued `_assert_exits_open` in private — the tx reverts atomically, D40), then burn via `Token.burn_*` under an authwit whose **consumer is that Token** (`context.this_address()` in `assert_inner_hash_valid_authwit`, `auth.nr:270-284`) with the hub as the `msg_sender` inside the inner hash (`burn_private` is `#[authorize_once]`, aztec-standards `main.nr:464-466`) — a token-A authwit cannot be replayed on token B. No owner, no upgrade; the guardian's only power is `set_exits_paused`. New coupling: the hub calls the protocol `ContractInstanceRegistry` by hard-coded selector (`publish_contract_instance.nr:49-51`, identical 5.0.1 → 5.2.0) — recorded in `UPDATE.md`.
- Private first claim = 2 txs (`mint_to_private` reads `minter` from the anchor block); public first claim = 1 tx via `register_and_claim_public` (D11).

### bridge-core — `packages/bridge-core/src/`

New: `portal-address.ts` (`portalInitCode(impl, erc20)`, `predictPortal(factory, impl, erc20)` via viem `getCreate2Address`), `register-hash.ts` (`registerContentHash`, `toNameWord`/`fromNameWord`, `REGISTER_SECRET(_HASH)`), `factory-abi.ts` (pinned to the forge artifact like `router-abi.test.ts`), `hub-token.ts` (`deriveHubTokenInstance(hub, tokenClassId, erc20, words)` — `getContractInstanceFromInstantiationParams` with `deployer: hub`, `salt: Fr(erc20)`, the `str<31>` ctor args synthesized from the words), `factory-registry.ts` (`readRegistration(factory, erc20)` — D28), `erc20.ts` (ONE ABI + `readErc20Metadata` + `readErc20Balances` via `multicall`), `token-list.ts`, `route-discovery.ts`, `gas-share.ts`. Changed: `candidate-schema.ts`, `journal.ts`/`backup.ts`, `promotion.ts`, `flows.ts`, `route.ts`/`quote.ts` (batched via Multicall3 `aggregate3`; the route builder asserts `hooks == 0`, currency continuity and endpoints before signing — the same grammar `_validateRoute` enforces, mirrored in `route-conformance.test.ts`), `router-abi.ts`, `noir-artifact-classids.test.ts`, `artifacts.ts` (adds hub exports; legacy exports removed in P8).

```ts
// candidate-schema.ts — v2, .strict() everywhere; bridge: null ⇒ placeholder (D4)
interface ManifestV2 { schema: 2; network; l1ChainId; walletChainId;
  bridge: null | { l1: { registry; factory; implementation; guardian; router; permit2; swapTarget; feeJuicePortal;
                          swap?: { poolManager; quoter; weth; feeJuice; tiers: Pool[]; ethFj: Pool; slippageBps; minFuelFj; fjPerTx; fjRegister } }   // D14, D31
                   l2: { hub: L2Record /* args: [tokenClassId, factory, guardian] */; guardian: string; tokenClassId: string; tokenArtifactSha256: string }
                   tokens: Array<{ erc20; portal; l2Token; nameWord: Hex; symbolWord: Hex; decimals; displayName; displaySymbol;
                                   source: "permissionless-mint" | "canonical"; sourceContract?; maxWholePerTx?; pools?: Record<string, Pool> }> }
  feeJuice: { portal; asset; feeAssetHandler?; minFj }; privateFpc?: {…}; privateClaimMode: "salt-v2" }
// superRefine: tokens[i].portal == predictPortal(factory, implementation, erc20); tokens[i].l2Token == deriveHubTokenInstance(...).address; every Pool.hooks == 0
// journal.ts — schema 3 (D18): discriminated union on intent
type JournalRecord = Base & ({ intent: "token" | "token+gas"; token: TokenBlock } | { intent: "gas"; token?: never })
interface TokenBlock { erc20; portal; l2Token; nameWord; symbolWord; decimals; displaySymbol; registerKey?; registerIndex? }   // rewritten from registrations[token] after the L1 receipt (D30)
// recoveryKeyMessage keeps {chainId, portal, bridge, secretHashHex}
// token-list.ts — fetch(origin, { redirect: "error" }); stream with a byte cap and abort on overflow; zod-validate; chainId filter;
//   persist ONLY the validated, filtered subset under its own key (the live body is ~670 KB, `cache-control: no-store`); QuotaExceeded ⇒ memory-only
loadTokenList({ chainId, fetch, kv, now, ttlMs = 24h, origin: "https://tokens.uniswap.org", byteCap, tokenCap }) → { tokens: CatalogToken[]; provenance: "fresh" | "cache" | "fallback" }
// route-discovery.ts (D15)
type RouteOutcome = { kind: "route"; route: FuelRoute; quoteOut: bigint } | { kind: "identity" } | { kind: "no-route"; tried: number } | { kind: "unavailable"; reason: "rpc" | "config" }
// gas-share.ts (D14)
proposeGasShare({ amount, decimals, txTarget = 20, fjPerTx, fjRegister /* added when token_for == 0 */, minFuelFj, rate, slippageBps }) → { fuelAmount; fuelFj; capped: "min" | "half" | null }
// signed floor: minFuelOutput = max(quote × (1 − slippage), minFuelFj)  (D31)
```

### Faucet — `apps/faucet/src/`

`SendView.vue` (tab `SEND`; replaces `BridgeView.vue` + `FuelView.vue`) hosting `WizardShell` (`direction → token → amount → review → stepper → receipt`, D27), `StepStrip` (the extension `StepIndicator` pattern, roving tablist), `TokenStep`/`TokenList`/`TokenTile`/`PasteAddress`, `AmountStep`/`ChoiceCards`/`GasBreakdown`, `ReviewStep`/`ReviewDetails`; then the existing `BridgeStepper`/`BridgePhaseRail` (+ optional `register` phase) and `BridgeReceipt` (token + gas + "review said / you got"). Composables: `useTokenCatalog` (manifest ∪ list ∪ pasted), `useTokenSelection` (multicall metadata + balances; reads `registrations[erc20]` + `hub.token_for` for the three-way state (D29); previews the L2 token from the record's words or, pre-creation, from sanitized live metadata; **requests the per-token wallet grant under a selection epoch, then** registers the instance — D22), `useRouteQuote` (replaces `BridgeForm.vue:149-184`), `useGasShare`, `useSend` (replaces `useDeposit.ts` + `useFuel.ts` over a generalized `deposit-flow.ts`; after the L1 receipt re-reads the registration record and rewrites the journal token block — D30; claim-time `token_for` branch + race retry — D29; signs `max(quote×(1−s), minFuelFj)` — D31), `useHubExit` (replaces `useWithdraw.ts`; **preflights both `withdrawsPaused` (L1) and `exits_paused` (L2) before the authwit/exit** and the review states that the burn happens before FINISH — SEC-8). `TokenTile` = committed sprite `src/assets/token-sprite.svg` keyed by `chainId:address` (D25) + monogram/hue fallback. **Mainnet (D4)**: `manifest.bridge === null` ⇒ `App.vue` renders `MainnetPlaceholderView` only — no tabs, no wallet session singleton, `assertNodeChainMatches` skipped, CSP without node/wallet origins (A4). CSP (testnet): `cspConnectSrc` += `https://tokens.uniswap.org`; `img-src` unchanged. Every new interactive element gets a `TESTIDS` entry first.

### Flows

- **(a) first-time, public**: token selected → wallet grant (D22) → `(portalOf, token_for) == (0, 0)` → review shows the first-time note → seal + sign witness (`tokenPortal = predictPortal(erc20)`) → `router.bridge`/`bridgeWithFuel` → router `createPortal` + deposit in ONE L1 tx (two Inbox leaves: `register`, `mint_to_public`) → receipt: read `registrations[erc20]`, re-derive + regrant if needed, rewrite the journal block (D30) → CROSSING → `token_for` still 0? ONE L2 tx `register_and_claim_public` (D11), else `claim_public`; a dropped register (race) retries as `claim_public` (D29) → CONFIRM.
- **(b) first-time, private**: same L1 tx (`mint_to_private`) → receipt re-derive (D30) → L2 tx 1 `register_token` unless `token_for ≠ 0` (REGISTER phase; relayer-able; race → skip) → next block → L2 tx 2 `claim_private` (F-007) → CONFIRM.
- **(c) known + gas**: `bridgeWithFuel` with the discovered route; both leaf indices from the event; gas claim ladder as today; `claim_*(token, …)`.
- **(d) gas only**: `bridgeWithFuel{ fuelAmount = totalAmount, tokenPortal = 0 }` — no portal, no register; journal `intent: "gas"`; AZTEC gas-only = `bridge(tokenPortal = feeJuicePortal)` (today's path); AZTEC + gas = `bridgeWithFuel(path = [], tokenPortal = portalOf(AZTEC))` (D23).
- **(e) exit**: direction `Aztec → Ethereum` → L1 + L2 pause preflight → authwit `Token.burn_*` for the hub → `exit_to_l1_*(token, …)` → PROVE → `portal.withdraw(...)` on the token's clone → FINISH. A withdraw pause delays FINISH; the record is never discarded.

### Non-obvious mechanics

- **Clone initcode** (OZ 5.7.0 `Clones.sol:279-293`): `0x61 ‖ uint16(20 + 0x2d) ‖ 3d81600a3d39f3363d3d373d3d3d363d73 ‖ impl ‖ 5af43d82803e903d91602b57fd5bf3 ‖ erc20` = 75 bytes; runtime 65 bytes; `portal = keccak256(0xff ‖ factory ‖ salt ‖ keccak256(initcode))[12:]`. Keystone: Solidity `predictPortal` ↔ TS `predictPortal` on a fixed vector.
- **Register word** (D8): `nameWord = bytes32(uint256(uint248(bytes31(sanitized))))` = `0x00 ‖ b0..b30`, equal to Noir `FieldCompressedString::from_string(name).value` as a big-endian field; TS derives `str<31>` args from the word, never from UTF-8.
- **Register hash**: `sha256ToField(selector4 ‖ pad32(erc20) ‖ pad32(portal) ‖ nameWord ‖ symbolWord ‖ pad32(decimals))`, 164 bytes; 4th keystone vector next to the three in `ContentHash.t.sol` / `keystone/src/main.nr` / `content-hash.test.ts`.
- **Hub token address**: `AztecAddress::compute(PublicKeys::default(), PartialAddress::compute(TOKEN_CLASS, salt = erc20, init_hash = poseidon2([selector(constructor_with_minter), hash_args(65 fields)], DOM_SEP__INITIALIZER), deployer = HUB, immutables_hash = 0))`; keystone: a TXE test registers a fixed vector and asserts `token_for(erc20)`; `hub-token.test.ts` asserts the same literal.
- **Genesis (D2, D32)**: publish the Token + hub classes (idempotent) → `factory = getContractAddress({from: deployer, nonce})` → `hub = derive(hubClass, salt = Fr(factory), [tokenClassId, factory, guardian])` → deploy the factory (nonce asserted at send; mismatch = journal + abort, nothing else exists yet) → deploy the hub instance (idempotent) → assert `factory.L2_HUB() == hub` and `hub.l1_factory == factory` before any pre-create.
- **Gas share (D14)**: `fuelFj = max(minFuelFj, txTarget × fjPerTx)`; `rate = quote(probe) / probe`; `fuelAmount = ceil(fuelFj / rate × (1 + slippageBps/1e4))`, capped at 50 % of `amount`; the real slice is re-quoted for the signed `minFuelOutput`.
- **Route discovery (D15)**: one Multicall3 `aggregate3` of dust `quoteExactInputSingle` per candidate; "initialized ≠ liquid"; best output wins; all fail ⇒ `no-route`; RPC failure ⇒ `unavailable` (distinct UI state).

### File change map (per arc; cross-checked against `recon.md` rows)

**Arc 1 (L1)** — add `src/{TokenPortalImpl,PortalFactory}.sol`, `src/interfaces/IPortalFactory.sol`, `test/Keystone.t.sol`, `test/{PortalFactory,PortalFactoryFuzz,PortalFactoryInvariant,FormalFactory,FormalClone,BlackhatFactory,FactoryFork}.t.sol`, `test/mocks/{FeeOnTransferERC20,Bytes32MetadataERC20,NoDecimalsERC20,HugeNameERC20,WeirdDecimalsERC20}.sol`, `script/DeployGeneration.s.sol` (fixture, D26); bridge-core `src/{portal-address,register-hash,factory-abi,factory-registry}.ts` (+ tests). Modify `SwapBridgeRouter.sol` (#3), `FormalRouter.t.sol`, `SwapBridgeRouter{,Fuzz,Invariant}.t.sol`, `BlackhatAudit.t.sol`, `ContentHash.t.sol` (+1 vector, #8), `PortalRoundtripFuzz.t.sol` (against a clone), `content-hash.ts`, `router-abi.ts`, `_bridge-contracts.yml` (halmos table), `contracts/bridge/evm/README.md` (A-1 section).
**Arc 2 (L2)** — add `token_bridge_hub/` crate (+ `src/test/{register,claims,claims_private,exits,guards,utils}.nr`, `txe-manifest.txt`, committed `target/*.json`), `contracts/bridge/aztec/txe-server/{package.json,bun.lock}` (D24); bridge-core `src/hub-token.ts` (+ test). Modify `keystone/src/main.nr` (+register vector, +`REGISTER_SECRET_HASH`, +hub-token vector), `check-sole-consumer.sh` (hub path, named sites, `[REGISTER_SECRET]` dataflow, self-test fixtures), `compile.sh` + `run-txe-tests.sh` (`--crate` allowlist, per-crate manifest + staged deps, frozen-lockfile server install), `noir-artifact-classids.test.ts` (+hub, +Token class id; legacy pins stay until P8), `artifacts.ts` (+hub exports), `txe-ts-map.md`, `_bridge-contracts.yml` (sole-consumer path; **hub compile + class-id/bytecode parity with the committed artifact — D33**; halmos `check_*` names committed).
**Arc 3 (core)** — add `src/{erc20,token-list,route-discovery,gas-share}.ts` (+ tests), `scripts/deploy-generation.ts` (+ `pre-create` subcommand). Modify `candidate-schema.ts` (#11), `journal.ts`/`backup.ts` (#17), `promotion.ts`, `flows.ts`, `route.ts`/`quote.ts` (#18), `route-conformance.test.ts`, `script-l2.ts` (`deriveInstance` with a deployer param, #9), `script-l1.ts`, `script-bootstrap.ts`, `deploy-manifest.ts`, `deploy-sandbox.ts` (#11; smoke generalized), `live-intent.ts` (readbacks: factory owner, router owner + `swapTarget`, `L2_HUB`, `l1_factory`), `verify-l1.ts`, `apps/faucet/scripts/verify-deployments.ts` (v2), and **every operator script that reads the manifest** (`tsconfig.scripts.json` includes `scripts/**`): `smoke-existing-testnet.ts` (v2, two tokens), `smoke-swap-existing-testnet.ts`, `fuel-testnet.ts` (+ `fjPerTx` calibration output), `deposit-testnet.ts`, `relay-claim-testnet.ts`, `restore-swap.ts`, `fee-juice-canary-testnet.ts`, `check-fpc-version.ts`, `discover-mainnet-fuel.ts`, `SeedTokenPool.s.sol` (called by the conductor). Delete `deploy-bridge-{testnet,mainnet}.ts`, `smoke-existing-mainnet.ts` (D4), `reuse-token.ts` (+ test), `build-portal-artifact.ts`. Manifests: `public/testnet-bridge.candidate.json` v2 (sandbox fixture), `public/mainnet-bridge.json` → v2 stub `bridge: null` (candidate + journal retired).
**Arc 4 (wizard)** — add components/composables (§ Faucet), `src/assets/token-sprite.svg` (D25), `MainnetPlaceholderView.vue`, `tests/e2e/send-smoke.test.ts`. Modify `bridge-deployments.ts` (v2 reader), `capabilities.ts` (per-token grant, D22), `useWalletConnection.ts`, `useBridgeJournal.ts`, `deposit-flow.ts` (+ re-pin `useDeposit.characterization.test.ts` once), `bridge-steps.ts`, `testids.ts`, `network-targets.ts`, `main.ts` (mainnet skips node assert), `vite.config.ts` (mainnet CSP), `App.vue`, `BridgeStepper.vue`/`BridgePhaseRail.vue`/`BridgeReceipt.vue`/`BridgeJournalCard.vue`. **P8 retire legacy (one step)**: delete `BridgeView.vue`, `FuelView.vue`, `BridgeForm.vue`, `FuelForm.vue`, `useDeposit.ts`, `useFuel.ts`, `useWithdraw.ts`, `useL1Usdc.ts`, `asset-label.ts`, `MintTestUsdc.vue` (→ `MintTestToken.vue` per manifest token), `BridgeAddToken.vue` (→ receipt CTA), the `bridge-smoke`/`fuel-smoke` e2e; contracts `token_bridge/`, `token_minter_proxy/`, `upstream/NuloTokenPortal.sol` + `.build.json`, `test/PortalReinit.t.sol`, `test/FormalPortal.t.sol` (its role passes to `FormalClone`); bridge-core `scripts/portal-artifact.ts` (+ test), legacy `artifacts.ts` exports, legacy class-id pins; `test/MainnetFuel.fork.t.sol` + `script/DeployBridgeMainnet.s.sol` legacy portal refs.
**Arc 5 (docs)** — READMEs ×4, `.claude/skills/aztec-update/SKILL.md` Branch B, `UPDATE.md`, `CLAUDE.md` pointers, `.env.example`, `implementations-plan/index.md`, `audit/**` references left as history.

### Trade-offs / not taken

Per-token L2 bridges (locked out). AVM CREATE2 in the hub (D1). `bind_l1` (D2). Expiring pause / rescue timelock / per-portal pause (D3; A7 offered). Legacy-exit affordance (D4). FoT delta accounting (D5). Factory create-and-deposit helpers (D6). Wallet-side mixed `BatchCall` (D11). 1-tx private first claim via `Token.initialize_transfer_commitment` + `mint_to_commitment` — feasible, deferred (a third consume site under the F-007 tripwire). Universal-deploy tokens (SDK publish) — front-run on the enqueued ctor. Connector routes + `T/FJ` pools (D15, mainnet arc). V3 leg. Bundled-only token list (owner chose runtime; mitigations kept). Wildcard Token scopes (D22).

## Phases

Gate legend — **fast**: `bun run lint && bun run typecheck:all` · **forge**: `cd contracts/bridge/evm && bun ../../../packages/bridge-core/scripts/gen-remappings.ts && forge build && forge test --no-match-contract Fork` · **halmos**: `forge build --ast --force && halmos --match-contract '^Formal'` (per-contract proof counts as `_bridge-contracts.yml` asserts) · **fork**: `SEPOLIA_RPC_URL=… forge test --match-contract Fork` · **txe**: `contracts/bridge/aztec/scripts/run-txe-tests.sh --crate token_bridge_hub` (per-crate named-test manifest; positive count asserted) · **keystone**: `cd contracts/bridge/aztec/keystone && aztec-nargo test --force` · **sole**: `bash contracts/bridge/aztec/scripts/check-sole-consumer.sh --self-test && bash contracts/bridge/aztec/scripts/check-sole-consumer.sh` · **core**: `bun run --cwd packages/bridge-core test` · **faucet**: `bun run test:faucet && bun run --cwd apps/faucet test:e2e` · **sandbox**: `bun run --cwd packages/bridge-core deploy:sandbox --smoke` (runs ALONE on this box) · **verify**: `bun run --cwd apps/faucet verify:deployments` · **audit**: `bun run audit:faucet`. Every gate includes **fast**; every contract phase (P1–P4, P8) includes **forge + halmos**. Pass = exit 0 for every listed command plus the stated criteria. Lessons: `lessons/phase-N.md`.

### Arc 0 — Spikes + keystones · branch `any-erc20-bridge/spike` (never merges; keystone tests cherry-picked into Arcs 1–2)

**P0 ✓ (2026-09-02)** — Verdict B (by source); D11/D36/D21/D35 proven in TXE + keystones; D39: `aztec compile` required. See `lessons/phase-0.md`.

- **P0 — spike + keystones.** (1) `run-txe-tests.sh --crate <dir>` (allowlist; per-crate manifest + staged deps; D24 server install). (2) Minimal `token_bridge_hub` (`constructor`, `register_token`, `register_and_claim_public`, `_register`, `claim_public`, `claim_private`) + TXE tests, each `call_private` being its own tx: tx A `register_and_claim_public` (publishes, registers, constructs, mints — one tx); tx B `claim_private` mints; tx C a wrong-metadata `register_token` fails in the private phase (no publish, no nullifier) AND a correct `register_token` for the same erc20 still succeeds afterwards (D36). Verdict A: publish works in TXE. Verdict B: D17 harness fallback (register-path tests marked conditional in the manifest). Verdict → `lessons/phase-0.md`. (3) D21 check: the word round-trip (`from_field → to_bytes → as_str_unchecked → from_string`) compiles on the 5.0.1 toolchain and the derived address matches the TS keystone; D39: `compile.sh --check` (pinned `aztec compile` + `bb`) diffs class id + bytecode against the committed artifact; the keystone `derive_token` is a `contract_library_method` with a plain `#[test]` (D35). (4) Keystones (permanent): forge `Keystone.t.sol` asserts `predictPortal`, `registerHash`, `REGISTER_SECRET_HASH` on fixed vectors; TS `portal-address.test.ts` / `register-hash.test.ts` assert the same literals; keystone crate asserts the register hash + `compute_secret_hash([0])`; a TXE test registers a fixed vector and asserts `token_for`; `hub-token.test.ts` asserts it.
  **Gate**: fast · forge (`--match-contract Keystone`) · keystone · txe (≥ 4 named tests) · core (the three new test files). Pass: every vector agrees across toolchains; spike verdicts written.

### Arc 1 — L1 · PR `any-erc20-bridge/l1` · `/code-review medium` + `/security-review`

**P1 ✓ (2026-09-02)** — factory + clones + keystones green (forge 106, halmos 4/1/2/2, fork 4/4 live Sepolia, core 292); halmos concretized around `createPortal` per I6 (sha256 stub sort bug) — `FormalFactoryTest 2` with a ∀-token CREATE2 proof. Two legacy fork suites drift against live Sepolia (7702 delegation on the `0xA11CE` signer; pool already initialized) — deferred to P2/P6. See `lessons/phase-1.md`.

- **P1 — `TokenPortalImpl` + `PortalFactory` + Arc-1 keystones.** Contracts + interfaces; `portal-address.ts`, `register-hash.ts`, `factory-abi.ts`. Tests: `PortalFactory.t.sol` (metadata decoding string/bytes32/missing/huge/non-ASCII; EOA target; `decimals` 256 or missing → `NoDecimals`; decimals 0/18/19/38/255 accepted; idempotent create; both pause bits; `Ownable2Step`; renounce reverts; exact-in FoT revert; u128 cap; **the D1 tuple test** — capturing Inbox actor == factory, content == `registerHash(token, portal, words, decimals)`, `portal == predictPortal`, `portal.underlying() == token`, `registrations[token]` incl. `registerIndex`, `tokenOf`, event fields all agree), `PortalFactoryFuzz.t.sol` (∀ erc20: `predictPortal == deployed`; sanitization total; `tokenOf(portalOf(t)) == t`), `PortalRoundtripFuzz` against a clone (vectors unchanged), `ContentHash.t.sol` +register vector, `PortalFactoryInvariant.t.sol` (I1 `predictPortal(t)` constant; I2 per-portal balance ≥ Σdeposits − Σwithdraws; I3 only the guardian flips pause), `FormalFactory.t.sol` (`check_createPortal_frontrunPreservesAddress`, `check_setPaused_revertsForNonOwner` failing via `assertTrue(false)`, `check_createPortal_idempotent`), `FormalClone.t.sol` (`check_deposit_revertsWhenPaused`, `check_withdraw_revertsWhenPaused`) — every `Formal*` suite carries a forge-level mutation canary per proof (D39), `BlackhatFactory.t.sol` (front-run create is identity-preserving; callback-driven create during a hostile `name()` under `staticcall`; returndata bomb bounded), `FactoryFork.t.sol` (Sepolia: real registry/inbox, real USDC + WETH metadata, real clone).
  **Gate**: fast · forge · halmos (`FormalRouterTest 4`, `FormalPortalTest 1`, `FormalFactoryTest 2`, `FormalCloneTest 2`; 4 summaries) · fork · core. Pass: zero legacy vector edits.
**P2 ✓ (2026-09-02)** — D23 router over the real factory: forge 140, halmos 8/1/2/2, router fork 15/15 live Sepolia (real Permit2 + V4 + registry/Inbox + FeeJuicePortal), core 295; `.gas-snapshot` + CI check; I5 measured at 323k live (the 150–200k estimate was low — see `lessons/phase-2.md`). `WitnessHash` vectors unchanged.

- **P2 — Router.** D23 rule, fuel-only, identity swap, caps, create-before-pull; `SwapBridgeRouter.t.sol` + fuzz (hostile-portal flip), invariants extended, `FormalRouter.t.sol` (+3), `BlackhatAudit.t.sol` PoCs (fake portal, FoT, partial AZTEC+gas to the FeeJuice portal rejected), router fork (real Permit2 + V4 through `createPortal` + `bridgeWithFuel` fuel-only + AZTEC identity + partial); `forge snapshot` for first-time vs known `bridge()` (I5); `_bridge-contracts.yml` halmos table **with committed `check_*` names** (D33); README A-1 → factory threat model. Budget time for concretizing symbolic paths through `CREATE2`/`EXTCODECOPY` (halmos may not support them symbolically).
  **Gate**: fast · forge · halmos (`FormalRouterTest 8`, `FormalPortalTest 1`, `FormalFactoryTest 2`, `FormalCloneTest 2`; 4 summaries) · fork · core (`router-abi`, `factory-abi` pins). Pass: `WitnessHash.t.sol` unchanged; gas snapshot committed.

### Arc 2 — L2 · PR `any-erc20-bridge/l2` · `/code-review medium` + `/security-review`
**P3 ✓ (2026-09-02)** — hub crate + register lib + keystones on `any-erc20-bridge/l2`; `compile.sh --check` (class-id parity, mutation-verified) + CI `hub-parity` job via the existing `setup-aztec@5.0.1` (no toolchain mini-project needed); TXE 6/6, keystone 10+2, core 298. See `lessons/phase-3.md`.

- **P3 — Hub + register lib + keystone additions.** Full API (D11, D21); `hub-token.ts`; `txe-server/` (D24); `compile.sh` + `run-txe-tests.sh` hub crate; artifacts committed path-scrubbed; keystone crate vectors + the `derive_token` library `#[test]` (D35); `artifacts.ts` +hub exports; `noir-artifact-classids.test.ts` +hub/+Token; **CI hub compile + parity job** (D33).
  **Gate**: fast · forge · halmos · keystone · txe (the P0 tests) · core · `contracts/bridge/aztec/scripts/compile.sh --check` (D39).
**P4 ✓ (2026-09-02)** — 64 named TXE tests (exact `should_fail_with` strings, all in the manifest), sole-consumer guard → hub (3 sites, 7 self-test regressions), `txe-ts-map.md`. See `lessons/phase-4.md`.

- **P4 — TXE suite + tripwires.** `register.nr` (happy paths per verdict; wrong metadata + retry; double register; foreign sender; **two distinct factory-authored leaves for one erc20 with different words → second fails on `token_of`**), `claims.nr`/`claims_private.nr`/`exits.nr` over TWO tokens (a message from portal A cannot mint token B; **a token-A authwit replayed against token B fails** (consumer = Token); mismatched `{token, portal}` exit fails; F-007 redirect rejection; 2-tx private first claim; **relayer-first: `register_token` by a stranger then the depositor's `claim_public` succeeds; a second `register_and_claim_public` for an already-registered token is rejected** (D29)), `guards.nr` (`_register`/`_assert_exits_open` only-self; unregistered token read reverts "Trying to read from uninitialized PublicImmutable" in both domains); `pause.nr` (guardian pauses exits: public + private exits revert with an exact string and no burn persists; claims unaffected while paused; non-guardian `set_exits_paused` rejected); `check-sole-consumer.sh` → hub (three named consume sites, D36); `txe-ts-map.md`; `_bridge-contracts.yml` sole path. Legacy crates stay until P8 (D19).
  **Gate**: fast · forge · halmos · keystone · txe (≥ 40 named tests, D20) · sole · core · `compile.sh --check`. Pass: exact-string `should_fail`s only.

### Arc 3 — bridge-core + conductors · PR `any-erc20-bridge/core` · `/code-review medium`
- **P5 — Schema, journal, flows, discovery, list, scripts.** `candidate-schema.ts` v2 (+ superRefine derivations + hookless pools), `journal.ts` schema 3 union + `backup.ts` + `promotion.ts`; `erc20.ts`, `token-list.ts` (streaming byte cap, `redirect: "error"`, zod, chainId filter, subset persistence, quota fallback), `route-discovery.ts` (D15) + `route-conformance.test.ts`, `quote.ts` batching, `gas-share.ts`, `flows.ts` (`runSend` covering (a)–(e) with the claim-time `token_for` branch + race retry (D29), post-receipt re-derive (D30), `minFuelOutput` floor + `fjRegister` (D31), any-token exits, exit pause preflight); `factory-registry.ts`; every manifest-reading script moved to v2 (Arc-3 list) or deleted. Tests: strict fixtures, list timeout/size/schema/TTL/poisoning/redirect, cross-token journal/backup rejection + missing-block fail-closed, route matrix + `no-route` vs `unavailable`, gas-share bounds.
  **Gate**: fast · core. Pass: `typecheck:all` green with zero legacy manifest readers left under `tsconfig.scripts.json`.
**P5 ✓ (2026-09-02)** — manifest v2 (strict, self-deriving, nullable bridge), journal schema 3 + backup validation, `runSend`/`claimViaHub`/exit preflight, factory-registry, erc20/token-list/route-discovery/gas-share/batched quotes; every operator script on v2 or deleted (`script-send.ts` shared bindings, `deploy-manifest.ts` generation journal, `verify-l1.ts` code-hash + cross-binding readbacks); `typecheck:all` + `lint` exit 0, bridge-core 327 tests. Deferred to P6: hub `exits_paused()` view, `PoolSetupHelper` extraction, the mainnet `bridge: null` stub (would break the v1 faucet reader — ships with P8). See `lessons/phase-5.md`.
- **P6 — Conductor + sandbox smoke.** `deploy-generation.ts` (journaled + idempotent per D2/D32: publish classes → predict factory → derive hub (`salt = Fr(factory)`) → deploy factory (nonce-asserted) → deploy hub → readbacks → `pre-create` (`createPortal`, then `register_token` only if `token_for == 0`) → TOKEN/WETH pool seed via `SeedTokenPool.s.sol` for fake USDC + fake USDT (D41) → `fjPerTx`/`fjRegister` worst-case calibration across the privacy × fee-mode matrix (A2) → candidate), `deploy-sandbox.ts` rewritten (factory + hub; `anvil_setCode` Multicall3 like Permit2), `--smoke` = flows (a)–(e) with (c) driven by an **explicit mock route** (the sandbox has `MockSwapTarget`, no V4) + relayer-first register + concurrent first-time deposits + a portal-exists-but-unregistered token + a `no-route` token + **a failed public registration then a corrected retry under each supported fee mode (sponsored, fjwc, private FPC)** (INF-3) + **real `register_*` publication** (blocking under D17), `verify-deployments.ts` v2, `verify-l1.ts` + `live-intent.ts` **full cross-binding readbacks with code hashes (D38)**; sandbox-derived `testnet-bridge.candidate.json` v2 fixture; mainnet manifest → `bridge: null` stub.
  **Gate**: fast · core · sandbox (**`AZTEC_HOME=~/.aztec/versions/5.2.0`**, the JS line the live networks run; a registry-selector canary asserts the hub's `publish_for_public_execution` selector resolves) · verify (`BRIDGE_MANIFEST=<sandbox candidate>`). Pass: all five flows settle with token-specific balances; crash-resume at every conductor step tested; hub-published tokens observed on the sandbox.
**P6 ✓ (2026-09-02)** — `generation.ts` (journalled, identity-stamped, nonce-pinned, adopts landed steps) + `deploy-generation.ts` testnet conductor + an OWNED local network with a 13-flow smoke through the production modules (4 full green runs), measured `fjPerTx`/`fjRegister`, `verify-deployments` v2, `verify-l1` strict + `live-intent` full-verifier promotion. Arc-3 loop: reviewer 17 findings (one blocker: forgeable router events) → codex 13 (three blockers: wallet chain id, partial promotion verify, split node identity) → 6 residuals → round 3 "no new material findings". See `lessons/phase-6.md`.

### Arc 4 — Wizard · PR `any-erc20-bridge/wizard` · `/code-review medium`
- **P7 — Foundation.** `testids.ts` entries first; `bridge-deployments.ts` v2 reader (`bridge: null` ⇒ placeholder); `capabilities.ts` per-token grant with selection epoch + returned-scope verification + resume gating (D22); `useTokenCatalog`/`useTokenSelection`/`useRouteQuote`/`useGasShare`/`useSend`/`useHubExit`; `useBridgeJournal` per-token `deploymentMatches` + **resume/import validation of the whole token block against `registrations[erc20]`** (D18/D30 — words, decimals, key/index, derived `l2Token`; mismatch = hard stop); `register` phase in `bridge-steps.ts`; `deposit-flow.ts` any-token (re-pin the characterization snapshot once).
  **Gate**: fast · `bun run test:faucet`. Pass: composables ≥ 10 cases each incl. the grant flow.
- **P8 — UI + smoke + retire legacy.** `WizardShell` (direction segment, D27), `StepStrip`, `TokenStep`/`TokenList`/`TokenTile`/`PasteAddress`, `AmountStep`/`ChoiceCards`/`GasBreakdown`, `ReviewStep`/`ReviewDetails` ("portal verified" = `predictPortal` vs `factory.portalOf`; burn-before-finish line on exits), `MainnetPlaceholderView`, stepper/receipt/journal cards per token, sprite + monogram, CSP origin, a11y (roving tablist, `aria-live`, focus order); `send-smoke.test.ts` (jsdom, mocks per `bridge-smoke.test.ts`, real journal engine, a fixture token list): list/paste/grant prompt at Sign & send (incl. a stale-selection completion discarded)/no-route/first-time note/portal-exists-unregistered/2-tx private auto-continue/gas-only/exit with L1+L2 pause preflight/mainnet placeholder. Then the **retire-legacy step** (D19 list) with the halmos table → `FormalRouterTest 8`, `FormalFactoryTest 2`, `FormalCloneTest 2`; 3 summaries.
  **Gate**: fast · faucet · audit · verify · forge · halmos · txe · sole · core · `compile.sh --check`. Pass: every interactive element has a testid; no `v-html`; no `logoURI` request; parity/drift/contrast tests green; both build targets pass — mainnet renders the placeholder only; `git grep -nE 'TokenMinterProxy|NuloTokenPortal|token_bridge\b' -- ':!implementations-plan' ':!audit' ':!*.md' ':!contracts/bridge/aztec/claim_secret' ':!contracts/bridge/aztec/keystone' ':!packages/bridge-core/src/claim-secret.ts'` is empty (the `DOM_SEP__TOKEN_BRIDGE_PRIVATE_CLAIM_SECRET` keystone identifier stays; docs move in P9).

### Arc 5 — Docs + testnet · PR `any-erc20-bridge/docs` · `/code-review low`
- **P9 — Docs.** `aztec-update/SKILL.md` Branch B → generation deploy runbook (predict/assert nonce, idempotent re-run, pre-create, pool seed per token, `fjPerTx` calibration, promote, canaries); READMEs ×4 (stale counts fixed; legacy references removed); `UPDATE.md` couplings (hub ↔ factory, tokenClassId ↔ artifact, hub → protocol `ContractInstanceRegistry` selector, sandbox = JS line, list origin ↔ CSP, txe-server lockfile); `CLAUDE.md` pointers; `.env.example` completed; `implementations-plan/index.md`.
  **Gate**: fast (pre-commit `check-no-brand.sh` covers path leaks) · reviewer read of Branch B against `deploy-generation.ts --help`.
- **P10 — Testnet deploy + sign-off.** Owner runs `live-intent.ts build` + `deploy-generation.ts` (secrets; a **dedicated recorded guardian EOA** for testnet); agent drives candidate smokes (`smoke-existing-testnet.ts` v2, `fuel-testnet.ts PRIVATE_RUNS=1` incl. `fjPerTx` calibration, fee-juice canary), a real-origin token-list canary asserting **origin/schema/filter/cache behavior** (not membership), promote, deploy `testnet.tools.nulo.sh`; owner live sign-off with the **wallet-seam checklist** (D34): register-and-claim public of a pasted token (one prompt at Sign & send), the 2-tx private first claim (auto-continue, two prompts), gas-only, an exit with the authwit prompt, and a paused-exit rejection with the testnet guardian.
  **Gate**: audit · verify on the promoted manifest · canaries · **owner sign-off** recorded in `lessons/phase-10.md`.

## Security & Adversarial Considerations

- **Threat model**: any L1/L2 user, any token author, the list origin, a compromised guardian key, a compromised **router owner** key (`setSwapTarget` rotation invalidates pending witnesses; `sweep` moves only stranded residue — no user funds at rest), a compromised RPC. Assets: per-token reserves (isolated per clone), L2 supplies, in-flight messages, browser state.
- **Poisoning**: impossible by construction — the hub consumes `register` only from the immutable factory, `mint_*` only from `portal_of[token]` written once from that message; portal = f(factory, impl, erc20); L2 token = f(hub, class, erc20, attested words). A front-run `createPortal` yields the same portal and L2 token; it can only freeze the first-observed metadata (cosmetic). The D1 tuple test (P1) pins the whole chain end to end.
- **Metadata**: attacker-controlled strings → gas-capped `staticcall`, bounded returndata copy, 31-byte ASCII sanitization (D8); rendered as text nodes only (no `v-html`); same-symbol tokens distinguished by address; sprite keyed by address (D25). `decimals` missing or > 255 ⇒ no portal; any `u8` accepted, all math in base units (D13).
- **Token behaviors**: FoT rejected on deposit (D5) and the reserve is protected on withdraw by exact portal-debit (D37) — a tax a token enables *later* is paid by the withdrawer, never by the reserve; rebasing unsupported — positive rebases accrue to the portal, negative under-collateralize; documented and shown as a caution for pasted tokens; ERC-777 hooks meet `ReentrancyGuardTransient` + checks-effects; `amount > u128` rejected (D13).
- **Guardians (D3 + D40, owner-confirmed)**: L1 guardian = pause deposits/withdraws, **global**; L2 guardian = pause exits only. A stolen L1 key can freeze every exit indefinitely (ransom-grief, no theft); exits **burn before FINISH**, so an L1 pause raised between burn and withdraw leaves the L2 side burned until unpause — pausing L2 exits at the same time stops new burns, and the app preflights both bits, states the order in the review, and never discards the record; a hub bug can be frozen but not recovered — the hub is immutable and ownerless, so recovery = new generation with reserves stranded behind the pause. `Ownable2Step` + disabled renounce; testnet guardian = a dedicated recorded EOA; a multisig before mainnet (recorded for `/harden security`). The pause-after-burn window (for exits already burned when the L1 pause lands) and the global blast radius are intrinsic to D3 and **owner-accepted**.
- **Hub systemic risk**: one L2 contract serves every token and every L2 token shares one class — a hub auth/portal-selection/mint bug or a Token-class bug is **generation-wide**; per-clone L1 isolation contains only *malicious ERC-20 behavior* to that token's reserve. Mitigations: minimal surface, F-007 body verbatim, sole-consumer tripwire with named sites, cross-token TXE tests (authwit replay, mismatched exits, second-leaf registration). **Mainnet decision recorded for `/harden security` (blocking)**: the recovery/migration story for a broken generation (the L2 exit-only pause is now in, D40).
- **Router**: portal derived on-chain per entrypoint (D23) before any pull; witness binds portal/token/amounts/recipients/route/swapTarget; fuel-only requires zero recipient + zero portal; hookless routes enforced on-chain (`_validateRoute`) and asserted off-chain before signing; signed `minFuelOutput` is the floor. **Accepted risk — a compromised RPC**: quotes and slippage are relative to the wallet's own RPC, so a false quote can authorize a poor floor bounded only by the 50 % slice cap; there is no independent price source inside the CSP. This is the same trust the user already places in that RPC for balances; the review's Details line shows the effective rate. Live cross-binding readbacks (D38) close the other half — a router bound to a foreign factory.
- **Token list**: single pinned origin, HTTPS, CORS verified, `redirect: "error"`, streamed byte cap before parsing, zod at the boundary (count/length caps, chainId filter), no `logoURI` ever fetched or rendered, only the validated subset persisted, cache re-validated on read, fails closed to cache → manifest tokens. A poisoned list can promote a counterfeit address under a familiar name — list entries are labeled untrusted, the address is always shown in Details, and identity (portal, L2 token) never comes from the list. CSP `connect-src` widened by exactly one host, `img-src` unchanged.
- **Journal/backup**: union schema (D18) — a `token`/`token+gas` record without a token block is invalid; `deploymentMatches` re-derives the portal; backups reject header/seal mismatches including `token`.
- **Wallet grants (D22)**: exact-address scopes only; the per-token re-grant is a visible wallet prompt at Sign & send; no wildcard `burn_*`. The tools app touches no wallet code (D34).
- **Genesis (D2)**: no roles; a nonce race aborts before the hub exists; a front-run identical hub/class is idempotent success after readback; `factory.L2_HUB()`/`hub.l1_factory` cross-checked before pre-create.
- **Supply chain / least privilege**: viem only (already present); Multicall3 canonical address from `viem/chains`; OZ pinned by commit in CI (`cab19933` = **5.7.0**, has `ReentrancyGuardTransient`); TXE server deps under a committed lockfile (D24); CI compiles the hub and asserts parity with the committed artifact (D33); sprite SVGs committed + svgo-stripped; root lockfile frozen; CI keeps `permissions: contents: read`; fork and TXE jobs stay secret-free.

## Assumptions

**Facts** (verified): OZ **5.7.0** at the pinned commit `cab19933` (`lib/openzeppelin-contracts/package.json`; `Clones.sol:197-293`; `ReentrancyGuardTransient.sol` present); `Inbox.sendL2Message` binds `msg.sender` as the L1 actor (`Inbox.sol:107`); router A-1 + `0 < fuel < total` (`SwapBridgeRouter.sol:154-158`), router owner `setSwapTarget`/`sweep` (`:143,:290`); portal preimages (`upstream/NuloTokenPortal.sol:104,136,176-180`); v5.0.1 `publish_contract_instance_for_public_execution` private-only, `deployer == this`, reads the instance from the PXE oracle (`publish_contract_instance.nr:13-19`); initializer guard (`initialization_utils.nr:152-163`); `PublicImmutable` read asserts initialization in both domains (`public_immutable.nr:184,329`); a **private historical** read cannot see a same-tx initialization (`private_context.nr:82-86`) — ordered public reads can; enqueued public calls execute in side-effect-counter order (`sort_by_counter.nr`; `tail_to_public_output_composer.nr`); private nullifiers with counter ≥ `min_revertible_side_effect_counter` are revertible (`split_to_public.nr:59`); TXE `call_private` is one tx + one block per call and forbids nested contract calls (`test_environment.nr:1060-1070`, `private_context.nr:749-751`); `txe_oracles::deploy` takes a `deployer`; `Map` accepts `PublicImmutable` values; Token `constructor_with_minter(str<31>, str<31>, u8, minter, auth)`, `burn_*` authwit-gated with `#[authorize_once]`; `str<31>` = 31 fields; aztec.js encodes strings via `charCodeAt`; `FieldCompressedString::{from_field, from_string, to_bytes}` exist at 5.0.1 and `[u8; N]::as_str_unchecked` is a stdlib builtin (`noir_stdlib/src/array/mod.nr:303`; the 5.0.1 toolchain is nargo 1.0.0-beta.22); `hash_args` is an unbounded Poseidon2 (`aztec-nr/aztec/src/hash.nr:56`); the aztec-standards `Token` asserts nothing about its deployer; the authwit consumer is the Token (`auth.nr:270-284`); wallet grants re-prompt and REPLACE on a new address/function, scope checkers union downstream (`dispatcher.ts:304-327,384-415`, `dispatcher.test.ts:1488`); `Inbox` hashes the leaf index into the message (`Inbox.sol:102,114`) so the register leaf index is a required recovery datum; today's `minFuelOutput` signs `quote × (1 − s)` (`deposit-flow.ts:738,751`); `tokens.uniswap.org` → 200 `application/json`, CORS `*`, `cache-control: no-store`, ~670 KB, 1,709 tokens (2 on Sepolia, 401 on mainnet); today's app uses only `BatchCall(wallet, [])` carriers (`fuelClaim.ts:123,170,228`); wallet grants are exact-address, no wildcards (`capabilities.ts:33`); wildcard authwits route to silent execution (`method-scope-checkers.ts:273-278`); `run-txe-tests.sh` hardcodes `token_bridge` (`:18`) and does a lockless `bun add` (`:68-70`); TXE not in CI; CI `permissions: contents: read`; manifest/journal shapes (`research/bridge-core.md`); `deploymentMatches` + recovery binding (`useBridgeJournal.ts:317-337,379`); **mainnet renders all three tabs today** (only the default changes, `App.vue:20-25,39-49`); CSP plugin + per-target `cspConnectSrc`; halmos counts asserted in CI; `tsconfig.scripts.json` includes `scripts/**`.
**Inferences** (labeled): I1 — TXE's `deploy` cheatcode bypasses the registry, so Verdict B is the likelier spike outcome; under it TXE proves neither publication nor revert semantics (the sandbox does — D17). I2 — testnet exercises the real list fetch/validate/cache path with whatever the origin returns for 11155111 (2 entries today); canaries assert behavior, not membership; the mainnet catalog is unexercised until the mainnet arc. I4 — (superseded by D36) wrong-metadata registration has no side effects because the consume happens in private before any publish; the TXE retry test (P0/P4) and the sandbox retry-under-each-fee-mode test (P6) stay as regression pins. I5 — inline `createPortal` costs ~150–200k gas once per token — **measured in P2: 322,900 on live Sepolia** (the 4-slot `Registration` record + the real Inbox leaf were under-counted); accepted as a once-per-token cost. I6 — halmos can model the `CREATE2`/`EXTCODECOPY` paths in `FormalClone` / `check_bridge_rejectsForeignPortal`; if not, those checks concretize the clone deployment and prove only the supported logic — the per-proof mutation canaries (D39) make a vacuous proof visible either way.
**Asks** (approval gate): A1 ✓ (2026-09-02) — real Sepolia WETH (1-hop) + fake USDC/USDT/cbBTC/WBTC; TOKEN/WETH pools seeded for fake USDC **and fake USDT** (D41); cbBTC/WBTC show `no-route` on testnet. A2 ✓ — `txTarget = 20` (D42); `fjPerTx` and `fjRegister` are **measured worst-case gas-unit budgets with a 20 % margin** across the privacy × fee-mode matrix (public combined registration; private registration + claim; sponsored / fjwc / private-FPC fees), written to the manifest per generation (mainnet arc scales by `getCurrentBaseFees()`). A3 ✓ — list TTL 24 h, caps 2 MB / 10k tokens / 64-char names. A4 ✓ — mainnet placeholder copy: "Bridging is being upgraded — back with the next mainnet generation" + links (`nulo.sh`, extension, docs); no wallet connect, no node assert. A5 ✓ (all four) — brief-text deltas: (a) D6 no factory create-and-deposit helpers; (b) D8 event carries `bytes32` words + register key/index; (c) D9 plain-address salt; (d) portal bodies content-hash-identical, not byte-identical. A6 ✓ — one wallet prompt per new token, raised at "Sign & send" for deposits / at selection for exits (D22). A8 ✓ — exit direction as a segment above Step 1 (D27). A9 ✗ — no extension test; the tools app is a standard dApp (D34). Owner Q1 → D11 option (a); owner Q2 → D40. **No open Asks remain.**

## Delivery

Stacked via `gh stack` off `dev`: `any-erc20-bridge/l1` (Arc 1) → `/l2` (Arc 2) → `/core` (Arc 3) → `/wizard` (Arc 4) → `/docs` (Arc 5). Arc 0 lives on `any-erc20-bridge/spike` outside the stack and never merges; its keystone tests are cherry-picked into Arcs 1–2 (D12). Each PR independently revertable in reverse order; Arcs 1 and 2 are independent of each other; every arc keeps `typecheck:all` green (D19). `/code-review medium` on Arcs 1–4, `low` on Arc 5; Arcs 1–2 additionally get `/security-review` before their codex loops (immutable surfaces). PRs are opened only in the Delivery step after every loop converges. `gh stack merge` is the owner's call.

## Post-implementation

Per arc, at its boundary and before `gh stack add` for the next arc: (1) `/code-review <level per Delivery> --fix` on the arc's diff → skim → commit separately from implementation commits; (2) codex `xhigh` audit of the arc diff + the code-review commit summary + this plan + the ledger + the arc map ("arc N of 5 PR arcs; later arcs build X on it") + the adversarial/security ask + the two verbatim rules below; (3) triage (verify codex's factual claims against the repo first), apply accepted fixes, commit, log the round in `lessons/phase-N.md`, RESUME the same codex session with the fix diff, repeat until a round yields no new material findings — hard stop and surface after 3 rounds. After Arc 5: one FRESH codex session over the net diff from `8d6cca3d` asking for cross-arc issues (seams, duplication, plan drift), same loop. Then Delivery: `gh stack sync` if `dev` moved, `gh stack submit --auto`, `gh pr edit` bodies (≤ 93-char conventional titles), `gh pr checks --watch`. Recorded, not scheduled: `/harden security` before any mainnet deploy.

**No-over-engineering rule** (verbatim in every post-impl codex prompt): *"Report bugs and small, targeted improvements only. Do not propose speculative abstractions, extra configuration surface, new layers, or rewrites — the smallest change that fixes each real problem. If code works and is clear, leave it alone."*

**Comment-quality rule** (verbatim, same prompts): *"Audit the comments for value per character. Flag any comment that narrates what the code visibly does, restates its line, references implementation plans / phases / reviews, or spends a paragraph where a sentence works — and flag places where a non-obvious invariant or constraint deserves a comment it doesn't have. Comments are permanent context every future reader, human or LLM, pays to re-read: they must be few, dense, and exact."*

## Audit verdicts

| Round | Codex | Fable |
|---|---|---|
| Contradiction check | done — 9 + 4, all applied — [audit-codex-contradiction.md](audit-codex-contradiction.md) | done — 12 + 4, all applied — [audit-fable.md](audit-fable.md) |
| Round 1 | **NOT-READY** (blocking SEC-1, SEC-2, SEC-5, SEC-8, INF-3, ASK-1, IMP-1, IMP-3) — all eight resolved (D11, D18, D21–D24 added; P1/P3/P4 gates widened) — [audit-codex-r1.md](audit-codex-r1.md) | **READY-WITH-FIXES** (S1, C1, F2, C2, C3, C4, C5, F1, S2, O1 + text) — all applied (D11, D22, D25–D27; A7) — [audit-fable.md](audit-fable.md) |
| Round 2 (split) | **NOT-READY** (M1–M4, B-ASK-1) — all resolved (D28–D33; A1 at the gate); retracted SEC-1/SEC-5 severities, confirmed D11 ordering, closed D21 + witness disputes — [audit-codex-r2.md](audit-codex-r2.md) | **READY-WITH-FIXES** (R2-S1…R2-O2) — all applied (D28–D35) — [audit-fable.md](audit-fable.md) |
| Final fresh-context codex | **`conditional approve`** (new session `codex-rjmE35L7`): conditions (1) exact-out withdrawals → D37 (portal-debit half; recipient-credit rejected — it would brick withdrawals); (2) live cross-binding + honest RPC classification → D38 + § Router; (3) registration key + authoritative resume validation → D28/D18 amended; (4) rollback + compile/parity gates blocking → D36 removes the rollback dependency, D39 makes parity a local + CI gate; (5) A1/A2/A5/A6/A8 individually, A7 removed → § Asks. — [audit-codex-final.md](audit-codex-final.md) | — |

## Seeds (FINAL — approved scope, no conditions)

ELI5: Artifact `https://claude.ai/code/artifact/d422d96d-db9e-4b9e-853d-42abd4343bcb` · source `implementations-plan/any-erc20-bridge/eli5.html` (redeploy the same path to update).

```
/goal All eleven phases (P0–P10, the phase headers in implementations-plan/any-erc20-bridge/plan.md) marked ✓ in the file, each ✓ backed by that phase's Validation gate reported passing in the transcript; for each phase `LESSONS_FILE=implementations-plan/any-erc20-bridge/lessons/phase-N.md` printed; `/code-review` complete per PR arc at the level in Delivery with fixes committed; the codex fix loop converged for every PR arc AND the final cross-arc pass, each evidenced by a resumed codex pass reporting no new material findings quoted in the transcript; the five stacked PRs exist on GitHub, created only after all loops converged (`gh stack view` in the transcript); `bun run test:all` and `bun run lint` both exit 0 in the transcript; owner testnet sign-off recorded in lessons/phase-10.md.
```

```
/loop 15m Drive implementations-plan/any-erc20-bridge forward. Never idle waiting for my input. Each firing: (1) reality check — read plan.md + lessons/ (authoritative), rebuild the task list from plan.md if empty, `git status`, `git log --oneline -5`; PR exists? `gh stack view`; else `gh run list --branch $(git branch --show-current) --limit 1 --json status,databaseId`. (2) CI wait is fine — `gh run watch <id>` up to 10 min; use the wait to prep the next phase. (3) No task in hand? pick the next pending step in plan.md; after each meaningful edit run `bun run lint` + the touched package's tests; commit → push (Arc 0 on `any-erc20-bridge/spike`; Arcs 1–5 via `gh stack push`, `gh stack sync` if dev or a lower arc moved). (4) Decision I'd normally bring to you? `/codex xhigh` with full context, converge, act, log in lessons/phase-N.md — hard limits stay hard (no merge to dev/main, no deploy, no secrets, no scope beyond plan.md; any deploy or secret-touching step = surface and hold). (5) Same step failed 5×? stop, reassess with codex. (6) Phase green = THAT phase's Validation gate in plan.md passes verbatim → paste the result, mark ✓, write lessons, print `LESSONS_FILE=implementations-plan/any-erc20-bridge/lessons/phase-N.md`. Arc boundary (per Delivery)? run the arc's quality loop FIRST (`/code-review <level> --fix` → separate commit → `/security-review` on Arcs 1–2 → codex loop with the arc map + the plan's no-over-engineering + comment-quality rules until clean) THEN `gh stack add <next-arc-branch>`. (7) All phases ✓? final fresh-codex cross-arc pass → `gh stack submit --auto` + `gh pr edit` bodies → `gh pr checks --watch` → wrap-up report (what shipped, every contentious decision with ELI5 context, open items) → surface and stop. Keep the native task list current; plan.md stays the source of truth.
```
