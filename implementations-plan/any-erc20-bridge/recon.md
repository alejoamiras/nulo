# Recon — any-erc20-bridge

Base: worktree `worktree-any-erc20-bridge` from `origin/dev` @ `8d6cca3d`. Module-level research (the mega-deep research phase) lives in `research/{l1-contracts,l2-contracts,bridge-core,tools-frontend}.md`; this file is the reuse map + the feasibility verdicts the plan and every audit build on.

## Reuse map

| # | Capability needed | Existing code (or absence + search trail) | Verdict |
|---|---|---|---|
| 1 | L1 portal factory: deterministic minimal-proxy clones with immutable args | `contracts/bridge/evm/lib/openzeppelin-contracts` = **OZ 5.6.1** (`package.json:4`); `contracts/proxy/Clones.sol` has `cloneDeterministicWithImmutableArgs` `:197`, `predictDeterministicAddressWithImmutableArgs` `:229,242`, `fetchCloneArgs` `:261`; remap `@oz/` wired in `foundry.toml`. CI pins OZ to commit `cab19933` — verify that commit ≥ 5.2 (it is, 5.6.1). | **reuse-as-is** (`Clones`) |
| 2 | Portal implementation (deposit/withdraw bodies, content hashes) | `contracts/bridge/evm/upstream/NuloTokenPortal.sol` — deposit/withdraw bodies byte-identical to canonical; init-once + deployer-pinned `initialize`. Content-hash keystones `test/ContentHash.t.sol`, `test/PortalRoundtripFuzz.t.sol` (fakes `CapturingInbox/Outbox/FakeRollup/FakeRegistry` `:85-151`). | **adapt**: new `TokenPortalImpl` = same deposit/withdraw bodies, storage → implementation immutables + clone args (`fetchCloneArgs`), NO `initialize`; keystones must pass unchanged |
| 3 | Router portal binding (A-1) + fuel-only path | `src/SwapBridgeRouter.sol` takes `tokenPortal` as a param (`:84,:99`), rejects `fuelAmount == totalAmount` (`:154-158`); witness 12 fields pinned 3-way (`src/SwapBridgeRouter.sol:52-56` ↔ `packages/bridge-core/src/l1.ts:11-14` ↔ `test/WitnessHash.t.sol:56-57`); halmos `test/FormalRouter.t.sol` (4 checks), invariants `test/SwapBridgeRouterInvariant.t.sol`. | **adapt**: derive expected portal from `bridgeToken` via `factory.portalFor()` / `predictDeterministicAddress…`, allow `fuelAmount == totalAmount` (skip token leg); keep the 12-field witness; extend halmos + invariants |
| 4 | L2 hub: consume L1→L2 with app-chosen sender, send L2→L1 to app-chosen recipient | `contracts/bridge/aztec/token_bridge/src/main.nr:94-158` already passes `config.portal` explicitly to `consume_l1_to_l2_message` / `message_portal`; v5.0.1 `PrivateContext/PublicContext::consume_l1_to_l2_message(content, secret, sender, leaf_index)` and `message_portal(recipient, content)` are plain params. | **adapt**: `TokenBridgeHub` generalizes `token_bridge` with a per-token `portal_of` map; claim/exit bodies + F-007 `derive_claim_secret` carried over verbatim |
| 5 | Per-token L2 Token deployed by the hub | Wonderland `vault_deployer` (aztec-standards sibling clone, `src/vault_deployer/src/main.nr:111-181`): `ContractInstance{…deployer: self.address}.to_address()` + `publish_contract_instance_for_public_execution` (private-only, asserts `deployer == this`) + enqueued public initializer (`deployer == msg_sender` guard). **Present at v5.0.1** (`~/nargo/…/v5.0.1/noir-projects/aztec-nr/aztec/src/publish_contract_instance.nr:13`). `vault_deployer` has **no TXE tests** — the pattern is unproven in TXE. | **adapt** (port the derive/publish/enqueue triple into the hub); **spike first** (TXE proof of in-contract deploy is Phase 1's first gate) |
| 6 | Hub derives `portal(erc20)` = CREATE2 in the AVM | No CREATE2/EthAddress-from-keccak precedent in aztec-nr, noir-contracts, or nulo (`token_portal_content_hash_lib` uses keccak only at comptime). Building blocks at v5.0.1: `keccak256::keccak256(bytes, len)` → Brillig `Keccakf1600` → AVM `KECCAKF1600` (`avm-transpiler/src/opcodes.rs:79`); `field_from_bytes<N<32>` (`crates/types/src/utils/field.nr:1`); `EthAddress::from_field` (`address/eth_address.nr:29`). Private-circuit keccak cost unknown (stdlib source not on disk) — design uses keccak in PUBLIC only. | **build new** (`create2_eth_address(deployer, salt, init_code_hash)` in the hub, public-only) + a Solidity/Noir/TS **address keystone** |
| 7 | Per-token portal readable in PRIVATE at constant cost | `Map<AztecAddress, PublicImmutable<EthAddress>>` composes (`state_vars/map.nr` `Map::at` → `V::new(slot)`; `public_immutable.nr:329-333` private `read` = nullifier-exists + historical read, ~4k gates, hard revert "Trying to read from uninitialized PublicImmutable"). Same-tx private read of a value initialized in that tx is impossible (anchor header predates the tx). | **reuse-as-is** (`Map<…, PublicImmutable<EthAddress>>`); accept **private first claim = 2 txs** |
| 8 | L1-attested token metadata on L2 | `token_portal_content_hash_lib` (sha256-to-field content hashes, comptime selectors) + `contracts/bridge/aztec/keystone` + `packages/bridge-core/src/content-hash.ts` + `test/ContentHash.t.sol`. aztec-standards `Token::constructor_with_minter(name: str<31>, symbol: str<31>, decimals: u8, minter, auth_contract)`; `burn_*` gated by authwit/auth hook (not minter). | **adapt**: add a 4th content hash `register(address,bytes32,bytes32,uint8)` to lib + keystone + TS + Solidity |
| 9 | Aztec instance derivation in TS with a NON-zero deployer | `packages/bridge-core/scripts/script-l2.ts:23-34 universalDeployInstance` and `apps/faucet/src/contracts/bridge-deployments.ts:125-157` hardcode `deployer: AztecAddress.ZERO`; Token artifact import `@aztec-foundation/aztec-standards/artifacts/src/artifacts/Token.js` (5 sites). | **adapt**: one `deriveHubTokenInstance(hub, erc20, meta)` in bridge-core reused by faucet + conductors + smokes; keystone-pinned against the hub's in-circuit derivation |
| 10 | CREATE2 prediction in TS | None (searched `getContractAddress|getCreate2Address|CREATE2|create2` in `packages/*`, `apps/*`; `keccak256` only for bytecode pins in `scripts/portal-artifact.ts` and the EIP-712 typehash in `src/l1.ts:14`). viem ships `getCreate2Address`. | **build new** (`predictPortal(factory, erc20, implementation, args)` mirroring OZ's immutable-args clone initcode) + keystone vs forge |
| 11 | Deploy conductor + journal + candidate manifest | `scripts/deploy-bridge-testnet.ts` (journaled resume, `journaledEvmDeploy`, `writeCandidate`, `buildFuelCarry`), `deploy-manifest.ts`, `candidate-schema.ts` (`.strict()` zod), `live-intent.ts promote`, `apps/faucet/scripts/verify-deployments.ts`, `deploy-sandbox.ts --smoke`. | **adapt**: new generation shape (`factory/implementation/hub/tokens[]`), conductor collapses to factory+hub+pre-create; sandbox smoke generalized to any-token |
| 12 | Runtime token list fetch + TTL cache + schema | No fetch/TTL-cache helper (`fetch(` only in `apps/faucet/src/main.ts:31`; `localStorage` only the journal). zod primitives `evmAddress/decimalString` + `.strict()` idiom in `packages/bridge-core/src/candidate-schema.ts:10-13`. Design precedent (extension-only): price feed TTL/sanity-band/cache-or-nothing (`ARCHITECTURE.md` §4). | **build new** (`token-list.ts` in bridge-core: zod schema for the Uniswap list shape, TTL cache, fail-closed to bundled fallback) |
| 13 | CSP for the list origin + logos | `apps/faucet/vite.config.ts:100-131 headersPlugin` — `connect-src ${target.cspConnectSrc}` parameterized (`network-targets.ts:38,46,56`), `img-src 'self' data:` hardcoded. No static `public/_headers`. `WalletPickerModal.vue:30-54` icon protocol allowlist + `@error` fallback (CSP-dead for https today). | **adapt**: add `https://tokens.uniswap.org` to `cspConnectSrc` per target; logos stay `'self'` (bundled sprite) — no `img-src` change |
| 14 | ERC-20 metadata + balances for N tokens | `scripts/script-l1.ts:14 ERC20_MIN_ABI`, `apps/faucet/src/composables/useL1Usdc.ts:10 ERC20_ABI` (duplicated), `src/reuse-token.ts:21-38 Erc20Metadata + assertReusedTokenMetadata`. **No multicall anywhere.** `useL1Wallet.ts:29-38` publicClient tunnels through `window.ethereum` (reads only while connected). | **adapt** (one shared ERC-20 ABI + `Erc20Metadata`); **build new** (viem `multicall` via Multicall3 for balances/metadata; reads stay connect-gated by design) |
| 15 | Wizard shell / step strip / card choice / list | `apps/extension/src/onboarding/components/StepIndicator.vue` (5-step brutalist strip, extension-only → copy the pattern, not the import); `apps/faucet/src/components/BridgeForm.vue:447-458` `.mode` cards (`aria-pressed`) — upgrade to CLAUDE.md's roving tablist; `WalletPickerModal.vue:129-161` rows (icon + name + action, `aria-live`); `AccountSwitcher.vue:92-105` keyboard menu. `BridgeStepper`/`BridgePhaseRail` are the in-flight rail (kept, gains an optional REGISTER phase). | **adapt** (new `WizardShell`, `StepStrip`, `ChoiceCards`, `TokenList` built from these shapes) |
| 16 | Token tile: logos + monogram fallback | No token logo assets (`apps/faucet/public` = favicon + manifests); `packages/design` `Icon.vue` is a closed JSON path dictionary; no identicon/hue helper anywhere (`getColorFromAddress` retired; `apps/extension/src/utils/string.ts getInitials` is the monogram precedent, extension-only). | **build new** (`TokenTile.vue` + bundled SVG sprite for the curated set + monogram/hue fallback) |
| 17 | Journal per-token binding | `packages/bridge-core/src/journal.ts:35-57 JournalBase{chainId, portal, bridge}` + `assetKind`; consumers: `apps/faucet/src/composables/useBridgeJournal.ts:317-337 deploymentMatches` (binary fee-juice vs the ONE bridge), `:379` recovery-key binding `{chainId, portal, bridge, secretHashHex}` (crypto, not display), `packages/bridge-core/src/backup.ts:16-27,74-89,208-217`, `useWithdraw.ts:276-279` hardcoded `portal/bridge`, `src/lib/asset-label.ts`. | **adapt** — a real design decision, not a field add: binding becomes `{chainId, hub, portal(erc20)}` + `token` block; recovery-key message includes the portal (already per-token under the factory) — audits must confirm the domain change is safe for existing sealed records (none exist in production; legacy retired) |
| 18 | Quote / route discovery for arbitrary tokens | `packages/bridge-core/src/route.ts:141 buildFuelRoute` (fixed 2-hop), `src/quote.ts` (chained `quoteExactInputSingle`, `QuoteUnavailableError`), `scripts/discover-mainnet-fuel.ts` (tier sweep via `extsload` slot0 + dust quotes). | **adapt**: `discoverFuelRoute(token)` = candidate shapes × tiers with a typed `no-route` outcome; `route-conformance.test.ts` extended |
| 19 | Test harness | forge fakes/mocks (§2,§3), `BlackhatAudit.t.sol` PoC shape, halmos 0.3.3 (`halmos.version`, `Formal{Router,Portal}.t.sol`), TXE harness `token_bridge/src/test/utils.nr` + `run-txe-tests.sh`, keystone crate, TS keystones, faucet smoke mocks (`tests/e2e/{bridge,faucet,fuel}-smoke.test.ts`), `deploy-sandbox.ts --smoke`. | **reuse-as-is** patterns; **adapt** for factory/clone/hub |
| 20 | Deploy documentation | `.claude/skills/aztec-update/SKILL.md` Branch B (the load-bearing single-token redeploy runbook), READMEs (`apps/faucet`, `packages/bridge-core`, `contracts/bridge/evm` incl. the A-1 "hard-blockers" section, `contracts/bridge/aztec`), `UPDATE.md` couplings. `ARCHITECTURE.md` + CLAUDE.md release runbook do NOT cover bridge deploys. | **adapt** all five; A-1 section becomes the factory threat model |

## Feasibility verdicts (v5.0.1 Noir line — the repo's deliberately held line; `implementations-plan/aztec-5.2.0-js-line/`)

| Question | Verdict | Evidence |
|---|---|---|
| keccak256 in a public function | **yes** — native `KECCAKF1600` AVM opcode | `v5.0.1/avm-transpiler/src/opcodes.rs:79` |
| keccak256 in a private function | usable, cost unknown — **design avoids it** (portal read from `PublicImmutable` in private) | no stdlib source on disk |
| `Map<AztecAddress, PublicImmutable<EthAddress>>` | **yes** | `state_vars/map.nr` generic `V: StateVariable`; `public_immutable.nr:329-333` |
| In-contract deploy from the hub | **yes, private-only** (`publish_contract_instance_for_public_execution(&mut PrivateContext, target)`, `deployer == this` or universal); no public publish path at 5.0.1/5.2.0 | `publish_contract_instance.nr:13-19`; registry has a single private `publish_for_public_execution` |
| Hub enqueues `Token.constructor_with_minter` | **yes** — `msg_sender == hub == deployer` satisfies `assert_initialization_matches_address_preimage_public` | `initialization_utils.nr:152-163` |
| Private mint in the same tx as the token ctor | **no** (historical read needs a mined block) → private first claim = 2 txs | `private_context.nr:82-86` |
| Token API | `constructor_with_minter(name: str<31>, symbol: str<31>, decimals: u8, minter, auth_contract)`; `mint_*` check `minter`; `burn_*` need an authwit to the hub | aztec-standards `token_contract/src/main.nr:97-112,419-431,475-543` |
| TXE can drive the register/deploy flow | **unproven** — `vault_deployer` has no TXE tests; TXE `deploy` is a cheatcode, but the hub's `publish_contract_instance_for_public_execution` call into the canonical registry has never been exercised in TXE here | risk #1 below |
| `@aztec-foundation/aztec-standards@5.2.0` | exists on npm now, but the repo holds 5.0.1 deliberately; this plan **stays on 5.0.1** (everything needed is present; a Noir-line bump is the `aztec-update` skill's job, not this plan's) | `bun pm view` |

## Collision / dedup risks

- Two ERC-20 ABIs (`script-l1.ts`, `useL1Usdc.ts`) → unify in bridge-core before adding a third.
- Address derivation lives in 3 places (`script-l2.ts`, `bridge-deployments.ts`, `deploy-sandbox.ts`) → ONE `derive*` module in bridge-core, keystone-pinned.
- Journal `assetKind` binary is read in ≥7 places (`deploymentMatches`, recovery-key binding, backup validation, `asset-label`, `BridgeStepper`, `BridgeJournalCard`, `BridgeForm`) — a per-token model must replace the binary in all of them, not sit beside it.
- `check-sole-consumer.sh` and `noir-artifact-classids.test.ts` are pointed at `token_bridge` — re-point, don't add a second tripwire.
- `useDeposit.characterization.test.ts` pins `deposit-flow.ts` verbatim — the any-token refactor will churn the snapshot deliberately; re-pin once, not per phase.

## Risks the plan must sequence around

1. **In-contract deploy unproven in TXE** → Phase 1 opens with a spike: minimal hub `register_token` + TXE test that publishes a Token and mints on it. If TXE can't, fall back to SDK-side deploy of the hub-derived instance (same address; hub still verifies by derivation) — the design tolerates both.
2. **CREATE2 keystone**: Solidity (`Clones.predictDeterministicAddressWithImmutableArgs`) ↔ TS (viem) ↔ Noir (hub) must agree on the exact clone initcode bytes; a mismatch strands deposits. Fixed-vector keystone across all three toolchains from day one.
3. **Register content hash** joins the 3 existing keystone vectors (Solidity/Noir/TS) — 4th vector, same tests.
4. **Toolchain**: hub compiles with the 5.0.1 nargo via `compile.sh`; TXE server `@aztec/txe@5.0.1`. No bump.
5. **Tooling note for lessons**: `EnterWorktree` mid-run broke the shell of already-running subagents (both recon agents lost Bash); spawn agents AFTER homing.
