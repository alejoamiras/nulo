**Verdict**: ship-it

**Sign-off on items 1-4**

1. `confirmed`  
If the mutex is acquired before [executeAztecSendTx](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/service.ts:1695) and [executeNoFromSendTx](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/service.ts:1831), and released only after submit, it closes both holes. Everything before that boundary is non-PXE/journal-only or pure parsing: [processAztecJsPayload](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/operation-planner.ts:152), queued-record creation [queued-journal.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/wallet-sdk/queued-journal.ts:144), silent fast-forward [dapp-interaction/service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/dapp-interaction/service.ts:295). The first PXE-touching work on the standard path is authwit discovery/build/simulate [execution/service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/service.ts:1754), [authwit-discoverer.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/authwit-discoverer.ts:71); on NO_FROM it is build/register/simulate [execution/service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/service.ts:1867), [tx-request-builder.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/tx-request-builder.ts:395). Key it by `(profileId, chainId)`, matching PXE’s own per-chain guard [pxe/service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/aztec-runtime/src/pxe/service.ts:105), not per-account.

2. `confirmed`  
The pre-acquire controller is the minimal correct fix. For the normal queued path, `queuedJournalId` is the actual record id created in [queued-journal.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/wallet-sdk/queued-journal.ts:144), and the claim path keeps using that same id in [claim-helper.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/claim-helper.ts:141), so `cancelJob(id)` still finds the same controller in [execution/service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/service.ts:799).  
One edge to implement explicitly: the helper still has a `record not found -> createFreshRecord()` fallback [claim-helper.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/execution/claim-helper.ts:80). If that path survives, migrate or delete the pre-registered controller entry, otherwise the stale `queuedJournalId` key leaks and the fresh id will not have the waiter controller. That is an edge cleanup, not a reason for a broader cancel refactor.

3. `confirmed`  
Heartbeat-over-flag is the right choice with boot sweep staying unconditional. The restart semantics are clean: a fresh SW has lost the in-memory request/controller state, so failing every non-terminal row at boot in [reaper.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/operation-journal/reaper.ts:121) is correct.  
The read/write race is acceptable if heartbeat cadence is comfortably below the grace window; `updatedAt` is just a liveness signal, and a periodic read in [reaper.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/operation-journal/reaper.ts:168) seeing a slightly older timestamp is harmless if the delta is far from the threshold. One implementation constraint: heartbeat must cover waiters regardless of whether they are still `queued` or already `pending`, because silent sendTx still fast-forwards to `pending` before execution in [dapp-interaction/service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/dapp-interaction/service.ts:295). If you only heartbeat `queued`, silent waiters false-reap on the 2-minute `pending` window.

4. `confirmed`  
Phase 0 first is the right sequence. The current user-visible bug is the dead release-hook mismatch between [background.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/wallet-sdk/background.ts:222) and [dispatcher.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/wallet-bridge/src/dispatcher.ts:426), so fixing the end-to-end wiring and pinning it with the real integration test is the right baseline before moving the seam or adding the mutex. I would not temporarily resurrect the old `onTxRequestFinalized` path just to move it later; wiring the new approval hook end-to-end in Phase 0 is cleaner.

**Residual P0/P1**

None.

The only thing I would carry as an explicit implementation note is the item-3 nuance: silent-path waiters are `pending`, not `queued`, so the wait-heartbeat must be stage-agnostic. That does not change the verdict.