# audit-fable.md — single-sim-estimates (Round 1)

- **Verdict**: `conditional approve` (4 conditions F-1–F-4 + advisories F-5–F-10 — ALL adopted into rev 2; dispositions in plan.md §Revision 2 and the decision ledger)
- **Headlines**: Inference 1 RESOLVED from source (unstubbed discovery impossible — oracle throw pre-result); Sponsored contract-identity pin ATTACKED AND HELD in every direction (incl. the user-mintable sponsored-typed non-canonical row, storage laundering, cold cache, resets); `DiscoveryProbe` re-committed the ledger-#11 pure-extractor trap; recon fact 10 erratum (upstream ships stub-sized+clamped sends — conservative-in-our-favor); intent-hash authwit corner (F-4) found from aztec-nr Noir source.
- Cross-audit: convergent with codex r1 on sequencing, Inference 1, probe, Ask-1 honesty, clamp, measurement hardening; fable-unique F-4/F-6/F-8/F-10 + the airtight-pin verification; codex-unique fj fold + PrivateFPC funding footprint.

### Verbatim report

(paths repo-relative per repo policy; full text as returned)

---

**Verification summary** — recon accurate except fact 10 (erratum filed). Confirmed: strategy shapes (`fpc-strategy.ts:87-95,97-134,136-184`; cross-chain bail `:112-115`; P2 envelope seed `:163-168`); discovery call sites + `preDiscoveryActions` (`dapp-send-executor.ts:257-275,577-594`); discoverer lazy `getNodeInfo` + `assertLiveChainIdentity` ordering (`authwit-discoverer.ts:77,90-110`); stub plumbing end-to-end + upstream throw (`pxe.js:757-758`) + `isValidTx` on unvalidated sims costing one node RPC (`pxe.js:796-806`); reuse entry/ladder + `FeeEstimate` field trap; clamp helpers unused, multiplicative 0.1 pad, throw-on-exceed (`get_gas_limits.js`); workstream C mechanics airtight (double-opt-in config, agent.sh unset-else-branch, stamp asserted only-if-requested, backup-migration idiom, test docstring); testnet facts (PrivateFPC pinned, drip-canary zero-env template, prior n=1 datum ±0.03% noise); pin surfaces.

**1a. Sponsored 1-sim identity pin — ATTACKED, HOLDS.** `isProtocol` physically cannot persist (zod-stripped both directions); a crafted canonical-address row gains nothing (address IS identity by derivation — different bytecode unconstructible); `detectFpcType` DOES mint sponsored-typed rows for any artifact with `sponsor_unconditionally` — the pin rests entirely on address equality vs the bundled-artifact-derived canonical, so non-canonical ⇒ two-pass ⇒ fenced (fixture must include exactly this row shape); cold cache/races all DENY the fast path; cross-chain + resets covered by the row-chain check + per-chain cache purge. No input reaches the fast path with a non-canonical payload.

**1b. PrivateFPC folded P1 — bounded but mis-sequenced.** Stub-derived envelope affects only the inert `maxFee` arg and P2's sim envelope; final sizing re-derives from P2's REAL validated sim. Under-seed ⇒ loud pre-chain failure (estimate or prove), no funds move. The sim-vs-proof envelope drift class already exists today; the stub adds a measured-~0 delta on top. BUT it is stub-gas influence shipping before the plan's own measurement gate — resequence (F-2).

**1c. Effects-present rebuild — one real corner (F-4).** From aztec-nr v5.0.1 source: `assert_current_call_valid_authwit` emits the `CallAuthorizationRequest` offchain effect UNCONDITIONALLY (before verification, even with a valid witness attached) — call-authwit-bearing ops always land in the effects-present branch and re-validate; the protocol itself enforces the plan's safety sentence for call-shaped authwits. But `assert_inner_hash_valid_authwit` standalone (intent-hash kind — first-class in Nulo's action spec) emits NOTHING: an op pre-carrying intent authwits could take the 1-sim fold and size from a stub with never-verified witnesses. Closure: pre-attached `add_private_authwit` ⇒ forced validated path; extraction first-sim-only (re-sims re-emit — double-splice hazard); dedup discovered vs pre-attached.

**1d. Validation-loss (F-5).** Send/PrivateFPC-P2/fj-fjwc validated paths unchanged. Folded P1: P2 validates one sim later — negligible. dApp Sponsored no-authwit: the r1 mitigation ("reuse-miss still validates") elides that a reuse HIT runs zero sims — post-B2 the happy path has NO pre-proof `isValidTx` anywhere. Losses enumerated: double-spend/nullifier, instance validity, gas admission, expiry — all move to post-proof `sendTx` (node still rejects; no on-chain safety loss; wasted prove + late error). Re-present Ask 1 on this honest basis; note the saved node RPC.

**1e. Measurement false-confidence (F-9).** Arms must hold build shape constant (Nulo-shaped entrypoints, not aztec.js defaults) and differ only in `{stub, skipTxValidation}`; prior datum was Nulo-pipeline-instrumented (higher fidelity) — cross-check one shape through it; ±0.03% same-arm noise floor ⇒ interleave ≥2 repeats and report noise; canary is a smoke, not proof.

**2. Assumptions.** Inference 1 → FACT (chain: `PrivateExecutionOracle extends UtilityExecutionOracle`; `getAuthWitness` throws `Unknown auth witness...` aborting the ACVM run pre-result — `utility_execution_oracle.js:266-271`; reached from `verify_private_authwit` via the `aztec_utl_getAuthWitness` oracle; `skipTxValidation` gates only post-execution `isValidTx`). A2's unstubbed fallback impossible; fold is stub-or-nothing; outline B's spike premise moot. Inference 2 properly gated (and the unconditional-emission finding slightly supports it). Inference 3 fine (drip-canary proves the pattern). Inference 4 high-confidence. Fact 10 WRONG (erratum): upstream `EmbeddedWallet` sizes+sends from a stubbed payload-inclusive `skipTxValidation` sim then clamps (`embedded_wallet.js:85-160,230-264`) — correcting strengthens the plan. Asks: Ask 1 re-word (above); Ask 2 under-specified (node-advertised `txsLimits.gas` min protocol maxima, throw-on-exceed, customLimits line-item); Ask 3 fine.

**3. Implementation.** F-1: `extractEffects(sim)` one-arg signature is chain-bound-impossible — the EXACT trap ledger #11 recorded; respec with chain context, pin lazy ordering + the live-chain assert (dropping it = security regression). F-3: injection story contradicts itself ("into the strategy call" = forbidden ctx-hook vs "constructor collaborator") — resolve to constructor + two-instance ownership + send-path-no-probe structural pin; own the FpcStrategy-branching trade openly. A-vs-B: **A with the measure-first amendment** — A1's inert interlude is proven practice; B's spike premise is moot (resolved from source); B's stall cost is real (flaky testnet blocks everything); but A-as-sequenced contradicts its own gate — run B1 right after A1, gate PR 2 on the checkpoint. F-8: the cross-chain bail must re-enter the FOLDED two-pass or authwit ops on such rows regress to loud P1 oracle-failure. F-10: preflight as file-level `beforeAll` abort. Pin-churn map matches verified topology; seeds correctly encode the fences; no recon-reuse violations; charter compliance honored modulo F-3's wording.

**Findings index**: F-1 High (probe chain-bound), F-2 High (Inference-1 fact + sequencing), F-3 High (injection contradiction), F-4 Med (intent-hash corner), F-5 Med (Ask-1 honesty), F-6 Med (recon fact-10 erratum), F-7 Med (clamp spec), F-8 Med (bail composition), F-9 Low/Med (measurement fidelity), F-10 Low (preflight shape).

**Verdict: conditional approve** — conditions 1–4 = F-1, F-2, F-3, F-4; advisories F-5–F-10. Sponsored pin verified airtight; workstream C unconditional; outline B rejected with its measure-first insight absorbed.
