`reject (with blocking findings: PrivateFPC cannot pass the proposed unfunded gate, and silent inner-hash authorization remains misclassified)`

1. **Critical — Ask 4’s fallback is neither executable nor equivalent.** Shape 6 requires a canonical PrivateFPC P2 simulation ([plan.md:48](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/implementations-plan/single-sim-estimates/plan.md:48)), but P2 executes `pay_fee`, which deducts `max_gas_cost` from existing private-FJ notes ([private-fuel.ts:81](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/packages/bridge-core/src/private-fuel.ts:81)). `skipFeeEnforcement` does not bypass that Noir balance check. A fresh ephemeral account therefore cannot run the advertised “free … P2-envelope-sim comparison”; the established seed route itself requires the L1-funded run ([fuel-testnet.ts:426](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/packages/bridge-core/scripts/fuel-testnet.ts:426), [fuel-testnet.ts:47](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/packages/bridge-core/scripts/fuel-testnet.ts:47)). Even with an already-funded account, comparing two simulations does not exercise proving, the post-sim finalized envelope, inclusion, or fragmented-note selection. Round 1 explicitly required a fragmented-balance inclusion canary; that was dropped while [plan.md:137](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/implementations-plan/single-sim-estimates/plan.md:137) claims all findings were adopted. Resolution: provision a funded canonical-PrivateFPC canary, including fragmented notes, or defer A2. Option 4(b) is not sufficient.

2. **High — F-4 closes serialized witnesses, not silent authorization requirements.** The “any pre-attached `add_private_authwit`” rule does cover all four content kinds—call, encoded call, intent, and raw message hash ([authwit-content.ts:3](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/packages/wallet-bridge/src/authwit-content.ts:3)). But an app contract can call standalone `assert_inner_hash_valid_authwit` without supplying any witness action. That helper emits no effect, while the simulated account accepts it unconditionally; the folded fj/Sponsored sim therefore reports “no effects” and becomes the sizing sim. Proving against the real account then hits the missing-witness oracle throw ([utility_execution_oracle.ts:417](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/node_modules/@aztec/pxe/src/contract_function_simulator/oracle/utility_execution_oracle.ts:417)). This is a pre-submit proof failure, not an on-chain loss, but it disproves [plan.md:27](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/implementations-plan/single-sim-estimates/plan.md:27) and [plan.md:65](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/implementations-plan/single-sim-estimates/plan.md:65). Ask 1 must explicitly include this undetectable missing-authwit class and require an adversarial fixture.

3. **High — probe ownership still has an unpinned send route.** `DappSendExecutor` currently receives one `buildAndEstimate` dependency, used both by probe-intended Aztec confirm misses ([dapp-send-executor.ts:597](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/apps/extension/src/wallet/services/execution/dapp-send-executor.ts:597)) and probe-forbidden `executeSendTransaction` ([dapp-send-executor.ts:428](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/apps/extension/src/wallet/services/execution/dapp-send-executor.ts:428)). Merely owning two strategy pairs does not close that route. Specify separate typed dependencies—validated/send versus discovery-aware—and test Transfer, `executeSendTransaction`, embedded, and NO_FROM for zero probe/stub calls. A1’s gate must pin simulation options (`stubAccountAddresses` absent and validation enabled), not only counts. With that amendment, the PR2 measurement gate is sound.

4. **Medium — clamp blast radius remains understated.** Shared `finalizeGasLimits` also changes Embedded and NO_FROM ([embedded-strategy.ts:46](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/apps/extension/src/wallet/services/execution/fee/embedded-strategy.ts:46), [dapp-send-executor.ts:771](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/apps/extension/src/wallet/services/execution/dapp-send-executor.ts:771)), absent from Ask 2. Fetching live `txsLimits` also adds a node RPC because the builder already fetched—but did not retain—that data ([tx-request-builder.ts:124](/home/homelab/Projects/nulo/.claude/worktrees/single-sim-estimates/apps/extension/src/wallet/services/execution/tx-request-builder.ts:124)), negating the advertised −1 RPC unless reused. Enumerate every path, exact gas-versus-teardown custom-limit semantics, padding conversion, and RPC posture.

The e2e root cause, Inference 1, Sponsored identity fence, and overall three-PR ordering otherwise hold.