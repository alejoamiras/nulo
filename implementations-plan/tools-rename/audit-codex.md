# Codex audit — tools-rename

Model `gpt-5.6-sol`, effort `xhigh`, read-only sandbox, cwd = this worktree. Session `01a05ed6-33c6-7fb0-b52e-2d36a9509606`.

## Round 1 — fresh session, plan v1

### Prompt

See the round-1 prompt appended below (verbatim).

### Response (verbatim, appended below)

### Disposition

Every factual claim was re-verified against the tree (lockfile record shape at `bun.lock:2362,2594`; `generate.ts:107-115` + `scan.ts:118-129`; `createAztecWalletSession.ts:133-135,206-214,231-237,715-720`; `verify-live-run.ts:89-92`; `refresh-landing.yml:57-68`; `vitest.e2e.config.ts:15`; `background.ts:656`; every completeness hit). All adopted into plan v2 — see plan.md §Audit log. Held: one PR vs two (recorded as Ask A3).

---

I'm auditing a blueprint plan in a Bun monorepo (Nulo — Aztec wallet extension + a companion web app). Repo root for this audit: the current working directory (a git worktree on branch `worktree-tools-rename`, base `dev` @ eca082ca). Read these two files FIRST, in full:

- implementations-plan/tools-rename/plan.md   (the plan under audit)
- implementations-plan/tools-rename/recon.md  (codebase recon that fed it; contains the reuse map, the four buckets, and the keep list)

## Task being planned
Rename the workspace `apps/faucet` (`@nulo/faucet`) → `apps/tools` (`@nulo/tools`) across workspace/tooling, CI workflows, release scripts, the extension's e2e harness, `packages/bridge-core/scripts` path literals and docs (arc 1); then rename the in-app "Faucet" tab → "Drip" with a `localStorage` legacy-key fallback for the wallet-sdk `APP_ID` (arc 2). The deployed product is already `tools.nulo.sh` (PR #319). Cloudflare Pages projects `nulo-tools-{mainnet,testnet}` build from a dashboard-configured root directory the repo cannot change.

## Fixed owner decisions — do NOT relitigate
Tab name = Drip; tier = light; `faucet-cluster` (a complexity burn-down inside apps/faucet) merges before this implements; two stacked PRs is the plan's default (you may challenge that one).

## Facts the plan relies on (verify them — file:line in plan §Assumptions)
F1–F11 in plan.md §Assumptions. Key ones: `bun.lock` keys workspace members by path; `scripts/complexity-baseline/manifest.json` has 10 `apps/faucet` entries regenerated by `bun run baseline:complexity`; `pr-quick.yml` has `workflow_dispatch`; wallet grants keyed by (origin, chainId, profileId) in `packages/wallet-bridge/src/session-types.ts`; `createAztecWalletSession.ts:133,135` derives localStorage keys from `config.appId`; `.sol` edits break Etherscan verification; `gh secret list` shows NO `CLOUDFLARE_FAUCET_DEPLOY_HOOK` (only `CLOUDFLARE_PAGES_DEPLOY_HOOK`).

## Inferences (unverified — challenge them)
I1–I5 in plan.md §Assumptions (Pages root dir currently `apps/faucet`; wallet-sdk uses `appId` for labelling only; no i18n layer; testnet Pages project slug `nulo-tools-testnet`; evm README prose not a verification input).

## What I want from you
Be critical. Find problems, don't validate. Verify every concrete claim against the repo (you have read access; run `git grep`, read the workflow YAMLs, package.json files, the e2e harness under apps/extension/scripts/e2e and apps/extension/tests/e2e, scripts/ci-cd/behavior-gating.test.ts, scripts/release/*, packages/bridge-core/scripts/*). Cite file:line for every finding.

Return, in this order, under these exact headings:

### 1. Adversarial / security
What could go wrong? What would an attacker target? What are we trusting that we shouldn't? Where does a rename silently weaken an existing gate — lockfile regen, CI `changes` outputs/filters, forbid-grep scopes in workflows (there was a prior incident: a `setForceLocal` grep scope left pointing at the old path after packages/→apps/), hostname-integrity pins (`apps/faucet/src/lib/network-targets.ts`, `preview-hosts.ts`, `build-integrity.ts`), required-check names, the Pages root-dir cut-over sequencing?

### 2. Assumption attack
Attack §Assumptions. Which Facts are misstated? Which Inferences are unsafe? Which Asks should be surfaced that the plan silently assumes? Return findings under **Facts / Inferences / Asks** sub-buckets.

### 3. Implementation critique
Critique §Architecture & Implementation. Is the four-pass / two-arc structure right, or is there a simpler/safer sequencing? Is the lockfile "path-only diff" gate sufficient and is the proposed grep for it correct for bun.lock's actual format? Is the `legacyAppId` fallback the smallest correct mechanism (read the actual code in apps/faucet/src/composables/createAztecWalletSession.ts around lines 120-140 and wherever the stored values are parsed)? Do the validation gates prove what they claim — especially: does a `workflow_dispatch` of pr-quick.yml actually run the `build-faucet` matrix (check the `if:` conditions and how `changes` outputs behave on dispatch), and does anything prove the six-file e2e env-var rename is complete? Does anything duplicate or ignore what recon.md found reusable? What would you build differently, and why?

### 4. Completeness
Run `git grep -n -i faucet -- ':!implementations-plan' ':!audit' ':!.claude/worktrees' ':!CHANGELOG.md'` yourself. List (a) any identity-position hit the plan's change map misses (file:line), and (b) any keep-list item the plan would wrongly rename. Also check: does any workflow/script/test reference `_build-faucet.yml` by name beyond those the plan lists? Does anything import `@nulo/faucet` by package name? Is there a `.github/labeler` / CODEOWNERS / release-please `extra-files` / git-cliff `include-path` that names apps/faucet?

### 5. Verdict
Exactly one of: `approve` | `conditional approve (with conditions: ...)` | `reject (with blocking findings: ...)`. Rank findings by severity (Critical / High / Medium / Low) and keep the whole response under ~1500 words.
### 1. Adversarial / security

- **High — lockfile gate is porous.** Bun dependency records contain versions and hashes without the literal words `version`, `resolved`, or `integrity`, e.g. `bun.lock:2362` and `bun.lock:2594`. The proposed grep would therefore accept dependency substitutions. Compare the result against an exact transformation of the old lockfile—only `apps/faucet→apps/tools` and `@nulo/faucet→@nulo/tools`—instead of grepping selected fields.

- **High — Pages validation can false-pass or cannot pass.** Phase 5 invokes `verify-live-run.ts` without `SHA`/`VERSION`; both default empty (`scripts/release/verify-live-run.ts:89-92`), disabling the freshness check (`scripts/release/verify-live.ts:61-69`). It checks only testnet, not `tools.nulo.sh`. Worse, after renaming, explicit `target=tools` will still fail when the acknowledged missing hook is unset—the current behavior intentionally fails an explicit target (`.github/workflows/refresh-landing.yml:57-68`). The plan nevertheless requires that dispatch to succeed.

- **High — `appId` is trusted as more than a label.** The SDK sends it in discovery (`node_modules/@aztec/wallet-sdk/src/extension/provider/extension_provider.ts:222-228`), stores it on sessions (`.../background_connection_handler.ts:319-328`), and includes it in every wallet call (`.../extension_wallet.ts:242-250`). Nulo ignores it for grant lookup, but another compliant wallet may namespace state or wallet construction by it. A Nulo-only manual check does not prove ecosystem compatibility.

- **Medium — hostname-integrity is not actually validated.** `verify-build-target` checks only target, chain ID and manifest digest (`apps/faucet/scripts/verify-build-target.ts:30-35`). Tests duplicate—not import—the production hosts (`apps/faucet/src/lib/build-integrity.test.ts:4-5`), so an accidental or malicious edit to the real pins at `network-targets.ts:49,62` can pass all proposed build gates. Add direct assertions over `TESTNET_TARGET.host` and `MAINNET_TARGET.host`.

- **Medium — CI’s reusable guard is incomplete.** `behavior-gating.test.ts` verifies the filter and reusable filename only (`scripts/ci-cd/behavior-gating.test.ts:104-106`), not the output, computed flag, job `if`, or `quality-status.needs`. The real dispatch proves the current graph once, but leaves no durable regression pin.

### 2. Assumption attack

**Facts**

- F1: only the host pins are repository-verifiable (`network-targets.ts:49,62`); DNS/302 state is external and could not be reproduced in the restricted environment.
- F2: the release step does skip an unset secret (`release.yml:410-416`), but `gh secret list` is external and unavailable. It also conflicts with explicit refresh behavior above.
- F3, F5, F7, F8 and F11 are substantially correct. Importantly, dispatch really does force all filters (`pr-quick.yml:152-166`) and therefore runs the matrix (`pr-quick.yml:185-190,271-283`).
- F4’s count is correct—ten entries (`manifest.json:34-39,60-63`)—but its regeneration claim is false: a renamed path is classified as growth (`scan.ts:118-129`) and plain regeneration refuses it without `--adopt` (`generate.ts:107-115`).
- F6 is correct specifically for Nulo: executable lookup uses profile, origin and chain (`apps/extension/src/wallet/services/dapp-session/service.ts:127-133`). `session-types.ts` merely documents it and does not establish SDK-wide semantics.
- F9 is overbroad. Editing deployed source affects metadata, but unrelated scripts/tests such as `DeployBridge.s.sol:113` or `TestUsdc.t.sol:30` are not necessarily verification inputs. Keeping all Solidity untouched remains conservative.
- F10 is stale: `worktree-faucet-cluster` currently has two `apps/faucet` commits, contradicting `plan.md:116`; the Aztec branch still has one.

**Inferences**

- I1 and I4 are unsafe dashboard guesses. Neither Pages roots, branch mappings nor project slugs are repository facts.
- I2 is false as a general SDK statement; the wallet-sdk trace above materially changed this assessment.
- I3 is supported: there is no i18n dependency or source tree; the only locale use is numeric formatting (`apps/faucet/src/lib/format.ts:22`).
- I5 appears safe: no repository verification path references `contracts/bridge/evm/README.md`.

**Asks**

Surface these before implementation: confirmed Pages project/root/build-command/branch configuration; whether `CLOUDFLARE_TOOLS_DEPLOY_HOOK` will be created or explicit refresh removed from the gate; supported third-party-wallet compatibility for the `appId` change; branch-protection required contexts beyond `quality-status`; and whether both persisted storage namespaces must migrate.

### 3. Implementation critique

The phase ordering is internally impossible. Phase 1 demands zero `apps/faucet` hits (`plan.md:61`), while `_build-tools.yml` is deliberately left containing `apps/faucet` until Phase 2 (`plan.md:65`; current workflow paths at `_build-faucet.yml:41-59`). It also says bridge paths must move in the same commit as `git mv` (`plan.md:38`) but assigns them to Commit 2 (`plan.md:59`).

Use one coherent arc-1 implementation phase for directory/package, all path consumers, CI, e2e and exact lock/manifest transforms; validate afterward. Move `APP_ID`, capability identity and the app-wide `tl-` prefix into arc 1. Leave only the Faucet-feature→Drip vocabulary in arc 2. Given the external cut-over and fuzzy intermediate allow-list, one PR would be safer; two PRs do not make arc 1 independently revertible after the dashboard root changes.

The legacy fallback is incomplete:

- Both `preferred-wallet` and `selected-accounts` need fallback (`createAztecWalletSession.ts:133-135,206-214,246-268`).
- Successful remembered connections deliberately do not write the new preferred key (`createAztecWalletSession.ts:715-720`), so this is permanent fallback, not one-time migration.
- `clearPreferred()` removes only the new key (`createAztecWalletSession.ts:231-237`). A bad legacy preference therefore survives failure and “forget wallet,” causing repeated auto-selection. Clear both keys on explicit clear/failure, and write the new key after a successful legacy restore.
- Test both namespaces, new-key precedence, successful migration, and failed/forgotten legacy cleanup.

The six-file e2e rename has no complete proof. Its unit test only asserts six numeric values (`resolve-ports.test.ts:25-30`), while shell/JSON boundaries occur at `agent.sh:60-64,210-211` and `resolve-ports.ts:152-176,199-211`. Add a zero-old-token assertion across exactly the six files or broaden the Phase-1 grep to all case-insensitive `faucet` hits there. The extension smoke suite does not use this global setup (`vitest.e2e.config.ts:11-15`).

### 4. Completeness

Identity-position misses from the plan’s change map include:

- Build-target API/env identity: `network-targets.ts:21-24,71-82`, `build-integrity.ts:2,21,46,56`, `env.d.ts:8`, `vite.config.ts:11,35,143,166`, and `verify-build-target.ts:13,18`.
- Split path missed by the literal `apps/faucet` grep: `packages/bridge-core/src/candidate-schema.test.ts:7-16`.
- Omitted live script path: `packages/bridge-core/scripts/fuel-testnet.ts:54`.
- Additional code identities: `apps/faucet/scripts/deploy-config.ts:29,36,45`, logger namespace at `deploy.ts:55`, and `FaucetManifestInput`/`buildFaucetManifest` at `capabilities.ts:36,79`.
- Unmapped live prose: `.env.example:29` and `public/theme-boot.js:3`.

The explicit named keep items are protected. However, Phase 4’s zero-hit gate would wrongly force removal of allowed generic prose such as “faucet-style mint” (`useL1Usdc.ts:52`), contradicting the stated generic-prose exception.

No additional `_build-faucet.yml` callers exist beyond `pr-quick.yml:126,279` and its guard at `behavior-gating.test.ts:106`. There are no imports of `@nulo/faucet`; only package identity/README/lock entries (`apps/faucet/package.json:2`, `bun.lock:103,732`). No labeler exists; CODEOWNERS is wildcard-only (`.github/CODEOWNERS:1-8`), release-please extra-files target only the extension (`release-please-config.json:11-13`), and git-cliff is scoped to `apps/extension/**` (`release.yml:313-328`).

### 5. Verdict

reject (with blocking findings: unsafe lockfile gate, impossible Phase 1/5 validations, incomplete legacy-storage migration, and unverified functional `appId` compatibility)

---

## Round 2 — resumed session, plan v2

### Prompt (verbatim)

Thank you — every claim checked out against the tree and all of them are folded into plan v2. Please re-read `implementations-plan/tools-rename/plan.md` in full (it was rewritten) and re-evaluate.

What changed, mapped to your findings:

1. **Lockfile gate** → exact transform-and-diff: `git show <pre>:bun.lock | sed 's#apps/faucet#apps/tools#g; s#@nulo/faucet#@nulo/tools#g' | sort` vs `sort bun.lock`, must be empty, then `bun install --frozen-lockfile`. (§Architecture, first bullet.)
2. **Phase ordering** → arc 1 is now ONE coherent implementation phase (Phase 1, seven ordered commits; the `git mv` commit carries every literal path consumer including `_build-tools.yml` internals and the split `join(…,"apps","faucet",…)` forms), followed by a proof phase (Phase 2: both builds + `verify:build-target`, smoke e2e, `pr-quick.yml` dispatch) and docs (Phase 3). No gate now asserts a state a later phase creates.
3. **Baseline manifest** → `bun run baseline:complexity -- --adopt` + transform-diff against the pre-rename manifest via `jq -S`, must be empty.
4. **Storage migration** → both namespaces read new-then-legacy; a remembered connection restored from the legacy key writes the new key on success (one-time promotion); `clearPreferred` removes both keys; map writes land on the new key; six unit cases + one jsdom e2e case. Implemented against the post-`faucet-cluster` controller shape (behaviour is the contract).
5. **`appId`** → I2 withdrawn; new F12 records your SDK trace + `background.ts:656` (Nulo displays `discovery.appName ?? discovery.appId`); rename recommended as Ask A5 (worst case one re-approval in a third-party wallet); `APP_ID`, `metadata.name`, `tl-` prefix moved to arc 1 as identity; arc 2 is Drip vocabulary only.
6. **Phase 5** → refresh-landing dispatch dropped as a proof; gate = each project's served `/build.json` `sha` equals the tracked head + build log shows `apps/tools`; `verify-live-run.ts` only with `VERSION`/`SHA` set, testnet, optional.
7. **Host pins** → new direct test on `TESTNET_TARGET.host` / `MAINNET_TARGET.host`.
8. **CI guard** → three durable pins in `behavior-gating.test.ts` (outputs contain `tools` + `needs-tools-build`; `jobs["build-tools"].if` references `needs-tools-build`; `jobs.status.needs` includes `build-tools`).
9. **e2e harness proof** → scoped case-insensitive grep over the seven files + standalone `bun apps/extension/scripts/e2e/resolve-ports.ts` key check; smoke suite acknowledged as not exercising the harness.
10. **Completeness** → `FaucetTarget`/`FaucetTargetKey`/`resolveFaucetTarget`/`makeFaucetConfig`, `VITE_FAUCET_TARGET`, `FAUCET_TARGET`, `candidate-schema.test.ts:7-16`, `fuel-testnet.ts:54`, `deploy-config.ts` (token config names → arc 2 as Drip vocabulary), `deploy.ts:55`, `capabilities.ts:36,79` (→ arc 2 `buildDripManifest`), `.env.example:29`, `theme-boot.js:3` all mapped; Phase-4 gate is a reviewed allow-list, generic prose kept.
11. F9 marked conservative; F10 refreshed (two `faucet-cluster` commits).

Held, deliberately: **two stacked PRs** (Ask A3). Your argument (arc 1 is not independently revertible after the dashboard flip) is recorded verbatim for the owner; I kept the split because arc 2 IS independently revertible and a `git mv`-clean identity PR is easier to review. If you still think one PR is materially safer rather than merely simpler, say why in one paragraph and I'll put your reasoning in the ELI5.

Please answer under these headings, under ~700 words, citing file:line for anything new:

### Regressions in v2
Anything v2 introduced or still gets wrong (e.g. is the `sort`-based lockfile multiset compare sound given Bun's re-sort? does `--adopt` have any other side effect the `jq -S` diff would not catch? is the storage migration's "write new key on remembered success" the right trigger, or should the read itself promote?).

### Remaining gaps
Any finding from round 1 not actually closed, or a new identity-position miss.

### Verdict
Exactly one of: `approve` | `conditional approve (with conditions: ...)` | `reject (with blocking findings: ...)`.

### Response (verbatim)

### Regressions in v2

- **High — Phase 5’s proof cannot pass.** `/build.json` has `buildId`, not `sha` ([vite.config.ts:68](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/apps/faucet/vite.config.ts:68)); `jq -r .sha` therefore returns `null`, contradicting [plan.md:116](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/implementations-plan/tools-rename/plan.md:116). Reuse the existing tested mechanism: extract the final component of `buildId` and compare it with `HEAD[:8]` ([verify-live.ts:61](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/scripts/release/verify-live.ts:61)).

- **High — mainnet cut-over is still sequenced unsafely.** If mainnet tracks `main`, flipping its root immediately after the `dev` merge knowingly points it at a nonexistent directory and cannot produce the required `apps/tools` build log ([plan.md:114](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/implementations-plan/tools-rename/plan.md:114)). Flip testnet after the `dev` merge; flip mainnet only once the rename reaches its tracked branch.

- **Medium — sorting the whole lockfile is not an exact comparison.** `bun.lock` is nested JSONC ([bun.lock:4](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/bun.lock:4)); line sorting loses object ownership and array ordering, so semantic line relocation can preserve the multiset. Frozen install does not prove equality with the pre-image. Parse both versions and recursively sort object keys while preserving arrays, then deep-compare. The repo already parses this format structurally ([lockfile-exception-diff.ts:15](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/scripts/lockfile-exception-diff.ts:15)).

- **Medium — the baseline transform will fail after the recorded day.** Generation always rewrites `generated` to today ([generate.ts:118](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/scripts/complexity-baseline/generate.ts:118)), while the expected file retains the pre-rename date ([manifest.json:3](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/scripts/complexity-baseline/manifest.json:3)). Normalize that field deliberately. Also assert the command changed only the manifest: `--adopt` can write directives before generating it ([generate.ts:71](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/scripts/complexity-baseline/generate.ts:71)); count equality alone cannot detect a removed-and-replaced directive within the same file/rule.

- **Medium — the standalone e2e proof is misstated.** `resolve-ports.ts` writes JSON to `.e2e-state/ports.json` but stdout is a summary without `faucetUrl` ([resolve-ports.ts:199](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/apps/extension/scripts/e2e/resolve-ports.ts:199), [resolve-ports.ts:209](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/apps/extension/scripts/e2e/resolve-ports.ts:209)). Run it, then `jq` the written file for `tools`/`toolsUrl` and absence of legacy keys.

- **Storage trigger is correct.** Promote only after full remembered-connect success, not on read. The current success boundary follows capability selection and contract setup ([createAztecWalletSession.ts:703](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/apps/faucet/src/composables/createAztecWalletSession.ts:703)); read-time promotion would bless merely parseable stale or attacker-controlled same-origin state.

### Remaining gaps

- **Medium — residue gates remain internally impossible.** Tests must contain `nulo-faucet:*`, yet Phase 1 declares that regex zero-hit; the final allow-list also omits the permanent legacy literal ([plan.md:88](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/implementations-plan/tools-rename/plan.md:88), [plan.md:110](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/implementations-plan/tools-rename/plan.md:110)). Use an exact reviewed file/count allow-list.

- **Medium — one identity-position miss:** `register-token.test.ts` names both `FAUCET_DEV_PORT` and `faucetUrl` outside the seven-file harness grep ([register-token.test.ts:23](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/apps/extension/tests/e2e/network/register-token.test.ts:23)). Map it into Arc 1. Arc 2 should also review the `faucet-drip` comments at [incoming-transfers.test.ts:12](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/apps/extension/tests/e2e/network/incoming-transfers.test.ts:12).

- **Low — stale instruction:** Phase 4 still treats `candidate-schema.test.ts:13` as a semantic fixture ([plan.md:108](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/implementations-plan/tools-rename/plan.md:108)), although it is the split app path correctly assigned to Arc 1 ([candidate-schema.test.ts:7](/home/homelab/Projects/nulo/.claude/worktrees/tools-rename/packages/bridge-core/src/candidate-schema.test.ts:7)).

Two PRs are no longer materially less safe once mainnet cut-over is branch-gated; I withdraw that objection.

### Verdict

conditional approve (with conditions: fix the structural lock/baseline comparisons, executable e2e/residue/live gates, mainnet cut-over timing, and the two missed e2e comment sites)