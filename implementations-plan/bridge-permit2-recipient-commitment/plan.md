# Plan — bridge-permit2-recipient-commitment

**Status: ✅ APPROVED 2026-07-06 (human sign-off + all Asks answered; final fresh-context codex `approve`)** · Tier: `/blueprint deep`

### Approved scope amendments (from the sign-off)

- **A-1 → RATIFY reuse** (fuel-only = `bridge(tokenPortal = FeeJuicePortal)`, zero new Solidity, no router redeploy). **Follow-up recorded**: the on-chain phishing mitigation (a hardwired/allowlisted portal so `bridge()`/fuel don't trust a caller-named `tokenPortal`) is a DELIBERATE future work item for BOTH the bridge and fuel paths — out of scope here, tracked for a subsequent hardening pass (the user explicitly wants to work on it afterward). Until then it's client-pinning + the phishing docs.
- **A-3/A-5 → WIPE (major simplification).** Only testnet infra exists and no prior state matters, so: freeze the old faucet, wipe, cut over — **no preservation of old-stack claimability**. This SUPERSEDES the migration-preservation machinery the audits built: **DROP** the v1 legacy claim script, the v2 new→old rollback recovery path (final-codex HIGH-2 / Phase 7b — moot with nothing to preserve), the drain-window-for-in-flight-claims, and the stale-deployment legacy support burden. **KEEP** the candidate-first deploy + live canaries (that's deploy SAFETY — prove the new stack before flipping, unrelated to state preservation) and the salt-v2 interlock (cheap; prevents a stray preview build stranding even dust and documents the code↔config coupling for the eventual value-token version). Phases 7–8 are amended below with `[WIPE]` markers; the dropped machinery is struck but its rationale kept for the trail.
- **A-2/A-4/A-6/A-7 → defaults.** A-2 legacy-claims: moot under WIPE. A-4: rewire `deposit-testnet.ts` to the router path. A-6: dedicated fresh relayer key, sponsored fees, never logs secrets. A-7: parameter name `claim_salt`.
- **Next step**: finalize seeds; the user runs the build session. Phases 6–7 live-deploy/promotion stay gated on explicit go regardless. (rubric: blast radius / irreversibility / migration cost / external coupling / security sensitivity all HIGH; novelty moderate) · Baseline: `dev@0f4c95c`

Three coupled changes to the L1↔L2 bridge stack, one cutover:

- **(a)** Bridge-only ERC20 deposits move onto the router's existing Permit2 witness-bound `bridge()` entrypoint; the direct approve+portal path is **deleted** (no fallback flag — user-locked).
- **(b)** Fuel-only deposits become Permit2 witness-bound — **with zero new Solidity**: `bridge()` called with `tokenPortal = FeeJuicePortal` executes the fuel-only deposit verbatim (ABI-identical `depositToAztecPublic` — see ledger L1).
- **(c)** Private claims become **recipient-committed**: `claim_private` derives the L1→L2 consumption secret from the actual recipient in-circuit, so a relayer holding the claim inputs can submit the claim *for* a user but can never redirect funds. **The real driver is the new RELAYER CAPABILITY, not closing red-team F-007** (which the report rates Low 2.6, accepted-risk — stated honestly so the irreversible-cutover decision is made on real terms; fresh-audit LOW). Forces a fresh L2 trio (token, minter proxy, bridge) + a fresh NuloTokenPortal instance (one-shot wiring); L1 AZLO, router, swap target, pools, FeeJuicePortal all stay.

Plus: the Foundry suite gains its first fuzz tests; a minimal relayer script ships in bridge-core; the final phase is a focused red-team re-audit of the changed surface.

Companion docs: [brief.md](brief.md) (task + ground truth), [draft-main.md](draft-main.md), [audit-codex.md](audit-codex.md), [audit-fable.md](audit-fable.md), [eli5.html](eli5.html).

---

## Decision ledger

Three independent drafts (main / codex / fable) were consolidated. Sources and rejections:

| # | Decision | Source | Rejected alternatives (and why) |
|---|---|---|---|
| L1 | **(b) reuses `bridge()` with `tokenPortal = FeeJuicePortal`. Zero new Solidity, NO router redeploy.** | fable (dissent) | main: new `fuel()` + 3-field FuelWitness + router redeploy; codex: new `fuel()` + 5-field FuelWitness + router redeploy. Rejected because `ITokenPortal.depositToAztecPublic` ≡ `IFeeJuicePortal.depositToAztecPublic` (verified: `contracts/bridge/evm/src/interfaces/ITokenPortal.sol:9-11`, `IFeeJuicePortal.sol:12-14`), the router takes `tokenPortal` per-call, and item (c) doesn't touch the router — so a new entrypoint buys only a fuel-named event + a hardwired portal at the cost of a redeploy plus a second TYPEHASH/TYPE_STRING/TS-mirror/pin surface maintained forever. Client-side fail-closed asserts replace the hardwiring (existing pattern `apps/faucet/src/composables/useL1FeeAsset.ts:69-71`). Witness aliasing analyzed and safe: a fuel-only witness IS a bridge-only witness whose two interpretations execute the identical call; cross-entrypoint replay vs `bridgeWithFuel` impossible (`bridge()` zeroes `fuelAmount`, `bridgeWithFuel` requires it > 0 — disjoint witness spaces). **Residual accepted**: the coupling is an interface coincidence, not a contract — documented in `fuel.ts`, re-attacked in Phase 9. Needs user ratification at the approval gate (Ask A-1). |
| L2 | **(a) rides the LIVE router** — no redeploy for (a) either; Phase 1 verifies `bridge()` on the deployed bytecode (`0x4c3f…4068`) before anything depends on it. | fable + codex | main: fold (a) into a new router deploy — mooted by L1 (no redeploy exists to fold into). |
| L3 | **(c) scheme**: `claim_private(recipient, amount, claim_salt, message_leaf_index)` re-derives `secret = poseidon2([claim_salt, recipient], DS)` in-circuit; bearer parameter removed entirely; portal bytecode + content hashes + existing keystone pins untouched. | all three (identical independently) | Content-hash commitment family — pre-rejected by the user in Phase 0 (bias confirmed: no planner found a hole in the derivation; see soundness argument). |
| L4 | **Domain separator**: `DOM_SEP__TOKEN_BRIDGE_PRIVATE_CLAIM_SECRET = 3140354885` = `poseidon2_hash_bytes("nulo_dom_sep__token_bridge_private_claim_secret") as u32`. Verified with the real crypto; ≠ FPC `3952304070`, ≠ protocol `SECRET_HASH 4199652938`. | main (nulo_ prefix — honest: this is a Nulo constant, not an Aztec-canonical one) + codex (the precise `private_claim` name) | codex's `az_dom_sep__…` string (value 3189952094, also verified) — rejected on naming only: the `az_` prefix on the FPC constant exists because it must byte-match Wonderland's Noir; ours is free, so it shouldn't imply canonicity. fable's `az_dom_sep__token_bridge_claim_secret` — same reason, plus less precise name. |
| L5 | **Shared Noir lib** `contracts/bridge/aztec/claim_secret/` holds the derivation; `token_bridge` AND `keystone` depend on it, so the keystone pins the *same code object* the contract executes. | fable | Inline formula in `main.nr` + re-typed copy in keystone (main, codex) — rejected: the two copies can drift independently while keystone stays green. |
| L6 | **Pin vectors** (TS-verified with real crypto, to be byte-matched from Noir in Phase 2): DS above; `(0,0) → secret 0x1bcb2e97aaeb9788f9b23d331c90b0e138b8fa84890f1fb045c4c260b26e1a4f, secretHash 0x087647b3c1976bde1cf12bfa5213c1d619f4f0b9a0bae6e1000d7030fcb9d5fc`; `(1,2) → 0x12e754c717173c3dd6f22932580bf8332274034b2c4a6a8ec8d682b482e104a5, 0x23d9fa16980e66c10886f09780ea412bd1c942a0a6e588d838ea05d7228c6754`; `(0x1234567890abcdef, 0xdeadbeef) → 0x02c856f2079a8e1cc126d02e81cd409c7ee02e0c4c7cd2dd9450edaa8c681e7d, 0x2a636f84225eec69e412ed5dffd735f1ac8ff6d28b24dac5d796f0c2a648ac1c`. | codex (vector methodology; its vectors verified byte-exact for its DS) recomputed for L4's DS | — |
| L7 | **Reuse L1 AZLO** (`EXISTING_L1_TOKEN` env on the deploy script, with symbol/decimals/cap read-back asserts). Keeps the router config, AZLO/WETH pool, quoter, and every user's L1 balance valid. | fable (as mandatory; codex surfaced as ask) | Fresh L1 token per the script's current default (`packages/bridge-core/scripts/deploy-bridge-testnet.ts:170-171`) — rejected: silently orphans pools + balances (fable R4). |
| L8 | **Candidate-first cutover** using the deploy script's EXISTING candidate-manifest + write-ahead-journal machinery (`deploy-bridge-testnet.ts` header): deploy → `testnet-bridge.candidate.json` → scripted canaries against the candidate → promote by copying over `testnet-bridge.json`. | codex (proposed) + fable (verified it already exists) | main's "flip config at cutover phase" without a candidate stage — strictly worse. |
| L9 | **Single-release promotion rule (HARD) + runtime interlock**: faucet code that derives private secrets must NEVER go live against the old bridge (its `claim_private` takes a raw secret — derived-secret deposits against it strand). Code + candidate-promotion land as ONE release; Phases 3–5 stay dev-only until Phase 7 promotes. **Backed by a technical interlock, not just process** (codex audit cond. 2): the manifest carries `privateClaimMode: "salt-v2"`, and the deposit code REFUSES to build a private deposit against any manifest lacking it — so a stray preview/static deploy of new code against an old manifest fails closed instead of stranding funds. | fable (self-review) + codex (the interlock) | Process-only L9 (both plans' original) — rejected: an accidental branch preview or static host could still strand (codex I6/finding 3/15). |
| L10 | **Extend `verify:deployments` with a bridge-manifest check** (it currently only rebuilds the faucet's own contracts — `apps/faucet/scripts/verify-deployments.ts:14-37` — so `testnet-bridge.json` drift is ungated). | codex (dissent, verified) | main's assumption that `audit:faucet` already gates the manifest — was wrong. |
| L11 | ~~Legacy claims = pinned script + `stale-deployment` quarantine; old stack stays claim-live indefinitely.~~ **SUPERSEDED by the A-3/A-5 WIPE sign-off**: no legacy preservation — old state is discarded. The gate-lift hazard (claim path builds from config `BRIDGE`, not `rec.bridge`, `useDeposit.ts:309`) is now moot since there are no old records to claim. The `stale-deployment` quarantine still fires harmlessly on any residual old record. | fable (original) → user (WIPE) | codex's in-faucet legacy registry AND the fable pinned-script — both dropped under WIPE. |
| L12 | **Fuzz strategy**: fable's four targets + codex's hostile-portal target (five total) + explicit exclusions (below), absorbing codex's revert-bound cases. | fable (structure), codex/main (overlapping targets) | Broader fuzz lists (fuzzing content hashes, `_hashRoute` injectivity, Noir TXE fuzz) — rejected per the repo's succinctness philosophy; the excluded properties are cross-toolchain byte-equality pins or solc's contract, which fuzzing can't express. |
| L13 | **Relayer**: dedicated fresh Aztec account (never the deploy key), sponsored-FPC fees, and the canary asserts the recipient's balance from the recipient's own wallet — doubling as the note-discovery check for third-party-submitted mints. | fable (R6 note-discovery) + all (script shape) | — |
| L14 | Envelope: token-private deposits reuse the existing optional `DepositEnvelopeV2.salt` field (`recovery-crypto.ts:104-116`); journal deposit records bump to `schema: 3` for the SIGN-everywhere stepper layout. **`envelope.salt` is now domain-overloaded** — FPC-salt (DS 3952304070) for direct-fuel records, token-claim-salt (DS 3140354885) for token records, disambiguated by `assetKind` — so Phase 3 adds a test asserting each `assetKind` derives with its own DS. **Fueled-private is NOT a collision** (fresh-audit Leg C, verified): the token salt seals into `envelope.salt`, the fuel salt into the separate `journal.bridgeSecretSalt` (`journal.ts:90`) — BUT the fuel salt is plaintext-journal-only, not in the sealed backup blob (pre-existing durability asymmetry, now documented, NOT papered over by "additive, no v3"). | fable (reuse) + fresh-audit (the overload test + the durability-asymmetry disclosure) | codex's "v3 private envelope" — still unnecessary for correctness (funds fail-safe on cross-wiring), but the durability caveat is now explicit rather than hidden. |

**Unresolved disputes**: none among planners after consolidation. **Open Asks** (approval gate): A-1…A-7 below.

### Contradiction-check round (codex resume + fable resume — both returned `contradictions-found (8)`; all 16 folded)

Fable's (F) and codex's (C) findings, all adopted:
- **F1 HIGH** — the L10 verifier extension, gated in Phase 5, could never pass: it would rebuild the bridge from the NEW artifact against the OLD live manifest → permanent DRIFT. Moved to Phase 6 against the candidate manifest; the live manifest is only gated from promotion (Phase 7.7) onward. Corollary noted under L9: the dev faucet's connect-time bridge rebuild is *expectedly* broken against the old stack during the Phase 2→7 window.
- **F2 HIGH** — the fueled-private token leg was asserted in the design spec but implemented nowhere: `runSwapBridge` draws `Fr.random()` for the token secret (`flows.ts:294`) — post-(c) every private fueled deposit would strand. Phase 3 adds a required `tokenClaimSalt` to the swap params with an F-005-style fail-closed throw.
- **F3 HIGH** — R7's "revert the manifest commit" rollback violated L9 (derived-secret code left live against the bearer bridge). Post-promotion rollback = redeploy the last pre-cutover RELEASE (code + config as one unit).
- **C1 HIGH** — the wrong-recipient canary was non-diagnostic as ordered (message already consumed by the relayer claim). Reordered: wrong-recipient claim FIRST on the unconsumed deposit (assert fails + not consumed), then the correct relayer claim.
- **C2 HIGH** — `claim_salt` keeps the old parameter arity/types, so a stale caller passing a raw secret typechecks and fails only at runtime (fail-safe — the deposit stays claimable — but confusing). Phase 3 gate gains a stale-semantics check: grep + tests asserting every `claim_private` caller passes a salt, never `envelope.secret`.
- **C3 MED** — "fuel-only asserts `isPrivate === false`" conflated the ROUTER param with record privacy (private fuel-only exists and stays: L1 deposit public to the FPC, record `isPrivate: true`). Renamed `routerIsPrivate` in the spec; private fuel-only added to the canary set.
- **C4 MED / F7 LOW** — the legacy claim script had no committed pre-cutover manifest to pin against. Phase 7.7 commits `packages/bridge-core/legacy/testnet-bridge.v1.json` at promotion; Phase 8 gates against it.
- **C5 MED** — Phase 3's residue grep only covered `depositToAztecPrivate`. Expanded to both deposit functions' direct `writeContract` usages.
- **C6 MED** — Phase 6 changes the deploy script but its gate had no TS gates. Added bridge-core typecheck + tests before any live deploy.
- **C7 MED** — bridge-only now requires router+permit2, but the manifest keeps them under optional `l1.fuel`. Phase 5 promotes them to REQUIRED bridge config in `bridge-deployments.ts`; the Phase 6 verifier fails when absent.
- **C8 LOW** — Phase 2 gate gains `token_minter_proxy` nargo test + an explicit artifact-diff check (byte-stable or STOP).
- **C9 LOW** — `fuel()` rejection re-ratified by both reviewers; documented that fuel indexing parses the FeeJuicePortal `DepositToAztecPublic` event (as `fuel.ts:67-74` already does), never the router `Bridge` event alone.
- **F5 MED** — the private+FUELED path was exercised by no gate. Added to the canary set (Phase 7).
- **F6 LOW** — `cd token_bridge && nargo test` was vacuous (zero `#[test]`s) and toolchain-unqualified. Phase 2 gate now uses the pinned-toolchain `compile.sh` as the compile-proof + keystone/proxy nargo tests, with the rc.2 `aztec-nargo` requirement stated.
- **F8 LOW** — the Phase 8 legacy-claim test deposit's creation mechanism now stated: made through the still-live OLD faucet UI during the Phase 7.6 drain window, before promotion.
- **F4 LOW** — `claim_salt` as the committed-ABI parameter name surfaced as Ask A-7; the stale-tab post-promotion note restored to R1. Both reviewers independently recomputed the L4/L6 DS + vectors: byte-consistent (now verified three times).

### Double-audit round (codex resume `conditional approve`, 26 findings + fresh-fable auditor — all folded)

**Codex (5 conditions + supporting):**
- **HIGH (cond. 2 / findings 3,15)** — L9 as process-only could still strand on a preview/static deploy. Fixed by the manifest `privateClaimMode: "salt-v2"` interlock (ledger L9 above; Phases 5/6/7 + design spec).
- **HIGH (cond. 1 / findings 2,20,11)** — Phase 1's fork tests deploy a LOCAL router, so the gate proves nothing about the DEPLOYED router. Added a required live-bytecode `bridge()` selector-call gate (Phase 1); the "router redeployed from source containing bridge()" claim moved Fact → Inference (I1 is now the load-bearing one, gated).
- **HIGH (cond. 3 / findings 4,21)** — A2 sole-consumer invariant not structurally gated. Phase 2 gate gains a static callsite assertion: exactly the expected `consume_l1_to_l2_message` sites, every private-mint path routed through `derive_claim_secret`.
- **HIGH (cond. 4 / findings 1,5,8,16)** — the generic-router phishing surface (`bridge()` takes an arbitrary `tokenPortal`; the router `forceApprove`s it, so a malicious portal can `transferFrom` the pulled tokens). Verified real against `SwapBridgeRouter.sol:220-227`. It's phishing-only (the faucet pins the portal from the manifest; the user's own signature names it), and the OLD direct path had the same trust shape — but now documented explicitly (Security section), client-pin asserted (Phase 5), and a **5th fuzz target** (hostile-portal `bridge()`) added. A-1 reframed to name the accepted risk.
- **MED (cond. 5 / findings 24,25)** — manifest copy/verify commands underspecified. Phase 6 verifier takes an explicit `BRIDGE_MANIFEST=…candidate.json`; Phase 7 copies live→`legacy/testnet-bridge.v1.json` (hash-verified) BEFORE `cp candidate → live` (ordering bug — my Phase 7.8 wording could have pinned the candidate as v1).
- **MED (findings 6,19)** — relayer must never be authoritative salt storage and must not log salts/secrets (A-6 recommended option updated; Phase 3 script requirement).
- **MED (finding 7)** — Noir git-tag deps aren't a cryptographic pin (no `Nargo.lock`). Recorded as an Ask/Inference; Phase 2 keeps the artifact-diff + compile-provenance check. (Deferred like F-003 — flagged, not silently accepted.)
- **MED (finding 13 / I2)** — `bridgeWithFuel` uses the router's IMMUTABLE `feeJuicePortal`, NOT arbitrary `p.tokenPortal`, so "already true live" does NOT transfer to `bridge(tokenPortal = FJ portal)`. I2 rewritten to depend solely on the new Phase 1 fork leg.
- **MED (finding 14 / I4)** — note discovery for relayer-submitted mints made a HARD promotion blocker (not just canary-asserted): if recipient wallets can't discover third-party-submitted private mints, promotion stops until the real fix (possibly `registerSender` wallet work) is known.
- **MED (findings 9,18 / A-5)** — failed-candidate-after-L1-deposit recovery enumerated (dust amounts; persist candidate manifest + claim material before each canary; documented abandon procedure). A-5 split: does the drain window FREEZE old deposits or merely announce?
- **LOW (finding 10)** — the 32-bit DS is a namespace tag, not a cryptographic boundary (the field-hash does the work); documented as such, exact-string re-derivation test kept.

**Fresh-fable auditors (two independent spawns — both `conditional approve`; they cross-checked each other and both confirmed the (c) crypto core sound, A2 sole-consumer structural, and witness-space disjointness real):**
- **HIGH — CF Pages preview-deploy breaks L9's I6.** The faucet is CF Pages Git-integration with no `wrangler.toml`; its default is a public preview URL per branch/PR. A preview build of Phases 3–5 code = derived-secret deposit code + old-bridge manifest = the strand configuration L9 forbids, publicly reachable. **Largely mitigated by the codex `privateClaimMode: "salt-v2"` interlock** (a preview build reads the old manifest, which lacks the field → deposit code refuses, fail-closed) — the audits compound. Residual: add a pre-Phase-3 blocker to confirm CF previews are disabled/access-gated for this project (belt and suspenders; the interlock is the technical guarantee, the gate is the operational one).
- **HIGH — wrong-recipient canary non-diagnostic if it dies at the client re-derivation guard.** It would prove the client guard works, not that the CIRCUIT rejects a wrong recipient. Phase 7.3 mandate: `relay-claim-testnet.ts --wrong-recipient` BYPASSES the client guard and submits `claim_private(recipient=WRONG, …)` directly to the sequencer, asserting revert AND still-claimable.
- **HIGH — fork legs auto-skip → gate green with I2 unproven.** `forge test` exits 0 when the `SEPOLIA_RPC_URL`-gated legs `vm.skip(true)` (verified at `SwapBridgeRouterPermit2Fork.t.sol:108-110`). Phase 1 + Phase 6 gates must assert the NAMED fork legs PASSED (grep the forge output for the new leg names), not merely exit 0.
- **HIGH — rollback new→old stranding (the direction R7 missed).** Between promotion and a post-promotion rollback, users make derived-secret deposits against the NEW stack; a rollback to the old release (old config, raw-secret claim, legacy script pinned to v1) leaves those deposits with NO reachable claim path (flagged stale, unclaimable by both the legacy script and the rolled-back faucet). R1–R8 covered old→new exhaustively and missed this. R7 fixed: keep the promoted release claim-reachable (a versioned build or a `v2` legacy script), symmetric to L11.
- **HIGH→MED (reconciled between the two auditors) — the fueled-private two-salt durability asymmetry.** Leg B flagged an envelope-salt collision; Leg C CORRECTED it: no collision — the token-claim salt seals into `envelope.salt`, the fuel salt lives in the separate `journal.bridgeSecretSalt` (`journal.ts:90`, verified). The real issue: the fuel salt is plaintext-journal-only, NOT in the sealed backup blob (pre-existing to private-fuel, but the plan must acknowledge it, not claim L14 "additive, no v3" covers full fueled-private backup durability). Fix in L14 + a test asserting each `assetKind` uses its own DS (envelope.salt is now domain-overloaded: FPC-salt DS 3952304070 for fuel records, token-claim DS 3140354885 for token records, disambiguated by `assetKind`).
- **MED — bearer-signature intent-authenticity (distinct from codex's tokenPortal phishing).** A user phished into signing a VALID `BridgeWitness` with the ATTACKER's `aztecRecipient` → the router faithfully bridges to the attacker. Witness binding proves integrity, not intent. Worse than today's bridge-only (approve+portal pulls from `msg.sender` — no bearer-signature vector existed); AZLO's max-Permit2 pre-approval (`MintableERC20.sol:47-50`, INFO-1) removes the approve gate, so one blind `signTypedData` drains AZLO. Testnet-valueless → accept, but stated plainly + a HARD blocker for any value-token port. The new one-time `approve(Permit2, max)` on the canonical fee asset likewise creates a standing Permit2-drainable balance — noted, not framed as pure UX.
- **MED — C2 grep scope.** Must span `packages/bridge-core/{src,scripts}` + `apps/faucet/src` and enumerate all FIVE `claim_private` sites (verified: `l2.ts:56`, `fuel-testnet.ts:271`, `deploy-sandbox.ts:292`, `useDeposit.ts:381` fueled-private, `useDeposit.ts:476` non-fuel) — the blast-radius table under-enumerated. Folded into Phase 3.
- **MED — Phase 2 proves derivation, not consumption-wiring.** Vectors round-trip the derivation + DS byte-match, but a transposed `consume_l1_to_l2_message(content_hash, secret, …)` or mis-passed `recipient.to_field()` is first caught at Phase 4. Phase 2 gate's claim downgraded to "derivation + compile + sole-consumer only; consumption correctness deferred to Phase 4" (a `token_bridge` TXE test added if the harness supports it).
- **LOW — honest reframing (both auditors): the real driver is the RELAYER FEATURE, not closing F-007** (which the red-team rated Low 2.6, accepted-risk). The irreversible cutover is justified by the new capability; stated as primary so the risk-acceptance is made on honest terms.
- **LOW — recipient-commitment removes the claim-time safety net**: recipient is fixed at DEPOSIT time now, so a wrong/unspendable recipient strands PERMANENTLY (client must bind `recipient` = connected wallet + test it). **LOW — `swapTarget` binding** couples bridge-only/fuel-only signature liveness to pool migrations (`setSwapTarget` invalidates in-flight sigs; deadline-bounded, accepted). **LOW — I1 positive-probe method**: `cast call <router> "bridge(...)" <zero-amount params>` must revert with `"SwapBridgeRouter: zero amount"` (selector present) vs empty revert (absent) — a bare cast can't distinguish. **LOW — relayer deanonymizes** the claim to itself (Phase 8 docs). **Verified NOT broken**: `capabilities.ts` scopes by `{contract, function}` name, not arg shape — the secret→salt change flows through (Leg C downgraded it to a one-line sanity check).

---

## Design specification

### (c) Recipient-commitment — the exact scheme

New Noir lib `contracts/bridge/aztec/claim_secret/src/lib.nr` (path-dep'd by `token_bridge` and `keystone`, precedent `token_bridge/Nargo.toml:7`):

```noir
/// poseidon2_hash_bytes("nulo_dom_sep__token_bridge_private_claim_secret") as u32 — pinned
/// literal; the TS keystone re-derives it from the string and asserts equality, and asserts
/// it differs from the FPC fuel separator (3952304070) and the protocol SECRET_HASH (4199652938).
pub global DOM_SEP__TOKEN_BRIDGE_PRIVATE_CLAIM_SECRET: u32 = 3140354885;

pub fn derive_claim_secret(claim_salt: Field, recipient: AztecAddress) -> Field {
    poseidon2_hash_with_separator([claim_salt, recipient.to_field()], DOM_SEP__TOKEN_BRIDGE_PRIVATE_CLAIM_SECRET as Field)
}
```

`token_bridge/src/main.nr` — `claim_private` (current `:104-122`) becomes:

```noir
fn claim_private(recipient: AztecAddress, amount: u128, claim_salt: Field, message_leaf_index: Field) {
    self.enqueue_self._assert_not_paused();
    assert(amount > 0, "Amount must be greater than zero");
    assert(!recipient.is_zero(), "Recipient cannot be zero");         // NEW (strand-to-unspendable edge)
    let config = self.storage.config.read();
    let content_hash = get_mint_to_private_content_hash(amount);       // UNCHANGED
    let secret = derive_claim_secret(claim_salt, recipient);           // NEW: in-circuit derivation
    self.context.consume_l1_to_l2_message(content_hash, secret, config.portal, message_leaf_index);
    self.call(TokenMinterProxy::at(config.token_minter_proxy).mint_to_private(recipient, amount));
}
```

The raw-secret parameter is REMOVED (renamed + re-semanticized, same arity/types); no other entrypoint in the contract consumes `mint_to_private` content (the contract has exactly two `consume_l1_to_l2_message` sites, one per claim). `claim_public`, both exits, pause, ownership: untouched.

TS counterpart `packages/bridge-core/src/claim-secret.ts` (sibling of `private-fuel.ts`, same pinned-literal-never-computed-at-import discipline): `DOM_SEP__TOKEN_BRIDGE_PRIVATE_CLAIM_SECRET = 3140354885`, `deriveTokenClaimSecret(salt, recipient)`, `tokenClaimSecretHash(salt, recipient)`.

Flow: deposit draws `claim_salt = Fr.random()` → `secretHash = computeSecretHash(deriveTokenClaimSecret(salt, recipient))` → sealed envelope gains `salt` (existing optional field) BEFORE the irreversible L1 tx → Permit2 witness (`tokenSecretHash = secretHash`) → `router.bridge()`. **The deposit code refuses to build a private deposit unless the active manifest declares `privateClaimMode: "salt-v2"`** (the L9 runtime interlock — a derived-secret deposit against an old bearer-bridge manifest fails closed instead of stranding). Claim (self or relayer): `claim_private(recipient, amount, salt, leafIndex)`. **Client fail-closed assert at claim time**: re-derive and check `computeSecretHash(derived) === rec.secretHashHex` before sending. The **fueled private token leg uses the same derivation**: `runSwapBridge`'s params gain a required `tokenClaimSalt` when `isPrivate` (F-005-style fail-closed throw when absent — a `Fr.random()` token secret would strand the deposit against the new `claim_private`); the fuel leg keeps its separate FPC derivation with its own salt.

**Soundness**: the L1 message commits `secretHash = H(poseidon2_ds([salt, R]))`; consumption requires the unique preimage; the message's L2 actor is the new bridge (init-once), the protocol only lets the addressed contract consume its own messages, and the new bridge's only consumer of `mint_to_private` content derives the secret from its `recipient` argument — so any successful consumption mints to an `R'` with `poseidon2_ds([salt', R']) = secret`, a targeted second preimage for `R' ≠ R`. Rests on: (A1) poseidon2 collision/second-preimage resistance AND `computeSecretHash` (the outer poseidon2-based hash the message actually commits to) second-preimage resistance — Leg C confirmed the binding is preimage-resistant (~2^254), attacker controls both `salt'` and `R'` but must still hit a fixed target; (A2) no bearer entrypoint exists or is ever reintroduced (documented invariant + Phase 2 sole-consumer static check + Phase 9 audit — Leg C verified exactly two `consume_l1_to_l2_message` sites today); (A3) protocol message addressing; (A4) domain separation (≠-asserts in keystone); (A5) client derivation correctness at deposit time (keystone vectors + claim-time assert + sandbox smoke + live canary gate). Client MUST bind `recipient` = the connected wallet: post-(c) the recipient is fixed at DEPOSIT time, so a wrong/unspendable recipient strands PERMANENTLY (no claim-time correction, unlike today — fresh-audit LOW). **The security does NOT come from secret secrecy** — the relayer can compute the derived secret; it has nowhere to spend it that redirects funds. It **cannot redirect, self-claim, or steal**. It CAN (fresh-audit HIGH-1): (i) consume the message early, nullifying it — which kills the user's self-claim path and, IF I4 holds (third-party mints need `registerSender` to be discovered), leaves the mint in a note the recipient can't find until they register the relayer — a withholding/support lever, recoverable but not "cannot ransom" as an earlier draft overstated; (ii) deanonymize — it learns `recipient↔amount↔leaf` and, if shared across users, links their L1 addresses to leaves. Both are inherent to delegating a claim; the derivation guarantees only no-theft, and the mitigation is "trusted relayer or none" (Phase 8 docs; malicious-relayer griefing is in T1).

### (a)+(b) Single Permit2 path — deletion blast radius (verified)

| Surface | Today | After |
|---|---|---|
| `packages/bridge-core/src/flows.ts:60-135` (`runDeposit`, `depositPublic/Private`) | mint→approve→portal; random secret; leaf via Inbox `MessageSent` | `runRouterDeposit`: mint→Permit2 witness sign→`router.bridge()`; private secret derived; leaf via router `Bridge` event; `RecoveryHooks.onSecret` gains `saltHex`; bearer-language rewritten (salt loss strands; salt leak no longer redirects) |
| `packages/bridge-core/src/l2.ts` `claimPrivate` | raw secret | `claim_salt`; plus `submitPrivateClaim` usable by a third-party sender |
| `packages/bridge-core/src/router-abi.ts` | `bridgeWithFuel` only | + `bridge` + `Bridge` event (pinned by `router-abi.test.ts`) |
| `apps/faucet/src/composables/useDeposit.ts:849-926` non-fuel branch | approve + direct portal | Permit2 allowance assert (AZLO pre-approves; assert anyway, fail-closed) + witness sign + `router.bridge()`; private secret at `:637` → salt+derived; `TokenPortalAbi` import deleted |
| `apps/faucet/src/composables/useFuel.ts:151-169` | approve + direct `FeeJuicePortal.depositToAztecPublic` | one-time fee-asset `approve(Permit2, max)` (allowance-skip + fail-closed read-back) + witness sign + `router.bridge(tokenPortal = FUEL_PORTAL, routerIsPrivate = false)` — the ROUTER param is always false (FJ deposits are always public at L1); the RECORD's `isPrivate` stays meaningful (private fuel lands at the FPC with its derived secret, unchanged). Leaf still from the portal's `DepositToAztecPublic` event (`fuel.ts:67-74`, present in the router-tx receipt) — fuel indexing parses the portal event, never the router `Bridge` event alone |
| `packages/bridge-core/src/flows.ts:271-373` (`runSwapBridge`) | private token secret = `Fr.random()` (`:294`) | required `tokenClaimSalt` param when `isPrivate`, fail-closed throw when absent (F-005 pattern); `fuel-testnet.ts` private leg + `swap.test.ts`/`flows.test.ts` updated |
| `apps/faucet/src/lib/bridge-steps.ts` | approve vs sign keyed on `fueled` | SIGN for all token records (`schema: 3`); fuel-only records approve(skippable-once)+sign; pre-cutover records keep their historical layout |
| `packages/bridge-core/scripts/deploy-sandbox.ts` smoke legs | direct portal | router path (script already etches Permit2 + deploys the router) + relayer-submitted claim leg |
| `packages/bridge-core/scripts/{deposit-testnet,smoke-existing-testnet}.ts` | direct portal (+ a `simulateContract` preflight) | router path; `smoke-existing` gains `--private` (the candidate gate); `deposit-testnet` fate = Ask A-4 |
| Extension | no token-bridge usage (grep-verified) | no change |

Client-side fail-closed guards for (b): fuel-only plans assert the WITNESS field `routerIsPrivate === false` — NOT the record's `isPrivate`, which stays meaningful (private fuel lands at the FPC with record `isPrivate: true`; only the L1 router-deposit privacy is always public). An `routerIsPrivate=true` witness would revert atomically on the FJ portal's missing `depositToAztecPrivate` — the assert just fails earlier. The existing `UNDERLYING()` cross-check stays. (Final-codex MED: do NOT implement this as `isPrivate === false` — that would break private fuel-only.)

---

## Phases

> Fork gates need `SEPOLIA_RPC_URL`; live phases need funded `PRIVATE_KEY` (Sepolia) and a provable Aztec testnet account — the agent surfaces a blocker instead of proceeding if these are absent. Per L9, nothing from Phases 3–5 ships to the live faucet before Phase 7's single-release promotion.

### Phase 1 — L1 ground truth: fuzz suite + `bridge()` fork legs (zero Solidity source changes) ✓
`[✓ 2026-07-06]` 5 fuzz targets + 7 fork legs green; **I1 + I2 upgraded inference→verified** (deployed-router `bridge()` selector probe + real-FeeJuicePortal fuel-only leg both PASS on live Sepolia). 3 pre-existing `DeployFuelLive.fork` live-drift failures noted (untouched file, moot under WIPE). LESSONS_FILE=implementations-plan/bridge-permit2-recipient-commitment/lessons/phase-1.md
1. New `contracts/bridge/evm/test/SwapBridgeRouterFuzz.t.sol` — the five targets in §Fuzz.
2. Extend `test/SwapBridgeRouterPermit2Fork.t.sol`: `bridge()` vs REAL Permit2 — happy public/private, per-field witness tamper, nonce replay, expired deadline (mirror the `bridgeWithFuel` legs).
3. New fork leg (real-data integration for (b)): mint fee asset via the live `FEE_ASSET_HANDLER`, `approve(Permit2)`, `router.bridge(tokenPortal = canonical FeeJuicePortal)` — the fuel-only shape against the real portal.
4. **Required live-bytecode gate** (codex cond. 1 — not a PR note): a fork test with a POSITIVE probe (fresh-audit L4) — `cast call <router> "bridge(...)" <zero-amount params>` must revert with `"SwapBridgeRouter: zero amount"` (selector present + reached the body) vs an empty revert (absent); a bare cast can't distinguish. Plus the fuel-only shape executed against the real FeeJuicePortal. A local-router-only pass is insufficient — the whole plan (I1) rides on the deployed bytecode having `bridge()`.

**Gate**: `cd contracts/bridge/evm && forge build && forge test` (all green, fuzz runs visible, incl. the 5 fuzz targets) · `SEPOLIA_RPC_URL=… forge test` **with an assertion that the NAMED fork legs PASSED** (grep the forge output for the new leg names — `forge test` exits 0 when `SEPOLIA_RPC_URL`-gated legs `vm.skip(true)`, so bare exit-0 is NOT proof; fresh-audit H3, verified at `SwapBridgeRouterPermit2Fork.t.sol:108-110`). Layers: L1 unit + fuzz + fork (real Permit2/V4/FJ-portal + live router bytecode).

### Phase 2 — L2 recipient-commitment + cross-toolchain keystone ✓
`[✓ 2026-07-06]` claim_secret lib + claim_private rewrite + sole-consumer static check + 6/6 keystone + TS keystone; **Noir↔TS derivation byte-matches** (DS 3140354885 + 3 vectors identical in both toolchains); artifact-diff limited to token_bridge. LESSONS_FILE=implementations-plan/bridge-permit2-recipient-commitment/lessons/phase-2.md
1. Snapshot the legacy artifact: commit `packages/bridge-core/legacy/token-bridge.v1.json` (pin for the Phase 8 legacy script).
2. New `claim_secret` lib; wire path-deps; `claim_private` change per spec; module doc states the sole-consumer/non-bearer invariant.
3. Keystone: 3 derivation vectors + 3 secretHash vectors (L6 literals) as Noir tests; existing content-hash pins MUST stay byte-identical (that is the proof the portal layer didn't move).
4. Recompile via `contracts/bridge/aztec/scripts/compile.sh`; commit artifacts (a `token_minter_proxy` artifact diff = toolchain drift = STOP).
4b. **Sole-consumer static check** (codex cond. 3): a scripted assertion (grep/AST) that `token_bridge/src/main.nr` has exactly two `consume_l1_to_l2_message` callsites (one per claim) and every private-mint path routes through `derive_claim_secret` — the A2 invariant becomes machine-checked, not review-only.
5. `packages/bridge-core/src/claim-secret.ts` + `claim-secret.test.ts`: same vectors, DS re-derivation-from-string, DS ≠ {3952304070, 4199652938}.

**Gate** (all nargo/compile steps use the pinned rc.2 `aztec-nargo` per `contracts/bridge/aztec/scripts/compile.sh` — default nargo fails on the aztec macros): sole-consumer static check green (step 4b) · `bash contracts/bridge/aztec/scripts/compile.sh` (the compile-proof; `token_bridge` has no `#[test]`s) · a `token_bridge` TXE test for `claim_private` happy-path + wrong-recipient-reverts IF the aztec-nr TXE harness supports L1→L2 message injection — otherwise this gate proves **derivation + compile + sole-consumer only**, and CONSUMPTION-wiring correctness (a transposed `consume_l1_to_l2_message(content_hash, secret, …)` or mis-passed `recipient.to_field()`) is explicitly deferred to the Phase 4 sandbox (fresh-audit M3) · `cd contracts/bridge/aztec/keystone && nargo test` · `cd contracts/bridge/aztec/token_minter_proxy && nargo test` · artifact-diff check: `git diff --stat` limited to `token_bridge` (+ keystone) artifacts — a `token_minter_proxy` artifact diff = toolchain drift = STOP · `bun run --cwd packages/bridge-core test` · `bun run --cwd packages/bridge-core typecheck` · `bun run lint`. Pass: vector literals byte-identical across Noir and TS; content-hash pins unchanged. Layers: Noir unit + TS crypto + cross-toolchain pins.

### Phase 3 — bridge-core: single router path, salt semantics, relayer script
Per the blast-radius table: `router-abi.ts` + pin; `flows.ts` rewrite + hook/docs semantics; `runSwapBridge` gains the required `tokenClaimSalt` (F2 — fail-closed when `isPrivate`); `l2.ts` salt + `submitPrivateClaim`; `fuel.ts` router-path builder + `routerIsPrivate===false` assert; new `scripts/relay-claim-testnet.ts` (dedicated account, sponsored FPC, asserts recipient balance from the recipient's wallet); testnet scripts rewired (incl. `fuel-testnet.ts`'s private leg on the new salt semantics); `flows.test.ts`/`swap.test.ts` rewritten (router path, salt injection, event-missing failure; F-005 rejection tests preserved + the new tokenClaimSalt rejection).

**Gate**: `bun run --cwd packages/bridge-core test` · `bun run --cwd packages/bridge-core typecheck` · `bun run typecheck:all` · `bun run lint`. Pass: no direct-portal deposit write remains in bridge-core (grep BOTH `depositToAztecPrivate` AND `depositToAztecPublic` `writeContract` usages → ABI/event-parser/comment refs only); **stale-semantics check spanning `packages/bridge-core/{src,scripts}` + `apps/faucet/src`, covering ALL FIVE `claim_private` sites** (`l2.ts:56`, `fuel-testnet.ts:271`, `deploy-sandbox.ts:292`, `useDeposit.ts:381` fueled-private, `useDeposit.ts:476` non-fuel — fresh-audit M1) asserts each passes a salt, never a raw `secret`; per-`assetKind` DS test green (L14); ABI pin green. Layers: TS unit + ABI↔bytecode pin.

### Phase 4 — full-system local proof: sandbox smoke (a+b+c together)
Extend `deploy-sandbox.ts --smoke`: public + private deposits via `router.bridge()`; private claim via salt; **relayer leg** — `claim_private` submitted from a second account, asserting sender ≠ recipient AND that the recipient discovers the note from its OWN PXE (the I4 note-discovery pre-verification — a HARD gate before the live cutover; if it needs `registerSender`, pre-write that fallback here, fresh-audit M4); fuel-only leg via `router.bridge(tokenPortal = sandbox FeeJuicePortal)`; withdraws unchanged. This is also the FIRST proof of consumption-wiring correctness (Phase 2 deferred it).

**Gate**: `bun run --cwd packages/bridge-core deploy:sandbox --smoke` against a running sandbox — all legs PASSED. Layers: full local stack (anvil + real Permit2 bytecode + router + fresh portal + L2 trio + PXE); first end-to-end proof of the new circuit consuming a real message.

### Phase 5 — faucet: single-path UI, approve-once fuel UX, salt sealing, required config
Per the blast-radius table: `useDeposit.ts` / `useFuel.ts` / `bridge-steps.ts` (+ `schema: 3`); claim dep passes `envelope.salt` + the pre-send re-derivation assert; **`bridge-deployments.ts` promotes router+permit2 to REQUIRED bridge config** (they currently live under the optional `l1.fuel` block, but bridge-only now depends on them — C7); **the deposit-time fail-closed refusal CODE**: private deposits refuse unless the active manifest declares `privateClaimMode: "salt-v2"` (L9 interlock, codex cond. 2). **CRITICAL ORDERING (final-codex HIGH-1): this phase adds only the READER code — the `privateClaimMode: "salt-v2"` VALUE is written ONLY into the CANDIDATE manifest in Phase 6, NEVER into the live `testnet-bridge.json` before promotion.** Writing it to the live manifest here (while it still points at the old bearer bridge) would defeat the interlock — old-bridge deposits would pass the salt-v2 check and strand. So during Phases 5→7 the live manifest lacks the flag and the dev faucet's private deposits fail closed by design. Plus a client-pin test asserting `tokenPortal`/`bridgeToken`/`router`/`permit2` come only from the manifest, never from caller input (codex cond. 4 phishing boundary); copy: "one signature — no allowance needed" / "first time: approve once, then sign". Testids preserved; new steps get new testids. (The L10 manifest-verifier extension lands in Phase 6 against the CANDIDATE manifest — extending it here against the old live manifest with the new artifact would self-DRIFT, F1.)

**Pre-Phase-3 operational blocker (final-codex HIGH-1)**: BEFORE any derived-secret code is pushed to a branch Cloudflare Pages builds, confirm branch/preview deployments are disabled or access-gated for the faucet project (no `wrangler.toml` today → CF default is a public per-branch preview URL). The salt-v2 interlock makes a preview build fail closed on deposit, but this removes the phishing/confusion surface entirely. Record the confirmation in `lessons/phase-3.md` before starting Phase 3.

**Gate**: `bun run test:faucet` · `bun run audit:faucet` (typecheck:all → tests → lint → verify:deployments → build). Pass: stepper tests cover token±private, fuel-only, fueled; no direct-portal write remains in faucet src. Layers: UI unit/component + build + deployment self-check.

### Phase 6 — testnet deploy (candidate manifest; nothing user-visible)
1. Deploy-script change: `EXISTING_L1_TOKEN` env skips the fresh `MintableERC20` (read-back asserts symbol/decimals/cap against AZLO `0x457f…d389`); carry the existing `l1.fuel` + `l1.feeJuice` config blocks into the candidate verbatim.
2. **Extend `apps/faucet/scripts/verify-deployments.ts` with the bridge-manifest check** (L10, moved here per F1): takes an explicit `BRIDGE_MANIFEST=apps/faucet/public/testnet-bridge.candidate.json` (codex cond. 5 — the script currently hardcodes the live path), rebuilds the bridge instance from the new artifact, asserts against that manifest, fails when router/permit2 are absent (C7), and asserts `privateClaimMode: "salt-v2"` is present.
3. Run the journaled deploy (needs `PRIVATE_KEY`, `SEPOLIA_RPC_URL`): fresh NuloTokenPortal (reviewed bytes + drift alarm) → L2 proxy → token(minter=proxy) → bridge(proxy, portal) → `portal.initialize(registry, AZLO, bridge)` → `set_token` → one-shot `set_bridge` → read-backs → **`testnet-bridge.candidate.json` only, and the candidate is the ONLY place `privateClaimMode: "salt-v2"` is written** (the L9 interlock's value lives with the new stack, never the live manifest before promotion — final-codex HIGH-1).

**Gate**: `bun run --cwd packages/bridge-core typecheck` + `bun run --cwd packages/bridge-core test` (the deploy script changed — C6) · pre-deploy `SEPOLIA_RPC_URL=… forge test` **with the named-fork-leg-PASSED assertion** (same grep-the-output check as Phase 1 — a bare exit 0 hides skipped rehearsals; final-codex HIGH-3) · deploy completes with every read-back `✓` · `BRIDGE_MANIFEST=…candidate.json` verifier green (incl. the `privateClaimMode` presence check) · candidate committed with the journal · live `testnet-bridge.json` untouched AND still lacks `privateClaimMode`. Layers: TS unit + live L1+L2 deploy (no user traffic).

### Phase 7 — candidate canaries → drain window → promotion (the cutover)
Order (cheapest-fail-first), all against the candidate manifest:
1. `smoke-existing-testnet.ts --config candidate` — public bridge → `claim_public`.
2. Same, `--private` — **the strand-risk gate**: real money against the in-circuit derivation; minimal amount first, self-claimed.
3. Relayer sequence on ONE fresh private deposit, in this order (C1 — wrong-recipient before consumption): (i) `relay-claim-testnet.ts --wrong-recipient` — **submits `claim_private(recipient=WRONG, salt, amount, leaf)` DIRECTLY to the sequencer, bypassing the deposit-time client re-derivation guard** (fresh-audit H2 — otherwise it dies at the client assert and proves the guard, not the CIRCUIT); asserts the tx REVERTS and the message is still claimable; (ii) same script, correct recipient from the RELAYER account — settles, recipient balance asserted from the recipient's OWN wallet (F-007 closure + the I4 note-discovery HARD blocker).
4. `smoke-swap-existing-testnet.ts --config candidate` — `bridgeWithFuel` PUBLIC against the NEW portal value.
5. `fuel-testnet.ts` private leg — `bridgeWithFuel` PRIVATE: derived `tokenClaimSalt` token leg + FPC fuel leg (F5 — the only pre-promotion exercise of the fueled-private branch).
6. Fuel-only canaries — public (first-time approve-once + sign + FJ claim) and private (FPC-bound, record `isPrivate: true` — C3).
Then, **`[WIPE]` — amended per sign-off** (no old-state preservation): 7. freeze the old faucet build (stop new old-stack deposits), then promote: (i) `cp` candidate → `testnet-bridge.json` (**including its `privateClaimMode: "salt-v2"`**); (ii) `bun run audit:faucet`; (iii) ship faucet + code as ONE release (L9); 8. post-promotion manual UI canaries (public, private, fueled-private, fuel-only). ~~drain window / v1 snapshot / 7b v2 recovery path~~ **DROPPED (WIPE)** — the final-codex HIGH-2 v2-rollback path and codex cond. 5 v1-snapshot ordering existed only to keep old/new deposits claimable across the cutover; with nothing to preserve, rollback = re-point config + redeploy, no stranding possible. The salt-v2 write still goes ONLY into the candidate→promoted manifest (never a pre-promotion live write — the interlock stays).

Canary safety: every canary uses DUST amounts; a failed candidate is ABANDONED (dust written off), never promoted. `[WIPE]` The pre-deposit claim-material persistence (for recovering a failed-candidate deposit) is now optional — under WIPE a stranded dust deposit is simply discarded.

**Gate**: all scripted canaries settle with L2 balance asserts (not just inclusion); the wrong-recipient attempt fails WITHOUT consuming; the relayer canary's recipient sees the balance from its OWN wallet (I4 note-discovery blocker); `audit:faucet` green on the promoted config (manifest verifier gates the promoted manifest — F1). Layers: live-network e2e, real proofs.

### Phase 8 — forward-looking durability + docs   `[WIPE — shrunk]`
The legacy-preservation half of this phase is DROPPED per the WIPE sign-off (~~v1 legacy claim script, v2 rollback script, rollback drill, stale-deployment support~~ — nothing to preserve). What remains is the durability the NEW stack needs going forward, plus honest docs:
1. **Backup/recovery durability test** (final-codex MED — still relevant: real deposits on the new stack need recoverable backups) — a bridge-core vitest (`bun run --cwd packages/bridge-core test`) that seals a fueled-private record and asserts the TOKEN salt round-trips through the sealed `DepositEnvelopeV2.salt` blob (recoverable), and DOCUMENTS that its FUEL salt is plaintext-journal-only (`journal.bridgeSecretSalt`, not in the sealed blob) — so the docs scope "the sealed blob is the strand-prevention credential" to the token salt, not the fuel salt.
2. Docs in the same PR: red-team report F-007 status ("fixed on the new deployment via recipient-commitment"), F-003 still-open (CI deferred), **INFO-1 + the generic-router phishing surface carried forward as a value-token hard-blocker AND as the A-1 on-chain-portal-binding follow-up** (both bridge + fuel paths), bridge READMEs, `flows.ts`/`recovery-crypto.ts` bearer-language rewrite (the sealed token-salt = strand-prevention + linkage-privacy credential, NOT shareable), and the honest "relayer capability is the driver, not F-007" framing.

**Gate**: `bun run lint` · `bun run typecheck:all` · `bun run test:faucet` · the backup-durability test green. Layers: durability + docs.

### Phase 9 — focused red-team re-audit (final)
Scope (changed surface only; output `audit/security/<date>-recipient-commitment/`): `claim_secret` + `claim_private` (sole-consumer invariant A2, separator usage, u128/Field edges); client salt lifecycle (generate → seal → re-derive assert; no plaintext logging); single-path router wiring incl. deleted-path residue; the L1 witness-reuse aliasing argument re-attacked; deploy/cutover artifacts; relayer script key handling.

**Gate**: report with fix-or-accept triage for every finding; any fix lands with its own test; full stack re-run green: `SEPOLIA_RPC_URL=… forge test` **with the named-fork-leg-PASSED assertion** (final-codex HIGH-3 — not a bare exit 0) · both `nargo test` · `bun run --cwd packages/bridge-core test` · `bun run audit:faucet` · `bun run lint && bun run typecheck:all`. High/Critical findings fixed or explicitly user-accepted.

---

## Fuzz strategy (five targets; exclusions are decisions, not omissions)

`contracts/bridge/evm/test/SwapBridgeRouterFuzz.t.sol`, mock-based (fork stays deterministic):
1. **`test_fuzz_witnessTamperChangesHash`** — mutate exactly one of the 12 `BridgeWitness` fields; hash must change. The witness IS the security boundary; a field that hashes-but-doesn't-bind is invisible to fixed-vector pins.
2. **`test_fuzz_bridgeAccounting`** — `bridge()` over amount ∈ [1, 2^128]: portal receives exactly `amount`, zero token/allowance residue, `Bridge` event echoes inputs; zero-amount/zero-portal revert. This entrypoint goes from dormant to carrying 100% of bridge-only + fuel-only volume; residue = owner-sweepable = the theft-shaped failure.
3. **`test_fuzz_fuelSplit`** — `bridgeWithFuel` valid domain (0 < fuel < total): exact slicing, zero residue; invalid domain reverts. Integer edges (fuel = total-1, total = 1) currently covered at two hand-picked points only.
4. **`test_fuzz_hostileSwapConsumption`** — parameterized hostile swap (pulls `consumed`, returns `returned`, transfers `transferred`): succeeds iff `consumed == fuelAmount && returned >= minFuelOutput && transferred >= returned`. Generalizes `MaliciousPrefundSwap` to the full behavior lattice of the owner-replaceable target.
5. **`test_fuzz_hostilePortal`** (codex cond. 4 / finding 8) — `bridge()` with a parameterized hostile `tokenPortal` that returns success without pulling, pulls to an attacker via the router's `forceApprove`, or returns a fake `(key, index)`. Asserts: an honest portal leaves zero router residue and emits a truthful `Bridge` event; the arbitrary-`tokenPortal` call is the newly-hot trust boundary, so the official flow's manifest-pinning is what protects users (the on-chain call itself trusts the caller-named portal — this is the phishing surface, made explicit).

Excluded deliberately: content hashes + claim-secret derivation (cross-toolchain byte-equality pins — a property no single-runner fuzzer expresses); `_hashRoute` injectivity (solc ABI-encoding's contract); Noir TXE fuzzing (no TXE harness in repo; the sandbox smoke + live canary exercise the real circuit against real messages — stronger evidence for the strand risk in question).

---

## Security & Adversarial Considerations

**Threat model.** (T1) malicious relayer holding `(salt, recipient, amount, leaf)`; (T2) observer of L1 calldata/mempool; (T3) hostile dApp getting a Permit2 witness signed; (T4) router owner (hot EOA — residue + `setSwapTarget`); (T5) L1 MEV/front-runner; (T6) future maintainer reintroducing a bearer entrypoint. Surfaces: Permit2 signatures, router entrypoints, the L1→L2 message/secret scheme, L2 claims, client persistence, cutover config.

- **Bearer-signature intent theft (T3 — fresh-audit HIGH-2, distinct axis)**: a user phished into signing a VALID `BridgeWitness` naming the ATTACKER's `aztecRecipient`/`tokenSecretHash` → the router faithfully bridges the user's tokens to the attacker's L2 account. Witness binding proves integrity (the tx matches the signature), NOT intent (that the signature was for the user). This is a NEW vector vs today's bridge-only path (approve + `depositToAztecPublic` pulls from `msg.sender` — no bearer signature exists), and AZLO's max-Permit2 pre-approval (`MintableERC20.sol:47-50`, red-team INFO-1) removes the approve gate, so ONE blind `signTypedData` drains AZLO. The new one-time `approve(Permit2, max)` for fuel-only likewise makes the canonical fee asset standing-Permit2-drainable. Testnet-valueless → accepted (A-1), but stated plainly and a **HARD blocker for any value-token port** (INFO-1 carried into the Phase 8 docs).
- **Generic-router portal phishing (T3 — codex cond. 4, HIGH)**: `bridge()` accepts an arbitrary `tokenPortal` and the router `forceApprove`s it before calling (`SwapBridgeRouter.sol:220-227`, `:271`), so a malicious portal named in the witness can `transferFrom` the just-pulled tokens. This is intent-phishing, NOT a calldata-integrity break: witness binding proves the tx matches what was signed, but a user phished into signing a witness that names a hostile portal loses the signed amount — exactly as they would signing any malicious approval. Mitigations: the official faucet hard-pins `tokenPortal`/`bridgeToken`/`router`/`permit2` from the manifest (client-pin test, Phase 5); `test_fuzz_hostilePortal` (Phase 1); documented worst case. The only *on-chain* mitigation is a hardwired/allowlisted portal — noted as a future-router item, out of scope here (the old direct-portal path had the identical trust shape). A-1 accepts this for testnet.
- **Witness tampering (T1/T5)**: every executed field is witness-bound and re-derived from calldata by the router; Permit2 verifies. Fork tamper legs per entrypoint; fuzz target 1. A relayed signed payload executes verbatim or not at all. (Distinct from phishing above: this is integrity, that is intent.)
- **Replay/reuse (T1/T5)**: Permit2 nonce bitmap (fork-tested), deadline, domain binds chainId+Permit2, spender binds router. Cross-entrypoint: witness spaces disjoint on `fuelAmount`. Fuel/bridge aliasing (L1 ledger): identical execution, no privilege delta.
- **Front-running claims (T2/T5)**: public — bound recipient (unchanged). Private — after (c), a leaked salt only triggers the mint to the bound recipient: the bearer window closes by construction. The salt downgrades to linkage-secret + liveness-key.
- **Cross-protocol secret reuse (T1/T6)**: three separators, ≠-asserted in keystone. A salt accidentally reused across fuel and token deposits yields different secrets.
- **Reorg (L1)**: leaf index read from mined receipts (never simulated); a reorg invalidating the index is re-extractable from the canonical chain via the persisted `depositTxHash`; claim retry tolerates not-ready. Unchanged surface from today; Sepolia-depth reorgs accepted (testnet).
- **Least privilege**: no new privileged roles; router owner unchanged (`Ownable2Step`; residue fuzz-asserted zero on the newly-hot path); new L2 trio reproduces the F-002 posture (owner has zero standing authority post-wiring); relayer holds no privilege — that is the feature; deploy key = testnet-only funds.
- **Input validation**: router — amount/portal/fuel-domain requires (fuzz-covered); L2 — amount > 0, `!recipient.is_zero()`, pause, protocol message membership + nullifier; client — fail-closed: Permit2 allowance read-backs, fuel `routerIsPrivate===false` (the WITNESS field, NOT record `isPrivate` — see the (b) guard note), claim-time re-derivation, FPC pin (existing), fuel floor (existing); deploy — per-step read-backs + candidate-never-live discipline.
- **T6 regression**: sole-consumer invariant documented at the entrypoint, Phase 9 audit check, structurally visible (any new `consume_l1_to_l2_message` call site is a one-file diff).
- **Cryptography**: poseidon2 via `@aztec/foundation` (pinned 5.0.0-rc.2) and aztec-nr's `poseidon2_hash_with_separator` — battle-tested protocol primitives; nothing hand-rolled. The 32-bit domain separator is a **namespace tag, not a cryptographic boundary** (the field hash provides the security); the exact-string re-derivation test keeps it honest (codex finding 10). Supply chain: zero new npm deps; canonical deployed Permit2. **Noir git-tag deps are not a cryptographic pin** (no `Nargo.lock` — codex finding 7): the Phase 2 artifact-diff + compile-provenance check catches drift post-compile but not provenance; committing a Noir lockfile / pinning by rev is recorded as a follow-up (deferred like F-003, not silently accepted).

## Assumptions

**Facts** (all verified against the repo this session):
- `bridge()` exists witness-bound at `contracts/bridge/evm/src/SwapBridgeRouter.sol:244-284`; SignatureTransfer-only (`:307-326`); witness zeroes fuel fields (`:258`) vs `bridgeWithFuel` requiring `fuelAmount > 0` (`:155`).
- Bearer `claim_private` at `contracts/bridge/aztec/token_bridge/src/main.nr:104-122`; private content hash omits recipient (`main.nr:114`, `upstream/NuloTokenPortal.sol:116`); content-hash lib is the upstream git dep (`token_bridge/Nargo.toml`).
- One-shot wiring: `set_bridge` (`token_minter_proxy/src/main.nr:39-44`), portal init-once (F-001 fork) ⇒ fresh L2 trio + fresh portal instance required.
- Derivation precedent + pinned-literal discipline: `packages/bridge-core/src/private-fuel.ts:27,52-53`; keystone pattern `private-fuel.test.ts:42-47`; `DOM_SEP__FPC_BRIDGE_SECRET` re-derives to 3952304070 (re-verified live this session); L4/L6 DS + vectors computed with the real crypto this session.
- Envelope `salt` field exists (`recovery-crypto.ts:104-116`); journal pins `chainId/portal/bridge` (`journal.ts:53-56`); `stale-deployment` quarantine (`useBridgeJournal.ts:283-300`); claim path uses config `BRIDGE`, not `rec.bridge` (`useDeposit.ts:309`).
- Deploy script already: candidate manifest + write-ahead journal + `--from-journal` hard-stop; deploys a fresh L1 token today (`deploy-bridge-testnet.ts` header, `:170-171`).
- Sandbox script etches Permit2 + deploys the router (`deploy-sandbox.ts:1-14`).
- `verify-deployments.ts` does NOT check `testnet-bridge.json` (`apps/faucet/scripts/verify-deployments.ts:14-37`).
- Live config: router `0x4c3f…4068`, AZLO `0x457f…d389`, portal `0x96de…f864` (forked-v1), FJ portal `0xb06a…0a3a` (`apps/faucet/public/testnet-bridge.json`). (That the DEPLOYED router bytecode contains `bridge()` is an INFERENCE, I1 — the source was redeployed at rc.2 bump `bffb757` from a tree containing `bridge()`, but bytecode ≠ source; Phase 1.4 gate proves it on-chain — codex finding 11.)
- `ITokenPortal.depositToAztecPublic` and `IFeeJuicePortal.depositToAztecPublic` are **ABI-identical** (`interfaces/ITokenPortal.sol:9-11`, `IFeeJuicePortal.sol:12-14`) — ABI-only; semantic acceptance is I2, gated by the Phase 1 fork leg + live canary (codex finding 12).
- Foundry suite: 32 tests, zero fuzz, no CI invocation. Extension does not touch the token bridge.

**Inferences** (unverified — audits: attack these):
- I1 (**load-bearing, HIGH**): the deployed router **bytecode** contains `bridge()` (source predates deploy) — Phase 1.4's required live-bytecode gate verifies on-chain before anything rides on it. If false, (a) and (b) both need a router redeploy after all (re-opening L1/L2).
- I2: the canonical Sepolia FeeJuicePortal accepts router-originated `depositToAztecPublic` for arbitrary `to` on the **`bridge()`** path. NOTE (codex finding 13): the live `bridgeWithFuel` proof does NOT transfer — it deposits via the router's IMMUTABLE `feeJuicePortal`, not an arbitrary `p.tokenPortal`. The ONLY proof is the new Phase 1 `bridge(tokenPortal = FJ portal)` fork leg + the Phase 7 fuel-only canary.
- I3 (**strengthened — fresh-audit MED-1**): `poseidon2_hash_with_separator` is available to CONTRACT code in aztec-nr 5.0.0-rc.2 and byte-matches TS `poseidon2HashWithSeparator([salt, recipient])` incl. `AztecAddress.to_field()` serialization. NOTE: there is NO in-repo Noir precedent for `poseidon2_hash_with_separator` (the private-fuel precedent is a TS↔Wonderland-EXTERNAL match, a different code path). If the import path is wrong or the serialization diverges, EVERY private deposit strands — but the Phase 2 keystone vectors are hardcoded expected values asserted IN Noir, so this is fail-closed at Phase 2. The DS `3140354885` + the L6 vectors are **TS-verified three times this session but Noir-side gate-ENFORCED** (not runnable without Barretenberg) — treat them as gate-enforced, not settled fact.
- I4 (**HARD promotion blocker, codex finding 14**): recipient-side note discovery works for relayer-submitted mints. The Phase 4 sandbox + Phase 7 relayer canary assert the recipient sees the balance from their OWN wallet; if they can't, **promotion STOPS** until the real fix is known — which may be more than a documented `registerSender` step (possibly recipient/relayer wallet integration work), not a redesign of the derivation but not assumed trivial either.
- I5: `token_minter_proxy` recompiles byte-stable (source unchanged); a diff = toolchain drift = Phase 2 STOP.
- I6: the faucet production site only changes at `main` promote (CF Git-integration), so the dev-side window between Phase 5 and Phase 7 is user-invisible — L9 makes this a hard rule rather than a hope.

**Asks** (to resolve at the approval gate):
- **A-1**: Ratify ledger L1 — fuel-only reuses `bridge()` (zero new Solidity, no router redeploy, no new witness type), **AND with it accept the generic-router phishing surface** for testnet (a hostile dApp could get a user to sign a witness naming a malicious `tokenPortal`; on-chain the router trusts the caller-named portal — the faucet mitigates by manifest-pinning). The alternative (dedicated `fuel()` + FuelWitness + router redeploy + a hardwired portal) shrinks that surface for the fuel path but costs a redeploy + a second witness surface forever, and does NOT close it for the bridge path.
- **A-2**: Legacy claims UX — pinned script + `stale-deployment` note sufficient (recommended), or keep an old faucet build reachable at a versioned URL?
- **A-3**: Confirm the extension-side consequence: new L2 token ⇒ users re-add it; old L2 AZLO balances stay on the old token (exitable via legacy script). Same shape as an aztec-update network reset.
- **A-4**: `deposit-testnet.ts` — rewire to the router path (default) or retire it in favor of the overlapping `smoke-existing-testnet.ts`?
- **A-5**: Drain-window length + announcement channel — AND (codex finding 18): does the window **freeze/disable old deposits**, or merely announce while old deposits stay open (in-flight legacy volume keeps growing until promotion)? Recommend freeze-at-announce on the old faucet build.
- **A-6**: Relayer canary account — dedicated fresh key, sponsored-FPC fees, bounded funding, never the deploy key, **and never logs salts/secrets** (codex findings 6/19) — recommended.
- **A-7**: Confirm `claim_salt` as the new `claim_private` parameter name — it lands in the committed artifact ABI (F4).

## Migration / cutover risks

**`[WIPE]` note**: the sign-off (only testnet infra, no prior state matters) neutralizes most of R1/R5/R7 — there is nothing to preserve, so stranding old deposits is a non-event. The risks are kept below for the trail with their WIPE status.

R1 in-flight records — **MOOT (WIPE)**: old records are discarded, not preserved; `stale-deployment` quarantine still fires harmlessly. R2 derivation-drift strand — **LIVE, the real risk**: defense in depth (shared lib, dual-toolchain vectors, claim-time assert, sandbox, minimal-amount live canary as hard gate) — this protects the NEW stack's real deposits going forward. R3 partial deploy (write-ahead journal + `--from-journal` hard-stop; one-shot `set_bridge` is why). R4 fresh-L1-token trap (L7 reuse of AZLO + pools is still the efficient choice — the WIPE is of L2 STATE, not the L1 token/pools). R5 old L2 balances — **MOOT (WIPE)**: discarded by decision. R6 relayer note discovery (I4, canary-asserted, hard promotion blocker). R7 rollback — **SIMPLIFIED (WIPE)**: pre-promotion free (candidate is additive); post-promotion = re-point config + redeploy, no stranding possible (nothing to preserve) → the whole "keep both stacks claim-live / v2 recovery path" apparatus is dropped. R8 fee-asset approve UX (one-time, mainnet-standard; allowance-skip keeps steady-state at one signature). R9 CF Pages previews (fresh-audit HIGH-1): a branch preview of Phases 3–5 code reads the live manifest lacking `privateClaimMode: "salt-v2"` → the interlock (L9) makes the deposit code refuse (fail-closed); the salt-v2 interlock is KEPT (cheap, and the code↔config coupling documents intent for the eventual value-token version). Under WIPE the residual is dust, so the pre-Phase-3 CF-preview confirmation is advisory, not a hard blocker.

## Post-implementation

Per protocol: `/code-review max --fix` (separate commits) → codex post-impl audit (net diff + code-review commit summary + adversarial ask) → fix loop. Hardening: the focused re-audit IS Phase 9 (user-selected); no `/harden` scheduled. Contracts CI (F-003) deferred to a follow-up plan by user decision — recorded, not forgotten.

## Audit verdicts

- Contradiction-check (codex, resumed session `019f34a5`): `contradictions-found (8)` — all folded (C1–C9 above; C10 re-verified the crypto pins)
- Contradiction-check (fable, resumed planner): `contradictions-found (8)` — all folded (F1–F8 above; it also re-verified the crypto pins byte-exact)
- Double audit (codex, resumed `019f34a5`): `conditional approve` (26 findings, 5 conditions) — all folded (see the double-audit subsection above).
- Double audit (fresh fable ×2, independent spawns): both `conditional approve` — the two auditors cross-checked (Leg C corrected Leg B on the salt-field question) and all findings folded. Both independently confirmed the (c) crypto core sound, A2 sole-consumer structural, witness-space disjointness real, and the (b) ABI-identity bet real.
- Final fresh-context codex (NEW session `019f34d8`): initial `reject` (3 HIGH prose-not-gate + 2 MED) → folded into executable gates → re-check `reject` (1 HIGH: v2 rollback sequenced post-cutover) → folded (Phase 7b pre-promotion gate) → **`approve`** (one non-blocking fuzz-count nit, fixed). The reject→reject→approve arc is the value of the fresh pass: it forced the audit fixes from prose into gates a `/loop` agent actually hits.

## Seeds (DRAFT — finalized post-approval)

**Recommended: `/goal`** (completion is transcript-observable via gates + plan.md ✓ marks):

```
/goal All phases 1-9 marked ✓ in implementations-plan/bridge-permit2-recipient-commitment/plan.md (the per-phase headers in the file, not just the chat), each ✓ backed by its phase's validation gate (as defined in plan.md) reported passing in the transcript; for each phase the agent has printed LESSONS_FILE=implementations-plan/bridge-permit2-recipient-commitment/lessons/phase-N.md in the transcript; every fork-test gate reported the NAMED fork legs PASSED (not a bare forge exit 0 — skipped legs don't count); the five Phase 7 canaries (public, private self-claim, private relayer-claim + wrong-recipient failure submitted directly to the sequencer, bridgeWithFuel-private, fuel-only) reported settled with balance asserts; the Phase 8 backup-durability test green; /code-review max --fix complete with findings applied and committed separately; codex post-impl audit complete with high/critical findings addressed; `bun run audit:faucet` and `cd contracts/bridge/evm && forge test` and both `nargo test` suites and `bun run --cwd packages/bridge-core test` all report exit 0 in the transcript. The live-deploy (Phase 6) and promotion (Phase 7) steps were gated on explicit user go. NOTE the WIPE amendment: no legacy/rollback-preservation work (v1/v2 scripts, drain window, rollback drill are dropped — Phase 7 = freeze old + flip config, Phase 8 = durability test + docs only).
```

**Alternative: `/loop 15m`**:

```
/loop 15m Drive implementations-plan/bridge-permit2-recipient-commitment forward. Never idle waiting for my input. Each firing: (1) Reality check: read plan.md + lessons/ (authoritative state), `git status`, `git log --oneline -5`; if a PR exists, `gh pr view --json statusCheckRollup`. (2) Waiting on CI is fine if it progresses; use the wait to review the diff or prep the next phase. (3) No task in hand? Pick the next pending step from plan.md; after each meaningful edit run the touched packages' fast gates (bun run lint + the package test); commit → push. (4) Stuck or facing a decision? Call /codex xhigh, reach a defensible verdict, act, log in lessons/phase-N.md. Hard limits stay hard: never merge to main, never publish/deploy the faucet, never run the Phase 6/7 live-deploy or promotion steps without an explicit go in the transcript, never expand scope beyond plan.md. (5) Same step failed 5 times? Stop retrying; reassess with codex. (6) Phase green = ITS VALIDATION GATE as written in plan.md passes; paste the result, mark ✓ in plan.md, file lessons, print LESSONS_FILE=..., advance. (7) All phases ✓? Run /code-review max --fix → commit separately → codex post-impl audit → address high/critical → wrap-up report with every contentious decision explained ELI5 → surface and stop.
```

Use exactly ONE per session — they don't compose.
