# plans-scaffolding: recon

Base: `origin/dev` @ `9f11de70b13933be2d54c3eb79622b1ff2719aba` (2026-09-24, #692). The recon was read-only: four mappers (status, outside refs + non-md assets, untrack set, reusable tooling) and a completeness critic. **The critic wins wherever the two disagree.** ✔ marks a claim re-run against `origin/dev` while writing this file. The per-dir table (253 rows), the link lists and the prototypes sit in the recon scratchpad. They are disposable and some hard-code machine paths, so the plan regenerates them with committed tools on the final base SHA. Paths are repo-relative code spans, not links, so this file survives its move to `archive/`.

## Reuse map

| # | Capability needed | Existing code found (✔ = re-verified), or absence + search trail | Verdict | Why / delta |
|---|---|---|---|---|
| 1 | Per-plan status classification | All inputs are stale: the `index.md` status column (free-form; git or gh contradicts 25 of the 27 lines that read as open), front-matter `status:` (23 of 26) and in-plan status lines ("draft" on plans merged months ago). The only classifier is the recon's scratch script (keywords + gh evidence). Its keyword lists are fitted to today's index text, with false positives at required-check-mismatch and stable-release-0.26.0. No repo tooling: `scripts/**` and `implementations-plan/**/tools` hold no status parser | **adapt** | Commit it as a one-shot tool under `implementations-plan/plans-scaffolding/tools/`. Re-run it on the final base SHA and diff the result against the recon's. It outputs an owner-review table, never a decision (decision 2). |
| 2 | PR / issue extraction | gh wrappers make per-PR side-effect calls only: `scripts/ci-cd/required-checks.ts:78`, `scripts/release/{auto-unstick,open-sync-pr}-run.ts`, `scripts/ci-cd/preview-comment.ts` ✔. None enumerates PRs. The recon snapshot has 652 PRs (615 MERGED, 36 CLOSED, 1 OPEN: #669). PRs and issues share one number space: #58, #59, #279–#285, #293, #301, #312, #334 and #336 are issues. #420, #518, #567, #593, #602, #608, #621 and #638 are `gh stack` ids. #182–#184 and #448 are closed-unmerged by design | **adapt** | Commit a frozen `gh pr list --state all` + `gh issue list --state all` JSON next to the tool and join locally. Only gh state, or a `(#N)` squash subject on dev, proves a merge. |
| 3 | Outcome block generation | Template: `send-publish-ledger/plan.md:15`, the only exact `^## Outcome\s*$` on dev ✔. Its fields: Date, Status, Delivered, Dropped, Gates, "Seeds retired: … spent", Follow-ups. `fix-transport-sessions/plan.md:72` has `## Outcome (B-06/…)` mid-file, which fails the exact regex ✔. No generator: `scripts/**` holds only the landing's legal-page renderer | **build new** (reuse the template) | A one-shot job with no existing emitter to extend. It must be idempotent on the exact regex, insert after the front matter only when `---` is at byte 0 (26 plans ✔), and handle the 3 plans with fenced metadata (connect-chain-mismatch, dapp-popup-cancel-focus, profile-fenced-execution). It must never read `## Outcome & Quality Bar` as closed. |
| 4 | Index split (active vs archive) | `index.md:3` states the skill's format exactly ✔. The #692 index-pruning logic (drops lines by leading dir) lives only in the recon scratchpad, with hard-coded machine paths | **adapt** | New work: synthesize lines for the 76 unindexed dirs from each H1; collapse the 2 duplicate-dir lines (the later wins); drop the L86/L88 section; rewrite each archived status as `closed <date> (#PRs)` rather than copying stale "PR #N open" text. |
| 5 | Link inventory + rewrite to permalinks | Parser: `Bun.markdown.render(md, {link, image})` in Bun 1.4.2 ✔ (skips code spans and fences, gives no line numbers). Precedent: `apps/landing/scripts/legal-pages.ts:119` (`Bun.markdown.html`) ✔. Convention from #691/#692: full 40-hex SHA, `/blob/` for files, `/tree/` for dirs; 15 such links, all at `6611f861` ✔. No markdown library in `bun.lock`. The recon's link prototypes are scratch only | **adapt** | Resolve every target against the **git index** (`git ls-files`), never the filesystem: `git rm --cached` leaves the 667 files on disk, so a filesystem check passes locally and fails in CI. Verify each permalink with `git cat-file -e <sha>:<path>` at rewrite time. |
| 6 | Tracked-but-ignored inventory | Git itself: `git ls-files -ci --exclude-standard -- implementations-plan`. It returned exactly 667 in a scratch repo, and I reproduced 667 ✔ with `check-ignore --no-index`. Pitfall: plain `git check-ignore -v` prints nothing for tracked files | **reuse-as-is** | It is index-based, so it works on CI's depth-1 checkout. |
| 7 | Offline link checker as a CI gate | **Absent.** Searched scripts, .github and `*.test.ts` for lychee, markdown-link-check, linkinator, "link check/integrity/rot", "broken link" and "dead link"; found only prose and the legal-page `href` asserts in `apps/landing/scripts/legal-pages.test.ts`. **Wiring:** `test:ci-gating` = `bun test scripts/ci-cd/` ✔ auto-discovers `*.test.ts`. It runs in `_unit-tests.yml` (15-minute job, depth-1 checkout) ✔, called by `pr-quick.yml:211`, `release.yml:216` and `nightly.yml:161` ✔. **Patterns:** `complexity-baseline.test.ts` `ratchetBase()` returns null on push, schedule and dispatch and fetches `--depth=1` only under a PR ✔ (the model for "network only on pull_request"). `scripts/complexity-baseline/scan.ts` `scanTree` + `parseGrepWithContext` scan the index with git grep ✔. `Bun.YAML.parse` already runs in `behavior-gating.test.ts` ✔ | **build new** `scripts/ci-cd/<plan-tree>.test.ts` | Nothing exists. lychee or markdown-link-check would be a new dependency behind the 7-day min-age gate, with network-fetching defaults, to replace about 150 lines of Bun. Fragments: Bun's heading ids collapse `--` and GitHub's keep it (`#troubleshooting--when-x-fails`, `#branching--merging`), so either skip fragments or hand-write a slugger pinned on those anchors. |
| 8 | Seed retirement | Template: "Seeds retired: the `/goal` + `/loop` seeds below are spent" (send-publish-ledger). Archive-aware `/loop` clauses exist in 2 plans (`send-publish-ledger/plan.md:604`, `popup-escape-closes/plan.md:223`) ✔. Seeds: `/goal` in 104 and `/loop` in 85 top-level plan.md by raw grep ✔; 5 nested plan.md; about 28 non-plan files (`complexity-residue-round-3/goal.md`, `harden-2026-09-remediation/seed.md`, `*/scope.md`, `bun-1.4-adoption/adoption-map.md`, `dedup-ledger/README.md`, …). No tooling | **build new** (part of #3) | Retire seeds with the Outcome line in each dir's Outcome host, never by editing seed text. Copies in published ELI5 Artifacts stay stale; that is acceptable because the path is gone, so `/loop` stops. |
| 9 | lessons.md mining + budget enforcement | No root `lessons.md`. 150 dirs have `lessons/` ✔ (786 files, 4.6 MB). No size gate exists anywhere. `CLAUDE.md` § Skills routes *durable domain* lessons to `.claude/skills/*`, so `lessons.md` takes only cross-cutting gotchas no skill owns, or a pointer to the owning skill | mining **manual**; budget **build new** | The budget is one `byteLength ≤ 8192` assertion in #7's test, with no existing gate to extend. |
| 10 | follow-ups mining | Sources are listed under Numbers. Live-cited: `third-party-notices/follow-ups.md` (`legal/README.md:68`, `packages/third-party-notices/README.md:113`), `network-e2e-required/FOLLOWUP-opts-from-clobber.md` (`dispatcher.test.ts:936`). Open issues ✔: #336, #334, #312, #285, #284 (wallet); #293, #280 (bridge, which belongs to unleashed); #1 (Renovate) | **adapt** | Where an issue exists, point to it instead of restating it. Curate everything else by hand. |
| 11 | Non-md asset relocation | 259 non-md files ✔. Live consumers ✔: 4 KAT imports of `key-model-v2/reference/vectors.json` (`packages/aztec-runtime/src/account/{account-seed-vectors,derivation-vectors}.test.ts`, `packages/wallet-crypto/src/{account-derivation,mnemonic-master}.test.ts`). `scripts/ci-cd/test-soak/cli.ts:39` `BASELINES_DIR` is a dirty-check pathspec (`:343`) that no test pins. `CLAUDE.md:47` runs `isolated-linker-store/tools/phantom-sweep.ts`, which is cwd-only and moves unedited. `packages/wallet-crypto/vectors/` exists (PROVENANCE.md + bip39 json) ✔; `PROVENANCE.md:29` cites the plan-dir vectors ✔ | **adapt** (`git mv` + repoint) | Anything a live consumer reads leaves the archive-bound dirs first. Keep the reference projects out of `packages/wallet-crypto/`: N2 digest-binds its bytes, it has no `files` field, and `packages/**` is Biome-linted with complexity budgets. #669 already moves the soak baselines (H2). |
| 12 | Local-path scrubbing | `scripts/check-no-local-paths.sh` ✔ matches only `/Users/[A-Za-z]\|/home/[A-Za-z]` and exempts all of `implementations-plan`. **Only `.githooks/pre-commit` runs it; no workflow or CI test does** ✔. An uninstalled hook or `--no-verify` skips it. Its pattern misses this driver machine's HOME, which is under `/mnt/`; that prefix has 0 hits on dev ✔ | **adapt** | Add the missing HOME prefix, narrow the exemption to `implementations-plan/archive/**`, and add a CI mirror in #7's test (the complexity-baseline local + CI pairing). First scrub the 16 kept files the narrowing exposes. |
| 13 | `.gitattributes` | 5 eol lines, no linguist ✔. Nothing reads it (git grep over `*.ts`, `*.yml`, `*.sh`) | **reuse-as-is** | Append the 3 standard lines. |
| 14 | Hygiene files | `fee6b4a2` on `feat/ux-1-first-run-wording` ✔ has the exact 5-line `.gitignore` and the `/archive/` `.ignore`. `tools-extraction/.gitignore` (4 patterns, no re-include) overrides the parent's `!**/lessons/**`, as mapper-verified. `vitest-on-bun/lessons/baselines/full/.gitignore` (`*`, `!.gitignore`) must keep winning; #669 moves it | **reuse-as-is** | Delete `tools-extraction/.gitignore` when the parent lands, coordinating with that session, which owns the file. |
| 15 | Moves | `git mv`. A combined untrack + move + Outcome commit has 887 inexact rename sources against the default `diff.renameLimit` of 1000 | **reuse-as-is** | Split untrack, Outcomes and the pure move into separate PRs (H6). |
| 16 | Outside-reference repoint | 216 rows point into 69 plan dirs: LIVE-DOC 89 in 14 files, DOC+RUN 1 (`CLAUDE.md:47`), COMMENT 66 in 52 files, HISTORY 53 in 24, EXEC 7 in 6. Only 24 are rendered links. Also brace expansion (`CLAUDE.md:517`), bare `name/lessons/…` paths, and a 16-name list (`e2e-testing/SKILL.md:699`). The `update-docs` skill owns `CLAUDE.md` edits | **adapt** | Regenerate the inventory with #5 plus a git-grep path-token sweep. The gate must scan backticked and plain path tokens in live docs, since a link-only check sees 24 of ~200 references. |

## Numbers (critic corrections applied)

- **Tree** ✔: 253 plan dirs, 2,118 files, 37.2 MB. There are 230 top-level plan.md (278 including nested), and 23 dirs have no plan.md. `index.md` has 190 lines and 180 entries, 179 of them linked. It covers 178 distinct dirs, but only 177 exist: `incoming-tip-first-scan` is a proposal with no dir. That leaves **76 unindexed dirs**. 26 plans carry YAML front matter ✔. 8 index targets are not plan.md (fix-plan.md, findings.md, runbook.md, adoption-map.md, scope.md ×2, README.md, seed.md).
- **Classes** (from the recon table ✔): CLOSED 114 · CLOSED-EVIDENCE 59 · AMBIGUOUS 78 (legacy-import 39, stale-active-git 16, closed-with-residual 9, stale-active 7, partial 4, stale-idle 2, unindexed-no-evidence 1) · ACTIVE 1 (`tools-extraction`) · PARKED 1 (`transport-ready-handshake`), plus the index-only proposal `incoming-tip-first-scan`. 33 of the 114 CLOSED lines mention follow-ups, deferrals or owner calls.
- **Active off dev** (the critic's correction to "only tools-extraction is active"): `vitest-5-bump` (#669, open, parked by the owner), `ux-feedback` with nested b1/b2 (branches `feat/ux-1-first-run-wording` and `wip/ux-2-*`), and this plan.
- **Untrack set** ✔: 667 files / 19,602,480 B (52.7% of the tree's bytes): `audit-*.md` 446, `eli5.html` 132, `plan-*.md` 86 (42 leg drafts + 44 revisions), `_*.md` 3. One lessons file is re-included: `harden-quality-arc/round-2/lessons/audit-fixups.md`. **12 dirs empty out**: 8 top-level ✔ (aztec-4.2.0-bump, bb-wasm-hardening, bundle-fpc-nft, contacts-export-uxr, contacts-rename-export-senders, phase-2-plus, pre-a11-ux-cleanup, registry-stealth-notes) and 4 subdirs (M2/3/d, M3/0, M4/10-network-rework, any-erc20-bridge/drafts). 1,451 files stay tracked.
- **Plan-of-record hazard:** of the 24 dirs with a `plan-*.md` revision, 22 lose their plan of record if revisions are untracked. In 11 there is no plan.md at all; in 11 more, plan.md is v1, pre-audit or explicitly superseded. Live code cites two revisions as specs: `embedded-fpc-cap.ts:64` → `embedded-fpc-firsttx-cosmetic/plan-v2.md`, and by inference the wallet-bridge dispatcher's "plan-v3 §3/§5" → `wallet-sdk-implicit-account-grant/plan-v3.md`.
- **Pattern misfits:** the audit glob catches 3 curated files (`M3/7/audit-findings.md`, `incoming-trust-state-machine-refactor/audit-response-round{1,2}.md`). 40–43 transcript-shaped files escape the patterns (`draft-*`, `codex-*`, `leg-*`, `outline-*`, `reviews/`, `eli5-v2.html`, `M2/4/codex-review.md` at 285 KB; about 680 KB in total). Plan-shaped HTML is not matched either.
- **Links into the untrack set:** 112 clickable links (111 md links in 41 kept files, plus 1 `href` in `faucet-bridge/status.html:102`) reaching 83 targets, and 525 more non-link mentions. 17 mentions cross into another plan or come from outside the tree, 6 of them outside (5 code comments, 1 HTML page). 2 links are broken already. `dapp-interaction-lock-fix-v1/plan.md:11` points at a file that was never committed, so de-link it. `e2e-network-recovery/plan.md:179` doubles a dir, so resolve it relative to its source.
- **Links broken by the extra `archive/` level:** 41 sibling-plan links (35 files, 33 pairs), 10 repo-path links (each needs one more `../`) and 3 HTML hrefs (`phase-2/approval.html:429`). With only tools-extraction and transport-ready-handshake active, 0 links cross the active/archive line. Keeping an AMBIGUOUS plan active adds crossings: bun-1.4-adoption 4, complexity-budgets 2, and 1 each for storage-migration-framework, aztec-5.0-upgrade and journal-stage-restructure.
- **Already-broken links in kept files:** 210 in the plan tree (126 absolute home-path, 65 repo-root `:line`, 19 other), 884 in `architecture/` (14 files on the old layout) and 262 in `audit/`. The core live docs (CLAUDE.md, CI.md, ARCHITECTURE.md, the READMEs, `.claude/skills`) have 0.
- **Home paths** ✔: 80 plan-tree files hold `/Users/<u>` or `/home/<u>` paths, 64 of them in the untrack set. 16 stay tracked (15 with personal prefixes), in harden-findings-remediation/lessons (3 consult logs), backup-log-hygiene/lessons (6), backup-restore-security-hardening (consult + 2 reviews), profile-flow-dedup-q2/draft-codex.md, wallet-sdk-implicit-account-grant/plan.md, wallet-ux-fixes/lessons/phase-2.md and accelerator-server-ci/lessons/phase-1.md. Public history keeps all 80.
- **Non-md assets** ✔ 259: executable 27 (vectors.json; 24 baselines + a `.gitignore`; phantom-sweep.ts); provenance generators 14 (3 reference projects, each with its own package.json and bun.lock — Renovate scans them, and its `@aztec` disable keeps them inert); one-shot tools and spikes 14; inert 204 (eli5 133, png 42, HTML 20, evidence JSON 7, dotfiles 2).
- **Follow-up sources:** `third-party-notices/follow-ups.md`, `faucet-bridge/follow-ups.md`, `network-e2e-required/FOLLOWUP-*.md` ×2, 9 STATUS.md ✔, 6 WRAP-UP.md ✔, ledgers (`dedup-ledger/ledger.md`, `harden-2026-09-remediation/{b3-isolation,b4-approval}/ledger.md`, `security-audit-remediation/decision-ledger.md`, `firefox-arc-closeout/closing-ledger.md`), `design-system-externalization/round-2-backlog.md`, `network-playground-testing/phase-2f-followups-plan.md`, `M4/4/wiring-followup.md`, the 33 CLOSED lines, and the residuals in Q1 and Q3.
- **Delivery limits:** GitHub renders at most 300 files per diff, and its PR-files API caps at 3,000. On `pull_request` events dorny/paths-filter lists files through that API (moderate confidence), so a PR touching `apps/**` must stay well under 3,000 files. No filter (pr-quick, smoke e2e, network e2e) lists `implementations-plan/**`, `scripts/**` or root `*.md` ✔, so a plans + gate PR runs only `quality-status`. Repointing the 4 KAT imports or the 66 source comments fires the package filters and both e2e suites.

## Coordination hazards

- **H1: the ux-feedback branch.** `fee6b4a2` ✔ (`feat/ux-1-first-run-wording`, no PR) adds the same `.gitignore` and `.ignore`. It also rewrites the README to say "Older plans … keep their committed transcripts", which contradicts decision 1, and "no committed file links to a transcript", which contradicts the 112 links. Whichever branch lands second conflicts on all three files. The same branch prepends an index line and puts executable mocks (`design/mocks/*.py`, `*.mjs`) in a plan dir.
- **H2: #669 is parked and must not be a dependency.** Its full list (109 files; `gh pr view` truncates at 100) ✔ removes 22 of the 24 plan-dir baselines and renames 2, moves `full/.gitignore` to `scripts/ci-cd/test-soak/baselines/`, repoints `cli.ts` and deletes `vitest.base.ts`. It adds `vitest-5-bump/` with a committed `audit-codex.md` + `eli5.html`, and it still touches `apps/tools` and `packages/bridge-core`, so it needs a rebase. The plan must hold whether #669 lands before this migration, after it, or never. The gate reds its transcripts whenever it lands, and the baselines move with whichever branch lands first.
- **H3: the blueprint skill races itself.** Step 3b makes every concurrent `/blueprint` run append the hygiene files and `git rm --cached` on its own branch, so partial migrations appear (ux-feedback already has one). Land the hygiene files first, or tell parallel sessions to skip 3b until this merges.
- **H4: search half-state.** Ripgrep (behind Claude Code Grep and Codex) hides tracked files that match `.gitignore`. A `.gitignore` landing before the `git rm --cached`, as H1 would do, hides 667 committed files from agent search; `git grep` still sees them. `rg` is absent on this machine, so this is unverified here.
- **H5: the gate runs in the release chain.** `test:ci-gating` runs in `pr-quick`, `release.yml` and `nightly.yml` on a depth-1 checkout with no PR context and a 15-minute budget shared with `test:all`. A docs typo must not block `attach-assets`, and nothing may use the network outside `pull_request` (the `ratchetBase()` model). Invariants: `git ls-files -ci --exclude-standard -- implementations-plan` is empty, and the offline link check passes.
- **H6: squash-merge and CI cost.** "The move in its own commit" means its own PR, or a merge-committed PR. The ruleset allows the latter, but convention reserves it for the sync PR; the owner decides, and every commit must be signed. Source repoints (`apps/**`, `packages/*/src/**`) ship in their own PR.
- **H7: the permalink base is per target.** Files added after `9f11de70` do not exist there, and permalinks at `6611f861` already exist, so the checker accepts both SHAs. All 667 paths are URL-safe and exist at `9f11de70` ✔. Force-push is blocked, so both SHAs stay reachable.
- **H8: seeds live outside top-level plan.md** (row #8). Only 2 `/loop` seeds stop cleanly when their plan moves to `archive/`.
- **H9: Outcome detection must be exact.** Match `^## Outcome\s*$`, never a prefix. New plans carry `## Outcome & Quality Bar`, and the skill's `/loop` reads `## Outcome` as "closed: STOP". The recon table flags fix-transport-sessions via a prefix match.
- **H10: a parked spec lives inside closed plans.** `index.md:108` keeps transport-ready-handshake's spec OPEN inside `e2e-deflake/flake-ledger.md` and `deflake-round-4/fix-plan.md`, both CLOSED. Lift those rows into `follow-ups.md` or the parked plan before either dir moves.
- **H11: unleashed B4.** At `6611f861`, `packages/bridge-core/scripts/live-intent.ts:171-174,191` ✔ allowlists `implementations-plan/{aztec-5.0.0-stable,aztec-5.0.1-line,tools-two-network,any-erc20-bridge}/lessons/` and reads `aztec-5.0.0-stable/lessons/intent.json`. These are inert in nulo but live data in unleashed. The rule unleashed mirrors ("every imported plan → archive/") therefore needs an asset exception, or unleashed must repoint live-intent. `6534e8f1` ✔ (off dev) also edits `index.md:190`, one more index conflict.
- **H12: the local-path guard** is blind to this machine's HOME and runs only as a hook (row #12). Agent-written `lessons.md` and `follow-ups.md` would go unguarded.
- **H13: history is evidence.** By owner constraint, the committed transcripts stay in git history. Untracking plus permalinks keeps them addressable. **Never propose a history rewrite, purge or force-push**, including to remove the home-path leak.
- **H14: N2 digest binding.** No reference material goes into `packages/wallet-crypto/` (row #11).
- **H15: untracking deletes files elsewhere.** When another worktree or clone pulls the `git rm --cached` commit, git deletes the 667 files from its disk. Warn parallel sessions that read local transcripts.
- **H16: the policy lands together with its doc rewrites.** `CLAUDE.md:470` ("Plans + audit transcripts … are committed artifacts"), `:474` and `:459-461`, the README layout and Retention sections, and the recorded deviations in `legal-terms/plan.md:376-381`, `third-party-notices/plan.md:172` and `tools-extraction/plan.md:312` must change in the same PR as the `.gitignore`. Otherwise agents keep committing transcripts.
- **H17: live-doc meaning that link checks miss.** `README.md:13` and `:27` ✔ send readers to `implementations-plan/README.md` for "what's still in flight", which will live in `index.md`. `.github/README.md:3` and `CI.md:3` say ci-cd's "audits live" there, but those 4 files get untracked.
- **H18: UI impact: none.** The plan touches docs, plans and CI gate scripts only.

## Corrections list

Critic corrections to the mappers:

1. **No** `<FREEZE_SHA>` placeholders are in `CLAUDE.md`, on dev or at `6534e8f1`; lines 10, 18 and 36 carry the real SHA `6611f861`. The mapper's claim came from a stale session snapshot, so there is no link-gate sequencing constraint.
2. The 210 broken plan-tree links are not `plan.md:52` anchors (there are 0 of those). They are 126 absolute home-path links, 65 repo-root `:line` links and 19 others: a scrubbing job plus a base-path decision.
3. Index lines L6, L99 and L171 stay valid when they move to `archive/index.md` together with their CLOSED targets.
4. authwit-lifecycle-and-execution-followups' roughly 40 `../` refs sit in its 2 transcripts, which are in the untrack set. The real sibling repair set is 41 + 10 + 3.
5. #669 already covers the soak-baseline relocation and the `CLAUDE.md:49` interop follow-up. Both are open only if #669 is abandoned.
6. network-test-triage is **not** a live tracker: the network suite has 0 unconditional skips ✔. The 3 `test.skip(` calls are smoke tests (`appearance.test.ts:79`, `contacts.test.ts:116`, `sw-resilience.test.ts:137`). So `CI.md:219` ✔, the README Retention text, `CLAUDE.md:461` and `vitest.e2e.network.config.ts:37` are stale.
7. The bridge/tools plans are already decided. tools-extraction decision 10 (see its `recon.md:72-73`) keeps the 36 MIXED plans in nulo, so they get archived here. #692 deleted the 27 TOOLS-ONLY dirs.
8. "Only tools-extraction is active" holds for dev only. The manifest row for azguard-attribution names a plan dir that no branch has. Its worktree exists (locked, `fix/azguard-attribution-followups`) ✔.
9. **12** dirs empty out, not 11: mapper 1 missed `M2/3/d`.
10. #281 is an issue.
11. The ruleset allows merge commits, so "the move needs its own PR" is a convention call.
12. The link counts reconcile at 112. Mapper 4's 110 came from parser edge cases.

Found while verifying for this file:

13. The `6611f861` permalinks number 15, on 13 lines in 7 files: 3 live docs (CLAUDE.md, UPDATE.md, the aztec-update skill) and 4 plan files (`any-erc20-bridge/lessons/phase-10.md`, `bun-1.4-adoption/adoption-map.md`, `complexity-residue-round-3/scope.md`, `private-fuel-fee-fix/plan.md`). The critic said "6 plan files" and mapper 4 said "9 in live docs"; both are wrong.
14. #669 **deletes** `vitest.base.ts` outright; it does not trim "−13 lines". Of the 24 dev baselines it removes 22 and renames 2, and it edits every `vitest.config.ts`.
15. `check-no-local-paths.sh` has **no CI mirror**: only the pre-commit hook calls it.
16. The exact-regex Outcome count is **1**, not 2.
17. The repo depends on no markdown parser, but Bun 1.4.2 ships `Bun.markdown` and `Bun.YAML` ✔, and `scripts/ci-cd` already uses `Bun.YAML.parse`. js-yaml and yaml sit in the lockfile with no direct importer.
18. The seed counts depend on method: grep finds 104 `/goal` and 85 `/loop`, the mapper counted 99 and 83 blocks. Whatever tool the plan builds is authoritative.

## Open questions for the owner

**Q1. Non-legacy AMBIGUOUS (39): close with residuals moved to `follow-ups.md`, or keep active?** For each plan, the one fact that makes it ambiguous:

- *closed-with-residual (9):* authwit-lifecycle-and-execution-followups: "PR #85 awaiting QA+merge", but #85 is merged · aztec-5.0-upgrade: "PR #122 open", but it merged 2026-06-21 · chrome-store-launch: completed, but "open items" sit in its "Launch status and follow-ups" · dapp-popup-cancel-focus: an owner macOS Space-switch check is still pending · execution-decomposition: "awaiting manual QA + merge", but #83 is merged · firefox-first-class-spike: "awaiting nightly + release evidence" (the CLAUDE.md Firefox checklist) · q3-transport-unification: "PR #121 awaiting final-network", but #121 is merged · required-check-mismatch: a false positive ("#172 deliberate-red"), leans closed · stable-release-0.26.0: a false positive ("gated on tag-integrity"), leans closed.
- *stale-active (7):* aztec-5.0.1-line: "executing", but #282, #287 and #324 are merged · key-model-v2: "#417/#418/#419 draft", but all three are merged · monorepo-restructure: "APPROVED", but #186 is merged · proverless-network-stabilization: "ready to implement", but #94 is merged · quality-arc-deferred: the index says "SCAFFOLDED" while plan.md says "ARC COMPLETE" · storage-migration-framework: "awaiting push/PR", but #246 is merged and the framework is live · vitest-vite8-dedupe: "e2e CI in progress", but #169 is merged.
- *stale-active-git (16):* aztec-5.0.0-stable: "Phase 6 in flight", but the line has since moved to 5.2.0 · bridge-permit2-recipient-commitment: its live deploy is "GATED on explicit go" · bun-1.4-adoption: a "dossier" whose arcs all landed · composition-test-rollout: "ready to PR", but it landed with #160 · dapp-preexisting-fee: "in progress", but #544 is merged · execution-pxe-injection-spike: "ready for PR", but it landed with #160 · harden-findings-remediation: its F-11 scope decision is still open · incoming-public-transfers: "implementing", but #315 and #318 are merged · key-model-v2-hardening: "awaiting owner approval", but #431 is merged and the code calls it ratified · light-theme-fix: a manual light-mode smoke is pending · network-e2e-required: "ready to implement", but the gate is required on both branches · nightly-release: "in progress", but `nightly.yml` exists · profile-fenced-execution: "in review", but #610 and #611 are merged · profile-flow-dedup-q2: "PR open", but #100 is merged · release-dev-to-main: "executing", but #142 is merged · token-identity: manual testnet validation is pending.
- *partial (4):* account-switch-isolation: only "Phase 0+1 ✓" was ever recorded · backup-restore-residuals: its 2 security fences were deferred as a multi-PR epic · complexity-budgets: "arc 1 in PR", while the burn-down finished in round-2 and round-3 · swap-fuel: "private gas + B-presets in follow-up".
- *stale-idle (2):* audit-448-remediation: the parent line says "in progress", but all 9 batch lines say completed, the last one "FINAL" · private-fuel: its remaining P2/P4/P5 were never recorded done (bridge domain).
- *unindexed-no-evidence (1):* journal-stage-restructure: no PR, but `network-e2e-unquarantine` says it "graduated", and 3 live files cite it.

**Q2. Legacy-import (39, sole commit is the 2026-05-19 open-source import): bulk-archive as "historical, pre-open-source"?** A11, M2, M3, M6, aztec-4.2.0-bump, backup-import-repair, bb-wasm-hardening, bundle-fpc-nft, ci-cd, contacts-export-uxr, contacts-rename-export-senders, dependency-hardening, docs-improvement, e2e-determinism, embedded-fpc-firsttx-cosmetic, fee-estimation-init-race, furthering-non-network-e2e, network-playground-testing, network-test-triage, parallel-e2e-isolation, passkey-e2e, passkey-in-page-modal, passkey-modal-export-import, phase-2-followup-{affordances-v4, card-affordances, feesettings, terminal-cards}, phase-2-plus, phase-2, playwright-migration, popup-handshake, pr-8c-mixed-and-fee, pr-8c-rehydration, pre-a11-ux-cleanup, registry-stealth-notes, typecheck-cleanup, vitest-vite-bumps, vue-router-renovate, zod-puppeteer. Exceptions to rule on separately:
- **passkey-e2e** is not a plan: it is one note, cited 9× and sanctioned by `CLAUDE.md:459-461`. Keep it live under a docs path, or archive it and repoint?
- **network-test-triage** is refuted as a tracker (correction 6). Archive it and rewrite the 4 stale texts?
- **playwright-migration** says PARKED in its own text.
- **M2/M3/M6/A11**: the README table says Landed.
- **ci-cd, embedded-fpc-firsttx-cosmetic, phase-2 and M6** have live refs.

**Q3. CLOSED-EVIDENCE (59): one bulk confirmation?** These are closed on git/gh evidence, not on text. Residuals that would become follow-ups: harden-quality-arc Q-13 (owner-gated), v3-followups P2 (deferred), any-erc20-bridge P10 (owner testnet deploy), security-audit-remediation ("pending final codex pass"), plus dedup-p3's front matter, which still says "implementing".

**Q4. Parked and proposed.** Should transport-ready-handshake stay in `index.md` under a "parked" marker, or go to `archive/` with a parked Outcome? Should `incoming-tip-first-scan` move to `follow-ups.md` (recommended)?

**Q5. Plans of record in `plan-*.md` (22 dirs):** either promote the latest revision to plan.md before untracking, so the 8 emptied dirs survive and the old plan.md becomes a tracked `plan-v1.md`, or keep each only as a permalink behind a generated stub and Outcome. Separately: is e2e-stabilization's plan of record plan.md or plan-final.md?

**Q6. Pattern edges:** keep the 3 curated `audit-*.md` via `!` lines or by renaming them? Widen the ignore to the 40–43 near-misses, beyond the four approved patterns? Cover same-shaped files outside the tree (`audit/quality/**/eli5.html`, `…/raw/_quality-prompt.md`, `wallets-architecture-research/synthesis/audit-*.md`)?

**Q7. Reference projects** (`reference/` in key-model-v2, key-model-v2-hardening and aztec-5.0.0-stable): move them to a non-package, non-Biome home such as a top-level `reference/` (H14)? Is the path-only edit to the 4 imports acceptable under the account-freeze review rule? The KAT values stay unchanged.

**Q8. Local-path guard:** narrow its exemption to `archive/**`, scrub the 16 kept files, add the missing HOME prefix, and add a CI mirror?

**Q9. The 66 plan citations in source comments,** which already break `CLAUDE.md`'s comment rules: repoint to `archive/`, convert to permalinks, or delete and keep the reason inline? Each option needs its own e2e-firing PR.

**Q10. Link-gate scope:** which roots count as live docs? Are `architecture/` (884 broken) and `audit/` (262) out of scope? Do `path:line` links become `#L<n>` permalinks? Are fragments checked?

**Q11. Delivery:** stacked PRs in this order:
1. hygiene, untrack and permalinks
2. asset relocation
3. Outcomes and seeding `lessons.md` / `follow-ups.md`
4. pure `git mv` plus the index split
5. the gate plus the doc rewrite

Or one merge-committed PR?

**Q12. Permalink base for newly untracked files:** `9f11de70`, or the migration's merge-base? Either works, as long as it predates the untrack. Per H7, the base is chosen per target regardless.
