# Phase 4 — docs and final read

Gate run 2026-09-06: `typecheck` exit 0, `test` 10 passed, `bun run lint` exit 0, `build` exit 0. The README's file map names every file under `apps/landing/src` and `apps/landing/scripts` (checked against `ls`).

Copy read: a grep for `notes`, `proofs`, `nullifier`, `self-custody` and `testnet only` over `index.html` hits only the record panel's rows, which is the one place the plan allows the technical words. The two gate conditions are in the page verbatim: the notice ("Nulo is a preview and hasn't been audited yet. Use small amounts you can afford to lose, or the test network, and tell us what breaks.") and the promise ("Private by default. Public when you say so. Nulo always shows you which is which."), with the per-token line in the caption under the two screens.

`implementations-plan/index.md` entry updated to implemented, PR pending.
