**High**
- `[Security][P3/P6]` The scopes mitigation is weaker than the plan claims. `plan.md:135` says parity review plus `multi-account-from` e2e will catch wrong scopes. It will not. The current `NO_FROM` flow has three distinct scope sites today, not one: discovery sim uses `additionalScopes`, real sim uses `scopesWithAccount`, and prove uses `scopesWithAccount` (`service.ts:2107-2124`, `2155-2166`). The named e2e also documents that it does **not** exercise the end-to-end “wrong account / second-account” claim (`multi-account-from.test.ts:14-23`). Concrete fix: Phase 3/6 must add executable assertions on the exact scopes passed to simulate/prove per path, and the `multi-account-from` e2e needs the second-account playground hook before being treated as a mitigation.
- `[Inferences][Fee path]` `plan.md:125` says `e2e:agent + unit pins + parity reviews are sufficient parity evidence; no golden-file harness needed`, but `plan.md:137-139` simultaneously admits byte-level `TxExecutionRequest` drift is an uncovered residual. That is not an honest closure for the signing/proving/fee path. A refactor can preserve scenario success while changing `maxFeesPerGas`, fee payload ordering, or authwit/call shape. Concrete fix: add deterministic structural parity tests in P2/P3 for the fee strategies and builder outputs (projected gas settings, max fees, fee payment method, call ordering, authwit counts), or surface explicit user approval for carrying that residual.

**Medium**
- `[Phase gate][P0]` Characterization is undercounted and the gate is too soft. `plan.md:23` says `tryConsumeTransferEstimate` has “six rejection branches,” but the current code has more observable exits: TTL, input drift, profile drift, no primary endpoint, primary changed, base-fee changed, base-fee fetch failed, pending-hash drift (`service.ts:635-711`). Also P0 now requires a baseline `e2e:agent` run (`plan.md:22`) while the gate still says “no e2e” (`plan.md:25`). Concrete fix: enumerate the actual exits and make the recorded baseline e2e result a required P0 artifact.
- `[Security/Gate][P3/P6]` The preserved `executeSendTransaction` no-slot quirk is load-bearing but not tied to a named executable gate. The source path still bypasses `acquireExecutionSlot` entirely (`service.ts:1130-1212`), and the plan’s named phase e2e list does not identify a covering test for that behavior. Concrete fix: require a dedicated unit pin in P3 and P6 asserting zero lane/slot calls for `executeSendTransaction`, and name that test in the phase text.

**Low**
- `[Facts]` The assumptions source map is still stale on Q17 duplication. `plan.md:115` omits the second ensure-registered prologue in `tx-request-builder.ts:412-424`, even though Phase 1 scope was corrected to include it (`plan.md:28`). Concrete fix: update the Fact entry so the source map matches the actual phase scope.
- `[Asks]` A1 is not enough. Since the Q23 bail-out is now explicitly user-surfaced (`plan.md:13`, `75`), the plan needs an A2 defining what evidence must accompany a Phase 7 bail decision. It also needs an A3: if structural fee/scopes parity tests are not added, is that residual explicitly accepted? Concrete fix: add both asks before approval.

conditional approve (with conditions: replace scope-review theater with executable scope assertions + real multi-account-from coverage; add structural fee-path parity evidence or explicit user acceptance of that residual; tighten Phase 0 characterization/gating and add the missing asks)