- **High**: **A4 is framed around the wrong surface, so the plan still under-covers one of the four send paths.** `executeSendTransaction` is not just an auth-registry revoke helper; it is the generic `send_transaction` path used by the main execution dispatcher and by dApp-confirmed operations, and `setRegistryEnabled` also calls it. The current ask/manual-QA only names revoke, so done-condition `(c)` (“ZERO behavior change, proven by network e2e per phase”) is still not honestly met for this path. Replace A4 with a coverage decision for **plain `send_transaction` as a whole**, not “revoke only.” Sources: [plan.md](<implementations-plan/execution-decomposition/plan.md:131>), [service.ts](<packages/extension/src/wallet/services/execution/service.ts:949>), [dapp-interaction/service.ts](<packages/extension/src/wallet/services/dapp-interaction/service.ts:376>), [auth-registry/service.ts](<packages/extension/src/wallet/services/auth-registry/service.ts:93>), [tx-sendTx-default.test.ts](<packages/extension/tests/e2e/network/tx-sendTx-default.test.ts:65>).

- **Medium**: **Phase 7’s mitigation story still over-trusts existing race tests.** The seam being moved includes not just cancel/mutex ordering, but also capacity rejection mapping (`ExecutionMutexCapacityError` → journal failed + `TooManyPendingError`) and queued-wait heartbeat/reaper protection. The plan’s Phase 7 / test-strategy text promises “lane microtask races” plus existing helper/mutex suites, but I do not see a named ask or explicit tests for these moved behaviors. That is not direct funds risk, but it is a real adversarial dApp abuse / stuck-journal surface, and today those semantics live in load-bearing code paths. Sources: [plan.md](<implementations-plan/execution-decomposition/plan.md:75>), [plan.md](<implementations-plan/execution-decomposition/plan.md:149>), [service.ts](<packages/extension/src/wallet/services/execution/service.ts:1327>), [service.ts](<packages/extension/src/wallet/services/execution/service.ts:1349>).

No D1-D7 or CC1-CC7 reversal jumps out after source verification. D1/D3 both look defensible after the CC6 lane-deps fix. On the adversarial side, the fee/scopes/cancel mitigations are mostly real after R1; the remaining weak spots are the mis-scoped A4 coverage story and the under-specified Phase 7 availability/backpressure tests. A3 reads like a conscious residual, not theater.

conditional approve (with conditions: replace A4 with a real coverage decision for generic `send_transaction` including dApp + both auth-registry callers, and add explicit Phase 7 acceptance/tests for capacity-reject + wait-heartbeat semantics)