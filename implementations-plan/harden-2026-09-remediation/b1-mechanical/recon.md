# B1 recon — reuse map (Phase 0.4, 2026-09-14, HEAD bfb59444)

| # | Capability | Existing code | Verdict |
|---|---|---|---|
| 1 | Exact-l1ChainId + canonical-range chain check | `assertLiveChainIdentity` (`packages/aztec-runtime/src/utils/chain-identity.ts:53-61`); exact-equality precedent `apps/extension/src/wallet/services/network/service.ts:565-572,612-618`; `Network.l1ChainId` already range-bounded at persistence (`network/spec.ts:69`); `assertCanonicalL1ChainId` (`packages/wallet-crypto/src/derive-account-seed.ts:19-23`, no external callers) | **adapt** — extend the helper's interface + body; every caller already passes the full `Network` |
| 2 | FPC picker canonical-only selection | `buildFeeMethods` (`popup/components/modules/send/fee-helpers.ts:157`), `readPrivate` (`execution/gas-balance-reader.ts:180-181`), `getFpcImpl` (`fpc/service.ts:426-432`), `isProtocol` decoration `fpc/service.ts:123-131`, predicate to mirror `fee/fpc-strategy.ts:111-115` | **adapt** — one predicate change at three sites |
| 3 | Name↔selector bind before the fast path | bind pattern `execution/service.ts:900-908`; `findFunctionBySelector`/`ContractResolver` (`execution/contract-resolver.ts:57-70,92+`); `view-executor.ts:361-381` (resolve → find-by-selector → compare → rebuild); `tx-request-builder.ts:566-582` (`validateEncodedCallFn`); `fast-path.ts:87-114,172-229` has none; `FastPathDeps` has no `resolver` | **adapt** — same shape inside `fast-path.ts`; add `resolver` to `FastPathDeps` |
| 4 | Log scrub for flattened RPC errors | `trim()` primitive branch (`wallet/logger/utils.ts` ~263), `scrubUrls` (`utils/scrub-urls.ts`), `projectError` (only `Error` instances scrubbed); ~40 `log{Error,Warn}(…getErrorMessage/errorMessageFromUnknown…)` sites; `utils/log-payload-ban.test.ts` (declarative name list + line scanner + ALLOWLIST) | **adapt** — backstop in `trim()`, fix the two demonstrated sites (`network/service.ts:1003`, `pxe/service.ts:207/926`), extend the ban test |
| 5 | Restore sanitization | `sanitizeString` (`utils/string.ts:33-42`) used in `ContactService.importContacts` (`contact/service.ts:188-189`) not in `restore` (~298-306); `TokenBalanceService.restore` (~690-720) passes `updatedAt` through; `token-balance/spec.ts:54` unbounded; `reconcile-pairs.ts:143` only re-projects on `0`; `utils/core.ts:142,160` negative age ⇒ fresh | **reuse-as-is** (`sanitizeString`), **adapt** (`updatedAt` clamp) |
| 6 | Keyed account reads bound to address | `account/service.ts:84` storage without identity options; `getAccount` `:177-180`, `getAccountContract` `:334-352`, `loadImportedAccountContract` `:367-387` (self-referential compare); `liveRows` `:88-106` (`key === accountRowIdOf(row)`); `EntityStorage.requireKeyIdentityMatch` is single-`id`-field only (`entity_storage.ts:152-172`), enabled on `auth-registry`, `profile/repository.ts:46`, `balance-repository.ts:33` | **adapt** — inline requested-address compare mirroring `liveRows` (the primitive cannot key a composite) |
| 7 | Pending passkey-restore TTL | `PENDING_RESTORE_TTL_MS` (`profile/service.ts:168`), `sweepStalePendingRestore` (`:184-200`, skips `exceptId`), `consumeDekRewrapContext` TTL idiom (`:221-225`), stash (`:2514-2530`), `finalizePasskeyRestoreHoldingLock` (`:2651-2689`, never reads `capturedAt`), `lockActiveProfile` (`:853-870`, maps untouched) | **reuse-as-is** — copy the `:221-225` idiom; force-sweep on lock |
| 8 | Discovery icon | `walletIcon: chrome.runtime.getURL("/src/assets/logo.png")` (`wallet-sdk/background.ts:106`); SDK types it `walletIcon?: string` and forwards verbatim (`background_connection_handler.d.ts:99-100`, `.js:116`, `manager/wallet_manager.js:196`) with no scheme validation; manifest `web_accessible_resources` `manifest.config.ts:53-58` | **adapt** — inline `data:` URI producer; drop the manifest entry. No existing asset→data-URI helper found (`grep -rn "data:image" apps/extension/src`) |
| 9a | `getPasshash` zeroize | `encryption-key.ts:123-125`; also `getHashHex` `:132-138` (carries plaintext master/entropy/DEK on the backup-checksum path `useFullBackupImport.ts:93`, `full-backup-helpers.ts:145`); `mnemonic-master.ts:39-43` shows the `finally { zeroize }` pattern. A third password-string site was not found (Codex NF-12/13's other items are session-token/export buffers) | **adapt** — `try/finally` zeroize at both |
| 9b | Secret `<input>` autocomplete | `packages/design/src/ui/Input.vue:77,297` already forwards an `autocomplete` prop; `auth.vue:257` uses it; `onboarding/pages/create.vue:156-172`, `components/composite/import/ImportSecretForm.vue:41-107`, `components/composite/SecretUnlockSection.vue:29-35`, `popup/pages/settings/security/change-password.vue:112-188` do not | **reuse-as-is** — pass the prop |
| 9c | CSV formula neutralization | `components/JsonViewer/logs-csv.ts:24` quote-doubling only; no helper in repo (`grep -rn formula apps packages --include=*.ts`) | **build new** — a 3-line prefix guard colocated in the file |
| 9d | `registerContract` ordering | `packages/aztec-runtime/src/pxe/service.ts:434-454`: class write `:447` before instance/address check `:449-453` | **adapt** — reorder |
| 9e | `buildNoFrom` ordering | `tx-request-builder.ts:213-224` (`resolveBuildContext`, assert first) vs `buildNoFrom` `:366-453` (resolver work, then assert at `:404-407`) | **adapt** — hoist the assert |
| 9f | Full address on import preview | `popup/pages/settings/accounts/import.vue:227` inline `trimAddress(…, 8, 6)`; `components/AddressDisplay.vue` (`full` prop `:17-20`, click-to-toggle `:52-65`) used by ~20 surfaces | **reuse-as-is** — `<AddressDisplay :address full />` |

Call-site inventory for `assertLiveChainIdentity` (all pass a full `Network`, so the interface change is churn-free): `execution/service.ts:230,875`; `tx-request-builder.ts:221,407`; `dapp-send-executor.ts:854`; `discovery-probe.ts:74`; `authwit-discoverer.ts:109`; `fast-path.ts:179` (`FastPathDeps.network: {chainId:number}` — type must widen); `view-executor.ts:211`; `helpers/batched-view-simulation.ts:204,362`. Unguarded: `wallet/utils/fn.ts:85-95` (callers `token/service.ts:711,716,720` have the network in scope).

Test conventions to match: `packages/aztec-runtime/src/utils/chain-identity.test.ts` (table-style vectors, already has the Sepolia composite); `fee-helpers.test.ts` (`buildFeeMethods` cases); `fast-path.test.ts` (two layers: pure `rehydrateOptimizablePrefix` with `rpcShapedPublicStaticCall()`, then mocked `simulateViaNode` + `fakeNode()`; `contract-resolver.test.ts`'s fake `IPXE` idiom); `log-payload-ban.test.ts` (declarative list + scanner); `profile/service.integration.test.ts` for the TTL case.
