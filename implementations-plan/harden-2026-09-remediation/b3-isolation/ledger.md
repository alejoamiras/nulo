# harden-b3-isolation — reconciliation ledger (codex round 1 × fable round 1)

Both legs: `reject`. Both name the same blocking item (a corrupted `dekSealed` has no repair path once the DEK is load-bearing). Every finding below was verified at source by the driver before the decision.

| # | Finding | Codex | Fable | Decision → plan change |
|---|---|---|---|---|
| 1 | Recovery dead end: `exportBackupMaterial` refuses an undecryptable slot pointing at password change (`profile/service.ts:1802`); the plan makes password change point at export; passkey restore throws at `:2420` and passkey profiles have no phrase | blocking — keep the refusal, describe truthful recovery (intact backup / phrase), add a test of the advertised route | blocking — make export tolerate the slot (fresh DEK in the blob; imported rows fall into the restore-side orphan taxonomy) and make passkey restore tolerate an unopenable blob (fresh destination DEK, skip the rewrap stash) | **Adopt fable's code fix + codex's truthful messages.** Export with an undecryptable slot succeeds with a fresh DEK; passkey restore finalizes non-degraded on an unopenable blob; password change refuses; both error strings say what is lost (imported keys). Tests: the two fable cases + the refusal. |
| 2 | Warm PXE bypass: store keys and runtimes survive lock/profile switch (`pxe/service.ts` `storeKeys`, `onActiveProfileChanged` no-op), so a degraded re-unlock never hits the throwing provider | blocking — gate newly admitted profile-bound PXE requests in recovery mode, preserve admitted jobs, test the warm sequence | — | **Adopt.** `PxeService.setProfileRecoveryMode(profileId, on)` (new RPC, sender-gated like the rest): `withPxeRead/Write` reject NEW requests for that profile with `RecoveryModeError` before the lock; in-flight jobs finish; the cached store key is dropped on entry. The runtime calls it on a degraded unlock (on) and a non-degraded unlock (off). Test: unlock → provision → lock → corrupt → degraded unlock → a read on a second chain rejects. |
| 3 | Reflected offscreen responses: the offscreen service accepts requests from any same-extension sender and replies to the supplied `from` (`offscreen/service.ts:38-59`), so a popup with a stolen `{from, requestId}` can settle the victim's request with a genuine response | blocking — authenticate the request direction (only the wallet background context), exact URL compare, keep Firefox tab-hosted documents | should-fix (#4) — the URL narrowing is spoofable by a same-extension page that opens/embeds the offscreen URL; keep the check, rewrite the claim | **Adopt both.** Requests to the offscreen service are accepted only from the background context (Chrome: `sender.url` equals the service-worker script URL; Firefox: the background page URL); responses to the client only from the offscreen document URL (exact path, query allowed, `sender.tab` permitted for Firefox). The Security section states the residual: a same-extension page can still stand up an offscreen-URL context — F-14 stays Low by boundary. Tests: reflected response, foreign sender, tab URL, Chrome/Firefox positive controls. |
| 4 | Journal events broadcast every profile's records to every Port (`operation-journal/service.ts:332,367,454` → base emit) | blocking — gate wire event delivery by ownership | nit (#7) — document as still-trusted | **Adopt codex.** The journal filters its wire emits by `record.profileId === activeProfile.id` (in-process events untouched). Cheap, and the F-11 claim "the popup sees only the active profile" is otherwise false. Test through a connected Port. |
| 5 | Stale sync deletion: `syncAuthwit` awaits the node then deletes whatever row holds the numeric id (`auth-registry/service.ts:365-372`); a purge + restore during the await can put another tuple's row there | blocking — re-read and compare tuple/hash/pending under the lock; capture profile/chain once | — | **Adopt.** Delete only if the re-read row's `(profileId, chainId, account, hash)` and `pending` match the captured row; the sync's profile/chain are captured at entry. Test with a deferred node reply and an id replaced by a foreign-tuple row. |
| 6 | `reconcileAuthwits(txHash)` crosses the tuple boundary (`:120-128,177-185`) | should-fix — thread `tx.profileId/chainId/account`, skip when absent | leave unscoped (H) — worst case over-claims, the safe direction | **Adopt codex.** The tuple is already on the tx row; the exception to the batch invariant is not worth keeping. Tests: equal hashes across profiles/chains, provenance absent → skipped. |
| 7 | Authwit page live updates keep address-only acceptance; enabled events carry only an address (`authwits/index.vue:55,67-71`) | should-fix | — | **Adopt.** Full-tuple list predicate; enabled/disabled events carry the tuple. Test: same-address foreign-chain event ignored. |
| 8 | Weak tests: the MAC case must sign a p2-targeted payload under p1's real key; `recoveryMode` must be asserted through `getActiveProfile` (`getProfileInfo :497`), kept out of the stored shape; reconcile must assert p2's keyless rows AND status vanish while other-chain rows survive; all five removed methods rejected; the read gate fails closed without `ProfileService` | should-fix | #2, #6, #9 agree | **Adopt all.** |
| 9 | `recoveryMode` projected in `toInfo` only; `ProfileInfo` flows into backups and every event | — | should-fix (#2) | **Adopt.** `recoveryMode?: boolean` set in `getProfileInfo`/`getActiveProfile` (and `toInfo`); excluded from the persisted `Profile`. |
| 10 | `defineRpcMethods` is exhaustive over `Methods`; pruning `rpcMethods` alone is a type error; `invoke` also receives `backup`/`restore` | — | should-fix (#3) | **Adopt.** Prune `Methods` and the five client wrappers; the override delegates to `super.invoke` for anything but the two reads. |
| 11 | Provider semantics: locked → `undefined` (today's marker contract), degraded → throw | — | should-fix (#5) | **Adopt.** |
| 12 | Read gate must fail closed when `profileService` is absent | #8 | #6 | **Adopt.** Port-path tests register a fake `ProfileService`. |
| 13 | `assertWithinCap`: `ctx.network` already carries `profileId`/`chainId` | — | nit (#8) | **Adopt.** No `BuildContext` widening. |
| 14 | Gates: `src/popup/app.test.ts` does not exist; sequential `--shard` halves boot two sandboxes for no gain; add `opfs-storage.test.ts`, `imported-account-execution.test.ts`; the `isAcceptedSender` override needs an app-side test | — | nits (#10, #11) | **Adopt.** One `e2e:agent` invocation over the eight files; the banner test lives with the Home module; an app-side override test. |
| 15 | Let `RecoveryModeError` propagate from signing sites unchanged (do not fold into "Wallet is locked") | — | nit (#12) | **Adopt.** |
| 16 | Storage writer can trigger recovery mode via `envelopeMac` corruption too | A | — | **Adopt** in the Security section. |
| 17 | Helper shape: package-private, returns the HKDF base key, each site keeps `deriveKey`/`deriveBits` inline; export only the concrete derivations | F | F | **Adopt** (the plan's `{salt, info, usage}` helper was one abstraction too many). |
| 18 | Legacy hidden rows: do not claim overwriting a codec-hidden key is harmless; reuse the physical-key occupancy check for id allocation | G | G (no sweep) | **Adopt.** `recordPendingAuthwits` allocates over `getKeys()` like `restore` does. |
| 19 | Journal residual: size-dependent scan timing and popup storage privileges remain | C | — | **Adopt** as a documented limit; no constant-time machinery. |

Asks resolved: A throwing provider + toast + banner, export reachable (with #1, #2, #9); B sender checks both directions, no Port, claim corrected; C remove `createOperation`/`countOperations` too; D remove the mint, refuse the password change (with #1); E `purgeForProfile`; F narrow helper; G hidden rows, no sweep; H scope reconciliation (codex over fable — cheap, keeps the invariant exception-free).
