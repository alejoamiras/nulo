# harden-b4-approval — reconciliation ledger (codex round 1 × fable round 1)

Both legs: `reject`. Every finding below was verified at source by the driver before the decision. Paths repo-relative.

| # | Finding | Codex | Fable | Decision → plan change |
|---|---|---|---|---|
| 1 | The vocabulary map keyed by name loses the second arity of `transferPrivate`/`transferPublicToPrivate` (`descriptors.ts:356-358,381-383`) — the 2-arg `transfer` would be `unverified` again | blocking (#9) | blocking (#1) | **Adopt.** `ReadonlyMap<name, readonly Signature[]>`, matched on `(name, args.length)`; the behavioural cases assert `transfer` accepts BOTH shapes. |
| 2 | "From: this account" is false for NO_FROM (`tx-request-builder.ts:403`; `dispatcher.ts:945,951` omits `opts.from`) | blocking (#9) | blocking (#2) | **Adopt.** The inferred sender renders only for account execution; for `default_entrypoint` a 2-arg transfer shows "From: transaction entrypoint — not this account". Component case added. |
| 3 | The vocabulary pin is tautological (both sides derive from the descriptors) | should-fix (#11) | should-fix (#9) | **Adopt.** The test holds an independent `{name → arities}` table (9 names) and asserts the module equals it. |
| 4 | A foreign estimate bypasses name↔selector: `operation-fingerprint.ts:130` omits an encoded call's `name`, so a popup-estimated `{name: real, selector: S}` reuses under a stored `{name: other, selector: S}` (`dapp-send-executor.ts:670` skips `tx-request-builder.ts:577`) | blocking (#1) | — | **Adopt, and remove the vector itself.** `estimateOperationFee` becomes estimate-BY-REFERENCE: `(interactionId, index, feeSettings, …)` — the SW materializes the operation from the stored payload (the only caller is the execute window), so no popup-supplied operation reaches the estimator; `name` still enters the encoded-call and encoded-authwit fingerprints as defense in depth (one line, one test with a name-only difference). |
| 5 | The rebuild baseline for discovered hashes is popup-authored (`delta.discoveredMessageHashes`), and `tryConsume` consumes the entry before a miss (`operation-estimate-reuse.ts:120`), so the reuse cache cannot serve as the SW-held baseline | blocking (#4) | should-fix (#5, option b) | **Adopt (SW-owned preview).** A separate `previewSnapshots` TTL map keyed by `estimateId` holds `{ interactionId, index, fingerprint, discoveredHashes }`, written whenever discovery ran at estimate time (reuse-eligible or not) and NOT consumed by `tryConsume`; the delta carries only `estimateId`; confirm — reused or rebuilt — resolves the snapshot for its `(interactionId, index)` and enforces discovered ⊆ snapshot. The popup no longer sends hashes. |
| 6 | NO_FROM signs discovered authwits at confirm with no preview (`dapp-send-executor.ts:874`) | blocking (#5) | — | **Adopt.** The same snapshot guard runs in `addDiscoveredNoFromAuthwits` before `createAuthWit`, outside its "not a CallAuthorizationRequest" catch. Whether NO_FROM estimates run discovery is verified at Phase 1; if not, the estimate path must, or the confirm aborts on every discovered authwit. |
| 7 | Confirm is reachable with no estimate result (`execute/index.vue:558-565`; a failed estimate only toasts), so "changed since preview" would lie | — | should-fix (#6) | **Adopt.** No snapshot → distinct abort: "Fee estimate did not complete — retry the estimate"; pinned. |
| 8 | `feeSettings` in the delta can change the fee PATH: self-pay is locked to `fj` only in the popup (`OperationCard.vue:75`), execution picks the strategy from the supplied settings (`execution/service.ts:965`); an embedded path can be overwritten | blocking (#2) | should-fix (#7) | **Adopt both.** Apply `delta.feeSettings` only when the materialized op `requiresFeeSelection` (embedded stays embedded); for self-pay (`isSelfPay(exec, opts.from)`, `packages/wallet-bridge/src/fee-payer.ts:69`) require `paymentMethod.kind === "fj"`; otherwise the kind must be a selectable method. Tests: self-pay + `fpc` delta rejected; embedded + conflicting delta ignored. |
| 9 | `previewedInterface` is not authenticated by its contract/chain check (`execution/service.ts:745-749`): same identity, altered function mappings persist (`token/service.ts:385`); a mismatch re-parses rather than rejects | blocking (#3) | nit (#11, residual) | **Adopt codex.** `previewedInterface` leaves the envelope; the executor always parses the interface SW-side. The seed lists it among acceptable deltas; accepting fewer is within scope. |
| 10 | A `CapabilityPayload` has `session` but no `params.operations`; `approveInteraction(capabilityId, [])` would claim and delete the record — a request-kill | — | should-fix (#4) | **Adopt.** Reject unless `Array.isArray(payload.params.operations)`; that check and the delta-length check run BEFORE `storage.delete(id)`. |
| 11 | Returning `true` for a queued verify window weakens B-13: a queued session dispatches messages before any verification shows, and never shows one while other windows stay open; the outline's two-tab justification is wrong (`session-tabClose.test.ts:34` approves verify before closing the tab) | blocking (#6) | accepts the plan (Ask D) | **Adopt codex.** Verify capacity is reserved at DISCOVERY admission, before `approveDiscovery`, on every approval site (returning-untrusted, fresh after the popup, dedupe waiters); excess handshakes queue before key exchange; `handleSessionEstablished` opens the window against the held reservation and the establishment gate is unchanged. The SDK's key-exchange response is never delayed. |
| 12 | The throttle has no drain trigger (lazy clock); a finite burst's queued tail is never served | blocking (#7) | should-fix (#3) | **Adopt.** A cancellable `clock.setTimeout` is armed while the queue is non-empty (next refill or next expiry); unit tests advance fake timers instead of calling `admit` again. |
| 13 | The verify queue is unbounded and dead entries are checked only on dequeue | should-fix (#8) | nit (#12) | **Adopt codex.** One per-origin admission queue (bound 4) covers throttle AND window capacity; entries expire at the discovery deadline (rejected); reservations are removed on termination. The claim that discovery serializes fresh connections is withdrawn (`background.ts:661` allows concurrent chains) — fresh windows count. |
| 14 | `rejectDiscovery` deletes the SW entry and does not notify the page (`background_connection_handler.ts:294`); the flood cell must size for the dApp's 60 s timeout | blocking (#7) | should-fix (#8) | **Adopt.** Flood cell: N = 4 tabs (3 immediate + 1 queued ≈ 20 s), ≤ 2 verify targets, the 4th connects within ~30 s; "rejected → not connected" only in the unit harness. |
| 15 | Raw-argument disclosure: `safeWire(v, 64)` truncates a 66-char address (`humanize.ts:42`); action rows hide args for `call`/`encoded_call` | should-fix (#10) — cover both action forms | Ask F: no — the only dApp producer of `send_transaction` is `grantPublicAuthwit` (`dispatcher.ts:1082`), whose row already renders args | **Adopt fable on scope, codex on values.** Canonical addresses render through `AddressDisplay` (full value), other values `safeWire(v, 128)`, at most 32 rows then "+N more (JSON)"; `OperationActionRow` untouched (no dApp path). |
| 16 | Tests that stay green with the fix deleted: sequential cells vs F-18, a short capped flood vs the throttle, record/card tests vs the binding, recipient/amount fingerprint vs the name bypass, a spread merge vs "ignored on non-send" | should-fix (#11) | (tests section) | **Adopt.** Negative cases listed per phase; executor stubs assert strict equality with the materialized op; timed admission asserted separately from window counts; `waitForPopup` armed before closing a window. |
| 17 | The fix-loop exit ("continue to delivery with the residue") contradicts the seed's convergence gate | should-fix (#12) | — | **Adopt.** "Stop, reassess, record unresolved findings in lessons." |
| 18 | Stale comments: `dapp-send-executor.ts:441-449,537-545` (popup-built operations "without further validation"); milestone tags at `operation-estimate-reuse.ts:64`, `discovery-probe.ts:4` | — | nit (#13) | **Adopt** where the lines are touched. |
| 19 | Asks A (delete mint set), B (`abiBuilder` names, per-variant), E (one delta envelope) | A yes, B yes, E yes | A yes, B yes, E yes | **Converged.** |
