# B2 lessons

## 2026-09-14 — recon
One Sonnet sweep (`b2-backup-passkey/recon.md`). The seed's "drop the network slice" undersold the change: unlike the FPC slice (an optional post-token loop entry), network restore is a mandatory stage the pipeline depends on — `createdNetworks` feed the account-state chain-sync, the active pointer and the `networkId` remap across slices. Design chosen: a reseed stage (`NetworkService.seedDefaultsForProfile(profileId)` reusing the locked seed loop under a restore-style fence) plus a `chainId`-keyed remap, with `active-chain-id` replacing the raw `active-network-id`. Two of the seed's three open asks were answered by recon before codex: `IMPORT_BLOCKING_ACK` acknowledges migration versions only (registry edits suffice), and no `.well-known/webauthn` exists or is needed. `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b2.md`

## 2026-09-14 — blueprint round 1 (codex `high`)
Verdict `reject`, one blocking: the reseed reused `activateSeededLocked`, which writes `this.nodes` keyed by `chainId` — restoring profile B while A is active would replace A's cached node for that chain with B's seed endpoint (`network/service.ts:306`, `getNode` `:721` never checks ownership). Adopted: the reseed writes only B's persisted active pointer and never touches the node cache; a test pins A's custom endpoint surviving B's reseed. Five should-fix, all adopted: (2) a deletion epoch is not existence — `seedDefaultsForProfile` requires a live target profile, is idempotent on existing rows, re-asserts under the lock; (3) `content_scripts.matches: *://*/*` already covers the RP host — add `exclude_matches` for `passkey.nulo.sh` and its descendants, test effective matching, pin the literal `RP_ID`, document that `*.passkey.nulo.sh` stays content-free; (4) the synthetic backup builders (`tests/e2e/helpers/import-drivers.ts`, `passkey-backup.test.ts`) and the dead-RPC suite emit or rewrite a `network` slice — in scope for Phase 2/3; (5) the remap's claim narrowed ("imported data cannot define the destination network or endpoint"), strict numeric chain lookup incl. chain 0, malformed/unseeded account-state items dropped + reported, transaction rows assigned from their chain even without an old id; (6) the base class already supplies null `backup/restore` — delete the four overrides and their helpers (`validateRestoredNetwork`, `ERR_BACKUP_TOO_OLD` if unused). Asks: A keep chain remap + `active-chain-id` as an untrusted preference; B fail-closed; C remove now; D creation-only + assert management stays; E no `.well-known/webauthn`; F registry edits only.

## 2026-09-14 — blueprint round 2
Verdict `reject`, one new blocking: the dead-RPC fixture must not bind the compiled seed's port (`localhost:8080` by default, the sandbox's port on network builds) — another agent's run may own it, and REFUSED could then hit a real node. Adopted codex's first option: the stubs keep ephemeral run-owned ports and the test rewires the seeded URL through CDP `Fetch` interception on the service-worker and offscreen targets (`continueRequest({url})` / `failRequest(ConnectionRefused)`), owned by a small `helpers/rpc-intercept.ts`. Round 1's cache-isolation blocker confirmed resolved; nothing else outstanding.

## 2026-09-14 — blueprint round 3
Verdict `approve`. Implementation starts on `harden-b2-backup-passkey`: Phase 1 RP ID, Phase 2 slices + reseed, Phase 3 Developer-Mode gate + smoke.

## 2026-09-14 — Phase 1 green
`RP_ID = "passkey.nulo.sh"`, host permission moved, `content_scripts[0].exclude_matches` for the host and its descendants, SECURITY.md rewritten around the suffix-match rule. `manifest.test.ts` pins the literal value, the exact https host permission, effective injection (a small match-pattern evaluator: RP host + descendant excluded, `nulo.sh`/`tools.nulo.sh`/others still matched) and scans `apps/landing` + `apps/tools` deployables for the host (none). Gate: passkey + manifest suites 28 passed, `build:chrome` with `check-rp-id` OK and the built manifest carries the exclusion, lint clean, typecheck 0. Lesson: a recursive `walk` closure in the scan test scored 17 under the cognitive budget — a flat `readdirSync(recursive)` pipeline reads better and passes. `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b2.md`

## 2026-09-14 — Phase 2 green
Registry: `network` and `fpc` entries gone (a backup carrying either, even empty, rejects as an unknown slice; README notes the retirement). Export: no network/FPC slices, `active-chain-id` replaces the raw id, account-state items carry `chainId`. Import: `reseedNetworksStage` → `NetworkService.seedDefaultsForProfile(profileId)` (live-profile lookup, restore-style fence re-asserted under the lock, idempotent, writes only the target's pointer — never `this.nodes`), `remapNetworkIdByChain` (strict numeric chain lookup incl. 0; malformed/unseeded account-state items and transaction rows dropped and reported through `appendErrors`; transaction rows assigned from `chainId` even without an old id), `resolveRestoredActiveNetworkIdByChain`. `NetworkService.backup/restore`, `FpcService.backup/restore`, `validateRestoredNetwork`, `isNewShapeNetwork`, `ERR_BACKUP_TOO_OLD` deleted (base-class null defaults stay inert); the restore-only test files removed; B1's FPC gate tests plant rows on disk instead. Tests rewritten: registry/migrator fixtures, `useFullBackupImport(.stages)` (chain remap, doctored `network` slice rejected before any write, unseeded rows dropped + reported, active pointer by chain incl. non-numeric/unseeded, seeding failure rollback), `full.test.ts`, helpers (chain resolver + remap), network service (`seedDefaultsForProfile` ×4 incl. node-cache isolation), account-state backup carries `chainId`. E2E fixtures: `buildSyntheticBackup` and the passkey builder carry no network slice; `backup-restore-integrity` no longer falls back through `data.network`; `import-dead-rpc` reroutes the compiled LOCAL seed through CDP `Fetch` interception (`helpers/rpc-intercept.ts`) — the PXE fetches from a dedicated worker inside the offscreen document, which page-level interception never sees, so the helper auto-attaches workers (`Target.setAutoAttach` flatten) and attaches targets by TYPE because a target's url is still empty at `targetcreated`. Gate: unit 87 files / 1342 passed, `test:ci-gating` 100/100, typecheck 0, lint clean; fixture smoke specs (dead-rpc ×3, import-paths, passkey-backup, backup-migration, backup-roundtrip) green on an armed build. `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b2.md`

## 2026-09-14 — Phase 3 green
`networks/index.vue` reads `developerMode` through `ConfigServiceClient.getValue` on mount and follows `onUpdate`; `network-new-btn` renders under `v-if`; the commented-out opener in `SelectNetworksPopup.vue` is gone. Component test: hidden by default, follows a live update both ways, mounts visible when already on; rows stay listed. Smoke: `setDeveloperMode(page, on)` helper (explicit state, returns to the general tab — the settings subpages hide the nav tabs `navigateToSettings` clicks, which is what the first run tripped on, and a vitest retry reuses the profile a prior attempt switched on, which is why the "absent by default" case forces OFF first); the two NewNetworkPopup cases enable it; a new case asserts the button is absent by default while a built-in network's `endpoint-add-btn` stays reachable. Final gate: `audit:vue` first pass red on one stale `"nulo.sh"` expectation in `passkey-ceremony.test.ts` (outside Phase 1's gate paths — the RP-ID grep for stale literals should have been the gate), fixed; armed two-shard smoke 61 + 53 passed with `settings-crud` red on the navigation bug above, fixed and re-run alone 9/9; `audit:vue` re-run for the record below. `test:ci-gating` 100/100.
