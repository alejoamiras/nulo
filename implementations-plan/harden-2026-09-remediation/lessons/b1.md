# B1 lessons

## 2026-09-14 — blueprint round 1 (codex `high`)
Verdict `reject`: exact-L1 equality does not pin `rollupVersion` under the 32-bit composite — `(1 ^ 4248422647) >>> 0 === (1 ^ 8543389943) >>> 0` (reproduced with `bun -e`). Design changed to u32 range guards on both live values + exact L1 + composite. All other round-1 findings verified at source and adopted (cold protocol-address cache in `getFpcImpl`; bind before the fast-path `try`; `syncFailure` on restore; consume-then-reject for the pending passkey secret; `AddressDisplay` hides the address behind a matched name; `registerContract` writes before the compare; extra zeroization sites; per-cell CSV neutralization; `autocomplete="off"` on phrase inputs). Open asks A/B/C answered as recorded in plan.md. Nothing rejected.

## 2026-09-14 — blueprint round 2
Verdict `approve`. Four low-severity coverage notes folded into the phase test lists (cold-cache PrivateFPC, ordering pin before `ensureRegistered`, fallback through `ViewExecutor`, Phase 5 inventory + rejection-path cleanup). Implementation starts on `harden-b1-mechanical`.

## 2026-09-14 — Phase 1 green
`assertLiveChainIdentity` now takes `{chainId, l1ChainId}`, range-guards both live values, requires exact L1 for every row and the composite for non-local rows; `simulate()` in `wallet/utils/fn.ts` takes the network and asserts; `buildNoFrom` asserts before `ensureRegistered`. Five test fixtures gained `l1ChainId` (fast-path, batched-view ×2, discovery-probe, tx-request-builder pins); new pin: `buildNoFrom` drift rejects with calls `["getNetwork","getNodeInfo"]` only. Gate: aztec-runtime 10/10, extension execution+utils+token 802 passed, typecheck 0 errors both packages, lint clean (33 pre-existing warnings untouched). `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b1.md`

## 2026-09-14 — Phase 2 green
`bindOptimizableCalls` in `fast-path.ts` resolves each prefix call by selector against the ABI before the infrastructure `try`; name mismatch throws the same scope-violation text as `execution/service.ts`, an unresolvable contract or a non-public-static rebuild returns `null` (standard arm re-validates every call), inputs are never mutated. `ViewExecutor` passes its resolver. `PxeService.registerContract` derives the address with `computeContractAddressFromInstance` before either write. Tests: 6 new fast-path cases (`@vitest-environment node` — selector derivation walks BB WASM, as the tx-request-builder pins already do), the ViewExecutor fallback now asserts the standard path receives every original call, 3 new PXE cases (genuine pair writes class then instance; tampered address / tampered salt write nothing). Gate: execution 511 passed, pxe 142 passed, lint clean, typecheck 0/0. `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b1.md`

## 2026-09-14 — Phase 3 green
Picker (`fee-helpers.ts`), balance reader and `getFpcImpl` select/accept a `PrivateFpc` row only when `isProtocol === true`; `getFpcImpl` derives protocol addresses instead of reading the restart-empty cache and refuses a non-protocol PrivateFPC (custom sponsored FPCs unaffected). Tests: poisoned-first → canonical selected (picker + reader), only-poisoned → "not available"/null, cold-cache genuine row resolves, foreign PrivateFPC refused, custom sponsored resolves; six legacy fixtures gained `isProtocol: true`. Gate: send+fpc+execution suites green, lint clean, typecheck 0. `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b1.md`
Amended: `FeeSettingsCard.test.ts` mocks `getFpcs` with numeric-typed rows; seven PrivateFPC fixtures gained `isProtocol: true`. Full extension unit suite re-run before amending.

## 2026-09-14 — Phase 4 green
`ContactService.restore` sanitizes `name` like the import path; `TokenBalanceService.restore` writes `updatedAt: 0` and drops `syncFailure`; `getAccount`/`getAccountContract`/`exportAccount` require `row.address === requested` and the imported loader compares the rebuilt address to the requested one. Tests: bidi-override contact name restored as "Alice"; future-dated balance restored stale; a row transplanted under another address's key is neither returned nor used as a signer. Gate: contact+token-balance+account suites exit 0, lint clean, typecheck 0. `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b1.md`

## 2026-09-14 — Phase 5 green
`finalizePasskeyRestoreHoldingLock` enforces `PENDING_RESTORE_TTL_MS` on the entry it consumes (zeroizes both buffers, drops the rewrap context via the existing `dropPendingDekRewrap`, rejects with the existing missing-secret error); `lockActiveProfile` sweeps every pending restore before awaiting closure. Zeroization: `getPasshash`/`getHashHex` encoded bytes, the `Buffer.from` copies of the session bearer token in `wrapPair`/`unwrapPair`, the account-export plaintext on both sides. Tests: two profile integration cases (aged stash refused + dropped; lock drops the stash), `zeroize-sites.test.ts` (4), `account-export.zeroize.test.ts` (2). Gate: profile suite 244, wallet-crypto 116, aztec-runtime account 59, key vectors byte-identical, lint clean, typecheck 0/0/0. Lesson: a first attempt added a second `dropPendingDekRewrap` (one already existed at line ~1382) and typed a Buffer as `ArrayBufferLike` — both caught by typecheck. `LESSONS_FILE=implementations-plan/harden-2026-09-remediation/lessons/b1.md`

## 2026-09-14 — Phase 6 green
F-17: `background.ts` imports `logo.png?inline` and the manifest drops the logo's `web_accessible_resources` entry; the built SW chunk carries the `data:image/png;base64,` string (assumption c verified; no base64 fallback needed). The playground never renders the discovery icon, so "verify against the playground" reduces to the smoke connect flow. F-10: `trim()` scrubs URLs in pre-flattened strings; 100 flatten-before-log sites across `apps/extension`, `extension-messaging` and `aztec-runtime` now pass the `Error` (a regex pass, hand-reviewed: three non-log hits were reverted — a `setFailure({message})` payload and two `throw new Error(getErrorMessage(e))` rethrows — and `DappSendExecutor`/`TransferExecutor` deps widened `logDebug(msg, ...rest)`); the ban test denies `getErrorMessage(`/`errorMessageFromUnknown(` as a log argument, no file allowlist entries added. Autocomplete on the four secret forms, per-cell CSV formula neutralization, full address in the import preview. Tests: `logs-csv.test.ts` (+3: every trigger, quote/line-break vector stays in one cell, ordinary cell untouched), `utils.test.ts` (+1 string branch), new `src/manifest.test.ts` (no WAR; icon import is a data URI). Gate: `audit:vue` exit 0 (5800 tests, 33 pre-existing lint warnings, build ok); smoke e2e (armed build, two `--shard` halves): 16 files / 61 passed + 15 files / 55 passed, 0 failed.
Lesson (smoke sharding): the first smoke attempt was self-inflicted red — the `dist/chrome-shard2` copy is a prefix match for shard 1's teardown `pkill -f "chrome.*--load-extension=…/dist/chrome"` (four `ConnectionClosedError` at `openPopup` in `endpoints.test.ts` the second shard 1 finished), and the `audit:vue` build is unarmed, so `backup-migration.test.ts` throws its remedy. Rerun on the armed build (`VITE_NULO_E2E_MIGRATION_FIXTURE=1 … bun run build:chrome`, `NULO_E2E_MIGRATION_FIXTURE=1` at run time) with the copy at `dist/smoke2`; recipe recorded in the `e2e-testing` skill.
Lesson: the regex conversion must be gated on "the window contains a log call" AND re-read by hand — the first pass rewrote a `setFailure` payload and two rethrows that merely sat next to a log line.
New finding for the owner (not B1 scope): crxjs still emits `web_accessible_resources` for the content-script chunks (`assets/{handlers,crypto,content.ts}-<hash>.js`, `matches: *://*/*`, `use_dynamic_url: false`), so a page that knows a build's chunk hash can still probe for the install; F-17 closed only the static-logo vector. Mitigation is a crxjs option / `use_dynamic_url: true`, to be evaluated separately.

## 2026-09-14 — post-implementation codex round 1 (GPT-6 Astra `high`, session `01a0a0fb-df93-7c92-93e9-9aa8fbe13230`)
Verdict `fix` (items 1–5 should-fix, 6–8 nits); every item verified at source and adopted, none rejected.
1. Import preview: `SettingItem`'s description style is `nowrap` + ellipsis, so the "full address" still truncated. Now rendered through the `#description` slot as a wrapping monospace block; the smoke spec asserts the rendered text equals the address and `scrollWidth <= clientWidth` at the popup width.
2. `execute/index.vue` still logged `getErrorData(err)` (the raw stack) next to the Error at `error` level — dropped; the Error is projected by `trim()`.
3. `account-export.zeroize.test.ts` asserted on the wrong buffer (`fromPassword` encodes the password before the payload, so `encoded[0]` was the passhash input the crypto package already wipes). Rewritten to capture the argument handed to `EncryptionKey.encrypt`; added the rejection paths (failing encrypt, failing decode, failing digest in wallet-crypto).
4. Phase 5 gate: the TTL test aged the stash under a running clock (past the boundary, not at it) and the lock test neither failed the close nor checked the buffers. Now: `Date.now` frozen — exactly at the TTL refused, one tick inside finalizes; lock with a rejecting `sessionManager.close()` still empties both maps and zeroizes every retained buffer.
5. Phase 2 gate: the ViewExecutor fallback test mocked `runFastPath`. Added, in `fast-path.test.ts` (node env), the chain through the real binder, `ViewExecutor` and a real `TxRequestBuilder`: a forged-static first call sends both calls to the standard path, where the second call's name/selector mismatch throws `Scope violation` before `simulateViaNode` or `pxe.simulateTx` run; plus a bind through the real `ContractResolver` against a fake PXE.
6. Restored balance fed to `reconcilePlan` → lands in `staleTokens`; ban-test self-test fixture for `getErrorMessage(`/`errorMessageFromUnknown(` as log arguments.
7. `manifest.test.ts` claim narrowed to the source manifest (the crxjs content-script entries remain, tracked above).
8. Binder comment cut to the contract; biome's import sort had placed the logo import ABOVE `@nulo/wallet-sdk-schema-patch/register` — moved into the second import group so the schema patch stays first (a real ordering regression the round caught).
Gate: execution+profile+token-balance+ban+csv+manifest 295 passed, wallet-crypto 5, aztec-runtime 4, typecheck 0, lint clean (format fixes applied); the account-import smoke spec re-run on an armed build for item 1: 4/4 passed with the no-clipping assertion. Fix commit `af13fdfb`; round 2 resumed on the same session.

## 2026-09-14 — post-implementation codex round 2
Verdict `fix`, one should-fix: the catch-all `console.error(getErrorData(error))` at `execute/index.vue:228` (a pre-existing raw-stack string log I had flagged as a question) is in F-10's scope — replaced with `console.error("Failed to initialize execution", error)`, `getErrorData` import dropped. Nothing else still wrong; items 1–8 confirmed. Round 3 is the hard stop.

## 2026-09-14 — post-implementation codex round 3
Verdict `approve` on the whole B1 range after `d19a3e5c`. Loop converged in three rounds (fix → fix → approve); no residue. Final gate re-run on the converged tree (audit:vue, armed build, two-shard smoke) before `gh stack sync` + `submit`.

## 2026-09-14 — final gate, first pass
`audit:vue` red on `src/design/theme-vars.test.ts`: the import-preview style referenced `--nulo-font-mono`, a token base.css does not declare (the mono token is `--font-mono`). Fixed; the guard is exactly what the design-token contract is for. Smoke shards from that pass are still valid for behaviour (the var only styled the font); the armed build + import spec re-run on the final tree before submit.

## 2026-09-14 — delivered
Final gate on the converged tree: `audit:vue` green after the token fix (5806 tests, build ok), armed two-shard smoke 61 + 55 passed, import spec 4/4 on the final tree. `gh stack sync` hit an `index.md` row conflict on the audit-docs base (dev's tools-recovery row changed next to the appended harden row) — resolved by hand on both branches (keep dev's row, keep the arc's row once), then sync pushed both. `gh stack submit --auto --open`: PR #591 (audit docs + seed → dev), PR #592 (B1 → #591), stack #593. Titles/bodies set with `gh pr edit`. CI (quality, smoke, network) runs on the PR; the owner merges.
