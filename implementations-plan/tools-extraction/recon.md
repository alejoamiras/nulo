# Recon — tools-extraction

Base: `dev` @ `9b2c747b`. Three read-only explorers (package coupling, CI + Cloudflare, plan classification); load-bearing claims re-verified by the driver are marked ✔.

## Reuse map

| Capability needed | Existing code found | Verdict |
|---|---|---|
| History-preserving path extraction | none in repo (`git-filter-repo` not installed; searched `scripts/`, `.github/`, plans for `filter-repo`, `subtree split`) | **build new** — one-shot, run from a throwaway clone; nothing to reuse |
| UI primitives for tools without `@nulo/design` | `packages/design`: tools uses 10 components → transitive set of 12 SFCs (`Toast Button Icon AddressDisplay Flex BalanceRow Card DisclaimerTag DripButton EmojiGrid Tag Spinner`) + `severity.ts`, `color-names.ts`, `layout-names.ts`, `internal/icons.json`, `base.css`, `testing.ts` | **adapt** — copy that closure into a local UI package, same API + testids; rename `--nulo-*` token prefix, neutral palette |
| Component auto-resolution | `apps/tools/scripts/design-resolver.ts` (resolves only `Flex`), `components-plugin.ts` | **adapt** — repoint resolver at the local UI package |
| Layout-agnostic asset resolution | `packages/resolve-asset` (one dependency-free file) | **reuse** — travels per the shared-package decision |
| Account key derivation for deploy scripts + test wallet | `@nulo/wallet-crypto` `deriveNuloAccountKeys`, `EncryptionKey` type (3 tools files, 11 bridge-core files) → pulls `@nulo/wallet-core/utils` (7 imports ✔) | **reuse** — travels per the shared-package decision; closure is FOUR packages, not three |
| Custom-RPC schema patch | `@nulo/wallet-sdk-schema-patch/register` (tools only; bridge-core does not use it); fail-open by design | **reuse** |
| Fee prediction in the wallet | `packages/bridge-core/src/fee-juice.ts` (148 lines, `@aztec/*`-only imports ✔) consumed by 4 extension prod files + 1 test ✔ | **adapt** — relocate into `packages/aztec-runtime` BEFORE extraction; unleashed keeps its own copy |
| Static-site Worker (`assets` binding) + Workers Builds | none. `infra/passkey-rp/wrangler.jsonc` is a code Worker deployed by hand (`workers_dev:false`, `preview_urls:false`, custom-domain route) | **build new**, borrowing `compatibility_date` / `account_id` / route shape from passkey-rp |
| Response headers on Workers | landing: committed `apps/landing/public/_headers` (CSP, STS, cache). tools: `_headers` GENERATED by `headersPlugin()` in `apps/tools/vite.config.ts` (COOP/COEP + per-target CSP) | **reuse** — Workers static assets honours `_headers`; verify COOP/COEP survive |
| Preview-host allowlist + build id | `apps/tools/src/lib/preview-hosts.ts`, `build-integrity.ts`, `buildMetaPlugin()` — keyed on `CF_PAGES_URL` / `CF_PAGES_BRANCH` / `CF_PAGES_COMMIT_SHA` and exact `*.pages.dev` hosts; `network-targets.ts` pins `tools.nulo.sh` / `testnet.tools.nulo.sh` as the only prod hosts | **adapt** — Workers Builds env + `*.workers.dev` preview host shapes; hosts become config |
| Exact-state CI aggregator | `scripts/ci-cd/aggregate-status.sh` (+ test); sole caller is `pr-tools-e2e.yml` | **reuse in unleashed**, delete or keep-orphaned in nulo |
| Required-check repointing | `scripts/ci-cd/required-checks.{ts,sh}` | **adapt** — drop `e2e:tools` label + `bridge-contracts-status` rename in nulo |
| npm publish pipeline | none (searched `npm publish`, `publishConfig`, `provenance`, `id-token: write`, release-please components) | **build new** IF the publish route is kept |

## Shared-package surface

- tools → `bridge-core` (99 files; subpaths `.`, `/sandbox`, `/private-fpc-artifact`, `/artifacts`), `design` (8 files barrel + `base.css` + `/testing`), `resolve-asset`, `wallet-crypto`, `wallet-sdk-schema-patch`.
- bridge-core → `resolve-asset`, `wallet-crypto` (→ `wallet-core/utils`).
- All four candidate packages: `private: true`, `exports` → raw `.ts`, no build, `@aztec/*` as exact plain deps (duplicate-WASM-instance risk if pins drift; tools' vite config already dedupes `@aztec/noir-*`).
- `contracts/bridge/**` imports no `@nulo/*`.

## Reverse coupling (nulo → tools/bridge) — everything that breaks on deletion

| Site | Kind |
|---|---|
| `apps/extension/package.json:62` + 4 prod files importing `@nulo/bridge-core/fee-juice` ✔ | hard import |
| `scripts/release/chain-guard.ts:15` imports `apps/tools/src/lib/chain-constants.ts` ✔ | hard import (release pipeline) |
| `scripts/release/verify-live{,-run}.ts` tools half — compares tools build sha to nulo's release sha; defaults to `https://testnet.tools.nulo.sh` | structurally invalid post-split → remove |
| `apps/extension/src/manifest.test.ts:66` — `readdirSync("apps/tools")` | throws |
| `scripts/ci-cd/behavior-gating.test.ts` — reads deleted workflows, asserts tools filters | throws |
| `scripts/ci-cd/aggregate-status.test.ts:41-45` — reads `pr-tools-e2e.yml` | throws |
| `scripts/ci-cd/required-checks{,.test}.ts` — `e2e:tools` label, `bridge-contracts-status` | stale asserts |
| `scripts/complexity-baseline/manifest.json` — 3 acceptances (2 tools, 1 bridge-core) | `bun run lint` red until regenerated; CLAUDE.md counts (35 / 7 custody scripts) shift |
| `.github/actions/setup-playwright/action.yml` — hardcodes `apps/tools/` paths | delete if sole consumer is tools-e2e |
| `fee-helpers.ts:218` `FEE_JUICE_BRIDGE_URL = "https://tools.nulo.sh"` ✔; `apps/landing/index.html:44,215` ✔ | live user-facing links |
| `price-map.ts:37-41`, `default-tokens.ts:57,68`, `fpc/service.ts:37` | comments citing tools paths |
| root `package.json`: `dev:tools build:tools test:tools e2e:tools e2e:tools:reap audit:tools`; `build` | scripts |
| root `tsconfig.json` reference to `apps/tools`; `biome.json:17,343` | config |

## CI

- **Delete**: `bridge-contracts.yml`, `_bridge-contracts.yml`, `pr-tools-e2e.yml`, `_tools-e2e.yml`, `_build-tools.yml` (→ recreate in unleashed).
- **Edit**: `pr-quick.yml` (tools filter, `needs-tools-build`, `build-tools` job, status needs), `release.yml` (`deploy-tools`, `verify-live` needs, status needs), `refresh-landing.yml` (`tools|both` target).
- **Untouched**: `nightly.yml`, `_unit-tests.yml`, `_lint-and-typecheck.yml`, `actionlint.yml` (workspace globs auto-shrink).
- `bridge-contracts-status` / `tools-e2e-status` were never required on either branch → no branch-protection change on GitHub. `main` still carries `contracts/bridge`, `packages/bridge-core`, `apps/faucet` ✔ — removal reaches it at the next promote.

## Cloudflare today

- Pages projects: `nulo` (nulo.sh, landing, deploy hook `CLOUDFLARE_PAGES_DEPLOY_HOOK`), `nulo-tools-testnet` (testnet.tools.nulo.sh), `nulo-tools-mainnet` (tools.nulo.sh, placeholder while `bridge: null`), secret `CLOUDFLARE_TOOLS_DEPLOY_HOOK` (may be unset).
- Env: `BUN_VERSION=1.4.0`; tools also `NODE_VERSION=24` (vite config imports raw `.ts`).
- No `_redirects`, no SPA fallback in either app. Landing: single static `index.html`.

## Docs to rewrite

`CLAUDE.md` (~15 sites: bridge pointer, § Two products, Pages/NODE_VERSION, advisory aggregators, Renovate third pin site, complexity counts, quality-gate rows, release runbook deploy-tools / verify-live / troubleshooting), `CI.md` (rows + 2 sections + release narrative + gating bullets), `ARCHITECTURE.md:239,241`, `UPDATE.md` (§ Any-ERC-20 bridge couplings + 5 bullets → unleashed), `.github/README.md:14-15`, `.claude/skills/aztec-update/SKILL.md` (Phase 0 probe + all of Branch B → unleashed). Clean: `README.md`, `SECURITY.md`, `renovate.json`.

## Layout constraints unleashed must preserve

`contracts/bridge/evm/foundry.toml` (`allow_paths = ["../../../node_modules"]`), `packages/bridge-core/scripts/gen-remappings.ts`, `apps/tools/scripts/e2e/agent.sh` (`--cwd ../../packages/bridge-core`), `../../vitest.base` imports, and two tools tests hardcoding `packages/design/src/base.css` (`theme-vars.test.ts:7`, `app.css.parity.test.ts:10`). Root shared config tools/bridge rely on: `vitest.base.ts`, `bunfig.toml`, `patches/` (`@aztec/noir-*`), `biome.json`, `.githooks`, `commitlint`.

## Implementation plans (268 dirs; no `archive/`, no top-level `lessons.md` / `follow-ups.md`)

- **TOOLS-ONLY (36) → move**: any-erc20-bridge, bridge-evm-verification, bridge-form-stepper, bridge-hardening, bridge-permit2-recipient-commitment, bridge-private, bridge-scripts-dedup, bridge-seal-backup, bridge-security-remediation, bridge-ux-feedback, bridge-ux-trust, bun-native-tooling, dedup-bridge-conductors, deposit-decomposition, design-system-faucet-adoption, faucet, faucet-bridge, faucet-cluster, faucet-journal-bugs, faucet-multi-account, faucet-receipt-hero, faucet-wallet-picker, fuel-direct-bridge, fuel-l1-mint, fuel-portal-v5-fix, journal-engine-decomposition, no-fuel-claim-fee-source, operator-gates, private-exit-fence, private-fuel, private-fuel-fee-fix, send-wizard-ux, swap-fuel, tools-console, tools-recovery, tools-two-network.
- **Correction (N5b review, 2026-09-24)**: 8 of the 36 above are MIXED too — their own PRs changed code that stays in nulo — so they move to unleashed AND stay here: any-erc20-bridge (#544, the wallet's dApp-named self-pay), bridge-permit2-recipient-commitment (#260, tx enrichment + dApp send executor), bridge-private (#78, wallet-bridge scope enforcement), dedup-bridge-conductors (#377, dispatcher auth), operator-gates (#525, `scripts/ci-cd/test-soak`), private-fuel and swap-fuel (wallet-bridge dispatcher, embedded FPC cap), private-fuel-fee-fix (the embedded fee-cap pin and its deferred fjwc inclusion risk). Test used: files each plan's own commits changed outside the tools roots.
- **MIXED (36) → stay in nulo**; unleashed gets them through preserved history only where a live reference needs them: `packages/bridge-core/scripts/live-intent.ts:171-191` allowlists `aztec-5.0.0-stable/lessons/`, `aztec-5.0.1-line/lessons/`, `tools-two-network/lessons/`, `any-erc20-bridge/lessons/` (+ `NO_RESET_BASELINE` intent.json) → those lessons dirs must exist in unleashed.
- Live references into plans from nulo that must keep resolving: `CLAUDE.md:46` (`isolated-linker-store/tools/phantom-sweep.ts`, executable), `scripts/ci-cd/test-soak/cli.ts:39` (`vitest-on-bun/lessons/baselines`), and doc cites to MIXED plans — all stay. `contracts/bridge/aztec/scripts/run-txe-tests.sh:4` cites `bridge-hardening/lessons/` — moves with it.
- `index.md`: flat list, tools entries interleaved; 4 TOOLS-ONLY plans have no entry.

## Absence claims (search trails)

- No `apps/faucet` on dev (`find -iname '*faucet*'` → plan dirs only); present on `main` ✔.
- No infra Worker for tools (`infra/` = `passkey-rp` only).
- No `faucet`/`dripper` in `.github/` (`grep -rni`).
- No `.worktreeinclude` at the repo root.
- No tools/bridge path in `renovate.json`, `SECURITY.md`, root `README.md`.
