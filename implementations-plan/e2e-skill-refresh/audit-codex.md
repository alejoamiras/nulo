# Codex plan audit — 2026-09-06 (xhigh, read-only, fresh session)

Verdict: `plan needs changes`. All five findings adopted; see plan.md § Audit outcome.

- **Medium — Cold-wake needs a stronger postcondition.** [cold-wake-discovery.test.ts:103](apps/extension/tests/e2e/network/cold-wake-discovery.test.ts:103) requires **no live worker before discovery**; [helpers.ts:1768](apps/extension/tests/e2e/fixtures/helpers.ts:1768) accepts an already-running replacement. This is the sole caller where that witness cannot establish the test’s premise. Its existing absence assertion prevents false greens, but the fallback becomes a failure. **Fix:** explicitly preserve the cold-stop requirement and assertion; allow caller-specific handling instead of promising an unconditional import-only conversion. Do not warm the worker to satisfy this test. **Confidence: high.**

- **Medium — Removing the canaries’ absent-worker branch loses valid recovery behavior.** Both [frozen:45](apps/extension/tests/e2e/network/frozen-account-canary.test.ts:45) and [passkey:56](apps/extension/tests/e2e/network/passkey-execution-canary.test.ts:56) tolerate an already-stopped worker. The shared helper waits for a target before stopping it ([helpers.ts:1740](apps/extension/tests/e2e/fixtures/helpers.ts:1740)); waiting does not initiate recovery. “Should see a fresh one within 15s” is unsupported. **Fix:** retain an explicit absence-only path into recovery, without swallowing actual stop failures. Preserve passkey’s anchor popup/FrameTreeNode. **Confidence: high.**

- **Medium — Validation has a false retry-zero claim and incomplete canary coverage.** Smoke hardcodes [retry: 2](apps/extension/vitest.e2e.config.ts:41), so `NULO_E2E_RETRY=0` does nothing there: specify `--retry=0`. Frozen once locally plus CI supplies functional evidence, not a load soak; its recovery already documents starvation failures ([frozen:245](apps/extension/tests/e2e/network/frozen-account-canary.test.ts:245)). **Fix:** add repeated prover-ON frozen runs under `taskset -c 0,1`, and one prover-ON passkey run: the [CI canary selection:237](.github/workflows/pr-network-e2e.yml:237) excludes passkey. **Confidence: high on configuration; moderate on soak sufficiency.**

- **Medium — The rewrite already proposes wrong guidance.** [plan.md:125](implementations-plan/e2e-skill-refresh/plan.md:125) changes “3 attempts” to “2”; `retry: 2` permits **three attempts**, confirmed by the installed [runner’s inclusive loop:2934](node_modules/.bun/@vitest+runner@4.1.10/node_modules/@vitest/runner/dist/chunk-artifact.js:2934). Drop that edit. Also replace [plan.md:100](implementations-plan/e2e-skill-refresh/plan.md:100)’s ambiguous “two designed outcomes” with **two deliberately gated scenarios, each enforcing its own contract** ([backup header:4](apps/extension/tests/e2e/network/backup-restore-sw-restart.test.ts:4)). The likeliest propagation mechanism is promoting stale explanations into authoritative rules because their symbols still exist. The proposed grep checks existence, not meaning. **Fix:** review complete sentences against executable behavior. **Confidence: high.**

- **Low — Keep procedures, qualify generalizations.** The proposed structure and ≤550 lines are credible if narratives move to references. Retain first-truthy-versus-dwell guidance ([helpers.ts:1301](apps/extension/tests/e2e/fixtures/helpers.ts:1301)), documented click workarounds, `domcontentloaded`, and file-backed diagnostics. Remove universal claims about mtime ordering, inevitable `networkidle0` failure, and swallowed passing-test stdout; preserve their actionable precautions. **Confidence: high.**

Other caller contracts survive: [imported-account:118](apps/extension/tests/e2e/imported-account-lifecycle.test.ts:118), both backup scenarios, and [connect-locked:110](apps/extension/tests/e2e/network/connect-locked-queue-sw-restart.test.ts:110) need the old instance gone. Connect’s baseline → stop → reopen → freshness ordering remains appropriate; freshness alone is not restart proof. A4 is confirmed by the [measured comparison:44](implementations-plan/deflake-round-3/lessons/phase-3.md:44). No broader timeout/fixture work is justified; the remaining documentation scope fits one PR.

plan needs changes