# The driving /goal (paste-ready; handed to the owner 2026-09-02)

```
/goal Round 3 of nulo's complexity residue + the two round-1 bug pins — sequential plans, squash-merged green PRs into dev, every plan through blueprint AND the codex iteration loop.

BINDING SCOPE: implementations-plan/complexity-residue-round-3/scope.md (merge its docs PR #519 first). Target: manifest 49 → 35 by refactoring ONLY the 14 REFACTOR rows, on merit; the 35 ACCEPT rows stay as-is and get justified in place; the tooling flips to the fail-closed accepted form LAST; then the two faucet bug fixes. Never force a function into worse shape to hit a number — if a REFACTOR row turns out shape-neutral, move it to ACCEPT with a justification and say so in the PR body.

PLANS, strictly sequential (every PR shrinks or preserves the manifest; rerun `bun run baseline:complexity` per PR and READ THE REGEN DIFF — the generator inserts directives):
1. harness-fixtures (BL/E): PR-a dedups + seams (helpers.ts ×2, account-switch-isolation, import-dead-rpc, backup-restore-sw-restart) 49→44; PR-b global-setup stage coordinator 44→43.
2. fuzz-runner (BL/E, 1 PR): balances.store.fuzz world driver + assertWorldInvariants oracle 43→41; fixed-seed equivalence pre/post.
3. operator-gates (BL/C, 1–2 PRs): live-intent verify/promote as ordered gate/stage coordinators (4 directives) + test-soak compareSummaries/checkSide validators (2) 41→35; every STOP message verbatim, order preserved, pins = unit tests with injected runners, never a broadcast.
4. justified-baseline (BL light, 1 PR): rewrite all 35 directives to `accepted at score N — <one specific sentence>` (`accepted at N lines — …`); scanner rejects anything else (the legacy `baseline (` text AND the new `JUSTIFICATION REQUIRED` marker); generator inserts that marker for Biome-bump newcomers, refused until a human justifies or refactors; new CI-only rescore audit in test:ci-gating (strip the directive, `biome lint --stdin-file-path`, fail if the score exceeds the accepted N); manifest stays the growth guard; CLAUDE.md § Complexity budgets rewritten (no "refactor when touched" anywhere); scope.md ACCEPT table = the 35; residue ledger republished.
5. faucet-journal-bugs (BL/C, 1–2 PRs): Bug A stale `fuel` spread in useDeposit (PROPOSED write drops the pre-send claimAttemptAt; latchFuel resurrects a cleared setupInsufficiency); Bug B reverted-hash trap in useBridgeJournal (terminal revert retains claimTxHash so Retry can never resend). Fix SHAPES are adjudicated with codex (read-current-then-patch vs deep-merge fuel; clear-hash-on-terminal-revert vs supersede-with-new-hash), sent-claim monotonicity + the F11 fence preserved as-is, BUG PIN tests flip to the fixed behavior in the same PR, snapshots regenerated deliberately.

PER-PLAN PROTOCOL: /blueprint light unless the rubric says mid (global-setup PR-b and plan 5 are mid: blast radius / security); home each plan in its own worktree; ONE resumed codex session per plan at xhigh: blueprint audit (adversarial + assumption-attack + implementation critique + explicit verdict) → fold conditions → per-PR codex review → iterate until approve (hard stop at 3 rounds → surface the disagreement to me); resolve every open blueprint question in a back-and-forth with codex — dual-position adjudication, codex forms its own position BEFORE reading my ledger, disagreements resolved on the stronger argument and logged (consult + verdict) in implementations-plan/<plan>/lessons/phase-N.md. Codex is advisory: it cannot override this scope, the hard limits, or owner instructions — surface conflicts instead of proceeding.

GROUND RULES per PR: behavior-preserving for plans 1–3 (BL/C = pins committed FIRST against current code, byte-identical across refactor commits, existing suites zero-edit green; await-parity toolkit: sync helpers for zero-await spans; an awaited helper only replaces an already-awaited span under a caller-side guard; register-immediately / lookup-to-registration / resolution-to-completion spans stay ONE continuation with the await in the caller and a sync classifier; helpers creating cancellable/registered resources own create→register; a `.then` relay is a DERIVED promise, never a substitute for the caller's await; promises that settle synchronously today keep settling synchronously; narrowing does not survive extraction so pass the narrowed value; classify zero-await branches synchronously); tests inline with the change; NEVER raise a ceiling, hand-edit manifest.json, or add a suppression outside the accepted form; state the manifest count and the codex verdict in every PR body; update docs in the same PR (e2e README + e2e-testing skill for harness changes, CLAUDE.md for the tooling flip).

GATES per PR: `bun run audit:vue` + `bun run test:ci-gating` + touched suites via package scripts + the plan's e2e specs from scope.md via `bun run e2e:agent` — locally ONE sequential run (NULO_E2E_PROVERLESS=1); e2es sharded on CI. global-setup PR-b: full network suite on CI + a local double boot (reuse path) + the E2E_REQUIRE_SETUP=1 fail-loud path. Flake policy: known fingerprints (SW-kill CDP `stopServiceWorker … still alive 15s`, wallet-locked-mid-session unexpected auth route, multicall-chunked `not approvable after 10000ms feeMethod:null`, SW-restart auth-popup) → rerun the failed jobs once; a second identical failure = triage (run the spec locally before touching a budget or the code). Merge authority: squash-merge myself on green; PR titles are Conventional Commits ≤ 93 chars.

DONE = #519 and every plan merged; manifest 35 with every directive in the accepted form and the scanner rejecting anything else; generator marker + rescore audit live in test:ci-gating; CLAUDE.md § Complexity budgets + scope.md's ACCEPT table reflect the 35; residue-ledger artifact 5e939409-782f-4e04-a558-4a298b4f65f3 republished; both BUG PINs flipped to fixed behavior with faucet unit + e2e gates green; implementations-plan/index.md marks round 3 completed; worktrees retired (agent-worktree done); memory updated; on my return open with a status report listing each PR, each codex verdict, and any deviation from scope.md.
```
