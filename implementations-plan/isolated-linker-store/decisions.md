# Arc B — every codex-converged decision, with reasoning

The owner's standing protocol for this arc: plan-time gates and open questions resolve by iterating with Codex to convergence; merges stay the owner's. This is the consolidated ledger (details: plan.md § Audit log, lessons/, audit-codex.md, audit-fable.md).

| # | Decision | Ruled by | Reasoning that carried |
|---|---|---|---|
| 1 | **Consumers-first** migration (make tooling layout-agnostic on hoisted, THEN flip) over the ~10-line `publicHoistPattern` bridge | pre-arc consult + both round-1 auditors + final pass (unanimous) | The bridge recreates the phantom-dependency API the isolated linker exists to kill, makes the least-documented feature corner load-bearing, leaves two pre-existing bugs in place, and still requires every deferred refactor to reach `hoist = false`. Kept only as a time-bounded emergency fallback with a sunset. |
| 2 | A NEW bottom-layer package `@nulo/resolve-asset` over in-place fixes ×6 or sharing `vite.shared.ts` | round-1 codex + fable | Six copies had already drifted (the bug class itself); the layer ban forbids packages importing from apps/extension. |
| 3 | Discovery via **`require.resolve.paths(pkg)` scan** (documented search order + manifest-name validation) over the original three-attempt exports-guessing cascade | owner design challenge → fresh codex round (alt-9) | The owner's "monkey-patchy" instinct was right about the mechanism, wrong about the package: codex steelmanned 9 alternatives; the package survived all, the cascade lost. `entry` anchors deleted from the API. `/@fs/` normalizer kept centralized (pushing it to callers spreads cwd/transform assumptions). |
| 4 | **Flip FIRST on the unchanged v1 lock, regenerate SECOND** (codex's ordering) over fable's regen-first | round-1 resolution + final-pass endorsement | Flipping on a frozen resolution isolates LAYOUT as the only variable; the regen then isolates RESOLUTION on a proven layout. Fable's underlying demand (one root cause per commit) is fully met. |
| 5 | Explicit `@aztec/sqlite3mc-wasm: "5.0.1"` declaration (a manifest edit, NOT a version-line change) + a permanent **lockstep** realpath assertion vs `@aztec/kv-store`'s copy | round-1 codex (approved under the delegated gate) + fable (lockstep guard) | The graph already resolved exactly that pin; declaring it removes hoisting luck from a SHIPPED asset; the lockstep test closes the future kv-store-skew channel. Two-hop resolution rejected: the extension owns its shipped assets. |
| 6 | Wallet-grade regen review = full-record diff (integrity + resolved + edges + bins + scripts), added/removed names, byte-identity for the frozen `@aztec*`/`@alejoamiras` scopes, `bun pm diff` on every bundle-reachable or @aztec-subtree move regardless of semver class, provenance + publish age per move, advisory delta, Noir dup count — done on a DRY regen BEFORE execution | round-1 both + final pass (restored the classes v2 had dropped) | "Seven-day age is a delay, not provenance"; version equality cannot see a registry-side tarball swap; a patient ≥7d attacker lives exactly in "patch-within-range auto-pass". |
| 7 | Remove the aged-out `minimumReleaseAgeExcludes` BEFORE the regen | fable High | Otherwise the arc's one full re-resolve would run with the entire @aztec namespace blanket-exempt from the gate. (Supersedes Arc A's deferral of the same cleanup.) |
| 8 | Global-store probes in SCRATCH before any repo install; concurrency as N-way STRESS + interruption + patch-isolation, framed as risk ACCEPTANCE not proof | codex High | A two-install smoke is anecdote; extraction-time integrity doesn't cover later same-UID mutation. |
| 9 | **Committed default = isolated linker WITHOUT `globalStore`**; the store is a narrow per-user opt-in (`~/.bunfig.toml`) for trusted single-user worktree-heavy dev boxes only — never CI/shared/release | Phase 2 posture memo + keep/abort gate | Probed: `globalStore` is off by default and lives INSIDE `~/.bun/install/cache/links`, which CI's `actions/cache` restores — so the committed default avoids the F-1 exposure by construction. |
| 10 | Dev-server smoke must PRECEDE the production build that e2e consumes | Phase 3 incident | Under @crxjs, `vite dev` overwrites `dist/chrome` with dev loaders; the smoke suite then loaded an extension whose service worker imported from a dead dev server → 84 spurious failures. |
| 11 | **KEEP** the flip despite the speed thesis failing (committed default 1.22s vs hoisted 1.17s; opt-in store 0.36s) | fresh-context codex keep/abort gate | A shipped production asset already depended on hoisting luck — phantom enforcement addresses a DEMONSTRATED failure mode; the compatibility tax was exercised unusually thoroughly (builds, dev server, vitest pools, storybook, forge, packaged outputs, live network shard); the regen adds independent value (40 → 23 advisories). |
| 12 | `hoist = false` stays OUT of the arc (follow-up after soak) | pre-consult + all audits | Unknown phantom `require`s inside the @aztec transitive graph would hard-fail at runtime; needs its own gate. |
| 13 | Post-impl round-1 findings all adopted: realpath lockstep anchor (the guard was circular under isolated — verified), exact-line remap assertion, per-PID temp file, `components.d.ts` regen (storybook's regen had dropped 9 globals), README pre-generation step, `forge build` gate actually run (lib populated) | post-impl codex | Each verified against the repo before adoption. |

Not decided by codex (owner-reserved, honored): every merge; the `@aztec/*` version line (untouched); required checks (untouched).
