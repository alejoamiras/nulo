# Fable plan audit — round 1 (2026-09-06, Plan subagent on Fable 5.1, read-only, independent of the codex round)

Verdict: `conditional approve (with conditions: (1) shards sequential or one-per-worktree; (2) comparator evaluates held before priced; (3) rewrite the Phase 2/6 test claims and add the price-client mock + storage backing to TokensView.test.ts; (4) add token-menu-trigger and the ids→symbols fetch to Phase 6; (5) bound symbols with sanitizeWireString(…, 32), validate the popup payload and the stored map, scope deletion cleanup to the event's profileId; (6) glyph-coverage check as a Phase 1 gate item; (7) Bootstrap: canonical-clone wording and git push before the stack exists; (8) use useEntityCrud's real API (refresh, incremental+accept); (9) drop sanitize from the search input. Recommended: absorb outline-alt §3 (SettingItem picker) and §5 (ConfirmPopup single-action).)`

Conditions 1, 2, 3 (partly), 7, 8, 9 coincide with codex round 1 and were already in plan r2. The rest are folded into r3 as below. Every factual claim was re-verified on the tree.

## Findings and disposition

| # | Sev | Finding (verified?) | Disposition |
|---|---|---|---|
| A1 | Medium | Symbols/names are contract-supplied and unbounded in the popup body, the pinned list and the toasts; layout, not XSS. Precedent: `IncomingTrustPopup.vue:52` routes through `sanitizeWireString(symbol, 32)` (`dapp-session/capability-meta.ts:176`). **Verified.** | **Adopted.** Every symbol rendered by the pin flow (popup, toasts) passes through `sanitizeWireString(…, 32)`; the popup payload is validated (strings, ≤ 3). |
| A2 | Medium | The design `Input`'s `sanitize` prop is a normalizer, not a guard; kills `/ ' $ +` in a search. | **Adopted** (also codex #5): no sanitizer, `maxLength` only. |
| A3 | Low | Hostile MAP, not just hostile list: the read-modify-write re-persists junk sibling keys forever. | **Adopted.** Non-object → `{}`; non-array values dropped; each list validated; the sanitized map is what gets written back. |
| A4 | Low | Deletion consumers MUST scope to the DELETED token's profile (`token/spec.ts:220-225`, in caps). **Verified.** | **Adopted.** Cleanup writes to `pinnedTokensKey(event.profileId)` and removes the contract from the list for `event.chainId`. |
| A5 | Low | Pins keyed by `networkId` while tokens are per `chainId`. | **Adopted, stronger**: pins are keyed by **chainId** (the token set's own scope), so deletion cleanup and the picker share one key space. |
| A7 | — | Make the "no new deps" claim a literal gate command. | **Adopted**: `git diff --exit-code bun.lock` at every arc boundary. |
| B | — | Fact drift: `tokens-menu-*` span `TokensView.vue:389-424`; the touched SFCs are plain-JS `<script setup>`; the payload idiom is the `:payload="popupStore.getPayload(...)"` PROP (`PopupManager.vue:346`), not a self-read; `useEntityCrud` is `refresh({clear})` with `mode` as a construction option; recon slips (storage-keys holds one key; 29 popups; no "helper conventions table" in the e2e README; "View Archives"). **Verified.** | **Adopted**: facts corrected; new SFCs are `<script setup lang="ts">`, touched JS SFCs stay JS; recon.md patched. |
| B-Inf1 | — | The cap of 3 is safe for a narrower reason: no e2e imports more than two tokens on top of the fixture's one; the real exposure is first-card helpers (`fiat-send.test.ts:47`, `helpers.ts:796`) and `waitForTokenCardAmount` (`helpers.ts:1528`). Fixture tokens are unpriced, so name order is preserved today. | **Adopted** as the inference's wording; the symbol-aware `navigateToTokenDetail` from r2 stays. |
| B-Inf4 | Medium | `MaterialIcon` renders by ligature (`MaterialIcon.vue:7`) from a 343 KB subset; a missing glyph renders the literal word. | **Resolved to a Fact**: `uvx --with brotli --from fonttools python -c "…getGlyphOrder()…"` on the Mac shows `home` and `push_pin` present (3,972 glyphs). The one-liner stays in the Phase 1 gate for the homelab. |
| C1 | High | Comparator class order puts a priced zero-balance token above a funded unpriced one. | **Already in r2** (codex #6): emptiness first. |
| C2 | High | `TokensView.test.ts:51-106` mocks exactly four clients; a `PriceServiceClient` (whose `usePrices` calls `refreshIfStale()` at construction, `usePrices.ts:35`) breaks every existing case unmocked; Phase 6's storage read needs an in-memory `chrome.storage.local` (`vitest.setup.ts:88-90` leaves it `{}`). **Verified.** | **Adopted.** Phase 2 adds the price-client mock; Phase 6 adds the storage backing; the claim "existing cases untouched" is gone. |
| C3 | Medium | `TokenCard` select mode + `TokenList mode` is the wrong boundary for the Send picker: N per-row price clients in a popup, a button-root variant, listbox semantics, a fold inside a picker. Same rows + same order + search satisfies the owner's decision. | **Adopted, flagged at the gate**: `SelectTokenPopup` keeps its `SettingItem` rows, sorts them with `orderTokenRows`, gains the search `Input` past three tokens, no fold. `TokenCard` is untouched; `TokenList` has one consumer. The owner picked "same component" — the gate asks whether "same order + search" is acceptable. |
| C4 | Medium | `TokenList` props leak the page's service graph. | **Moot after C3** (one consumer); `dustThresholdUsd` still defaults to 0 → empty-only fold. |
| C5 | Medium | `useEntityCrud` `resync` mode is for scope-less payloads; balances carry `account` — use `incremental` + `accept: tb => tb.account === active` and `refresh({ clear: true })` on scope change. **Verified** (`useEntityCrud.ts:27-58`). | **Adopted.** |
| C6 | Medium | A single-action mode on `ConfirmPopup` is one `v-if` + one relabel (`ConfirmPopup.vue:135-153`) versus a new SFC + registration + tests; the description slot already takes text. | **Adopted** (reverses r2 / codex #13's preference): `cacheStore.confirm.single = true` hides Cancel; body carries the symbols inline. Recorded as a disputed point for the final codex pass. |
| C7 | Medium | The token page loads ONE token (`[id].vue:122`); composing the pinned symbols needs the chain's token list. | **Adopted**: on `"full"`, `tokenService.getTokens(profile.id, network.chainId)` maps contracts → symbols. |
| C8 | Medium | The "⋯" trigger (`[id].vue:169`) has no testid; e2e cannot open the menu under the testid rule. | **Adopted**: `data-testid="token-menu-trigger"`. |
| C9 | Medium | Phase 4's e2e claim targeted `sendTransfer`, which never opens the picker. | **Already in r2** (codex #2); picker rows get `select-token-row` + `data-symbol`. |
| C10 | Low | Pure helpers under `modules/holdings/` make `holdings/` an upstream of `general/` and `popups/`. | **Adopted**: `src/utils/token-order.ts`, `token-fold.ts`, `token-search.ts`, `token-aggregate.ts`. |
| C11 | Low | `storage-keys.ts` is an `as const` string map; a function inside it changes the shape. | **Adopted**: a separate `pinnedTokensKey(profileId)` export beside `UI_STORAGE_KEYS`. |
| C12 | Low | Touched files carry banned milestone vocabulary (`TokensView.vue:38-46,188,237`); the post-impl comment loop will flag it. | **Adopted as a rule**: an arc cleans only the comment lines it touches. |
| Gates | — | Arc A's network files run under name order (fixtures unpriced) and do not exercise value ordering. `fiat-send.test.ts:27-33` seeds a quote via `nulo:core:token-prices` + reload. | **Adopted**: `holdings.test.ts` (Phase 3) seeds a quote the same way and asserts the priced token ranks first; Phase 2's unit test covers the comparator. |
| D2 | Medium | Bootstrap assumes `~/Projects/nulo`; say "the canonical clone per `~/.agents/clones.md`". | **Adopted** (the homelab's is `/home/homelab/Projects/nulo`, confirmed over ssh; the wording now says both). |
| D3 | Medium | `gh stack push` before the stack exists. | **Already in r2**. |
| D4 | Low | `frozen-account-canary` runs under a proverless build in the full pool; not the prover-ON signal. Complexity budgets (`bun scripts/complexity-baseline/check.ts`, 15/80) are a risk for TokenList's computeds. `holdings-search` lands on the `Input` root wrapper (`Input.vue:252`): drive `[data-testid="holdings-search"] input` with `replaceInputValue`. | **Adopted** as notes in Phases 3 and 7. |
| E | Low | Copy: `View all` clashes with `View Archives` (keep one voice); sort labels in the uppercase-mono voice (`BY VALUE` / `A–Z`); "Hide dust" fine; `HOLDINGS` as Home's section title triples the word. | **Adopted**: `View all` (no arrow), `BY VALUE` / `A–Z`. `HOLDINGS` on Home stays (owner-approved mockup); `View Archives` untouched. |
