# Codex plan audit — round 1 (2026-09-06, GPT-6 Astra `high`, read-only, fresh session)

Verdict: `reject (with blocking findings: unsafe shard execution, invalid smoke gates, pin-persistence correctness gaps, and missing Send-picker integration coverage)`. Every factual claim was re-verified on the tree before adoption; the plan revision that follows adopts all but one.

Session: `01a0787f-43e7-7ba1-a1ba-3373a9322321`. Confidence: high on source-backed findings; unknown on homelab capacity and bundled icon coverage.

## Findings and disposition

| # | Sev | Finding (verified?) | Disposition |
|---|---|---|---|
| 1 | High | Smoke gates never build the extension: `tests/e2e/global-setup-smoke.ts:38` only checks that `dist/chrome/manifest.json` exists; a fresh checkout fails and an old checkout tests stale code. CLAUDE.md requires `audit:vue` before a UI PR. **Verified.** | **Adopted.** Every smoke gate is now `bun run build && bun run test:e2e`; the arc boundaries run `bun run audit:vue`. |
| 2 | High | `sendTransfer` (`helpers.ts:871`) never selects a token, so Phase 4's "existing transfer tests cover the picker" was empty. Selection must emit `tb.token.id`, not the balance-row id (`token-balance/spec.ts:66`). **Verified.** | **Adopted.** Phase 4 adds `tests/e2e/network/send-picker.test.ts` (open `send-token-trigger`, pick a different token by `data-token-id`, assert the trigger shows it, reopen and see it selected). `TokenList` emits `token.id`. |
| 3 | High | Parallel shards from ONE worktree collide: `agent.sh` rewrites `.e2e-state/ports.json`, clears boot sentinels and rebuilds `dist/chrome`; `global-setup.ts` kills Chrome by extension path; the e2e README says concurrent shards need separate worktrees. `sleep 900` enforces nothing. **Verified.** | **Adopted.** Phase 7 runs shards sequentially in one tmux session by default; the optional 2× speed-up uses two detached worktrees at the same commit, each with its own install, and a real wait loop on the `EXIT=` lines. |
| 4 | High | Pin persistence: a per-instance promise chain does not serialise two contexts (popup + side panel); event-only deletion cleanup misses deletions while disconnected; token ids are **max+1 reallocatable** (`token-balance/service.ts:519` comment), so a stale id can pin a successor token; three dangling ids can make `pin()` return `"full"` forever. **Verified.** | **Adopted.** Pins store lowercase **contract addresses** per network (identity-safe; a re-added token keeps its pin by design). The cap counts only pins that intersect the current token set; dangling entries are pruned on the next write. Writes go through a module-level chain keyed by storage key, re-read inside the chain; a `chrome.storage.onChanged` listener refreshes every instance. Cross-context simultaneous writes remain last-writer-wins and the plan says so. Tests added for missed deletion, contract reuse, concurrent writers, scope change. |
| 5 | Medium | `useEntityCrud` exposes `refresh`, not `refetch`; the design `Input`'s `sanitize` strips punctuation (`packages/design/src/internal/sanitize.ts:12`), so a symbol like `A+B` cannot be searched. **Verified.** | **Adopted.** Facts corrected; the search field does NOT use `sanitize` — trim + lowercase, literal `includes`. |
| 6 | Medium | Comparator puts a priced zero-balance token above an unpriced funded one; `updatedAt === 0` means never-synced, not empty (`TokenCard.vue:50`), and must not fold. **Verified.** | **Adopted.** Classes are now pinned → held-priced (fiat desc) → held-unpriced (name) → never-synced (name) → empty (name); never-synced rows never fold. |
| 7 | Medium | Holdings data path under-specified: reconnect resnapshot, loading/error presentation, null-scope guards, one scope watch; Send's fetch-on-open needs a scope guard. | **Adopted** in § Data & control flow. |
| 8 | Medium | Implicit Send behaviours: fold in the picker, query/fold reset on reopen, within-pinned ordering, fiat-disabled fallback; which clients supply Holdings' pins and Send's props; `aria-selected` on bare buttons is wrong. | **Adopted.** Decisions recorded in § Scope and § Algorithms; select mode uses `role="listbox"` / `role="option"` + `aria-selected`. |
| 9 | Medium | Numeric path: `usdThresholdToMicro(1e308)` → `BigInt(Infinity)` throws; invalid decimals throw at `10n ** BigInt(decimals)` outside the try. **Verified.** | **Adopted.** Config schema bounds the threshold (`.max(1_000_000)`); the predicate wraps all bigint math in the existing try → fail open; tests for extreme threshold, bad decimals, malformed balances. |
| 10 | Medium | Bootstrap: `git worktree add --track -b` fails when the branch already exists locally; verify Bun ≥ 1.4, Node, tmux, Chrome, foundry; `gh stack push` is used before `gh stack init`; rerun gates after the final sync. **Verified** (the command form). | **Adopted.** Both worktree-add forms given; a tool check block; plain `git push` until the stack exists; a post-sync gate rerun. |
| 11 | Low | "≥ 10 cases per the L3/L4 bar" misstates CLAUDE.md (L4 has no minimum). | **Adopted**: behaviour list, no count. |
| 12 | Low | Copy: "Home is full" → "Pin limit reached"; sort labels `Value ↓` / `Name A–Z`; explain empties stay folded when dust is off. | **Sort labels adopted.** "Home is full" **kept**: it is the owner-approved mockup copy and the body sentence carries the limit. The fold label already reads `N empty` when the threshold is 0. |
| 13 | — | Builds the main plan (TokenList, selectable TokenCard, UI storage key, dedicated popup); the alternative violates fixed decisions (separate Send rendering, two PRs). Absorb its persistence advantages without putting presentation policy on `Token`. Migration avoidance is not a distinguishing argument pre-production. | **Adopted** as the decision-ledger entry; the "no migration" argument is demoted to "no row-shape coupling". |

Not raised by codex but fixed in the same revision: `navigateToTokenDetail` clicks the FIRST card, and value ordering can change which card is first — the helper takes an optional symbol and selects by `data-symbol`.

# Final fresh-context pass — round 1 (2026-09-06, GPT-6 Astra `high`, read-only, NEW session over plan r3 + both audits + the ledger)

Verdict: `reject (with blocking findings: incomplete chain isolation, unsafe numeric handling, and contradictory pin-write rules)`. Session `01a07895-c235-7991-9c53-efba604c424d`. Every factual claim re-verified; all adopted in r4.

| # | Sev | Finding (verified?) | Disposition |
|---|---|---|---|
| 1 | High | Holdings is not chain-scoped: `getTokenBalances` filters by token id and account only (`token-balance/service.ts:183-197`), the live-token map is loaded for every chain of the profile (`:160,460`), and identical addresses across chains are supported (`:563-576`). Foreign-chain rows would contaminate totals, order and the pins' known set. **Verified** — and `TokensView` has the same latent exposure today. | **Adopted.** `forChain(rows, chainId)` in `token-order.ts`; applied in Holdings' fetch + `accept`, the picker, and TokensView's ordering step (Phase 2) with a same-address-other-chain test in each. Fact 20 records it. |
| 2 | High | Numeric safety stops at the dust predicate: `classifyRow` would `BigInt()` hostile balance strings; `tokenAmountToUsdMicro` (`price/convert.ts:71-75`) exponentiates unvalidated `decimals`; moving the exponent inside a try neither rejects `decimals > 77` nor avoids the expensive computation. **Verified.** | **Adopted.** New `src/utils/token-amount.ts` (`parseRawBalance`, `isValidDecimals` with `MAX_DECIMALS = 77` checked BEFORE exponentiation, `safeFiatOf`) is the only parser; a malformed row becomes the `unknown` class (visible, unpriced, never folds); the dust predicate validates decimals first. Tests on the public helpers, not just the receipt predicate. |
| 3 | High | Pin write path contradiction: ordinary writes pruned against the CALLER's `knownContracts()`, while deletion cleanup targets the event's (possibly foreign) profile/chain — a foreign deletion would erase unrelated pins; queued ops need captured scope. **Verified** (r3 text). | **Adopted.** Deletion removes ONLY the event's contract from the event's list, never prunes. Ordinary ops capture scope + known set at enqueue, no-op if the scope changed, prune only when the captured set is loaded and matches. Tests: foreign-profile deletion preserves that profile's other pins; scope switch during a queued op. |
| 4 | Medium | Fact 6 wrong: the incoming filter obeys the fiat kill-switch indirectly — `getQuotes()` returns `{}` when disabled (`price/service.ts:138`, `incoming-transfer/service.ts:596`). **Verified.** | **Adopted.** Under the kill-switch only empty rows fold; Scope 6, Fact 6 and the fold algorithm corrected. |
| 5 | Medium | `TokenCard`'s `.symbol` has no overflow bound (`TokenCard.vue:163-169`); escaping is not layout safety. **Verified.** | **Adopted.** `max-width` + ellipsis on the symbol span (TokenCard's only change) + a long-symbol render test; Fact 21. |
| 6 | Medium | Inference 1's "fixtures are unpriced" is unsafe: the agent build maps every sandbox contract to USDC; a seeded quote or funded-vs-empty can change first-card identity. **Verified** (`price-map.ts:52-62`). | **Adopted.** Inference 1 rewritten; Phase 2 audits every direct first-card selector, not only `navigateToTokenDetail`. |
| 7 | Medium | "At most one pin lost" is unjustified: a whole-map write from another context can overwrite several changes on sibling chains. | **Adopted.** Honest wording in Scope 8; policy unchanged (last-writer-wins, v1). |
| 8 | Medium | "No unresolved Asks" contradicts two "confirm at the gate" items. | **Adopted.** The Asks section now names the two open Asks as gate items and forbids starting implementation before they are recorded. |
| 9 | Medium | "Same order" for the picker is not implemented by `orderTokens(TokenInfo[])` (pinned-then-name, no balances). **Verified** (r3 text). | **Adopted.** The picker fetches chain-scoped balances and one price client and uses `orderTokenRows`; `orderTokens` removed. |
| 10 | Medium | Hostile-map validation incomplete: array roots, non-canonical chain keys, own entries, size bound. | **Adopted.** Root must be a plain object; keys `/^\d+$/`; own enumerable entries; ≤ 32 chains; sanitized map written back. |
| 11 | Medium | `[data-testid="holdings-search"] input` is a structural selector, banned by the e2e rule. **Verified** (CLAUDE.md). | **Adopted.** The search is a native `<input data-testid="holdings-search">`; same for `select-token-search`. |
| 12 | Low | Bootstrap: `jq` is required by `agent.sh:55`; the Bun check only warned; machine paths in a committed plan. **Verified.** | **Adopted.** Tool loop exits on a missing tool; Bun check exits; the plan names "the canonical clone per `~/.agents/clones.md`" and no machine path. |
| 13 | Low | Fact 17 understates `audit:vue` (`typecheck:all`). **Verified** (`package.json:38`). | **Adopted.** |
| — | — | Sides with: `ConfirmPopup` single mode (with a callback-absent case and an informational→destructive regression), "Home is full", `SettingItem` rows. | Recorded in the ledger; the regression test added to Phase 6. |

# Final pass — round 2 (resumed with the r4 changes)

Verdict: `reject (with blocking findings: malformed rows still reach unsafe rendering code, and the new unknown classification overrides pin priority)`. Confirmed every r4 adoption; seven remaining/new findings, all adopted in r5.

| # | Sev | Finding (verified?) | Disposition |
|---|---|---|---|
| 1 | High | The numeric helpers guard ordering/folding, but `TokenCard` parses its own props (`TokenCard.vue:34` `BigInt(...)`, its own `usePrices` lookup) and the plan allowed only CSS there. **Verified.** | **Adopted.** `TokenCard` parses through `parseRawBalance` / `safeFiatOf` and renders `—` for a malformed row; `TokenList` tests mount the REAL `TokenCard` with a malformed balance and `decimals: 500`. |
| 2 | High | `classifyRow` returned `unknown` before checking pins, so a malformed pinned token fell outside Home's cap; unknown rows' effect on the aggregate's `partial` was unspecified. **Verified** (r4 text). | **Adopted.** Pin membership is classified first and never depends on the row's numbers; a malformed row counts as an unpriced holding → `partial`. Tests for both. |
| 3 | Medium | `BalanceView` still fetched across chains (`BalanceView.vue:190`), so the aggregate-only hero could disagree with Holdings; Phase 3 lacked an explicit Holdings foreign-chain regression. **Verified.** | **Adopted.** `forChain` on BalanceView's fetch + events (Scope 3, Phase 2 test); a colocated `holdings.test.ts` case for a foreign-chain row. |
| 4 | Medium | A known-set snapshot taken at enqueue could prune a token added-and-pinned afterwards. | **Adopted.** The known set is read live at run time through the getter; a test covers the add-then-pin ordering. |
| 5 | Medium | `/^\d+$/` accepts `"00"` and arbitrarily long strings; a 33rd chain could make a successful write vanish on the next read. | **Adopted.** Canonical safe-integer spelling required; the chain being written is always kept when trimming to 32. |
| 6 | Medium | Only the symbol was clipped; the name/fiat line (`TokenCard.vue:102`) was unbounded. **Verified.** | **Adopted.** Both lines clipped; test with a long symbol AND long name. |
| 7 | Low | A stale "loss bounded to one pin" sentence remained in Trade-offs. | **Adopted.** Removed. |

Support retained for `SettingItem` rows, `ConfirmPopup` single mode and "Home is full".

# Final pass — round 3 (resumed with the r5 changes)

Verdict: **`conditional approve (with conditions: reconcile the pin-write algorithm, ensure pruning uses a current token list, enforce the chain limit after mutation, and record the two open owner decisions)`**. Confidence high. Findings 1, 2, 3, 6, 7 of round 2 confirmed addressed; no architectural redesign needed.

| # | Sev | Condition | Disposition |
|---|---|---|---|
| 1 | Medium | § Algorithms still captured the known set at enqueue while § Data flow said "live". **Verified** (r5 text). | **Adopted (r6).** The pin-toggle algorithm reads storage, re-checks the scope, `await`s the known set, re-checks the scope again, then prunes/mutates. |
| 2 | Medium | A getter over a mount-only token fetch is not current: the token page would not know a token added-and-pinned elsewhere. **Verified** (r5 Phase 6). | **Adopted (r6).** `knownContracts` is an authoritative (optionally async) getter read at write time; the token page fetches `getTokens(profile.id, chainId)` inside it; the Phase 6 test changes the mocked list between mount and the click. |
| 3 | Medium | The 32-chain cap was applied during validation, before the mutation, so a 32-chain map plus a new pin could persist 33 and lose the new chain on the next read. | **Adopted (r6).** Cap applied after the mutation, the written chain always kept; test: 32-chain map → new-chain pin → 32 persisted → refresh keeps the pin. |
| 4 | — | Record the two open owner decisions before implementation. | **This is the approval gate**: the Send-picker rows and the popup implementation are asked explicitly there and written into § Asks before the homelab session starts. |
