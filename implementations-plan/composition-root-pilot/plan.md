# Arc 8 — composition-root-pilot (Q-04, PILOT ONLY then STOP)

[deep] tier of the 2026-08-16 remediation follow-ups ([spec](../remediation-followups/plan.md) §8). Quality arc: characterization pins FIRST, zero behavior change. HARD SCOPE CAP: the pilot's two pieces + write-up; decomposing `initWalletSdkHandler` or the rest of `ExecutionService.init()` is OUT OF SCOPE without owner sign-off. Validation: repo gates + `audit:vue` + **SOLO `NULO_E2E_PROVERLESS=1 bun run e2e:agent`**. Diff budget: the ten enumerated paths — `execution/service.ts`, `execution/fee/build-fee-strategies.ts`, `execution/service.composition.test.ts`, `wallet-sdk/background.ts`, `wallet-sdk/tab-lifecycle.ts`, `wallet-sdk/tab-lifecycle.test.ts`, the two e2e symbol-re-anchors, this plan + `pilot-findings.md` — nothing else (generated-type churn excepted).

## Deep-tier record — three independent plans, consolidated

Main + codex xhigh + fable planned independently off the same recon. Full texts preserved in the session scratchpad; the decision ledger:

- **CONVERGED (all three): the `buildFeeStrategies` shape.** Module-level `buildFeeStrategies(deps: FeeStrategyDeps): Map<FeeSettings["paymentMethod"]["kind"], FeeStrategy>` in a new `execution/fee/build-fee-strategies.ts`; the `feeDeps` literal (including the LAZY `simulateTxTask` closure over `this.coordinator`) STAYS in `init()` verbatim (closure identity + eager/lazy capture modes unchanged); the builder RETURNS the map and the caller assigns `this.feeStrategies = buildFeeStrategies(feeDeps)` (a `this`-writing builder would re-import the hazard the pilot exposes); keys stay literal (deriving from `strategy.kind` would silently accept a kind/slot mismatch — the test asserts the agreement instead).
- **CONVERGED: pins BEFORE extraction, at the composition layer.** Fable's recon correction (verified): NOTHING in the repo pins `feeStrategies` — the composition test's "fpc" case takes the reuse fast path and never crosses the `:907` dispatch; an extraction that never assigned the field would pass every unit/integration test (the SOLO proverless e2e gate would catch it — the unit-layer gap is the point). New pins (dispatch-only assertions, never strategy internals): (1) the initialized map's exact four kind→strategy pairs through REAL `init()`; (2) unknown kind → throws `"Invalid fee payment method"`; (3) dispatch via a spy on the initialized `"fj"` entry, asserting the op+actions CLONE reaches the strategy. Codex's simplification adopted: NO separate builder unit test — the composition pins cover the production caller.
- **DIVERGED (2-vs-1): `wireTabLifecycle`.** Codex: fee-only (the tab piece doesn't exercise the `= null!` hazard; a browser-event harness makes "two unrelated experiments"; leave the stale e2e comments). Fable + main: include it — ON MERIT (the spec's "and/or" permits either scope, so inclusion must not lean on mandate): Q-04 names TWO god-inits and a pilot touching only one says nothing about `initWalletSdkHandler`'s closure-root shape; the harness is precedented (four prior extractions from this same function with colocated tests); it converts an e2e-only net into six unit pins — a measurable pilot benefit for the write-up. **Consolidation: BOTH pieces; re-posed to codex in the contradiction-check round — codex WITHDREW the objection ("include… I see no material harm"), and the fresh hostile auditor independently endorsed inclusion on the merits.**
- Fable's shape for the tab piece adopted: structural `TabLifecycleDeps` (`terminateForTab`, `terminateSession`, `getActiveSessions`, `logger` — modeled on `session-established.ts`'s deps interface, NOT `handler:` — that's what makes the unit test possible without the SW graph); call site stays at its exact position BEFORE `handler.initialize()` (moving past it is a real delta — fable R3); handler methods arrow-wrapped (`this` preservation, per the `:226` precedent); no unsubscribe (nothing tears down in an SW). Stale e2e doc-comment cites re-anchored BY SYMBOL, not line (line cites rot — itself a pilot finding).

## The pilot pieces

1. **`buildFeeStrategies`** (`execution/service.ts:328-340` → `execution/fee/build-fee-strategies.ts`), per the converged shape above.
2. **`wireTabLifecycle`** (`wallet-sdk/background.ts:416-444` → `wallet-sdk/tab-lifecycle.ts`), per fable's shape. Six unit pins: tab close → `terminateForTab`; same-origin `loading` nav → NO terminate (the SPA carve-out); cross-origin nav → `terminateSession` for the matching tabId only; non-`loading` update → no-op; absent `changeInfo.url` → no-op; malformed URL → `terminateForTab` fallback.

## The write-up (`pilot-findings.md` — the arc's real deliverable)

Merged outline (fable + codex, as corrected in the audit rounds): the five `FeeStrategyDeps` entries classified eager/lazy; what succeeded (leaf builders with caller-owned assignment are mechanically safe when dep-complete; closure-root wirings become unit-testable); **what the pilot actually surfaced** (the most concrete defect was a MISSING PIN — "is this field ever populated?" was untestable; parameterizing a `= null!` field buys greppability, NOT type safety); what it cannot prove (the forward-referencing eager fields, `initWalletSdkHandler`'s shared-mutable concerns, the remaining asserted fields across other services — counts deferred to a verified inventory); owner recommendation: **dependency-DAG first** — inventory and classify every `= null!` field's edges (eager vs invocation-time), choose a representation that makes invalid states unrepresentable, with type improvement an outcome of that design rather than a prerequisite; backfill "field populated + consumer resolves" composition pins; continuation leaf-by-leaf, owner-gated. Stop is a first-class option.

## Process
Deep tier: consolidation done (above) → contradiction-check + audit round (codex resume with the tab-lifecycle pushback + fresh fable pass over this consolidated plan) → implement pins-first → end-diff codex pass → gates.

## Audit ledger

**Contradiction-check + double audit round:**

- **Codex (resumed planner): `approve consolidated` — and WITHDREW its tab-lifecycle exclusion** ("My objection does not survive the pushback… I see no material harm from inclusion"). Its refinements, all landed: pins-first sequencing for the tab piece (helper + unit tests added while the inline block remained, THEN the swap); the `fj` dispatch pin implemented via a spy on the initialized strategy (never its deep simulation); the three-session cross-origin matrix + listeners-register-once pins; the write-up's owner recommendation switched to **dependency-DAG first** (type improvement is an outcome of that design, not a safe prerequisite); count overclaims removed ("the remaining asserted fields", no unverified totals).
- **Fresh fable (hostile, no prior context): `conditional approve`** — conditions landed: pin 3 re-specified (its snapshot predated the landed spy pin; the final form also asserts the CLONED op + actions reach the strategy — pinning the previously-unpinned clone semantics); the ledger misquote fixed (the spec says "and/or" — inclusion stands on merit, which this auditor independently endorsed); the diff budget restated as the ten enumerated paths; the "would pass every test" overclaim narrowed to unit/integration; the reach-in idiom aligned to the file's `as unknown as` convention; `TabLifecycleDeps.tabId` tightened to required `number` (the SDK's `ActiveSession.tabId` is never undefined); the catch-scope comment expanded (it swallows more than URL parsing — fail-closed for the whole branch).

**End-of-arc codex diff pass: `conditional approve` → conditions met → CONVERGED.** Verified zero-delta ("both bodies are faithful, fee capture/assignment order is preserved, tab wiring remains before `initialize()`, handler binding is preserved"), pins "honest and correctly layered", scope exactly the ten authorized paths. Conditions, all landed: the write-up outline reconciled with the corrected ledger (stale count/type-first language removed); the "pin gap, not the structure" overclaim softened (the pilot never assessed the structural edges — they remain real and unmeasured); a logger-spy pin added to the cross-origin tab test (logging is part of the moved dependency surface; exactly one entry, for the terminated session only).
