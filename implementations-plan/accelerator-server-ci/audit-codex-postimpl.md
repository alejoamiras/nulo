needs-fixes

1. [high] The rollback kill switch is still broken in the reusable workflow. [`disable_accelerator` is computed correctly in `pr-network-e2e.yml`](/Users/alejoamiras/Projects/nulo/nulo-2/.github/workflows/pr-network-e2e.yml:138), but `_network-e2e.yml` then sets `VITE_NULO_ACCELERATOR_REQUIRED` with `${{ inputs.disable_accelerator && '' || '1' }}` [here](/Users/alejoamiras/Projects/nulo/nulo-2/.github/workflows/_network-e2e.yml:98). In GitHub Actions expressions, the value after `&&` must be truthy or the `||` branch wins, so the empty-string branch collapses and this resolves to `'1'` even when `disable_accelerator == true`. Result: install/start steps are skipped, but the wallet is still built in required-mode and the “rollback” run fails hard instead of falling back to WASM. Fix the expression before merge. GitHub’s expression docs call this out explicitly: https://docs.github.com/en/actions/reference/workflows-and-actions/expressions

2. [medium] The SHA pin is not enforced on the common cache-hit path. [`setup-accelerator-server/action.yml`](/Users/alejoamiras/Projects/nulo/nulo-2/.github/actions/setup-accelerator-server/action.yml:41) restores `$RUNNER_TEMP/accelerator-bin` from `actions/cache`, skips the download+hash check on a hit [here](/Users/alejoamiras/Projects/nulo/nulo-2/.github/actions/setup-accelerator-server/action.yml:48), and then trusts the restored binary with only `which`/`ldd` [here](/Users/alejoamiras/Projects/nulo/nulo-2/.github/actions/setup-accelerator-server/action.yml:82). That weakens the stated “repo-pinned SHA-256” defense: the hash is only verified on cache miss, not on every run. I’d re-hash the restored `accelerator-server` binary against `expected_sha256` before adding it to `PATH`.

3. [medium] The hard-fail guarantee still depends on an unverified build-time flag. `_network-e2e.yml` relies on `VITE_NULO_ACCELERATOR_REQUIRED` [here](/Users/alejoamiras/Projects/nulo/nulo-2/.github/workflows/_network-e2e.yml:91), and the runtime switch is entirely `import.meta.env`-driven [here](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/src/accelerator/config.ts:26). But the only existing bundle assertion in the agent proves the inline `VITE_LOCAL_NETWORK_RPC_URL` stamp [here](/Users/alejoamiras/Projects/nulo/nulo-2/packages/extension/scripts/e2e/agent.sh:32); it does not prove the inherited workflow env for accelerator-required made it into the bundle. If that flag ever stops propagating, Layer 2 silently disappears and the suite can pass on WASM. I’d add an explicit assertion for the accelerator-required stamp, not just the RPC URL stamp.

The rest looks sound. Your production-safety trace in (a) is correct, the concurrent `getOrInit` model does not add a new race, and the `onPhase` throw does propagate out of `createChonkProof()` because the SDK does not catch callback exceptions around the `fallback`/`denied` emissions [here](/Users/alejoamiras/Projects/nulo/nulo-2/node_modules/@alejoamiras/aztec-accelerator/src/lib/accelerator-prover.ts:329). Placement of the `setForceLocal` grep in [`_lint-and-typecheck.yml`](/Users/alejoamiras/Projects/nulo/nulo-2/.github/workflows/_lint-and-typecheck.yml:39) is reasonable. On the tar question: GNU tar on this runner rejects `..` members and strips leading `/`, and you only move the named `accelerator-server` file into the final path, so I don’t see a new traversal issue in the extraction step itself.

I also re-ran `actionlint` on the three workflow files and the new [`chain-runtime` unit test](/Users/alejoamiras/Projects/nulo/nulo-2/packages/aztec-runtime/src/pxe/chain-runtime.test.ts:1); both were clean.