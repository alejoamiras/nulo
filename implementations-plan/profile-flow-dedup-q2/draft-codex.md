Verdict: Q2 is worth doing, but keep activation shell-injected, preserve existing drift unless it is one of the two locked quirks, and treat popup create’s post-create chain as a behavior pin, not as the design to generalize.

- Highest risk: your `complete*(profile)` injection instinct is right, but for a stronger reason than you stated. Create/import service methods already open the session in the service layer ([service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/profile/service.ts:104), [service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/profile/service.ts:214), [service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/profile/service.ts:761), [service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/profile/service.ts:788)). Popup import relies on the popup app listener to react to that ([popup/app.vue](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/popup/app.vue:130)); onboarding has no equivalent listener, so it must bootstrap explicitly ([onboarding/import.vue](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/onboarding/pages/import.vue:110)). Popup create’s manual post-create chain ([new.vue](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/popup/pages/profile/new.vue:153)) is weaker than `useProfileBootstrap.bootstrapActiveProfile()` ([useProfileBootstrap.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/composables/useProfileBootstrap.ts:63)) and even skips waiting when already logged in. So: unify the pre-activation flow, inject one coarse shell callback `onComplete(profile: ProfileInfo): Promise<void>`, and do not let the shared composable own activation/routing/toasts.

- Clean boundary: return flat refs/computeds/handlers from the composable, not a nested “controller” object. The two templates bind directly to `profileName`, `password`, `isAllowedToContinue`, `handleCreate`, `handleImportSeed`, etc., and flat refs minimize adapter noise. But do not let the new composables import/connect service clients themselves; inject a thin `profileApi`/callbacks surface so they stay closer to the repo’s C1 rule.

- Double-bootstrap quirk: the fix is probably safe, but not because `bootstrapActiveProfile()` is side-effect-free. It is not idempotent in the strict sense; it disconnects/recreates clients, runs `ensureDefaultAccount`, re-syncs transactions, and can rewrite active-account storage ([useProfileBootstrap.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/composables/useProfileBootstrap.ts:22)). What is safe is removing the first onboarding call and keeping the second, load-bearing one inside `completeImport()`. The test must assert call count `1`, not just end state, or both pre/post implementations will look “green.”

- Enter-guard quirk: lower confidence as a real bug. Popup create lacks the documented target guard ([new.vue](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/popup/pages/profile/new.vue:177)), but `isCreating` is latched before the first await, so today it likely already suppresses duplicate creates. The guard is still safe because `Button.vue` renders a native `button` ([Button.vue](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/components/ui/Button.vue:77)), so focused-submit Enter still gets native click. The test must target button-focus keydown semantics specifically; a coarse “one profile created” test will not distinguish old vs new behavior.

- Drift-resolution challenge: do not normalize popup’s `fillError("unknown", err)` into onboarding’s better shape, and do not normalize passkey-failure title casing in Q2. Both are user-visible behavior changes, and neither is one of the two locked quirks. Also, do not opportunistically add Enter handling to onboarding import or onboarding create just for symmetry.

- Facts check: mostly correct. The only concrete correction I found is the export page path is `packages/extension/src/popup/pages/settings/security/export/full.vue`, not `src/popup/settings/...`. Your importer counts are otherwise right: 6 runtime dialog importers, 2 runtime `runPasskeyCeremony` importers, plus tests/doc-comment references.

- Security/adversarial: the dangerous regressions are not in the path move itself, but in semantics. Do not change `teleport to="#popup"`, Escape/unmount abort, `AbortError`/`NotAllowedError` to `UserRejectedError`, `allowedCredentials` targeting, PRF wiring, or the create→get PRF fallback ([PasskeyCeremonyDialog.vue](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/popup/components/popups/PasskeyCeremonyDialog.vue:54), [passkey-ceremony.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/popup/utils/passkey-ceremony.ts:38)). Do not reintroduce early activation during full-backup restore; `restore()`/`finalizeRestore()` are intentionally split to avoid default-network/account races ([service.ts](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/wallet/services/profile/service.ts:977)). Keep secret zeroing shell-owned; onboarding currently zeroes on unmount, popup create does not, and “fixing” that in the shared composable would be extra behavior change.

**Plan**
- Phase 1: relocate `PasskeyCeremonyDialog` and `runPasskeyCeremony` to neutral shared paths, update all runtime importers, tests, and stale comments. Gate: `bun run typecheck`, `bun run lint`, `bun run test`. Ship only if passkey dialog tests still pin teleport, cancel normalization, and abort-on-unmount.
- Phase 2: extract `useProfileImportFlow`. Keep `useFullBackupImport` as the leaf. Inject `onComplete(profile)` plus shell-specific passkey-failure and error-log callbacks. Apply only the locked onboarding quirk: remove the per-handler bootstrap calls so `completeImport()` is the sole activation path. Add a BUG-PIN test for “explicit onboarding import bootstraps exactly once.”
- Phase 3: extract `useProfileCreateFlow`. Inject `onComplete(profile)` and keep popup’s current manual post-create sequence behind that callback; do not migrate popup create to `useProfileBootstrap` in this arc. Apply only the locked popup Enter-target guard. Add a BUG-PIN test that Enter from input still submits, while focused-submit Enter does not go through the document-level path.
- Final gate: repeat `bun run typecheck`, `bun run lint`, `bun run test`, then from `packages/extension` run `bun run test:e2e` once. Do not run network e2e.

**Looks Fine**
- The neutral passkey move is real, not speculative; onboarding already declares `#popup` ([onboarding/app.vue](/Users/alejoamiras/Projects/nulo/nulo-1/packages/extension/src/onboarding/app.vue:76)).
- Q2 is genuinely orchestration-level now; the lower-level leaves already exist.
- Preserving `data-testid` should be straightforward because the templates can stay page-local while only the script orchestration moves.