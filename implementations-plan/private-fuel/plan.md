# private-fuel — private Fee Juice across the bridge

**Tier:** `deep` — Novelty HIGH (first private-FJ integration, the Wonderland FPC, the bridge-secret derivation), Security HIGH (privacy + fee payment), Irreversibility HIGH (wrong FPC address / random secret = unrecoverable FJ), External coupling HIGH (Wonderland FPC, version-specific address). Rubric leans mega-deep but the Holonym/Wonderland prior art + the in-repo shipped pieces de-risk novelty. Branch `feat/private-fuel` off dev. Aztec pinned **4.2.0**. Testnet only. Never main.

**Goal:** a PRIVATE token bridge arrives with gas via the Wonderland PrivateFPC — no L1 address leak. Gas privacy FOLLOWS token privacy (public→public FJ, private→private FJ; the private-token+public-gas combo stays impossible by construction). Ships with the B-presets UI (private default) and removes the "fuel withheld on private" guard. Fee-method matrix: no-fuel → wallet chooses (stop forcing Sponsored); public fuel → `FeeJuicePaymentMethodWithClaim` (fjwc); private fuel → Wonderland `PrivateMintAndPayFeePaymentMethod`.

Sources: [draft-main.md](draft-main.md) · [draft-codex.md](draft-codex.md) · [draft-opus.md](draft-opus.md) (the best-researched; this consolidation adopts its skeleton).

## The mechanism (verified against the repo)
- **Private fuel L1 deposit**: `fuelRecipient = PrivateFPC L2 address`, `fuelSecretHash = computeSecretHash(deriveBridgeSecret(bridgeSecretSalt, claimer))` where `deriveBridgeSecret = poseidon2HashWithSeparator([salt, claimer], DOM_SEP)`, `DOM_SEP = poseidon2HashBytes("az_dom_sep__fpc_bridge_secret") & 0xffffffff === 3952304070`. The L1 `SwapBridgeRouter.bridgeWithFuel` already carries `fuelRecipient`/`fuelSecretHash` witness-bound (`SwapBridgeRouter.sol:89` comment: *"user for public fuel, FPC for private fuel"*). **No Solidity change.**
- **Private fuel L2 claim**: `PrivateMintAndPayFeePaymentMethod(fpc, amount, secret, salt, leafIndex)` → `FeeJuice.claim(fpc,…)` then `PrivateFPC.mint_and_pay_fee(amount, salt, leafIndex)`; the FPC re-derives from `msg_sender`, asserts `amount ≥ max_gas_cost`, credits `(amount − max_gas_cost)` to the user's private internal balance, pays the sequencer.
- **Already shipped (not this arc):** the extension bundles the PrivateFPC artifact (`vite.config.ts:49-50` alias `@private-fpc-artifact`) and auto-registers it at `salt=Fr.zero(), deployer=ZERO` (`fpc/service.ts:88-94`); the TS↔Noir secret keystone is proven in `tests/e2e/fixtures/aztec-private-fpc-bridge.ts:33,60`.
- **The fund-loss bug to fix:** `runSwapBridge` hardcodes `fuelSecret = Fr.random()` (`bridge-core/src/flows.ts:252`) — correct for public (recipient-bound), **fatal for private** (the claimer must reconstruct the secret). Make it injectable.

## Decision ledger

| # | Decision | Source | Status |
|---|---|---|---|
| L1 | **No Solidity change** — `bridgeWithFuel` already carries witness-bound `fuelRecipient`/`fuelSecretHash` (`SwapBridgeRouter.sol:89`, fork-tested). Private vs public is purely what bridge-core computes. | unanimous (verified) | **ADOPTED** |
| L2 | **FPC address from the INSTALLED `215fd08` artifact, NEVER the rc.2 clone.** Wonderland's `yarn compute` uses the clone's bytecode → a *different* address → unrecoverable deposits. Runtime-derive in each consumer from the bundled artifact + a test pinning the string (drift tripwire) + check live `node_getNodeInfo.nodeVersion` against the tarball before any fund-moving phase + dust canary. | unanimous | **ADOPTED** |
| L3 | **Injectable fuel secret** — add `fuelSecret?: Fr` to `runSwapBridge`; derived secret for private, keep `Fr.random()` for public (byte-identical public path). Derivation lives at the call site where recovery persistence already lives, not branched inside the flow. | opus (codex concurs) | **ADOPTED** |
| L4 | **Two salts (fund-loss/double-claim trap):** FPC-ADDRESS salt = `Fr.zero()` (fixed, matches the wallet's registration); BRIDGE-SECRET salt = per-deposit `Fr.random()` (persisted + sealed) — `mint_and_pay_fee`'s nullifier binds the salt, so reuse collides. Code comments must call this out. | opus+codex | **ADOPTED** |
| L5 | **Wallet = EXTERNAL-honor, NO new embedded mode** — CONFIRMED by BOTH contradiction-checks via independent code traces: aztec.js `mergeExecutionPayloads([feePayload, fnPayload])` puts the fee method's 2 calls first + keeps the single `feePayer` (`@aztec/aztec.js/contract_function_interaction`), `aztec_sendTx` carries the whole `ExecutionPayload`, the dispatcher forwards `exec` verbatim (`wallet-bridge/dispatcher.ts:539`), the planner iterates every `exec.calls` entry then `detectEmbeddedFeePayment(fpc,user)→"fpc"→EXTERNAL` (`operation-planner.ts:198-238`). The cold-start 2-call setup crosses verbatim. Codex's dedicated `private_fjwc` mode is a CONTINGENCY only (resurrect iff the P2 network-e2e shows EXTERNAL can't carry it). | opus over codex; both contradiction-checks confirm | **ADOPTED (confirmed)** |
| L6 | **Keystone is "lift + pin"** — `deriveBridgeSecret` is already proven in the e2e fixture; bridge-core lifts it into a shared helper + a TS↔Noir↔fixture parity test, not a from-scratch derivation. | opus (codex concurs) | **ADOPTED** |
| L7 | **No-fuel fee = OMIT-FOR-FUNDED, BLOCK-COLD (user decision: never send Sponsored).** A dApp `aztec_sendTx` with no fee injection → `detectEmbeddedFeePayment(undefined,from)→PREEXISTING_FEE_JUICE` (`operation-planner.ts:216,239`), payable only if the account already holds FJ; the popup's Sponsored auto-select (`FeeSettingsCard.vue:249`) is NOT on the dApp path. Decision (Ask 2 → **block**): account holds FJ ⇒ omit dApp fee injection (wallet self-pays with its own FJ); account COLD (zero FJ) ⇒ the faucet **BLOCKS the no-fuel claim with guidance** (enable fuel, or fund first), NEVER sends Sponsored. Honors the original "never send Sponsored" rule; the forced-Sponsored branch is deleted outright. | double-audit + user decision | **ADOPTED** |
| L8 | **Journal schema** — additive optional `bridgeSecretSalt?`/`fpc?`/`fuelPrivacy?` on the fuel block (schema-2 compatible, old records parse); opportunistically fix the root-schema inconsistency codex flagged (`journal.ts:35-37` vs `97-99`). Codex argued for a full schema bump; rejected as heavier than needed. | opus over codex | **ADOPTED** |
| L9 | **Explicit gas from the dApp** — the private claim passes `maxFeesPerGas = current min fees`, `teardownGasLimits = 0` (Holonym pattern); `applyEmbeddedFpcGasCap` passes dApp-supplied fees through. A gas-cap parity test in P2. | codex+opus | **ADOPTED** |
| L10 | **B-presets, private default, gas-follows-isPrivate** — two cards PUBLIC/PRIVATE (private selected), remove the `watch(isPrivate → fuelOff)` guard + the `!isPrivate` submit gate; fuel privacy is driven solely by `isPrivate` (no hidden `fuelType`). | opus+codex | **ADOPTED** |
| L12 | **L5 proof is network-e2e, not manual** (codex contradiction-check blocker): the extension's EXTERNAL transport of the cold-start 2-call private payload is provable only by a test that loads the real extension. Add a `tests/e2e/network` case driving a private-fuel cold-start claim (mirror `fee-methods.test.ts`) — P2's gate runs it. `audit:vue`/headless script can't (they bypass the extension). | codex contradiction-check | **ADOPTED** |
| L13 | **No-fuel: prove FUNDED-account wallet-self-pay, BLOCK the cold case** (codex contradiction-check blocker, refined by the user's block decision): the faucet OMITS dApp fee injection for a FUNDED account so the wallet self-pays with its own FJ; a `tests/e2e/network` no-fuel claim on a FUNDED account PROVES the wallet pays before the forced-Sponsored branch is deleted. The COLD case is a faucet-side fail-closed guard (pre-flight FJ-balance read → zero ⇒ block with guidance, no Sponsored), proven by a component/unit pin — not network-e2e. Forced-Sponsored removal is gated by the funded-account e2e (P3's gate), not deferred to P4. | codex contradiction-check + user decision | **ADOPTED** |
| L14 | **Explicit gas is mandatory, not optional** (opus contradiction-check): Wonderland's `getGasSettings()` returns undefined and the faucet currently sends `.send({from, fee})` with NO gasSettings (`useDeposit.ts:270`). The private claim MUST add explicit `maxFeesPerGas`=current-min + `teardownGas=0`, else `applyEmbeddedFpcGasCap` falls back to a value that may not match the bridged budget → `mint_and_pay_fee` reverts. A P3 pin (not prose). | opus contradiction-check | **ADOPTED** |
| L11 | **Insufficient-fuel: FAIL-CLOSED + private recovery, NEVER the public ladder (double-audit CRITICAL).** The existing standalone recovery `sendStandaloneFjClaim` claims to `recipientAddr=user` and is paid Sponsored (`useDeposit.ts:144`, `fuel-claim-state.ts:79`) — for a private deposit (`fuelRecipient=FPC`) it is BOTH a privacy leak AND structurally UNREACHABLE (the user-recipient nullifier never matches the FPC-recipient message). Private records must NEVER use it. Instead: (a) FAIL-CLOSED pre-gate bound to AUTHORITATIVE amounts, NOT a soft quote (final-codex HIGH): pre-deposit require `minFuelOutput >= requiredBudget` (the signed floor the swap guarantees `fuelReceived >=`) AND pre-claim require `fuel.received >= requiredBudget` (the event amount), where `requiredBudget = maxGasCostFor(explicit maxFeesPerGas, chosen gasLimits)` from the SAME explicit gas settings the claim uses (L14) — closes the quote-ok/actual-short slippage gap; (b) recovery is scoped to SETUP-PHASE insufficiency ONLY (final-codex HIGH): only the `mint_and_pay_fee` `amount < max_gas_cost` assertion makes the bundled tx INVALID/not-included → FJ message UNCONSUMED → retry the PRIVATE FPC-recipient claim (never public/Sponsored). For ALL OTHER failures, receipt/consumption state is authoritative (an INCLUDED app-revert CONSUMES the FJ, per `fuel-claim-state.ts:63`) — the private ladder marks consumed/waits, it does NOT retry and NEVER routes to the public standalone. | double-audit + final-codex | **ADOPTED** |
| L15 | **Runtime FPC-address kill-switch ships with P3/P4, not P5 (codex double-audit HIGH).** The tripwire/nodeVersion/canary are dev-time; a post-merge Aztec bump can strand stale clients. Ship a runtime fail-closed check: derive the FPC address at runtime + assert it equals the pinned-for-version value; mismatch ⇒ **FAIL-STOP** (final-codex MED): surface "private fuel unavailable (version mismatch) — reselect a mode", NEVER silently auto-convert a private-fuel choice to public/no-fuel (that is an unchosen privacy downgrade) and never deposit. | codex double-audit + final-codex | **ADOPTED** |
| L16 | **The P2/P3 network-e2e needs a playground extension first (opus double-audit HIGH).** The network-e2e harness drives `packages/playground` (fixed buttons; imports `@defi-wonderland/aztec-standards`, NOT `@wonderland/aztec-fee-payment`) — it CANNOT emit the 2-call private payload as-is. P2 must FIRST extend the playground app to construct `PrivateMintAndPayFeePaymentMethod` (scoped work), else the L12/L13 gates are unrunnable. **Harness DECIDED (final-codex MED): extend the playground app** to construct/emit the `PrivateMintAndPayFeePaymentMethod` 2-call payload — it is the only path that drives the REAL extension transport end-to-end (a wallet-bridge unit test bypasses the extension and so cannot prove L5/L7). The playground extension is scoped P2 work, sequenced before the network-e2e. | opus double-audit + final-codex | **ADOPTED** |

**Contradiction-check outcome:** L5 RESOLVED (confirmed — both reviewers code-traced the verbatim payload merge); L8 RESOLVED (additive is safe given P0 fixes the root-schema bug). Two gate-adequacy blockers folded (L12, L13 below): `audit:vue` + the headless `fuel-testnet.ts` both BYPASS the real extension (the script uses `EmbeddedWallet`), so the extension-integration claims (EXTERNAL transport for L5, no-fuel fee auto-selection for L7) need **network-e2e** (the `test:e2e:network` / `e2e:agent` harness that loads the real extension).

## Phases

### P0 ✓ — Safety keystone (determinism + secret parity + schema; RISKIEST FIRST, moves no funds)
- bridge-core shared helper: `deriveBridgeSecret(salt, claimer)`, `privateFuelSecretHash(salt, claimer)`, pinned `PRIVATE_FPC_ADDRESS` (test re-derives from the bundled `215fd08` artifact — see the design note below). Journal `DepositFuelBlock` gains optional `bridgeSecretSalt?`/`fpc?` (`fuelPrivacy` dropped — `record.isPrivate` is the single source of truth per L10); schema-semantics comment made coherent.
- Keystone test: `deriveBridgeSecret` byte-equals the Noir constant (`DOM_SEP === 3952304070`, verified) + pinned secret/secretHash vectors for 3 cases incl. `salt=0`/`claimer=0`. Address tripwire re-derives from the installed artifact and pins the string; `scripts/check-fpc-version.ts` records + reconciles live `node_getNodeInfo.nodeVersion`.
- **Design (L2/L15):** the FPC address is a PINNED CONSTANT + a CI re-derivation tripwire, NOT a runtime re-derive — bridge-core bundles into the faucet browser build, the raw 2.2 MB artifact is outside the package `exports`, and Wonderland's JS wrapper pulls `@aztec/aztec.js` (`document`/`window`). Pinning is strictly fail-closed: the runtime never reads the mutable artifact, so it can't silently drift; a Wonderland bump fails the tripwire → conscious re-pin + re-canary. See `lessons/phase-0.md`.
**Validation gate:** `bun run --cwd packages/bridge-core test` (keystone + address-tripwire green) — **PASSED** (104/104; `DOM_SEP=3952304070`, `PRIVATE_FPC_ADDRESS=0x1b1706cc0947eca1de6527562af65d43e95540f9009a896dcd847afea92ede1e`, matches the wallet's derivation by construction). Live `nodeVersion` reconcile — **RESOLVED: testnet 4.3.1, user-confirmed backward-compatible with the 4.2.0 artifact (Ask 1); no re-pin.** Address pinned + tripwire-guarded; the P4 dust canary stays the on-network proof. Layers: unit + read-only live RPC.

### P1 ✓ — bridge-core private-fuel plumbing + headless proof scaffolding
- `fee-juice.ts`/new `fpc.ts`: `privateMintAndPayFee(fpc, amount, secret, salt, leafIndex)` wrapping Wonderland's method (keeps the dependency in one place). Add `@wonderland/aztec-fee-payment` to `bridge-core/package.json` (same tarball pin).
- `flows.ts`: add `fuelSecret?: Fr` to `SwapBridgeParams`; private path sets `fuelRecipient = derivePrivateFpcAddress()` + injected derived secret; public path byte-identical. Persist `bridgeSecretSalt` + `fpc` via the existing `onSecrets` ordering (before the irreversible tx).
**Validation gate:** `bun run --cwd packages/bridge-core typecheck && bun run --cwd packages/bridge-core test`. Pins: private witness `fuelSecretHash === computeSecretHash(deriveBridgeSecret(salt, claimer))`; public witness bytes unchanged (golden vs `l1.test.ts`). Layers: unit. **PASSED** (107/107; injected secret binds the on-chain `fuelSecretHash`; public path byte-identical; `privateMintAndPayFee` feePayer=FPC + 2-call payload).

### P2 — wallet: scope + simulate the cold-start private EXTERNAL payload (NO new mode — L5)
- `faucet/src/lib/capabilities.ts` bridge manifest: add the PrivateFPC to `contracts`; scope `{ FeeJuice, "claim" }` + `{ FPC, "mint_and_pay_fee" }` in BOTH `transaction.scope` and `simulation.transactions.scope` (the existing `claim_and_end_setup` stays for public fjwc).
- Bridge session: explicit `registerContract` for the PrivateFPC (belt-and-suspenders so the EXTERNAL sim never hits "Function artifact not found", even though `FpcService` auto-discovers it).
- Gas: confirm dApp-supplied explicit `maxFeesPerGas`/`teardownGas=0` survives `applyEmbeddedFpcGasCap` for the 2-call payload (extend `embedded-fpc-cap.test.ts`).
**Validation gate:** `bun run audit:vue` green (unit + typecheck + build, incl. the manifest-scope + gas-cap pins) AND a NEW `tests/e2e/network` case (run via `bun run test:e2e:network`, or `bun run e2e:agent` for the parallel-safe sandbox) that drives a private-fuel **cold-start** claim through the REAL extension and asserts the EXTERNAL path carries + executes both setup calls (`FeeJuice.claim` + `PrivateFPC.mint_and_pay_fee`) with `feePayer=FPC`, no scope violation (L5/L12). Pins: `detectEmbeddedFeePayment(fpc, user) === "fpc"`; manifest scopes both selectors; gas-cap parity for the 2-call payload. Layers: unit + typecheck + build + **network-e2e (real extension)**. (L5 is proven HERE, automatically — not manual smoke, not deferred.)

### P3 — faucet: B-presets UI, gas-follows-privacy, wallet-chooses-no-fuel, private claim
- `BridgeForm.vue`: two-card PUBLIC/PRIVATE (private default), fuller copy (per `../swap-fuel/fuel-privacy-ux.html`), REMOVE the `watch(isPrivate → fuelOff)` guard (`:151-153`) + the `!isPrivate` submit gate; fuel privacy strictly follows `isPrivate`.
- `useDeposit.ts`: no-fuel claim (L7) — omit dApp fee injection when the account holds FJ (wallet self-pays); **pre-flight FJ-balance check ⇒ a COLD zero-FJ account is BLOCKED with guidance, NEVER Sponsored**; delete the forced-Sponsored branch (`:150`); private-fuel deposit (`fuelRecipient=FPC`, injected derived secret, persist salt+fpc); private claim builds `privateMintAndPayFee(...)` and MUST pass explicit `gasSettings` (`maxFeesPerGas`=current-min, `teardownGas=0` — L14, mandatory); FAIL-CLOSED pre-gate on `minFuelOutput >= requiredBudget` (pre-deposit) + `fuel.received >= requiredBudget` (pre-claim) where requiredBudget = `maxGasCostFor(explicit gas)`; private recovery ONLY for the setup-insufficiency error (retry FPC-recipient, never public — L11); a runtime FPC-address kill-switch that FAIL-STOPS (reselect, no silent public downgrade — L15).
- Journal/backup carry the new optional fields; private salt sealed like the private token secret.
**Validation gate:** `bun run audit:faucet` + `bun run audit:vue` green. Component/unit pins: private card default, guard gone, gas follows `isPrivate`; private claim builds the 2-call payload with `feePayer=FPC` + explicit gasSettings (L14); **no-fuel: a COLD zero-FJ account is BLOCKED with guidance (no Sponsored sent), a FUNDED account omits injection so the wallet self-pays** (L7); **private fuel fails closed on `minFuelOutput`/`fuel.received < requiredBudget`** (pinned unit cases: a private record with `received < requiredBudget` surfaces ONLY the private FPC-retry, never public/Sponsored; an INCLUDED private app-revert is treated as consumed, not retried — L11); **the version kill-switch FAIL-STOPS** (a mismatched FPC address surfaces reselect, never auto-downgrades to public — L15); backup restore-roundtrip of a private fuel record (salt+fpc) parses + restores. Plus the L12 network-e2e (private cold-start through the real extension, requires the L16 playground extension). Layers: unit + component + build + network-e2e.

### P4 — live private-FJ run (REQUIRED dev-merge gate; REAL proofs, both privacies, dust canary)
- Fork `runVariant` → `runPrivateVariant` in `fuel-testnet.ts`: register FPC, set `fuelRecipient=fpc.address`, derive+inject the bridge secret, pay via `PrivateMintAndPayFeePaymentMethod` + explicit gas. Public variant unchanged. First private slice = minimal **dust canary** (assert the credit lands before scaling). Recalibrate `MIN_FUEL_FJ` from the worst observed fee.
**Validation gate:** `bun run --cwd packages/bridge-core scripts/fuel-testnet.ts` — both variants land with real proofs. Private asserts: the claim is ONE tx paying its own fee via the FPC (`getFeePayer()===fpc`); the user's **public FJ does NOT increase** (privacy proof); private token balance ≥ bridged; **no Sponsored FPC touched the private claim**; the L1 deposit calldata's `fuelRecipient === fpc` (no user address). Scope note: this headless script proves the **mechanism on real testnet** (bridge-core + Wonderland + the FPC address/secret) — it uses `EmbeddedWallet`, NOT the extension, so the extension-integration claims (EXTERNAL transport, no-fuel fee selection) are proven by P2/P3's network-e2e, not here. Layers: live-network e2e (the wrong-address/wrong-secret canary).

### P5 — `/harden security` close-out
- `/harden security` over the bridge+fee surface (FPC integration, secret derivation, fee-method matrix, manifest scope, recovery ladder). Add fail-closed guards: private-token+public-gas impossible at the API layer (not just hidden in Vue); version/address checks around the FPC. Triage + fix high/critical.
**Validation gate:** `/harden security` report filed under `audit/security/<date-run-id>/`; high/critical addressed; `bun run audit:vue && bun run audit:faucet && (cd packages/bridge-evm && forge test)` green. Layers: whole bridge+fee surface.

## Security & Adversarial Considerations
- **Wrong FPC address ⇒ unrecoverable FJ (headline):** derive from the installed `215fd08` artifact only; address tripwire test; live `nodeVersion` check before any fund-moving phase; dust canary on first use + any artifact re-pin; persist the per-deposit `fpc` for post-hoc drift detection.
- **`deriveBridgeSecret` drift ⇒ FJ stranded:** one shared helper + the TS↔Noir↔fixture keystone gates the build. Assert the runtime `DOM_SEP === 3952304070`.
- **Random vs derived secret:** the injected-secret design + a pin asserting the private witness uses `deriveBridgeSecret`, never `Fr.random()`.
- **Privacy — precise claim (double-audit LOW):** private fuel hides the user's AZTEC address (never on L1) and *which* user the gas credits (the FPC is a shared anonymity set), but the L1 `depositToAztecPublic(FPC)` is sent by the user's EOA — the "this EOA funded the FPC by amount X" link IS public on L1. Don't overstate "no L1 leak"; state it precisely in UI copy + Inferences.
- **Privacy invariant:** the private claim MUST self-pay via the private FJ; NEVER Sponsored/public on the private path — including recovery (L11: private recovery is FPC-retry only). P4 asserts no Sponsored touched it + public FJ doesn't increase. There is NO Sponsored fallback anywhere (L7: cold no-fuel claims are BLOCKED, not Sponsored), so no Sponsored path can bleed into the fueled private path.
- **Mid-flight Aztec upgrade (double-audit MED):** the FPC address binds the bytecode+version (classId); a deposit sent before an upgrade + claimed after strands. The L15 runtime check + announced testnet upgrades mitigate; document an abort-window / "don't bridge private fuel near a known upgrade" operational note.
- **Two-salts confusion:** FPC-address salt zero vs per-deposit bridge-secret salt — code comments + a pin that a reused salt is rejected (nullifier collision).
- **Insufficient fuel ⇒ bundled-claim revert with irreversible L1 deposit:** pre-gate `received ≥ estimatedMaxGasCost`; recovery ladder reachable for the private case.
- **Manifest under-scoping ⇒ post-deposit "artifact not found":** scope `claim` + `mint_and_pay_fee` exactly, no wildcards; re-consent via the field-diff.
- **FPC version drift:** an Aztec bump changes the FPC bytecode/address → a silent fund-loss trap; pin + runtime-assert + documented re-derive.
- **Supply chain:** the Wonderland tarball is a GitHub-release pin in `bun.lock` (not npm) — frozen-lockfile CI covers drift; outside the 7-day npm gate (note it).

## Assumptions
**Facts** (verified, file:line):
- Extension bundles + auto-registers PrivateFPC at `salt=Fr.zero()` (`extension/vite.config.ts:49-50`, `wallet/services/fpc/service.ts:88-94`).
- Keystone fixture proves `deriveBridgeSecret` with `DOM_SEP===3952304070` (`extension/tests/e2e/fixtures/aztec-private-fpc-bridge.ts:33,60`).
- `runSwapBridge` hardcodes `fuelSecret = Fr.random()` (`bridge-core/src/flows.ts:252`); `fuelRecipient` is a param; witness binds it (`l1.ts`).
- Faucet claims dApp-side via `.send({ fee })` (`faucet/src/composables/useDeposit.ts:270`); no-fuel forces Sponsored (`:150`).
- `EmbeddedStrategy` honors the embedded payload verbatim, EXTERNAL for `feePayer≠from` (`extension/.../fee/embedded-strategy.ts` header + `:31`); `detectEmbeddedFeePayment` (`utils/fee-detection.ts:8-13`).
- Router comment "user for public fuel, FPC for private fuel" (`bridge-evm/src/SwapBridgeRouter.sol:89`).
- `PrivateMintAndPayFeePaymentMethod(fpc, amount, secret, salt, leafIndex)` + the Noir `derive_bridge_secret`/`mint_and_pay_fee(amount≥max_gas_cost)` (Wonderland clone `src/ts/fee-payment-methods/private.ts`, `src/nr/private_contract/src/main.nr:38,77-108,230`).
- Installed Wonderland = `4.2.0-prerelease.215fd08`; clone = `4.2.0-aztecnr-rc.2` (different bytecode) (`bun.lock`, clone `package.json`).
- Holonym working reference: `fuelRecipient = PRIVATE_FPC`, overridden `fuelSecretHash`, claim via `PrivateMintAndPayFeePaymentMethod` (Holonym `bridge/bridgeL1ToL2.ts`, `hooks/useL1Operations.ts`).

**Inferences** (attack):
- The dApp's 2-call private setup payload survives the EXTERNAL path verbatim (proven only for single-call `pay_fee` today — **the L5 disputed risk; P2 sim + P4 live prove it**).
- ~~The wallet auto-selects Sponsored for a cold no-fuel account~~ — **FALSE for the dApp path** (double-audit): `aztec_sendTx` with no fee injection → `PREEXISTING_FEE_JUICE`, which a cold account can't pay; the popup auto-Sponsored is not on the dApp path. ⇒ resolved by BLOCKING cold no-fuel claims (never Sponsored), per the user decision (L7).
- Private fuel hides the user's Aztec address + which user gets gas, but the L1 EOA→FPC `depositToAztecPublic` funding link is public (precise the privacy claim).
- Testnet's Aztec version matches the `215fd08` bytecode → the derived address is the network's (P0 verifies).

**Asks** (gate decisions):
1. **Version reconciliation — RESOLVED (user, 2026-06-14):** testnet runs nodeVersion 4.3.1; the user confirmed 4.3.1 is backward-compatible and has DEPLOYED everything 4.2.0 has, so the 4.2.0-derived `PRIVATE_FPC_ADDRESS` is valid on testnet — no re-pin / no stack bump. `check-fpc-version.ts` now guards only MAJOR drift; the P4 dust canary stays the on-network proof.
2. **No-fuel cold-start — RESOLVED (user chose BLOCK):** a cold zero-FJ account cannot self-pay a no-fuel claim on the dApp path; rather than a Sponsored fallback, the faucet BLOCKS the no-fuel claim for cold accounts with guidance (enable fuel, or fund first) and NEVER sends Sponsored — honoring the original "never send Sponsored" rule. The forced-Sponsored branch is deleted (L7/L13).
3. **FPC address home:** deployment config vs generated snapshot/test-only? (Sufficiency source-of-truth — quote vs floor vs received — is RESOLVED in L11: the gate binds to `minFuelOutput` + `fuel.received`, never the quote.)
4. **Dust-canary policy:** every promote vs only on artifact/version change?

## Post-implementation hardening
`/harden security` is **P5** (scheduled, per the gate decision) over the bridge+fee surface.

## Seeds (FINALIZED at the approval gate — use exactly ONE per session)

**Recommended — `/goal`** (completion is transcript-observable):
```
/goal All phases P0-P5 marked ✓ in implementations-plan/private-fuel/plan.md (per-phase headers), each backed by its validation gate reported passing in the transcript; for each phase LESSONS_FILE=implementations-plan/private-fuel/lessons/phase-N.md printed. P0's FPC-address tripwire + secret keystone green + live nodeVersion reconciled. P4's live private run passed (public FJ unchanged, no Sponsor touched the private claim, fuelRecipient===fpc). /code-review max --fix done + committed separately; codex post-impl audit done with high/critical addressed; bun run audit:vue + bun run audit:faucet + (cd packages/bridge-evm && forge test) + bun run --cwd packages/bridge-core test all exit 0 in the transcript. Constraints: branch feat/private-fuel only; testnet only; never main; commits unsigned via git -c commit.gpgsign=false; NEVER deposit real FJ to an unverified FPC address (dust-canary first); the faucet NEVER sends Sponsored (cold no-fuel claims are blocked with guidance); private recovery NEVER falls back to public/Sponsored.
```

**Alternative — `/loop 15m`**:
```
/loop 15m Drive implementations-plan/private-fuel on feat/private-fuel. Reality-check plan.md + lessons + git first; fix red suites; else next pending phase honoring its validation gate (P0 keystone → P1 plumbing → P2 wallet scope+network-e2e → P3 faucet+matrix → P4 live → P5 harden); validate per the phase gate; commit small conventional unsigned via git -c commit.gpgsign=false; push. Decisions → /codex xhigh, log in lessons/phase-N.md. Hard limits: testnet only; this branch only; never main; the faucet NEVER sends Sponsored (cold no-fuel blocked); NEVER deposit real FJ to an unverified FPC address; private recovery never public/Sponsored; 5 failures on one step ⇒ stop + reassess with codex. All ✓ ⇒ /code-review max --fix → codex post-impl audit → address high/critical → STOP with the manual checklist.
```

## Audit verdicts
- Contradiction-check (codex): **fail → fixed** — L5 confirmed [OK] via code trace; 2 gate-adequacy blockers (P2's L5 proof was manual-only; L7 no-fuel unproven + sequencing drift) folded as L12/L13 (network-e2e gates + guarded sequencing). Transcript: [audit-codex.md](audit-codex.md).
- Contradiction-check (opus): **pass** — independently code-traced L5 [OK] and judged P2-sim + P4-canary sufficient; one minor (L14 explicit-gas mandatory) folded. Transcript: [audit-opus.md](audit-opus.md).
- Double audit (codex): **reject → fixed** — CRITICAL private insufficient-fuel leak + 4 HIGH/MED; folded as L11/L15/L13/L16 + P3 gate additions. Transcript: [audit-codex.md](audit-codex.md).
- Double audit (fresh opus): **reject → fixed** — sharpened the CRITICAL (recovery UNREACHABLE, not just leaky), INVERTED L7 (no-fuel dApp path can't auto-Sponsored), exposed the network-e2e/playground gap (L16), privacy-claim precision. All folded. Transcript: [audit-opus.md](audit-opus.md).
- Final fresh-context codex pass (fresh session 5tYfc5dl): **conditional approve** → 4 conditions, all folded: L11 budget bound to `minFuelOutput`+`fuel.received` (not quote) + private retry scoped to setup-insufficiency only; L15 fail-stop (no silent downgrade); L16 harness = extend the playground. No residual public/Sponsored recovery leak. Verdict line: "conditional approve (with conditions: make L11's budget check floor/received-based, scope private retry to setup-insufficiency only, make L15 fail-stop/no silent downgrade, and fix the P2 harness choice)" — conditions addressed in this revision.
