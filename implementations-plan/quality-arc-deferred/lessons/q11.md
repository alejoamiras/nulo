# Q11 — WalletSdkDispatcher session-account dedup (re-verify LIVE → dual-model CONVERGED)

## Re-verify
dispatcher.ts 1011→1226 lines. The grant-response/enrich path (`enrichGrantedCapabilities`, ~915-947) reimplements `formatSessionAccounts` (430-442) line-for-line (resolveNetwork → getAccounts → getSessionAccountAddresses → filter → map `{alias, item}`), wrapped in a `canGet` gate. The single-account caip projections (502/593/636) are just one-line `formatCaipAccount` calls in different handlers.

## Dual-model consult — CONVERGED (no surface)
- **codex** (sess `019ef11a-...` wait, Q11 codex = the bxmbbw84l run): core projection dedup safe for `canGet:true`; the exact `canGet ? formatSessionAccounts : []` ternary is NOT behavior-preserving (skips the unconditional network/account reads on `canGet:false` → call-count change + suppresses a throw). Single-account sites = false-DRY, leave. Tests: enrich-path `accounts` array unpinned; add a `requestCapabilities` projection test + a `canGet:false` edge test. Biggest risk: silent `canGet:false` throw→`[]` change.
- **claude Plan agent**: identical core verdict (safe for canGet:true; the ternary changes the degenerate `canGet:false`+unresolvable-chain throw path). Single-account sites = false-DRY (the one-liner is already shared; the resolve-validate preamble is 2 copies with load-bearing per-handler error strings). Test gap: enrich-path `accounts` contents untested (only flags pinned) → add content test FIRST (red-green). Biggest risk: refactoring an untested projection.

## Decision (behavior-preserving, per the verbatim mandate)
Do NOT use the lazy ternary (it changes the `canGet:false` resolve/throw behavior — a real if degenerate change). Instead:
1. **Pin first:** add an `enrichGrantedCapabilities`/`requestCapabilities` test asserting the `accounts` array projection (canGet:true → exact filtered+ordered `[{alias,item}]`; alias hit / name-fallback / empty; canGet:false → `[]`).
2. **Extract a pure helper** `projectSessionAccounts(allAccounts, sessionAddresses, chainId, aliases) → {alias,item}[]` for the shared filter+map (the drift-prone format logic the header says is "pinned by unit tests").
3. Both `formatSessionAccounts` and the grant-response path call it, each KEEPING its own resolve/getAccounts/getSessionAccountAddresses (grant-response keeps the unconditional resolve → `canGet:false` throw-parity preserved exactly; call-count unchanged).
4. **Leave** the single-account sites (false-DRY, both agreed).
Constraints preserved: formatSessionAccounts wire-parity (now pinned at BOTH call sites by the new test); 3-copy nulo-schema-patch untouched.
