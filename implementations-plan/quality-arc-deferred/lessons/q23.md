# Q23 — claim/cancel temporal coupling (CONCURRENCY-CRITICAL) — decision ledger

**STATUS: ✓ DONE via A′ — comment-only fix merged `27561c0` (PR #158); structural seam + cancel-race test DEFERRED to #125/#126. Fresh-claude post-impl ACCURATE-SHIP. Full gate green (net 28021969303 8/8).**

## The finding (audit verbatim)
The cancel/claim correctness was described as "correctness-by-microtask-interleaving" across files. The
header at `claim-helper.ts:160-162` literally calls it "fragile against future refactors of either side."
Audit's smallest-safe-refactor hint: "a narrower abstraction around the controller/journal handshake,
backed by the existing race tests … any extraction must own `_transitionLocked` semantics too."

## Re-verify (STEP 1) — LIVE
The coupling persists post-#83. The mechanisms (both models independently mapped the SAME ones):
- controller pre-registered before the wait — `execution-lane.ts:227-232`
- mutex enqueues synchronously before its first await — `execution-mutex.ts:113-123`, `140-163`
- claim reuses that same controller — `claim-helper.ts:123-165`
- cancel transitions FIRST, aborts SECOND — `execution-lane.ts:168-179`
- journal lock is the arbiter of claim-vs-cancel — `operation-journal/service.ts:223-307`

## Dual-model verdicts (OPPOSITE top-lines)

### claude `a7460098` (Agent/Plan) — **LEAVE IT**
- The "implicit microtask interleaving" premise is WRONG. Three independent serialization layers protect
  the handshake (journal transitionLock arbiter; pre-acquire reuseController always in activeControllers;
  post-claim checkCancelled re-reads signal.aborted) — any one is sufficient.
- Extracting an abstraction WIDENS blast radius: moving `_transitionLocked` out of the pure-storage
  `OperationJournalService` = layering violation / deadlock risk.
- Biggest risk = "abort-before-transition" reordering.
- Recommends: leave the structure, FIX the misleading comment, optionally add ONE deterministic lane test.

### codex `019ef195` (xhigh, read-only; dir `codex-rmDRk1G8`) — **REFACTOR (narrowly)**
- A safe abstraction exists, but ONLY as a very narrow queued-claim/cancel handshake seam. NOT a broad
  "cancellation coordinator."
- Design: an opaque `QueuedClaimHandle` (`queuedJournalId + controller`):
  - `prepareQueuedWait(jobId)` — create controller, register in `activeControllers`, return handle, called
    BEFORE `ExecutionMutex.acquire(...)`.
  - `claimQueuedOrPending(handle, …)` — owns queued|pending decision; NEVER creates/swaps controllers on
    claimed/reused/silent-pending paths; only re-binds/asserts the same controller identity; throws
    `JobCancelledSentinel` on any other stage.
  - `cancelQueuedJob(id)` — owns queued|pending→cancelled; aborts + deletes the controller ONLY after the
    journal transition resolves; if the FSM rejects, must NOT abort.
- Critical rule: "if a queued id exists, controller identity must be continuous from pre-acquire
  registration through claim and execution." Makes queued-claim-without-a-handle a loud failure.
- **Keeps the journal lock as arbiter** (does NOT move `_transitionLocked` out of the journal — so claude's
  main objection does not apply to this specific design).
- "Leaving it purely documented is worse" — the invariant is split across files and the comment describes
  the older, more fragile microtask story.

## Reconciliation (where they actually land)
- **AGREE (the safety substance):** no live bug; real safety = controller-identity-continuity +
  transition-before-abort + journal-lock-arbiter (NOT microtask luck); the `claim-helper.ts:160-162` comment
  is misleading/outdated; the heavy `concurrent-confirm` shard is the real gate; existing race tests cover
  most of it; biggest silent break = a fresh controller after claim OR an await before binding/aborting it
  (a cancelled-journal-but-still-submitted-tx).
- **DISAGREE:** whether to ENCODE the invariant in a new `QueuedClaimHandle` seam (codex: yes, it's safe and
  doc-only is worse) or NOT (claude: the doc + one test is enough; structural change on the money-cancel
  path carries asymmetric downside).

## Why SURFACED (not autonomously resolved)
Goal hard-limit: "Q19/Q23 dual-model DISAGREEMENT on an authz/concurrency-critical call → SURFACE to user."
This is the money-cancel race (a dropped/raced cancel can submit a tx the user cancelled). The models gave
opposite top-line verdicts. Even "do the safe subset + defer the seam" is itself a decision ON the contested
refactor → the user's call.

## My recommendation (for the user)
Do the **agreed safe subset autonomously-pending-approval**: (1) rewrite the `claim-helper.ts:160-162`
comment to describe the REAL invariant (controller-identity-continuity + transition-before-abort +
journal-lock-arbiter) instead of the "microtask luck" story; (2) add ONE deterministic test pinning
`acquireSlot → claim → cancel` preserves the SAME controller object (and/or queued-claim-without-handle
fails hard). **DEFER** codex's structural `QueuedClaimHandle` extraction to a separate, human-reviewed PR —
a structural refactor of the cancel race should have a human in the loop (which is exactly what this surface
is enforcing). Options put to the user: (A) safe subset + defer seam [rec]; (B) do codex's full narrow
`QueuedClaimHandle` refactor now; (C) leave fully as-is (claude's pure verdict, no comment/test change).

## User question (2026-06-23): "does this relate to PR #125/#126?" — YES (investigated)
Both OPEN, based on `dev`, unmerged:
- **#125** `feat/execution-pxe-injection-spike` — added `execution/service.composition.test.ts`: a
  **cancel-mid-prove** test driving the REAL `executeTransfer` + `cancelJob` through the REAL
  `OperationJournalService` FSM + transition lock, in-process (FakeBrowserApi + a `ProofGate`). Asserts the op
  ends terminally `cancelled`, never `submitting`/`succeeded`, proof dropped (`toTx` never called), `sendTx`
  never fired. ALSO carries the **PXE injection factory** (the same thing deferred in Q10's closeout).
- **#126** `feat/composition-test-rollout` (stacked on #125) — shared `composition-harness.ts` + DappSession/
  Token composition tests + a normative `COMPOSITION-TESTS.md`.

**Relationship to Q23 (precise):** #125's test covers the cancel-mid-prove CONTRACT (cancel-wins, proof-dropped,
no-submit) at the SERVICE level — it cancels at the *proving* stage, AFTER the controller is registered. It does
NOT exercise the specific Q23 micro-race: the no-await window in `claim-helper.ts:144-163` between the journal
stage-write and `activeControllers.set()`. So #125 covers the contract, NOT the registration interleaving the
misleading comment is about. BUT its real-journal + ProofGate harness is the IDEAL home for any Q23 race test +
(if ever) the structural seam — building a parallel one on dev-quality would duplicate it.

## Refined options (surfaced to user; awaiting pick)
- **A′ [my rec]** — fix the misleading `claim-helper.ts:156-162` comment on dev-quality NOW; defer the
  cancel-race test + structural seam to ride #125/#126's composition harness.
- **B** — codex's `QueuedClaimHandle` seam now on dev-quality (heaviest; later needs reconciling with #125).
- **C** — leave fully as-is; mark Q23 ✓-documented, no change.
- **D (new, from the #125/#126 investigation)** — do ALL of Q23 (comment + cancel-race test + optionally the
  seam) ON #125/#126 where the harness lives; mark Q23 ✓-on-dev here with nothing changed on dev-quality.

**Why still parked (not autonomously resolved):** every option except a pure defer is a CHOICE on the contested
seam-vs-no-seam concurrency call (e.g. A′/C lean claude = no seam; B = codex). The goal forbids autonomously
deciding a Q23 concurrency disagreement → awaiting the user's A′/B/C/D pick.
