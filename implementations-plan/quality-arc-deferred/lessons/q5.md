# Q5 — duplicated execution lifecycle tail (re-verify → SHRUNK → dual-model SPLIT → surfaced)

## Re-verify
#83 (execution-decomposition) extracted the slot/journal/controller PRIMITIVES into `ExecutionLane` and the shared prove→send→record body into `ExecutionCoordinator.proveAndSend`. Residual = the orchestration WRAPPER (acquireSlot → hoist journalId → try{checkCancelled, markJournal stages, per-path build/sim/prove, addTransaction} → catch(sentinel vs failed) → finally(controller cleanup + slot release)) still duplicated across the 3 dapp-send pipelines (`executeSendTransaction`:174 / `executeAztecSendTx`:257 / `executeNoFromSendTx`:426). Concurrency-sensitive (frozen ordering invariants in the file header).

## Dual-model design consult
- **codex** (sess `019ef11a-3161-7572-a45a-6129f8b33bf7`): 3-path wrapper = false-DRY (executeSendTransaction is no-slot pinned). BUT a **2-path** helper (`runClaimedDappSendLifecycle` over executeAztecSendTx + executeNoFromSendTx, prepare/execute callbacks) IS worth doing — "the smallest safe refactor." executeSendTransaction + transfer-executor stay separate.
- **claude Plan agent**: **Mostly false-DRY — do NOT extract the wrapper** (even 2-path). The 3 frames genuinely differ; #83's `proveAndSend` docblock shows the team DELIBERATELY kept catch/finally + slot/claim per-path. Invariant #2 (journalId hoist + finally release) is currently locally-checkable; a helper converts it to "trust the helper" for ~4-6 lines — a bad trade given the slot-leak-wedges-the-lane failure mode is invisible to unit tests. Max defensible = a tiny `markFailedUnlessCancelled(error, journalId, markJournal, tag)` free function (centralizes the cancel discriminator, invariant #4, WITHOUT touching the frame).

## Agreement / disagreement
AGREE: 3-path wrapper bad · executeSendTransaction stays separate (no-slot pin, `service.characterization.test.ts:70`) · transfer-executor stays separate (different journal kind + cancel mapping `maybeRethrowAsRpcCancel` + task lifecycle) · same biggest risk (try/finally boundary → slot leak) · value is LOW (residual; #83 drew the boundary on purpose).
DISAGREE (concurrency-critical): codex = extract the 2-path helper; claude = don't (at most the free function).

## Decision: SURFACED to user (per /goal: dual-model disagreement on a concurrency-critical call → surface)
My judgment leans **claude** (conservative): value is low + both agree the slot-leak risk is the dominant concern + #83 consciously held this boundary → the 2-path helper's marginal LOC win doesn't justify obscuring invariant #2. Recommended: ship ONLY the `markFailedUnlessCancelled` free function (safe, frame-independent, centralizes invariant #4) — or mark Q5 ✓-residual (no change) if even that isn't worth it. Awaiting the user's pick before implementing.

## USER DECISION (2026-06-23): catch-disposition free-fn only
User picked option A — extract ONLY the catch arm; leave acquireSlot/journal/finally inline.

## Implementation
- New `packages/extension/src/wallet/services/execution/mark-failed-unless-cancelled.ts`:
  `markFailedUnlessCancelled(error, journalId, lane): Promise<unknown>` → if NOT `JobCancelledSentinel`,
  `lane.markJournal(journalId, {stage:"failed"}, normalizeError(error, "dapp_execute"))`; always RETURNS the
  error so the caller `throw`s it (the throw stays at the call site → catch/finally shape + ordering unchanged).
- The 3 dapp-send catch arms (`executeSendTransaction`:246 / `executeAztecSendTx`:409 / `executeNoFromSendTx`:598
  — byte-identical) → `} catch (error) { throw await markFailedUnlessCancelled(error, journalId, this.deps.lane) }`.
  `normalizeError` dropped from the dapp-send-executor import (now only used in the helper).
- **transfer-executor EXCLUDED (assessed):** its catch differs — `maybeRethrowAsRpcCancel(error, transferTask)` +
  a local `markJournal` closure over `transitionJournal` + `"transfer"` context + `transferTask.fail(error)`.
  Folding it would force a generic helper AND obscure the transfer-specific cancel mapping. Stays inline.
- Behavior-identical (cancelled → rethrow, no mark; else → mark failed + rethrow). Net change small.
- Tests: `mark-failed-unless-cancelled.test.ts` (3) — cancelled→no-markJournal+returns error; generic→markJournal
  failed + returns error; undefined journalId passthrough. Gate: lint ✓ · typecheck:all ✓ · execution suite 284 ✓.
