# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
bun-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets / env (committed templates are .env.example only)
.env
.env.*
!.env.example

# Storybook build output (M6 phase 2)
storybook-static/

apps/extension/src/external/*

# E2E test generated config
apps/extension/tests/e2e/.test-config.json

# E2E parallel-agent state (port pack, ownership lockfile, child logs)
apps/extension/.e2e-state/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.tsbuildinfo
wallet_data_*/


# sandbox deploy output (instance-specific) — deploy-sandbox.ts writes here for manual inspection
packages/bridge-core/sandbox-deploy/

# bridge e2e runtime artifacts (in-browser/script PXE data + MCP captures)
.playwright-mcp/
/deposit-public-success.png
**/pxe_data_*/
**/wallet_data_*/

# Bridge cutover deploy artifacts (transient; the promoted manifest is testnet-bridge.json)
apps/faucet/public/testnet-bridge.candidate.json
apps/faucet/public/testnet-bridge.journal*.jsonl
