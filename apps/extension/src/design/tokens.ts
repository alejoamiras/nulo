/**
 * Design tokens — re-exported from the canonical source, `@nulo/design`.
 *
 * The token names/scales now live in `packages/design` (`token-contract.ts` → generated
 * `tokens.ts`). This file keeps the extension's historical `@/design/tokens` import path working
 * with zero call-site churn. Do not add tokens here — edit the contract in `@nulo/design`.
 */
export * from "@nulo/design/tokens"
