{
	"name": "@nulo/landing",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"predev": "bun run scripts/ensure-release-json.ts",
		"dev": "vite",
		"prebuild": "bun run scripts/fetch-latest-release.ts",
		"build": "vite build",
		"preview": "vite preview",
		"pretypecheck": "bun run scripts/ensure-release-json.ts",
		"typecheck": "tsc --noEmit",
		"pretest": "bun run scripts/ensure-release-json.ts",
		"test": "vitest run"
	},
	"devDependencies": {
		"typescript": "^6.0.3",
		"vite": "^8.0.16",
		"vitest": "^4.1.9"
	}
}
