# Phase-4 Verification Record (Claude) — QUALITY Ultra Audit

Verifier independently RE-READ the cited `file:line` source for all 22 consolidated
findings (Q-01..Q-22). Did not trust finding prose — opened each file and confirmed the
smell is present at the cited location. Extra scrutiny on the 5 `claude-only` findings
(Q-16, Q-17, Q-19, Q-20, Q-22) and instance-accuracy spot-checks (~5 each) on the big
instance lists (Q-01, Q-02, Q-03, Q-12, Q-13, Q-14).

**Tally: 21 CONFIRMED · 1 ADJUSTED (Q-22) · 0 REFUTED.** One minor count nuance on Q-17.

| Finding | Verdict | What I checked | Confidence |
|---|---|---|---|
| Q-01 | CONFIRMED | Spot-checked 6/8: entity_storage.ts:49 `JSON.parse(raw as string) as T`; value-storage.ts:21 `JSON.parse(res[root] as string)`; decode.ts:15 `JSON.parse(result) as T`; utils.ts:28 `res as T`; useDappInteractionPayload.ts:16+86 `unknown`→`as TPayload`; useFullBackupImport.ts:165+201-214 inline `Record<string,unknown>` casts. All real, all show the smell. PXE-skips-zod nuance corroborated (client.ts:92 `as Record` vs `.parseAsync` elsewhere). | high |
| Q-02 | CONFIRMED | base/index.ts:11 `MethodsMap = Record<string,(...params:any[])=>unknown>` (correctly flagged as the justified-NOT-the-real-issue, has variance biome-ignore). Downstream confirmed: base-service.ts:125 `this as Record<string,fn>` invoke; base-client.ts:205; dispatcher.ts:275 `(methodName:string,args:unknown[])`, :328/:349 `args[0] as ...`; scope-checkers.ts:28 + :59-79 `args[0] as Record`; scope-enforcement.ts:93-94 `args[0/1] as Record`. | high |
| Q-03 | CONFIRMED | account/client.ts: 6 mechanical `return this.request("m",...args)`. network/client.ts: 14 validate→request→validate restatements. PXE list repeated across spec.ts:24-81 (`Methods`), ipxe.ts:27-50 (`IPXE`), proxy.ts:26-103 (`PXEProxy implements IPXE`), client.ts:76-201 (request+parse). 4 of the claimed surfaces verified directly. | high |
| Q-04 | CONFIRMED | materialize.ts:44-61 `Materialized* & Record<string,unknown>` union + per-branch casts; **`as unknown as Operation` confirmed verbatim at dapp-interaction/service.ts:294** (exact cite). execute/types.ts:36-58 the `DraftOperation` duplicate. Kind switches at materialize.ts:77 + execution/service.ts:377-453. `AccessLevel.None` fallback confirmed at service.ts:514. | high |
| Q-05 | CONFIRMED | capabilities.ts:16-20 (`item:unknown`) + 53-59 (`Capability` union). dispatcher.ts:704 `as Record<string,unknown>[]`; :727-753 repeated `cap as unknown as {Accounts,Contracts,Transaction,Simulation}Capability`. **Drift claim corroborated: grep shows `contractClasses` is ABSENT from the dispatcher delta branches** (handles 4 of 6 cap types) — the missed-branch the finding predicts is real. | high |
| Q-06 | CONFIRMED | encryption-key.ts:11/87/97 (`salt/passhash:ArrayBuffer`, `getPasshash→ArrayBuffer`); password-secret-box.ts:57-66 (`guard/secret:string` base64), 80-156 primitive params; passkey-credential.ts:7-14 (`id/prf/userHandle:string`). **The dangerous overload confirmed verbatim at profile/spec.ts:250-262: `masterKey:string` = base64 master key for password OR credentialId for passkey, same slot.** | high |
| Q-07 | CONFIRMED | jobs/types.ts:82 `kind: string` (bare) w/ JSDoc enumerating drifted values; jobs/error.ts:38+53-59 `normalizeError`/`extractMessage`; errors.ts:20 `details?: unknown`. **makeRemoteError body byte-identical at background/client.ts:134-140 and offscreen/client.ts:113-117** (surrounding comments differ, bodies identical). | high |
| Q-08 | CONFIRMED | `diff` of faucet (24-95) vs playground (23-94) schema-patch bodies returns EMPTY — identical. `(WalletSchema as any)` confirmed in all 3 copies; 3 RPCs × 3 files = 9 mutations. Matches finding's own "moderate" action-confidence (tradeoff documented, dup still real). | high |
| Q-09 | CONFIRMED | Hex idiom `b.toString(16).padStart(2,"0")` in 3 DIVERGENT forms: map at encryption-key.ts:114, loop at random.ts:9, loop again at content-hash.ts:43. Base64 split: `Buffer.from(...).toString("base64")` (password-secret-box.ts:160) vs `Buffer.from(x,"base64")` (passkey-credential.ts:37/39). | high |
| Q-10 | CONFIRMED | design/ui/Button.vue:8-26 `size/variant: {type:String}` (not token unions); :77 `style[props.variant]` UNGUARDED (size guarded via `hasCorrectSize`, variant not). Badge.vue:4-9 `variant:{type:String,default:"info"}` + `$style[variant]`. Design-side smell solid; wrapper re-copy not re-opened. | high |
| Q-11 | CONFIRMED | Badge.vue:22-40 vocab `info\|warning\|error\|purple`; composables/toast.ts:13-16 `ToastOptions.color?: string` (free string). Divergence between a typed-ish union and a bare string already demonstrates the smell. Directly verified 2 of 4 cited vocabularies. | high |
| Q-12 | CONFIRMED | balance-of-private.ts and transfer-public.ts are structurally identical catalogs: `enum *Impl{Default}` → abstract `*Fn` w/ `buildArgs`/`static new`(switch)/`getCandidates`(spread+`points` scoring+sort)/`getDefault` → `Default*Fn` w/ literal `abi():FunctionAbi` + predicate-filter `getCandidates`. 9 function modules present (confirmed via ls). | high |
| Q-13 | CONFIRMED | token/service.ts:186+543 `array_max(keys.map(+x))+1`; :535-552 `try/catch→toRestoreError→Restored<Token>[]`; :521 onProfileDeleted cascade. contact/service.ts:72 `profileId!==profile.id` guard, :99 `do{getRandomHex(8)}while(contains)`. Same idioms re-confirmed across account/fpc/network/auth-registry via grep. | high |
| Q-14 | CONFIRMED | execute/index.vue dApp-window shell: interaction+profile client setup (83/408), connect onMount (412-413), `beforeunload`→reject (438), reject (370), removeEventListener (377), disconnect onUnmount (442-443), profile-change reject (301). Popup disconnect-on-hide in NewContactPopup:153-154, EditContactPopup:296-297, NewFpcPopup:98. | high |
| Q-15 | CONFIRMED | dapp-send-executor.ts:204-254 and transfer-executor.ts:128-187 share the same scaffold: `registerController`→`checkCancelled`(abort→JobCancelledSentinel)→`markJournal({stage:"simulating"})`→`buildAndEstimate`→`recordTransaction`→`catch markFailedUnlessCancelled`→`finally deleteController`. Exactly the `withExecutionSlot` extraction target. | high |
| Q-16 (claude-only) | CONFIRMED | core.ts:44-50 `AppServices` declares network/transaction/account as REQUIRED; :75-77 initializes them `null as unknown as {Network,Transaction,Account}ServiceClient`; :132-139 lazy assignment in initTransactionService. The type lie is even self-documented in the :40-43 JSDoc ("undefined at runtime ... tightening it is deferred"). | high |
| Q-17 (claude-only) | CONFIRMED (count nuance) | profile/service.ts:113 defines `runExclusive`. grep: 22 `lock.enter()` + 22 `lock.leave()` total. **All 21 cited inline bypass pairs match exactly** (143/148 … 1057/1106). Nuance: the headline "22 bypasses" overcounts by 1 — the 22nd enter/leave (115/118) IS runExclusive's own body, not a bypass. So 21 inline bypasses, not 22. Every listed instance is valid; substance fully holds. | high |
| Q-18 | CONFIRMED | known-artifacts.ts:40-68 loads Token/NFT/Wonderland/PrivateFPC and calls `getContractClassFromArtifact` (line 66). note-schemas.ts:66-83 loads the SAME 4 artifacts and calls `getContractClassFromArtifact` again (lines 71/74/78/82) under a separate cache. Double-loaded + double-hashed confirmed. | high |
| Q-19 (claude-only) | CONFIRMED | chain-runtime.ts:26-40 `ProductionPxeFactoryOptions` flat booleans; its own JSDoc says proverless "mutually exclusive with required" (runtime-only) = boolean blindness. Dual key: `pxe/${profileId}/${chainId}` (chain-runtime:123, service:458) vs `${profileId}:${chainId}` (chain-runtime:215, service:127). TWO `NetworkInfo` interfaces: chain-runtime.ts:52 + artifact-registry.ts:13. All 3 sub-claims verified. | high |
| Q-20 (claude-only) | CONFIRMED | config/store.ts:14 `Object.entries(config).map(...as ConfigProp)`; :35 `{key,value} as ConfigProp`; :47-52 `apply` double-casts to `Record<string,unknown>` and validates by `typeof src[key]===typeof dst[key]` — a literal-union value (theme) passes as long as it's a string. Verified verbatim. | high |
| Q-21 | CONFIRMED | chrome-browser-api.ts:136-137 adapter lastError getter (localized double-cast, correct). runtime-port.ts:54 the canonical `lastError` port helper exists. files.ts:71-72 RE-SHIMS `(chrome.runtime as any).lastError` instead of reusing the port. general.js:21/32 untyped JS + general.d.ts:7/9 hand-maintained type shadow (drift-prone). Both narrowed drifts real; matches finding's "moderate". | moderate |
| Q-22 (claude-only) | **ADJUSTED** | Core drifts CONFIRMED: aztec README.md:62 `4.2.0` vs package.json pin `5.0.0-rc.1`; service.ts:362 + batched-view-simulation.ts:91/355 cite `@aztec/*@4.2.0`; wallet-crypto README.md:17 "PBKDF2 250k" vs encryption-key.ts:2 `600_000` (security-relevant). **BAD INSTANCE removed:** "wallet-core README says `types: []`" does NOT verify — grep of wallet-core/README.md for types/node/tsconfig/`[]` finds no such claim (only the tsconfig.json:11 `["node"]` half is real). Thesis holds; 1 of ~8 instances is phantom. | high (on core drifts) |

## Notes on instance-list accuracy

- **Q-22** had one phantom instance: the "wallet-core README `types: []`" claim has no
  corresponding text in `packages/wallet-core/README.md`. Only the `tsconfig.json:11
  "types": ["node"]` side is real, so there is nothing for it to contradict. Removed.
- **Q-17** headline count is off by one (21 inline bypasses, not 22 — the 22nd
  enter/leave pair is `runExclusive`'s own implementation). All cited instance pairs are
  accurate; only the summary number overcounts.
- All other big instance lists (Q-01, Q-02, Q-03, Q-12, Q-13, Q-14) spot-checked clean —
  every sampled location was real and exhibited the claimed smell. No phantom/duplicate/
  wrong-line entries found.
- Several findings are notably *honest* split-resolutions that NARROW an over-broad smell
  rather than inflate it: Q-02 (MethodsMap base `any[]` explicitly excused), Q-21 (broad
  "adapter unsafe" falsified → 2 concrete drifts), Q-05/Q-04 carry concrete already-real
  drift evidence (missed `contractClasses` branch; `AccessLevel.None` fallback).
