### Finding: Unreachable `dappSession` guard in `handleSendTx`

1. **Title:** Unreachable `dappSession` guard in `handleSendTx`
2. **Smell name:** Dead Code (Fowler)
3. **Maintenance impact:** structural. Blast radius: 1 file (`packages/wallet-bridge/src/dispatcher.ts`), 1 method. Change frequency: `dispatcher.ts` has 18 commits in its history (highest churn file in the cluster) — this method sits in a file that gets touched often, so misleading dead code here is read by whoever edits nearby code repeatedly.
4. **Concrete evidence:** In `handleSendTx` (`dispatcher.ts:636-699`), `resolveNetworkAndAccount(ctx, dappSession, requestedFrom)` is called at `dispatcher.ts:650` — *before* the `if (!dappSession)` check at `dispatcher.ts:658-660`. `resolveNetworkAndAccount` (`dispatcher.ts:1335-1363`) only returns normally via the branch `if (dappSession?.accounts && dappSession.accounts.length > 0)` (`dispatcher.ts:1347`), which requires `dappSession` to be truthy. Every other path throws (`dispatcher.ts:1344`, `1356`, `1358`, `1362`) before control could ever reach line 658 with `dappSession` still falsy. So the guard `if (!dappSession) throw new Error(\`No dApp session found for origin ${ctx.origin}\`)` at `dispatcher.ts:658-660` can never execute. Contrast with the other three popup handlers (`handleCreateAuthWit:714-716`, `handleRegisterToken:766-768`, `handleGrantPublicAuthwit:807-809`), which check `dappSession` *before* calling `resolveNetworkAndAccount`, so their guards are live.
5. **Why it harms future change:** A maintainer editing `handleSendTx` (a file/method touched frequently, per commit history) will read this guard and reasonably assume "the no-session case is handled here with this exact message" — then build on that assumption (e.g. writing a test expecting `"No dApp session found for origin"` for a no-session `sendTx` call, which will actually fail because `resolveNetworkAndAccount`'s different message fires first). It also masks that `handleSendTx`'s session-check ordering silently diverged from its three siblings during a prior refactor, so a future consolidation of the four handlers (see the Duplicate Code finding below) would carry the divergence forward unless someone notices this block never runs.
6. **Smallest safe refactoring:** Dead Code Removal — delete `dispatcher.ts:658-660`. This is a behavior-preserving change specifically because the code never executes today.
7. **What disappears:** The false signal that `handleSendTx` has its own no-session branch distinct from the other three handlers, and the trap for anyone writing a test or refactor against that assumption.
8. **Instances:** `packages/wallet-bridge/src/dispatcher.ts:658-660` (the only instance — the other five `if (!dappSession)` guards at lines 530, 715, 767, 808, 864 are all reachable).

### Finding: Core module reaches into a transport-subdir type (layering inversion)

1. **Title:** `core/base-client.ts` imports a transport-specific type from `offscreen/telemetry.ts`
2. **Smell name:** Inappropriate Intimacy (Fowler) — applied at module-layer granularity: the abstract, transport-agnostic `core/` layer (whose own doc comment states it exists to be shared by *any* transport) directly imports a leaf transport module's type instead of owning that vocabulary itself or having it handed in generically.
3. **Maintenance impact:** architectural (crosses the package's own documented layering: `core/` is supposed to sit below/be transport-agnostic relative to `background/`+`offscreen/`). Blast radius: 2 files today (`core/base-client.ts`, `offscreen/telemetry.ts`); latent blast radius extends to any future third transport that wants `onTerminal` telemetry. Change frequency: low (`base-client.ts` 3 commits, `telemetry.ts` 1 commit total) — the inversion has been stable but is a landmine for the next transport, not a currently-thrashing hotspot.
4. **Concrete evidence:** `packages/extension-messaging/src/core/base-client.ts:8` — `import type { RequestTerminalStatus } from "../offscreen/telemetry"`; used at `base-client.ts:52` (`TerminalRecord.status: RequestTerminalStatus`) and `base-client.ts:245` (`this.onTerminal({..., status, ...})`). `RequestTerminalStatus` is defined and doc-commented at `packages/extension-messaging/src/offscreen/telemetry.ts:37-48` as part of the "Offscreen `ServiceClient` telemetry surface." The repo-map itself flags this exact edge as a "layering inversion... core → offscreen... worth flagging to the downstream audit as an architectural wart."
5. **Why it harms future change:** `TerminalRecord` (the type `core/` itself owns and constructs, `base-client.ts:47-54`) is pinned to a status vocabulary defined inside one specific transport's file. If a third transport (or `background/client.ts`, which currently no-ops `onTerminal`) ever wants meaningful terminal telemetry with a status value `offscreen` doesn't need (or vice versa), the type has to be forked or the offscreen-owned enum has to grow transport-irrelevant cases — either way `core/` (meant to be the single shared foundation) ends up coupled to a specific transport's evolution.
6. **Smallest safe refactoring:** Move Field/Move Type — relocate the `RequestTerminalStatus` type declaration into `core/` (e.g. inline in `base-client.ts` beside `TerminalRecord`, which already references it) and have `offscreen/telemetry.ts` import it from `core/` instead of the reverse. Purely a type relocation; no runtime behavior changes.
7. **What disappears:** The core→offscreen import edge; `core/` becomes genuinely transport-agnostic as its own doc comment claims, and a new transport can implement `onTerminal` without importing from a sibling transport's directory.
8. **Instances:** `packages/extension-messaging/src/core/base-client.ts:8,52,245`; type owned at `packages/extension-messaging/src/offscreen/telemetry.ts:37-48`.

### Finding: Type-level import cycle between `action.ts` and `authwit-content.ts`

1. **Title:** `action.ts` ↔ `authwit-content.ts` mutual type import
2. **Smell name:** Cyclic dependencies (named analog to Fowler's coupling smells — mutual, bidirectional module dependency, here at the type level).
3. **Maintenance impact:** structural. Blast radius: 2 files (`packages/wallet-bridge/src/action.ts`, `packages/wallet-bridge/src/authwit-content.ts`); both are depended on by `operation.ts` (imports `Action`, `packages/wallet-bridge/src/operation.ts:9`) and by `method-scope-checkers.ts`/`dispatcher.ts` transitively via `AuthwitContent`/`Action`. Change frequency: both files show only the repo's initial-import commit in `git log` — effectively unchanged since inception, so the cycle hasn't caused a resolution incident yet, but it is a structural trap for the next person editing either type.
4. **Concrete evidence:** `packages/wallet-bridge/src/action.ts:1` — `import type { AuthwitContent } from "./authwit-content"`, used at `action.ts:28` (`AddPrivateAuthwitAction.content`) and `action.ts:34` (`AddPublicAuthwitAction.content`). `packages/wallet-bridge/src/authwit-content.ts:1` — `import type { CallAction, EncodedCallAction } from "./action"`, used at `authwit-content.ts:5` (`CallAuthwitContent = Omit<CallAction, "kind"> & {...}`) and `authwit-content.ts:10` (`EncodedCallAuthwitContent = Omit<EncodedCallAction, "kind"> & {...}`). Both imports are `import type`, so it's erased at build and causes no runtime cycle, but it is a genuine two-file type-level cycle (confirmed by direct read, matching the repo-map's flag).
5. **Why it harms future change:** Anyone reasoning about "what does `action.ts` depend on" or trying to extract either file into a smaller module (e.g. splitting `Action` variants across files, a natural next step as the union grows) has to solve the mutual reference first. It's also a readability trap: `authwit-content.ts` builds two of its four variants by `Omit`-ing a field off of `action.ts`'s types rather than owning its own shape, so a change to `CallAction`/`EncodedCallAction` silently ripples into `AuthwitContent` variants without an explicit signal at the authwit-content.ts call site.
6. **Smallest safe refactoring:** Extract Class (module-level) — pull the shared field shape (`to`/`selector`/`args`/etc., minus `kind`) that both `CallAction`/`EncodedCallAction` and `CallAuthwitContent`/`EncodedCallAuthwitContent` need into a small neutral module (e.g. `call-shapes.ts`) that both `action.ts` and `authwit-content.ts` import from one-directionally.
7. **What disappears:** The bidirectional file dependency; both files become one-directional consumers of a shared base, so either can be split, moved, or reasoned about independently.
8. **Instances:** `packages/wallet-bridge/src/action.ts:1,28,34`; `packages/wallet-bridge/src/authwit-content.ts:1,5,10`.

### Finding: Repeated 5-step popup-handler scaffold in the dispatcher

1. **Title:** Duplicated session-check → resolve → format → build-request → execute/unwrap scaffold across four popup handlers
2. **Smell name:** Duplicate Code (Fowler). Note: this is *not* a re-flag of the 2026-08-14 dedup audit's Q-13 (`resolveNetworkAndAccount` reuse) — that fix already consolidated *step 2* (account resolution) into one shared private method, which all four handlers below correctly call. What remains duplicated is the *surrounding* scaffold: the guard clause, the CAIP-format call, and the execute+unwrap tail.
3. **Maintenance impact:** structural. Blast radius: 4 call sites in 1 file (`packages/wallet-bridge/src/dispatcher.ts`). Change frequency: high — `dispatcher.ts` has 18 commits, and several past commits (e.g. `#377 "dispatcher auth reuse"`, `#272 "harden 14 audit findings"`) specifically touched this handler cluster, meaning each of the 4 sites has independently absorbed edits before.
4. **Concrete evidence:** The identical guard clause `if (!dappSession) throw new Error(\`No dApp session found for origin ${ctx.origin}\`)` appears at `dispatcher.ts:530, 659, 715, 767, 808, 864` (6 sites; see the Dead Code finding above for why line 659 never actually runs). Beyond the guard, the same 5-step shape — (1) session check, (2) `resolveNetworkAndAccount(...)`, (3) `formatCaipAccount(ctx.chainId, account.address)`, (4) build a per-method `*Request` object, (5) `dappInteractionService.execute(...)` then `this.unwrapResult(results[0])` — recurs verbatim in: `handleSendTx` (`dispatcher.ts:636-699`, resolve at 650, format at 651, build `sendOp` at 675-681, execute+unwrap at 683-698), `handleRegisterToken` (`dispatcher.ts:764-792`, resolve at 775, format at 776, build `registerOp` at 780-784, execute+unwrap at 786-791), `handleGrantPublicAuthwit` (`dispatcher.ts:802-847`, resolve at 813, format at 814, build `grantOp` at 817-832, execute+unwrap at 834-846), and the uncovered branch of `handleCreateAuthWit` (`dispatcher.ts:733-743`, resolve already done at 718/formatted at 735 reused, build `authwitReq` at 733-737, execute+unwrap at 738-742).
5. **Why it harms future change:** Adding a 5th popup-gated method (a realistic scenario — three have been added incrementally already per git history: `sendTx`, then `registerToken` in #50, then `grantPublicAuthwit` in a later feature) means copy-pasting this scaffold a 5th time rather than calling one helper, and any future fix to the scaffold itself (e.g. adding a new backpressure key to the `execute()` options bag, as `originKey` was added to `handleSendTx`/`handleGrantPublicAuthwit` but conspicuously *not* to `handleRegisterToken`/`handleCreateAuthWit`'s popup path — worth checking) has to be hand-applied at each of the 4 sites, risking silent drift like that.
6. **Smallest safe refactoring:** Extract Method — a private helper (e.g. `runPopupOperation(dappSession, requestedAccount, ctx, buildRequest: (caipAccount: string) => Request, executeOpts?: {...})`) that performs steps 1, 3, 5 and takes the per-method `buildRequest` callback for step 4 (step 2 is already the shared `resolveNetworkAndAccount`).
7. **What disappears:** 4 duplicated instances of the same 15-25 line scaffold, and the divergence risk (e.g. missing `originKey`) between them.
8. **Instances:** `dispatcher.ts:636-699` (`handleSendTx`), `dispatcher.ts:733-743` (`handleCreateAuthWit` uncovered branch), `dispatcher.ts:764-792` (`handleRegisterToken`), `dispatcher.ts:802-847` (`handleGrantPublicAuthwit`); guard clause also at `dispatcher.ts:530, 864`.

### Finding: Parallel per-capability-type coverage functions behind a switch

1. **Title:** `isCapabilityCovered` switch + 5 sibling "is this request already covered" functions
2. **Smell name:** Switch Statements (Fowler) — a discriminant-typed switch with one structurally-parallel helper function per case, where adding a new case (`Capability` variant) requires touching every layer in lockstep.
3. **Maintenance impact:** structural (contained to `packages/wallet-bridge/src/capabilities.ts` + `dispatcher.ts`, same package). Blast radius: 2 files today; a 7th capability type would touch `capabilities.ts` (new union member) + `dispatcher.ts` (new `KNOWN_CAPABILITY_TYPES` entry, new `isCapabilityCovered` case, new `xRequestCovered` function) — 4 edit points for 1 new type. Change frequency: `capabilities.ts` has only 1 commit total (the union hasn't grown since inception), so this is latent risk rather than active pain, but `dispatcher.ts` itself is high-churn (18 commits).
4. **Concrete evidence:** `dispatcher.ts:247-254` (`KNOWN_CAPABILITY_TYPES` — one entry per `Capability["type"]`), `dispatcher.ts:256-258` (`isKnownCapabilityType`), `dispatcher.ts:274-299` (`isCapabilityCovered` switch, one case per type), backed by 5 near-parallel helper functions: `contractsRequestCovered` (`dispatcher.ts:176-185`), `scopeCovers` (`dispatcher.ts:191-201`, shared helper), `transactionRequestCovered` (`dispatcher.ts:203-206`), `simulationRequestCovered` (`dispatcher.ts:210-224`), `dataRequestCovered` (`dispatcher.ts:226-236`), `accountsCapsEqual` (`dispatcher.ts:238-240`). `Capability`'s 6 variants are declared at `packages/wallet-bridge/src/capabilities.ts:53-59`.
5. **Why it harms future change:** A new `Capability` variant (plausible — the type union has grown before, e.g. `contractClasses` was added later per the map's own note that it's "TYPE-ONLY" coverage, a known filed gap) forces edits across 4 spots that must all agree on semantics; TypeScript's exhaustiveness check on the `switch` (no `default`) will catch a *missing* case at compile time, but it cannot catch a case whose coverage semantics are subtly inconsistent with its siblings — exactly the class of bug the existing `wallet-sdk-capability-field-diff` filed gap (type-only `contractClasses` coverage vs field-aware coverage for the other 5) already demonstrates happened once.
6. **Smallest safe refactoring:** Replace Conditional with Polymorphism, TS-idiomatic form — collapse the switch into a `Record<Capability["type"], (existing: GrantedCapabilityRecord[], requested: Capability) => boolean>` strategy table (mirroring how `METHOD_REGISTRY` already replaced 6 parallel tables in `method-descriptors.ts`), so a new type is one table entry instead of a switch case + a same-shaped standalone function.
7. **What disappears:** The switch statement and the requirement to keep 2 separate constructs (`KNOWN_CAPABILITY_TYPES` record + `isCapabilityCovered` switch) plus a same-shaped standalone function in sync for every capability type.
8. **Instances:** `dispatcher.ts:176-185, 191-201, 203-206, 210-224, 226-236, 238-240, 247-254, 274-299`.

### Finding: Adding a new operation-kind method requires touching 3-4 files in lockstep

1. **Title:** New wallet-sdk method with a new `Operation` kind fans out across `operation.ts`, `method-descriptors.ts`, `dispatcher.ts`'s build switches, and `method-scope-checkers.ts`
2. **Smell name:** Shotgun Surgery (Fowler).
3. **Maintenance impact:** structural (contained within `packages/wallet-bridge`, doesn't cross package boundaries). Blast radius: up to 4 files for one new method. Change frequency: high on `dispatcher.ts` (18 commits) and moderate on `method-descriptors.ts` (4 commits, one of which — `e9c51dd0` — was specifically the prior refactor that reduced 6 parallel tables to this registry, so the team has already invested effort narrowing this exact surface once).
4. **Concrete evidence:** `method-descriptors.ts:11-15` documents the residual split explicitly: *"this owns the method-name-keyed METADATA only. It does NOT own the kind→Operation build switches (`buildNetworkOperation`/`buildAccountOperation` in dispatcher.ts)... Adding a method that needs a NEW Operation kind still touches those switches."* Concretely, a new operation-kind method requires: (a) a new `Operation` variant in the union at `operation.ts:14-34`, (b) a new row in `METHOD_REGISTRY_SOURCE` at `method-descriptors.ts:170-308`, (c) a new `case` in `buildNetworkOperation` (`dispatcher.ts:1218-1253`) or `buildAccountOperation` (`dispatcher.ts:1267-1307`), and (d) usually a new `checkX` function in `method-scope-checkers.ts` (419 LOC of exactly these) referenced from the new registry row.
5. **Why it harms future change:** Every one of the ~20 existing methods in `METHOD_REGISTRY_SOURCE` already required this multi-file dance (visible in the registry's own per-method `routing: { via: "network-operation" | "account-operation", kind: ... }` field, which only encodes *where* to route, not the actual object-building logic) — so it's an established, recurring cost, not a one-off. A contributor who only edits the registry (the most discoverable "single source of truth" per the module's own docs) will get a type error from the `buildNetworkOperation`/`buildAccountOperation` switches' `default: throw` branch, but only at the specific call path exercised by tests, not necessarily as an immediate compile error, unless `Operation["kind"]` narrowing is exhaustive there too (it uses an explicit runtime `throw`, not a TS exhaustiveness check on `buildNetworkOperation`/`buildAccountOperation` — worth an exhaustiveness check like the one already used for `NETWORK_ONLY_KINDS`/`ACCOUNT_KINDS`).
6. **Smallest safe refactoring:** Move Method — attach a `buildOperation: (args: unknown[], networkId: string, accountAddress?: string) => Operation` function directly to each `MethodDescriptor` row (alongside the already-present `scopeCheck`/`argSchema`), the same consolidation pattern `method-descriptors.ts` used for the other 6 formerly-parallel tables; `buildNetworkOperation`/`buildAccountOperation` then become one generic dispatch over `descriptor.buildOperation(...)` instead of a kind-keyed switch.
7. **What disappears:** Two of the four touch points (the `buildNetworkOperation`/`buildAccountOperation` switch cases) collapse into the registry row, leaving only the `Operation` type union and the registry entry to edit per new method.
8. **Instances:** `packages/wallet-bridge/src/operation.ts:14-34`; `packages/wallet-bridge/src/method-descriptors.ts:11-15,170-308`; `packages/wallet-bridge/src/dispatcher.ts:1218-1253,1267-1307`; `packages/wallet-bridge/src/method-scope-checkers.ts` (per-method `checkX` functions throughout).

### Finding: Near-identical scope-checker bodies

1. **Title:** `checkGetContractMetadata`/`checkIsTokenRegistered` and `checkGetAddressBook`/`checkRegisterSender` are byte-near-identical
2. **Smell name:** Duplicate Code (Fowler) — explicitly acknowledged as deliberate/parallel in the module's own comments, but still literal body duplication with a real consequence (item 5).
3. **Maintenance impact:** local (contained to `packages/wallet-bridge/src/method-scope-checkers.ts`, a 419-LOC leaf module). Blast radius: 4 functions in 1 file. Change frequency: low (`method-scope-checkers.ts` has 3 commits total) — this is a slow-moving but real duplication.
4. **Concrete evidence:** `checkGetContractMetadata` (`method-scope-checkers.ts:78-88`) and `checkIsTokenRegistered` (`method-scope-checkers.ts:90-102`) are structurally identical: `String(args[0])` → `grantsOfType<ContractsCapability>(grants, "contracts")` → `if (!caps.length) return` → `caps.some((c) => c.canGetMetadata && inAddressList(address, c.contracts))` → throw a near-identical message; the only difference is the method name baked into the string and comment text. `checkIsTokenRegistered`'s own comment at line 96 says "the same consent surface as getContractMetadata." Likewise `checkGetAddressBook` (`method-scope-checkers.ts:369-375`) and `checkRegisterSender` (`method-scope-checkers.ts:382-388`) are identical: `grantsOfType<DataCapability>(grants, "data")` → `if (!caps.length) return` → `caps.some((c) => c.addressBook === true)` → throw, differing only in the method name in the thrown string. `checkRegisterSender`'s comment explicitly calls itself "F-004 (paired)."
5. **Why it harms future change:** If the `contracts`/`canGetMetadata` gate or the `data`/`addressBook` gate ever needs a behavior change (e.g. adding a second required sub-flag, which is exactly the kind of change `F-003`/`F-004` already made once to sibling checkers per the file's own audit-marker comments), a maintainer has to remember to edit both twins — the file's own "(paired)" comment is essentially a hand-written substitute for the compiler catching this, which is fragile.
6. **Smallest safe refactoring:** Extract Method — factor each pair into one parametrized helper (`checkContractsAddressGate(methodName: string, args, grants)` for the first pair; `checkAddressBookGate(methodName: string, grants)` for the second), with each named export becoming a 1-line wrapper (the same pattern the file already uses for `checkSendTx`/`checkSimulateTx`/`checkProfileTx` wrapping `checkTransactionCalls`/`checkSimulationTransactions` at `method-scope-checkers.ts:395-405`).
7. **What disappears:** 2 duplicated function bodies; a future gate-behavior change becomes a single edit instead of a "remember the paired comment" convention.
8. **Instances:** `packages/wallet-bridge/src/method-scope-checkers.ts:78-88, 90-102, 369-375, 382-388`.

### Finding: Three structurally repetitive method-patch blocks in the schema patch

1. **Title:** `applyNuloSchemaPatch`'s three method-patch blocks repeat the same if/else drift-guard shape
2. **Smell name:** Duplicate Code (Fowler).
3. **Maintenance impact:** local (contained to a single 112-LOC function in a 3-file, ~176-LOC package). Blast radius: 1 function, 3 blocks. Change frequency: very low (`apply.ts` has 2 commits total).
4. **Concrete evidence:** `applyNuloSchemaPatch` (`packages/wallet-sdk-schema-patch/src/apply.ts:51-112`) repeats the shape `if (name in schema) { const existing = target.name; if (existing !== PATCHED_SCHEMA) { const items = existing?.def?.input?.def?.items; if (<arity/type checks>) throw new Error(...) } } else { target.name = PATCHED_SCHEMA }` three times: `registerToken` block (`apply.ts:55-74`), `isTokenRegistered` block (`apply.ts:76-90`), `grantPublicAuthwit` block (`apply.ts:92-111`). Each differs only in the constant used (`PATCHED_SCHEMA`/`REGISTERED_QUERY_SCHEMA`/`GRANT_AUTHWIT_SCHEMA`), the arity/type checks, and the error-message wording.
5. **Why it harms future change:** A 4th Nulo-custom method (plausible — the package exists specifically because this pattern was needed 3 times already, growing from the original 2 to 3 over the package's short history per its own README reference to "three byte-identical inline copies" it replaced elsewhere) means copy-pasting this 20-line block a 4th time; any change to the drift-guard's overall shape (e.g. a shared way of reporting *which* upstream shape was found) has to be replicated 3-4x.
6. **Smallest safe refactoring:** Extract Method / Replace Repeated Blocks with a Loop — the repo-map's own suggested remedy: collapse the three blocks into a loop over a small `{ name, schema, validate }` table, where `validate` is a per-entry predicate over `existing?.def`.
7. **What disappears:** 3 near-identical 15-20 line blocks collapse into one loop body plus a 3-row table; a 4th method becomes one table row.
8. **Instances:** `packages/wallet-sdk-schema-patch/src/apply.ts:55-74, 76-90, 92-111`.

### Finding: `handleRequestCapabilities` is a 200-line, multi-phase method

1. **Title:** `handleRequestCapabilities` mixes delta computation, popup invocation with rejection bookkeeping, grant merging, and rejection merging in one method
2. **Smell name:** Long Method (Fowler).
3. **Maintenance impact:** structural. Blast radius: 1 method, but it's the most complex single unit in the highest-churn file in the cluster. Change frequency: high — `dispatcher.ts` overall has 18 commits, and several of the referenced audit markers inside this method (`F-1.5`-style capability re-consent logic, the try/catch rejection-persistence block) are exactly the kind of security-sensitive logic that gets revisited.
4. **Concrete evidence:** `handleRequestCapabilities` spans `dispatcher.ts:857-1056` (~200 lines) and internally does: early-exit for empty manifests (864-874), Phase 1 grant/rejection bookkeeping setup (876-880), delta computation with a nested filter closure (882-903), Phase 2 early return for an already-granted delta (905-918), Phase 3 popup-argument assembly including a conditional account-list fetch (920-936), the popup call wrapped in try/catch that on failure computes and persists `RejectedCapabilityRecord`s before rethrowing (938-961), a "safety net" branch reconciling `selectedAccounts` into `grantedResults` (963-973), an accounts-merge-and-persist block (975-992), approved/rejected type computation with a `replacementFor` closure using `JSON.stringify` comparison (994-1011), grant merging (1012-1028), rejection merging (1032-1039), a session reload (1042), and final enrichment + response shaping (1044-1055).
5. **Why it harms future change:** Each of these ~10 phases has its own local variables and closures (`delta`, `existingCaps`, `availableAccounts`, `replacementFor`, `deltaApprovedTypes`, `mergedGrants`, `newRejections`, `mergedRejections`) all sharing one function scope, so understanding or safely modifying any one phase (e.g. the accounts-merge logic) requires holding the whole 200-line method's state in your head; the try/catch at 938-961 in particular buries a security-relevant side effect (persisting rejections) in the middle of a much longer method rather than as a clearly-scoped, independently testable unit.
6. **Smallest safe refactoring:** Extract Method — split into named private methods along the existing phase boundaries the doc comment already describes ("3-phase approach"): e.g. `computeCapabilityDelta(...)`, `runCapabilityPopup(...)` (owns the try/catch + rejection persistence), `mergeApprovedGrants(...)`, `trackCapabilityRejections(...)`, with `handleRequestCapabilities` reduced to the top-level sequencing.
7. **What disappears:** A single 200-line method with ~10 interleaved concerns becomes ~5 named, independently readable and unit-testable methods, each with its own narrow variable scope.
8. **Instances:** `packages/wallet-bridge/src/dispatcher.ts:857-1056`.

### Finding: `DraftOperation`'s Omit-pattern repeated verbatim (minor)

1. **Title:** `DraftAztecSendTxOperation`/`DraftSendTransactionOperation` repeat the same `Omit<X, "feeSettings"> & { feeSettings?: FeeSettings }` template
2. **Smell name:** Duplicate Code (Fowler) — type-level, minimal instance.
3. **Maintenance impact:** cosmetic. Blast radius: 2 type declarations in 1 file. Change frequency: very low (`operation.ts` has 4 commits total).
4. **Concrete evidence:** `packages/wallet-bridge/src/operation.ts:205-207` (`DraftAztecSendTxOperation = Omit<AztecSendTxOperation, "feeSettings"> & { feeSettings?: FeeSettings }`) and `operation.ts:210-212` (`DraftSendTransactionOperation = Omit<SendTransactionOperation, "feeSettings"> & { feeSettings?: FeeSettings }`) apply the identical 3-line template to the two send-like `Operation` kinds.
5. **Why it harms future change:** Purely cosmetic today (2 instances, 3 lines each) — flagged because it was an explicit lead to verify. If a 3rd send-like operation kind is ever introduced, this becomes a 3rd copy-pasted block instead of a one-line application of a generic.
6. **Smallest safe refactoring:** Introduce a small generic type `type DraftFeeSettings<T> = Omit<T, "feeSettings"> & { feeSettings?: FeeSettings }` and apply it at both sites.
7. **What disappears:** The literal 3-line template repetition; a 3rd send-like kind becomes a 1-line `DraftFeeSettings<NewKind>` application.
8. **Instances:** `packages/wallet-bridge/src/operation.ts:205-207, 210-212`.

### Finding: Schema-patch drift guard is intimate with zod's private `.def` representation

1. **Title:** `applyNuloSchemaPatch`'s drift guard reaches into zod's internal `.def` shape, not its public API
2. **Smell name:** Inappropriate Intimacy (Fowler) — applied to a third-party library boundary: the code depends on `zod`'s internal object representation (`.def.input.def.items`, `.def.output.def.type`) rather than any documented public accessor.
3. **Maintenance impact:** local today, but change-cost is architectural in kind (a routine dependency bump can silently invalidate the safety mechanism this code exists to provide). Blast radius: 1 function (`applyNuloSchemaPatch`), but its entire reason to exist is this guard. Change frequency: low (`apply.ts` 2 commits), but its trigger condition (a zod version bump) is exactly the kind of change that wouldn't show up as a commit to this file until it silently breaks.
4. **Concrete evidence:** `apply.ts:58` (`existing?.def?.input?.def?.items`), `apply.ts:63` (`existing?.def?.output?.def?.type !== "void"`), `apply.ts:79-80`, `apply.ts:95-96, 99-100` all destructure zod's `.def` internal representation rather than a public `zod` API (`.parameters()`/`.returnType()`, which the doc comment at `apply.ts:16-21` explicitly says the v4 upgrade *removed* in favor of this internal shape). Compounding this: the doc comment at `apply.ts:28` states "Pinned upstream version: `@aztec/wallet-sdk == 5.0.0`" while `packages/wallet-sdk-schema-patch/package.json:15-16` pins `@aztec/aztec.js`/`@aztec/stdlib` at `5.0.1` — the doc-comment pin and the actual dependency pin have already drifted apart once.
5. **Why it harms future change:** A future `zod` major/minor bump that changes the internal `.def` shape (not a public-API break, so it wouldn't necessarily show up in `zod`'s own changelog as breaking) would cause the drift-guard's `items?.length !== 2` / `.def?.output?.def?.type` checks to silently evaluate differently — either false-triggering the "signature changed" throw on a perfectly fine upstream, or worse, failing to detect an actual conflicting upstream method (since the check itself is built on assumptions about a representation zod doesn't promise to keep stable). Either failure mode defeats the exact protection this function was written to provide, and neither would be caught by TypeScript (the whole block is under a `biome-ignore lint/suspicious/noExplicitAny` cast at `apply.ts:52-53`).
6. **Smallest safe refactoring:** None purely mechanical — this needs a decision, not a refactor (isolate the `.def`-reaching logic behind a small `describeZodFunctionShape(fn)` helper with one clearly-commented "this couples to zod's internal representation, re-verify on any zod bump" note, and add a CI check or test that runs against zod's actual currently-pinned version to catch drift early rather than at runtime).
7. **What disappears:** Nothing structurally — this is a fragility-reduction rather than a duplication-reduction; flagging it so the "revisit on bump" comment has teeth (e.g. a test that fails loudly on a zod bump instead of relying on someone remembering to re-check).
8. **Instances:** `packages/wallet-sdk-schema-patch/src/apply.ts:52-53, 58, 63, 79-80, 95-96, 99-100`.

## Non-findings considered

- `background/client.ts` vs `offscreen/client.ts` and `background/service.ts` vs `offscreen/service.ts` structural parallelism — reviewed directly per the map's pointer; the remaining per-transport code (Port lifecycle vs. sendMessage/uid routing, keepalive vs. none, telemetry vs. none) is genuinely different, not copy-paste residue; the historical duplication was already consolidated into `core/base-client.ts`/`core/base-service.ts`.
- `checkTransactionCalls` (`method-scope-checkers.ts:116-133`) vs `checkSimulationTransactions` (`method-scope-checkers.ts:152-184`) — related shape but not true duplication: the simulation variant's extra per-call null/type guard is load-bearing (it's the sole guard for `simulateTx`, which the dispatcher's `assertAuthRelevantArgShape` deliberately skips), while `checkTransactionCalls` is only reached via `sendTx`/`profileTx`, both of which *are* pre-guarded by `assertAuthRelevantArgShape`. Collapsing them would either weaken a load-bearing guard or add a redundant one; not flagged.
- `formatSessionAccounts`/`enrichGrantedCapabilities` accounts projection — already unified via the shared `projectSessionAccounts` helper (`dispatcher.ts:578-591`); no duplication remains.
- `background/index.ts` vs `offscreen/index.ts` 4-line re-export barrels — trivial, not a real duplication risk (per map, confirmed).
- `WalletSdkDispatcher` as a whole (1368-LOC file, 14 sibling-module imports) — considered as a standalone "Large Class" finding but declined in favor of the more specific, independently-actionable findings above (Long Method, Switch Statements, Shotgun Surgery, Duplicate Code) that already decompose its actual cost drivers; a generic "split the class" finding on top would be less concrete than those.
- `DiscoveryQueue`'s direct `chrome.action` badge side effect coupled into mutation points (`discovery-queue.ts`) — a real design note from the map, but not a duplication/coupling-cost pattern with a clean Fowler mapping within this cluster's scope; no concrete "future change gets harder" scenario identified beyond general side-effect-in-mutator style, which falls under "could be cleaner" without a named smell.
- `wallet-sdk-schema-patch` README's claim of "single source of truth" replacing three inline copies in consumer apps — verified the packages in scope don't reintroduce it; checking the four consumer apps themselves for a re-crept 4th copy is out of this cluster's scope (`apps/*` excluded).

## Bug handoffs

- `handleSendTx` never surfaces `"No dApp session found for origin ${ctx.origin}"` (dead guard at `dispatcher.ts:658-660`); a caller with an undefined `dappSession` instead sees `resolveNetworkAndAccount`'s `"No accounts authorized. The dApp must call requestCapabilities() with accounts type first."` — confirm this is the intended user-facing message (it diverges from the other 3 popup handlers' explicit no-session message).
- `packages/wallet-sdk-schema-patch/src/apply.ts:28` doc comment pins `@aztec/wallet-sdk == 5.0.0` while `package.json:15-16` pins `@aztec/aztec.js`/`@aztec/stdlib` at `5.0.1` — stale version-pin comment, worth a one-line doc fix and a sanity re-check that the drift-guard still matches 5.0.1's actual schema shape.