I have all the evidence needed. Here is the final report.

### Finding: Popup Enter-to-submit + show/hide lifecycle hand-rolled in 10 popups instead of reusing `usePopupEntity`

1. **Title:** Popup Enter-to-submit + show/hide lifecycle hand-rolled in 10 popups instead of reusing `usePopupEntity`
2. **Smell name:** Duplicate Code (Fowler)
3. **Maintenance impact:** structural | blast radius: 10 files (`NewEndpointPopup.vue`, `ChangeAuthwitsRegistryPopup.vue`, `NewContactPopup.vue`, `EditContactPopup.vue`, `RevokeAuthwitsPopup.vue`, `NewFpcPopup.vue`, `EditProfilePopup.vue`, `EditFpcPopup.vue`, `NewTokenPopup.vue`, `NewSenderPopup.vue`) | change frequency: low-to-moderate per file (1-3 commits each: `EditContactPopup.vue` 3, `NewSenderPopup.vue` 3, `NewContactPopup.vue`/`EditFpcPopup.vue`/`RevokeAuthwitsPopup.vue`/`EditProfilePopup.vue` 2, rest 1) but the underlying fix was already attempted once and stalled: commit `578861be` ("harden-quality arc Q-01..Q-22") shows `usePopupEntity` was created specifically to kill this duplication (Q-14/P16) but the rollout was explicitly scoped to only 5 popups, with the remaining 10 documented as deferred.
4. **Concrete evidence:** `apps/extension/src/composables/usePopupEntity.ts` (48 lines) exists and is consumed by exactly 5 popups: `EditNetworkPopup.vue:73-77`, `EditAccountPopup.vue`, `EditEndpointPopup.vue`, `NewAccountPopup.vue`, `NewNetworkPopup.vue`. The other 10 popups hand-roll the identical `watch(() => props.show, ...)` add/remove-listener + `onKeydown` guard shape:
   - `NewContactPopup.vue:120-142` (watch block), `:151-162` (`onKeydown`, comment at `152-155`)
   - `EditContactPopup.vue:161-186` (watch), `:195-203` (`onKeydown`, comment at `196-198`)
   - `NewFpcPopup.vue:94-116` (watch), `:121-127` (`onKeydown`, comment at `122`: *"see NewContactPopup for rationale"* — a cross-file comment reference, itself evidence of the duplication)
   - `NewEndpointPopup.vue:70,72` (add/remove), `:77` (`onKeydown`)
   - `ChangeAuthwitsRegistryPopup.vue:97,105` (add/remove), `:110` (`onKeydown`)
   - `RevokeAuthwitsPopup.vue:148,158` (add/remove), `:163` (`onKeydown`)
   - `EditProfilePopup.vue:88,105` (remove/add), `:110` (`onKeydown`)
   - `EditFpcPopup.vue:158,180` (remove/add), `:192` (`onKeydown`)
   - `NewTokenPopup.vue:273,291` (remove/add), `:296` (`onKeydown`)
   - `NewSenderPopup.vue:81,90` (remove/add), `:95` (`onKeydown`)

   The `NewContactPopup.vue:152-155` comment ("Only fire on input/textarea fields. Without this guard...") is copy-pasted near-verbatim into `EditContactPopup.vue:196-198` and paraphrased-by-reference in `NewFpcPopup.vue:122`.
5. **Why it harms future change:** the Enter-guard semantics (fire only when an `<input>`/`<textarea>` is focused, to avoid double-firing with the Button's native Enter-activation) is a subtle invariant now maintained independently in 10 places. A future fix (e.g., extending the guard to a new focusable element type, or fixing a double-fire edge case) requires 10 coordinated edits instead of one; missing even one instance silently reintroduces the bug in that popup only.
6. **Smallest safe refactoring:** Duplicate Code → Substitute Algorithm — replace each hand-rolled `watch(show)`/`onKeydown` pair with a `usePopupEntity(() => props.show, { submit, onShow, onHide })` call, exactly mirroring the 5 existing consumers (e.g. `EditNetworkPopup.vue:73-77`).
7. **What disappears after the refactoring:** ~10-20 lines of hand-rolled listener/watch wiring per file (100+ lines total), the copy-pasted guard comment, and the cross-file "see NewContactPopup for rationale" reference.
8. **Instances:** `NewEndpointPopup.vue:70-77`, `ChangeAuthwitsRegistryPopup.vue:97-110`, `NewContactPopup.vue:120-162`, `EditContactPopup.vue:161-203`, `RevokeAuthwitsPopup.vue:148-163`, `NewFpcPopup.vue:94-127`, `EditProfilePopup.vue:88-110`, `EditFpcPopup.vue:158-192`, `NewTokenPopup.vue:273-296`, `NewSenderPopup.vue:81-95`.

### Finding: Entity add/update/delete event wiring hand-rolled in 4 CRUD popups instead of reusing `useEntityCrud`

1. **Title:** Entity add/update/delete event wiring hand-rolled in 4 CRUD popups instead of reusing `useEntityCrud`
2. **Smell name:** Duplicate Code (Fowler)
3. **Maintenance impact:** structural | blast radius: 4 files (`NewFpcPopup.vue`, `NewContactPopup.vue`, `EditFpcPopup.vue`, `EditContactPopup.vue`) | change frequency: `EditContactPopup.vue` 3 commits, `NewContactPopup.vue`/`EditFpcPopup.vue` 2 commits, `NewFpcPopup.vue` 1 commit.

   **Correction to the mapping-phase lead:** the map's §9 claimed `NewTokenPopup.vue` and `NewSenderPopup.vue` also hand-roll this pattern. Verified against source: they do not. `NewSenderPopup.vue` fetches its sender list once with no live add/update/delete subscription at all. `NewTokenPopup.vue`'s `onBalanceAdded`/`onTaskUpdated` (`NewTokenPopup.vue:109-134`) are a bespoke bounded balance-wait mechanism, not an entity-list splice — a different shape entirely. The real instance count is 4, not 4-claimed-as-"NewFpcPopup + 3 structurally similar" — it's `NewFpcPopup` + `NewContactPopup` + two *Edit* popups the map didn't mention.
4. **Concrete evidence:** `apps/extension/src/composables/useEntityCrud.ts` is consumed by exactly the 5 settings list pages (`fpcs/index.vue:38-44`, `contacts/index.vue`, `tokens/index.vue`, `advanced/account-state/{authwits,senders}/index.vue`). The 4 popups instead hand-write the same three-handler splice shape:
   - `NewFpcPopup.vue:83-92` (`onFpcAdded`/`onFpcUpdated`/`onFpcDeleted`) wired at `:106-108`
   - `NewContactPopup.vue:33-57` (`onContactAdded`/`onContactUpdated`/`onContactDeleted`) wired at `:27-31`
   - `EditFpcPopup.vue:129-146` (same three handlers, plus in-place `fpcToEdit` sync) wired at `:168-170`
   - `EditContactPopup.vue:29-57` (same three handlers, plus in-place `contactToEdit` sync) wired at `:29-31`
5. **Why it harms future change:** each popup independently reimplements find-by-id/splice/push logic for its entity (FPC or contact). A future protocol change (e.g., the service starts emitting a batch-update event, or the identity key changes) must be hunted down and patched in 4 places with slightly different shapes (the `Edit*` variants have extra "is this the entity currently being edited" branches interleaved into the same handler), rather than one composable + local callback.
6. **Smallest safe refactoring:** Duplicate Code → Substitute Algorithm — replace the hand-written `onXAdded`/`onXUpdated`/`onXDeleted` + `.add()`/`.remove()` wiring with `useEntityCrud({ fetch, added, updated, deleted, mode: "incremental" })`, then layer the popup-specific "is this the entity I'm editing" logic as a `watch` on the returned `entities` ref (already the pattern list pages use for derived state).
7. **What disappears after the refactoring:** ~15-20 lines of duplicated splice-handler logic per file, plus the manual `.add`/`.remove` calls that must currently be paired correctly with `disconnect()` by hand in each popup's `show`-watcher.
8. **Instances:** `NewFpcPopup.vue:83-92,106-108`, `NewContactPopup.vue:27-31,33-57`, `EditFpcPopup.vue:129-146,168-170`, `EditContactPopup.vue:29-31,33-57`.

### Finding: dApp approval windows duplicate footer/status-strip markup and CSS across execute/discover/capabilities

1. **Title:** dApp approval windows duplicate footer/status-strip markup and CSS across execute/discover/capabilities
2. **Smell name:** Duplicate Code (Fowler) — logic was already extracted (`useDappApprovalWindow.ts`), but the markup/CSS layer was not.
3. **Maintenance impact:** structural | blast radius: 3 files (`popup/windows/execute/index.vue`, `popup/windows/discover/index.vue`, `popup/windows/capabilities/index.vue`) | change frequency: high relative to the rest of the cluster — `execute/index.vue` 5 commits, `capabilities/index.vue` 5 commits, `discover/index.vue` 3 commits, all clustered around active dApp-execution feature work (`#364`, `#349`, `#347`, `#303`) — this is one of the most actively-changed surfaces in the cluster, raising the odds that a footer/style tweak lands in only 1-2 of the 3 files.
4. **Concrete evidence:**
   - CSS `<style module>` block — `.wrapper`/`.scroll_area`/`.footer` byte-identical across all three: `execute/index.vue:561-577,592-598`, `discover/index.vue:209-225,231-237`, `capabilities/index.vue:388-404,410-416`.
   - Footer template (Tooltip error banner + Reject/Confirm `<Button>` pair) structurally identical, differing only in `data-testid` values, button labels ("Reject"/"Confirm" vs "Deny"/"Allow" vs "Reject"/"Approve"), and the `:disabled` expression on the primary button: `execute/index.vue:502-544`, `discover/index.vue:166-202`, `capabilities/index.vue:341-377`.
   - The composable that already extracted the *logic* half of this: `useDappApprovalWindow.ts:1-140` (mounted/unmounted lifecycle, `closeWindow`, active-profile guard) — confirming the markup was deliberately left out of that extraction, per the map's characterization.
5. **Why it harms future change:** a visual or accessibility change to the footer (e.g., adding a loading state to the Reject button, changing the error-banner icon logic, adjusting the border/background tokens) must be applied identically 3 times. Because the divergent parts (test-ids, labels, disabled conditions) are interleaved with the identical parts (layout, Tooltip structure, class names) in the same block, a partial edit is easy to make and hard to notice is partial — a real risk given the surface's high change frequency.
6. **Smallest safe refactoring:** Duplicate Code → Extract Component (Vue-specific composable-extraction-opportunity analog, but for markup rather than logic) — pull the footer into a shared `DappApprovalFooter.vue` composite taking `processingError`, `rejectTestId`/`confirmTestId`, `rejectLabel`/`confirmLabel`, `isLoading`, and `confirmDisabled` as props/slots, and move `.wrapper`/`.scroll_area`/`.footer` into that composite's `<style module>` (or a shared CSS module import).
7. **What disappears after the refactoring:** 3 duplicated ~35-line footer templates and 3 duplicated CSS blocks (~15 lines each), leaving each window's `<style module>` with only its window-specific classes (`.fullscreen_icon` for execute, `.sections`/`.body` for the others).
8. **Instances:** `execute/index.vue:502-544,560-598`, `discover/index.vue:166-202,208-237`, `capabilities/index.vue:341-378,387-416`.

### Finding: `settings/security/export/{seed,key}.vue` duplicate the agreement-gate block and unlock lifecycle

1. **Title:** `settings/security/export/{seed,key}.vue` duplicate the agreement-gate block and unlock lifecycle
2. **Smell name:** Duplicate Code (Fowler)
3. **Maintenance impact:** local-to-structural | blast radius: 2 files | change frequency: genuinely coupled historically — both files share 3 commits (`#380` clipboard-copy dedup, `#272` security hardening, `#186` repo restructure), i.e. every substantive change so far has touched both together, which is itself evidence this pairing needs to stay in sync by hand today.
4. **Concrete evidence:**
   - Agreement-gate template block, near-byte-identical except a div class (`export_section` vs `export_section_last`) and one noun substitution ("Seed phrase" ↔ "Plain key"): `seed.vue:100-121` vs `key.vue:151-172` (both contain the identical closing sentence "By continuing you agree to all risks and responsibilities.").
   - Duplicated unlock-lifecycle state/flow: `isStarted`/`isUnlocked`/`password`/`isWrongPassword` refs (`seed.vue:35-38` vs `key.vue:35-38`), `handleUnlock` try/catch shape (`seed.vue:52-62` vs `key.vue:63-73`), the `onKeydown`+`watch(isStarted)` pair (`seed.vue:73-82` vs `key.vue:83-92`, key.vue's watch has one extra `&& selectedKey.value === "private"` guard), and the `onBeforeUnmount` null-and-remove-listener cleanup (`seed.vue:84-89` vs `key.vue:94-100`).
   - Both already share the 4 composite imports and `useSecretCountdown`/`useSecretClipboardCopy` (per the map, and confirmed by the `// ... previously duplicated word for word here` comments at `seed.vue:66-67` and `key.vue:76-77`) — i.e. the codebase has a track record of deduplicating pieces of exactly this pair before, but stopped short of the agreement-gate/unlock-state layer.
5. **Why it harms future change:** the "before you continue" legal/risk copy and the unlock-attempt state machine are product-owned surfaces likely to change together (e.g., adding a rate-limit on `isWrongPassword`, or updating the risk disclosure wording for both secret types). Today that requires editing two files with slightly different branch conditions (`key.vue`'s extra `selectedKey.value === "private"` guards), risking the two pages drifting further apart the way the copy-paste history already shows they tend to.
6. **Smallest safe refactoring:** Duplicate Code → Extract Component for the agreement-gate block (parameterize by secret-type noun + `class` variant), and Extract Function/composable for the unlock lifecycle (bundle `isStarted`/`isUnlocked`/`password`/`isWrongPassword`/`handleUnlock`/`onKeydown`/`onBeforeUnmount` behind a `useSecretUnlockGate({ unlock, autoCloseMs, onTimeout })`-shaped composable, following the precedent set by `useSecretCountdown`/`useSecretClipboardCopy`).
7. **What disappears after the refactoring:** ~20 lines of duplicated agreement-gate markup and ~25 lines of duplicated unlock-state/lifecycle wiring, leaving each page with only its secret-retrieval call (`exportMnemonic` vs `exportPlain`/`exportEncrypted`) and copy text.
8. **Instances:** `seed.vue:35-38,52-62,73-89,100-121`, `key.vue:35-38,63-73,83-100,151-172`.

### Finding: `MigrationBarrier` and `AccountIntegrityBarrier` clone the raw-storage-overlay skeleton with two independently-invented staleness guards

1. **Title:** `MigrationBarrier` and `AccountIntegrityBarrier` clone the raw-storage-overlay skeleton with two independently-invented staleness guards
2. **Smell name:** Duplicate Code (Fowler), with a Shotgun-Surgery-risk analog on the divergent part — explained in (5).
3. **Maintenance impact:** structural | blast radius: 2 files, each mounted at the app root (`popup/app.vue:267-268`, `onboarding/app.vue:89` — `MigrationBarrier` only, no `AccountIntegrityBarrier` in onboarding) | change frequency: `MigrationBarrier.vue` introduced in `#246`, `AccountIntegrityBarrier.vue` introduced in `#303` by cloning it — one commit each so far, but the clone-on-creation history is itself the smell manifesting.
4. **Concrete evidence:**
   - CSS: `.wrapper`/`.card`/`.title`/`.sub`/`.detail` are byte-identical: `MigrationBarrier.vue:106-144` vs `AccountIntegrityBarrier.vue:91-129` (MigrationBarrier additionally has `.banner`/`.bannerText`/`.bannerDismiss` for its degraded-state banner, which AccountIntegrityBarrier has no equivalent of).
   - Component skeleton: both do `Teleport to="body"` + raw `chrome.storage.local` read (explicitly allowlisted in the storage-facade-ban in both, per their own comments) + `chrome.storage.onChanged.addListener` subscribed before the initial read + `onBeforeUnmount` listener removal: `MigrationBarrier.vue:1-76` vs `AccountIntegrityBarrier.vue:1-64`.
   - The divergent part: each solves "avoid an out-of-order `onChanged` event racing a slower `refresh()` snapshot" with its own bespoke mechanism — `MigrationBarrier.vue` uses a per-key `eventTouched` Set (`:40,47-59`), `AccountIntegrityBarrier.vue` uses a monotonic `refreshGeneration` counter (`:36-40,52-54`). Two different fixes for the identical race, invented independently.
5. **Why it harms future change:** if the out-of-order-resolution race pattern turns out to have an edge case (a plausible scenario given it's a hand-rolled ordering guard against unordered IPC), the fix must be independently re-derived and reapplied in both files because they don't share the guard mechanism — this is the Shotgun-Surgery-analog consequence: one conceptual bug fix requires two structurally-different patches, not one shared patch applied twice.
6. **Smallest safe refactoring:** Duplicate Code → Extract Component/composable — factor the shared shell (Teleport overlay, CSS classes, subscribe-before-read, `onBeforeUnmount` cleanup, and the `eventTouched`-style ordering guard, since it's the more general of the two mechanisms) into a shared `useStorageObserverBarrier(keys, guardFn)`-shaped composable or generic overlay component that both barriers configure.
7. **What disappears after the refactoring:** the duplicated `.wrapper`/`.card`/`.title`/`.sub`/`.detail` CSS block (~40 lines) and the duplicated subscribe/refresh/cleanup skeleton (~25 lines), and — most importantly — the second, independently-maintained staleness-guard implementation.
8. **Instances:** `MigrationBarrier.vue:1-76,106-144`, `AccountIntegrityBarrier.vue:1-64,91-129`.

### Finding: `balances.store.ts`'s `fetchGas`/`fetchFpc` share a near-identical single-flight-leg skeleton, inlined twice

1. **Title:** `balances.store.ts`'s `fetchGas`/`fetchFpc` share a near-identical single-flight-leg skeleton, inlined twice
2. **Smell name:** Duplicate Code (Fowler), with a Template Method extraction opportunity as the named refactoring target.
3. **Maintenance impact:** local-to-structural | blast radius: 1 file, but `balances.store.ts` is the single heaviest/most-consumed state owner in the cluster (per the map: consumed by `GasBalanceCard.vue`, `FeeSettingsCard.vue`, `send.vue`, `execute/index.vue`, `ChangeAuthwitsRegistryPopup.vue`, `RevokeAuthwitsPopup.vue`) | change frequency: only 1 commit in git history (`0700d7f2`, introducing the whole store), so no repeat-touch evidence yet — but the extensive inline "D11"/epoch-fence/codex-post-impl-audit commentary throughout both functions indicates the fetch-leg invariants are actively being reasoned about and are a plausible future edit site (only `gas`/`fpc` legs exist today — `BalanceLeg = "gas" | "fpc"` at `:82` — so this is exactly a 2-instance duplication, not a speculative N).
4. **Concrete evidence:** `fetchGas` (`balances.store.ts:356-466`, 111 lines) and `fetchFpc` (`:468-517`, 50 lines) both: (a) check `legFlights.get(`${key}|<leg>|${epoch}`)` for an in-flight run and early-return it (`:357-358` vs `:469-470`); (b) build a `run = (async () => {...})()` that guards on `entries.value[key]` existing, commits a `"fetching"` status, `await`s `withTimeout(rawReuse(key, "<leg>", epoch, () => client.call(...)), INIT_FETCH_TIMEOUT_MS, "...")` inside try/catch (`:376-405` vs `:475-485`); (c) re-checks the epoch fence (`if (epochOf(scope.profileId) !== epoch) return`, `:406` vs `:486`) and re-fetches `entries.value[key]` (`:411-412` vs `:487-488`); (d) on success, commits `status: "ready"`, bumps `version`, conditionally bumps `retryVersion`/clears `retryDebt` on `cause === "retry"` (`:413-436` vs `:489-502`); (e) on failure, commits `status: "degraded"` + `retryDebt: true` + `lastError` (`:437-450` vs `:503-509`); (f) registers the run in `legFlights` and cleans it up in a `finally` (`:452-465` vs `:511-516`). `fetchGas` additionally interleaves forced-refresh-only bookkeeping (`forcedGasPending`/`forcedGasSeq`, `:366-370,379-397,407-410,417,428,446,459-463`) that has no `fetchFpc` counterpart (fpc has no forced cause, per the inline comment at `:498`).
5. **Why it harms future change:** a fix to the shared skeleton — e.g., changing how the epoch fence interacts with the in-flight check, or how `retryDebt`/`retryVersion` are set on success — must be applied to both functions and re-verified against each one's own inline invariant comments; the ~70% shared shape is currently indistinguishable from the ~30% leg-specific logic at a glance, so a change aimed at "the shared part" risks being applied to only one leg.
6. **Smallest safe refactoring:** Duplicate Code → Template Method — extract the shared skeleton (in-flight check → fetching-status commit → timed `rawReuse` call → epoch-fence re-check → ready/degraded commit → `legFlights` cleanup) into a single higher-order `runLegFetch(key, leg, epoch, scope, opts, { call, onForcedPre, onForcedPost })`-shaped function, with `fetchGas` supplying the forced-cause hooks and `fetchFpc` supplying none — following the Template Method precedent already used for `definePassthroughsExhaustive` (Q-05) and `passthrough` shaping (Q-07) elsewhere in this codebase's messaging layer.
7. **What disappears after the refactoring:** ~50 lines of duplicated skeleton logic, leaving `fetchGas`/`fetchFpc` as thin callers supplying only the leg name, the RPC call, and (for gas) the forced-cause hooks.
8. **Instances:** `balances.store.ts:356-466` (`fetchGas`), `balances.store.ts:468-517` (`fetchFpc`).

## Non-findings considered

- `rawReuse`/`rawFlights` single-flight promise cache (`balances.store.ts:334-349`) — genuinely shared already (one function, called from both legs); not duplicated.
- `retryTimers`/`scheduleRetry`/`armRetry` backoff machinery (`balances.store.ts:547-579+`) — single implementation, not duplicated across legs; the heavy inline invariant commentary reflects real domain complexity (epoch fencing, forced-run supersession), not an extractable smell — "could be cleaner" without a concrete duplication/coupling consequence is explicitly out of scope.
- `settings/security/export/full.vue` — the map's §9 flags `seed.vue`/`key.vue` specifically as the near-identical pair; `full.vue` (full-backup export, 13 service-client imports per the map) is structurally a different flow and wasn't verified as sharing the agreement-gate/unlock-state duplication — left out of the finding rather than assumed.
- `NewTokenPopup.vue`/`NewSenderPopup.vue` as `useEntityCrud` duplication instances — the map's §9 claimed these two also hand-roll the entity-CRUD-wiring shape; verified against source and found not to match (see Finding 2's correction note) — reported as a map-claim correction, not flagged as a separate finding.
- `PopupCard.vue`'s template-only `useFullscreenPopupSetting()` service-client lifecycle bypassing the biome layer-ban (documented in the map §3) — this is an architecturally-sanctioned carve-out per the map's own account, not a fresh finding, and no additional coupling/duplication consequence was found beyond what the map already documents.
- Q-14/P16 dApp-window-shell markup deferral (git history at `578861be`) — the map's Finding 3 (dApp approval window footer/CSS duplication) is this exact deferred item resurfacing; not double-counted, just corroborated with its origin.

## Bug handoffs

- `EditContactPopup.vue:44` comment notes a previously-dead branch ("compared against the ref object instead of its value") was already fixed — no current bug, informational only, not handed off.