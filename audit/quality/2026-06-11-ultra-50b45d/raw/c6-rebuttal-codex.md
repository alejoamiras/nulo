## a. Misses
- Claude missed the repeated capability-fixture/grant choreography in `packages/extension/tests/e2e/fixtures/extension.ts`: four copies of the same `phase` wrapper (`:383-390`, `:407-414`, `:468-475`, `:524-530`), four copies of the same setup ladder (`:391-399`, `:415-421`, `:476-482`, `:532-538`), and three near-identical capability-grant branches (`:429-457`, `:490-513`, `:547-570`). Codex-1 was right to elevate that because this is the hottest scoped file.
- Claude also missed the temporal coupling between `setLastActiveProfileId` and `setSentinel`. The writes are split across helpers (`packages/extension/src/utils/lastActiveProfile.ts:7-13`, `packages/extension/src/utils/core.ts:143-148`), and only some activation flows do both (`onboarding/pages/create.vue:131-132`, `onboarding/pages/import.vue:122-123`, `popup/pages/profile/new.vue:160-170`, `popup/pages/import.vue:149-150`) while others only update last-active (`popup/pages/auth.vue:102`, `popup/components/popups/SelectProfilePopup.vue:32`). Codex-2 caught the change-amplification smell.
- Missed by all four: `openOnboarding()` and `openPopupOnce()` duplicate page bootstrap/listener setup in the harness: `patchPagePolling`, viewport/focus, resetting `ctx.consoleErrors/pageErrors`, and attaching `console`/`pageerror` listeners (`packages/extension/tests/e2e/fixtures/extension.ts:115-130` vs `:997-1018`).

## b. Overreach / wrong calls
- Claude1 F10 is partly out of scope. C6 scope is limited to `packages/wallet-core/src/**`, `packages/wallet-crypto/src/**`, specific extension source/config/scripts/fixtures (`audit/quality/2026-06-11-ultra-50b45d/raw/clusters.md:29-31`). That finding relies on `packages/wallet-core/README.md:13-48` and `packages/wallet-crypto/README.md:13-45`, which are outside scope. The in-scope subclaim about `packages/wallet-core/src/utils/serialization.ts:6-7` being stale is correct (`packages/wallet-core/tsconfig.json:11`), but bundling README drift into a C6 finding overreaches.
- Claude1 F2’s attack on the “extension remains the single test runner” comment is correct, not overreach: package-local runners do exist at `packages/wallet-core/vitest.config.ts:1-14`, `packages/wallet-crypto/vitest.config.ts:1-14`, and `packages/extension-messaging/vitest.config.ts:1-15`, and each package exposes its own `test` script (`wallet-core/package.json:16-18`, `wallet-crypto/package.json:9-11`, `extension-messaging/package.json:17-19`).

## c. Confirmed Claude findings
- Strongest convergence: entry-shell console bootstrap duplication. All four documents converge on the copied logger/rejection wiring in `packages/extension/src/wallet/index.ts:54-67`, `popup/index.ts:5-17`, `offscreen/index.ts:20-47`, and `onboarding/index.ts:13-23`.
- Strongest convergence: Vite/Vitest config sprawl plus mutable wrapper composition. The duplicated `resolvePackageFile` / alias / define blocks are real (`packages/extension/vite.config.ts:8-17,44-55,310-315`; `vitest.config.ts:13-22,37-44,46-51`), and the browser wrappers do mutate the imported base object in place (`vite.chrome.config.mts:7-18`, `vite.firefox.config.mts:7-18`).
- Strongest convergence: serialization-policy sprawl in `wallet-core`. The three separate serializers in `packages/wallet-core/src/utils/arrays.ts:23-39`, `utils/serialization.ts:24-57`, and `jobs/error.ts:60-77` are source-backed and costly to evolve independently.
- Claude2’s dead-surface finding is also confirmed on the core-lib side: `getRandomElement` (`wallet-core/src/utils/random.ts:18`), `Queue.dequeueBatch` (`utils/queue.ts:47-55`), `EntityStorage.getVersion/setVersion/findByPredicate` (`storage/entity_storage.ts:62-82,137-142`), and the unused `@aztec/stdlib` dependency (`wallet-crypto/package.json:13-16`) are real.

## d. Contradictions
- Direct contradiction: `src/setup/` dead-entry status. Claude1 F4 and Claude2 F9 treat it as dead because it is built but otherwise unreferenced; c6-codex-1 explicitly rejected that and treated `packages/extension/vite.config.ts:295-300` wiring as sufficient to avoid a dead-code call.
- Direct contradiction: `general.js` / `general.d.ts`. Claude1 F9 and Claude2 F10 elevate it as duplicated contract maintenance; c6-codex-1 explicitly rejects it as a non-finding.
- Threshold contradiction: `lastActiveProfile` / `sentinel`. c6-codex-2 flags temporal coupling; Claude2 explicitly rejects extraction here despite the split writes at `lastActiveProfile.ts:7-13` and `core.ts:143-148`.
- Threshold contradiction: `EntityStorage` local duplication. c6-codex-1 keeps `parseOrDelete` vs `getVersion` as a standalone smell (`entity_storage.ts:47-77`); Claude1 subsumes it into dead-method deletion, and Claude2 rejects it as below the bar.