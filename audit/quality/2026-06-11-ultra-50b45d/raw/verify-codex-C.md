### Q17 — CONFIRMED (high)
Independent assessment: before reading the writeup, I saw repeated PXE “resolve instances/artifacts then register if missing” flows plus repeated ABI lookup-by-name/selector logic across `tx-request-builder`, `authwit-discoverer`, `batched-view-simulation`, `execution/service`, `token/service`, and `fpc/service`. Instance check: the cited ranges all exhibit that smell; I did not find a false positive among them. Corrections: none to smell name or scope. Refactoring sanity: the safe smallest move is narrower than “one new module” suggests: extend the existing `ContractResolver` with tiny `ensureRegistered`, `findFunctionByName`, and `findFunctionBySelector` helpers, preserving current lookup order and current error strings.

### Q18 — ADJUSTED (high)
Independent assessment: the execution layer really does rely on both positional result tuples and repeated transfer primitive bundles. That is genuine Data Clumps plus Primitive Obsession. Instance check: every cited line matches, but the list is incomplete; missed tuple consumers include `execution/service.ts:894-895,1801-1805,1849,1953-1956`. Corrections: the finding is stronger on internal builder/strategy returns than on the public RPC surface; the instances need expansion. Refactoring sanity: replacing internal tuples with named result objects is safe and small. Converting the `spec.ts`/`client.ts`/service transfer API to a `TransferRequest` object is broader wire/API churn, so it is not the smallest safe first step.

### Q19 — ADJUSTED (high)
Independent assessment: many services copy the same `getActiveProfile()` guard, and the lock-state message has already drifted between `"Profile locked"`, `"Wallet locked"`, and `"Wallet is locked"`. Instance check: all cited lines are real duplicate guards; no listed false positives. The list is materially incomplete: additional live instances include `execution/service.ts:521-522,768-769,1080-1082,1425-1427`, `execution/tx-request-builder.ts:98-100,391-392`, `dapp-interaction/service.ts:254-256`, `execution/helpers/get-view-simulation-deps.ts:32-35`, `account/service.ts:205-207`, and `token-balance/service.ts:249-251`. Corrections: widen the blast radius. Refactoring sanity: `requireActiveProfile()` is the right direction, but it must preserve callers that intentionally return `undefined` instead of throwing, such as `dapp-session/service.ts:87-90`.

### Q20 — CONFIRMED (high)
Independent assessment: the bridge and extension each carry near-identical CAIP helpers, and the comments already show source-of-truth drift. Instance check: the cited bridge/extension ranges are valid; I did not see a false instance or another non-test production copy beyond this pair. Corrections: none, except that the stale header language is supporting evidence rather than the core smell. Refactoring sanity: consolidation is safe only if ownership moves downward, not upward. `wallet-bridge` or a still-lower shared package can own the helpers and the extension can re-export them; the reverse would violate layering. Preserve the extension’s `parseCaipChain` behavior when unifying.

### Q21 — ADJUSTED (high)
Independent assessment: `scope-enforcement.ts` contains repeated checker skeletons with only capability type, scope field, or error label changing. Instance check: all cited lines match. The instances are slightly incomplete: `checkExecuteUtility` and `checkGetPrivateEvents` at `scope-enforcement.ts:132-168` belong to the same family, not just their `!caps.length` lines at `141,158`. Corrections: expand the instance set; the smell name stays Duplicate Code. Refactoring sanity: a heavy checker-factory abstraction is probably not the smallest safe move for policy code. Local helper extraction that preserves exact error text and current “no grants => pass through” semantics is safer.

### Q22 — ADJUSTED (moderate)
Independent assessment: there are multiple serializer-like helpers, but they are not one uniform policy. `jsonStringify` is round-trip/wire-oriented, `normalizeError` builds a bounded error envelope, and `safeStringify` is a comparison-key canonicalizer. Instance check: `wallet-core/src/utils/serialization.ts` and `wallet-core/src/jobs/error.ts` do share overlapping BigInt/Error serialization logic. `wallet-core/src/utils/arrays.ts:23-39` does not convincingly share the same root cause; it has materially different semantics. Corrections: narrow this to parallel serializer logic in two helpers and drop `arrays.ts`; the three-helper “policy sprawl” framing overstates it. Refactoring sanity: sharing a low-level BigInt/Error replacer may be safe; forcing one serialization core across all three behaviors is not.

### Q23 — ADJUSTED (high)
Independent assessment: the claim/cancel path is obviously timing-sensitive across helper, service, journal lock, and mutex. The code explicitly documents “correctness-by-microtask-interleaving,” which is the maintainability smell. Instance check: the cited lines are valid but incomplete. Missed supporting instances include `execution/service.ts:1915-1930,2062-2077`, `operation-journal/service.ts:52-60,216-223`, and `execution-mutex.ts:97-180`, which hold the other side of the ordering contract. Corrections: widen the blast radius to include the journal and mutex collaborators. Refactoring sanity: “one coordinator” is directionally right but too broad for a smallest safe change. A narrower abstraction around controller/journal handshake, backed by the existing race tests, is safer.