## Missed
- `packages/extension/tests/e2e/fixtures/extension.ts` has an unflagged harness-duplication seam between `openOnboarding()` and `openPopupOnce()`: both do `newPage()`, `patchPagePolling()`, `setViewport()`, `bringToFront()`, clear `ctx.consoleErrors/pageErrors`, and attach identical `console`/`pageerror` listeners (`extension.ts:115-130` vs `:997-1018`). That is plain Duplicate Code in the hottest harness file, and none of us called it out.

## Over-asserted
- My `EntityStorage` duplication finding was too strong as a standalone smell. The duplicated recovery block is real in `packages/wallet-core/src/storage/entity_storage.ts:47-59` and `:65-77`, but `getVersion()`, `setVersion()`, and `findByPredicate()` are only referenced by their own tests (`packages/wallet-core/src/storage/entity_storage.test.ts:88-100`), not production callers. I should have framed that as a byproduct of dead surface / low-blast-radius local duplication, not as an independently important finding.
- My “E2E Vitest settings are spread across near-clone configs” wording overstated the sameness. There is real duplication (`"@"` alias, some `pool/isolate/retry`, `@aztec` inlining), but the files also diverge for substantive reasons: smoke uses `global-setup-smoke` and 60s timeouts (`vitest.e2e.config.ts:14-41`), all-suite omits `pool/retry` (`vitest.e2e.all.config.ts:13-31`), and network adds noir node aliases (`vitest.e2e.network.config.ts:17-54`). The duplication is narrower than my “near-clone” label implied.

## Anchoring corrections
- I was anchored by `clusters.md` focus hints around “3 stringify variants,” “config sprawl,” and “fixtures duplication,” and that bias pushed me toward the pre-labeled families instead of rereading the harness cold. The missed `openOnboarding`/`openPopupOnce` duplication is the clearest consequence.
- I was also anchored by the repo-map dead-export claims. That made me too ready to treat `EntityStorage.getVersion/setVersion/findByPredicate` as “dead-ish” evidence before separating “no production callers” from “test-pinned API” with a fresh grep.
- I let the framing around placeholder/setup discussions distort my confidence. The code proves `src/setup/` is built (`packages/extension/vite.config.ts:295-300`) and unreferenced in the scoped sources, but whether it should be removed is weaker than the more direct duplication/coupling smells elsewhere.