# Verified findings — 2026-06-11

Merge of dual-family verification (`raw/verify-claude-{A,B,C}.md`, `raw/verify-codex-{A,B,C}.md`) over `findings/consolidated.md`. Merge rules: both CONFIRM → confirmed at max agreement; one ADJUSTED → adjustment applied and noted; both ADJUSTED → both correction sets applied. Q16 follows the coordinator-of-coordinators convergence ruling (see finding).

**Re-ranks applied (rule 4):** Q16 narrowed from wide to medium blast radius under the convergence ruling — moved from the S/W/Warm block down to the head of the S/M/Warm block (after Q19). Q22 narrowed from a three-helper to a two-helper consolidation surface — moved below Q23 to last. Q10's bucket upgraded from "Potential architectural" to architectural (find-stage disagreement resolved by dual high-confidence confirmation); position unchanged. All other positions preserved from consolidated.md.

## Findings (final priority order)

### Q1: Wallet method metadata scattered across parallel registries
**Smell:** Shotgun Surgery via parallel registries. **Bucket:** architectural. **Blast radius:** wide (5 bridge files plus dispatcher consumers). **Change frequency:** hot (`dispatcher.ts`/`scope-enforcement.ts` are active hotspots). **Cross-model:** convergent
**Instances:** `packages/wallet-bridge/src/capability-map.ts:18,21-46`; `packages/wallet-bridge/src/dispatcher.ts:163-198,237-280,867-956`; `packages/wallet-bridge/src/scope-enforcement.ts:9-10,348-362`.
**Evidence:** `METHOD_CAPABILITY_MAP`, `EXEMPT_METHODS`, `METHOD_TO_KIND`, `NETWORK_ONLY_KINDS`, `ACCOUNT_KINDS`, both operation-build switches, and `METHOD_SCOPE_CHECKER` all restate the same method-name facts. `scope-enforcement.ts:9-10` carries the verbatim keep-in-sync warning.
**Why it harms change:** Adding or reclassifying one wallet method is a multi-file scavenger hunt with silent omission modes.
**Smallest safe refactoring:** Introduce a single `MethodDescriptor` registry and derive capability lookup, routing kind, and scope checker from it. Generated maps must preserve current method names/behavior exactly; `AUDIT F-00x` markers survive as descriptor fields. The three `nulo-schema-patch.ts` copies are a pinned house contract — out of scope for this registry.
**Corrections applied:** Kept the registry finding; dropped broader comment-style claims that did not add distinct change amplification.
**Verification:** confirmed (high) — both families verified all instances with no adjustments.

### Q2: Popup/onboarding profile flows duplicated, with shared passkey UI still housed under popup paths
**Smell:** Duplicate Code plus Inappropriate Intimacy. **Bucket:** architectural. **Blast radius:** wide (4 page files plus shared dialog). **Change frequency:** hot (`import.vue`/`profile/new.vue` are UI hotspots). **Cross-model:** convergent
**Instances:** `packages/extension/src/popup/pages/import.vue:92-340,459-539,656-667`; `packages/extension/src/onboarding/pages/import.vue:62-297,385-468,528-539`; `packages/extension/src/popup/pages/profile/new.vue:72-175`; `packages/extension/src/onboarding/pages/create.vue:51-136`; `packages/extension/src/onboarding/pages/import.vue:10,470`; `packages/extension/src/onboarding/pages/create.vue:8,263`; `packages/extension/src/popup/components/popups/PasskeyCeremonyDialog.vue:10-14,31,82`.
**Evidence:** Popup and onboarding import/create pages duplicate the same validation, passkey ceremony, bootstrap, and error-routing handlers — near line-for-line, down to the `shakeInput` keyframes. The shared passkey dialog imports `@/popup/utils/passkey-ceremony`, teleports to `#popup`, and its "Used by" header omits the onboarding consumers (stale). However, onboarding intentionally provides its own `#popup` anchor (`src/onboarding/app.vue:76-78`), so the coupling is "shared passkey UI still housed under popup paths," not a hard popup-only dependency.
**Why it harms change:** One profile-flow change now requires synchronized edits across two shells and one popup-housed dialog.
**Smallest safe refactoring:** Extract `useProfileImportFlow`/`useProfileCreateFlow` (parameterized — the two shells differ on completeImport, routes, toasts). Move the passkey dialog/utilities to a neutral shared module path; that is smaller and safer than an adapter rewrite.
**Corrections applied:** Merged the duplicated-flow and popup-boundary findings; kept the boundary erosion, not just the stale dialog comment.
**Verification:** ADJUSTED (moderate) — codex softened the "popup-only passkey UI" claim (onboarding provides the `#popup` anchor itself) and downgraded the refactoring from adapter to neutral module move; claude confirmed all instances.

### Q3: Background/offscreen RPC transports are forked base-class stacks
**Smell:** Duplicate Code — forked transport-policy base implementations. **Bucket:** architectural. **Blast radius:** wide (4 base classes, 40+ background consumers, PXE offscreen consumers). **Change frequency:** hot enough to matter because every transport-layer policy change touches both forks. **Cross-model:** convergent
**Instances:** `packages/extension-messaging/src/background/client.ts:37-46,94-132,134-247`; `packages/extension-messaging/src/offscreen/client.ts:36-52,88-137,176-296`; `packages/extension-messaging/src/background/service.ts:14-19,29-38,62-102,139-215`; `packages/extension-messaging/src/offscreen/service.ts:19-25,34-43,52-186`.
**Evidence:** Both clients own the same request map, id allocation, timeout, A6 JSON-fallback parse, and logging quartet; both services own the same validate/unwrap/invoke/sanitize/respond flow and byte-identical `ensureInitialized()` (background 187-199 = offscreen 158-170). Background reconstructs typed `WalletError` payloads (`walletErrorFromPayload`, client.ts:112); offscreen rejects with plain strings. Each side also owns genuinely different transport behavior (Port reconnects vs `sendMessage`, telemetry, keepalive, per-method timeouts).
**Why it harms change:** Every RPC-layer improvement or bug fix is paid twice, and equivalent transport failures surface with incompatible error contracts.
**Smallest safe refactoring:** Start with shared helpers for response decoding, timeout bookkeeping, the JSON fallback, and `ensureInitialized()`; a full shared request-correlator/service-core is a later step. Unifying the error contracts changes offscreen's rejection type (string → typed) — a deliberate behavior alignment that must be decided, not slipped in.
**Corrections applied:** Removed out-of-scope re-implementation evidence from outside C5; kept only in-scope transport duplication.
**Verification:** ADJUSTED (moderate) — codex renamed the smell (dropped "Alternative Classes with Different Interfaces"; the internals diverge, the public shapes do not) and shrank the smallest-safe step to helper slices; claude confirmed all instances including the error-contract divergence.

### Q4: `ExecutionService` remains a multi-responsibility hotspot
**Smell:** Large Class with Divergent Change. **Bucket:** structural. **Blast radius:** wide (central execution facade and its collaborators). **Change frequency:** hot (`service.ts` is the wallet hotspot). **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/execution/service.ts:252-335,405-823,914-1408,1476-1575,1713-2300`; additionally `service.ts:1033-1128` (register contract/sender/token handlers — further responsibility spread, added by verification).
**Evidence:** One 2302-line class owns transfer flows, journal/cancel flow, gas-balance caching, fee-estimate reuse, dApp execution, registration handlers, and Aztec RPC adapters — 13 injected collaborators, the largest state cluster and the widest fan-in in the execution subsystem. The file's own `execution-coordinator.ts` header corroborates the half-done extraction.
**Why it harms change:** Unrelated execution concerns collide in one file, so every change starts with the same large reload cost.
**Smallest safe refactoring:** One cohesive extraction at a time — e.g. gas-balance caching/query logic, or one execution pipeline — keeping the RPC surface and behavior stable. The "thin RPC facade" end-state is directional, not the first step.
**Corrections applied:** Dropped the overstated "no facade test exists" claim; the hotspot finding stands without it.
**Verification:** ADJUSTED (high) — codex added the 1033-1128 instance and re-scoped the refactoring to incremental single extractions; claude confirmed all ranges.

### Q5: Four journaled execution pipelines share a duplicated lifecycle tail
**Smell:** Duplicate Code. **Bucket:** structural. **Blast radius:** wide (4 top-level send paths in the hottest file). **Change frequency:** hot. **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/execution/service.ts:405-610,1130-1213,1860-2015,2022-2205`; false extraction doc at `packages/extension/src/wallet/services/execution/execution-coordinator.ts:15-19`.
**Evidence:** `executeTransfer`, `executeSendTransaction`, `executeAztecSendTx`, and `executeNoFromSendTx` each hand-roll the same lifecycle tail: AbortController registration, `checkCancelled` closure, journal staging (simulating→proving→submitting→terminal), `proveTxTask` → `toTx()` → `sendTxTask` → history persistence, `JobCancelledSentinel` catch, `finally` cleanup. Preparation logic legitimately differs per path (`executeNoFromSendTx` carries extra discovery). The `execution-coordinator.ts:15-19` comment affirmatively claims `proveAndSend` "DOES move" into the coordinator — but the method was never created; the doc is false, not merely stale.
**Why it harms change:** Any lifecycle-stage or persistence change is a 4-copy edit with proven drift risk.
**Smallest safe refactoring:** Extract a helper around the shared prove→submit→persist tail with per-path callbacks (offchain effects, receipt wait, activity shapes) — not a fully generic template over all preparation logic.
**Corrections applied:** Kept the `proveAndSend` doc evidence; reduced inflated duplicated-line counts from the raw reports.
**Verification:** ADJUSTED (high) — codex narrowed the duplication to the shared lifecycle tail rather than full methods end-to-end; claude found the doc-staleness is *stronger* than consolidated stated (comment claims the extraction happened; it never did).

### Q6: Activity-feed extraction is half-done around `RecentActivityView`
**Smell:** Large Component plus Duplicate Code. **Bucket:** structural. **Blast radius:** wide (widget, page, shared utils). **Change frequency:** hot (`RecentActivityView.vue` is a UI hotspot). **Cross-model:** convergent
**Instances:** `packages/extension/src/popup/components/modules/general/RecentActivityView.vue:91-109,148-185,205-251,345-400,704-817`; `packages/extension/src/popup/pages/activity.vue:38-44,52-77,87-93,147-156,172-179`; stale extraction comment at `packages/extension/src/utils/activity-rows.ts:11-14`. (`activity-rows.ts:42-76` and `journal-state.ts:324-352` are the *good* shared helpers — comparison anchors, not smell sites; removed from the instance list.)
**Evidence:** `RecentActivityView:91-109` reimplements the tx/journal/incoming triple-merge that `activity.vue:133-140` gets from the shared `buildActivityRows`; `activity-rows.ts:11-14` claims the merge was extracted from both surfaces — false for the widget. Incoming-transfer handlers, config-toggle reload, and token wiring are duplicated verbatim between widget and page. Awaiting-card prop derivation (345-400) mirrors the already-extracted `buildJournalTerminalCardProps` shape-for-shape. The token/!token template branches (704-817) duplicate the full awaiting+merged-rows block.
**Why it harms change:** Feed rule changes now hit the widget, the page, and partial utility extractions separately.
**Smallest safe refactoring:** Extract `useIncomingTransfers` and `buildJournalAwaitingCardProps`; collapse the two template branches into one renderer. Reusing `buildActivityRows` in the widget needs parameters (row budget, token-scoped filter).
**Corrections applied:** Kept the real awaiting/data-source duplication; dropped overbroad claims about terminal-card logic that is already shared.
**Verification:** ADJUSTED (moderate) — codex reclassified the two utils cites as good helpers rather than smell sites (kept the stale comment); claude confirmed all widget/page duplication including the false extraction claim.

### Q7: Build/test config policy is forked across Vite/Vitest configs
**Smell:** Config Sprawl plus Mutable Data in the browser wrappers. **Bucket:** structural. **Blast radius:** wide (7 config files). **Change frequency:** hot enough to show shipped drift. **Cross-model:** convergent
**Instances:** `packages/extension/vite.config.ts:8-17,44,48-55,310-316`; `packages/extension/vitest.config.ts:13-22,38-44,46-52,73-77`; `packages/extension/vitest.e2e.config.ts:7,21-41`; `packages/extension/vitest.e2e.network.config.ts:7,17-20,29-48,51-55`; `packages/extension/vitest.e2e.all.config.ts:7,10-31`; `packages/extension/vite.chrome.config.mts:7-22`; `packages/extension/vite.firefox.config.mts:7-22`.
**Evidence:** `resolvePackageFile`, aliases, define blocks, and e2e runner settings are duplicated under literal "Keep in sync" comments. `vitest.e2e.all.config.ts` already drifted from `vitest.e2e.network.config.ts` — missing noir aliases and `retry: 2`/`pool`/`isolate` — while its include glob covers the network suite and `test:e2e:all` is wired in package.json: shipped divergence. Browser wrappers mutate the imported base config in place (`push` onto `viteConfig.plugins`, in-place `build.outDir`).
**Why it harms change:** Tooling changes now require manual sync across build, unit, and e2e configs, with already-shipped divergence.
**Smallest safe refactoring:** Extract shared config helpers and compose wrappers via `mergeConfig` or config factories. The host-specific noir-alias comment must travel with the shared helper.
**Corrections applied:** Wrapper mutation kept as part of config sprawl; not as a separate temporal-coupling lead finding. Blast-radius count corrected from 8 to the 7 cited files.
**Verification:** confirmed (high) — both families confirmed all instances and the shipped drift; claude's minor count correction (7 files, not 8) applied.

### Q8: Popup form abstractions are half-finished, so dialogs reassemble common lifecycle logic
**Smell:** Duplicate Code and half-done abstraction — two distinct gaps: (a) `useFormState` lacks async rebase, (b) `useEntityCrud` adoption is incomplete. **Bucket:** structural. **Blast radius:** wide but smaller than consolidated stated (form composable, popup shell, the dialog family; the selector/balance list-sync sites are an adjacent family). **Change frequency:** hot across popup forms. **Cross-model:** convergent
**Instances:** `packages/extension/src/composables/useFormState.ts:89-117,153-169`; `packages/extension/src/popup/components/popups/EditEndpointPopup.vue:37-43,85,148`; `EditNetworkPopup.vue:43-53,78,137`; `EditContactPopup.vue:120-130,149,434-463`; hand-rolled event/list sync in `NewContactPopup.vue:30-47,181-292`, `EditContactPopup.vue:32-74,348-463`, `NewFpcPopup.vue:83-108,121-207`, `EditFpcPopup.vue:128-167,189-295`, `SelectFpcPopup.vue:47-50,77-87`, `SelectTokenPopup.vue:26-37`; adjacent family members outside the strict popup-form scope: `SelectBalanceTypePopup.vue:45-65`, `SelectProfilePopup.vue:20-23,46-60`, `BalanceView.vue:150-169`. (`useEntityCrud.ts:7-8` is evidence of the unused abstraction, not a smell site.)
**Evidence:** `useFormState` fixes its baseline at construction with no rebase API, so every async edit dialog hand-rolls refill and bypasses `form.isDirty` with per-field dirty computeds. `FormPopup` emits only button-click submit; consumers duplicate the identical Enter-keydown guard (NewContact's copy even documents the double-fire hazard a `FormPopup`-level fix must handle). List-sync drift is real: `SelectTokenPopup` has no update handler; `BalanceView`'s delete handler diverges.
**Why it harms change:** Edit-form, submit-key, and service-event behavior are all changed in N dialogs instead of in the shared abstractions.
**Smallest safe refactoring:** Two independent tracks: (1) add a `rebase()` API to `useFormState` and move Enter-key/error-shell ownership into `FormPopup` (handling the documented double-fire hazard); (2) migrate hand-rolled list sync to `useEntityCrud` dialog by dialog.
**Corrections applied:** Reframed from "consumers ignored existing abstractions" to "the abstractions do not yet cover the async edit lifecycle."
**Verification:** ADJUSTED (moderate) — codex split the finding into its two component gaps, deflated the blast radius, and reclassified `useEntityCrud.ts:7-8` plus the SelectBalanceType/BalanceView sites; claude confirmed every cited dialog including the drift.

### Q9: Service readiness is split between declarative deps and hand-copied `ensureInitialized()`
**Smell:** Temporal coupling plus Duplicate Code. **Bucket:** architectural. **Blast radius:** wide (the full service fleet, shared base class — broader than the ten counted services). **Change frequency:** warm, with repeated new methods extending the pattern. **Cross-model:** convergent
**Instances:** dependency declarations at `packages/extension/src/wallet/services/contact/service.ts:19` and `packages/extension/src/wallet/services/incoming-transfer/service.ts:68-75`; fallback mechanism at `packages/extension-messaging/src/background/service.ts:187-199`; topology contract at `packages/wallet-core/src/base/index.ts:55-70`; repeated preamble counts (grep-reproduced): profile 24, network 17, fpc 9, contact 8, account 7, token 7, auth-registry 4, dapp-session 3, transaction 2, config 0; the same preamble also recurs heavily in `account-state`, `execution`, `note`, `operation-journal`, `dapp-interaction`, and `token-balance` (added by verification).
**Evidence:** Only two services declare startup dependencies; the rest rely on a per-method `await this.ensureInitialized()` backed by a 500ms-poll/30s-timeout loop, plus reviewer discipline. `ServiceCollection.start()` already provides topological phases specifically to avoid mysterious readiness timeouts.
**Why it harms change:** "Does this method need the preamble?" is encoded per method, not per service graph, so readiness policy keeps drifting method-by-method — config has zero preambles.
**Smallest safe refactoring:** Centralize transport-side gating in the base service class and separately expand declared `dependencies` — NOT gating at one dispatch boundary, because many calls are in-process rather than RPC-only.
**Corrections applied:** Kept as architectural drift, not as proof of a pervasive current null-deref path.
**Verification:** ADJUSTED (high) — codex widened the instance family by six services and corrected the smallest-safe refactoring (dispatch-boundary gating misses in-process callers); claude reproduced the preamble counts exactly.

### Q10: Shared infrastructure wiring is half-migrated out of the composition root
**Smell:** Half-done composition root / ports migration. **Bucket:** architectural (upgraded from "Potential architectural" — find-stage disagreement resolved by dual high-confidence confirmation). **Blast radius:** wide (runtime wiring plus many services). **Change frequency:** warm. **Cross-model:** disagreement at find-stage → convergent at verification (both families confirmed, high)
**Instances:** `packages/extension/src/wallet/runtime.ts:105-125`; browserApi fallback seams at `contact/service.ts:35-39`, `profile/repository.ts:42-45`, `profile/session-manager.ts:130-132`; hard-coded storage in `account/service.ts:23`, `token/service.ts:42`, `transaction/service.ts:36`, `network/service.ts:143,687,752,758`, `fpc/service.ts:43`, `auth-registry/service.ts:29-30`, `dapp-session/service.ts:29`; per-service PXE clients at `token/service.ts:57`, `transaction/service.ts:52`, `network/service.ts:163`, `fpc/service.ts:60`, `note/service.ts:46`, `execution/service.ts:342`, `token-balance/service.ts:67`, `account-state/service.ts:36`; additional sites added by verification: `profile/service.ts:56-67`, `operation-journal/service.ts:74-87`, `token-balance/balance-repository.ts:19-20`, `incoming-transfer/repository.ts:33-35`.
**Evidence:** The runtime comment literally states migrated services accept `browserApi` while "remaining services still reach into `chrome.*` directly until their migration lands." Remaining services hard-code `chrome.storage.local` or construct their own `PxeServiceClient`, despite runtime-owned collaborators existing elsewhere. The migration is acknowledged in-flight, but the change-amplification cost is measurable, so the pre-existing-pattern exclusion does not apply.
**Why it harms change:** Storage/PXE wiring changes fan across service constructors and `init()` bodies instead of staying at the composition root.
**Smallest safe refactoring:** Per-collaborator factory/port injection from `runtime.ts` (storage first or PXE first), not a broad composition-root rewrite; centralize the fallback seam once.
**Corrections applied:** Dropped the refuted `lastActiveProfile`/`sentinel` coupling claim; kept only the source-backed composition-root drift.
**Verification:** ADJUSTED (high) — codex widened the instance set by four sites and re-scoped the refactoring to per-collaborator injection; claude verified every cited storage and PXE site individually.

### Q11: `WalletSdkDispatcher` is a hotspot class with a smaller duplicated session-account flow
**Smell:** Large Class, plus Duplicate Code in the session-account projection. **Bucket:** structural. **Blast radius:** wide (package hub for every wallet-sdk request). **Change frequency:** warm-to-hot. **Cross-model:** convergent
**Instances:** hotspot scope: `packages/wallet-bridge/src/dispatcher.ts:227-292,404-521,531-760,867-1006` (1,011-line file; class spans ~207-1011). True duplication sites: `494-497`, `721-747` (reimplements `formatSessionAccounts`'s caip/alias projection inline), `989-997`. (`347-358` is the extracted helper itself; `599-600` is only a partial account-load snippet — both reclassified as anchors, not duplicates.)
**Evidence:** One class owns routing, popup orchestration, grant persistence, response shaping, operation building, and session-aware account resolution. The `resolveNetwork → getAccounts(profileId, chainId) → getSessionAccountAddresses → filter/project` pipeline is repeated at the three duplication sites instead of routed through the existing helper.
**Why it harms change:** Protocol-routing, grants, popups, and account selection all collide in the same hub, so local bridge changes keep reopening the same file.
**Smallest safe refactoring:** Extract the account-resolution collaborator first (route all session-account projection through one shared helper); grant management extraction follows. Constraints: `formatSessionAccounts` pins wire-format parity via dispatcher unit tests, and `dispatcher.test.ts` pins the three-copy `nulo-schema-patch` shape — internal extraction must preserve both.
**Corrections applied:** Folded the duplicated session-account flow and `handleRequestCapabilities` maintenance pressure into this hotspot instead of keeping separate lower-level findings.
**Verification:** ADJUSTED (high) — codex reframed as "Large Class plus a smaller duplicated flow" and reclassified two of the five duplication cites; claude confirmed the inline `formatSessionAccounts` reimplementation and flagged the two wire-parity pins.

### Q12: E2E harness setup/grant/bootstrap logic is duplicated in the hottest fixture module
**Smell:** Duplicate Code (the fixture ladders and grant choreography) plus mixed-concern hotspot module (the bootstrap/DOM-helper load). **Bucket:** structural. **Blast radius:** wide across the e2e suite. **Change frequency:** warm-to-hot (`fixtures/extension.ts` is the hottest scoped file). **Cross-model:** codex-only at find-stage → dual-verified (both families confirmed, high)
**Instances:** duplication: `packages/extension/tests/e2e/fixtures/extension.ts:282-296,383-390,391-399,407-414,415-421,429-457,468-475,476-482,490-513,524-530,532-538,547-570` — four identical inline `phase` wrappers (383, 407, 468, 524), four launch→registerProfile→openPopup→waitForHash→switchToLocalNetwork→connectPlayground ladders, three near-identical capability-grant choreographies (454, 510, 567). Mixed-concern/hotspot evidence (not duplication per se): `extension.ts:997-1018,1088-1236` (`openOnboarding`/`openPopupOnce` repeat the page bootstrap), `helpers.ts:2,20` (generic DOM helpers imported back from the launcher; unexported `TEST_PASSWORD` re-declared in 4+ test files).
**Evidence:** The in-file comment documenting intentional duplication sets a "refactor once we have three or more such fixtures" threshold — now met (3 cap-grant fixtures), so the finding aligns with, not violates, the documented constraint. Explicitly in-cluster harness scope, so the test-code exclusion is satisfied.
**Why it harms change:** Harness changes repeatedly hit the same 1,249-line file and require synchronized edits across setup variants and helper call sites.
**Smallest safe refactoring:** Extract `phase`, a shared connected-playground setup helper, and a parameterized cap-grant helper; a single exported `TEST_PASSWORD`. Splitting DOM utilities into `fixtures/dom.ts` is optional second-step cleanup.
**Corrections applied:** Kept only in-scope fixture evidence; dropped broader out-of-scope password-count inflation from the raw reports.
**Verification:** ADJUSTED (high) — codex separated true duplication from hotspot/mixed-concern evidence and demoted the `TEST_PASSWORD` non-export from standalone smell; claude confirmed all sites and the now-met refactor threshold.

### Q13: PXE in-process contract surface is manually synchronized across subset interfaces
**Smell:** Manual subset interface projection — an unchecked synchronization surface (no proven drift to date). **Bucket:** architectural. **Blast radius:** wide (PXE spec, in-process facade, proxy, client). **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/aztec-runtime/src/pxe/spec.ts:24-81` (21 methods); `packages/aztec-runtime/src/pxe/ipxe.ts:27-50` (18-method hand-written subset, omitting getNoteSchemas/getBlockTimestamp/clearChainState); `packages/aztec-runtime/src/pxe/proxy.ts:32-102` (the same 18 network-currying delegations restated by hand); `packages/aztec-runtime/src/pxe/client.ts:72-201` (per-method restatement with zod parsing). (`packages/extension/src/wallet/services/pxe/client.ts:24` is a documented re-export shim — context, not an exhibiting instance.)
**Evidence:** The subset is not mechanically derived and no drift-pinning test exists, so omissions are unchecked. `IPXE` is Nulo-owned, not an upstream-interface mirror, so deriving it violates no upstream-mirror constraint (only the behavioral "mirrors upstream BaseWallet.simulateTx" comment exists).
**Why it harms change:** PXE surface changes must be replayed across multiple hand-maintained declarations even when only the subset boundary is meant to change.
**Smallest safe refactoring:** Define one canonical subset key list and add type-level assertions that `IPXE` and `PXEProxy` stay aligned with `Methods`. Full mapped-type derivation is a larger second step — it must drop the `network` param and promisify, and `client.ts`'s per-method zod response validation cannot be generated (partial mechanization only).
**Corrections applied:** Narrowed the claim from "already accidental drift" to "manual subset boundary with unchecked synchronization."
**Verification:** ADJUSTED (high) — codex dropped the re-export shim as an exhibiting instance and shrank the smallest-safe step to key-list + type assertions; claude counted the 21/18 method split exactly and flagged the derivation caveats.

### Q14: Per-item restore-result accumulation is duplicated across ten services
**Smell:** Duplicate Code. **Bucket:** structural. **Blast radius:** wide (10 loop sites plus a profile variant plus backup UI collectors). **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/config/service.ts:43-59`; `account/service.ts:213-234`; `contact/service.ts:290-319`; `token/service.ts:532-558`; `transaction/service.ts:302-324`; `network/service.ts:614-657`; `fpc/service.ts:470-520`; `auth-registry/service.ts:285-311`; added by verification: `account-state/service.ts:183-240` (twice, nested for senders/contracts) and `token-balance/service.ts:256-272`. Variant: `profile/service.ts:830-975` is NOT the loop — it is a single-entity restore with secret-handling and profile-type branching that shares only the `restoreError` catch shape.
**Evidence:** The same accumulate/persist/catch→`push({...entity, restoreError})` loop is reimplemented across the fleet, with live drift: `contact` stores the raw `err` object (contact:308-310) while peers normalize to `.message`; lock usage is inconsistent (transaction/account/config lockless).
**Why it harms change:** Any restore-contract change becomes a 10-file edit with live shape drift.
**Smallest safe refactoring:** Extract only the per-item result/error accumulation helper — a fully generic `restoreEntities` is too coarse, because per-service validation, locking, and id-allocation logic genuinely varies and stays as service-specific hooks.
**Corrections applied:** Kept the family; corrected the consequence from "error lost" to "error-shape drift."
**Verification:** ADJUSTED, both families (high) — profile/service.ts removed from the loop family (non-loop variant); two missed instances added (account-state, token-balance); family recounted as ten loop sites + one variant; refactoring narrowed to the accumulation helper.

### Q15: Lifecycle purge cascade is duplicated across the service fleet
**Smell:** Duplicate Code — lifecycle purge cascade (profile/chain/account-deletion variants). **Bucket:** structural. **Blast radius:** wide (9+ services, 13+ sites). **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/account/service.ts:43-50,194-202`; `token/service.ts:72-79,515-521`; `transaction/service.ts:74-82,166-174`; `contact/service.ts:256-270`; `fpc/service.ts:71-80,447-461`; `dapp-session/service.ts:325-338`; `network/service.ts:671-691`; `auth-registry/service.ts:56-70` (account-deletion cleanup — same family, different trigger); siblings added by verification: `incoming-transfer/service.ts:190-248` and `operation-journal/service.ts:154-160`.
**Evidence:** The same load/filter/delete/emit purge loop recurs for profile, chain, and account cleanup, with inconsistent lock discipline (account/token/transaction lockless; contact/fpc/dapp-session/auth-registry/network take `this.lock`) and intra-file strategy divergence (token's `clearChainState` inline-deletes+emits while its `onProfileDeleted` delegates to `deleteToken`; network adds nested `purgeChain` error-swallowing plus a raw `chrome.storage.local.remove`).
**Why it harms change:** Cleanup semantics, batching, or error policy must be changed across a dispersed family of nearly identical listeners.
**Smallest safe refactoring:** A shared row-iteration/delete helper with service-owned side effects (predicates, events, cache eviction, pending-map cleanup) — one global purge helper would be too aggressive. Emit order and lock-vs-lockless behavior must be preserved per service during extraction.
**Corrections applied:** Merged `clearChainState` and `onProfileDeleted` variants under one root cause; smell broadened from "profile/chain purge" to "lifecycle purge cascade" so `auth-registry`'s account-deletion site stays in-family.
**Verification:** ADJUSTED (moderate) — codex reframed auth-registry as a different-trigger sibling and added two missed siblings; claude verified all 11 original sites plus the lock-discipline and intra-file divergence claims.

### Q17: Contract/function resolution and PXE registration abstractions are half-done
**Smell:** Duplicate Code from stopped extractions. **Bucket:** structural. **Blast radius:** wide across execution/token/fpc helpers. **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/execution/tx-request-builder.ts:113-125,279-334,404-424`; `authwit-discoverer.ts:149-225`; `helpers/batched-view-simulation.ts:177-192,494-591`; `execution/service.ts:1434-1446`; `token/service.ts:275-359,361-449,289-305,374-390`; `fpc/service.ts:245-261,347-357`.
**Evidence:** The `getContracts→registerContract` prologue appears at 8 sites; the name-lookup pattern (`functions.find ?? nonDispatchPublicFunctions.find`) at 4 sites; the selector-scan loop at 3 sites; `token/service.ts:275-359` vs `361-449` is a second near-clone pair — all despite `ContractResolver` already existing for instance/artifact resolution.
**Why it harms change:** Contract-resolution policy changes now require synchronized edits across execution builders and unrelated service modules.
**Smallest safe refactoring:** Extend the existing `ContractResolver` with small `ensureRegistered`, `findFunctionByName`, and `findFunctionBySelector` helpers — preserving current lookup order and per-caller error strings. Constraint: `contract-resolver.ts:14-21` freezes per-caller error strings, and `tx-request-builder` uses different strings ("Contract not found") than the resolver — consolidation must parameterize messages. The batched-view "(parity)" comments are internal pre-extraction parity, not upstream byte-parity — unaffected.
**Corrections applied:** Dropped speculative broader centralization claims; kept only the verified lookup/registration families.
**Verification:** confirmed (high) — both families verified every site; codex refined the refactoring to extending `ContractResolver` rather than a new module (refinement, not correction).

### Q18: Execution contracts rely on positional tuples and primitive clumps
**Smell:** Data Clumps plus Primitive Obsession — strongest on internal builder/strategy returns, weaker on the public RPC surface. **Bucket:** structural. **Blast radius:** wide across spec/client/service/fee helpers. **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/execution/tx-request-builder.ts:69-70,373,477`; `fee/fee-strategy.ts:72-81`; `service.ts:538-545,739-742,903,1173-1177,1411,1967-1971,2081`; additional tuple consumers added by verification: `service.ts:894-895,1801-1805,1849,1953-1956`; `fee/fee-juice-strategy.ts:20-34`; `fee/fee-juice-with-claim-strategy.ts:28-42`; `fee/embedded-strategy.ts:35-51`; `fee/fpc-strategy.ts:47-85`; `spec.ts:18-27,48-56`; `client.ts:22-43,53-63`; `operation-planner.ts:71-79`; `service.ts:154-162,405-414,620-629,717-725`.
**Evidence:** Execution helpers exchange 6/7/8-slot tuples (`StandardTxRequestResult`, `FeeEstimateResult`; positional indexing `built[0]..built[7]` at service.ts:538-545) and repeat the same 7-param transfer bundle across spec, client, service, and planner. Six same-typed adjacent slots make transposition plausible. The `fee-strategy.ts` "Matches the pre-split return verbatim" comment is internal doc parity, not wire parity.
**Why it harms change:** Adding or reordering one field becomes a manual audit across builders, strategies, and call sites.
**Smallest safe refactoring:** Step 1 (safe, small): replace *internal* tuples with named result objects — intra-extension only, both ends ship together. Step 2 (separate, broader API churn): introduce `TransferRequest` as a first-class parameter object across `spec.ts`/`client.ts`/service — not the smallest first step.
**Corrections applied:** Removed the overclaim that client/service arity drift would compile silently; the same-typed transposition hazard remains.
**Verification:** ADJUSTED (high) — codex added four missed tuple consumers and split the refactoring into internal-first/API-later; claude confirmed every cited line and the DO-NOT-FLAG compliance.

### Q19: Active-profile guards are duplicated with drifted error strings
**Smell:** Duplicate Code. **Bucket:** structural. **Blast radius:** wide (wider than consolidated — ~75 non-test `getActiveProfile()` calls across the wallet services). **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/contact/service.ts:48,58,73,87,120,152,188`; `network/service.ts:169,204,214,224,238,263,284,308,331,374,422,449,473,495`; `fpc/service.ts:115,217,238,293,319,389,414`; `dapp-session/service.ts:44,86,109`; `token/service.ts:468,526`; `transaction/service.ts:282`; `auth-registry/service.ts:265` (cited line numbers are off by 1-5 — they point at the `ensureInitialized()` preamble; the guard follows immediately). Added by verification: `execution/service.ts:521-522,768-769,1080-1082,1425-1427`; `execution/tx-request-builder.ts:98-100,391-392`; `dapp-interaction/service.ts:254-256`; `execution/helpers/get-view-simulation-deps.ts:32-35`; `account/service.ts:205-207`; `token-balance/service.ts:249-251`.
**Evidence:** The same `getActiveProfile()`+throw preamble is copied across dozens of methods. Lock-state drift counted: "Profile locked" ×34, "Wallet locked" ×12 (token/service.ts drifts within one file: 468 vs 526), "Wallet is locked" ×2 — plus a fourth variant, `operation-planner.ts:80-82` throwing `"Unauthorized"` (its file header documents the string as preserved/load-bearing), and a behavioral divergence: `dapp-session/service.ts:87-89` silently returns `undefined` instead of throwing.
**Why it harms change:** Lock-state policy, typed-error migration, or telemetry grouping must be changed method-by-method instead of in one guard.
**Smallest safe refactoring:** Extract `requireActiveProfile()`, either preserving existing strings per-site or unifying deliberately. It must NOT absorb the `"Unauthorized"` site (load-bearing string) and must preserve callers that intentionally return `undefined` (`dapp-session/service.ts:87-90`).
**Corrections applied:** Removed `account/service.ts:189`; the divergent-throw-string count is three (plus the load-bearing `"Unauthorized"` outlier and one silent-undefined behavioral variant).
**Verification:** ADJUSTED (high) — codex widened the blast radius with ten additional instance groups; claude independently counted the drift variants and flagged the two behavior-preservation constraints.

### Q16: Dead symbol-level surface persists in the shared packages
**Smell:** Dead Code (symbol-level) plus one unused dependency. **Bucket:** structural. **Blast radius:** medium (two shared packages' export surface — narrowed from "wide" by the convergence ruling; re-ranked from the S/W/Warm block accordingly). **Change frequency:** warm enough because these surfaces stay visible to every maintainer. **Cross-model:** disagreement
**Convergence ruling (coordinator-of-coordinators):** codex verifier B REFUTED the finding as written (it bundled live exports and build-registered entrypoints with dead items); Fable verifier B CONFIRMED at symbol level with full grep evidence. Resolution: ADJUSTED (moderate), narrowed to ONLY the symbol-level dead items both verifiers accept.
**Instances (retained — both verifiers accept):**
- `packages/extension-messaging/src/lazy-listener.ts:1-129` and `packages/extension-messaging/src/subscribe-with-snapshot.ts:1-88` — zero importers of either subpath; only colocated tests; production mentions are comments only.
- `packages/wallet-core/src/utils/random.ts:18` (`getRandomElement`) — zero consumers.
- `packages/wallet-core/src/utils/queue.ts:47-55` (`Queue.dequeueBatch`) — zero consumers.
- `packages/wallet-core/src/storage/entity_storage.ts:62-82,137-142` (`getVersion`/`setVersion`, `findByPredicate`) — no production callers (`findByPredicate` test-only).
- `packages/wallet-crypto/package.json:15` — `@aztec/stdlib` declared, never imported (only `@aztec/foundation` is).
- `packages/wallet-crypto/src/index.ts:19` — symbol-level only: `ENCRYPTION_GUARD`'s index export is dead (internal + test use only; the canary test imports relatively); `PasswordSecretBox` on the same line is live. Explicitly re-verified by the Fable verifier.
All three shared packages are `"private": true` (no external consumers), and none of the cited symbols live in the extension's auto-imported dirs, so auto-import cannot rescue them.
**Instances (DROPPED by the ruling):** `src/setup/*` + `vite.config.ts:134-136,298` (wired into vite build inputs — needs its own investigation before any removal claim); `wallet-core/src/utils/event-handler.ts` and the `jobs/index.ts` barrel (live — `jobs/index.ts:10` carries heavily-used `JobCancelledSentinel`/`assertCanTransition`); `base/index.ts:33-34` topology re-exports (unproven — only one verifier assessed them); every other item either verifier marked live or unproven.
**Why it harms change:** Dead surface still counts as API surface: it expands review, audit, and compatibility burden without delivering behavior.
**Smallest safe refactoring:** Small, independently-proven removals per symbol — not a bulk sweep. Constraints: removing `ENCRYPTION_GUARD` from the index must not touch the canary tripwire test (relative import); `IllegalTransitionError`'s index export is consumed by operation-journal's test via the package subpath; do not touch the setup entry or any live export.
**Corrections applied:** (consolidated round) removed the phantom `isTerminalStage` claim, excluded live `loadProductionNoteSchemas`, corrected the false `walletErrorFromPayload` deadness story. (verification round) narrowed per the ruling above.
**Verification:** ADJUSTED (moderate) — convergence ruling applied: codex REFUTED-as-written vs Fable CONFIRMED-symbol-level; retained only the dual-accepted dead symbols; finding re-ranked into the medium-blast-radius block.

### Q20: CAIP parsing/formatting is duplicated across bridge and extension
**Smell:** Duplicate Code. **Bucket:** structural. **Blast radius:** medium (2 modules, many downstream consumers). **Change frequency:** warm enough because both sides are protocol helpers. **Cross-model:** convergent
**Instances:** `packages/wallet-bridge/src/caip.ts:24-70`; `packages/extension/src/wallet/utils/caip.ts:22-29,49-87`; stale header at `extension/src/wallet/utils/caip.ts:5-7` (supporting evidence, not the core smell).
**Evidence:** `formatCaipChain`, `formatCaipAccount`, `parseCaipAccount`, and `resolveNetworkByChainId` are functionally line-for-line duplicates (only TSDoc wording and the type source differ; the extension adds `parseCaipChain`). The extension header lists "dispatcher" among its consumers, but `dispatcher.ts:54` imports the bridge copy. No drift-pin test exists (unlike the sanctioned `nulo-schema-patch` triple); the bridge's deliberate-mirror header documents intent but pins nothing.
**Why it harms change:** Any CAIP rule change must be patched twice across two halves of the same protocol boundary.
**Smallest safe refactoring:** Make the bridge (or a still-lower shared package) the single owner and re-export from the extension — ownership must move *downward*; the reverse would violate layering ("bridge must not depend on extension internals"; the extension already depends on `@nulo/wallet-bridge`). Preserve the extension's `parseCaipChain` behavior when unifying.
**Corrections applied:** Dropped the false "both headers point at each other" framing; the code duplication remains.
**Verification:** confirmed (high) — both families verified the duplication and the one-way stale header; codex added the downward-ownership layering constraint.

### Q21: Scope-enforcement checker family is hand-copied
**Smell:** Duplicate Code. **Bucket:** structural. **Blast radius:** medium (one file, public policy surface). **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/wallet-bridge/src/scope-enforcement.ts:53-88,90-130,286-319`; added by verification: `checkExecuteUtility` + `checkGetPrivateEvents` at `scope-enforcement.ts:132-168` (same family, not just their empty-grants lines); repeated empty-grants pass-through at `58,70,82,99,118,141,158,288,302,315` (cited "83" corrected to 82).
**Evidence:** `checkGetAddressBook` (300-306) and `checkRegisterSender` (313-319) have byte-identical bodies modulo error label; `checkGetAccounts` (286-292) is the same flag-checker shape; checkRegisterContract/checkGetContractMetadata/checkGetContractClassMetadata (53-88) differ only in capability type, flag, and label; checkTransactionCalls vs checkSimulationTransactions (90-130) are near-clones.
**Why it harms change:** Shared scope-policy conventions must be propagated across many near-clones, so new checkers are born by copy/paste.
**Smallest safe refactoring:** Local helper extraction that preserves exact error text and the current "no grants ⇒ pass through" semantics (pinned by `scope-enforcement.test.ts`) — a heavy checker-factory abstraction is not the smallest safe move for policy code.
**Corrections applied:** Kept only source-backed checker families; did not promote adjacent comment drift as a separate finding.
**Verification:** ADJUSTED (high) — codex added two family members and softened the factory refactoring to local helpers; claude verified the byte-identical pairs and the off-by-one.

### Q23: Claim/cancel lifecycle depends on cross-file temporal coupling
**Smell:** Temporal coupling. **Bucket:** structural. **Blast radius:** medium-wide (claim helper, execution service, operation-journal lock, execution mutex — widened by verification to include the journal and mutex collaborators). **Change frequency:** warm. **Cross-model:** convergent
**Instances:** `packages/extension/src/wallet/services/execution/claim-helper.ts:82-165` (especially the explicit no-await invariant at `144-163`); `packages/extension/src/wallet/services/execution/service.ts:836-866,1285-1346`; counterparts added by verification: `operation-journal/service.ts:52-60,216-260` (`_transitionLocked` — the explicitly referenced "OTHER side" whose pre-abort awaits make the interleaving work), `execution-mutex.ts:97-180`, and `service.ts:1915-1930,2062-2077`.
**Evidence:** Controller registration, journal transitions, FIFO baton release, and cancellation semantics rely on exact interleaving across files; `claim-helper.ts:144-163` literally states the code is "correctness-by-microtask-interleaving and is fragile against future refactors of either side." `acquireExecutionSlot` (1285-1346) encodes synchronous-FIFO-enqueue-before-baton-release ordering.
**Why it harms change:** Refactors that look local can silently break queue/cancel invariants because the order dependency is implicit and spans four files.
**Smallest safe refactoring:** A narrower abstraction around the controller/journal handshake, backed by the existing race tests — "one cancellation/claim coordinator" is directionally right but too broad for a smallest safe change. Any extraction must own `_transitionLocked` semantics too.
**Corrections applied:** Kept as a maintainability finding; it is not just a correctness note.
**Verification:** ADJUSTED (high) — codex widened the blast radius to the journal/mutex collaborators and narrowed the refactoring; claude independently identified `_transitionLocked` as the missed counterpart.

### Q22: BigInt/Error serialization logic is paralleled across two wallet-core helpers
**Smell:** Parallel serializer logic (narrowed from three-helper "policy sprawl"). **Bucket:** structural. **Blast radius:** narrow-medium (2 shared helpers feeding wire and job-journal paths — narrowed from 3 by verification; re-ranked to last). **Change frequency:** warm enough to matter because drift is silent. **Cross-model:** convergent
**Instances:** `packages/wallet-core/src/utils/serialization.ts:24-57` (bigint→`"123"`, Error→`{name,message,stack?,code?,details?}`); `packages/wallet-core/src/jobs/error.ts:60-77` (bigint→`"123n"`, Error→`{__error:true,...}`, truncation). (`packages/wallet-core/src/utils/arrays.ts:23-39` DROPPED — `safeStringify` is a lossy, sorted-key in-memory comparison-key canonicalizer with materially different semantics, not the same root cause.)
**Evidence:** The two helpers share overlapping BigInt/Error serialization logic with divergent encodings. Constraints discovered: `serialization.ts:1-4` is a documented copy of `@aztec/foundation/json-rpc` — an upstream-mirror whose wire shape must not change; `jobs/error.ts:1-24` documents its divergences as deliberate audit-hardening (never-throws envelope, quota cap), so the drift is partly intentional. The real consolidation surface is mainly the Error shaping between the two paths.
**Why it harms change:** Exotic-value serialization rules can improve in one path while the other silently stays stale.
**Smallest safe refactoring:** Share a low-level BigInt/Error replacer between the two helpers only where the documented constraints allow; do NOT force one serialization core across both behaviors (and `arrays.ts` stays untouched).
**Corrections applied:** (consolidated round) kept the cross-file policy split over the weaker `EntityStorage` duplication. (verification round) dropped `arrays.ts`; recorded the upstream-mirror and deliberate-divergence constraints.
**Verification:** ADJUSTED, both families (moderate) — both dropped/demoted `arrays.ts` and narrowed the consolidation surface; claude surfaced the `@aztec/foundation/json-rpc` upstream-mirror constraint; finding re-ranked to last on the narrowed blast radius.

## Verification summary

| Q | Title | Verdict | Final confidence | Cross-model |
|---|---|---|---|---|
| Q1 | Wallet method metadata in parallel registries | confirmed | high | convergent |
| Q2 | Popup/onboarding profile flows duplicated | adjusted | moderate | convergent |
| Q3 | Background/offscreen RPC transports forked | adjusted | moderate | convergent |
| Q4 | `ExecutionService` multi-responsibility hotspot | adjusted | high | convergent |
| Q5 | Four send pipelines share duplicated lifecycle tail | adjusted | high | convergent |
| Q6 | Activity-feed extraction half-done | adjusted | moderate | convergent |
| Q7 | Build/test config policy forked | confirmed | high | convergent |
| Q8 | Popup form abstractions half-finished | adjusted | moderate | convergent |
| Q9 | Service readiness split (deps vs `ensureInitialized`) | adjusted | high | convergent |
| Q10 | Composition-root wiring half-migrated | adjusted | high | disagreement → convergent (verified) |
| Q11 | `WalletSdkDispatcher` hotspot + duplicated session flow | adjusted | high | convergent |
| Q12 | E2E fixture harness duplication | adjusted | high | codex-only → dual-verified |
| Q13 | PXE subset interfaces manually synchronized | adjusted | high | convergent |
| Q14 | Restore-result loop duplicated across ten services | adjusted (both) | high | convergent |
| Q15 | Lifecycle purge cascade duplicated | adjusted | moderate | convergent |
| Q16 | Dead symbol-level surface in shared packages | adjusted (ruling) | moderate | disagreement |
| Q17 | Contract resolution/registration half-extracted | confirmed | high | convergent |
| Q18 | Positional tuples + primitive clumps in execution | adjusted | high | convergent |
| Q19 | Active-profile guards with drifted error strings | adjusted | high | convergent |
| Q20 | CAIP helpers duplicated bridge/extension | confirmed | high | convergent |
| Q21 | Scope-enforcement checkers hand-copied | adjusted | high | convergent |
| Q22 | BigInt/Error serializer logic paralleled | adjusted (both) | moderate | convergent |
| Q23 | Claim/cancel cross-file temporal coupling | adjusted | high | convergent |

Totals: 23/23 survive verification (4 confirmed clean, 19 adjusted, 0 refuted outright; Q16 narrowed under a cross-family convergence ruling).

## Refactoring constraints registry

Constraints flagged by verifiers that bound any fix derived from these findings:

1. **`nulo-schema-patch` tri-copy pin** (Q1, Q11) — the three identical schema-patch files are a documented house contract pinned by `packages/wallet-bridge/src/dispatcher.test.ts`; never consolidate them under a registry refactor.
2. **`formatSessionAccounts` wire-format parity pin** (Q11) — its comment pins wire-format parity via dispatcher unit tests; account-resolution extraction must preserve the exact projection shape.
3. **`fpc-strategy` two-pass mutation ban / byte-parity sensitivity** (Q18-adjacent; from consolidated drops) — `FpcStrategy` is materially different from the three single-pass strategies and explicitly byte-parity-sensitive; tuple-to-object refactors must not normalize it into the family.
4. **`serialization.ts` upstream mirror** (Q22) — documented copy of `@aztec/foundation/json-rpc`; its wire shape must not change under any shared-replacer extraction.
5. **`jobs/error.ts` deliberate divergence** (Q22) — never-throws envelope + quota cap are documented audit-hardening; not a consolidation target.
6. **`contract-resolver.ts:14-21` frozen per-caller error strings** (Q17) — `tx-request-builder` uses different strings ("Contract not found") than the resolver; consolidation must parameterize messages. The batched-view "(parity)" comments are internal pre-extraction parity, not upstream byte-parity.
7. **`operation-planner.ts:80-82` `"Unauthorized"` string is load-bearing** (Q19) — file header documents the string as preserved; `requireActiveProfile()` must not absorb this site.
8. **`dapp-session/service.ts:87-90` silent-`undefined` behavior** (Q19) — intentionally returns `undefined` instead of throwing; guard extraction must preserve it.
9. **Scope-checker exact error text + empty-grants pass-through semantics** (Q21) — pinned by `scope-enforcement.test.ts`; helper extraction must be byte-faithful on error labels and the "no grants ⇒ pass" rule.
10. **Purge-helper extraction must preserve per-service emit order and lock-vs-lockless discipline** (Q15) — account/token/transaction purge locklessly; contact/fpc/dapp-session/auth-registry/network take `this.lock`.
11. **Restore hooks stay service-specific** (Q14) — id reallocation, schema validation, and dedup vary per service; only the per-item result/error accumulation is shared.
12. **`ENCRYPTION_GUARD` canary tripwire test** (Q16) — the tripwire test imports it relatively; removing the index export must not touch the test. `IllegalTransitionError`'s index export is consumed by operation-journal's test via the package subpath.
13. **Offscreen error-contract rejection type** (Q3) — unifying error contracts changes offscreen rejections from plain strings to typed `WalletError`; a deliberate behavior-alignment decision, not an incidental refactor side effect.
14. **PXE mapped-type derivation limits** (Q13) — derivation must drop the `network` param and promisify; `client.ts` per-method zod validation cannot be generated (partial mechanization only).
15. **CAIP ownership must move downward** (Q20) — bridge (or lower) owns the helpers, extension re-exports; the reverse violates layering. Preserve the extension's `parseCaipChain`.
16. **Noir-alias host-specific comment travels with the shared config helper** (Q7).
17. **`AUDIT F-00x` markers survive descriptor-registry refactors** (Q1) — security-relevant decision markers become descriptor fields, never dropped.
18. **`NewContactPopup` Enter-listener double-fire hazard** (Q8) — its comment documents the hazard any `FormPopup`-level submit-key fix must handle.
19. **Claim/cancel no-await invariant** (Q23) — `claim-helper.ts:144-147` no-await invariant and `_transitionLocked` pre-abort awaits are the ordering contract; any handshake extraction must own both sides and stay pinned by the existing race tests.
