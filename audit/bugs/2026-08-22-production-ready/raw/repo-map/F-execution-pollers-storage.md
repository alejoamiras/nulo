# Map F — Execution + pollers + storage services

> Mapper (explore agent), 2026-08-22. Line numbers verified against source read this session.

## 1. Execution pipeline (`apps/extension/src/wallet/services/execution/**`)

### 1.1 Service RPC surface (`service.ts`)
rpcMethods `:83-92`: executeTransfer, executeOperations, getGasBalances, peekGasBalances, estimateTransferFee, estimateOperationFee, cancelJob, cancelEstimate.

- **Batch dispatcher** executeOperations (`service.ts:483-618`): sequential loop; non-ok result makes all later ops `{status:"skipped"}` (`:498-500`); per-op TaskService subtask; switch over 17 op kinds (`:511-603`) — register_contract/sender/token (previewed-interface sanity check `:681-698`), send_transaction (hooks forwarded for per-origin slot bucketing, `:524-530`), view/simulate via ViewExecutor, aztec_sendTx (fence captured here before prove, D13, `:585`), aztec_createAuthWit (selector↔name binding enforced vs ABI truth `:818-858`; chain identity from live node `:807-815`).
- **Estimate admission** withEstimateAdmission (`service.ts:427-453`): tokened calls admitted via EstimateCancelRegistry (caps 4 active / 8 parked per profile, TTL sweep 15 min, settled-stash 120 s — `estimate-cancel-registry.ts:34-47`); admission counts until settle() not cancel (non-preemptible sims keep slots); sentinel → structured JobCancelledError.
- **Fee-strategy dispatch** buildAndEstimateTxRequest (`service.ts:882-915`): clones op + actions (fjwc/fpc mutate arrays — `:894-898`); strategy map built by `fee/build-fee-strategies.ts:18-25`: fj→FeeJuiceStrategy, fjwc→FeeJuiceWithClaimStrategy, fpc→FpcStrategy, embedded→EmbeddedStrategy; literal keys only.
- Cache-invalidation wiring in init: tx settle → gas-balance invalidateAccount (`:337-341`); profile switch → evictAll (`:343-348`); PrivateFPC mutation → invalidateAll (`:350-360`). GasBalanceReader TTL 5 min.

### 1.2 ExecutionLane / ExecutionMutex slot semantics
**ExecutionMutex** (`execution-mutex.ts`): bespoke promise-tail FIFO keyed `${profileId}:${chainId}` to match PXE's chainGuard scope. NO timeout, no force-release by design — wallet-core Lock force-releases at 5 min which would let T2 build while T1 still proves (`:5-10`); wedged holder wedges lane until SW restart (`execution-lane.ts:14-16`).
- acquire(key, signal?, caps?) (`:97-164`): pre-aborted fast reject; caps checked+incremented atomically BEFORE enqueue; synchronous tail install; abort splices waiter without breaking successor FIFO by chaining mine onto the real prior (`:147-161`); depth counters decrement ONLY in release (sole decrement path) so an aborted waiter over-counts until prior settles — cap cannot be bypassed.

**ExecutionLane**:
- acquireSlot(networkId, queuedJournalId?, onEnqueued?, originKey?) (`:223-285`): caps per-origin depth 8 / total-lane depth 32 (`:85-86`); absent origin bucketed under "__no_origin__" sentinel rather than bypassing (`:233-237`). Pre-acquire AbortController registered under queued id BEFORE the acquire's first await (`:240-244`). Baton release point: onEnqueued fires immediately after acquire() is CALLED (synchronous enqueue), before awaiting grant (`:246-260`). Zero-slot exceptions: ExecutionMutexCapacityError → journal terminalized failed HERE then throws TooManyPendingError -32005 (`:265-274`); ExecutionMutexAbortError → JobCancelledSentinel → 4001 (`:275-277`). Wait heartbeat: waiters set + 30 s interval bumps updatedAt so reaper doesn't false-declare a legitimately-waiting record stuck (`:76-83`, `:287-314`).
- claimOrCreateJournal (`:325-360`) → claim-helper.ts decision tree (`claim-helper.ts:81-242`): no queued id → create fresh + register controller; record missing (reaped) → create fresh, delete orphan controller; scope mismatch → in-place refile under allowed stages [queued,pending] honoring cancels; stage ∉ {queued,pending} → JobCancelledSentinel; queued → transition pending under journal lock, re-read on failure to disambiguate cancel-race from storage error; pending (silent-path pre-fast-forwarded) → skip transition, register controller only. Controller registered with NO await between stage write and set() (`:211-241`).
- cancelJob(jobId) (`:148-192`): profile-only ownership gate with existence non-disclosure; journal-transition FIRST, abort SECOND; FSM rejection drops the signal silently and does NOT abort (guards cancel-after-submit leaving "cancelled" journal over real on-chain tx) (`:164-185`).
- beginJournal (`:104-135`): creates dapp_execute rows carrying the AUTHORIZATION-time fence; never re-reads active profile after FIFO wait.

### 1.3 Executors
Shared tail ExecutionCoordinator.proveAndSend (`execution-coordinator.ts:165-183`), frozen:
```
checkCancelled → journal(proving) → proveTxTask [proofGate.wait inside :128] → checkCancelled
→ wantOffchainOutput?(provedTx) → toTx → journal(submitting, txHash) → checkCancelled
→ sendTxTask → recordTransaction(txHash) → journal(succeeded, txHash)
```
Cancel between prove and send drops the proof artifact silently (`:160-163`). Failure transitions stay per-caller (divergent by design `:67-70`).

**(a) TransferExecutor.execute** (`transfer-executor.ts:80-255`) — popup transfer. Takes NO execution slot — preserved quirk do-not-harmonize (`:14-18`); only controller registry. Sequence: task start → journal transfer create w/ amountRaw/recipient/transferType (`:91-115`) → controller registration (`:130-134`) → estimate-reuse tryConsume (`:140`) → mark simulating BEFORE build (uniform even on reuse path, `:157-163`) → build planner+strategies or hydrate live handles from snapshot (`:165-192`) → proveAndSend activity closure kept transfer-only (`:194-241`) → catch: maybeRethrowAsRpcCancel else mark failed + task.fail + rethrow (`:245-251`) → finally deletes controller.

**(b) DappSendExecutor.runInSlot** (`dapp-send-executor.ts:175-237`) — shared scaffold both slot-taking dApp paths. Frozen ordering: acquireSlot BEFORE claim/PXE work (`:200-205`); journalId hoisted OUTSIDE try so catch (mark-failed) + finally (controller cleanup + releaseSlot()) run even on claim/cancel-check throw — otherwise lane wedges until SW restart (`:16-23`, `:207-236`). Catch → markFailedUnlessCancelled (`mark-failed-unless-cancelled.ts:27-36`: sentinel rethrown synchronously preserving microtask timing of slot release).

- **executeSendTransaction** (`:373-454`): inside slot → mark simulating (`:414`) → probe-free validated build (`:417-418`) → proveAndSend whose one recordTransaction closure writes BOTH activity row AND recordPendingAuthwits (grants land pending, reconciled by tx outcome; recording-at-send keeps estimate/reject from leaking a grant) (`:428-449`). Callers: dispatcher grantPublicAuthwit + auth-registry revokeAuthwits/setRegistryEnabled.
- **executeAztecSendTx** (`:456-627`): default_entrypoint routes to NO_FROM (`:469-471`); feeSettings required (`:479-481`); inside slot: opts.from guard + payload parse FIRST, simulating marked AFTER so those failures stay pending→failed (`:503-515`); reuse consume validates fingerprint/pre-discovery normalization + profile + endpoint + pending-set + chain identity + FPC identity + base fee (`:521-531`); arms: reuse (live handles re-resolved never cached — cross-profile fail-closed `:543-557`) / embedded (skips discovery `:558-564`) / discovery-aware estimate (`:565-580`); offchain output extracted between prove and toTx (`:591-594`); NO_WAIT returns hash else awaits receipt (`:620-624`).
- **executeNoFromSendTx** (`:634-791`): embedded-payment enforced (`:644-646`); no nonce (history records Fr.ZERO, payment EXTERNAL, `:773-774`) so mutex is its ONLY interleaving protection (`:648-653`); mark simulating (`:670`) → buildNoFrom (`:672`) → inline feeOpts + embedded-FPC cap (`:682-693`) → kernelless discovery sim with stubbed account (`:712-742`, authwits derived from CallAuthorizationRequest offchain effects, live-node chain info) → real simulation + finalizeGasLimits (`:746-752`) → proveAndSend.

Slot-holding summary: slot spans authwit-discovery/build/simulate/prove/send for both dApp paths; estimate-only calls do NOT take it (admitted through EstimateCancelRegistry); transfer flow takes none.

### 1.4 Deletion fencing interplay
- Capture: captureFence() → ProfileService.captureExecutionFence() — atomic active-read + reserved-check + epoch-capture under facade lock (`service.ts:363-370`).
- Threading: fence → runInSlot.params.fence → claim/journal-create AND recordTransaction → addTransaction(..., fence, networkId) where tx service asserts deletionState.assertCurrent(profileId, epoch) + owner-row existence under tx lock (`transaction/service.ts:180-184`).
- Journal-side refusal: _createOperationLocked checks membership + epoch currency + network liveness (`operation-journal/service.ts:227-253`).

## 2. Operation journal

Record schema (`spec.ts:62-127`): id random 16-byte hex, kind transfer|dapp_execute|token_import, origin popup|dapp|seed, progress FSM stage, error iff failed, terminalAt, attempts, scope fields, UI fields, amountRaw/recipientAddress/contractAddress/transferType. initialStage restricted {queued}|{pending}; queued requires kind=dapp_execute ∧ origin=dapp ∧ sessionId. Root nulo:journal.

FSM — 8-stage transition table (`packages/wallet-core/src/jobs/fsm.ts:38-51`):
| from ↓ to → | pending | simulating | proving | submitting | succeeded | failed | cancelled |
|---|---|---|---|---|---|---|---|
| queued | ✔ | – | – | – | – | ✔ | ✔ |
| pending | – | ✔ | – | – | – | ✔ | ✔ |
| simulating | – | – | ✔ | – | ✔* | ✔ | ✔ |
| proving | – | – | – | ✔ | – | ✔ | ✔ |
| submitting | – | – | – | – | ✔ | ✔ | – |

\* shortcut legal only kind=token_import. Enforced `_transitionLocked` (`operation-journal/service.ts:309-381`): FSM legality; error iff failed; kind↔txHash invariants (`:334-350`) — transfer/dapp_execute must succeed WITH txHash and must NOT use simulating shortcut; token_import must succeed WITHOUT one; submitting.txHash === succeeded.txHash drift pin (`:361-367`).
Concurrency: global transitionLock serializes ALL load→write paths; only getOperation lock-free. touchOperation skips terminal records, emits nothing. refileOperationScope moves scope in place under allowed-stage guard (`:517-540`).

Reaper (`reaper.ts`): boot sweep start() runs reap({unconditional:true, bootCutoff}) (`:127-154`); bootCutoff captured BEFORE services.start() in composition root (`runtime.ts:298-304`); records createdAt >= bootCutoff spared (`>=` errs toward sparing — `:186-192`); others → failed: sw_restart_post_prove (proving) / stuck_queued (queued) / stale_on_resume (others) (`:202-213`). Periodic tick alarm 1 min honoring grace windows queued 10 min / pending 2 min / simulating 10 min / proving 35 min / submitting 5 min (`:72-82`).

GC (`gc.ts`): hourly alarm; per-(profileId, accountAddress|null) terminal cap 50; ONLY succeeded evictable — failed/cancelled exist nowhere else (`:126-133`); oldest-by-terminalAt evicted.

Lifecycle across executors: queued creation at wallet-sdk message arrival tryCreateQueuedJournal (`wallet-sdk/queued-journal.ts:102-207`) — pre-auth gates (active profile, dapp session, transaction capability grant `:121-124`, sender resolved by dispatcher-identical rule incl. visible-accounts-only `:131-147`), epoch at arrival, atomic caps per-session 8 / global 32 under module-level queuedCreationLock (`:34-41`, `:156-173`). Failed-before-claim paths: capacity reject marks failed directly (`execution-lane.ts:265-274`); user cancel → cancelled; abandonment → reaper stuck_queued after 10 min.

## 3. Pollers / updaters

### Token balance BalanceJobQueue
Dedup layers: Queue.priorityPass keyed by balance id (`balance-job-queue.ts:59`) + pendingTasks Map gating fresh task mint (enqueue `:120-126`). Ticker 1000 ms; tick drains until empty, batches ≤12 grouped by first-queued account (`:142-160`); a throw ends the tick.
syncBatch (`:186-293`): defensive task start (stale pointer → mint fresh instead of throwing outside try — that escape would strand pendingTasks forever, `:195-210`); projector.project groups (account,chainId), chunks 12, public-first two-pass (`balance-projector.ts:55-95`); per result: error → failTask + writeSyncFailure onto LIVE re-read row (`:165-184`); success → existence re-check (orphan detection `:232-237`), SYNCHRONOUS fence check isBalanceInvalidated with NO await before repo.set (`:248-253`), ownership gate isRowEmittable (`:256-259`), post-write emit re-check (`:264-265`); batch catch fails every member (`:267-282`); finally clears pointers identity-checked (`:283-292`).
Service fences: invalidatedBalanceIds added BEFORE awaited delete, checked synchronously before every queue write; fenced ids NEVER reallocated within worker lifetime (allocateUnfencedId, service.ts:202-211); profileGeneration bumped per switch, re-checked after EVERY await incl. A→B→A same-id switches (:61-65, :253-313); queue.reset() cancels owned tasks then unconditionally clears queue+pointers in finally (balance-job-queue.ts:97-116). Causal refresh requestBalanceRefresh returns {taskId}|{busy}|{missing} — busy never false-acks; missing positive. Purge purgeForTokens + raw malformed second pass (:326-345). Backup export scoped by AUTHORITATIVE token ids not in-memory map (:393-402).

### Incoming transfers
Scheduling: singleflight setInterval per (networkId, accountAddress) note arm (`:139-145`, default 30 s `:47-50`) and per (networkId, contract) public arm serving every account (`:147-155`); reentrancy Sets polling/publicPolling.
Hydration: hydrateSchedulers() (`:705-773`) builds desired descriptor set OFF-MAP, bails if serviceEpoch moved since entry, commits atomically (tear down ALL + install, no awaits in commit); bumps serviceEpoch at ENTRY covering every caller (init/profile-change/account-add/clearProfile/clearChain/onTokenDeleted). Each interval captures bornAtEpoch; ticks bail if epoch moved (`:783-789`, `:807-816`); initial poll kicked immediately.
Epoch usage sites: scanContract captures epoch before PXE discovery; per-note critical sections bail on mismatch (`:994-1047`); getReceiptFee pins fee-cache write to entry-epoch (`:443-470`); emitSyncStateIfChanged epoch-gates (`:486-512`); clearProfile/clearChain bump FIRST, evict caches, re-evict after hydration in finally (`:634-688`); onAccountAdded resets public cursors + class-gate cache inside lock with own bump BEFORE reset (`:302-335`); onTokenDeleted bumps before teardown (`:887-892`); onAccountDeleted targeted teardown uses account.profileId NOT active profile (`:337-387`).
Note scan flow: unlocked getNotesRaw (`:1002-1010`), E2E park-gate (`:1016-1023`), per-note locked commit with live re-reads of tokens/outgoing/inflight/trust (`:1039-1125`); outbox row written BEFORE the record (`:1115-1118`).
Public arm: cursor-page scan via PublicEventIndexer; class gate cached by finalized tip + checkpoint HASH (same-height reorg-safe, unresolved never cached, `:1218-1239`).
Outbox drain (D4/L17): drainBalanceOutbox on init + every tick of both arms (`:293-295`, `:839`, `:988`); anchored-task causal semantics (`:1861-1914`): success→delete, failure/missing→clear anchor, transient throw→KEEP row, busy→keep unanchored.
Trust machine unknown→pending→trusted/blocked; flips refuse when contract deregistered, fail closed (`:579-632`); visibility toggle fails CLOSED everywhere (`:1132-1144`); dust filter fails OPEN (`:528-565`); getIncomingTransferById active-profile-scoped (`:422-429`); profile deletion handled by coordinator calling clearProfile directly awaited NOT a subscriber (`:259-265`).

### Price service
Alarm nulo:price:refresh 3-min (`:24-27`); created on unlock+enabled, cleared on lock/disable; boot reconcile init (`:116-133`); dispatch stays external via module-scope shim; refreshes run ONLY while unlocked (privacy posture).
One batched CoinGecko fetch of full static id set; validated at WRITE (parseResponse/isValidQuote `:383-404`, `:282-297` — future-dated entries INVALID) AND READ (readUsable `:261-280`); per-id sanity bands from price-map.ts.
Generation counter bumped SYNCHRONOUSLY on kill-switch flip (`onConfigUpdated :213-249`); callers capture entry-generation BEFORE awaits; doRefresh re-checks gen at four points incl. post-cache-write; dead generation resets neither backoff nor watermark nor emit. Kill-switch disable neutralizes in-flight refresh synchronously (abort + inflight=undefined) so following enable can't adopt stale run (`:222-228`); config flips chained via configTransition.
Backoff 30 s ×2^n capped at quote TTL, jitter ±25%; lastCompletedFetchAt watermark stops refetch-forever for permanently delisted ids; monotonic merge never overwrites newer stored quotes; local 10 s timeout AbortController retract-if-still-published. Executors read getUsableQuote cache-or-nothing — never fetch at tx time.

### Token seeder
Single-flight run() + coalescing rerun flag (`:103-121`). Attempt cap SEED_ATTEMPT_CAP = 3, reset once per extension version; attempts recorded BEFORE risky work so SW death still counts toward cap (`:238-243`).
Tombstones: user deletion → outcome:"deleted" permanent, survives chain purges (`:123-130`, purge skips deleted `:169-171`); marker blob treated as hostile (`readMarkerState :335-350`); all mutations serialized behind one promise chain (withMarkerLock `:132-161`); tombstone never clobbered by late seed pass (updateMarker guard `:157`).
Snapshot validation: TOFU class-id pin enforced INSIDE single-pass preview before any PXE registration (PinMismatchError hard skip `:297-313`); metadata bounds name≤80/symbol≤32/decimals≤18 + exact expectedSymbol; COMMIT indivisible inside ONE marker-lock critical section (tombstone recheck + persist + seeded-marker; purges bump epoch BEFORE queueing on same lock — no resurrection window either way) (`:248-278`); lifecycle guard re-checked around every await incl. double epoch check (`guardsHold :198-208`).

## 4. Storage layer

EntityStorage (`packages/wallet-core/src/storage/entity_storage.ts`): keys `${root}@${id}` in injected MinimalStorageArea; optional injected codec. Per-row resilience: BOTH JSON-syntax failure AND codec-validation failure KEEP the row and return undefined — read path never deletes (`decodeRow :62-92`; rationale: old fire-and-forget remove raced concurrent valid writes; storage has no atomic compare-and-delete). Maintenance surfaces: rawEntries (true storage-key suffix, codec-free, `:158-171`), rawStringEntries/rawValue (exact bytes for guarded deletes, `:179-198`).
ValueStorage: single key; THROWS on malformed value, never deletes (`:14-21`).

Migrator wiring (`runtime.ts`): boot order uninstall URL → Migrator.run FIRST (before config.load() — config-reshaping migration must not be shadowed) → blocked statuses persisted (needs-recovery/breaking-failure ⇒ retrySafe=false fail closed, services never start on un-migrated data `:156-186`) → Promise.all([config.load(), BarretenbergSync.initSingleton]) (BB failure vetoes retry memo since initSingleton memoizes its rejection) → service registration → journalBootCutoff = Date.now() BEFORE services.start() → start → fire-and-forget resumePending (same cutoff), reaper.start, GC.start, session-storage probe, liveness heartbeat. Single-flight start memo: retrySafe vetoed on migration-block / BB failure / registration zone (`:107-123`).

Purge/repair raw second pass (`services/purge-rows.ts`): purgeRows centralizes delete-before-emit order; deliberately NOT best-effort. purgeMalformedRows (F-B23, `:58-84`): raw string enumeration (rows invisible to codec), predicate on parsed object + TRUE storage id, guarded delete re-reads rawValue and refuses unless bytes equal snapshot — shrinks (not eliminates) window; exclusion comes from key-attribution or store's lock. canonicalNumericStorageId rejects alias suffixes ("01"/"1e3") collapsing onto another valid row's id. Users: journal purgeForProfile, token-balance purgeForTokens, transaction purgeForAccounts same-lock second pass, network purgeForProfile.

Backup export/import slice registry (`services/backup/backup-migration-registry.ts`): pinned serviceName → SliceDescriptor map (root w/ value-derived idOf anchors / lossy value-projection (config, null-proto target blocks __proto__ injection) / non-storage (PXE account-state) / block-listed (profiles, auth-registry-enabled)); normalize/denormalize never renumber ids; denormalize re-derives each row's id from VALUE and rejects mismatch (`:351-356`); duplicate row ids rejected; unknown slice name rejected; compile-time anchor pins.
Backup migrator (`backup-migrator.ts`): version guards → normalize → fail-closed preflight (only branded backup-safe migrations; block-listed roots block import; every footprint ref registry-covered; read of absent NON-optional slice rejects) → real Migrator over seeded MemoryStorageArea scratch → denormalize. Same numbered migrations as live boot.
Trust-gate ORDER (`composables/useFullBackupImport.ts:116-181`): checksum over ORIGINAL body first (checksum stripped before hashing) → compat-epoch (non-migratable hard reject; CURRENT_COMPAT_EPOCH=4; epoch-4 password blobs REQUIRE entropy + imported-keys DEK carrier; passkey blobs must NOT carry entropy) → backup-schema-version range → migrate pure in-memory → restore. Checksum integrity-detection only never authentication.
Restore pipeline: account slice first + provenance filter of every account-owned slice to THIS restore's allow-set (`restoreAccountsAndFilterOwnedSlices :196-256`); index-paired network/token re-link (field-match rejected as attacker-ambiguous) + chain-equality cross-check (`:701-743`, relinkRestoredTokenBalances :266-309); unconditional profileId remap; bounded rollback of orphan profiles w/ liveness gate on disconnect-classified failures; account-state restored AFTER finalizeRestore (needs open-session PXE store key) on bounded chain-sync budget (`:894-910`).

## 5. Transaction + account + network services

TransactionService: TxStatus ladder Pending → Proposed → Checkpointed → Proven → Finalized, plus Dropped (`getTxStatus :458-475`). Worker polls due txs each tick (`runWorker :321-340`), re-armed Pending rows only on init.
addTransaction (`:153-218`): under tx lock — fence assert + owner existence (D13), dup-hash reject, submittedEndpointUrl resolved by EXECUTOR never re-derived from active-profile state (auto-lock/profile-switch during prove would mis-route receipt polls cross-profile, `:189-196`).
Receipt poll updateTx (`:361-456`): node fetch UNLOCKED pinned to submitting endpoint (anti cross-profile leak); DROPPED debounce (60 s grace + 3 consecutive streaks, observations during grace count); guarded re-check + persist UNDER lock with OBJECT-IDENTITY fence (pending.get(hash) !== tx && droppedWatch.get(hash) !== tx → bail) — hash-membership would break ABA re-creation after purge; dropped resurrection watch (30 min window, 15 s cadence, never re-armed from storage — backup-controlled URLs must not be dialed).
Purges: purgeForAccounts sole-owner semantics for unscoped rows; shared-address rows from other profiles permanently marked ambiguous instead of deleted; raw malformed second pass in same lock hold. Restore: never restores Pending (D16), create-only against hash collisions, schema-parse before write.

AccountService: createAccountInternal (`:199-232`) — auth gate first; next index over SAME-TYPE rows only; derivation input VERIFIED exact L1 id (custom nets probed live fail-closed); deriveAccountSeed(master, l1ChainId, type, index) → address from NuloAccount.new(secret).address; per-(profileId,chainId,type) tuple locks prevent duplicate-index race creation; ensureDefaultAccount idempotent lowest-index provisioning excluding Imported.
Signing materialization getAccountContract (`:279-300`): re-derive from ROW-CARRIED l1ChainId; constructed address ≠ stored → raiseRuntimeMismatch (AccountAddressInconsistency path); Imported accounts load via DEK-unsealed signing key with zeroization single-account blast radius (`loadImportedAccountContract :312-341`).
First-tx init wrapping (`packages/aztec-runtime/src/account/nulo-account.ts`): buildTxExecutionRequest (`:137-178`) computes siloed private initialization nullifier + membership check; absent witness → buildWithInitialization wraps [ctorCall, ...userPayload] via MulticallEntrypoint (`:211-237`); constructor call consumes SAME frozen instantiation descriptor derivation used — cannot split-brain; requiresInitialization exposed; payload chunking above APP_MAX_CALLS recursive wrap; PXE-local registration guards avoid node cascades.
Cleanup: clearChainState cascade w/ imported-key deletion (`:134-146`); orphan imported-key sweep at init fire-and-forget (`:114-127`); restore serialized by restoreLock.

NetworkService: addNetwork (`:370-389`) probe outside lock → under lock per-profile duplicate-chain + name checks → build+persist+emit. deleteNetwork (`:407-433`) refuses ACTIVE network; reserves id in deletingNetworks BEFORE cascade so isNetworkLive refuses creators for whole window (row deliberately outlives cascade); purgeChain awaited THEN row deleted. purgeChain (`:730-754`): awaited subscriber pipeline in registration order (Transaction → Token → Fpc → Account → OperationJournal) then ALWAYS-last offscreen PXE clear; FAIL-FAST aggregate propagate so deletion coordinator keeps tombstone + retries rather than false "deleted". purgeForProfile clears node caches, purges each chain, deletes rows, raw malformed second pass, removes active-network key. Endpoint mutations enforce EXACT L1 equality with unlocked probe + locked re-read (`:467-536`); restore enforces shape + URL allowlist + (profileId,chainId) collision rejection + id re-roll avoiding source-id aliases.

Authwit revoke (auth-registry/service.ts): revokeAuthwits (`:178-237`) ownership check per id (foreign id treated as nonexistent — no existence oracle) → executeSendTransaction with set_authorized(hash,false) actions → waitForTx → waitForTxProven — polls PROVEN tip past receipt block because sequencer executes consumers against proven state not latest; timeout THROWS (never report unverifiable security mutation as success) (`:300-331`) → syncAuthwits reconcile. Pending authwit rows reconcile by tx outcome mined/dropped. setRegistryEnabled mirrors pattern.

## 6. Hazard candidates noticed while mapping (file:line only)

- transaction/service.ts:221-227 — waitForTx unbounded busy-poll loop (no timeout/cap); awaited by auth-registry/service.ts:217 and :266, so perpetually-Pending tx hangs those flows indefinitely.
- transaction/service.ts:321-340 — runWorker infinite while(true), no cancellation hook; skips polling entirely whenever no active profile exists (pending txs stall while locked).
- price/service.ts:206 — refresh(gen).catch(() => {}) on profile-change: failure swallowed with NO log.
- incoming-transfer/service.ts:784-789, :810-813 — scheduler ticks retry every interval, no backoff/cap on persistent PXE failure.
- balance-job-queue.ts:142-160 — tick retries whole queue every 1 s, no backoff; persistent projector failure re-attempted continuously per enqueue trigger.
- incoming-transfer/service.ts:634-663 / :665-688 — classGateCache NOT swept in clearProfile/clearChain (only feeCache + syncState are); stale entries for purged profiles linger until worker restart (mitigated by finalizedTip+checkpointHash keys, but a cache-invalidation gap).
- network/service.ts:370-389, :467-505, :507-539 — chain-identity probe OUTSIDE lock trusted without re-probe at commit; chainId/l1ChainId could drift between probe and write (documented for deletion race only).
- transfer-executor.ts:130-134 — AbortController only created when journal creation succeeded; if createJournalOperation fails, transfer runs uncancellable (journal-less, invisible to cancelJob).
- dapp-send-executor.ts:737-741 — NO_FROM discovery loop pushes authwits into live txRequest with silent per-effect catch {}; systematically malformed effect stream degrades silently.
- runtime.ts:315-317/:325-326/:331-332 — resumePending/reaper.start/GC.start fire-and-forget; failures logged but not journaled anywhere durable.
- account/service.ts:114-116 — orphan imported-key sweep fire-and-forget catch-log only.
- operation-journal/service.ts:479-490 — countOperations materializes ALL records to count (O(n) per queued arrival under queuedCreationLock).
- incoming-transfer/service.ts:1871-1877 — outbox drain splits composite keys manually and continues on malformed parts; malformed row silently never drains (no repair/report path).
