NOTE FOR CODEX: This is a code-correctness review of AUTHORIZATION and SESSION-BINDING logic in our own browser-extension wallet, commissioned by the code owner before release. Goal: find places where the authorization rules the code intends to enforce are not actually enforced, so the owner can fix them. You run in a read-only sandbox; output your full report as your response text. Do NOT read anything under node_modules; stay within packages/ and apps/extension/src. Use neutral engineering language throughout your report (e.g. "unauthorized outcome", "control gap", "misuse path"). Read first: audit/security/2026-09-13-high-prerelease/raw/CONTEXT.md and audit/security/2026-09-13-high-prerelease/raw/SECURITY-PROMPT-codex.md (output format + what not to flag), then the repo maps raw/repo-map/wallet-bridge.md and raw/repo-map/extension-services-dapp-exec.md.

# Cluster c06-dapp-bridge-dispatch — dApp RPC boundary: content script → wallet-sdk background → dispatcher, sessions, capabilities, scopes

Source files (read all, in full): packages/wallet-bridge/src/*.ts (non-test); packages/wallet-sdk-schema-patch/src/*.ts; apps/extension/src/content-script/content.ts; apps/extension/src/wallet/services/wallet-sdk/*.ts (non-test); apps/extension/src/wallet/services/dapp-session/*.ts. Tests as intent evidence: packages/wallet-bridge/src/dispatcher.test.ts (widening + scope sections), scope-enforcement.test.ts, session-established.test.ts.

Questions (answer each with evidence; finding or explicit non-finding):
1. Origin attribution relies on the top-frame tab URL plus subframe rejection. Cases: sender.tab undefined, about:blank/data: top frames, top-frame navigation mid-session with the same tab id, sandboxed iframes. Can a request from origin A be handled under a session that belongs to origin B?
2. Session establishment + verify window: the pending marker keyed by request id, freshness window, profile equality, URL-carried verificationHash. Can a prior approval be reused; can two concurrent handshakes for one (origin, chainId) cross; is the trusted/untrusted decision bound to the right session after a service-worker restart?
3. requestCapabilities + accounts widening (#582): can a dApp learn which accounts are held, escalate flags, or obtain an account the user declined; check accountsAdditions echo-binding, requiresGrant under the lock, and the membership-only shortcut that answers without a popup.
4. Scope enforcement per METHOD_REGISTRY row: does each checker inspect every dApp-controlled target field (contract address, function name AND selector, account scopes, additionalScopes, capsules, authwit contents)? Empty-name rule, wildcard rules, encoded_call vs call parity, batch leg re-checks. In particular compare how the simulateTx fast path (apps/extension/src/wallet/services/execution/fast-path.ts) binds name↔selector versus sendTx/executeUtility.
5. createAuthWit / grantPublicAuthwit: is any path left that signs an opaque hash; is the silent route bounded by tx/simulation scope; caller/contract outside scope; chain binding of the authwit message.
6. DappSession row MAC: key derivation per profile (does the key derivation include the profile id? see ProfileService.deriveDappSessionMacKey), canonicalisation completeness over all authority-bearing fields, verify-or-drop semantics.
7. Fail-closed audit: every place the dispatcher/handlers return an empty grant list, an undefined session, or catch-and-continue.
8. Schema patch methods and reachability of any method not in the registry.
9. Discovery flood + locked queue caps and per-origin coalescing (subdomain variants).
10. Response epoch / profile-switch suppression correctness.
