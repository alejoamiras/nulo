NOTE FOR CODEX: This is a DEFENSIVE hardening review of our own code, commissioned by the code owner, so we can fix weaknesses before release. Report weaknesses with concrete traces and recommended fixes. You run in a read-only sandbox: do NOT try to write files. Output your FULL report as your response text (it will be saved as raw/c09-pxe-node-network-codex.md by the orchestrator). Ignore the 'Write your report to …-claude.md' line at the end. Where the instructions say to read SECURITY-PROMPT.md, read audit/security/2026-09-13-high-prerelease/raw/SECURITY-PROMPT-codex.md instead.

You are a Phase 2 cluster auditor in a map-reduce security audit. Working directory is the repo root (a git worktree of origin/dev). READ FIRST, in this order: `audit/security/2026-09-13-high-prerelease/raw/CONTEXT.md`, then `audit/security/2026-09-13-high-prerelease/raw/SECURITY-PROMPT.md` (the exact audit prompt + negative list + output format — follow it exactly), then the repo-map files named below for orientation. Then read the cluster source files IN FULL (not excerpts). Verify every claim against the code; cite file:line. Do not modify any file except your output file.

# Cluster c09-pxe-node-network — PXE host in the offscreen document, node/RPC trust, network config, notes, incoming transfers, price feed

Repo maps: `raw/repo-map/aztec-runtime.md`, `raw/repo-map/extension-services-dapp-exec.md` (§3e, §3f).

Source files (read all, in full):
- `packages/aztec-runtime/src/pxe/*.ts` (all non-test), `packages/aztec-runtime/src/adapters/*.ts`, `packages/aztec-runtime/src/ports/*.ts`, `packages/aztec-runtime/src/utils/*.ts`
- `apps/extension/src/wallet/services/network/{service.ts, spec.ts, *.ts}`, `apps/extension/src/wallet/services/pxe/*.ts`, `apps/extension/src/wallet/services/note/*.ts`, `apps/extension/src/wallet/services/incoming-transfer/*.ts` (service, repository, public-event-indexer, spec), `apps/extension/src/wallet/services/account-state/{service.ts, normalize.ts}`, `apps/extension/src/wallet/services/price/*.ts`, `apps/extension/src/wallet/runtime.ts` (store-key provider registration ~line 540)
- Tests: `public-events.test.ts`, `opfs-store*.test.ts`, `incarnation-fence.test.ts`, `network/*.test.ts` (skim URL allowlist cases).

Specific questions:
1. Node-response trust: for each node RPC consumed (`getNodeInfo`, `getContract`, `getPublicLogsByTags`, `getBlock*`, `getCurrentMinFees`, membership witnesses, `getL1ContractAddresses`) — what does the wallet DO with the response and what can a hostile/compromised RPC (the seeded dRPC URL with an embedded API key is a shared third party) achieve: wrong chain identity (collision), fake contract instances (class-id verification — is `verifyArtifactClassId` on the path for node-sourced instances, or only dApp-sourced?), fake incoming transfers (trust prompts → user action?), fake balances, fee inflation, store wipes (documented: refuse-not-wipe), stalls/availability loss.
2. `isAllowedRpcUrl` + `RpcUrlSchema`: exact allowlists; IPv6/loopback spellings (`[::1]`, `0.0.0.0`, `127.1`, `localhost.`), `https://user@host` rejection, IDN hosts, port ranges; the local-network carve-out that hardcodes chainId 0 — can a custom network masquerade as local (URL match) to skip identity probing?
3. Seeded network rows: `assertCanonicalStoredL1`/`SEED_L1_BY_KIND` — is the composite chainId ALSO pinned for seeded networks, or only `l1ChainId`? Can a tampered/restored Network row for "mainnet" point at a hostile endpoint while keeping the pinned identity (F-011 vs restore path)?
4. PXE OPFS store: per-(profile,chain) directory naming (`chain-coordinates.ts` codec — injection via profileId/chainId strings?), store-key provisioning lifecycle (generation/incarnation fences, what happens to a store whose profile was deleted, orphan sweep deleting the WRONG profile's store), legacy IndexedDB sweep scope, and whether PXE data for profile A is reachable from profile B's session in the same offscreen document (the offscreen doc is shared across profiles!).
5. PXE RPC surface (25 methods): which are reachable from a dApp via the dispatcher and which only from the SW; `registerContractClass` denied at scope — is it also unreachable via any other path; `clearChainState`/`clearProfileState`/`provisionChainStoreKey` reachable only from the SW (sender check on the offscreen channel — see c05, but state your own conclusion)?
6. Public events indexer + incoming transfers: untrusted page ordering, dust thresholds, trust prompts — can a untrusted sender force prompts/notifications, plant a fake "received" row, or pin the sync watermark; are amounts/decimals from untrusted party tokens sanitised before display?
7. Notes: `getNotes`/`getNotesRaw` decode — untrusted party-crafted notes causing decode crashes or misdisplay; cross-account note visibility.
8. Price feed: CoinGecko over HTTPS, cache in chrome.storage.local re-validated on read; sanity bands; can a tampered quote change a security decision (fee display only? any threshold logic like dust in USD?).
9. `fetch.ts` timeouts/retries — amplification or hang; accelerator prover endpoint (`127.0.0.1:59833` plain HTTP, no auth — per shell map): what does the wallet send it (witness? private inputs?) and what does it trust back (a proof — verified before broadcast?). A local unprivileged process listening on that port: what can it learn or do?

Write your report to `audit/security/2026-09-13-high-prerelease/raw/c09-pxe-node-network-claude.md`.
