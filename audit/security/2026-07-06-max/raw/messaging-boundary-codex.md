CLUSTER: messaging-boundary
## Findings
_No findings meeting the bar._

## Notes
- Checked dynamic RPC dispatch: `BaseService.handleRequest` rejects non-safe `requestId`s and methods outside `rpcMethods`/framework RPCs before `invoke` uses `this[method]`, so prototype/framework method dispatch is gated (`packages/extension-messaging/src/core/base-service.ts:81`, `packages/extension-messaging/src/core/base-service.ts:90`, `packages/extension-messaging/src/core/base-service.ts:124`, `packages/extension-messaging/src/core/rpc-methods.ts:23`).
- Checked wire-shape handling: request envelopes are type/content gated in both transports, params must be object-like, sparse params are bounded by `MAX_RPC_ARITY`, and malformed JSON fallback responses reject instead of hanging (`packages/extension-messaging/src/background/service.ts:58`, `packages/extension-messaging/src/offscreen/service.ts:36`, `packages/extension-messaging/src/core/base-service.ts:98`, `packages/extension-messaging/src/utils.ts:1`, `packages/extension-messaging/src/core/base-client.ts:178`).
- Checked error reconstruction: services only attach structured `errorPayload` for local `WalletError` instances, and clients reconstruct typed errors from that payload with flat `Error` fallback (`packages/extension-messaging/src/core/error-response.ts:21`, `packages/extension-messaging/src/background/client.ts:134`, `packages/extension-messaging/src/offscreen/client.ts:113`, `packages/extension-messaging/src/errors.ts:220`).
- Checked sender/origin trust. The Port service does not inspect `Port.sender`, but the manifest has no `externally_connectable` exposure and the repo’s dApp content-script wrapper uses the wallet SDK relay rather than `runtime.connect`, so I did not find a concrete page-to-Port exploit path (`packages/extension-messaging/src/background/service.ts:36`, `apps/extension/manifest/manifest.config.ts:31`, `apps/extension/src/content-script/content.ts:11`).
- Checked offscreen messaging. The offscreen service trusts any message addressed to its service name and does not read `MessageSender`, but the reachable repo handoff from page to content script does not expose a repo-level path for a dApp to emit arbitrary top-level `{to, from, type: Request}` runtime messages; PXE handlers also parse security-sensitive Aztec types before use (`packages/extension-messaging/src/offscreen/service.ts:36`, `packages/extension-messaging/src/offscreen/client.ts:103`, `apps/extension/src/content-script/content.ts:11`, `packages/aztec-runtime/src/pxe/service.ts:300`, `packages/aztec-runtime/src/pxe/service.ts:460`).